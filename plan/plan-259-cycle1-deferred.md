# Plan 259 — Cycle 1 deferred review findings

Status: active-deferred

## Repo-policy basis for deferral

Read order followed before deferral: `CLAUDE.md`, `AGENTS.md`, relevant `.context/**` planning/deferred artifacts and current reviews, `.cursorrules` (absent), `CONTRIBUTING.md` (absent), and `docs/` policy files (absent).

Repo rules preserved:

- Commits for any future work remain bound by `AGENTS.md:3-4` and `CLAUDE.md:227-228` (commit, push, gitmoji; prompt also requires signed commits for implementation work).
- Node/toolchain work remains bound by `CLAUDE.md:217` (Node.js 24+, TypeScript 6.0+).
- The only repo-wide permanent deferral in `CLAUDE.md:212-214` is 2FA/WebAuthn; no item below is marked permanently deferred.
- For cross-process singleton findings, this repo explicitly states the shipped topology is single web-instance/single-writer and says not to horizontally scale until coordination state is moved to shared storage: `CLAUDE.md:160`, `README.md:146`. That quote is the specific repo rule permitting deferral of those horizontal-scaling correctness risks.

Security, correctness, and data-loss items from the aggregate are scheduled in `plan/plan-258-cycle1-review-fixes.md` unless the single-writer rule above explicitly covers the risk.

## Deferred items

| Finding | Original severity / confidence | Citation | Concrete reason for deferral | Exit criterion |
| --- | --- | --- | --- | --- |
| **AGG-C1-07 — Image queue shares CPU/memory with SSR/API** | High / High | `apps/web/src/instrumentation.ts:1-6`; `apps/web/src/lib/image-queue.ts:138-145`; `apps/web/src/lib/process-image.ts:17-26,537-542` | Performance/scaling architecture change; moving processing to a worker or adding metrics is larger than this cycle's security/correctness fix lane. | Re-open when upload bursts cause web latency, when queue concurrency is raised, or when a worker/container topology is prioritized. |
| **AGG-C1-08 — Uncached public listing pages run expensive grouped/window-count queries** | High / High | `apps/web/src/app/[locale]/(public)/page.tsx:14-16,138-140`; `apps/web/src/lib/data.ts:653-668` | Performance-only query-shape redesign; no data loss/security issue, and prior repo plans defer exact-count/listing hot paths at personal-gallery scale. | Re-open when gallery size/crawler load makes home/topic TTFB or DB CPU unacceptable. |
| **AGG-C1-14 — Process-local restore/maintenance state under horizontal scaling** | Medium / High | `CLAUDE.md:160`; `README.md:146`; `apps/web/src/lib/restore-maintenance.ts:1-22`; `apps/web/src/app/[locale]/admin/db-actions.ts:310-343` | Deferrable only because repo rules explicitly state single web-instance/single-writer topology and forbid horizontal scaling until coordination state is shared (`CLAUDE.md:160`, `README.md:146`). | Re-open before any multi-replica/rolling deployment, or when shared DB/advisory maintenance state is introduced. |
| **AGG-C1-15 — Startup migrations lack cross-process lock** | Medium / Medium | `CLAUDE.md:160`; `README.md:146`; `apps/web/Dockerfile:90-94`; `apps/web/scripts/migrate.js:525-542` | Cross-process correctness risk is covered by the same explicit single-writer deployment rule; current shipped topology should not run concurrent web replicas. | Re-open before rolling/multi-replica deploys or when migrations are externalized. |
| **AGG-C1-17 — Auth/API layering imports Server Action modules as services** | Medium / High | `apps/web/src/lib/api-auth.ts:1-5`; `apps/web/src/app/actions/auth.ts:1-55`; `apps/web/src/app/actions.ts:1-30` | Architectural refactor with no current failing behavior; touching auth layering broadly would increase risk in this cycle. | Re-open when adding background jobs/API auth surfaces or when auth module refactor is already planned. |
| **AGG-C1-18 — Schema/table contracts duplicated across schema, restore scanner, migrator** | Medium / High | `apps/web/src/db/schema.ts:4-145`; `apps/web/src/lib/sql-restore-scan.ts:2-21`; `apps/web/scripts/migrate.js:247-464` | Maintainability risk; a generated table manifest spans restore/migration tooling and deserves a focused schema-tooling change. | Re-open when adding a new table or when a restore scanner/migration drift bug occurs. |
| **AGG-C1-19 — Locale identifiers hard-coded as `[a-z]{2}` in proxy/nginx** | Medium / High | `apps/web/src/lib/constants.ts:1-4`; `apps/web/src/proxy.ts:91-115`; `apps/web/nginx/default.conf:57-60,74-77,91-94,107-110,146-149` | Future-locale risk; current supported locales (`en`, `ko`) satisfy the two-letter contract. | Re-open before adding any locale outside `[a-z]{2}` or before generating nginx config from app constants. |
| **AGG-C1-20 — Search leading-wildcard LIKE scans** | Medium / High | `apps/web/src/lib/data.ts:1094-1201`; `apps/web/src/components/search.tsx:63-100` | Performance/search backend redesign; no current security/correctness bug after rate limits. | Re-open when search latency/DB CPU becomes user-visible or a full-text index is added. |
| **AGG-C1-21 — Upload tag persistence repeats DB lookups** | Medium / High | `apps/web/src/app/actions/images.ts:265-385`; `apps/web/src/lib/tag-records.ts:29-68` | Performance optimization for large tagged batches; correctness is preserved. | Re-open when tagged batch upload latency is problematic or per-file tag support is expanded. |
| **AGG-C1-22 — Upload/settings contract lock held through slow work** | Medium / Medium | `apps/web/src/app/actions/images.ts:172-180,265-455`; `apps/web/src/lib/process-image.ts:347-391` | Concurrency/performance redesign; current lock intentionally preserves upload-processing contract correctness. | Re-open when lock waits are observed or when config snapshot/versioning is designed. |
| **AGG-C1-23 — Batch delete scans derivative directories repeatedly** | Medium / High | `apps/web/src/app/actions/images.ts:650-670`; `apps/web/src/lib/process-image.ts:181-213` | Performance-only optimization; deletes still complete correctly. | Re-open when bulk deletes on large galleries become slow or storage backend refactor occurs. |
| **AGG-C1-24 — Infinite scroll retains all cards** | Medium / High | `apps/web/src/components/home-client.tsx:105-110,179-180,274-282`; `apps/web/src/components/load-more.tsx:97-107` | UI performance/virtualization feature; correctness unaffected and masonry virtualization needs product/design testing. | Re-open when galleries exceed mobile memory limits or when pagination/virtualization is prioritized. |
| **AGG-C1-25 — CSV export materializes large data** | Medium / High | `apps/web/src/app/[locale]/admin/db-actions.ts:36-116`; `apps/web/src/app/[locale]/admin/(protected)/db/page.tsx:94-107` | Performance/memory issue already capped; streaming route is a larger API/UX change. | Re-open when galleries exceed 30k images, CSV export freezes, or a streaming export endpoint is planned. |
| **AGG-C1-28 — Critical JS scripts only syntax-checked** | Medium / High | `apps/web/package.json:15-25`; `apps/web/scripts/check-js-scripts.mjs:38-40`; `apps/web/scripts/migrate.js:1-10` | Tooling maintainability improvement; no current gate failure. | Re-open when converting scripts to TS/checkJs or after a JS script semantic bug. |
| **AGG-C1-29 — CI init/build duplication around E2E** | Medium / High | `.github/workflows/quality.yml:65-79`; `apps/web/scripts/run-e2e-server.mjs:75-84` | CI performance/ergonomics; not a correctness/security failure. | Re-open when CI approaches timeout or when e2e server split scripts are introduced. |
| **AGG-C1-32 — Password-change behavioral coverage gap** | Medium / High | `apps/web/e2e/admin.spec.ts:36-38`; `apps/web/src/__tests__/auth-rate-limit-ordering.test.ts:20-103`; `apps/web/src/app/actions/auth.ts:281-425` | Test coverage expansion; implementation path is already covered by source invariants and existing gates, but needs a focused mocked-action suite. | Re-open on password-action changes or auth regression. |
| **AGG-C1-33 — Settings persistence coverage gap** | Medium / High | `apps/web/e2e/admin.spec.ts:45-63`; `apps/web/src/app/actions/settings.ts:40-166`; `settings-client.tsx:34-87` | Test coverage expansion beyond this cycle's code fixes. | Re-open on settings-action/UI changes or save/reload regression. |
| **AGG-C1-34 — Source-text tests for critical invariants** | Medium / High | `apps/web/src/__tests__/auth-rate-limit-ordering.test.ts`; `resolved-stream-source.test.ts`; `data-view-count-flush.test.ts`; `settings-image-sizes-lock.test.ts` | Broad testing strategy migration; current source-contract tests are intentional guardrails. | Re-open when an invariant test is touched or when adding AST/behavioral test infrastructure. |
| **AGG-C1-35 — Visual E2E screenshots are not assertions** | Medium / High | `apps/web/e2e/nav-visual-check.spec.ts:14,27,39`; `apps/web/playwright.config.ts:58-60` | Visual baseline workflow needs stable masks/baselines and review policy. | Re-open when visual regression testing is adopted or nav layout changes. |
| **AGG-C1-37 — Local E2E cannot list/run public smokes without DB fixtures** | Medium / High | `apps/web/playwright.config.ts:71-78`; `apps/web/scripts/run-e2e-server.mjs:75-78`; `apps/web/package.json:20` | Developer ergonomics; CI has DB env and current e2e gate remains authoritative. | Re-open when onboarding friction or local e2e failures block contributors. |
| **AGG-C1-39 — Route error boundary lacks public IA shell** | Medium / High | `apps/web/src/app/[locale]/error.tsx:16-37`; `apps/web/src/app/[locale]/not-found.tsx:19-52` | UX resilience improvement with app-shell/data dependency decisions; not security/data-loss. | Re-open when public error-shell redesign is prioritized or DB outage UX is targeted. |
| **AGG-C1-43 — Admin validation toast-only / not field-associated** | Medium / High | `topic-manager.tsx:59-94,172-190,259-279`; `seo-client.tsx:39-68,95-174`; `settings-client.tsx:35-68,100-155`; `upload-dropzone.tsx:145-176` | Broad form/action API redesign; current toasts preserve functional validation. | Re-open when structured field-error returns are added or accessibility audit targets admin forms. |
| **AGG-C1-50 — Home metadata extra listing query for OG image** | Low / Medium | `apps/web/src/app/[locale]/(public)/page.tsx:78-80,138-140`; `apps/web/src/lib/data.ts:589-615` | Low performance optimization; no correctness/security issue. | Re-open when home TTFB DB traces show duplicate listing work is significant. |
| **AGG-C1-52 — Default health/build can mask DB misconfiguration** | Low / Medium | `apps/web/src/db/index.ts:13-25`; `apps/web/src/app/sitemap.ts:24-46`; `apps/web/src/app/api/health/route.ts:18-25`; `apps/web/scripts/ensure-site-config.mjs:6-42` | Operational signal tradeoff; repo docs intentionally say `/api/live` is liveness-only and DB readiness is opt-in. | Re-open when production readiness probes are redesigned or DB-less build mode changes. |
| **AGG-C1-56 — Limited cross-browser/mobile E2E and no CI artifact upload** | Low / Medium | `apps/web/playwright.config.ts:58-70`; `.github/workflows/quality.yml:71-79` | CI coverage/diagnostics improvement; not a direct product bug. | Re-open when adding WebKit/mobile coverage or chasing flaky Playwright failures. |
| **AGG-C1-57 — Native login required validation not app-localized** | Low / Medium | `apps/web/src/app/[locale]/admin/login-form.tsx:51-80` | UX preference; native validation is acceptable browser-localized behavior for now. | Re-open if product requires app-localized login validation or browser messages conflict with locale policy. |
| **AGG-C1-59 — RTL hard-coded out** | Low / High | `apps/web/src/app/[locale]/layout.tsx:89-98` | Future-locale risk only; current locales `en` and `ko` are LTR. | Re-open before adding Arabic/Hebrew/RTL locale. |
| **AGG-C1-62 — Misc maintainability/dependency hygiene** | Low / Medium | `apps/web/vitest.config.ts:10-12`; `.github/dependabot.yml:3-12`; `apps/web/src/lib/data.ts:227-307`; `apps/web/src/lib/upload-limits.ts:1-6`; `apps/web/src/lib/process-image.ts:46`; `apps/web/nginx/default.conf:72-93` | Mixed low-priority hygiene bundle; broadening Vitest discovery/checking selectors/limits needs separate rollout to avoid surprise test/environment changes. | Re-open when test placement conventions, Dependabot workspace coverage, select-field helper cleanup, or limit generation are explicitly prioritized. |

## Disposition completeness check

Scheduled in Plan 258: AGG-C1-01, 02, 03, 04, 05, 06, 09, 10, 11, 12, 13, 16, 26, 27, 30, 31, 36, 38, 40, 41, 42, 44, 45, 46, 47, 48, 49, 51, 53, 54, 55, 58, 60, 61.

Deferred here: AGG-C1-07, 08, 14, 15, 17, 18, 19, 20, 21, 22, 23, 24, 25, 28, 29, 32, 33, 34, 35, 37, 39, 43, 50, 52, 56, 57, 59, 62.
