# Plan 339 — Run-9 Cycle-2 fixes (cycle 5/100; MED + scheduled LOW)

**Source:** `.context/reviews/_aggregate.md` (run-9 cycle-2 fan-out, 11 agents, no failures). HEAD at planning time: `1dde9b1e`.
**Commit discipline (CLAUDE.md / global CLAUDE.md):** GPG-signed (`git commit -S`), Conventional Commits + gitmoji, ONE commit per item, `git pull --rebase` then push after EACH commit, full gate run before cycle close. NO `Co-Authored-By`. No `--no-verify`, no force-push. Run `npm run typecheck --workspace=apps/web` before committing any test change.

**Context:** The prior run-9 cycle-1 batch (AGG-C4-01..07) landed clean — all 6 code fixes RE-VERIFIED CLOSED at HEAD by all 11 agents (line-by-line + regex-in-Node, not on the plan's word). This cycle's open set is the *adjacent gaps* those fixes left (the production-path backfill cleanup has no test while its in-app twin does; the `<select>` touch-target patterns missed the `max-` lookbehind the Button patterns got) plus one public-surface a11y touch-target finding and two cheap test-gate pins. No CRITICAL/HIGH-severity-HIGH-confidence defect; no plan-table hygiene needed. Total: 2 MED + 4 LOW scheduled.

**GATES this run:** eslint, typecheck, vitest, lint:api-auth, lint:action-origin, lint:public-route-rate-limit. All must be GREEN before cycle close. DEPLOY_MODE = per-cycle (`npm run deploy` once after green).

---

## Item 1 — AGG-C5-01: sidecar `flushBatch` orphan-cleanup has ZERO test coverage (MED · test-engineer + tracer · highest cross-agent signal)

- **Source:** TE-1 (test-engineer, High) + TRC-C5-01 (tracer, High — same root, independent). The two-agent agreement makes this the highest-signal item this cycle.
- **Where:** `apps/web/scripts/backfill-color-pipeline.ts:337` — `flushBatch` is a NESTED inner function (NOT exported). The `affectedRows===0` orphan-cleanup that commit `300009d4` added to BOTH UPDATE branches of `flushBatch` (the PRODUCTION re-encode path — the prod container lacks `tsx`, so the `--rm` sidecar IS how prod re-encodes per CLAUDE.md) is unpinned. The sidecar test `apps/web/src/__tests__/backfill-color-pipeline.test.ts:20` imports only `reprocessRow` + `ImageRow`; the AGG-02 column-set test asserts the returned `signals` shape, not the UPDATE SQL or its `affectedRows` cleanup. None of the 10 backfill test files exercise `flushBatch`.
- **Why it matters:** this is the exact asymmetric twin of the prior cycle's AGG-C4-05 — which CLOSED the *in-app runner's* equivalent gap with a dedicated proven-RED test (`admin-backfill-runner-deleted-mid-reencode-detection-failure.test.ts`). The production sibling was left CODE-ONLY (commit stat for `300009d4`: script + CLAUDE.md + plan, no test). A refactor dropping the sidecar's `affectedRows===0` guard, the `[]` dir-scan arg, or the post-commit cleanup call leaks orphaned derivative files on every prod backfill with a GREEN suite. The cleanup MECHANISM is correct (tracer proved it across all 4 delete/re-encode interleavings — `processImageFormats` fully materializes files before the row enters the batch, so cleanup always fires with a `sizes=[]` dir-scan; deferring the unlink past commit is REQUIRED for batch-tx integrity, not a window) — so the gap is purely test depth, but on a freshly-landed correctness guard on the PRODUCTION path.
- **Change:**
  1. Make `flushBatch` (or a thin, test-only seam exposing its `affectedRows===0 → cleanup` logic) reachable from a test. Cheapest robust option: export `flushBatch` from `scripts/backfill-color-pipeline.ts` (it currently closes over `batch`/`tx` locals — if extracting is too invasive, instead export a small pure helper `cleanupDeletedMidReencode(rows)` that the test can call, plus assert via a mock `tx.execute` returning `affectedRows:0` that the helper is invoked; match whatever seam the in-app runner test uses for symmetry). Prefer the smallest change that lets a test drive the `affectedRows===0` branch.
  2. Add a test (clone the in-app runner's `admin-backfill-runner-deleted-mid-reencode-detection-failure.test.ts` style) that: mocks the per-row UPDATE to return `affectedRows:0` (row deleted mid-reencode), then asserts (a) `deleteImageVariants` (or the cleanup helper) fires for webp/avif/jpeg with `[]` sizes (full dir scan), (b) the `deletedMidReencode` tally increments and is excluded from the encode-failure count, (c) `pipeline_version` is NOT bumped for that row.
  3. Prove the test is NON-VACUOUS: temporarily drop the `affectedRows===0` guard (or the `[]` arg) in the sidecar, observe RED, restore identical. Document the RED-on-revert proof in the commit body (the repo's established practice — see the prior cycle's AGG-C4-05 commit `2251b122`).
- **Acceptance:** the new test pins the sidecar's production-path orphan-cleanup; proven RED if the guard is removed; `npm test --workspace=apps/web` green; `npm run typecheck --workspace=apps/web` green (the script is covered by `typecheck:scripts`, and the test by `typecheck:app`). Run typecheck before committing.

## Item 2 — AGG-C5-02: `<select>` touch-target patterns missed the `max-` lookbehind the Button patterns got (LOW · critic · the adjacent test-gate gap)

- **Source:** NF-1 (critic, High; ORCHESTRATOR-VERIFIED empirically in Node).
- **Where:** `apps/web/src/__tests__/touch-target-audit.test.ts:409,413,417,421` — the four native-`<select>` FORBIDDEN patterns (R4C16 DES-R4C16-04). Each matches `\b(?:h-8|h-9|h-10)\b` with only a negative lookahead for `h-1[12]`/`min-h-1[12]`, and does NOT carry the `(?<!max-)` lookbehind that commit `40a65aef` added to the `<Button>`/`<button>` branches (and the scale-token catch-all) to fix AGG-C4-01.
- **Why it matters:** VERIFIED (critic ran the exact committed regex in Node): `<select className="max-h-10">` FLAGS as "renders below the 44 px floor" — FALSE, because `max-height` is a CEILING and never constrains the tap target. It is the very bug class `40a65aef` closed, re-introduced one tag-name over — the recurring "fix one sibling, miss the others" theme. Green TODAY only because no current `<select>` uses `max-h-{8,9,10}`; the moment a legitimate one is added, the blocking gate fires with a lying message, training devs to silence it with a bogus `min-h-11`. The does-not-flag self-check block (`:971-979`) also lacks `<select>` `max-` negative fixtures.
- **Change:**
  1. Add `(?<!max-)` to the bare-`h`/`w` (here `h-8|h-9|h-10`) portion of all four `<select>` patterns at `:409,413,417,421`, mirroring exactly how `40a65aef` anchored the Button/button patterns. Confirm `min-h-[<44px]` patterns are unaffected (they're true floors). Re-read the Button patterns at HEAD to copy the precise lookbehind form used there.
  2. Add `<select className="max-h-10">` and `<select className="max-w-9">` (and a `cn()` composite form) negative fixtures to the does-not-flag self-check block so a future regex edit can't silently re-introduce the false positive — symmetric with the 9 negative Button fixtures `40a65aef` added.
- **Acceptance:** the four `<select>` patterns no longer match `max-h-{8,9,10}`/`max-w-…` but still match real sub-44 `<select>` tokens (`h-8`, `min-h-[40px]`); the new self-check fixtures pass; the full `touch-target-audit.test.ts` still passes (no current file regresses); `npm test --workspace=apps/web` green. Run `npm run typecheck` before committing the test change.

## Item 3 — AGG-C5-03: three public inline `<Link>` recovery actions below the 44 px floor (MED a11y · designer)

- **Source:** DES-C5-1 (designer, High; className verbatim + line-box math + audit-gap confirmed at `:424-466`).
- **Where (all PUBLIC surfaces):**
  - (a) `apps/web/src/components/topic-empty-state.tsx:18` — `className="underline hover:text-primary"` (~24 px line-box, no height token).
  - (b) `apps/web/src/components/home-client.tsx:434` — `className="text-sm underline hover:text-primary"` (~20 px; highest-traffic public route).
  - (c) `apps/web/src/app/[locale]/(public)/timeline/page.tsx:152` — `className="text-sm text-muted-foreground hover:text-primary transition-colors underline underline-offset-4"` (~20 px; year-in-review).
- **Why it matters:** each renders at its line-box height (no `h-*`/`min-h-*`/`py-*` token), below the repo's documented **44 px floor** (WCAG 2.5.5 Target Size (Enhanced), AAA — which the repo explicitly claims to enforce repo-wide) and, for (b)/(c) at ~20 px, below the **WCAG 2.5.8 Target Size (Minimum), AA = 24 px** floor. Each is the SOLE recovery action in an empty/filtered state on a touch-first public route — a 20 px target is a documented fat-finger miss risk, exactly the failure mode the 44 px gate exists to prevent. These ship green because they sit in the touch-target audit's DELIBERATE bare-link gap: the `<Link>`/`<a>` patterns at `:424-466` only fire when the link carries an EXPLICIT sub-44 sizing token (the committed comment at `:430-432` states verbatim "plain text links never trip"), so a link with no height token at all is out of scope by design. `KNOWN_VIOLATIONS` correctly lists neither file (bare links are out of audit scope) — the finding is that three REAL recovery links sit in that intentional gap.
- **Change:** add `inline-flex items-center min-h-11 px-2` to the three links (matching the `min-h-[44px]` pattern footer/on-this-day links already use), so each presents a >=44 px tap target while keeping the visible underline affordance:
  - (a) `topic-empty-state.tsx:18` → `className="inline-flex items-center min-h-11 px-2 underline hover:text-primary"`.
  - (b) `home-client.tsx:434` → add `inline-flex items-center min-h-11 px-2` to the existing classes (keep `text-sm underline hover:text-primary`).
  - (c) `timeline/page.tsx:152` → add `inline-flex items-center min-h-11 px-2` to the existing classes.
  - Verify each renders correctly in its flex-column parent (the empty-state containers already use `flex flex-col items-center gap-*`, so an `inline-flex` child centers cleanly). The durable audit-hardening (a heuristic that flags bare interactive `<Link>`/`<a>` with no sizing token) is intentionally harder (it would false-positive every link inheriting >=44 px from a sized flex parent) and is DEFERRED in plan-340 — part 1 here resolves the live a11y defect.
- **Acceptance:** all three links present a >=44 px tap target (`min-h-11`); no visual regression (the underline affordance + hover state preserved); no hardcoded English introduced (strings still via `t()`). Because the audit cannot SEE these (bare-link gap), the verification is manual reasoning + reading the rendered classes; optionally add the three links to a small positive-assertion pin (DEFERRED to plan-340 part 2). lint + typecheck + full gate run green.

## Item 4 — AGG-C5-T1: no committed en/ko leaf-key parity test (LOW test gate · test-engineer)

- **Source:** TE-5 (test-engineer, High / absence-verified).
- **Where:** new file under `apps/web/src/__tests__/` (e.g. `i18n-key-parity.test.ts`). The "837 = 837, 0 drift" figure in the aggregate is an orchestrator-side MANUAL count — there is no committed gate. Existing i18n tests (`humanize-transfer-function-i18n.test.ts`, `color-pipeline-decision-i18n.test.ts`, the `cycle{4,5}-rpf-source-contracts.test.ts` files) pin only specific newly-added keys, never the full set; no `scripts/check-*` does it either.
- **Why it matters:** a hand-edit that accidentally drops a pre-existing key from `ko.json` (e.g. during a nested-namespace refactor) makes next-intl render the raw key string verbatim to Korean users at that surface, with NO test failing. The cycle source-contract tests catch intentionally-ADDED keys but not accidentally-DROPPED ones.
- **Change:** add one small test that imports `messages/en.json` + `messages/ko.json`, flattens each to its leaf-key set (recursive key-path flatten), and asserts `expect(enKeys.sort()).toEqual(koKeys.sort())`. **CRITICAL — assert on KEYS only, never values:** per CLAUDE.md DOC-R5C3-07 the en side uses ICU `plural` blocks and ko uses a single fixed form, so the VALUE shapes legitimately differ; a key-set equality is exactly the right gate and is what's missing. Match the existing flatten helper shape in `humanize-transfer-function-i18n.test.ts:20-36` if convenient.
- **Acceptance:** the new test passes at HEAD (837 = 837) and would FAIL if a key is added to one locale but not the other (prove by temporarily deleting a `ko.json` key in a scratch run → RED → restore). `npm test --workspace=apps/web` green; `npm run typecheck` green. Run typecheck before committing.

## Item 5 — AGG-C5-T2: upload-queue `[]`-sizes cleanup has no direct call-path pin (LOW test depth · test-engineer)

- **Source:** TE-6 (test-engineer, Medium; author-acknowledged residual in `18de78eb`'s message).
- **Where:** `apps/web/src/lib/image-queue.ts:384-386` — the `affectedRows===0` delete-race cleanup now passes `[]` as the 3rd `deleteImageVariants` arg (the AGG-C4-04 fix). The fix relies on the INDIRECT `process-image-variant-scan.test.ts` proving `deleteImageVariants(dir,fn,[])` dir-scans, but no test asserts the queue worker actually passes `[]` at the call site.
- **Why it matters:** a regression to the queue passing default sizes again would re-open the original non-default-size orphan leak with a green suite. LOW because admin-only + low-prob + disk-leak-only and the dir-scan contract IS solid; the queue-worker call path is genuinely harder to unit-isolate than the sidecar's batch fn.
- **Change:** add a cheap SOURCE-SHAPE pin (consistent with the existing blur-wiring call-site pin pattern in `process-image-blur-wiring.test.ts` / `images-action-blur-wiring.test.ts`): read the `image-queue.ts` source and assert the delete-race cleanup block matches `deleteImageVariants\([^,]+,\s*[^,]+,\s*\[\]\)` (the 3-arg `[]` form) AND `not.toMatch` the 2-arg form in that block. Bundle this with Item 1 if convenient (both are backfill/queue cleanup pins) or as its own commit.
- **Acceptance:** the source-shape pin passes at HEAD and would fail if the cleanup reverted to the 2-arg default-sizes form. `npm test --workspace=apps/web` green; typecheck green. Run typecheck before committing.

## Item 6 (OPTIONAL — may be taken this cycle or re-affirmed DEFER) — AGG-C4-09: recount stale `KNOWN_VIOLATIONS['components/image-manager.tsx']`

- **Source:** prior-deferred AGG-R8c3-15 / AGG-C4-09, re-confirmed open by critic + test-engineer (both re-measured: real count is **1**, budget is **6**; 5 of the 6 `size=` buttons now carry `h-11`, only `batchAddButton:328` trips).
- **Where:** `apps/web/src/__tests__/touch-target-audit.test.ts` — the `KNOWN_VIOLATIONS['components/image-manager.tsx']` entry (was `:182`; verify current line).
- **Why it's optional:** test-PRECISION nit, not a live defect; the aggregate budget masks up to ~5 NEW sub-44 targets in that one file before the gate fires, but the stale-budget detector is informational. Since Item 2 already edits this same test file, the implementer MAY opportunistically tighten 6→1 in that commit with a measurement-dated comment. If NOT taken, it remains DEFERRED on the existing rationale (plan-340 carries it forward).
- **Acceptance (if taken):** recount the real scanner value (zero the budget, read the audit output, restore), tighten the entry to the true count (1) with a comment noting the measurement date and the `batchAddButton:328` rationale; full audit + gate run green.

---

## Progress

- [x] Item 1 — AGG-C5-01 sidecar `flushBatch` orphan-cleanup test — DONE: extracted two module-level exports from the `main()` closure — `collectDeletedMidReencodeFiles(results)` (the `affectedRows===0` partition) + `cleanupDeletedMidReencodeVariants(files)` (the `[]` dir-scan unlink) — and rewired `flushBatch` to use them. New `backfill-color-pipeline-deleted-mid-reencode.test.ts` (4 tests) pins the partition (only `affectedRows===0` rows selected) + the cleanup (webp/avif/jpeg all unlinked with `[]` sizes). PROVEN NON-VACUOUS: breaking the filter (select-all) + the `[]` arg → 3/4 RED, restored → 4/4 GREEN. typecheck app+scripts green; sidecar test group 11/11.
- [x] Item 2 — AGG-C5-02 `<select>` `(?<!max-)` lookbehind + negative self-check fixtures — DONE: added `(?<!max-)` before the `(?:h-8|h-9|h-10)` group in both native-`<select>` `h-8/h-9/h-10` patterns (`:414,418`), mirroring the Button fix; `min-h-[<44px]` patterns left as true floors. Added 4 `<select> max-` negative fixtures to the does-not-flag block (`max-h-10`, `max-h-8`, `cn("max-h-10")`, `max-h-screen`). The existing positive block (real `<select> h-10`/`min-h-[40px]`) still flags, proving no over-neuter. PROVEN NON-VACUOUS: reverting the lookbehind → `max-h-10` fixture RED, restored → 12/12 green.
- [x] Item 3 — AGG-C5-03 three public inline `<Link>` recovery actions — DONE: added `inline-flex items-center min-h-11 px-2` to `topic-empty-state.tsx:18` (clear-filter), `home-client.tsx:434` (clear-filter), `timeline/page.tsx:152` (year-in-review) — each now presents a >=44 px tap target while keeping the underline affordance. ALSO took plan-340 Deferred-1's pragmatic middle-path partial close: added a positive-assertion `it` block to `touch-target-audit.test.ts` that reads the three source files and pins each recovery `<Link>` carries `min-h-11` (so a future drop is caught — the full bare-link heuristic stays deferred). PROVEN NON-VACUOUS: stripping `min-h-11` from topic-empty-state → pin RED, restored → 13/13 green. lint + typecheck green.
- [x] Item 4 — AGG-C5-T1 en/ko leaf-key SET-equality test — DONE: new `i18n-key-parity.test.ts` flattens both message objects and asserts `Object.keys` set-equality (KEYS only, never values — DOC-R5C3-07: en ICU-plural / ko fixed-form shapes differ legitimately) + a no-duplicate-keys sanity. Confirmed 837 = 837 at HEAD. PROVEN NON-VACUOUS: dropping `nav.dashboard` from ko.json → RED naming the exact missing key, restored → 2/2 green. typecheck green.
- [ ] Item 5 — AGG-C5-T2 upload-queue `[]`-sizes cleanup source-shape pin.
- [ ] Item 6 (optional) — AGG-C4-09 recount `image-manager.tsx` budget 6→1.

(Updated by the implementer during PROMPT 3.)
