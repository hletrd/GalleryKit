# Plan 11: R4 UX & Accessibility Fixes

**Priority:** P1 (m4/m8) to P2 (rest)  
**Estimated effort:** 3-4 hours  
**Sources:** UX Review R4 m1-m11 + missing items

---

## Quick wins (2-line each, P1)

### 1. Wire remaining admin aria-labels (m4)
**Files:**
- `src/components/image-manager.tsx:278` — `aria-label="Select all images"` → `aria-label={t('aria.selectAll')}`
- `src/components/upload-dropzone.tsx:253` — `aria-label="Remove file"` → `aria-label={t('aria.removeFile')}`
- Keys already exist in both en.json and ko.json

### 2. Translate search toggle hint (m8)
**Files:**
- `src/components/search.tsx:187` — `"to toggle search"` → `t('search.toggleHint')`
- `messages/en.json` — add `"toggleHint": "to toggle search"`
- `messages/ko.json` — add `"toggleHint": "검색 열기/닫기"`

### 3. Translate skip-to-content (m7)
**Files:**
- `src/app/[locale]/layout.tsx:80` — use `getTranslations('common')` then `t('skipToContent')`
- `src/app/[locale]/admin/(protected)/layout.tsx:16` — same
- Add `"skipToContent": "Skip to content"` / `"skipToContent": "본문으로 건너뛰기"` to both locales

## Medium effort (P2)

### 4. Admin nav locale prefix (m3)
**File:** `src/components/admin-nav.tsx:13-18`
- Import `useLocale()` from `next-intl`
- Prefix each href: `` `/${locale}${link.href}` ``

### 5. Histogram canvas aria-label i18n (m5)
**File:** `src/components/histogram.tsx:244`
- Add `"histogramLabel": "Color histogram, {mode} mode"` / `"histogramLabel": "컬러 히스토그램, {mode} 모드"` to both locales
- Replace hardcoded template literal with `t('aria.histogramLabel', { mode: MODE_LABELS[mode] })`

### 6. Error boundary i18n (m6)
**Files:**
- `src/app/[locale]/error.tsx` — import `useTranslations`, use `error` namespace
- `src/app/[locale]/admin/(protected)/error.tsx` — same
- Add `error` namespace to both locale files: `"title": "Error" / "오류"`, `"description"`, `"tryAgain"`, `"backToGallery"`, `"backToDashboard"`
- `global-error.tsx` stays hardcoded (no i18n context)

### 7. Photo nav sr-only dynamic announcement (m1)
**File:** `src/components/photo-navigation.tsx:202-204`
- Replace static English with `t('aria.photoNavStatus', { hasPrev: !!prevId, hasNext: !!nextId })`
- Add i18n keys to both locales

### 8. Topic label in masonry overlay (m2)
**Files:**
- `src/app/[locale]/page.tsx` — pass `topics` array as prop to HomeClient
- `src/components/home-client.tsx` — accept `topics` prop, build `topicsMap`, use `topicsMap[image.topic] ?? image.topic` at line 266

### 9. FocusTrap only when expanded (m11)
**File:** `src/components/info-bottom-sheet.tsx:136`
- Change `active={isOpen}` to `active={isOpen && sheetState === 'expanded'}`

### 10. Focus ring on masonry cards (m10)
**File:** `src/components/home-client.tsx:201`
- Add `focus-within:ring-2 focus-within:ring-primary focus-within:ring-offset-2` to card className

### 11. Back-to-top scroll listener → useEffect (m9)
**File:** `src/components/home-client.tsx:302-318`
- Move scroll listener from ref callback to useEffect
- Use `showBackToTop` state variable instead of CSS class on `<html>`

## Missing items (P3)

### 12. Loading spinner role + aria-label
- `src/app/[locale]/loading.tsx` — add `role="status"` and `aria-label={...}` using common.loading key
- Same for admin loading.tsx

### 13. Nav aria-label for landmark disambiguation
- `src/components/nav-client.tsx` — add `aria-label={t('aria.mainNav')}`
- `src/components/admin-nav.tsx` — add `aria-label={t('aria.adminNav')}`

---

## Verification
- [ ] Korean admin screen readers hear "전체 선택" not "Select all images"
- [ ] /ko search panel shows "검색 열기/닫기" not "to toggle search"
- [ ] /ko skip link shows "본문으로 건너뛰기"
- [ ] Error boundary on /ko shows Korean error messages
- [ ] Topic label shows "TWS" not "tws" in masonry overlay
- [ ] Focus ring visible on masonry cards during keyboard navigation
- [ ] Build + lint pass
