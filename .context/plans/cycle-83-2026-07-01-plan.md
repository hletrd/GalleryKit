# Cycle 83/100 Implementation Plan

Start HEAD: `cc46b1d69c11cb175c88df69f17cbe526d23aa0d`.
Review aggregate: `.context/reviews/cycle-83-2026-07-01/_aggregate.md`.

## Goal

Close the safe Cycle 83 findings, keep review/plan ledgers current, strengthen the Cycle 82 result-label regression contracts, run all required gates, commit/push all work, and deploy with `npm run deploy` from the repo root.

## Scheduled Fixes

1. `C83-01`: close Cycle 82's release ledger by marking commit/push complete with signed `cc46b1d6` / `origin/master` evidence, recording that Cycle 83 starts from deployed `cc46b1d6`, and moving Cycle 82 out of the active plan index.
2. `C83-02`: strengthen focused source-contract tests so search and similar-photo result labels must flow from normalized `label` values into visible text, props, `title`, `aria-label`, and `alt` output.
3. `C83-03`: keep Cycle 83 review/plan artifacts trackable by extending the existing `.gitignore` whitelist for the new plan and deferred files.

## Deferred This Cycle

No new Cycle 83 findings are deferred.

Carry-forward deferred items remain in `.context/plans/cycle-83-2026-07-01-deferred.md`.

## Regression Coverage

- Search result labels: source-contract coverage must prove `SearchResultItem` renders `{label}` and does not restore raw `image.title || image.description` fallback.
- Similar-photo labels: source-contract coverage must prove the normalized `label` is passed to `SimilarThumb` and used for thumbnail `title`, `aria-label`, and image `alt`.
- Ledger: Cycle 82 must no longer be listed as active, and Cycle 82's terminal state must record signed commit/push/deployed-start evidence for `cc46b1d6`.

## Validation

Run required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then signed commit, `git pull --rebase`, push, and deploy with `npm run deploy`.

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run required gates.
- [x] Commit, pull --rebase, push.
- [x] Terminal release state recorded in Cycle 84: no committed Cycle 83 deploy transcript was found; Cycle 84 deploy supersedes production state after its pushed fix.

## Gate Evidence

- Focused regression (initial): `npm test --workspace=apps/web -- --run src/__tests__/photo-title.test.ts src/__tests__/search-disclaimer.test.ts src/__tests__/cycle-21-source-contracts.test.ts src/__tests__/failed-image-retry.test.ts` - failed because the new source-contract slice matched `interface SearchResultItemProps` instead of `interface SearchResult`, producing an empty body.
- Focused regression (rerun): `npm test --workspace=apps/web -- --run src/__tests__/photo-title.test.ts src/__tests__/search-disclaimer.test.ts src/__tests__/cycle-21-source-contracts.test.ts src/__tests__/failed-image-retry.test.ts` - pass: 4 files passed, 48 tests passed.
- `npm run lint --workspace=apps/web` - pass.
- `npm run lint:api-auth --workspace=apps/web` - pass: both admin API routes OK.
- `npm run lint:action-origin --workspace=apps/web` - pass: all mutating server actions enforce same-origin provenance.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
- `npm run typecheck --workspace=apps/web` - pass.
- `npm run build --workspace=apps/web` - pass. Local sitemap generation logged the expected unavailable local MySQL fallback and exited successfully.
- `npm test --workspace=apps/web` - pass: 301 files passed, 2 skipped; 2835 tests passed, 4 skipped.
- Commit/push evidence recorded during Cycle 84: signed HEAD `023ae28d41ee757caaa408710bd864d88087a40c` matched `origin/master`; `git log -1 --show-signature` reported a good signature from `Jiyong Youn <01@0101010101.com>`.
- Deploy evidence gap recorded during Cycle 84: no committed Cycle 83 `npm run deploy` transcript was present at review time. Cycle 84's required per-cycle deploy is scheduled to supersede production state after its pushed fix commit.

## Gate Fix Iterations

1. Fixed the new search source-contract slice to stop at `interface SearchResult {` instead of the earlier `interface SearchResultItemProps`.
