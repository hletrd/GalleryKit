# Run-10 Cycle 5/100 Implementation Plan

Date: 2026-07-07
Start HEAD: `591b44bdaa7fb51c2c0ff8aa12d9274563147561`
Review aggregate: `.context/reviews/_aggregate.md`

## Scope

This plan schedules the cycle-5 findings that are concrete, local, and fixable without an operator-only production action or a new product decision. Every finding from the aggregate is either scheduled below or recorded in `cycle-5-2026-07-07-deferred.md`.

Cycle-4 artifacts were not archived in this planning pass because `.context/plans/cycle-4-2026-07-07-plan.md` still records pending deploy/e2e evidence; that plan is not fully implemented/done under the repo's own evidence rules.

## Work Packages

### WP1 - Decouple maintenance scheduling from image queue

- Covers: C5-01, doc sync part of C5-18
- Files likely touched: `apps/web/src/lib/image-queue.ts`, `apps/web/src/lib/maintenance-scheduler.ts`, `apps/web/src/instrumentation.ts`, `apps/web/src/lib/queue-shutdown.ts`, tests, `CLAUDE.md`
- Plan:
  1. Extract session/rate-limit/audit/view retention startup purge and hourly cadence into an idempotent maintenance scheduler module.
  2. Start it from `instrumentation.ts` independently from `bootstrapImageProcessingQueue()`.
  3. Keep queue-local retry-map pruning in the image queue or expose a queue-owned callback without coupling site retention to queue bootstrap.
  4. Add a regression/source-contract test proving instrumentation starts the scheduler separately from the image queue.
  5. Update lifecycle docs to point retention ownership at the new module.
- Progress: implemented. Extracted `maintenance-scheduler.ts`, started it from `instrumentation.ts`, left queue-local retry pruning in `image-queue.ts`, updated CLAUDE.md, and added `maintenance-scheduler-source.test.ts`.

### WP2 - Enforce embedding bootstrap scan caps exactly

- Covers: C5-02
- Files likely touched: `apps/web/src/lib/image-queue.ts`, `apps/web/src/__tests__/image-queue-embedding-bootstrap-cap.test.ts`
- Plan:
  1. Compute remaining `SEMANTIC_SCAN_LIMIT` before each missing-embedding query.
  2. Query with `Math.min(BOOTSTRAP_EMBEDDING_RETRY_BATCH_SIZE, remaining)`.
  3. Add a non-multiple limit regression test, e.g. `scanLimit: 75`, proving the second query is limited to 25 and the cursor does not skip id ranges.
- Progress: implemented. The bootstrap now queries only the remaining scan budget, and `image-queue-embedding-bootstrap-cap.test.ts` pins the 75-row non-multiple case.

### WP3 - Page sidecar color backfill candidates

- Covers: C5-03
- Files likely touched: `apps/web/scripts/backfill-color-pipeline.ts`, tests or source-contract tests
- Plan:
  1. Replace the one-shot candidate fetch with a keyset `id > lastId ORDER BY id ASC LIMIT BATCH_SIZE` loop.
  2. Process and flush one fetched page at a time while preserving counters, force mode, detection-failure semantics, and deleted-mid-reencode cleanup.
  3. Add focused regression coverage that catches an unbounded one-shot `SELECT` returning all candidates.
- Progress: implemented. The sidecar now keyset-pages candidates by `id > lastCandidateId ORDER BY id ASC LIMIT BATCH_SIZE`, processes each page, and has source-contract coverage.

### WP4 - Add feed/sitemap updated-order indexes

- Covers: C5-04
- Files likely touched: `apps/web/src/db/schema.ts`, `apps/web/drizzle/NNNN_*.sql`, `apps/web/drizzle/meta/_journal.json`, `apps/web/scripts/migrate.js`, tests
- Plan:
  1. Add root and topic index shapes for processed/updated/created/id feed and sitemap ordering.
  2. Add a monotonic Drizzle journal entry and SQL migration.
  3. Mirror the indexes in `reconcileLegacySchema`.
  4. Add or update migration/reconcile coverage.
- Progress: implemented. Added `0029_feed_updated_indexes.sql`, the Drizzle schema index declarations, the journal entry, and `reconcileLegacySchema` index creation.

### WP5 - Bound background analytics writes

- Covers: C5-05
- Files likely touched: `apps/web/src/lib/background-db-writes.ts`, `apps/web/src/app/actions/public.ts`, tests
- Plan:
  1. Add a small bounded queue for anonymous analytics writes with low concurrency and max pending count.
  2. Preserve shutdown/drain behavior.
  3. Add tests for concurrency, overflow/drop behavior, and drain.
- Progress: implemented. Public analytics actions now use a bounded analytics write queue with low concurrency, max pending count, drain support, and regression coverage.

### WP6 - Lifetime-cover stale service-worker image revalidation

- Covers: C5-06
- Files likely touched: `apps/web/public/sw.template.js`, generated `apps/web/public/sw.js`, `apps/web/src/__tests__/sw-template-contract.test.ts`
- Plan:
  1. Wrap stale-path `startRevalidate()` in `extendLifetime(event, ...)`.
  2. Regenerate `sw.js`.
  3. Extend contract coverage so the generated worker cannot drift.
- Progress: implemented. Stale image revalidation now uses `extendLifetime(event, startRevalidate())`; `sw.js` was regenerated and the template/generated worker contract was extended.

### WP7 - Add executable coverage for LR upload and restore failure paths

- Covers: C5-12, C5-13
- Files likely touched: `apps/web/src/__tests__/lr-upload-*.test.ts`, `apps/web/src/__tests__/db-restore*.test.ts`, possibly small testability extraction in route/helper code
- Plan:
  1. Add a behavioral LR upload route test for at least one success path and one late rejection path, asserting tracker settlement, lock release, cleanup, insert/enqueue ordering, and response shape.
  2. Add focused restore failure-mode coverage for child-process timeout/nonzero/error cleanup. Prefer extracting a small helper only if the current code cannot be tested cleanly.
  3. Keep source-contract tests where useful, but ensure the new tests execute behavior rather than only matching text.
- Progress: implemented. Added LR upload late-HDR-rejection behavior coverage and restore child-process failure source-contract coverage. Focused tests passed; broader restore helper extraction remains unnecessary until a behavior seam exists.

### WP8 - Fix documentation/product copy and search shortcut copy

- Covers: C5-19, non-deferred part of C5-20, C5-21
- Files likely touched: `README.md`, `apps/web/README.md`, `apps/web/src/components/search.tsx`, tests
- Plan:
  1. Document that visited-image PWA caching applies to same-origin derivatives; cross-origin CDN derivatives are network-only unless proxied same-origin.
  2. Keep smart-collection authoring out of public/admin-operable docs; preserve the CLAUDE.md warning that no admin UI invokes the actions.
  3. Use neutral shortcut copy or robust client platform detection for search and add coverage for non-Mac copy.
- Progress: implemented. README/app README clarify same-origin-only SW image caching; smart-collection docs remain non-operable; search shortcut copy is platform-neutral and source-tested.

### WP9 - Dependency/CSP security disposition

- Covers: C5-10, C5-11
- Files likely touched: `package.json`/lockfile if a safe override works, `apps/web/src/lib/content-security-policy.ts`, docs/tests
- Plan:
  1. Test whether a safe npm override can move the vulnerable dev-only `esbuild` transitive dependency to a patched version without breaking `drizzle-kit` and gates.
  2. If no safe override is available, record the upstream-blocked state in the deferred register with current latest-version evidence and keep production runtime separation documented.
  3. Document the CSP inline-style tradeoff in code or docs if it cannot be removed cleanly this cycle.
- Progress: implemented/deferred split. `npm view` verified `drizzle-kit@0.31.10` and `esbuild@0.28.1` as current latest on 2026-07-07; `npm explain esbuild --workspace=apps/web` confirmed the remaining vulnerable `esbuild@0.18.20` is nested under `@esbuild-kit/core-utils@3.3.2` via current `drizzle-kit`. The upstream-transitive item stays in the deferred register; CSP inline-style tradeoff is documented and source-tested.

## Required Gates

Run all cycle gates after implementation:

- `npm run lint --workspace=apps/web`
- `npm run lint:api-auth --workspace=apps/web`
- `npm run lint:action-origin --workspace=apps/web`
- `npm run lint:public-route-rate-limit --workspace=apps/web`
- `npm run typecheck --workspace=apps/web`
- `BASE_URL=https://gallery.atik.kr npm run build --workspace=apps/web`
- `npm test --workspace=apps/web`
- `npm run test:e2e --workspace=apps/web` when browser-flow coverage is required

## Completion Ledger

- Prompt 1 aggregate written: complete.
- Prompt 2 plan/deferred written: complete after `cycle-5-2026-07-07-deferred.md` is present.
- Prompt 3 implementation: focused implementation complete.
- Focused tests: `npm test --workspace=apps/web -- --run src/__tests__/image-queue-embedding-bootstrap-cap.test.ts src/__tests__/maintenance-scheduler-source.test.ts src/__tests__/sw-template-contract.test.ts src/__tests__/backfill-color-pipeline-deleted-mid-reencode.test.ts src/__tests__/background-db-writes.test.ts src/__tests__/lr-upload-route-behavior.test.ts src/__tests__/db-restore.test.ts src/__tests__/content-security-policy.test.ts src/__tests__/search-status-source.test.ts` - pass, 9 files / 92 tests.
- Gates: green except optional local e2e environment blocker:
  - `npm run lint --workspace=apps/web` - pass.
  - `npm run lint:api-auth --workspace=apps/web` - pass.
  - `npm run lint:action-origin --workspace=apps/web` - pass.
  - `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
  - `npm run typecheck --workspace=apps/web` - pass.
  - `BASE_URL=https://gallery.atik.kr npm run build --workspace=apps/web` - pass after fixing the prebuild PWA icon generator to write through temp files; build still logs the existing local-DB-unavailable sitemap fallback and completes.
  - `npm test --workspace=apps/web` - pass, 336 files / 3126 tests; 2 files / 4 tests skipped by existing policy.
  - `npm run test:e2e --workspace=apps/web` - attempted because this cycle touched browser-facing search/PWA surfaces, but the Playwright webServer failed before serving the app: `scripts/migrate.js` could not connect to MySQL at `127.0.0.1:3306` (`ECONNREFUSED`). No local MySQL service is running; starting/removing a Docker MySQL container is intentionally not performed under the repo's destructive-action safety rule.
- Commit/push: pending.
- Deploy: pending.
