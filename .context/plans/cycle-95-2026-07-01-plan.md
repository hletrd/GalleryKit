# Cycle 95/100 Implementation Plan

Start HEAD: `750729ada2403c0c01267670b9552a05e0ead217`.
Review aggregate: `.context/reviews/cycle-95-2026-07-01/_aggregate.md`.

## Goal

Close the only newly confirmed Cycle 95 issue: stale Cycle 94 release-ledger state. Do not change application source because no new source defect was confirmed.

## Scheduled Fixes

1. `C95-01`: update `.context/plans/cycle-94-2026-07-01-plan.md`, `.context/plans/README.md`, and `.context/reviews/_aggregate.md` so committed artifacts record that Cycle 94 completed as signed/pushed/deployed HEAD `750729ada2403c0c01267670b9552a05e0ead217`, with production smoke evidence.

## Deferred This Cycle

All carry-forward findings are recorded in `.context/plans/cycle-95-2026-07-01-deferred.md`. No Cycle 95 confirmed finding is deferred.

## Validation

Required gates, in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `BASE_URL=https://gallery.atik.kr npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit. If changes are committed, create a GPG-signed Conventional Commit with gitmoji and Lore trailers, `git pull --rebase`, push, deploy with `npm run deploy`, and smoke:

- `curl -fsSIL https://gallery.atik.kr`
- `curl -fsS https://gallery.atik.kr/api/health`

## Progress

- [x] Review completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled docs/artifact fix.
- [x] Run required gates.
- [ ] Commit, pull --rebase, push.
- [ ] Deploy with `npm run deploy`.
- [ ] Smoke production endpoints.

## Gate Evidence

- Pre-change production smoke for the deployed Cycle 94 HEAD: `curl -fsSIL https://gallery.atik.kr` returned `HTTP/2 307` then `HTTP/2 200`; `curl -fsS https://gallery.atik.kr/api/health` returned `{"status":"ok"}`.
- `npm run lint --workspace=apps/web` - pass.
- `npm run lint:api-auth --workspace=apps/web` - pass: both admin API routes OK.
- `npm run lint:action-origin --workspace=apps/web` - pass: all mutating server actions enforce same-origin provenance.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass: all scanned public route handlers covered or explicitly exempt.
- `npm run typecheck --workspace=apps/web` - pass: route types generated, app/scripts typechecks clean, 8 JavaScript script files checked.
- `BASE_URL=https://gallery.atik.kr npm run build --workspace=apps/web` - pass. Local sitemap generation logged the expected unavailable local MySQL fallback and exited successfully.
- `npm test --workspace=apps/web` - pass: 302 files passed, 2 skipped; 2844 tests passed, 4 skipped.

## Gate Fix Iterations

- None.
