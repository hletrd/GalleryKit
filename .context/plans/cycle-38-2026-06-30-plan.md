# Cycle 38/100 Implementation Plan

Date: 2026-06-30 KST
Source review: `.context/reviews/cycle-38-2026-06-30/_aggregate.md`
Reviewed HEAD: `564a7679`

## Scope Rules

- Follow `AGENTS.md` and `CLAUDE.md`.
- Commit and push a GPG-signed Conventional Commit with gitmoji and Lore trailers.
- Run the configured blocking gates before commit/deploy:
  - `npm run lint --workspace=apps/web`
  - `npm run lint:api-auth --workspace=apps/web`
  - `npm run lint:action-origin --workspace=apps/web`
  - `npm run lint:public-route-rate-limit --workspace=apps/web`
  - `npm run typecheck --workspace=apps/web`
  - `npm run build --workspace=apps/web`
  - `npm test --workspace=apps/web`
- Run `git diff --cached --check` before commit.
- After successful push, deploy once from the repo root with `npm run deploy`.

Every Cycle 38 aggregate finding is either scheduled below or explicitly deferred in `cycle-38-2026-06-30-deferred.md`. Security, correctness, and deploy-safety findings are scheduled unless they require broader design or destructive/operator policy.

## Scheduled Findings

### C38-P1 - Harden action-origin exemption semantics

Status: implemented
Findings: `AGG-C38-01`, `AGG-C38-03`
Original severity/confidence: Low / High; Medium / High
Files:

- `apps/web/scripts/check-action-origin.ts`
- `apps/web/src/__tests__/check-action-origin.test.ts`

Plan:

- Require `@action-origin-exempt: <reason>` with a non-empty reason.
- For non-public, non-auth action files, require exempt protected reads to call `isAdmin()`, `getCurrentUser()`, or `requireSameOriginAdmin()` before protected DB/admin reads.
- Preserve the explicit public action and auth primitive carve-outs.

Acceptance:

- Bare and empty-reason exemption fixtures fail.
- Exempt admin `db.select()` without auth fails.
- Exempt admin reads after auth, auth primitives, and public action patterns remain green.

### C38-P2 - Resolve public route exported handler aliases

Status: implemented
Findings: `AGG-C38-02`
Original severity/confidence: Medium / High
Files:

- `apps/web/scripts/check-public-route-rate-limit.ts`
- `apps/web/src/__tests__/check-public-route-rate-limit.test.ts`

Plan:

- Resolve `export const POST = handler` and `export const GET = handler` to local function bodies.
- Audit resolved aliases under the exported HTTP method.
- Fail closed on unresolved aliases.

Acceptance:

- Mutating aliases without a limiter fail.
- Mutating aliases with a limiter pass.
- Expensive GET aliases without a limiter fail.
- Cheap GET aliases pass.
- Unresolved aliases fail closed.

### C38-P3 - Account for queued sidecar backfill task rejections

Status: implemented
Findings: `AGG-C38-04`
Original severity/confidence: Medium / High
Files:

- `apps/web/scripts/backfill-color-pipeline.ts`
- `apps/web/src/__tests__/backfill-color-pipeline-deleted-mid-reencode.test.ts`

Plan:

- Retain the promises returned by `queue.add()`.
- Await them with `Promise.allSettled()`.
- Count and log rejected queued tasks before final flush/summary/exit-code handling.
- Add a source contract because the sidecar `main()` path requires a live DB and filesystem.

Acceptance:

- Source contract proves queued task promises are retained, settled, counted, and logged.

### C38-P4 - Correct deploy/docs aggregate drift

Status: implemented
Findings: `AGG-C38-05`, `AGG-C38-06`
Original severity/confidence: Medium / Medium; Low / High
Files:

- `CLAUDE.md`
- `.context/reviews/_aggregate.md`
- `.context/plans/README.md`
- `.context/reviews/cycle-38-2026-06-30/_aggregate.md`

Plan:

- Change manual disk-recovery default to `docker volume prune -f`.
- Mention `docker volume prune -af` only as dedicated-host break-glass after inspection.
- Replace the top-level latest aggregate body with a Cycle 38 pointer/summary.
- Update the plan README to include Cycle 38 as the latest active cycle.

Acceptance:

- Runbook no longer recommends host-global all-volume prune as generally safe.
- Latest aggregate no longer embeds stale Cycle 35 detail.

## Deferred Findings

See `.context/plans/cycle-38-2026-06-30-deferred.md`.

## Progress

- [x] Review lanes completed and findings aggregated.
- [x] Plan and deferred records created.
- [x] C38-P1 implemented and tested.
- [x] C38-P2 implemented and tested.
- [x] C38-P3 implemented and tested.
- [x] C38-P4 implemented.
- [x] Full required gates passed.
- [ ] Signed commit pushed.
- [ ] Per-cycle deploy complete.

## Verification Evidence

- `npm test --workspace=apps/web -- --run src/__tests__/check-action-origin.test.ts src/__tests__/check-public-route-rate-limit.test.ts src/__tests__/backfill-color-pipeline-deleted-mid-reencode.test.ts`: passed, 148 tests.
- `npm run lint --workspace=apps/web`: passed.
- `npm run lint:api-auth --workspace=apps/web`: passed.
- `npm run lint:action-origin --workspace=apps/web`: passed.
- `npm run lint:public-route-rate-limit --workspace=apps/web`: passed.
- `npm run typecheck --workspace=apps/web`: passed.
- `npm run build --workspace=apps/web`: passed; local MySQL was unavailable and sitemap used its documented homepage-only fallback.
- `npm test --workspace=apps/web`: passed, 2643 passed / 4 skipped.
