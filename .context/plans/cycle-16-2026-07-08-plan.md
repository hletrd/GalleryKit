# Run-10 Cycle 16/100 Implementation Plan

Status: IMPLEMENTED - GATES GREEN; COMMIT/PUSH/DEPLOY PENDING  
Aggregate: `.context/reviews/_aggregate.md`  
Date: 2026-07-08 KST  
Start HEAD: `4b237f7e`

## Scope

This plan schedules the Cycle 16 findings that are confirmed, bounded, and safe to fix in this cycle. Every aggregate finding is either scheduled here or recorded in `cycle-16-2026-07-08-deferred.md`.

## Scheduled Work Packages

### WP1 - Prevent color settings/backfill stale-byte races

Findings: `AGG-C16-01`

Files:

- `apps/web/src/app/actions/settings.ts`
- `apps/web/src/lib/advisory-locks.ts` / advisory release helper as needed
- `apps/web/src/__tests__/settings-backfill-required-action.test.ts` or a focused sibling test

Plan:

1. Add a non-blocking color-backfill lock probe around byte-impacting settings commits.
2. If a setting in `SETTINGS_BACKFILL_WARNING_KEYS` actually changes while `LOCK_COLOR_PIPELINE_BACKFILL` is held, return a structured localized error instead of committing.
3. Hold/release the same lock briefly while committing such settings so a new runner cannot snapshot mid-save.
4. Add regression coverage for lock-held rejection and lock release on success/failure.

Acceptance:

- Settings updates that require backfill do not commit while color backfill is active.
- Existing no-op and no-image settings paths remain unchanged.
- Advisory-lock release follows destroy-on-failed-release discipline.

### WP2 - Normalize admin-action connection acquisition failures

Findings: `AGG-C16-02`, `AGG-C16-03`

Files:

- `apps/web/src/app/actions/admin-users.ts`
- `apps/web/src/app/actions/embeddings.ts`
- Existing/admin action tests or new focused tests

Plan:

1. Move dedicated MySQL connection acquisition inside structured error handling.
2. Use nullable connection variables and guard rollback/release paths.
3. Add tests that reject `connection.getConnection()` and assert the localized/typed response shape.

Acceptance:

- `deleteAdminUser()` returns `failedToDeleteUser` on acquisition failure.
- `backfillClipEmbeddings()` returns `{ status: "error", message: embeddingBackfillFailed }` on acquisition failure.

### WP3 - Fix current-cycle docs and operator copy drift

Findings: `AGG-C16-04`, `AGG-C16-05`, `AGG-C16-06`, `AGG-C16-07`, `AGG-C16-08`, `AGG-C16-43` copy-only portion, `AGG-C16-48` setup-doc portion

Files:

- `.context/plans/README.md`
- `apps/web/scripts/backfill-clip-embeddings.ts`
- `apps/web/src/lib/storage/types.ts`
- `apps/web/messages/en.json`
- `apps/web/messages/ko.json`
- `README.md` or `apps/web/README.md` for CLIP one-time download / deploy-local config wording

Plan:

1. Make the plan index point at Cycle 16 as the active current cycle and move Cycle 15 to completed/superseded text.
2. Add the missing production env gate to the CLIP sidecar example or point to the canonical runbook.
3. Rewrite storage interface method comments as future/intended use, not live integration.
4. Clarify Settings semantic-search copy: stub is wiring-test-only; production activation is operator-runbook-only.
5. Clarify upload-token copy: UI-created tokens do not expire by default unless an expiry control is later added.
6. Clarify CLIP model seeding downloads public weights once, while runtime photo/query inference stays local.
7. Clarify deploy-local `site-config.json` metadata risk for copied worktrees.

Acceptance:

- English and Korean message keys remain in parity.
- Product/operator copy no longer claims UI capabilities the code does not expose.

### WP4 - Fix two narrow UX/perf inconsistencies

Findings: `AGG-C16-09`, `AGG-C16-17`, `AGG-C16-19`

Files:

- `apps/web/src/components/image-manager.tsx`
- `apps/web/messages/en.json`
- `apps/web/messages/ko.json`
- `apps/web/src/app/[locale]/(public)/timeline/page.tsx`
- `apps/web/src/app/[locale]/(public)/year/[year]/page.tsx`
- `apps/web/src/components/photo-viewer.tsx`
- Focused tests/source contracts where existing patterns permit

Plan:

1. Name the target image in the individual image-delete confirmation dialog using title/id/filename fallback.
2. Add `prefetch={false}` to timeline/year photo links to match main/shared grid behavior.
3. Shorten the desktop info-sidebar transition to a professional-tool-friendly duration.

Acceptance:

- Delete confirmation gives target context.
- Archive links no longer prefetch detail pages on viewport.
- Reduced-motion behavior remains intact.

### WP5 - Gate verification and deploy

Findings: quality-gate requirement for this cycle.

Plan:

1. Run all configured gates:
   - `npm run lint --workspace=apps/web`
   - `npm run lint:api-auth --workspace=apps/web`
   - `npm run lint:action-origin --workspace=apps/web`
   - `npm run lint:public-route-rate-limit --workspace=apps/web`
   - `npm run typecheck --workspace=apps/web`
   - `npm run build --workspace=apps/web`
   - `npm test --workspace=apps/web`
   - `npm run test:e2e --workspace=apps/web` if browser-flow coverage is required by the touched surface
2. Fix blocking errors before committing/pushing.
3. Commit and push in fine-grained signed Conventional Commits with gitmoji.
4. After pushed gates are green, run `npm run deploy` once because `DEPLOY_MODE=per-cycle`.

## Progress

- [x] WP1 color settings/backfill lock coordination
- [x] WP2 admin action connection acquisition handling
- [x] WP3 docs/copy drift
- [x] WP4 UX/perf inconsistencies
- [ ] WP5 gates green; commit, push, and deploy pending

## Gate Evidence

- `npm test --workspace=apps/web -- settings-backfill-required-action.test.ts settings-semantic-mode-action.test.ts admin-user-delete-audit-detach.test.ts embeddings-action-behavior.test.ts client-source-contracts.test.ts backfill-clip-embeddings-reembed.test.ts` - PASS, 6 files / 54 tests.
- `npm test --workspace=apps/web -- mysql-runtime-ssl.test.ts` - PASS, 1 file / 2 tests.
- `npm run lint --workspace=apps/web` - PASS.
- `npm run lint:api-auth --workspace=apps/web` - PASS.
- `npm run lint:action-origin --workspace=apps/web` - PASS.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - PASS.
- `npm run typecheck --workspace=apps/web` - PASS after fixing the release-task promise typing.
- `npm run build --workspace=apps/web` - PASS; local sitemap DB fallback warning deferred as `GATE-C16-W01`.
- `npm test --workspace=apps/web` - PASS, 353 files passed / 2 skipped, 3254 tests passed / 4 skipped.
- `npm run test:e2e --workspace=apps/web` - PASS against disposable cycle database `gallery_e2e_cycle16`, 45 passed / 2 skipped; non-fatal server-log noise deferred as `GATE-C16-W02`.

## Gate Fixes

- Fixed a TypeScript predicate/type error in the color-lock release path by using an explicit release promise array.
- Fixed an ESLint `no-require` issue introduced while resolving the e2e startup warning.
- Fixed the standalone e2e startup warning caused by `module.require` being unavailable after bundling the MySQL option helper.
