# Cycle 2/100 Implementation Plan — 2026-07-18

Start HEAD: `ba4bc60a`
Source: `.context/reviews/_aggregate.md` and all Cycle 2 provenance reviews
Status: Implementation complete; signed push and per-cycle deploy pending

## Finding disposition

Every new aggregate finding is scheduled below. Revalidated findings remain in
the authoritative `.context/plans/deferred-carry-forward.md` register with their
original severity/confidence, concrete reasons, repository-policy constraints,
and exit criteria. No security, correctness, or data-loss finding is newly
deferred.

The deploy-owner hypothesis raised by code-reviewer/critic/verifier/test/docs is
explicitly resolved, not silently dropped: the repository owner already owns the
executed checkout script, so accepting that owner's private env file does not
cross an additional privilege boundary. The security reviewer independently
confirmed this. Documentation wording will be corrected so the trust is not
misdescribed as an explicitly configured principal; an immutable/root-owned
installed helper would require a different policy.

## Work packages

- [x] **C2-01 sitemap runtime ownership:** prevent a DB-less production build
  from sealing fallback rows into a fresh one-hour ISR artifact; keep the route
  resilient to runtime DB outages; update misleading recovery diagnostics; add
  source/build-artifact regression coverage.
- [x] **C2-02 responsive image scheduling:** restore a mobile-safe SSR eager
  budget and provide browser-evaluated, media-qualified preload hints for the
  additional desktop first-row cards without forcing mobile downloads; add
  source and browser request-timeline coverage at 320 px and desktop width.
- [x] **C2-03 combobox state:** make `aria-expanded` track actual listbox
  presence, retain dialog-trigger semantics separately, and cover empty,
  loading, no-result, and result states.
- [x] **C2-04 semantic recall docs:** distinguish repeated backfill runs that
  complete a missing-embedding backlog from request-time recall; document that
  broader old-photo recall needs a larger bounded scan limit or a vector index.
- [x] **C2-05 deploy trust wording:** document that running a checkout-owned
  script inherently trusts the checkout owner and that the owner exception does
  not make an immutable installed helper safe; preserve the current shared-mount
  behavior and its private-file checks.
- [x] Run every configured blocking gate against the whole repository,
  including Playwright because C2-02/C2-03 change browser behavior.
- [ ] Update this ledger with implementation/gate evidence, commit each coherent
  work item with signed gitmoji Conventional Commits, pull --rebase, push
  `master`, and run the selected per-cycle `npm run deploy` once after all gates
  are green.

## Deferred-finding mapping

- ARCH-2-01 / TRC-C2-R1 / DBG-C2-R2: existing shared background DB-budget row.
- CR-2-03 / ARCH-2-03 / CRIT-2-03 / TEST-2-03 / DBG-C2-R1: existing failed
  deployment promotion/rollback/cleanup rows.
- SEC-C2-R1: existing warn-only single-writer/topology row.
- PERF-C2-R1: existing large GPS-map rendering row.
- PERF-C2-R2: existing semantic-vector scan/ranking row.
- TRC-C2-R2: existing SQL-restore/file-store generation boundary row.

All mappings above retain the authoritative register's original severity and
confidence. Their exit criteria have not fired in this cycle; they are not new
review findings and are not re-deferred here.

## Completion evidence

- Focused regression run: 3 files / 25 tests passed.
- Full Vitest: 361 files passed, 2 skipped; 3,410 tests passed, 4 skipped.
- Playwright: 46 passed, 2 skipped against a disposable MySQL 8 E2E database;
  the explicitly named temporary container was stopped and auto-removed.
- ESLint, API-auth lint, action-origin lint, public-route-rate-limit lint,
  typecheck, production build, production dependency audit, and
  `git diff --check`: passed.
- Built-artifact assertion: `/sitemap.xml` is absent from
  `.next/prerender-manifest.json`, proving the DB-less fallback is not sealed as
  a fresh ISR route.
- Gate repair: the first new typecheck found one incorrect helper import in the
  responsive-preload implementation; the import was corrected and the full
  typecheck/build rerun passed (`GATE_FIXES=1`).
- Signed fine-grained commits, pull/rebase, push, and the single selected
  per-cycle deploy remain the terminal release steps for this cycle.
