# Cycle 91/100 Implementation Plan

Start HEAD: `c648634b666f59c29cfe40ea5bbd547bc98d1885`.
Review aggregate: `.context/reviews/_aggregate.md` (Cycle 91).

## Goal

Close the safe, narrow Cycle 91 findings: release-ledger terminal evidence for `c648634` and stale lightbox accessibility source-contract coverage. Keep review/plan indexes current, run the required gates in order, commit/push, deploy with `npm run deploy`, and smoke `https://gallery.atik.kr`.

## Scheduled Fixes

1. `C91-01`: update the Cycle 90 terminal ledger so `c648634b666f59c29cfe40ea5bbd547bc98d1885` is recorded as the docs-only terminal-evidence sync on top of the primary `dcc8055` cycle commit/deploy, and remove "in progress" wording from `.context/plans/README.md`.
2. `C91-02`: update `apps/web/src/__tests__/a11y-us-p15.test.ts` so the lightbox position announcement test asserts the current live status-region contract instead of obsolete image-label/source-regex wording.
3. `C91-03`: keep Cycle 91 review and plan artifacts trackable by adding the required plan whitelist entries and making `.context/reviews/_aggregate.md` point at Cycle 91.

## Deferred This Cycle

All non-scheduled findings, carry-forward deferred items, and manual-validation risks are recorded in `.context/plans/cycle-91-2026-07-01-deferred.md`.

## Regression Coverage

- Ledger: `.context/plans/README.md` must list Cycle 91 as active and Cycle 90 as recent/completed, not still in-progress.
- Ledger: `.context/plans/cycle-90-2026-07-01-plan.md` must record terminal `c648634` evidence separate from the primary `dcc8055` deploy evidence.
- Accessibility source contract: `a11y-us-p15.test.ts` must fail if the lightbox status region loses `role="status"`, `aria-live="polite"`, or the `aria.photoPosition` label using `currentIndex + 1` and `totalCount`.

## Validation

Run focused test first:

- `npm test --workspace=apps/web -- --run src/__tests__/a11y-us-p15.test.ts`

Then run required gates in exact order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `BASE_URL=https://gallery.atik.kr npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then signed commit, `git pull --rebase`, push, deploy with `npm run deploy`, and smoke:

- `curl -fsSIL https://gallery.atik.kr`
- `curl -fsS https://gallery.atik.kr/api/health`

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run focused test.
- [x] Run required gates.
- [x] Commit, pull --rebase, push.
- [x] Deploy with `npm run deploy`.
- [x] Smoke production endpoints.

## Focused Evidence

- Starting refs: `HEAD == origin/master == origin/HEAD == c648634b666f59c29cfe40ea5bbd547bc98d1885`.
- Pre-edit production smoke for starting deployed HEAD: root returned `307 https://gallery.atik.kr/en`, following the redirect returned `200 https://gallery.atik.kr/en`, and `/api/health` returned `{"status":"ok"}`.
- Focused test: `npm test --workspace=apps/web -- --run src/__tests__/a11y-us-p15.test.ts` - pass, 1 file passed, 10 tests passed.
- Primary cycle release: signed commit `aacccbc99ccbafe473362c7daf9eaaaa44b6ccef` was pulled/rebased, pushed to `origin/master`, and deployed with `npm run deploy`.
- Post-deploy smoke: `curl -fsSIL https://gallery.atik.kr` returned the expected `307` locale redirect and `200` for `/en`; `curl -fsS https://gallery.atik.kr/api/health` returned `{"status":"ok"}`.

## Gate Evidence

- `npm run lint --workspace=apps/web` - pass.
- `npm run lint:api-auth --workspace=apps/web` - pass: both admin API routes OK.
- `npm run lint:action-origin --workspace=apps/web` - pass: all mutating server actions enforce same-origin provenance.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
- `npm run typecheck --workspace=apps/web` - pass.
- `BASE_URL=https://gallery.atik.kr npm run build --workspace=apps/web` - pass. Local sitemap generation logged the expected unavailable local MySQL fallback and exited successfully.
- `npm test --workspace=apps/web` - pass: 302 files passed, 2 skipped; 2840 tests passed, 4 skipped.

## Gate Fix Iterations

- `C91-01`: fixed one release-ledger/documentation drift by recording terminal `c648634` evidence and moving Cycle 90 out of active/in-progress state.
- `C91-02`: fixed one stale accessibility source-contract test so CI now checks the lightbox live status-region contract.
