# Run-10 Cycle 20/100 Implementation Plan

Status: IMPLEMENTED + SOURCE DEPLOYED; terminal docs-ledger deploy evidence is superseded by Cycle 21.
Aggregate: `.context/reviews/_aggregate.md`
Date: 2026-07-08 KST
Review HEAD: `bd0cc170412b0f70ae231cec27ca54ee50e638fd`
Source-fix HEAD: `d8e604ef1033584fe09046a921e3d0de9d39b767`

## Scope

This plan records the recovered Cycle 20 findings and the bounded fixes completed after the cycle subagent stalled. Every aggregate finding is either scheduled here or recorded in `cycle-20-2026-07-08-deferred.md`.

Repo rules read before scheduling/deferring: `CLAUDE.md`, `AGENTS.md`, `.context/plans/README.md`, `.context/plans/deferred-carry-forward.md`, the current top-level `.context/reviews/*.md` files, Cycle 19 dated plan/deferred files, and loop-B Cycle 9 plan/deferred files. No `.cursorrules` or `CONTRIBUTING.md` exists in this checkout.

## Scheduled Work Packages

### WP1 - Bound shared-group view-count draining during restore

Findings: `AGG-C20-01`

Files:

- `apps/web/src/app/[locale]/admin/db-actions.ts`
- `apps/web/src/__tests__/restore-drain-checklist.test.ts`

Plan:

1. Add a restore-specific bounded shared-group view-count drain.
2. Make it an explicit `runRestoreDrainChecklist()` stage before other process-local writers.
3. Remove the direct pre-checklist `await flushBufferedSharedGroupViewCounts()` path.
4. Update tests so the checklist has a named first stage and direct pre-checklist flushes are rejected.

Acceptance:

- Restore can abort on shared-group view-count drain timeout instead of hanging before the bounded checklist.
- Restore drain source tests no longer bless the risky ordering.

Status: implemented in `d8e604ef`.

### WP2 - Harden the admin mutation-barrier lint proof

Findings: `AGG-C20-02`

Files:

- `apps/web/scripts/check-action-origin.ts`
- `apps/web/src/__tests__/check-action-origin.test.ts`

Plan:

1. Count only `acquireAdminMutationSlot` identifiers imported from `@/lib/admin-mutation-barrier`.
2. Require a `using` declaration bound to the approved call.
3. Require an acquired-state early-return gate before mutation.
4. Add negative fixtures for local spoofing, wrong-module import, bare call, non-`using` assignment, and missing acquired check.

Acceptance:

- A future admin mutation cannot satisfy `lint:action-origin` with a spoofed or non-disposable barrier call.
- Existing legitimate action shapes still pass the scanner.

Status: implemented in `d8e604ef`.

### WP3 - Treat photo pages as revocable service-worker HTML

Findings: `AGG-C20-03`

Files:

- `apps/web/public/sw.template.js`
- `apps/web/public/sw.js`
- `apps/web/src/__tests__/sw-template-contract.test.ts`

Plan:

1. Classify localized and unlocalized `/p/:id` photo pages as revocable HTML.
2. Bypass offline HTML fallback for photo pages, matching other revocable object surfaces.
3. Regenerate the committed `sw.js` artifact.
4. Extend contract tests for `/p/123`, `/ko/p/123`, and `/en-US/p/123`.

Acceptance:

- Deleted/unpublished photo pages are not served from 24-hour offline HTML cache.
- Template and generated worker remain synchronized.

Status: implemented in `d8e604ef`.

### WP4 - Close bounded documentation and ledger findings

Findings: `AGG-C20-22`, `AGG-C20-23`

Files:

- `apps/web/.env.local.example`
- `.context/reviews/_aggregate.md`
- `.context/plans/cycle-20-2026-07-08-plan.md`
- `.context/plans/cycle-20-2026-07-08-deferred.md`
- `.context/plans/README.md`
- `.context/plans/deferred-carry-forward.md`

Plan:

1. Update the `DB_SSL_CA` example comment to name runtime, Drizzle Kit, and backup/restore CLI TLS.
2. Replace the stale Cycle 19 aggregate with Cycle 20 aggregation.
3. Add this dated Cycle 20 plan/deferred pair.
4. Update the plan index so the current cycle points at Cycle 20 and the bare historical `cycle-20/21/22-*` files are disambiguated.
5. Update the carry-forward register with a run-10 cycle-20 age-budget check and new deferred rows.

Acceptance:

- Future agents see one current Cycle 20 plan/deferred pair and do not treat bare historical files as the active cycle.
- The consolidated deferred register includes Cycle 20 and no longer advertises Cycle 19 as the latest check.
- Remote MySQL TLS setup guidance matches runtime and Drizzle behavior.

Status: implemented by this docs commit.

### WP5 - Gates, commit, push, and per-cycle deploy

Findings: cycle quality/deploy requirement.

Plan:

1. Run every configured gate against the whole app after source changes:
   - `npm run lint --workspace=apps/web`
   - `npm run lint:api-auth --workspace=apps/web`
   - `npm run lint:action-origin --workspace=apps/web`
   - `npm run lint:public-route-rate-limit --workspace=apps/web`
   - `npm run typecheck --workspace=apps/web`
   - `npm run build --workspace=apps/web`
   - `npm test --workspace=apps/web`
   - `npm run test:e2e --workspace=apps/web`
2. Commit and push signed, fine-grained commits after `git pull --rebase`.
3. Run `npm run deploy` after each pushed commit because `DEPLOY_MODE=per-cycle`.
4. Smoke `https://gallery.atik.kr` after deploy.

## Progress

- [x] WP1 restore shared-group view-count drain is bounded by the restore checklist.
- [x] WP2 action-origin mutation-barrier proof requires approved import, `using`, and acquired-state gate.
- [x] WP3 service worker treats photo pages as revocable HTML and `sw.js` is regenerated.
- [x] WP4 cycle aggregate, plan/deferred ledgers, carry-forward register, and env example are updated.
- [x] WP5 full source gate sweep passed for `d8e604ef`; source commit pushed and deployed.
- [x] WP5 docs ledger commit/deploy evidence for the terminal ledger-only change was not committed; Cycle 21 records this honestly and supersedes production with its per-cycle deploy.

## Gate Evidence

Source-fix gate sweep at `d8e604ef`:

- `npm run lint --workspace=apps/web` -> passed.
- `npm run lint:api-auth --workspace=apps/web` -> passed.
- `npm run lint:action-origin --workspace=apps/web` -> passed.
- `npm run lint:public-route-rate-limit --workspace=apps/web` -> passed.
- `npm run typecheck --workspace=apps/web` -> passed.
- `npm run build --workspace=apps/web` -> passed.
- `npm test --workspace=apps/web` -> 355 files passed, 2 skipped; 3333 tests passed, 4 skipped.
- `npm run test:e2e --workspace=apps/web` -> 45 passed, 2 skipped.

Deploy/smoke evidence for `d8e604ef`:

- `npm run deploy` -> remote Docker build, health wait, and post-up prune completed successfully.
- `curl -fsSI https://gallery.atik.kr` -> `307` to `/en`.
- `curl -fsSI -L https://gallery.atik.kr/en` -> `200`.
- `curl -fsS https://gallery.atik.kr/api/live` -> `{"status":"ok"}`.
- Missing-photo smoke `https://gallery.atik.kr/photos/__codex-smoke-missing-d8e604ef` -> `404` at `/en/photos/...`.

Terminal ledger note:

- The final Cycle 20 docs-ledger state at `45b32d1d` did not include committed deploy evidence. Cycle 21 treats `d8e604ef` as the last evidenced Cycle 20 deploy and supersedes the terminal docs-only state with the Cycle 21 per-cycle deploy after its gates pass.

## Deferred Register

All aggregate findings not listed in WP1-WP4 are recorded in `cycle-20-2026-07-08-deferred.md` with original severity/confidence, citations, deferral reason, and exit criterion.
