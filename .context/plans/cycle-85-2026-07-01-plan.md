# Cycle 85/100 Implementation Plan

Start HEAD: `1d29b98861098a68a8107746997a5d81d70f03f1`.
Review aggregate: `.context/reviews/cycle-85-2026-07-01/_aggregate.md`.

## Goal

Recover the safe Cycle 85 fixes after the NFS worktree outage, keep review/plan ledgers current, strengthen failed-image retry and delete-cleanup regression contracts, run all required gates, commit/push all work, and deploy with `npm run deploy` from the repo root.

## Scheduled Fixes

1. `C85-01`: close Cycle 84's release ledger by marking commit/push/deploy complete with signed `1d29b988` / `origin/master` evidence, recording that Cycle 85 starts from deployed `1d29b988`, and moving Cycle 84 out of the active plan index.
2. `C85-02`: strengthen `failed-image-retry.test.ts` so English and Korean retry aria-label locale templates must keep the `{label}` interpolation used for failed-image row identity.
3. `C85-03`: strengthen `image-queue-permanent-failure-cleanup.test.ts` so `deleteImage` and `deleteImages` each have an action-specific source contract for clearing `permanentlyFailedIds`.
4. `C85-04`: keep Cycle 85 review/plan artifacts trackable by extending the existing `.gitignore` whitelist for the new plan and deferred files.

## Deferred This Cycle

No new Cycle 85 findings are deferred.

Carry-forward deferred items remain in `.context/plans/cycle-85-2026-07-01-deferred.md`.

## Regression Coverage

- Retry aria labels: fixture coverage must prove both locale templates retain `{label}` for normal and retrying states.
- Delete cleanup: source-contract coverage must prove `deleteImage` clears the single deleted `id` and `deleteImages` clears `permanentlyFailedIds` inside the `foundIds` cleanup loop.
- Ledger: Cycle 84 must no longer be listed as active, and Cycle 84's terminal state must record signed commit/push/deployed-start evidence for `1d29b988`.

## Validation

Run required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then signed commit, `git pull --rebase`, push, and deploy with `npm run deploy`.

## Progress

- [x] Review aggregate recovered.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run required gates.
- [ ] Commit, pull --rebase, push.
- [ ] Deploy with `npm run deploy`.

## Gate Evidence

- Focused regression (initial): `npm test --workspace=apps/web -- --run src/__tests__/failed-image-retry.test.ts src/__tests__/image-queue-permanent-failure-cleanup.test.ts` - failed before running `image-queue-permanent-failure-cleanup.test.ts` because the fresh fallback clone lacked ignored `apps/web/src/site-config.json`; `failed-image-retry.test.ts` passed 19 tests.
- Focused regression (rerun): `npm test --workspace=apps/web -- --run src/__tests__/failed-image-retry.test.ts src/__tests__/image-queue-permanent-failure-cleanup.test.ts` - pass: 2 files passed, 26 tests passed.
- `npm run lint --workspace=apps/web` - pass.
- `npm run lint:api-auth --workspace=apps/web` - pass: both admin API routes OK.
- `npm run lint:action-origin --workspace=apps/web` - pass: all mutating server actions enforce same-origin provenance.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
- `npm run typecheck --workspace=apps/web` - pass.
- `BASE_URL=https://gallery.atik.kr npm run build --workspace=apps/web` - pass. Local sitemap generation logged the expected unavailable local MySQL fallback and exited successfully.
- `npm test --workspace=apps/web` - pass: 301 files passed, 2 skipped; 2838 tests passed, 4 skipped.

## Gate Fix Iterations

1. Created the documented ignored local `apps/web/src/site-config.json` from `apps/web/src/site-config.example.json` in the fallback clone after the initial focused test import failed before running the cleanup suite.
