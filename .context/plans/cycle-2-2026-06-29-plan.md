# Cycle 2/100 Implementation Plan

Date: 2026-06-29  
Review aggregate: `.context/reviews/_aggregate.md`  
Status: COMPLETE

Repo rules read before planning: `CLAUDE.md`, `AGENTS.md`, `.context/plans/README.md`, current `.context/reviews/**`, and `docs/superpowers/**`. No `.cursorrules` or `CONTRIBUTING.md` exists in this checkout.

## Scheduled Findings

### C2-01 - Exclude `.claude/` from Docker context

- Finding: AGG-C2-01.
- Original severity/confidence: Medium / High.
- Citation: `.dockerignore:1-22`, `apps/web/docker-compose.yml:4-6`, `apps/web/Dockerfile:67-75`.
- Plan: add `.claude` / `.claude/` to root `.dockerignore`.
- Validation: `git diff --check`; full gate set in Prompt 3.
- Progress: DONE. Root `.dockerignore` now excludes `.claude` / `.claude/`.

### C2-02 - Harden direct image default binding

- Finding: AGG-C2-15.
- Original severity/confidence: Medium / Medium.
- Citation: `apps/web/Dockerfile:83-85`, `apps/web/docker-compose.yml:14-21`.
- Plan: change Dockerfile default `HOSTNAME` to `127.0.0.1` so the standalone image defaults to the documented nginx/localhost envelope. Operators who intentionally expose the container can still override `HOSTNAME=0.0.0.0`.
- Validation: config/source test if one exists; full build gate.
- Progress: DONE. Dockerfile default `HOSTNAME` is `127.0.0.1`; compose still sets the documented localhost binding explicitly.

### C2-03 - Fix tracked-plan documentation mismatch

- Finding: AGG-C2-16.
- Original severity/confidence: Low / High.
- Citation: `AGENTS.md:41`.
- Plan: update the Gallery Workspace Rules line to state that `.context/reviews/` and `.context/plans/` are committed history, while local scratch plans should use ignored/local-only paths.
- Validation: doc diff review.
- Progress: DONE. `AGENTS.md` now describes `.context/plans/` as committed history, not private scratch state.

### C2-04 - Correct CLIP production backfill examples

- Finding: AGG-C2-21.
- Original severity/confidence: High / High.
- Citation: `apps/web/README.md:35-37`, `apps/web/scripts/backfill-clip-embeddings.ts:4-22`, `apps/web/scripts/backfill-clip-embeddings.ts:90-95`.
- Plan: update the README command table and script header production sidecar examples to use `--production --force` for pre-enable production backfills, with a note that plain `--production` is only after production/stub mode is already active.
- Validation: targeted text/source check plus full gates.
- Progress: DONE. README and script examples now use `--production --force` for pre-enable production CLIP backfills.

### C2-05 - Add admin route metadata

- Finding: AGG-C2-22.
- Original severity/confidence: Medium / High.
- Citation: `apps/web/src/app/[locale]/admin/page.tsx:6-15`, protected admin page modules, existing nav messages.
- Plan: add localized `generateMetadata` to admin login and protected admin page modules. Reuse existing translation namespaces (`nav`, route-specific namespaces where helpful). Keep the existing password metadata pattern compatible.
- Validation: source-level metadata coverage test or targeted test if existing route-metadata tests support it; full typecheck/build.
- Progress: DONE. Admin routes use localized metadata helpers; DB client route is covered by a server layout. `client-source-contracts.test.ts` covers route metadata exports.

### C2-06 - Localize timeline/year photo card labels

- Finding: AGG-C2-23.
- Original severity/confidence: Medium / High.
- Citation: `apps/web/src/app/[locale]/(public)/timeline/page.tsx:192-212`, `apps/web/src/app/[locale]/(public)/year/[year]/page.tsx:151-168`.
- Plan: load `common` and `aria` translations, pass localized `common.untitled` / `common.photo` fallback text to photo-title helpers, and set links to localized `aria.viewPhoto`.
- Validation: add or extend a focused source-contract test rejecting hard-coded `'Photo'` fallbacks/bare `aria-label={displayTitle}` in timeline/year cards.
- Progress: DONE. Timeline/year cards use localized `common` and `aria` labels; source-contract tests reject the prior hard-coded and bare-label patterns.

### C2-07 - Add timeline/year social metadata and localized invalid-year metadata

- Findings: AGG-C2-24, AGG-C2-25.
- Original severity/confidence: Low / High for both.
- Citation: `apps/web/src/app/[locale]/(public)/timeline/page.tsx:26-30`, `apps/web/src/app/[locale]/(public)/year/[year]/page.tsx:22-41`.
- Plan: add Open Graph/Twitter metadata to timeline/year pages using configured `seo.og_image_url` fallback and locale helpers. For invalid year params, load localized metadata before returning noindex not-found metadata.
- Validation: targeted metadata tests or source contract; full build.
- Progress: DONE. Timeline/year metadata include Open Graph/Twitter fallback metadata, and invalid year metadata uses localized not-found copy.

### C2-08 - Strengthen nav e2e assertions

- Finding: AGG-C2-18.
- Original severity/confidence: Medium / High.
- Citation: `apps/web/e2e/nav-visual-check.spec.ts:14,27,39`.
- Plan: keep screenshots as artifacts if useful, but add DOM/bounding-box assertions that prove the visual contract: nav is visible, controls meet 44px minimums, expanded mobile menu exposes expected controls without overlap, and desktop nav controls are visible.
- Validation: e2e spec type/lint/build; full gate set does not include Playwright e2e, so rely on static/type gates unless e2e is cheap locally.
- Progress: DONE. The nav e2e spec now measures visible target size and overlap, while retaining screenshots as artifacts.

### C2-09 - Strengthen browser upload enqueue payload test

- Finding: AGG-C2-19.
- Original severity/confidence: Medium / High.
- Citation: `apps/web/src/app/actions/images.ts:467-497`, `apps/web/src/__tests__/images-actions.test.ts:375`.
- Plan: assert the full queued processing/settings payload in the browser upload success test, matching the Lightroom coverage shape.
- Validation: targeted Vitest for `images-actions.test.ts`, then full gates.
- Progress: DONE. Browser upload success test asserts the queued quality, image sizes, color settings, auto-alt setting, and semantic-search mode snapshot.

### C2-10 - Complete required build verification

- Finding: AGG-C2-17.
- Original severity/confidence: Low / High.
- Citation: `apps/web/package.json:11`.
- Plan: run the configured `npm run build --workspace=apps/web` gate in Prompt 3 after fixes. If `public/sw.js` changes, inspect and commit the generated stamp intentionally.
- Validation: build gate output.
- Progress: DONE. `npm run build --workspace=apps/web` passed; the generated service worker stamp changed from `4cdb2259-p7` to `3d138704-p7`.

## Deferred Findings

See `.context/plans/cycle-2-2026-06-29-deferred.md`. Every aggregate finding not listed above is explicitly recorded there with original severity/confidence and exit criteria.

## Required Gates

Run the full cycle gates before commit/push/deploy:

- `npm run lint --workspace=apps/web`
- `npm run lint:api-auth --workspace=apps/web`
- `npm run lint:action-origin --workspace=apps/web`
- `npm run lint:public-route-rate-limit --workspace=apps/web`
- `npm run typecheck --workspace=apps/web`
- `npm run build --workspace=apps/web`
- `npm test --workspace=apps/web`

## Gate Evidence

- PASS: `npm run lint --workspace=apps/web`
- PASS: `npm run lint:api-auth --workspace=apps/web`
- PASS: `npm run lint:action-origin --workspace=apps/web`
- PASS: `npm run lint:public-route-rate-limit --workspace=apps/web`
- PASS: `npm run typecheck --workspace=apps/web`
- PASS: `npm run build --workspace=apps/web` (local sitemap DB fallback logged and handled)
- PASS: `npm test --workspace=apps/web` (243 files passed, 2 skipped; 2238 tests passed, 4 skipped)
