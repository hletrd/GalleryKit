# Run-10 Cycle 21/100 Deferred Findings

Status: OPEN
Aggregate: `.context/reviews/_aggregate.md`
Date: 2026-07-08 KST

This register records every Cycle 21 aggregate finding not scheduled in `cycle-21-2026-07-08-plan.md`. Severity and confidence are preserved from the aggregate. Deferred work remains bound by repo policy: GPG-signed Conventional Commits with gitmoji, `git pull --rebase` before push, required gates, no force-push, no `--no-verify`, and the repo's Node/Next/React/TypeScript toolchain rules.

Repo rule basis for deferral: `.context/plans/README.md` defines the carry-forward age budget; `.context/plans/deferred-carry-forward.md` is the committed register for open deferred rows. `CLAUDE.md` documents the single-web-instance topology, process-local state boundaries, DB-only backup/restore boundary, and host-nginx operator boundary. This register does not defer the direct data-loss/public-file cleanup issue (`AGG-C21-02`) or the direct mutation-barrier lint defect (`AGG-C21-01`); both are scheduled.

## Deferred Items

| ID | Severity / Confidence | Citation | Reason for deferral | Exit criterion |
|----|-----------------------|----------|---------------------|----------------|
| AGG-C21-03 | High / High | `apps/web/src/app/actions/images.ts:129-270`; `apps/web/src/app/api/admin/lr/upload/route.ts:84-630` | Shared ingest-service extraction is a broad architecture refactor across two hot upload paths. Cycle 21 schedules contained deletion durability and scanner correctness first. | Before adding upload-time privacy/metadata/processing fields, or the next confirmed browser/PAT drift. |
| AGG-C21-04 | High / High-Medium | `apps/web/src/app/actions/images.ts:129-149`; `apps/web/src/app/api/admin/lr/upload/route.ts:174-188`; `apps/web/src/app/[locale]/admin/db-actions.ts:407-729`; `apps/web/next.config.ts:111-119`; `apps/web/src/lib/upload-limits.ts:1-35` | Streaming multipart migration crosses browser upload, PAT upload, and restore. It needs RSS measurement and shared temp-file handoff design; no live RSS/OOM evidence was collected in this cycle. | Production RSS/OOM incident during upload/restore, or approved streaming-ingress project. |
| AGG-C21-05 | High / High | `apps/web/src/db/index.ts:21-45`; `apps/web/src/lib/image-queue.ts:121-153`; `apps/web/src/lib/admin-backfill-runner.ts:106-820`; `apps/web/src/lib/process-image.ts:36-1418` | Shared DB/CPU background-budget work changes queue/backfill concurrency semantics. This is already tracked as `C20-06` and earlier `C19-04`; no measured starvation evidence was collected this cycle. | Measured pool starvation during queue/backfill/search, or shared background-budget project approved. |
| AGG-C21-07 | High / High | `apps/web/src/__tests__/migrate-reconcile-coverage.test.ts:13-180`; `apps/web/src/__tests__/cycle-20-source-contracts.test.ts:31-63`; `apps/web/src/__tests__/semantic-scan-limit-source.test.ts:42-77`; `apps/web/scripts/migrate.js:348-493` | Broad test-infrastructure class. Cycle 21 schedules one direct scanner behavior proof and deletion durability tests, while DB-backed reconcile and child-process harness work need dedicated infrastructure. | Next high-risk source-contract-only finding, or behavior/integration test-hardening cycle. |
| AGG-C21-08 | High / High | `apps/web/src/__tests__/migrate-reconcile-coverage.test.ts:13-180`; `apps/web/scripts/migrate.js:348-493` | DB-backed schema convergence requires disposable MySQL orchestration and may be CI/nightly-only. This remains tracked from `C20-28`/`C19-07`; Cycle 21 adds a concrete migration but not the full test harness. | Next migration/schema cycle with DB test infra, or reconcile/schema drift incident. |
| AGG-C21-09 | High / Medium | `apps/web/src/__tests__/db-restore.test.ts:47-136`; `apps/web/src/app/[locale]/admin/db-actions.ts:157-405`; `apps/web/src/app/[locale]/admin/db-actions.ts:740-860` | Stub-binary child-process harness is substantial test infrastructure for restore. `CLAUDE.md` documents restore boundaries; no current restore execution bug was confirmed. | Next backup/restore cycle, child-process regression, or reusable spawn/fixture harness lands. |
| AGG-C21-10 | Medium / High | `apps/web/src/app/actions/public.ts:247-329`; `apps/web/src/lib/data.ts:1488-1816`; `apps/web/src/app/api/search/semantic/route.ts:263-311`; `apps/web/src/app/api/search/similar/[id]/route.ts:177-214`; `apps/web/src/app/[locale]/(public)/map/page.tsx:13-111`; `apps/web/src/lib/smart-collections.ts:221-267` | Umbrella performance finding decomposes into map/search/vector/smart-collection work below; contained cycle work focuses on correctness and a small index. | Any decomposed exit criterion below fires, or public performance project approved. |
| AGG-C21-11 | Medium / High | `apps/web/src/lib/data.ts:1766-1817`; `apps/web/src/db/schema.ts:49-131`; `apps/web/src/app/[locale]/(public)/map/page.tsx:42-110`; `apps/web/src/components/map/map-client.tsx:77-140` | Map clustering/viewport APIs and spatial indexing are product/performance scope. Cycle 21 schedules the map-marker a11y defect only. | GPS-visible rows approach thousands, mobile map trace shows jank, or map clustering project approved. |
| AGG-C21-12 | Medium / High | `apps/web/src/components/on-this-day-widget.tsx:10-22`; `apps/web/src/app/[locale]/(public)/page.tsx:232-235`; `apps/web/src/lib/data-timeline.ts:102-130`; `apps/web/src/db/schema.ts:123-131` | Generated month/day columns require a schema/product performance migration. Cycle 21 schedules deterministic datetime parsing, not a new query shape. | Measured home latency, corpus growth makes the scan costly, or next schema/index cycle folds the generated columns. |
| AGG-C21-13 | Medium / High | `apps/web/src/app/actions/public.ts:248-317`; `apps/web/src/lib/data.ts:1574-1737`; `apps/web/src/lib/smart-collections.ts:221-267` | Search-index redesign is product/performance work. Current request counts are rate-limited; no live latency evidence was gathered. | Search latency rises, corpus grows materially, or full-text/search-index cycle approved. |
| AGG-C21-14 | Low / High | `apps/web/src/lib/clip-embeddings.ts:36-235`; `apps/web/src/db/schema.ts:292-304`; `apps/web/src/app/api/search/semantic/route.ts:263-311`; `apps/web/src/app/api/search/similar/[id]/route.ts:178-214` | Bounded residual risk behind semantic mode gates/scan limits. A vector index or worker-thread redesign is larger than this cycle. | Semantic traffic/gallery size exceeds scan limits, measured CPU/RSS pressure, or vector-index/cache project approved. |
| AGG-C21-16 | Medium / Medium | `apps/web/src/lib/data.ts:49-63`; `apps/web/src/lib/data.ts:1392-1407`; `apps/web/src/lib/data.ts:1830-1834`; `apps/web/src/app/[locale]/(public)/g/[key]/page.tsx:111-142`; `apps/web/src/app/actions/public.ts:517-559` | Shared-group view counts are approximate analytics per `CLAUDE.md`; refactor should ride shared-group cache/analytics work. | Counter drift bug report, shared-group cache refactor, or analytics consistency cycle. |
| AGG-C21-19 | Medium / High-Medium | `CLAUDE.md:247`; `CLAUDE.md:510-522`; `apps/web/nginx/default.conf:1-306`; `apps/web/deploy.sh:51-108`; `scripts/deploy-remote.sh:31-93` | Operator/live-host validation. `CLAUDE.md` explicitly says deploys do not apply host nginx. | Operator applies/verifies nginx zones, proxy topology changes, or app-layer public page limiter project approved. |
| AGG-C21-20 | Medium / High-Medium | `apps/web/docker-compose.yml:15-23`; `apps/web/src/lib/rate-limit.ts:175-214`; `apps/web/nginx/default.conf:20-71`; `CLAUDE.md:97-98`; `CLAUDE.md:748` | Operator topology validation item. Correct action requires live request traces and proxy configuration authority outside repo edits. | Proxy topology changes, shared-IP false-positive incident, or operator runs/verifies topology checker. |
| AGG-C21-21 | Medium / High-Medium | `CLAUDE.md:244-248`; `apps/web/src/lib/single-writer-guard.ts:6-235`; `apps/web/src/lib/rate-limit.ts:299-427`; `apps/web/src/lib/pending-session-revocations.ts:17-24`; `apps/web/src/lib/admin-mutation-barrier.ts:6-31`; `apps/web/src/lib/restore-drain-checklist.ts:10-17` | `CLAUDE.md` documents single-instance topology and process-local state. Fail-closed singleton semantics require product/operator decision. | Scale-out is attempted, singleton contention incident occurs, or shared-state/fail-closed topology project approved. |
| AGG-C21-22 | Low / High for plaintext-at-rest; High / High for DB-only restore boundary | `CLAUDE.md:226-227`; `apps/web/src/app/api/admin/db/download/route.ts:21-89`; `apps/web/src/app/[locale]/admin/db-actions.ts:420-955`; `apps/web/src/app/[locale]/admin/(protected)/db/page.tsx:177-245`; `apps/web/messages/en.json:21-26`; `apps/web/docker-compose.yml:24-32` | `CLAUDE.md` explicitly documents plaintext DB backups as host/storage encryption boundary and restore as DB-only. Changing this is an operator/product backup architecture project. | Backup encryption requirement, filesystem rollback product decision, or restore/file integrity incident. |
| AGG-C21-23 | Low / High | `CLAUDE.md:248`; `CLAUDE.md:649-650`; `apps/web/src/app/actions/auth.ts:79-253`; `apps/web/src/lib/api-auth.ts:66-152` | Product/security model decision; repo history explicitly defers 2FA/WebAuthn and has no role/capability model. | Admin compromise risk posture changes, step-up auth project approved, or roles/capabilities are added. |
| AGG-C21-24 | Medium / High | `apps/web/src/lib/advisory-locks.ts:10-72`; `apps/web/src/lib/image-queue.ts:752-780` | Multi-gallery co-location is outside the documented single-deployment topology. Prefixing every advisory lock needs a coordinated lock migration. | Shared-MySQL multi-gallery support becomes a product requirement, or lock collision incident occurs. |
| AGG-C21-26 | Medium / High | `apps/web/src/__tests__/search-stale-response.test.ts:1-35`; `apps/web/src/__tests__/load-more-source-contracts.test.ts:7-30`; `apps/web/src/__tests__/semantic-scan-limit-source.test.ts:42-77`; `apps/web/src/components/search.tsx:163-281`; `apps/web/src/components/load-more.tsx:43-111` | Broad client test-harness work. Cycle 21 fixes direct browser-backed i18n/a11y defects first. | Next high-risk client source-contract false confidence, or jsdom/RTL behavior-test cycle approved. |
| AGG-C21-27 | Medium / High | `apps/web/playwright.config.ts:72-77`; `.github/workflows/quality.yml:75-80`; `apps/web/src/components/register-service-worker.tsx:13-23`; `apps/web/src/__tests__/sw-template-contract.test.ts:1-16` | Browser matrix expansion affects CI runtime/fixture stability and needs a test strategy decision. | Browser-specific regression, PWA/SW incident, or test-matrix project approved. |
| AGG-C21-28 | Low-Medium / High | `apps/web/e2e/nav-visual-check.spec.ts:40-86` | Screenshot baselines can be noisy without a deterministic fixture/theme pass. | Visual regression budget approved, or nav visual bug escapes existing checks. |
| AGG-C21-29 | Low-Medium / Medium | `apps/web/e2e/hydration-photo-page.spec.ts:20-49` | Flakiness hardening; no current failure reproduced. | Hydration spec flakes on `networkidle`, or photo-page test refactor touches this file. |
| AGG-C21-37 | Medium / High | `apps/web/src/app/[locale]/admin/(protected)/dashboard/dashboard-client.tsx:135-144`; `apps/web/src/components/image-manager.tsx:427-591` | Admin responsive workbench redesign is product/UX scope and should be paired with broader admin IA work. | Mobile/tablet admin operation becomes priority, wrong-row action report, or admin redesign cycle. |
| AGG-C21-38 | Low-Medium / High | `apps/web/src/components/admin-nav.tsx:15-49`; `apps/web/src/components/admin-header.tsx:13-26` | Admin IA redesign requires product decisions about workflow grouping and sensitive operation separation. | Admin nav redesign cycle, sensitive-operation IA concern, or user report of admin navigation confusion. |
| AGG-C21-39 | Low / Medium | `apps/web/src/components/masonry-card.tsx:149-155` | Low-risk public gallery presentation preference. Changing metadata placement needs visual/browser iteration. | Photographer/user report that mobile overlays obscure important crops, or masonry-card visual redesign cycle. |
| AGG-C21-40 | Medium / High | `apps/web/src/site-config.json:2-10`; `README.md:60-77`; `README.md:121-122`; `apps/web/README.md:15-20`; `apps/web/scripts/ensure-site-config.mjs:12-42`; `apps/web/src/app/sitemap.ts:14-18`; `apps/web/src/app/[locale]/layout.tsx:15-26` | This checkout is the configured production deployment for `gallery.atik.kr`, and the user explicitly chose that target for this loop. Changing committed site-config semantics would be a distribution/product decision. | Before distributing GalleryKit as a template/package, or if copied installs publish wrong canonical URLs. |

## Scheduled, Not Deferred

Scheduled in `cycle-21-2026-07-08-plan.md`: `AGG-C21-01`, `AGG-C21-02`, `AGG-C21-06`, `AGG-C21-15`, `AGG-C21-17`, `AGG-C21-18`, `AGG-C21-25`, `AGG-C21-30`, `AGG-C21-31`, `AGG-C21-32`, `AGG-C21-33`, `AGG-C21-34`, `AGG-C21-35`, and `AGG-C21-36`.

## Age-Budget Check

Newly deferred High-severity Cycle 21 rows (`AGG-C21-03`, `AGG-C21-04`, `AGG-C21-05`, `AGG-C21-07`, `AGG-C21-08`, `AGG-C21-09`) are broad architecture/performance/test-infrastructure items with explicit exit criteria, not direct unpatched authz/security/data-loss bugs with contained local fixes. Direct contained correctness/data-loss findings are scheduled. Topology and edge-security rows cite the documented single-instance and host-nginx operator boundaries in `CLAUDE.md`.
