# Cycle 43/100 Implementation Plan

Start HEAD: `82a21b82`.
Review aggregate: `.context/reviews/cycle-43-2026-07-01/_aggregate.md`.

## Goal

Close the Cycle 43 lint-gate proof gaps and context drift without schema changes, dependency changes, or production-runtime behavior changes.

## Scheduled Fixes

1. `C43-01` / `PA-43-01`: make `lint:action-origin` reject public analytics limiter helper shadowing from exported action parameters, not only declarations inside the action body.
2. `TV-C43-01`: make `lint:action-origin` and `lint:public-route-rate-limit` reject trusted guard/rate-limit calls when a local binding shadows the approved imported identifier.
3. `TV-C43-02`: tighten read-only `@action-origin-exempt` protected-read proof so fake auth imports and non-dominating auth calls cannot satisfy the scanner.
4. `DOC-C43-01`: update Cycle 42 plan/index state to show source commit/push complete while preserving that committed deploy evidence was not found.

## Deferred This Cycle

No new Cycle 43 findings are deferred. Prior deferred items are carried forward in `.context/plans/cycle-43-2026-07-01-deferred.md`.

## Regression Coverage

- Extend `check-action-origin.test.ts` for exported-action parameter shadowing of public analytics limiters.
- Extend `check-action-origin.test.ts` for approved `requireSameOriginAdmin` imports shadowed by a local binding.
- Extend `check-action-origin.test.ts` for ignored/conditional/fake auth read-only exemption proofs.
- Extend `check-public-route-rate-limit.test.ts` for approved rate-limit imports shadowed by route-handler parameters and local bindings.

## Validation

Run the required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then sign, `git pull --rebase`, push, and deploy with `npm run deploy` from the repo root.

## Progress

- [x] Review fan-out complete.
- [x] Aggregate/deferred/plan artifacts written.
- [x] Implement scheduled fixes.
- [x] Add focused regression tests.
- [x] Run gates.
- [x] Commit, push, deploy. Cycle 44 invocation evidence states `f417d86b` was the current deployed `master` HEAD at the start of Cycle 44.
