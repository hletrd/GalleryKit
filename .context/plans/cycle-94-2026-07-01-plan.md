# Cycle 94/100 Implementation Plan

Start HEAD: `33eca7b5e4102bd5097777dbb926ee2cb94c6d71`.
Review aggregate: `.context/reviews/cycle-94-2026-07-01/_aggregate.md`.

## Goal

Close safe, narrow Cycle 94 findings that can be handled with release-ledger updates and token-admin accessibility/error-state fixes. Preserve broader route-coverage, E2E, schema, restore, performance, keyboard interaction, and mobile-admin redesign work in the deferred ledger.

## Scheduled Fixes

1. `C94-01`: close the stale Cycle 93 terminal ledger by recording that Cycle 94 started from user-provided deployed `master` HEAD `33eca7b5e4102bd5097777dbb926ee2cb94c6d71`, and update `.context/plans/README.md` / latest aggregate state.
2. `C94-02`: update `apps/web/src/app/actions/lr-tokens.ts` and `apps/web/src/app/[locale]/admin/(protected)/tokens/tokens-client.tsx` so server-side invalid Lightroom token labels return structured label metadata and render through the existing inline field alert. Add source-contract coverage in `apps/web/src/__tests__/client-source-contracts.test.ts`.
3. `C94-03`: update `apps/web/src/app/[locale]/admin/(protected)/tokens/tokens-client.tsx` so token-list load failures render a persistent retryable alert instead of falling through to the empty state. Add source-contract coverage in `apps/web/src/__tests__/client-source-contracts.test.ts`.

## Deferred This Cycle

All non-scheduled confirmed findings are recorded in `.context/plans/cycle-94-2026-07-01-deferred.md`. No review finding is intentionally dropped.

## Validation

Run focused tests/checks first:

- `npm test --workspace=apps/web -- --run src/__tests__/client-source-contracts.test.ts`

Then run required gates in exact order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `BASE_URL=https://gallery.atik.kr npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit. If changes are committed, create a GPG-signed Conventional Commit with gitmoji and Lore trailers, `git pull --rebase`, push, deploy with `npm run deploy`, and smoke:

- `curl -fsSIL https://gallery.atik.kr`
- `curl -fsS https://gallery.atik.kr/api/health`

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run focused tests.
- [x] Run required gates.
- [x] Commit, pull --rebase, push.
- [x] Deploy with `npm run deploy`.
- [x] Smoke production endpoints.

## Gate Evidence

- Focused test: `npm test --workspace=apps/web -- --run src/__tests__/client-source-contracts.test.ts src/__tests__/lr-tokens-action.test.ts` - pass: 2 files passed, 25 tests passed.
- `npm run lint --workspace=apps/web` - pass.
- `npm run lint:api-auth --workspace=apps/web` - pass: both admin API routes OK.
- `npm run lint:action-origin --workspace=apps/web` - pass: all mutating server actions enforce same-origin provenance.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass: all scanned public route handlers covered or explicitly exempt.
- `npm run typecheck --workspace=apps/web` - pass: route types generated, app/scripts typechecks clean, 8 JavaScript script files checked.
- `BASE_URL=https://gallery.atik.kr npm run build --workspace=apps/web` - pass. Local sitemap generation logged the expected unavailable local MySQL fallback and exited successfully.
- `npm test --workspace=apps/web` - pass: 302 files passed, 2 skipped; 2844 tests passed, 4 skipped.
- Terminal release evidence: Cycle 95 started from user-provided deployed `master` HEAD `750729ada2403c0c01267670b9552a05e0ead217`; `git log --show-signature -1` reported a good GPG signature for `fix(tokens): ♿ surface token admin validation failures`; pre-change smoke on 2026-07-01 UTC returned `HTTP/2 307` then `HTTP/2 200` for `https://gallery.atik.kr` and `{"status":"ok"}` for `/api/health`.

## Gate Fix Iterations

- Fixed one ESLint error introduced during implementation by moving initial token loading away from synchronous state resets inside the mount effect path.
- Updated five LR token action test assertions to lock the new structured `{ field: 'label' }` invalid-label response.
