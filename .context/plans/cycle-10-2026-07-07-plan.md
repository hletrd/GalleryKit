# Run-10 Cycle 10/100 Implementation Plan

Date: 2026-07-07
Start HEAD: `09a0dcd3c6452f8a0fd9d76525b44ef863b2387d`
Review aggregate: `.context/reviews/_aggregate.md`

## Scope

This plan schedules concrete local fixes from the cycle-10 aggregate and records every intentionally unscheduled finding in `cycle-10-2026-07-07-deferred.md`. Security, correctness, and data-loss findings are scheduled here unless the deferred register quotes a repository rule that makes the work operator-owned or upstream-blocked.

No existing active plan was archived during Prompt 2. Cycle 9's plan is already marked complete except for its pending commit/deploy ledger, and its remaining work is represented by its deferred register.

## Work Packages

### WP1 - Preserve cycle-10 review and plan provenance

- Covers: Prompt 1 provenance and Prompt 2 plan output.
- Files likely touched: `.context/reviews/_aggregate.md`, `.context/reviews/cycle10-2026-07-07/*.md`, `.context/plans/cycle-10-2026-07-07-plan.md`, `.context/plans/cycle-10-2026-07-07-deferred.md`, `.context/plans/README.md`.
- Plan:
  1. Commit the untracked review artifacts that were left by reviewer-agent hook conflicts.
  2. Commit this plan/deferred pair and update the plan index so cycle-10 is discoverable.
  3. Use signed git plumbing if the local commit hook again demands a forbidden co-author trailer.
- Progress: pending.

### WP2 - Repair dependency and native-build drift

- Covers: AGG-C10-01, AGG-C10-02.
- Files likely touched: `package.json`, `package-lock.json`, `apps/web/Dockerfile`, `apps/web/src/__tests__/deploy-script-contract.test.ts`, dependency/source-contract tests.
- Plan:
  1. Re-check the current stable Next/PostCSS remediation path with npm registry/audit evidence before changing dependency metadata.
  2. Try a targeted npm override for Next's nested PostCSS or the smallest stable Next-family update that removes `next/node_modules/postcss@8.4.31`; do not use `npm audit fix --force`.
  3. Update Docker native SWC install pins or derive them from the lockfile so the Dockerfile cannot lag `package-lock.json`.
  4. Strengthen deploy contract coverage to compare exact native package versions against the lockfile.
- Progress: implemented/deferred split. Docker native SWC pins now match the lockfile and the deploy contract compares exact locked versions. The nested Next/PostCSS audit remains upstream/tooling-blocked after current-registry verification and two npm override attempts; see `DEF-C10-01-UPSTREAM`.

### WP3 - Make hot archive date predicates sargable

- Covers: AGG-C10-03.
- Files likely touched: `apps/web/src/lib/data-timeline.ts`, `apps/web/src/__tests__/data-timeline*.test.ts`.
- Plan:
  1. Replace public timeline/year `YEAR()`/`MONTH()` filters with equivalent inclusive/exclusive capture-date ranges.
  2. Preserve current null-date and sort behavior.
  3. Add source or behavior coverage rejecting date-function predicates on the public archive hot path.
- Progress: implemented. `getTimelineImages` now builds inclusive/exclusive `capture_date` ranges instead of `YEAR()`/`MONTH()` predicates, with source coverage.

### WP4 - Align embedding schema type with the physical migration

- Covers: AGG-C10-06.
- Files likely touched: `apps/web/src/db/schema.ts`, `apps/web/src/__tests__/semantic-embedding-storage-contract.test.ts`, `apps/web/src/__tests__/migrate-reconcile-coverage.test.ts`.
- Plan:
  1. Replace the Drizzle `text("embedding")` approximation with a MySQL custom binary type that emits `mediumblob`.
  2. Keep application encode/decode behavior unchanged.
  3. Add a contract test pinning Drizzle schema, migration SQL, and legacy reconcile metadata to the same `MEDIUMBLOB` invariant.
- Progress: implemented. `image_embeddings.embedding` now uses a Drizzle MySQL `mediumblob` custom type, and write/read call sites are typed around raw `Buffer` values.

### WP5 - Strengthen legacy reconcile structural coverage

- Covers: AGG-C10-07.
- Files likely touched: `apps/web/src/__tests__/migrate-reconcile-coverage.test.ts`, possibly `apps/web/scripts/migrate.js`.
- Plan:
  1. Extend the existing name-only source tripwire to cover high-risk structural details: embedding binary type, important defaults/nullability, index column order, and migration journal parity.
  2. Keep the test hermetic unless the repo already has a documented disposable MySQL path available this cycle.
  3. Do not introduce schema changes unless the coverage exposes a confirmed mismatch.
- Progress: implemented. Added high-risk structural pins for `image_embeddings` binary storage and model-version scan index shape in the reconcile coverage suite.

### WP6 - Add production lifecycle ownership for maintenance sweeps

- Covers: AGG-C10-12.
- Files likely touched: `apps/web/src/lib/maintenance-scheduler.ts`, `apps/web/src/instrumentation.ts`, `apps/web/src/__tests__/maintenance-scheduler-source.test.ts`.
- Plan:
  1. Add a production-facing `stopMaintenanceScheduler({ timeoutMs })` path that stops future ticks and awaits active/in-flight sweeps up to a bounded timeout.
  2. Wire instrumentation shutdown to call it alongside shared-group flushes.
  3. Preserve test-only reset helpers and add source/behavior coverage for shutdown wiring.
- Progress: implemented. Added `stopMaintenanceScheduler({ timeoutMs })`, wired graceful shutdown to stop/drain it, and covered the source contract.

### WP7 - Fix analytics view recording request-context leakage

- Covers: AGG-C10-17.
- Files likely touched: `apps/web/src/app/actions/public.ts`, `apps/web/src/__tests__/public-actions.test.ts`, `apps/web/src/lib/background-db-writes.ts`.
- Plan:
  1. Capture headers and build plain view-record parameters before enqueueing the background write.
  2. Run the rate-limit/admission guard before adding work to the analytics queue.
  3. Ensure queued callbacks no longer call request-scoped `headers()` and add regression coverage for the over-limit fast path.
- Progress: implemented. Analytics recorders now capture headers/build params and run the view-record admission guard before queueing background DB work; queued callbacks use only plain captured data.

### WP8 - Fail closed on malformed smart-collection predicates during topic deletion

- Covers: AGG-C10-18.
- Files likely touched: `apps/web/src/app/actions/topics.ts`, `apps/web/messages/*.json`, `apps/web/src/__tests__/topics-actions.test.ts`.
- Plan:
  1. Treat malformed `smart_collections.query_json` as an unsafe dependency check failure when deleting a topic.
  2. Return a localized admin-facing error instead of deleting through the ambiguity.
  3. Add regression coverage for malformed smart-collection JSON blocking topic deletion.
- Progress: implemented. Malformed smart collection predicates now block topic deletion with localized English/Korean errors and regression coverage.

### WP9 - Improve public archive/search accessibility and result context

- Covers: AGG-C10-15, AGG-C10-16.
- Files likely touched: `apps/web/src/app/[locale]/(public)/timeline/page.tsx`, `apps/web/src/app/[locale]/(public)/year/[year]/page.tsx`, `apps/web/src/lib/data.ts`, `apps/web/src/components/search.tsx`, related tests.
- Plan:
  1. Make repeated archive photo-link labels unique by appending stable photo identifiers or reusing the masonry accessible title pattern.
  2. Return public-safe tag display context for search results and use the same display-title fallback used by gallery cards.
  3. Add focused tests/source contracts for unique archive labels and tag-backed search-result labels.
- Progress: implemented. Timeline/year archive link labels append stable photo ids, and keyword search results carry tag context for tag-derived labels.

### WP10 - Correct stale documentation claims

- Covers: AGG-C10-19, AGG-C10-20.
- Files likely touched: `.omc/wiki/schema-derived-list-drift-migration-reconcile-lesson.md`, `.omc/wiki/clip-semantic-search-us-p51.md`, `.omc/wiki/gallerykit-architecture-overview.md`, docs contract tests if present.
- Plan:
  1. Rewrite the migration lesson to describe the current pending-migration versus drift-reconcile split.
  2. Replace wiki wording that implies CLIP semantic search is currently live with the repository's operator-enabled/runbook wording.
  3. Add or update doc-source contract coverage if an adjacent test already validates these claims.
- Progress: deferred after implementation discovery. The cited `.omc/wiki/*` pages are ignored runtime/wiki state, and `.gitignore` excludes `.omc`; prior plan history flags tracked `.omc` content as an artifact problem. Authoritative tracked docs (`CLAUDE.md`, `README.md`, `apps/web/README.md`) already carry current migration/CLIP wording, so the ignored-wiki cleanup is recorded as `DEF-C10-19` and `DEF-C10-20`.

## Deferred Register

See `cycle-10-2026-07-07-deferred.md`. Deferred findings: AGG-C10-01 residual, AGG-C10-04, AGG-C10-05, AGG-C10-08, AGG-C10-09, AGG-C10-10, AGG-C10-11, AGG-C10-13, AGG-C10-14, AGG-C10-19, AGG-C10-20, plus any WP-scoped residuals explicitly marked there after implementation evidence.

## Required Gates

Run all cycle gates after implementation:

- `npm run lint --workspace=apps/web`
- `npm run lint:api-auth --workspace=apps/web`
- `npm run lint:action-origin --workspace=apps/web`
- `npm run lint:public-route-rate-limit --workspace=apps/web`
- `npm run typecheck --workspace=apps/web`
- `BASE_URL=https://gallery.atik.kr npm run build --workspace=apps/web`
- `npm test --workspace=apps/web`
- `npm run test:e2e --workspace=apps/web` when browser-flow coverage is required

## Completion Ledger

- Prompt 1 aggregate: complete.
- Prompt 2 plan/deferred: complete.
- Prompt 3 implementation: complete for scheduled work; residual upstream/ignored-wiki items are recorded in `cycle-10-2026-07-07-deferred.md`.
- Focused tests: `npm test --workspace=apps/web -- --run src/__tests__/deploy-script-contract.test.ts src/__tests__/data-timeline.test.ts src/__tests__/semantic-embedding-storage-contract.test.ts src/__tests__/migrate-reconcile-coverage.test.ts src/__tests__/maintenance-scheduler-source.test.ts src/__tests__/public-actions.test.ts src/__tests__/rate-limit-saturated-fast-path.test.ts src/__tests__/topics-actions.test.ts src/__tests__/photo-title-stub-prefix-strip.test.ts src/__tests__/data-tag-names-sql.test.ts src/__tests__/client-source-contracts.test.ts src/__tests__/cycle-10-source-contracts.test.ts` - pass, 12 files / 221 tests.
- Additional focused regression tests: `npm test --workspace=apps/web -- --run src/__tests__/photo-title.test.ts src/__tests__/photo-title-stub-prefix-strip.test.ts src/__tests__/tracked-secrets.test.ts` - pass, 3 files / 38 tests; `npm test --workspace=apps/web -- --run src/__tests__/check-action-origin.test.ts src/__tests__/public-actions.test.ts src/__tests__/cycle-10-source-contracts.test.ts` - pass, 3 files / 130 tests.
- Gates:
  - `npm run lint --workspace=apps/web` - pass.
  - `npm run lint:api-auth --workspace=apps/web` - pass.
  - `npm run lint:action-origin --workspace=apps/web` - pass after updating the checker for pre-queue public analytics admission.
  - `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
  - `npm run typecheck --workspace=apps/web` - pass.
  - `BASE_URL=https://gallery.atik.kr npm run build --workspace=apps/web` - pass; build logged the existing local-DB-unavailable sitemap fallback and completed.
  - `npm test --workspace=apps/web` - pass, 343 files / 3171 tests; 2 files / 4 tests skipped by existing policy.
  - `E2E_ADMIN_ENABLED=false E2E_PORT=3102 DB_HOST=127.0.0.1 DB_PORT=33317 DB_USER=root DB_NAME=gallery_e2e_cycle10 ... npm run test:e2e --workspace=apps/web` - pass, 38 passed / 9 skipped. This used a new local MySQL 8.4 container `gallerykit-e2e-mysql-cycle10-33317` on 127.0.0.1:33317 to avoid touching the leftover cycle-7 container. Cleanup is intentionally not performed without explicit destructive-action confirmation.
- Commit/push: pending.
- Deploy: pending per `DEPLOY_MODE=per-cycle`.
