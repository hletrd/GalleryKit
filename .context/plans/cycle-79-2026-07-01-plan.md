# Cycle 79/100 Implementation Plan

Start HEAD: `9cc143d06f3b4f9fe1862316c0f449f745926829`.
Review aggregate: `.context/reviews/cycle-79-2026-07-01/_aggregate.md`.

## Goal

Close the safe Cycle 79 review findings, keep the review/plan ledgers current, run all required gates, commit/push all Cycle 79 work, and deploy with `npm run deploy` from the repo root.

## Scheduled Fixes

1. `C79-01`: harden `apps/web/scripts/check-public-route-rate-limit.ts` so expensive public GET/HEAD detection catches namespace/property-access marker calls and expensive `sharp`/filesystem imports under default, named alias, or namespace bindings; add focused fixtures.
2. `C79-02`: close the stale Cycle 78 ledger by recording commit/push/deploy evidence for `9cc143d0`, moving Cycle 78 out of the active plan index, and keeping the new Cycle 79 plan artifacts unignored/committable.
3. `C79-03`: correct the runner-stage Dockerfile comment so the `prod-deps` copy is documented as supporting both migration scripts and runtime external packages.

## Deferred This Cycle

No new Cycle 79 findings are deferred. All deduplicated findings are scheduled.

Carry-forward deferred items remain in `.context/plans/cycle-79-2026-07-01-deferred.md`.

## Regression Coverage

- Scanner fixtures: namespace `ImageResponse`, default-aliased `sharp`, namespace `node:fs/promises`, and named-aliased filesystem reads must be classified as expensive GET work unless an approved rate-limit gate runs first.
- Docker contract: the runtime prod-deps comment must not describe the copied node_modules tree as migration-only.
- Ledger: Cycle 78 plan/index must no longer present Cycle 78 as active or undeployed, and `git status` must show the Cycle 79 plan/deferred artifacts as committable.

## Validation

Run required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then signed commit, `git pull --rebase`, push, and deploy with `npm run deploy`.

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run required gates.
- [x] Commit, pull --rebase, push.
- [x] Deploy with `npm run deploy`.

## Gate Evidence

- Focused regression: `npm test --workspace=apps/web -- --run src/__tests__/check-public-route-rate-limit.test.ts src/__tests__/deploy-script-contract.test.ts` - pass: 2 files passed, 105 tests passed.
- Manual scanner probes: default-aliased `sharp` and namespaced `node:fs/promises` GET handlers now fail closed as expensive unmetered reads.
- Post-change verifier: initial pass requested changes because Cycle 79 plan files were still ignored; `.gitignore` now explicitly unignores both Cycle 79 plan artifacts.
- `npm run lint --workspace=apps/web` - pass.
- `npm run lint:api-auth --workspace=apps/web` - pass: both admin API routes OK.
- `npm run lint:action-origin --workspace=apps/web` - pass: all mutating server actions enforce same-origin provenance.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
- `npm run typecheck --workspace=apps/web` - pass.
- `npm run build --workspace=apps/web` - pass. Local sitemap generation logged the expected unavailable local MySQL fallback and exited successfully.
- `npm test --workspace=apps/web` - pass: 301 files passed, 2 skipped; 2823 tests passed, 4 skipped.
- Commit/push evidence: signed commit `8c4999c9` (`fix(security): 🐛 harden public route scanner`) is current `master` and `origin/master` at Cycle 80 start.
- Deploy evidence: Cycle 80 invocation states the current deployed `master` HEAD at start is `8c4999c9`, so Cycle 79's pushed scanner hardening was deployed before Cycle 80 began.

## Gate Fix Iterations

0 so far.
