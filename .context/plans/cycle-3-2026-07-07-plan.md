# Run-10 Cycle 3/100 Implementation Plan

Start HEAD: `e08b6f97`. Review aggregate: `.context/reviews/cycle-3-2026-07-07/_aggregate.md`
(mirrored to `.context/reviews/_aggregate.md`). 12 lanes; 38 deduped findings (C3-01..C3-38).
Deferred register: `cycle-3-2026-07-07-deferred.md`. No mandatory carry-over: cycle-2's deploy
succeeded and production was verified healthy (`e08b6f97`).

Repo rules consulted: `CLAUDE.md`, `AGENTS.md`, `.context/plans/README.md` (age-budget +
C1-32 drainage policies), prior deferred registers. Commit policy: GPG-signed (-S),
conventional + gitmoji, fine-grained, `git pull --rebase` before push, no `--no-verify`.
Gates (all blocking): eslint, typecheck, vitest, build, lint:api-auth, lint:action-origin,
lint:public-route-rate-limit, playwright e2e. DEPLOY_MODE per-cycle (`npm run deploy` from
repo root, exactly once, after gates green).

## Status legend
[ ] pending · [~] in progress · [x] done (commit noted) · [defer] moved to deferred register

## WP1 — [x] C3-01 (MED-HIGH/High, reproduced): migrate.js mixed-case batch swallow
Files: `apps/web/scripts/migrate.js`, `apps/web/src/__tests__/migrate-pending-migrations.test.ts`, `CLAUDE.md`.
- In the drift path of `prepareLegacyDatabaseIfNeeded`, baseline ONLY true-drift entries
  (`folderMillis <= cursor`); leave above-cursor pending entries un-baselined so
  `drizzle.migrate()` genuinely applies their SQL (DBG3-02/TRC3-04 fix shape). Never
  silently record a hash whose SQL did not run.
- Replace the "swallowed tail" warn with an explicit log: above-cursor entries are left
  for drizzle to apply; if reconcile already mirrored their DDL, the deploy fails LOUD
  (ER_DUP) instead of silently dropping DML — document that trade in the runbook
  (CRIT3-03's fail-loud principle preserved on the ambiguous branch).
- Add the mixed-batch regression test (3+ entries straddling the cursor; assert
  above-cursor entries are NOT baselined; below-cursor entries are).
- CLAUDE.md runbook: correct the mixed-case description; add the ARCH3-05 "migrations
  MUST be DDL-only for reconcile-mirroring purposes; DML rides only the drizzle-apply
  path" invariant note (doc half of C3-35).

## WP2 — [x] C3-02 + C3-03 (MED/High): single-writer guard keepalive, namespacing, cry-wolf
Files: `apps/web/src/lib/single-writer-guard.ts`, `apps/web/src/lib/advisory-locks.ts`,
`apps/web/src/__tests__/single-writer-guard.test.ts`, `CLAUDE.md`.
- Keepalive: periodic `SELECT 1` (60 s, `.unref()`d, cleared in `stopSingleWriterGuard`)
  on the held connection so the lock survives `wait_timeout`; on keepalive failure, warn
  once (lapse now detected AT failure time) and stop the timer.
- Namespace the lock per database (e.g. `gallerykit_web_singleton_<sha256(DB_NAME).slice(0,16)>`,
  staying under MySQL's 64-char lock-name cap for any DB name); fixes the multi-tenant
  false alarm + guardless-second-instance defeat (CRIT3-04).
- Cry-wolf: on initial `GET_LOCK` failure, re-probe once after ~25 s (unref'd) and only
  then log the LOUD error — absorbs the rolling-deploy drain overlap.
- Update the guard test; CLAUDE.md: fix the "holds it for the process lifetime" wording to
  describe the keepalive mechanism; add the (namespaced) lock to the advisory-lock scope
  list with its DB-scoped exception.

## WP3 — [x] C3-04 (MED/High): admin-backfill-runner detached cached-config read
Files: `apps/web/src/lib/admin-backfill-runner.ts:691`, test.
- One-line swap `getGalleryConfig()` → `getGalleryConfigUncached()`; extend
  `image-queue-uncached-config-wiring.test.ts` (or add a sibling contract test) to pin the
  backfill runner's detached call site too, so the next sibling can't regress silently.

## WP4 — [ ] C3-05 (MED/High, live-reproduced): 404 head metadata (conflicting robots, generic title)
Files: `apps/web/src/app/[locale]/layout.tsx`, `apps/web/src/app/[locale]/not-found.tsx`,
`apps/web/e2e/not-found-status.spec.ts`, i18n messages if a new title key is needed.
- Remove the explicit `robots: { index: true, follow: true }` from the locale layout's
  `generateMetadata` — Next elides the tag for the default case on valid pages (verified
  live by DES3-01), so valid pages are unchanged and 404 pages stop emitting the
  conflicting `index, follow` tag (framework-injected `noindex` becomes authoritative).
- Check Next 16 docs for whether `not-found.tsx` supports metadata exports; if yes, add a
  localized not-found title + explicit `robots: { index: false, follow: false }`; if not,
  record the title half honestly (status-code + single noindex is the shipped fix).
- Extend the e2e to assert: exactly one robots meta (noindex) and no `index, follow` tag
  on 404 responses; keep the 200-control assertion.

## WP5 — [ ] C3-10 + C3-22 (MED/Medium): SW touchMeta durability + size-0 LRU entries
Files: `apps/web/public/sw.template.js`, `apps/web/src/lib/sw-cache.ts`,
`apps/web/src/__tests__/sw-cache.test.ts` + `sw-template-contract.test.ts`, regenerate `public/sw.js`.
- `await touchMeta(...)` on the 304 and same-ETag branches (inside `respondWith`'s promise
  chain → SW lifetime covers the write; fixes both the termination-drop and the silent
  meta-write-failure freeze without `waitUntil` plumbing). Keep `.catch` so a failure
  still serves the cached bytes.
- Size-0 fix: when no prior meta exists and `Content-Length` is absent/0, fall back to
  the actual cached-body size before recording (never store a 0-size entry for a
  non-empty body). Mirror in `sw-cache.ts`; update contract tests; regenerate + commit `sw.js`.

## WP6 — [ ] C3-13 + C3-14 (MED/High): photo-navigation stale swipe visuals + missing gesture test
Files: `apps/web/src/components/photo-navigation.tsx`, e2e or targeted test.
- Reset: call `applySwipeVisuals(0, ...)` in the two success branches of `handleTouchEnd`,
  AND add a `useLayoutEffect` keyed on `[prevId, nextId]` that re-asserts resting styles
  (covers any in-place photo switch path — the shared-view `setCurrentImageId` case
  DBG3-01 reproduced by mechanism).
- Test: prefer an e2e touch-event spec asserting indicator opacity returns to 0 after a
  successful swipe (shared-group fixture if the e2e seed provides one; otherwise assert
  the reset on the snap-back + threshold paths on `/p/[id]` and pin the reset-on-id-change
  effect with a focused contract assertion). Honest scoping note in the commit if the
  shared-view path can only be pinned by contract.

## WP7 — [ ] C3-07 (MED/Med-High): embedding-bootstrap cursor starvation
Files: `apps/web/src/lib/image-queue.ts`, matching test.
- Persist the embedding-scan cursor on the processing-queue state (advance across
  invocations; reset to 0 only when a scan completes without hitting the cap), so a
  permanently-un-embeddable prefix ≥ `SEMANTIC_SCAN_LIMIT` cannot starve newer rows.
  Keep the per-invocation cap semantics and continuation log.
- Test: simulate a stuck prefix at the cap and assert the next invocation resumes past it.

## WP8 — [ ] C3-06 (MED/Medium, contested): defensive copy of retained embedding view
Files: `apps/web/src/app/api/search/similar/[id]/route.ts:160`, `apps/web/src/lib/clip-embeddings.ts` comment.
- `targetEmbedding = new Float32Array(decoded)` (one 512-float copy, off the hot loop);
  removes all dependence on mysql2 buffer-lifetime internals (CR3-01 vs tracer
  disagreement resolved by hardening; C1-31 class). Tighten the zero-copy comment: result
  is safe ONLY for immediate synchronous consumption; MUST copy before holding across DB I/O
  (also the PERF3-04 constraint rider for the deferred C2-14b cache).

## WP9 — [ ] C3-15 + C3-16 + C3-20 + C3-21 (MED..LOW): image-queue operability batch
Files: `apps/web/src/lib/image-queue.ts`, `apps/web/src/lib/gallery-config.ts`, `CLAUDE.md`, tests.
- `console.warn` when `QUEUE_CONCURRENCY` is clamped below the requested value (parity
  with the backfill runner); CLAUDE.md: document the pool-budget clamp formula + effective
  cap (2 at the shipped pool of 10) on the env-var row and pipeline step 3 (C3-15/DOC3-01).
- Fix the "escalating up to 25s" comment → 10 s (MAX_RETRIES=3) (C3-21).
- Track per-job retry `setTimeout`s on state, clear them in the defensive re-init
  alongside `gcInterval`, and clear them at shutdown (C3-20/ARCH3-04).
- Short-TTL (~2 s) module-level micro-cache + inflight dedupe inside
  `getGalleryConfigUncached()` so per-image side-effect gates stop paying a 17-row SELECT
  per processed image while keeping live-flip semantics within an accepted skew (C3-16/PERF3-01).

## WP10 — [ ] C3-08 + C3-09 + C3-12(doc half) + C3-26: nginx zones + apply/verify runbook + ledger honesty
Files: `apps/web/nginx/default.conf`, `CLAUDE.md` (operational section), cycle ledger.
- Add a dedicated generous limiter for the image optimizer:
  `limit_req_zone ... zone=nextimage:10m rate=30r/s` + `limit_req zone=nextimage burst=120 nodelay`
  on `location ^~ /_next/image` (CRIT3-02 shape; keeps cold-paint fan-out safe while
  capping per-IP floods on the one Sharp-re-encoding public endpoint).
- Extend the topology-contract comment with the realip half: switching
  `proxy_set_header X-Forwarded-For` does NOT change `$binary_remote_addr`; an LB-fronted
  topology ALSO needs `set_real_ip_from <LB>/real_ip_header X-Forwarded-For` (or PROXY
  protocol) for the `limit_req` zones themselves (TRC3-05 doc half; operator half stays
  chained on C1-11 in the deferred register).
- Runbook: explicit operator apply+verify steps for host-nginx config changes
  (`nginx -t`, reload, then confirm a >burst rapid-GET probe returns 429 and normal
  browsing does not) — with a checkbox this cycle's post-deploy section references.
- Ledger honesty (CRIT3-01/CRIT3-07): record C2-06's edge half as "shipped config;
  prod-apply + verify PENDING operator action" (not "closed"); same disposition language
  for e39ad990 (detects-not-prevents) and a4a2d250 (symptom-degrade, root env validation
  still absent) in this plan's ledger section.

## WP11 — [ ] C3-11 + C3-18 + C3-19: test-coverage batch
Files: new `apps/web/src/__tests__/not-found-layout-restore-maintenance.test.ts`,
`clip-inference.test.ts`, small pure-helper tests.
- 2×2 matrix (maintenance active/inactive × entity missing/present) for the three
  status-bearing 404 layouts, using the `photo-og-metadata.test.ts` mock pattern (TEST3-01).
- `clip-inference.test.ts`: 512-dim, range [-1,1], determinism, distinctness,
  image/text seed-namespace separation (TEST3-04).
- `csp-nonce.ts` (both NODE_ENV branches) + `settings-normalization.ts` (trim, empty,
  image_sizes delegation) direct tests (TEST3-05).

## WP12 — [ ] C3-25 + C2-31 exit-criterion fire: config-scanner tokenizer (bounded)
Files: `apps/web/src/__tests__/next-config-uploads-headers.test.ts`,
`apps/web/src/__tests__/api-csp-header.test.ts`.
- The C2-31 exit criterion fired (fresh string-parse fragility instance in cycle-2, per
  CRIT3-06): replace the `indexOf('\n    }')` boundary hack with a small brace-balanced
  location-block parser for the nginx-scanning assertions; land BEFORE/WITH WP10's nginx
  edit so the new block doesn't re-break the string offsets.
- Relax `api-csp-header.test.ts` from exact rule-count pinning to presence/shape
  assertions (the `/api/:path*` rule exists with the minimal CSP; no assertion on total).
- The broader lint-gate scanner tokenizer (check-public-route-rate-limit et al.) remains
  deferred (C2-31 remainder) with unchanged exit criterion — this WP is the concrete
  instance drainage, recorded in the deferred register.

## WP13 — [ ] C3-23 (LOW): info-bottom-sheet focus-restore doc-claim fix
- Correct the `restoreFocusRef` doc comment to describe the implemented
  close-while-mounted mechanism (the component is unconditionally mounted; a true
  unmount-while-open also unmounts the restore target) — no behavior change (DBG3-03).

## WP14 — [ ] C3-24 (LOW): OptimisticImage fallback-retry base
- Retry against the currently-displayed source (fallback once taken), not the original
  `src` prop; keep the one-shot fallback switch; unreachable today (no `fallbackSrc`
  callers) but shipped-trap removal (CR3-02). Pin with a small test.

## WP15 — [ ] C3-28 + C3-29 (LOW): micro-perf pair
- Memoize the CSP invariant halves + parsed `IMAGE_BASE_URL` at module level in
  `content-security-policy.ts` (nonce-only splice per request) (PERF3-06).
- `serve-upload.ts`: memoize `realpath(UPLOAD_ROOT)` per process; use `stat()` on the
  HEAD/304 branches, keep `open()`+`fstat` only for the streaming path (PERF3-07).
- Implement only if gates stay green within cycle budget; else move to deferred with
  exit criteria.

## WP16 — [ ] Docs/ledger batch
- CLAUDE.md edits from WP1/2/9/10 (folded with their WPs or one docs commit).
- Consolidated carry-forward register: new `.context/plans/deferred-carry-forward.md`
  with `[id | severity | first-deferred cycle | age | exit criterion | home register]`
  rows for every OPEN deferred item across cycle-96/cycle-1/cycle-2/cycle-3 registers,
  making the 8-cycle age budget mechanically checkable (C3-27/CRIT3-08); README pointer.
- `.context/plans/README.md`: cycle-2 → Recent (completed/deployed), cycle-3 active.
- Historical-record correction: cycle-2 plan WP1 line "9 crafted-buffer tests" → 4
  (VER3-02), marked as a cycle-3 correction note.
- Record dispositions: C3-31 (restore-scan tail: accepted-by-design note), C3-37/C3-38
  (SEC3 INFO items recorded, no action), C3-33 (process note).

## Ledger honesty dispositions (C3-26, kept current this cycle)
- C2-06 edge half (`af3b2f7d`): shipped config; **prod-apply + 429 verify = PENDING
  operator action** (runbook lands in WP10). Not "closed".
- C2-03 (`e39ad990`): shipped warn-only detector — WP2 makes detection durable, but it
  still detects-not-prevents; the scale-out correctness gap itself remains an accepted
  documented boundary.
- C2-37 (`a4a2d250`): symptom-degrade shipped; root cause (no boot validation for runtime
  `IMAGE_BASE_URL`) still open as a LOW residual (recorded in deferred register).

## Post-deploy verification items
- [ ] Production healthy after deploy (`/api/live` 200, page probes).
- [ ] 404 pages: single `noindex` robots tag (verify via e2e against the production
  build; prod HTML spot-check if reachable).
- [ ] nginx public/nextimage zones: operator apply per runbook (deploys do not touch host
  nginx) — recorded with exit criterion if not applied this cycle.
