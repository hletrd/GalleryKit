# Cycle 77/100 Implementation Plan

Start HEAD: `8aefc3659fa8b6c08bff0da62d29b9ceb40029c5`.
Review aggregate: `.context/reviews/cycle-77-2026-07-01/_aggregate.md`.

## Goal

Close the safe, reviewable Cycle 77 image-pipeline regression gaps, keep review/plan ledgers current, run all required gates, commit, push, and deploy with `npm run deploy` from the repo root.

## Scheduled Fixes

1. `C77-01`: add route-level coverage proving an old per-photo OG pipeline-version validator does not return `304`.
2. `C77-02`: add regression assertions that in-app and sidecar re-encode update branches retain `updated_at = CURRENT_TIMESTAMP`.
3. `C77-03`: extract and test the sidecar row-existence confirmation wiring so changed rows skip probes, live zero-row updates remain live, and absent zero-row updates are cleanup-eligible.

## Deferred This Cycle

New deferred items are recorded in `.context/plans/cycle-77-2026-07-01-deferred.md`.

The high-severity restore foreground mutation barrier is deferred because the correct fix is a cross-action whole-mutation lease or DB-backed protocol. A narrow second maintenance check near selected writes would reduce only a few example windows and would not satisfy the finding.

## Regression Coverage

- Per-photo OG route test: stale `pipelineVersion` input must force render/fetch instead of returning `304`.
- Admin backfill runner tests: full metadata and detection-failure derivative update SQL must bump `updated_at`.
- Sidecar tests: row-existence confirmation behavior must be directly covered and update SQL source must retain timestamp bumps for both update branches.

## Validation

Run required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then sign, `git pull --rebase`, push, and deploy with `npm run deploy`.

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run required gates.
- [x] Commit, pull --rebase, push.
- [ ] Deploy with `npm run deploy`.

## Gate Evidence

- Focused regression: `npm test --workspace=apps/web -- --run src/__tests__/admin-backfill-runner-deleted-mid-reencode.test.ts src/__tests__/admin-backfill-runner-deleted-mid-reencode-detection-failure.test.ts src/__tests__/backfill-color-pipeline-deleted-mid-reencode.test.ts src/__tests__/og-route-rate-limit-behavior.test.ts` - pass: 4 files passed, 38 tests passed.
- `npm run lint --workspace=apps/web` - pass.
- `npm run lint:api-auth --workspace=apps/web` - pass: both admin API routes OK.
- `npm run lint:action-origin --workspace=apps/web` - pass: all mutating server actions enforce same-origin provenance.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
- `npm run typecheck --workspace=apps/web` - pass.
- `npm run build --workspace=apps/web` - pass. Local sitemap generation logged the expected unavailable local MySQL fallback and exited successfully.
- `npm test --workspace=apps/web` - pass: 301 files passed, 2 skipped; 2814 tests passed, 4 skipped.

## Gate Fix Iterations

0 so far.

## Terminal Evidence

- Commit/push evidence: `9286bef16f3401fb0d8c17f52de5c96804c04533` is present at `HEAD`, `origin/master`, and `origin/HEAD` at Cycle 78 start.
- Deploy evidence: no committed Cycle 77 deploy transcript was present in this plan at Cycle 78 review time. Cycle 78 will run the required per-cycle deploy after its own pushed fix commit, deploying the current `master` state including Cycle 77.
