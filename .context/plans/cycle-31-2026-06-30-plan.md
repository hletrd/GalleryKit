# Cycle 31 Review Plan

Date: 2026-06-30 KST
Source reviews: `.context/reviews/*` cycle 31 plus `.context/reviews/_aggregate.md`

## Implementation Tasks

### P31-01 - Fix stale search results on semantic-mode toggle

- Findings: C31-AGG-01.
- Original severity/confidence: Medium / High.
- Files: `apps/web/src/components/search.tsx`, `apps/web/src/__tests__/search-semantic-toggle-source.test.ts`.
- Plan:
  1. Invalidate request ownership synchronously from `onCheckedChange` by calling the existing clear/invalidate path before setting the new mode.
  2. Preserve the existing debounce/effect ownership for the next search.
  3. Strengthen the source-contract test so the toggle handler proves both no direct `performSearch()` and synchronous invalidation.

### P31-02 - Harden public expensive-GET lint detection

- Findings: C31-AGG-02, C31-AGG-03, C31-AGG-04.
- Original severity/confidence: Medium / High; Low / High; Medium / High.
- Files: `apps/web/scripts/check-public-route-rate-limit.ts`, `apps/web/src/__tests__/check-public-route-rate-limit.test.ts`.
- Plan:
  1. Trace local helper functions that contain expensive DB/image/filesystem/embedding work.
  2. Treat calls to those helpers as expensive work in exported GET handlers.
  3. Fail closed when file-level exemptions coexist with more than one protected surface.
  4. Inspect or fail closed for expensive catch/finally branches that are reachable before a dominating limiter.
  5. Add fixtures for helper-hidden work, POST exemption plus expensive GET, and catch/finally expensive work.

### P31-03 - Add cheap Atom feed 304 fast path

- Findings: C31-AGG-05.
- Original severity/confidence: Medium / Medium.
- Files: `apps/web/src/lib/data.ts`, `apps/web/src/app/feed.xml/route.ts`, `apps/web/src/app/[locale]/(public)/[topic]/feed.xml/route.ts`, `apps/web/src/__tests__/feed-sized-derivative.test.ts`.
- Plan:
  1. Add a lightweight `getFeedUpdatedAt()` helper that reads only the newest processed feed timestamp.
  2. In feed routes, check `If-Modified-Since` against that timestamp before loading SEO/config/full rows.
  3. Keep the existing full-feed `Last-Modified` behavior for non-304 responses.
  4. Add source-contract coverage for the fast path.

### P31-04 - Fix CLIP inference slot handoff

- Findings: C31-AGG-06.
- Original severity/confidence: Medium / High.
- Files: `apps/web/src/lib/clip-model.ts`, `apps/web/src/__tests__/clip-model-contract.test.ts`.
- Plan:
  1. Make `waitForInferenceSlot()` resolve only after a slot is reserved for the waiter.
  2. Make the release path transfer the active slot to a waiter without decrementing through zero first.
  3. Add a source-contract assertion that release uses a dedicated handoff helper and does not decrement before resolving a waiter.

### P31-05 - Fix search status duplicate announcement

- Findings: C31-AGG-12.
- Original severity/confidence: Low / High.
- Files: `apps/web/src/components/search.tsx`.
- Plan:
  1. Keep the existing `sr-only` live region as the single assistive announcement.
  2. Mark the duplicated visible status/no-results copy as hidden from assistive tech.

### P31-06 - Repair docs and artifact hygiene

- Findings: C31-AGG-08, C31-AGG-09, C31-AGG-20.
- Original severity/confidence: Medium / High; Low / High; Low / High.
- Files: `CLAUDE.md`, `apps/web/src/lib/use-display-capability.ts`, `.context/plans/cycle-30-2026-06-30-plan.md`, `.context/plans/cycle-30-2026-06-30-deferred.md`.
- Plan:
  1. Update Firefox display-capability docs/comments to distinguish `color-gamut` caveats from `dynamic-range`.
  2. Replace brittle `CLAUDE.md` line-number pointers with symbol/search-string pointers.
  3. Remove cycle-30 plan trailing whitespace.

## Deferred Findings

### D31-01 - Semantic retrieval needs a search-owned indexing boundary

- Finding: C31-AGG-07.
- Original severity/confidence: Medium / High.
- Citation: `apps/web/src/app/api/search/semantic/route.ts:263`, `apps/web/src/app/api/search/similar/[id]/route.ts:164`, `CLAUDE.md:553`.
- Deferral reason: architectural scalability work, not a local correctness bug in current default-disabled semantic search. `CLAUDE.md` says production semantic search is operator-enabled and the repo proves gates/runbook rather than current live row count. A vector index/service choice would materially expand scope beyond this cycle.
- Exit criterion: production semantic mode is enabled for a corpus near or above `SEMANTIC_SCAN_LIMIT`, or operator requires corpus-wide recall/tail-latency guarantees.

### D31-02 - Mobile home filter hierarchy

- Finding: C31-AGG-10.
- Original severity/confidence: Medium / High.
- Citation: `apps/web/src/components/home-client.tsx:255`, `apps/web/src/components/tag-filter.tsx:63`.
- Deferral reason: UI layout change has broad visual impact and should be verified with screenshots across mobile/desktop and English/Korean. This cycle prioritizes correctness/security gate fixes.
- Exit criterion: next UI-focused cycle or any mobile first-viewport work.

### D31-03 - Lightbox hidden controls accessibility

- Finding: C31-AGG-11.
- Original severity/confidence: Medium / Medium.
- Citation: `apps/web/src/components/lightbox.tsx:201`, `apps/web/src/components/lightbox.tsx:371`.
- Deferral reason: modal accessibility behavior needs browser/AT verification to avoid creating a focus/visibility regression. It is not a data-loss/security issue and should be handled with targeted lightbox tests and browser snapshots.
- Exit criterion: next accessibility-focused cycle or any lightbox control refactor.

### D31-04 - Photo card accessible text verbosity

- Finding: C31-AGG-13.
- Original severity/confidence: Low / Medium.
- Citation: `apps/web/src/components/home-client.tsx:323`, `apps/web/src/components/home-client.tsx:353`.
- Deferral reason: low-severity AT polish that should be paired with card accessibility snapshot checks.
- Exit criterion: next card/grid accessibility pass.

### D31-05 - Live production search failure for `jihoon`

- Finding: C31-AGG-14.
- Original severity/confidence: Medium / High.
- Citation: `apps/web/src/components/search.tsx:160`, `apps/web/src/components/search.tsx:473`.
- Deferral reason: live production runtime evidence points to backend/config/data state; local dev data rendering was blocked by MySQL `ECONNREFUSED`, and there is no production log/DB access in this cycle. P31-01 fixes a related client stale-state bug, but the live outage needs operational evidence before changing backend behavior.
- Exit criterion: production logs or local DB reproduction shows the failing search route/action and error cause.

### D31-06 - RTL readiness

- Finding: C31-AGG-15.
- Original severity/confidence: Low / High.
- Citation: `apps/web/src/app/[locale]/layout.tsx:94`, `apps/web/src/components/lightbox.tsx:555`.
- Deferral reason: no RTL locale is currently exposed; repo docs confirm shipped locales are English/Korean. Activating RTL requires a dedicated locale/product decision.
- Exit criterion: adding an RTL locale or exposing an RTL preview.

### D31-07 - Routine UI transition timing

- Finding: C31-AGG-16.
- Original severity/confidence: Low / High.
- Citation: `apps/web/src/components/home-client.tsx:357`, `apps/web/src/components/photo-viewer.tsx:718`.
- Deferral reason: subjective polish with visual feel tradeoffs; not a gate/correctness failure.
- Exit criterion: next visual polish pass with screenshot/browser interaction review.

### D31-08 - Specialist value support line

- Finding: C31-AGG-17.
- Original severity/confidence: Low / Medium.
- Citation: `apps/web/src/components/home-client.tsx:255`, `apps/web/src/components/footer.tsx:34`.
- Deferral reason: content/positioning choice that should be user-approved or paired with copy review; not a code defect.
- Exit criterion: user requests marketing/positioning copy or a public-home content pass.

### D31-09 - Search failure recovery action

- Finding: C31-AGG-18.
- Original severity/confidence: Low / High.
- Citation: `apps/web/src/components/search.tsx:473`.
- Deferral reason: depends on resolving the live search failure cause and choosing the intended fallback behavior.
- Exit criterion: search backend failure cause is known or user asks for resilient fallback behavior.

### D31-10 - Restore executable failure-path test

- Finding: C31-AGG-19.
- Original severity/confidence: Low / Medium.
- Citation: `apps/web/src/__tests__/restore-upload-lock.test.ts:103`, `apps/web/src/app/[locale]/admin/db-actions.ts:493`.
- Deferral reason: low-severity test-depth improvement; source-order contract currently covers the cycle-30 regression and full module mocking is large relative to this cycle's correctness fixes.
- Exit criterion: restore flow is touched again or a dedicated restore-test hardening cycle runs.

### D31-MV-01 - Production TLS/proxy topology validation

- Finding: C31-AGG-MV-01.
- Original severity/confidence: Medium / Medium.
- Citation: `apps/web/nginx/default.conf:21`, `apps/web/src/lib/request-origin.ts:45`.
- Deferral reason: manual production validation task, not a local code defect. Repo deploy rules keep host/SSH details in gitignored `.env.deploy`; do not hardcode production topology in docs.
- Exit criterion: operator requests infrastructure validation or production proxy chain changes.

### D31-MV-02 - Restore dump provenance and DB grants

- Finding: C31-AGG-MV-02.
- Original severity/confidence: Medium / Medium.
- Citation: `apps/web/src/lib/sql-restore-scan.ts:12`, `apps/web/src/app/[locale]/admin/db-actions.ts:620`.
- Deferral reason: manual production grants/provenance validation; local code already guards restore statements and same-origin/admin access.
- Exit criterion: operator audits production MySQL grants or changes restore trust model.

### D31-MV-03 - Exact GPS map publication operator intent

- Finding: C31-AGG-MV-03.
- Original severity/confidence: Low / High.
- Citation: `apps/web/src/lib/data.ts:410`, `apps/web/src/app/actions/topics.ts:600`.
- Deferral reason: manual UX/runbook validation risk; current code intentionally gates exact GPS on `map_visible=true`.
- Exit criterion: map visibility UX/runbook is revised or sensitive galleries require coordinate rounding.

### D31-MV-04 - Process-local public expensive-route limits

- Finding: C31-AGG-MV-04.
- Original severity/confidence: Low / Medium.
- Citation: `apps/web/src/lib/rate-limit.ts:78`, `apps/web/src/app/[locale]/(public)/s/[key]/page.tsx:98`.
- Deferral reason: documented single-instance topology in `CLAUDE.md` means process-local budgets are acceptable for current deployment; shared limits are required before horizontal scaling.
- Exit criterion: app is scaled beyond a single web process or edge/shared rate limiting is introduced.

### D31-GW-01 - Local build logs sitemap DB fallback when MySQL is unavailable

- Finding: gate warning observed during cycle 31 `npm run build --workspace=apps/web`.
- Original severity/confidence: Low / High.
- Citation: `apps/web/src/app/sitemap.ts` runtime build path; build log reported `connect ECONNREFUSED 127.0.0.1:3306` and then `falling back to homepage-only sitemap`.
- Deferral reason: local validation environment lacks a running MySQL server. The build passed and the sitemap code intentionally catches this path. Fixing it would require local DB service setup, not a code change in this cycle.
- Exit criterion: warning appears in a production deploy with valid DB connectivity, or sitemap fallback stops being intentional.

## Progress

- [x] Prompt 1 aggregate written.
- [x] P31-01 implemented and tested.
- [x] P31-02 implemented and tested.
- [x] P31-03 implemented and tested.
- [x] P31-04 implemented and tested.
- [x] P31-05 implemented and tested.
- [x] P31-06 implemented and checked.
- [x] Full configured gates run.
- [ ] Commit, push, and per-cycle deploy complete.
