# Cycle 89/100 Implementation Plan

Start HEAD: `10cd16622c9c7d1d2b26dd45e9e6afe34b21b3e5`.
Review aggregate: `.context/reviews/cycle-89-2026-07-01/_aggregate.md`.

## Goal

Close the safe Cycle 89 findings, keep review/plan indexes current, run all required gates, commit/push, deploy with `npm run deploy`, and smoke `https://gallery.atik.kr`.

## Scheduled Fixes

1. `C89-01`: close Cycle 88's release ledger by marking commit/pull-rebase/push and deploy complete, recording signed `10cd16622c9c7d1d2b26dd45e9e6afe34b21b3e5` / `origin/master` evidence, recording that Cycle 89 started from deployed `10cd166`, and moving Cycle 88 out of the active plan index.
2. `C89-02`: replace the duplicated 256M-pixel post-reencode detection caps in both color-backfill paths with the shared `MAX_INPUT_PIXELS` full-image cap from `process-image.ts`.
3. `C89-03`: keep Cycle 89 review/plan artifacts trackable by extending the existing `.gitignore` whitelist for the new plan and deferred files.

## Deferred This Cycle

No newly deferred Cycle 89 findings.

Carry-forward deferred items remain in `.context/plans/cycle-89-2026-07-01-deferred.md`.

## Regression Coverage

- Ledger: Cycle 88 must no longer show unchecked release tasks after Prompt 3.
- Index: `.context/plans/README.md` must list Cycle 89 as active and Cycle 88 as recent with terminal state.
- Review aggregation: `.context/reviews/_aggregate.md` must point to the Cycle 89 aggregate.
- Backfill pixel cap: `cycle-89-source-contracts.test.ts` must fail if either color-backfill detection constructor reverts to a hard-coded `256 * 1024 * 1024` cap instead of `MAX_INPUT_PIXELS`.

## Validation

Run required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `BASE_URL=https://gallery.atik.kr npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run focused tests for `cycle-89-source-contracts.test.ts`, `git diff --cached --check` before commit, then signed commit, `git pull --rebase`, push, deploy with `npm run deploy`, and smoke:

- `curl -fsSIL https://gallery.atik.kr`
- `curl -fsS https://gallery.atik.kr/api/health`

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run required gates.
- [ ] Commit, pull --rebase, push.
- [ ] Deploy with `npm run deploy`.

## Focused Evidence

- `npm test --workspace=apps/web -- --run src/__tests__/cycle-89-source-contracts.test.ts` - pass: 1 file, 2 tests.

## Gate Evidence

- `npm run lint --workspace=apps/web` - pass.
- `npm run lint:api-auth --workspace=apps/web` - pass: both admin API routes OK.
- `npm run lint:action-origin --workspace=apps/web` - pass: all mutating server actions enforce same-origin provenance.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
- `npm run typecheck --workspace=apps/web` - pass.
- `BASE_URL=https://gallery.atik.kr npm run build --workspace=apps/web` - pass. Local sitemap generation logged the expected unavailable local MySQL fallback and exited successfully.
- `npm test --workspace=apps/web` - pass: 302 files passed, 2 skipped; 2840 tests passed, 4 skipped.

## Gate Fix Iterations

- `C89-02`: fixed one source-contract-backed resource-bound drift by sharing the env-backed full-image pixel cap with color-backfill detection.
