# Run-10 Cycle 17/100 Implementation Plan

Status: IMPLEMENTED - GATES GREEN; COMMIT/PUSH/DEPLOY PENDING
Aggregate: `.context/reviews/_aggregate.md`
Date: 2026-07-08 KST
Start HEAD: `fc15b235`

## Scope

This plan schedules the Cycle 17 findings that are confirmed, bounded, and safe to fix in this cycle. Every aggregate finding is either scheduled here or recorded in `cycle-17-2026-07-08-deferred.md`.

Repo rules read before scheduling/deferring: `CLAUDE.md`, `AGENTS.md`, `.context/plans/README.md`, `.context/plans/cycle-16-2026-07-08-plan.md`, `.context/plans/cycle-16-2026-07-08-deferred.md`, and the CLIP docs under `docs/superpowers/`. No `.context/README.md`, `.cursorrules`, or `CONTRIBUTING.md` exists in this checkout.

## Scheduled Work Packages

### WP1 - Close release-ledger and stale-comment drift

Findings: `AGG-C17-02`, `AGG-C17-04`

Files:

- `.context/plans/README.md`
- `.context/plans/cycle-16-2026-07-08-plan.md`
- `apps/web/src/app/actions/settings.ts`
- `apps/web/src/__tests__/settings-backfill-required-action.test.ts`

Plan:

1. Move Cycle 16 out of the active plan pointer and record commit/push evidence plus deploy gap/supersession.
2. Mark this Cycle 17 plan/deferred pair as active.
3. Rewrite settings/backfill comments and test narrative to describe the current soft `requiresBackfill` signal plus hard color-backfill advisory lock fence.

Acceptance:

- Agents no longer see Cycle 16 as active/pending work.
- Comments and tests no longer contradict the lock-enforced settings contract.

### WP2 - Harden DB maintenance and advisory-lock acquisition error paths

Findings: `AGG-C17-01`, `AGG-C17-08`

Files:

- `apps/web/src/app/[locale]/admin/db-actions.ts`
- `apps/web/src/lib/upload-processing-contract-lock.ts`
- `apps/web/src/lib/image-queue.ts`
- `apps/web/src/lib/admin-backfill-runner.ts`
- `apps/web/src/app/actions/topics.ts`
- `apps/web/src/app/actions/admin-users.ts`
- `apps/web/src/app/actions/embeddings.ts`
- focused tests/source contracts under `apps/web/src/__tests__/`

Plan:

1. Move DB backup/restore setup and connection acquisition inside structured typed error handling.
2. Guard release/rollback paths with nullable connection state.
3. Treat `GET_LOCK` query failures as possibly tainted: destroy or provably release pooled sessions instead of returning them cleanly to the pool.
4. Add regressions for DB action acquisition failure and acquisition-query failure patterns.

Acceptance:

- `dumpDatabase()` and `restoreDatabase()` return localized structured failures on setup/acquisition errors.
- Acquisition-query error handling no longer releases ambiguous lock-holding sessions back to the pool.

### WP3 - Fix sidecar backfill and Lightroom setup correctness

Findings: `AGG-C17-05`, `AGG-C17-06`, `AGG-C17-07`

Files:

- `apps/web/scripts/backfill-color-pipeline.ts`
- `apps/web/scripts/backfill-clip-embeddings.ts`
- `apps/web/src/app/api/admin/lr/upload/route.ts`
- focused tests/source contracts under `apps/web/src/__tests__/`

Plan:

1. Move color sidecar config read/settings snapshot after `LOCK_COLOR_PIPELINE_BACKFILL` acquisition and post-lock restore assertion.
2. On Lightroom upload-directory setup failure, settle the preclaimed upload tracker and return a no-store JSON error.
3. Make CLIP sidecar progress past failed low-ID candidates so later valid images are reachable across runs.
4. Add regressions for lock/snapshot ordering, LR setup failure settlement, and CLIP failed-prefix progress.

Acceptance:

- Color sidecar cannot encode with stale settings after another settings save wins the lock first.
- LR setup failures do not leak quota claims or return framework-level opaque 500s.
- CLIP sidecar no longer repeats the same failed prefix forever.

### WP4 - Fix narrow UI/operator-copy correctness

Findings: `AGG-C17-09`, `AGG-C17-10`, `AGG-C17-11`, `AGG-C17-12`, `AGG-C17-13`, `AGG-C17-14`, `AGG-C17-15`

Files:

- `apps/web/src/proxy.ts`
- `apps/web/src/app/manifest.ts`
- `apps/web/messages/en.json`
- `apps/web/messages/ko.json`
- `apps/web/src/site-config.example.json`
- `README.md`
- `apps/web/src/app/[locale]/admin/(protected)/tokens/tokens-client.tsx`
- `apps/web/src/app/[locale]/admin/(protected)/categories/topic-manager.tsx`
- `apps/web/src/app/[locale]/admin/(protected)/analytics/analytics-client.tsx`
- focused tests/source contracts under `apps/web/src/__tests__/`

Plan:

1. Exclude extensionless metadata icon routes from locale proxy redirects and add a regression.
2. Reword semantic-search Settings copy so Stub/Production boundaries are clear in English and Korean.
3. Add the optional `copyright` config key to public examples.
4. Include selected token metadata in revoke confirmation without exposing token plaintext.
5. Include alias and category context in alias-delete confirmation.
6. Localize analytics country names with raw codes preserved as secondary diagnostics.

Acceptance:

- `/icon` and `/apple-icon` are not locale-routed.
- Operators cannot mistake Stub mode for production semantic search or similar-photo enablement.
- Destructive dialogs preserve target context.
- Country analytics are localized for supported locales.

### WP5 - Gate verification, commit, push, deploy

Findings: cycle quality/deploy requirement.

Plan:

1. Run all configured gates:
   - `npm run lint --workspace=apps/web`
   - `npm run lint:api-auth --workspace=apps/web`
   - `npm run lint:action-origin --workspace=apps/web`
   - `npm run lint:public-route-rate-limit --workspace=apps/web`
   - `npm run typecheck --workspace=apps/web`
   - `npm run build --workspace=apps/web`
   - `npm test --workspace=apps/web`
   - `npm run test:e2e --workspace=apps/web` if browser-flow coverage is required by touched surfaces
2. Fix blocking errors before commit/push.
3. Commit and push in signed, fine-grained Conventional Commits with gitmoji.
4. Run `npm run deploy` once after pushed gates are green because `DEPLOY_MODE=per-cycle`.

## Progress

- [x] WP1 release-ledger and stale-comment drift
- [x] WP2 DB maintenance and advisory-lock acquisition hardening
- [x] WP3 sidecar backfill and Lightroom setup correctness
- [x] WP4 narrow UI/operator-copy correctness
- [x] WP5 local gates green; commit/push/per-cycle deploy outcome is reported by the cycle terminal report

## Gate Evidence

- `npm run lint --workspace=apps/web` - passed.
- `npm run lint:api-auth --workspace=apps/web` - passed.
- `npm run lint:action-origin --workspace=apps/web` - passed; all mutating server actions accounted for.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - passed; all scanned public mutating/expensive routes accounted for.
- `npm run typecheck --workspace=apps/web` - passed after fixing the `withTopicRouteMutationLock<T>` `finally` return type issue.
- `npm run build --workspace=apps/web` - passed. Local build emitted the existing sitemap homepage-only fallback because local MySQL at `127.0.0.1:3306` was unavailable; the command exited 0.
- `npm test --workspace=apps/web` - passed: 354 test files passed, 2 skipped; 3264 tests passed, 4 skipped.
- `npm run test:e2e --workspace=apps/web` - not run; the implemented surface is covered by source/behavior/unit contracts and did not require browser-flow coverage.

## Gate Fixes

- Fixed 2 blocking gate issues during this cycle:
  - Typecheck error: `src/app/actions/topics.ts` returned `undefined` from a generic `finally` path after ambiguous advisory-lock acquisition handling was added.
  - Unit-test error: `src/__tests__/cycle-6-source-contracts.test.ts` still asserted that failed CLIP rows consumed the scan budget; updated it to the intended actual-embedding-attempt budget contract.
