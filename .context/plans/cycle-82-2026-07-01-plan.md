# Cycle 82/100 Implementation Plan

Start HEAD: `c272c5217ffdf1d324f001d8c35145262be310b4`.
Review aggregate: `.context/reviews/cycle-82/_aggregate.md`.

## Goal

Close the safe Cycle 82 findings, keep review/plan ledgers current, run all required gates, commit/push all work, and deploy with `npm run deploy` from the repo root.

## Scheduled Fixes

1. `C82-01`: close Cycle 81's release ledger with terminal signed commit/push/deployed-start evidence for `c272c521`, then move Cycle 81 out of the active plan index.
2. `C82-02`: route public search and similar-photo result labels through a shared client-safe result-label helper that trims titles, rejects filename-like titles, preserves description fallback, and falls back to localized `Photo {id}`.
3. `C82-03`: give failed-image retry buttons localized per-row accessible names and connect the processing error as descriptive text.

## Deferred This Cycle

No new Cycle 82 findings are deferred.

Carry-forward deferred items remain in `.context/plans/cycle-82-2026-07-01-deferred.md`.

## Regression Coverage

- Search/similar labels: helper unit coverage must reject filename-like/blank titles before using description or fallback; component source contracts must import/use the helper and must not restore raw `title || description` / `title ?? description` fallbacks.
- Failed retry buttons: source contract must prove each retry button uses a row label in `aria-label` and `aria-describedby` points at the row processing error when present.
- Ledger: Cycle 81 must no longer be listed as active, and Cycle 81's terminal state must record commit/push/deployed-start evidence for `c272c521`.

## Validation

Run required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then signed commit, `git pull --rebase`, push, and deploy with `npm run deploy`.

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run required gates.
- [ ] Commit, pull --rebase, push.
- [ ] Deploy with `npm run deploy`.

## Gate Evidence

- Focused regression: `npm test --workspace=apps/web -- --run src/__tests__/photo-title.test.ts src/__tests__/search-disclaimer.test.ts src/__tests__/cycle-21-source-contracts.test.ts src/__tests__/failed-image-retry.test.ts` - pass: 4 files passed, 48 tests passed.
- `npm run lint --workspace=apps/web` - pass.
- `npm run lint:api-auth --workspace=apps/web` - pass: both admin API routes OK.
- `npm run lint:action-origin --workspace=apps/web` - pass: all mutating server actions enforce same-origin provenance.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
- `npm run typecheck --workspace=apps/web` - pass.
- `npm run build --workspace=apps/web` - pass. Local sitemap generation logged the expected unavailable local MySQL fallback and exited successfully.
- `npm test --workspace=apps/web` - pass: 301 files passed, 2 skipped; 2835 tests passed, 4 skipped.
- `git diff --cached --check` - pass after one review-artifact whitespace fix iteration.

## Gate Fix Iterations

1. Removed trailing spaces from `.context/reviews/cycle-82/perf-reviewer.md` after `git diff --cached --check` reported them.
