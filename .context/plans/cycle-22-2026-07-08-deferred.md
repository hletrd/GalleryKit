# Run-10 Cycle 22/100 Deferred Findings

Status: OPEN
Aggregate: `.context/reviews/_aggregate.md`
Date: 2026-07-08 KST

This register records every Cycle 22 aggregate finding not scheduled in `cycle-22-2026-07-08-plan.md`. Severity and confidence are preserved from the aggregate. Deferred work remains bound by repo policy: GPG-signed Conventional Commits with gitmoji, `git pull --rebase` before push, required gates, no force-push, no `--no-verify`, and the repo's Node/Next/React/TypeScript toolchain rules.

Repo rule basis for deferral: `.context/plans/README.md` defines the carry-forward age budget; `.context/plans/deferred-carry-forward.md` is the committed register for open deferred rows. `CLAUDE.md` documents the single-web-instance topology, process-local state boundaries, DB-only backup/restore boundary, host-nginx operator boundary, and permanently deferred 2FA/WebAuthn posture. This register does not defer the direct mutation-barrier lint defect (`AGG-C22-01`) or direct pending-deletion privacy/data-retention bug (`AGG-C22-02`); both are scheduled.

## Deferred Items

| ID | Severity / Confidence | Citation | Reason for deferral | Exit criterion |
|----|-----------------------|----------|---------------------|----------------|
| AGG-C22-05 | High / High | `apps/web/src/app/actions/images.ts:87-227`; `apps/web/src/app/actions/images.ts:325-445`; `apps/web/src/app/api/admin/lr/upload/route.ts:84-188`; `apps/web/src/app/api/admin/lr/upload/route.ts:254-631` | Shared browser/PAT ingest extraction is a broad architecture refactor across two hot upload paths. The direct Cycle 22 data-retention defect is scheduled first; this item preserves the recurring parity risk already represented by Cycle 21 `AGG-C21-03`. | Before adding upload-time privacy/metadata/processing fields, or the next confirmed browser/PAT drift. |
| AGG-C22-06 | High / High for source shape; Medium for live impact | `apps/web/src/app/actions/images.ts:87-106`; `apps/web/src/app/api/admin/lr/upload/route.ts:174-188`; `apps/web/src/app/[locale]/admin/db-actions.ts:409-420`; `apps/web/src/app/[locale]/admin/db-actions.ts:717-729`; `apps/web/next.config.ts:111-119`; `apps/web/src/lib/upload-limits.ts:1-35` | Streaming multipart ingress crosses browser upload, PAT upload, and restore. It needs RSS measurement and a shared temp-file handoff design; no live RSS/OOM evidence was collected this cycle. This preserves Cycle 21 `AGG-C21-04`. | Production RSS/OOM incident during upload/restore, or approved streaming-ingress project. |
| AGG-C22-07 | High / High | `apps/web/src/db/index.ts:21-45`; `apps/web/src/lib/image-queue.ts:121-153`; `apps/web/src/lib/admin-backfill-runner.ts:106-143`; `apps/web/src/lib/admin-backfill-runner.ts:716-727`; `apps/web/src/lib/process-image.ts:36-1418` | Shared DB/CPU background-budget work changes queue/backfill concurrency semantics. No measured starvation evidence was collected this cycle, and contained correctness work is scheduled first. This preserves Cycle 21 `AGG-C21-05`. | Measured pool starvation during queue/backfill/search, or shared background-budget project approved. |
| AGG-C22-08 | Medium / Medium | `apps/web/src/lib/data.ts:1392-1407`; `apps/web/src/lib/data.ts:1830-1834`; `apps/web/src/app/actions/public.ts:517-559` | Shared-group view counts are approximate analytics per `CLAUDE.md`; refactoring reader/counter boundaries should ride shared-group cache or analytics consistency work. | Counter drift bug report, shared-group cache refactor, or analytics consistency cycle. |
| AGG-C22-09 | High / High | `apps/web/src/__tests__/pending-file-deletions-source.test.ts:25-45`; `apps/web/src/__tests__/migrate-reconcile-coverage.test.ts:13-180`; `apps/web/src/__tests__/db-restore.test.ts:47-136`; `apps/web/src/__tests__/semantic-scan-limit-source.test.ts:42-77` | The direct scanner and pending-deletion behavior gaps are scheduled in WP1/WP2. Remaining DB-backed migration, child-process restore, semantic-route, and client behavior harness work requires dedicated test infrastructure. | Next high-risk source-contract-only finding, or behavior/integration test-hardening cycle. |
| AGG-C22-11 | Medium / High | `apps/web/playwright.config.ts:72-77`; `.github/workflows/quality.yml:75-80`; `apps/web/e2e/nav-visual-check.spec.ts:40-86`; `apps/web/e2e/hydration-photo-page.spec.ts:20-49` | Browser matrix and deterministic visual assertions affect CI runtime and fixture stability. This is test-strategy work, not a contained defect in current runtime behavior. | Browser-specific regression, PWA/SW incident, visual regression budget approved, or test-matrix project approved. |
| AGG-C22-12 | Medium / High | `apps/web/src/lib/data.ts:1766-1817`; `apps/web/src/app/[locale]/(public)/map/page.tsx:42-110`; `apps/web/src/components/map/map-client.tsx:77-140` | Map clustering/viewport APIs and spatial indexing are product/performance scope. Current cap and rate limits bound worst-case work; no live map jank measurement was collected. | GPS-visible rows approach thousands, mobile map trace shows jank, or map clustering project approved. |
| AGG-C22-13 | Medium / High | `apps/web/src/components/on-this-day-widget.tsx:10-22`; `apps/web/src/app/[locale]/(public)/page.tsx:232-235`; `apps/web/src/lib/data-timeline.ts:102-130`; `apps/web/src/db/schema.ts:123-131` | Generated month/day columns require a schema/performance migration. Cycle 22 schedules parser-backed test coverage, not a query-shape migration. | Measured home latency, corpus growth makes the scan costly, or next schema/index cycle folds generated month/day columns. |
| AGG-C22-14 | Medium / High | `apps/web/src/app/actions/public.ts:248-317`; `apps/web/src/lib/data.ts:1574-1737`; `apps/web/src/lib/smart-collections.ts:221-267` | Search-index redesign is product/performance work. Existing request counts are rate-limited; no live latency evidence was gathered this cycle. | Search latency rises, corpus grows materially, or full-text/search-index cycle approved. |
| AGG-C22-15 | Low / High | `apps/web/src/lib/clip-embeddings.ts:36-235`; `apps/web/src/db/schema.ts:292-304`; `apps/web/src/app/api/search/semantic/route.ts:263-311`; `apps/web/src/app/api/search/similar/[id]/route.ts:178-214` | Bounded residual risk behind semantic mode gates/scan limits. A vector index, worker-thread scorer, or cache redesign is larger than this cycle. | Semantic traffic/gallery size exceeds scan limits, measured CPU/RSS pressure, or vector-index/cache project approved. |
| AGG-C22-16 | Medium / Medium | `apps/web/nginx/default.conf:1-10`; `apps/web/nginx/default.conf:274-295`; `apps/web/deploy.sh:51-55`; `CLAUDE.md:510-522` | Operator/live-host validation. `CLAUDE.md` explicitly says deploys do not apply host nginx, so source edits alone cannot close this. | Operator applies/verifies nginx zones, proxy topology changes, or app-layer public page limiter project approved. |
| AGG-C22-17 | Medium / Medium | `apps/web/nginx/default.conf:20-28`; `apps/web/nginx/default.conf:59-71`; `apps/web/src/lib/rate-limit.ts:175-216`; `apps/web/src/lib/request-origin.ts:81-107` | Operator topology validation requires live request traces and proxy configuration authority outside repo edits. | Proxy topology changes, shared-IP false-positive incident, or operator runs/verifies topology checker. |
| AGG-C22-18 | Medium / Medium | `CLAUDE.md:245-247`; `apps/web/src/lib/single-writer-guard.ts:6-16`; `apps/web/src/lib/single-writer-guard.ts:218-235` | `CLAUDE.md` documents the shipped single-web-instance topology and process-local state. Fail-closed singleton semantics require product/operator decision because rolling deploy overlap is tolerated. | Scale-out is attempted, singleton contention incident occurs, or shared-state/fail-closed topology project approved. |
| AGG-C22-19 | Low / High | `apps/web/src/app/[locale]/admin/db-actions.ts:228-243`; `apps/web/src/app/api/admin/db/download/route.ts:21-89`; `CLAUDE.md:223-228` | `CLAUDE.md` explicitly documents plaintext DB backups as host/storage encryption boundary and restore as DB-only. Changing this is an operator/product backup architecture project. | Backup encryption requirement, filesystem rollback product decision, or restore/file integrity incident. |
| AGG-C22-20 | Low / High | `apps/web/src/db/schema.ts:193-200`; `apps/web/src/app/actions/auth.ts:79-150`; `apps/web/src/app/actions/auth.ts:230-253`; `apps/web/src/db/schema.ts:225-241`; `CLAUDE.md` "Permanently Deferred" | Repo rule quote: `CLAUDE.md` permanently defers "2FA/WebAuthn: Not planned. Multiple root admins with Argon2id + rate limiting is sufficient for a personal gallery." | Admin compromise risk posture changes, step-up auth project approved, or roles/capabilities are added. |
| AGG-C22-21 | Low / Medium | `apps/web/src/app/[locale]/admin/db-actions.ts:593-635`; `apps/web/src/lib/restore-drain-checklist.ts:10-17` | Future-risk registry hardening. Cycle 22 schedules the actual new background drain and its restore suppression; a generic registry API is broader than the contained fix. | Next background writer/queue is added, or restore-drain omission escapes review. |
| AGG-C22-22 | Medium / High | `apps/web/src/app/[locale]/admin/(protected)/dashboard/dashboard-client.tsx:135-144`; `apps/web/src/components/image-manager.tsx:427-620` | Admin responsive workbench redesign is product/UX scope and should be paired with broader admin IA work. No wrong-row action incident was reported. | Mobile/tablet admin operation becomes priority, wrong-row action report, or admin redesign cycle. |
| AGG-C22-23 | Low-Medium / High | `apps/web/src/components/admin-nav.tsx:15-49`; `apps/web/src/components/admin-header.tsx:13-26` | Admin IA redesign requires product decisions about workflow grouping and sensitive operation separation. | Admin nav redesign cycle, sensitive-operation IA concern, or user report of admin navigation confusion. |
| AGG-C22-24 | Low / High | `apps/web/src/components/masonry-card.tsx:149-155` | Low-risk public gallery presentation preference. Changing metadata placement needs visual/browser iteration. | Photographer/user report that mobile overlays obscure important crops, or masonry-card visual redesign cycle. |
| AGG-C22-25 | Medium / High | `apps/web/src/site-config.json:2-10`; `README.md:60-77`; `README.md:121-122`; `apps/web/README.md:15-20`; `apps/web/scripts/ensure-site-config.mjs:12-42`; `apps/web/src/app/sitemap.ts:14-18`; `apps/web/src/app/[locale]/layout.tsx:15-26` | This checkout is the configured production deployment for `gallery.atik.kr`, and the user explicitly chose that target for this loop. Changing committed site-config semantics would be a distribution/product decision. | Before distributing GalleryKit as a template/package, or if copied installs publish wrong canonical URLs. |

## Scheduled, Not Deferred

Scheduled in `cycle-22-2026-07-08-plan.md`: `AGG-C22-01`, `AGG-C22-02`, `AGG-C22-03`, `AGG-C22-04`, `AGG-C22-10`, plus targeted behavior-test coverage from `AGG-C22-09`.

## Age-Budget Check

Newly deferred High-severity Cycle 22 rows (`AGG-C22-05`, `AGG-C22-06`, `AGG-C22-07`, residual `AGG-C22-09`) are broad architecture/performance/test-infrastructure items with explicit exit criteria, not direct unpatched authz/security/data-loss bugs with contained local fixes. Direct correctness/privacy/data-retention items are scheduled. Topology and edge-security rows cite the documented single-instance and host-nginx operator boundaries in `CLAUDE.md`.
