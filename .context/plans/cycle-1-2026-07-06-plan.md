# Run-10 Cycle 1/100 Implementation Plan

Start HEAD: `657eb0243f49898c0f902fda60669d63b17a512d` (== origin/master after cycle-start reconciliation).
Review aggregate: `.context/reviews/cycle-1-2026-07-06/_aggregate.md` (34 dispositions from 12 lanes).

## Goal

Close all 30 scheduled run-10 cycle-1 findings (including draining the 8-cycles-aged C77-ARCH-01
restore fence), repair the ledger (orphaned cycle-99 review, provenance files, plan index), run every
required gate, commit/push fine-grained GPG-signed gitmoji commits, and deploy once via `npm run deploy`.

## Work packages (scheduled fixes)

### WP1 — Public rate-limiter saturated fast path (C1-01, Medium/High)
- `apps/web/src/app/actions/public.ts`: add a read-only in-memory saturated early return to
  `checkLoadMoreRateLimit` and `checkViewRecordRateLimit` (mirroring `searchImagesAction`'s
  `entry.count >= MAX` fast path) so over-limit callers never touch `incrementRateLimit`/`checkRateLimit`/`decrementRateLimit`.
- Tests: pre-saturate the in-memory bucket; assert no DB limiter calls for repeated over-limit attempts.
- Closes the orphaned cycle-99 architect finding (ledgered via WP14).

### WP2 — Backup/restore completeness verification (C1-02, High/Medium — correctness, not deferrable)
- `apps/web/src/app/[locale]/admin/db-actions.ts`: backup writes to `<outputPath>.tmp` and atomically
  `rename()`s to the canonical filename only after existing post-hoc checks pass (size > 0, header,
  no write-stream error) PLUS a new mysqldump completion-trailer check.
- `apps/web/src/lib/db-restore.ts`: add `hasMysqldumpCompletionTrailer` (tail scan for `-- Dump completed`)
  and a provenance-aware completeness rule: when the file header identifies a mysqldump artifact
  (`-- MySQL dump`), require the trailer; non-mysqldump SQL (operator-authored) keeps current behavior
  so legitimate hand-written dumps are not rejected.
- Tests: truncated-at-clean-boundary mysqldump fixture rejected; complete dump accepted; non-mysqldump
  SQL unaffected; backup tmp-rename contract.

### WP3 — Restore-window admin mutation fence (C1-03 == C77-ARCH-01, High/High, aged 8+ cycles — drained now)
- New `apps/web/src/lib/admin-mutation-barrier.ts`: process-local shared/exclusive barrier
  (single-web-instance topology per CLAUDE.md Runtime topology). Mutating admin work runs inside
  `withAdminMutation(fn)` (shared slot; rechecks restore maintenance after acquisition); restore
  acquires the exclusive side before durable import and drains in-flight shared holders.
- Wire `withAdminMutation` into the mutating server actions that write application tables
  (`settings.ts`, `topics.ts`, `tags.ts`, `sharing.ts`, `collections.ts`, `seo.ts`, `images.ts`
  mutations, `admin-users.ts`, `lr-tokens.ts`, `admin-backfill.ts`) and the LR upload route.
- `db-actions.ts` restore path: enter exclusive barrier after setting the durable marker, before import.
- Tests: mutation admitted pre-marker blocks restore import until it settles; mutation attempted
  mid-restore is rejected with the maintenance message; barrier releases on error paths.

### WP4 — Image queue robustness (C1-04 Medium/High, C1-06 Medium/High, C1-19 Low/High, C1-20 Low/High)
- `apps/web/src/lib/image-queue.ts`:
  - Claim exhaustion persists `processing_error`/`failed_at` (specific "could not acquire processing
    lock" message) so the row surfaces in the admin failed-images panel with Retry, instead of silent
    infinite re-enqueue (C1-04).
  - `RELEASE_LOCK` failures log at `console.error` (C1-04 secondary).
  - `bootstrapMissingActiveEmbeddings` gains an in-flight guard so overlapping scans dedupe (C1-06).
  - Embedding-insert FK rejection for a deleted image logs at debug, not a raw warn (C1-19).
  - `processing_error` truncation becomes code-point-safe (C1-20).
- Tests: source/behavior coverage for each contract.

### WP5 — Topic temp-file cleanup age gate (C1-05, Medium/High)
- `apps/web/src/lib/process-topic-image.ts`: age-gate `cleanOrphanedTopicTempFiles` on `mtimeMs`
  (mirror `ORPHANED_DERIVATIVE_TEMP_MIN_AGE_MS` pattern from `image-queue.ts`).
- Update `process-topic-image.test.ts`: fresh files survive; stale files removed.

### WP6 — First-page listing `COUNT(*) OVER()` removal (C1-07, Medium/High; re-opened deferred C94-11)
- `apps/web/src/lib/data.ts`: drop the window column from `getImagesLitePage` and the smart-collection
  first-page branch; compute the header total via the existing lean `getImageCount()` shape in
  `Promise.all` with the page query; `hasMore` from the N+1 row (already the case).
- Retire the `data-tag-names-sql.test.ts` window-function assertion in the same change (C1-32 policy applied).
- Verify callers (`page.tsx`, `[topic]/page.tsx`, `c/[slug]/page.tsx`, `home-client.tsx`) keep exact counts.

### WP7 — Admin/public a11y fixes (C1-08 Medium-High/High, C1-09 Medium/High, C1-10 Medium/High)
- New shared hook (e.g. `apps/web/src/lib/use-restore-focus-after-pending.ts`); wire into login-form,
  password-form, settings-client, seo-client, tokens-client, db page so focus returns to the submit
  control (or error region) when `isPending` falls (C1-08).
- `timeline/page.tsx` + `year/[year]/page.tsx`: real text separator between month/year heading text and
  the photo-count span (ICU-owned or text node), both locales (C1-09).
- `tokens/page.tsx` + `users/page.tsx`: page-level `<h1>` consistent with sibling admin pages (C1-10).
- Tests: DOM/source contracts for separator + `<h1>` presence; focus-restore hook unit test.

### WP8 — Small route fixes (C1-14 Low-Med/High, C1-15 Low/Med, C1-16 Low/Med, C1-17 Low/High, C1-18 Low/Med)
- `api/og/route.tsx`: fold `OG_TEMPLATE_VERSION` + `IMAGE_PIPELINE_VERSION` into the topic OG ETag hash (C1-14).
- `api/admin/lr/upload/route.ts`: wrap post-insert work in try/catch returning JSON (upload committed ⇒
  JSON success; never a non-JSON 500) (C1-15).
- `api/health/route.ts`: bound the DB probe (~2 s race) → 503 on timeout (C1-16).
- `api/search/similar/[id]/route.ts`: correct the false "rollback" comment to the actual
  charge-and-never-refund posture (C1-17).
- `actions/tags.ts` `updateTag`: surface `tagSlugCollision` on `ER_DUP_ENTRY` like sibling paths (C1-18).

### WP9 — Security hygiene (C1-12 Low/High; C1-11 Medium/Medium docs part)
- `db-actions.ts` `runPostRestoreMigrations`: minimal child env (PATH, NODE_ENV, DB_*/MYSQL_* that
  `mysql-connection-options` actually reads, LANG/LC_ALL) instead of `...process.env` (C1-12).
- `nginx/default.conf`: reconcile the header comment with the shipped XFF-overwrite semantics —
  document nginx-as-edge as the shipped mode and the `$proxy_add_x_forwarded_for` + `TRUSTED_PROXY_HOPS`
  variant for LB-fronted topologies (C1-11). Config behavior unchanged.

### WP10 — Masonry resize re-render (C1-21, Low/High)
- `components/home-client.tsx`: quantize the stored viewport width (bucketed) so state changes only at
  meaningful boundaries; keep column behavior identical.

### WP11 — Test hardening (C1-22 Low/High, C1-23 Medium/High, C1-24 Low-Med/High)
- `image-queue-permanent-failure-cleanup.test.ts`: disambiguate `extractFnBody` headers with trailing `(` (C1-22).
- New behavior tests for `actions/collections.ts` CRUD (validation, auth-gate, restore-gate, slug rules) (C1-23).
- Focused tests for `actions/embeddings.ts` backfill action contracts (C1-24).

### WP12 — Break the `photo-viewer` ⇄ `lightbox` value cycle (C1-29, Low-Med/High)
- Extract `isEditableTarget` to a leaf client-safe module (`apps/web/src/lib/editable-target.ts`);
  update both components; keep a re-export only if pinned source-contract tests require staged migration —
  otherwise update those tests in the same change (C1-32 policy applied).

### WP13 — Documentation corrections (C1-25 High/High, C1-27 VLow/High, C1-28 VLow/High, C1-13 docs part, C1-33 docs part)
- CLAUDE.md: smart_collections bullet states the truth — server actions + public `/c/[slug]` read side
  exist; no admin UI/API surface ships yet; rows are currently DB-authored (C1-25).
- CLAUDE.md: `ProPhoto` → `prophoto` search pointer (C1-27); session-cookie `secure` wording matches
  `requestIsHttps || production` (C1-28); deployment checklist reinforces `TRUST_PROXY=true` behind any
  proxy and its failure mode (C1-13); operational note documents the per-upload transient RSS envelope
  (~file size per in-flight upload) pending measurement (C1-33).

### WP14 — Ledger repair + process policy (C1-26 Medium/High, C1-34 Medium/High)
- Write `.context/reviews/cycle-99-2026-07-01/_aggregate.md` retroactively; its single finding is
  closed by WP1 (recorded, not silently dropped).
- `.context/plans/README.md`: add run-10 cycle-1 active entry; move completed cycle-95..98 entries out
  of "Active"; add the carry-forward age-budget policy (a deferred High crossing 8 cycles unchanged is
  either scheduled or explicitly reclassified with a product decision); note the single authoritative
  timeline (origin lineage) per CRIT-06.
- Commit the 10 local-only `cycle-85-2026-07-01/*.md` per-agent provenance files.
- `.gitignore`: whitelist the new cycle-1 plan/deferred files (pattern from prior cycles).

### WP15 — drizzle-kit pin (C1-30, Low/High)
- Verify the current published stable `drizzle-kit` release compatible with `drizzle-orm@^0.45` (docs
  lookup); repin from the git-hash snapshot `1.0.0-beta.9-e89174b` to that stable with a range; run
  gates. If a safe pin cannot be verified this cycle, record in the deferred register with exit criterion.

## Deferred this cycle

See `.context/plans/cycle-1-2026-07-06-deferred.md` (C1-31, C1-32 broad part, C1-33 measurement part,
C1-13 startup fail-loud part, C1-25(a) admin UI product decision, C1-11 operator topology confirmation,
C1-30 fallback). Carry-forward broad register: `.context/plans/cycle-96-2026-07-01-deferred.md`
(C77-ARCH-01 leaves it via WP3; C94-11 leaves it via WP6).

## Validation (gates, run from repo root, whole repo)

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `BASE_URL=https://gallery.atik.kr npm run build` (root `npm run build`)
7. `npm test --workspace=apps/web`
8. `npm run test:e2e --workspace=apps/web` (requires local MySQL; record honestly if infra-blocked)

Then: `git diff --cached --check`, GPG-signed conventional+gitmoji commits (fine-grained, one per work
package or tighter), `git pull --rebase` before every push, push, then `npm run deploy` once.

## Progress

- [x] Review fan-out completed (12 lanes; 8 re-spawned after session-limit kill).
- [x] Aggregate written (cycle dir + top-level pointer).
- [x] Implementation plan created.
- [x] Deferred register created.
- [x] WP1 rate-limiter fast path
- [x] WP2 backup/restore completeness
- [x] WP3 restore mutation fence
- [x] WP4 image queue robustness
- [x] WP5 topic temp age gate
- [x] WP6 COUNT(*) OVER() removal
- [x] WP7 a11y fixes
- [x] WP8 small route fixes
- [x] WP9 security hygiene
- [x] WP10 masonry quantization
- [x] WP11 test hardening
- [x] WP12 module cycle break
- [x] WP13 docs corrections
- [x] WP14 ledger repair
- [x] WP15 drizzle-kit pin
- [x] All gates green
- [ ] Committed, pulled --rebase, pushed
- [ ] Deployed (`npm run deploy`, once)

## Gate-discovered findings (implementation phase)

- `C1-35` (High/High): `SEPARATOR CHAR(1)` ER_PARSE_ERROR broke every `/s/[key]` shared-link render at
  the inherited HEAD — fixed in `4d02f695`; pinned by `shared-link-runtime-contracts.test.ts`.
- `C1-36` (Medium/High): `sharedGroup.backToSharedPhotos` missing from both locales while rendered by
  `/g/[key]` — fixed in `4d02f695`; broad usage-scan validation deferred as `C1-36(b)`.

## Gate evidence

1. `npm run lint --workspace=apps/web` — passed.
2. `npm run lint:api-auth --workspace=apps/web` — passed.
3. `npm run lint:action-origin --workspace=apps/web` — passed (all mutating actions guarded; barrier
   slots acquired after the origin guard per the scanner's pre-guard side-effect contract).
4. `npm run lint:public-route-rate-limit --workspace=apps/web` — passed.
5. `npm run typecheck --workspace=apps/web` — passed (typecheck:app incl. tests + typecheck:scripts).
6. `BASE_URL=https://gallery.atik.kr npm run build` — passed (compiled successfully; static generation OK).
7. `npm test` (full vitest) — 308 files passed / 2 skipped; 2908 tests passed / 4 skipped.
8. `npm run test:e2e --workspace=apps/web` — first run FAILED and surfaced two real inherited production
   bugs (C1-35 SQL separator parse error; C1-36 missing i18n key) — both fixed this cycle; final re-run
   result recorded below before push.

## Gate fix iterations

- failed-image-retry pinned the pre-fix UTF-16 truncation shape — updated to pin the code-point-safe contract.
- data-tag-names-sql pinned the `COUNT(*) OVER()` window column — retired per the C1-32 policy and
  replaced with lean-parallel-count contracts.
- image-queue-quiesce pinned the unguarded embedding-retry launch — updated for the C1-06 in-flight guard.
- og-route-rate-limit-behavior replicated the unversioned topic OG ETag — updated for C1-14.
- nginx-config forbids the XFF append-form directive string — C1-11 comment reworded to avoid the literal.
- action-origin scanner classifies `acquire*` imports as pre-guard side effects — barrier slots moved
  after `requireSameOriginAdmin()` (also the better security posture).
- Next `ProcessEnv` augmentation (readonly required NODE_ENV) — migrate-child env typed accordingly.
