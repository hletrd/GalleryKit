# Cycle 46/100 Implementation Plan

Start HEAD: `3aa470dd`.
Review aggregate: `.context/reviews/cycle-46-2026-07-01/_aggregate.md`.

## Goal

Close the two Cycle 46 findings without changing unrelated behavior, then run all required gates, commit, push, and deploy with `npm run deploy`.

## Scheduled Fixes

### C46-F1 - Deleted-mid-reencode cleanup on encode failure

- Add an in-app runner row-existence check in the `processImageFormats()` catch path.
- If the row disappeared, reuse `cleanupDeletedMidReencodeVariants()` and return `deleted-mid-reencode`.
- Add a regression test proving an encode failure after deletion is not counted as `encode-failed`.
- Add the equivalent sidecar `reprocessRow()` path, with a DB-backed row-exists callback from `main()`.

### C46-F2 - Expire stale SW image cache entries on failed/offline probes

- Add a derivative image max-age constant matching the one-hour cache policy.
- Stamp cached derivative responses with `sw-cached-at`.
- On cached hits, evict and return a network-error response if the cache entry is too old and cannot be revalidated.
- Update contract tests and regenerate `apps/web/public/sw.js`.

## Deferred This Cycle

No new Cycle 46 findings are deferred. Prior carry-forward deferrals are recorded in `.context/plans/cycle-46-2026-07-01-deferred.md`.

## Regression Coverage

- Focused Vitest coverage for the in-app encode-failure deleted-row branch.
- Focused sidecar coverage for encode-failure deleted-row classification and cleanup.
- Service-worker source-contract coverage for cached-at stamping, max-age eviction, and generated `sw.js` parity.

## Validation

Run the required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then sign, `git pull --rebase`, push, and deploy with `npm run deploy` from the repo root.

## Progress

- [x] Review findings aggregated.
- [x] Implementation plan created.
- [x] Implement C46-F1.
- [x] Implement C46-F2.
- [x] Run required gates.
- [x] Commit, push, deploy. Source commit `ab38f260` pushed to `origin/master`; Cycle 47 invocation states `ab38f260` was the current deployed `master` HEAD at start, confirming the Cycle 46 fixes were deployed before Cycle 47 began.

## Gate Evidence

- `npm run lint --workspace=apps/web` - pass
- `npm run lint:api-auth --workspace=apps/web` - pass
- `npm run lint:action-origin --workspace=apps/web` - pass
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass
- `npm run typecheck --workspace=apps/web` - pass
- `npm run build --workspace=apps/web` - pass
- `npm test --workspace=apps/web` - pass, 2704 passed / 4 skipped
