# Cycle 15/100 Implementation Plan - Recovery Run

Status: IMPLEMENTED - GATES GREEN, PUSH/DEPLOY PENDING
Source: `.context/reviews/_aggregate.md` at HEAD `6256a988`
Deferred register: `.context/plans/cycle-15-2026-07-08-deferred.md`

Repo rules read before planning/deferral: `CLAUDE.md`, `AGENTS.md`, `.context/plans/README.md`, `.context/plans/deferred-carry-forward.md`, current `.context/reviews/_aggregate.md`, and current `.context/plans/**` indexes. No `.context/project/`, `.context/development/`, `.cursorrules`, `CONTRIBUTING.md`, or docs style/policy files were present in this checkout. Future work remains bound by GPG-signed Conventional Commits with gitmoji, `git pull --rebase` before push, no force push, no `--no-verify`, the full app gates, and the configured per-cycle deploy.

Every `AGG-C15-*` finding is scheduled below or explicitly recorded in the deferred register with original severity/confidence and an exit criterion. Security, privacy, restore/data-loss, and auth/session defects are scheduled unless an explicit repo rule documents the current behavior as an accepted operator boundary.

## Legend

- `[ ]` pending
- `[~]` in progress
- `[x]` done
- `[defer]` recorded in deferred register

## Scheduled Work Packages

### WP1 - Auth and Restore Mutation Barriers

Findings: `AGG-C15-01`, `AGG-C15-02`, `AGG-C15-03`, `AGG-C15-04`

Scope:

- Add restore/admin mutation-slot coverage to `login` before its first DB mutation and hold it through rate-limit/session writes.
- Make token-authenticated admin usage tracking restore-safe so `admin_tokens.last_used_at` cannot write during restore before route guards run.
- Guard the in-app color-backfill trigger with the same foreground mutation barrier or a restore-safe equivalent.
- Fix `logout` so pending session revocation is queued when the DB session delete fails.
- Add focused regression tests/source-contract tests for ordering and failure behavior.

Acceptance:

- Auth actions keep same-origin semantics and localized return shapes.
- Restore drain cannot miss login, token usage tracking, or admin backfill trigger writes.
- A failed logout DB delete does not mark revocation successful.

Progress:

- [x] Inspect auth/API/backfill call flow and existing tests.
- [x] Implement barrier/queue fixes.
- [x] Add targeted tests.

### WP2 - Restore SQL Scan and Queue Quiesce Hardening

Findings: `AGG-C15-05`, `AGG-C15-07`, `AGG-C15-08`, `AGG-C15-35`

Scope:

- Add a bounded timeout path to `quiesceImageProcessingQueueForRestore()` and let restore abort cleanly instead of hanging forever.
- Fix the restore SQL scan file-read loop so short reads do not skip bytes.
- Fix allowed app-table `DROP TABLE` masking across chunk-boundary splits inside table names.
- Return the typed restore failure shape for rare scan file IO errors.
- Add tests for timeout/short-read/chunk-boundary behavior where feasible.

Acceptance:

- Restore drains either complete or fail closed with existing translated error behavior.
- Scanner consumes every byte from the dump stream under short reads.
- Legitimate own-schema drop statements split across chunk boundaries are accepted; dangerous targets remain rejected.

Progress:

- [x] Inspect restore scanner and quiesce tests.
- [x] Implement scanner/quiesce fixes.
- [x] Add/extend tests.

### WP3 - Upload Privacy and Legacy Original Guard

Findings: `AGG-C15-06`, `AGG-C15-38`

Scope:

- Make truncated ISOBMFF `iinf` boxes fail closed in GPS stripping.
- Extend legacy public-original startup guard to recursively reject nested files, symlinks, and non-empty legacy content, or document and test any intentionally narrower contract. This plan selects the stricter recursive guard because it is a privacy boundary.
- Add regression tests for malformed `iinf` and nested/symlink legacy-original entries.

Acceptance:

- Structurally incomplete HEIC/HEIF/AVIF item catalogs do not produce a false "clean" GPS result.
- Production startup guard proves the legacy public-original tree has no residual content.

Progress:

- [x] Inspect GPS stripper and upload-path tests.
- [x] Implement privacy guard fixes.
- [x] Add targeted tests.

### WP4 - Topic, Queue, Lock, and Process-State Correctness

Findings: `AGG-C15-09`, `AGG-C15-30`, `AGG-C15-32`, `AGG-C15-33`, `AGG-C15-34`, `AGG-C15-36`, `AGG-C15-37`

Scope:

- Put `deleteTopicAlias()` under the topic route-segment advisory lock.
- Clear image-queue claim retry counts after a successful claim, independent of processing retry scheduling.
- Reset embedding scan cursor id and model version together during restore quiesce.
- Re-arm single-writer boot-time reprobe through the existing reacquire scheduler and correct retry wording.
- Normalize upload-processing contract lock release/error handling through the shared advisory-lock release helper.
- Fix the restore-maintenance durable dirname helper for bare root marker paths.
- Add the zero-size guard to service-worker LRU `recordAndEvict` in both reference and template, then regenerate `sw.js`.

Acceptance:

- Route mutations serialize consistently.
- Queue/process lifecycle state resets are paired and bounded.
- Advisory-lock and SW cache invariants are test-locked.

Progress:

- [x] Inspect each helper and existing tests.
- [x] Implement small correctness fixes.
- [x] Regenerate service worker if template changes.
- [x] Add or update focused tests/source-contracts.

### WP5 - Current-Cycle Provenance and Operator Docs

Findings: `AGG-C15-39`, `AGG-C15-40`, `AGG-C15-41`, `AGG-C15-42`, `AGG-C15-43`, `AGG-C15-44`, `AGG-C15-45`, `AGG-C15-46`

Scope:

- Publish the current aggregate and update plan indexes so Cycle 15 no longer points at stale Cycle 14 evidence.
- Refresh CLIP backfill script header/examples to match current production sidecar runbook.
- Refresh alt-text backfill comments to match current local hint implementation and operator controls.
- Document migrations 0028/0029 and current indexes where appropriate.
- Document pending session revocation queue and flush paths after WP1 fix.
- Document `site-config.json` `copyright`.
- Correct the migration comment's non-monotonic journal example month.
- Add concise CLAUDE notes for advisory-lock release, pending-session revocation, and watchdog helper invariants if missing.

Acceptance:

- Plan/review provenance is traceable to the current aggregate.
- Operator examples do not contradict current source.
- Documentation updates do not claim unsupported product behavior.

Progress:

- [x] Current aggregate written at `.context/reviews/_aggregate.md`.
- [x] Update plan index and docs/scripts.

## Gate Plan

Run the full configured gates after implementation and before commit/push:

- [x] `npm run lint --workspace=apps/web`
- [x] `npm run lint:api-auth --workspace=apps/web`
- [x] `npm run lint:action-origin --workspace=apps/web`
- [x] `npm run lint:public-route-rate-limit --workspace=apps/web`
- [x] `npm run typecheck --workspace=apps/web`
- [x] `npm run build --workspace=apps/web`
- [x] `npm test --workspace=apps/web`
- [x] `npm run test:e2e --workspace=apps/web` not required; no browser-flow behavior changed

## Deployment

After all scheduled work is committed and pushed and all required gates pass, run the configured per-cycle deploy command exactly once:

```bash
npm run deploy
```

Record the deploy result in the final cycle report.

## Deferred Summary

All non-scheduled findings are recorded in `.context/plans/cycle-15-2026-07-08-deferred.md`; none are silently dropped. Deferrals are limited to repo-documented operator boundaries, large performance/schema/architecture work, UX/product redesign, browser-matrix/coverage-policy investments, or manual validation items.
