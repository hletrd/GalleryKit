# Cycle 75/100 Implementation Plan

Start HEAD: `29f4176df6d0147301dbf7cfc3928125fb0a049c`.
Review aggregate: `.context/reviews/cycle-75-2026-07-01/_aggregate.md`.

## Goal

Fix the safe, reviewable Cycle 75 findings, keep review/plan ledgers current, run all required gates, commit, push, and deploy with `npm run deploy` from the repo root.

## Scheduled Fixes

1. `C75-01`: add conservative per-photo OG ETags and a 304 branch before derivative fetch/render; include route coverage that proves matching validators skip expensive work.
2. `C75-02`: add a shared `If-None-Match` matcher that uses weak entity-tag comparison and wire feeds, upload derivative serving, and topic OG conditional responses through it.
3. `C75-03`: add direct behavior coverage for `getImageProcessingState()` so pending rows are not only protected by source-grep and route mocks.
4. `C75-04`: close the Cycle 74 plan/index/aggregate ledger using signed HEAD `29f4176d` and Cycle 75's deployed-start evidence.
5. `C75-05`: keep the mobile bottom-sheet download menu inside the modal focus-trap subtree by allowing a contained dropdown portal and source-locking that wiring.
6. `C75-06`: make empty Atom feeds use a stable empty-feed timestamp and add root/topic route tests proving stable ETags and 304s.
7. `C75-07`: update the OG rate-limit rollback comment to match the current single pre-DB rollback invariant.

## Deferred This Cycle

New deferred item:

- `C75-08`: bulk-edit validation field association, recorded in `.context/plans/cycle-75-2026-07-01-deferred.md`.

Carry-forward deferred items remain referenced in the Cycle 75 deferred file.

## Regression Coverage

- `http-etag.test.ts`: shared `If-None-Match` weak comparison, wildcard handling, malformed candidates, and comma-in-opaque-tag parsing.
- `feed-conditional.test.ts`: strong/weak equivalent validators for weak feed ETags, empty root/topic feed stable ETags, and 304s.
- `serve-upload.test.ts`: strong equivalent validator matches the weak derivative ETag.
- `og-route-rate-limit-behavior.test.ts`: topic OG weak validator 304 and per-photo OG 304 before derivative fetch/render.
- `image-processing-state-data.test.ts`: invalid ids, pending rows, processed rows, and misses for `getImageProcessingState()`.
- `bottom-sheet-dropdown-portal.test.ts`: contained dropdown portal support and bottom-sheet download menu wiring.

## Validation

Run required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then sign, `git pull --rebase`, push, and deploy with `npm run deploy`.

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run required gates.
- [x] Commit, pull --rebase, push.
- [x] Deploy with `npm run deploy`.

## Gate Evidence

- Focused regression: `npm test --workspace=apps/web -- --run src/__tests__/http-etag.test.ts src/__tests__/feed-conditional.test.ts src/__tests__/feed-sized-derivative.test.ts src/__tests__/serve-upload.test.ts src/__tests__/og-route-rate-limit-behavior.test.ts src/__tests__/image-processing-state-data.test.ts src/__tests__/bottom-sheet-dropdown-portal.test.ts src/__tests__/og-photo-fallback.test.ts` - pass: 8 files passed, 74 tests passed.
- `npm run lint --workspace=apps/web` - initial run failed on `react-hooks/refs` for reading `sheetRef.current` during render in `info-bottom-sheet.tsx`; fixed with a callback ref + state container.
- `npm run lint --workspace=apps/web` - pass after fix iteration.
- `npm run lint:api-auth --workspace=apps/web` - pass: both admin API routes OK.
- `npm run lint:action-origin --workspace=apps/web` - pass: all mutating server actions enforce same-origin provenance.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
- `npm run typecheck --workspace=apps/web` - pass.
- `npm run build --workspace=apps/web` - pass. Local sitemap generation logged the expected unavailable local MySQL fallback and exited successfully.
- `npm test --workspace=apps/web` - pass: 301 files passed, 2 skipped; 2805 tests passed, 4 skipped.

## Gate Fix Iterations

- 1.

## Terminal Evidence

- Commit/push/deploy evidence: Cycle 76 started from deployed `master` HEAD `a295ae4432f071c374cb68278a706f5a516ae593` per the Cycle 76 invocation.
