# Cycle 16/100 Implementation Plan

Date: 2026-06-30 KST
Status: COMPLETE
Source reviews: `.context/reviews/*.md`, aggregate `.context/reviews/_aggregate.md`

Repo rules reviewed before planning: `CLAUDE.md`, `AGENTS.md`, `.context/plans/README.md`, current `.context` review/plan history. No `.cursorrules`, `CONTRIBUTING.md`, or `docs/` policy files were present. Scheduled work remains bound by repo policy: small reversible diffs, no new dependencies, GPG-signed Conventional Commits with gitmoji, `git pull --rebase` before push, full quality gates, then `npm run deploy` for this per-cycle run.

## Scheduled Fixes

| ID | Finding(s) | Severity / confidence | Implementation plan | Progress |
| --- | --- | --- | --- | --- |
| C16-P01 | AGG-C16-03 DB-backed rate-limit rollback can subtract unpersisted attempts | Medium / High | Thread DB-increment success through affected rate-limit admission paths and skip `decrementRateLimit` when the current request's persistent increment failed. Add regression coverage for load-more/search/share/admin-user rollback branches. | DONE |
| C16-P02 | AGG-C16-04 sitemap stale `lastModified`; AGG-C16-05 tag-only feed false 304 | Medium / High; Medium / High | Select/use `images.updated_at` for sitemap photo `lastModified`. Touch parent image `updated_at` when tag links are actually inserted/deleted in tag actions and bulk image tag edits. Add sitemap/feed freshness tests. | DONE |
| C16-P03 | AGG-C16-06 unbounded smart-collection AST breadth | Medium / High | Add shared smart-collection structural limits for group child count, total AST node/predicate count, and serialized JSON bytes. Reject create/update payloads beyond limits and add parser/action tests. | DONE |
| C16-P04 | AGG-C16-07 Lightroom cookie upload attribution; AGG-C16-19 stale `withAdminAuth` token-argument comment | Low / High; Low / High | Resolve the cookie-authenticated admin actor for LR uploads when PAT context is absent, use it for `uploaded_by` and audit, and update `withAdminAuth` docs to describe `getAdminAuthToken(request)` accurately. Add route/source coverage. | DONE |
| C16-P05 | AGG-C16-08 public API scanner local-helper blind spot; AGG-C16-09 action-origin catch/finally blind spot; AGG-C16-10 scanner/docs message drift; AGG-C16-21 read-only public action rate-limit gate gap | Medium / High; Medium / High; Low / High; Low / Medium | Add failing fixtures first, harden scanner traversal for local helper and catch/finally mutation shapes, and align the docs/error message with early-return dominance. Add a lightweight public read-action rate-limit gate or scanner fixture coverage for exempt public reads. | DONE |
| C16-P06 | AGG-C16-11 home DB-unavailable broken/loading shell | High / High | Catch non-critical homepage metadata latest-image failures and render a stable public unavailable state when listing data queries fail, while preserving logs for real DB/schema errors. Add a focused test/source assertion where practical. | DONE |
| C16-P07 | AGG-C16-12 off-screen search keyboard highlight; AGG-C16-13 map marker bypasses popup; AGG-C16-26 duplicate login error announcements | Medium / High; Medium / Medium; Low / Medium | Keep search result refs and scroll active option into view. Change marker click behavior to expose the popup/open affordance consistently. Use inline login errors as the primary validation announcement and avoid duplicate toast announcements for the same failed login. Add UI/source tests where existing harnesses support it. | DONE |
| C16-P08 | AGG-C16-18 topic delete race reports generic failure | Low / Medium | Map MySQL FK-reference errors from topic deletion to `cannotDeleteCategoryWithImages` so concurrency-safe DB rejection produces the expected user-facing message. Add action-level coverage. | DONE |
| C16-P09 | AGG-C16-22 upload 2 GiB/proxy cap mismatch; AGG-C16-23 Lightroom wording; AGG-C16-24 under-proven "high-performance"; AGG-C16-25 "HDR-capable" wording | Medium / High; Low / High; Low / Medium; Low / Medium | Adjust upload help/docs to distinguish per-request proxy cap from rolling app quota, standardize LR copy on external/PAT upload clients, soften or qualify performance copy, and rename HDR badge/copy toward source-vs-delivery honesty. Update English/Korean messages and docs consistently. | DONE |
| C16-P10 | AGG-C16-02 nginx behind-edge client identity mismatch | Medium / High | Make the checked-in nginx topology contract explicit and less misleading: document that real-IP normalization must be configured with trusted edge CIDRs before using this template behind a TLS edge/LB, and add source/config guard coverage for the header/topology contract where practical. | DONE |

## Deferred Findings

All other aggregate findings are recorded in `.context/plans/cycle-16-2026-06-30-deferred.md` with original severity/confidence, reason, and exit criterion.

## Required Gates

- `npm run lint --workspace=apps/web`
- `npm run lint:api-auth --workspace=apps/web`
- `npm run lint:action-origin --workspace=apps/web`
- `npm run lint:public-route-rate-limit --workspace=apps/web`
- `npm run typecheck --workspace=apps/web`
- `npm run build --workspace=apps/web`
- `npm test --workspace=apps/web`

Gate evidence collected in cycle 16:

- PASS `npm run lint --workspace=apps/web`
- PASS `npm run lint:api-auth --workspace=apps/web`
- PASS `npm run lint:action-origin --workspace=apps/web`
- PASS `npm run lint:public-route-rate-limit --workspace=apps/web`
- PASS `npm run typecheck --workspace=apps/web`
- PASS `npm run build --workspace=apps/web`
- PASS `npm test --workspace=apps/web` (260 passed, 2 skipped; 2425 tests passed, 4 skipped)

## Deployment

Deploy mode: per-cycle. After all scheduled fixes are implemented, committed, pushed, and gates are green, run exactly:

```sh
npm run deploy
```
