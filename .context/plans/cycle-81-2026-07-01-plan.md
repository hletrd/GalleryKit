# Cycle 81/100 Implementation Plan

Start HEAD: `4733d475be8f19fbddf4b82b589e28d6ca083992`.
Review aggregate: `.context/reviews/cycle-81-2026-07-01/_aggregate.md`.

## Goal

Close the safe Cycle 81 findings, keep review/plan ledgers current, run all required gates, commit/push all work, and deploy with `npm run deploy` from the repo root.

## Scheduled Fixes

1. `C81-01`: route public map marker `displayTitle` through `getPhotoDisplayTitle()` so empty, whitespace-only, and filename-like titles fall back to the localized photo label instead of becoming map alt/button/list text.
2. `C81-02`: close Cycle 80 release-ledger drift by recording signed commit/push evidence, recording the explicit Cycle 80 deploy-evidence gap, and moving Cycle 80 from active to recent.

## Deferred This Cycle

No new Cycle 81 findings are deferred.

Carry-forward deferred items remain in `.context/plans/cycle-81-2026-07-01-deferred.md`.

## Regression Coverage

- Map accessibility: the map source contract must import/use `getPhotoDisplayTitle()` and must not restore the raw `img.title ?? tPhoto('titleWithId', { id: img.id })` fallback.
- Ledger: Cycle 80 must no longer be listed as active, and Cycle 80's terminal release state must record commit/push evidence plus the deploy-evidence gap.

## Validation

Run required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then signed commit, `git pull --rebase`, push, and deploy with `npm run deploy`.

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run required gates.
- [x] Commit, pull --rebase, push.
- [x] Deploy with `npm run deploy`.

## Gate Evidence

- Focused regression: `npm test --workspace=apps/web -- --run src/__tests__/map-thumb-wiring.test.ts src/__tests__/photo-title.test.ts src/__tests__/alt-text-fallback.test.ts` - pass: 3 files passed, 33 tests passed.
- `npm run lint --workspace=apps/web` - pass.
- `npm run lint:api-auth --workspace=apps/web` - pass: both admin API routes OK.
- `npm run lint:action-origin --workspace=apps/web` - pass: all mutating server actions enforce same-origin provenance.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
- `npm run typecheck --workspace=apps/web` - pass.
- `npm run build --workspace=apps/web` - pass. Local sitemap generation logged the expected unavailable local MySQL fallback and exited successfully.
- `npm test --workspace=apps/web` - pass: 301 files passed, 2 skipped; 2830 tests passed, 4 skipped.
- `git diff --cached --check` - pass after one review-artifact whitespace fix iteration.
- Commit/push evidence recorded during Cycle 82: signed HEAD `c272c5217ffdf1d324f001d8c35145262be310b4` matched `origin/master` and remote `refs/heads/master`; `git log -1 --show-signature` reported a good signature from `Jiyong Youn <01@0101010101.com>`.
- Deploy evidence recorded during Cycle 82: the Cycle 82 invocation identified the current deployed `master` HEAD at start as `c272c521`, so Cycle 81's pushed map-title fix was deployed before Cycle 82 began.

## Gate Fix Iterations

1. Removed trailing spaces in `security-reviewer.md` and the extra EOF blank line in `designer-accessibility.md` after `git diff --cached --check` reported them.
