# Cycle 63/100 Implementation Plan

Start HEAD: `ecfda466cab14cd6a9ffbe03e6dc7d42023c8e82` at cycle start.
Review aggregate: `.context/reviews/cycle-63-2026-07-01/_aggregate.md`.

## Goal

Fix the actionable Cycle 63 findings, keep review/plan ledgers current, run all required gates, commit, push, and deploy with `npm run deploy` from the repo root.

## Scheduled Fixes

1. `C63-01`: make search dialog results/status belong to a settled query, invalidate stale in-flight work on input changes, and use one announcement source per state.
2. `C63-02`: give admin Analytics photo/share links a 44 x 44 px pointer target and add source-contract coverage.
3. `C63-03`: correct the service-worker offline HTML comment to match the enforced share/smart/map bypass contract and regenerate `sw.js`.

## Deferred This Cycle

No new Cycle 63 findings are deferred. `C62-04` is superseded by scheduled `C63-01`.

Carry-forward items remain in `.context/plans/cycle-63-2026-07-01-deferred.md`.

## Regression Coverage

- Add source-contract coverage for search query ownership, stale-result invalidation, and one live announcement source per state.
- Add source-contract coverage for the two admin Analytics links requiring `inline-flex min-h-11 min-w-11`.
- Keep service-worker template/generated-worker parity covered by the existing `sw-template-contract` tests.

## Validation

Run required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then sign, `git pull --rebase`, push, and deploy with `npm run deploy`.

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run required gates.
- [x] Commit, pull --rebase, push.
- [x] Deploy with `npm run deploy`.

## Gate Evidence

- Focused regression: `npm test --workspace=apps/web -- search-status-source analytics-link-touch-targets sw-template-contract search-disclaimer` - pass: 4 files passed, 33 tests passed.
- Targeted stale-response rerun after contract update: `npm test --workspace=apps/web -- search-stale-response search-status-source` - pass: 2 files passed, 5 tests passed.
- `npm run lint --workspace=apps/web` - pass.
- `npm run lint:api-auth --workspace=apps/web` - pass.
- `npm run lint:action-origin --workspace=apps/web` - pass.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
- `npm run typecheck --workspace=apps/web` - pass.
- `npm run build --workspace=apps/web` - pass. Local sitemap generation logged the expected unavailable local MySQL fallback and exited successfully.
- `npm test --workspace=apps/web` - pass: 289 files passed, 2 skipped; 2746 tests passed, 4 skipped.
- `git diff --cached --check` - pass before commit.
- Signed implementation commit: `254a68c2c9424040691903b13ff363ba0dabedad`; GPG verified as a good signature from Jiyong Youn; no `Co-Authored-By` trailer.
- Required `git pull --rebase` before push: pass, branch was up to date.
- Push: pass, `ecfda466..254a68c2 master -> master`.
- Deploy: `npm run deploy` from repo root - pass. Remote checkout reached `254a68c2`, `gallerykit-web` reported `healthy`, and `https://gallery.atik.kr/api/live` returned `{"status":"ok"}`.

## Gate Fix Iterations

2 source-contract updates: `search-disclaimer.test.ts` before the full gate sequence, and `search-stale-response.test.ts` after the first full Vitest run matched the old `searchImagesAction(searchQuery)` string.
