# Run-10 Cycle 24/100 Implementation Plan

Status: IMPLEMENTED - GATES PASSED; PUSH/DEPLOY PENDING
Aggregate: `.context/reviews/_aggregate.md`
Date: 2026-07-08 KST
Review start HEAD: `0f3e48e044bf0e6a8019f8910dd649d706a9e91b`
Planning HEAD: `4b43fad7ab471287b82fe5c8dac85c05c511220a`

## Scope

This plan schedules every contained Cycle 24 finding that can be fixed safely in this cycle without broad architecture, operator, product, or live-host authority. Every aggregate finding not scheduled here is recorded in `cycle-24-2026-07-08-deferred.md` with original severity/confidence, citation, reason, and exit criterion.

Repo rules read before scheduling/deferring: `CLAUDE.md`, `AGENTS.md`, `.context/plans/README.md`, `.context/plans/deferred-carry-forward.md`, Cycle 23 plan/deferred, current Cycle 24 review artifacts, and docs/style/policy search result. No `.cursorrules` or `CONTRIBUTING.md` exists in this checkout; no additional tracked docs policy file was found outside `CLAUDE.md`/`AGENTS.md`/`.context/**`.

## Scheduled Work Packages

### WP1 - Replace false-confidence auth rollback source assertion

Findings: `AGG-C24-04`

Files:

- `apps/web/src/__tests__/auth-rate-limit-rollback.test.ts`
- `apps/web/src/app/actions/auth.ts` only if a behavior-test seam is required

Plan:

1. Remove or harden the parser helper that currently scans past the named function and finds the wrong `catch` block.
2. Prefer a behavior test around `login()` that forces an error after login rate-limit pre-increment and asserts login rollback helpers are not called from the verification/runtime failure path.
3. If a narrow source helper remains, make it brace-match the named function body and add a self-check that `login` resolves to the catch near `auth.ts:261`, not `updatePassword`.
4. Run the targeted auth rollback test.

Acceptance:

- The test fails if `rollbackLoginRateLimit(...)` is reintroduced into the `login()` verification/runtime catch.
- The test no longer inspects `updatePassword` while claiming to inspect `login`.

Status: implemented. The source helper now brace-matches the requested function body and the test includes a parser-scope regression proving `login` and `updatePassword` resolve to distinct outer catches.

### WP2 - Repair Cycle 23/24 provenance and carry-forward age basis

Findings: `AGG-C24-05`, `AGG-C24-06`

Files:

- `.context/plans/README.md`
- `.context/plans/cycle-23-2026-07-08-plan.md`
- `.context/plans/cycle-24-2026-07-08-plan.md`
- `.context/plans/cycle-24-2026-07-08-deferred.md`
- `.context/plans/deferred-carry-forward.md`

Plan:

1. Move Cycle 23 from active to recently completed with terminal source/push evidence and deploy supersession language.
2. List Cycle 24 plan/deferred as the active current-cycle ledger pair.
3. Update Cycle 23 plan status so future agents do not treat push/deploy as pending work.
4. Refresh the carry-forward register top text and table basis from `Age @ r10c21`/Cycle 23 prose to `Age @ r10c24`.
5. Add Cycle 24 deferred rows to the carry-forward register and preserve severity/confidence and exit criteria.

Acceptance:

- Plan index points at Cycle 24 as active and Cycle 23 as completed/superseded.
- Carry-forward register has a current `Age @ r10c24` basis and records Cycle 24 deferrals.
- Every `AGG-C24-*` finding is scheduled here or explicitly deferred in the Cycle 24 register.

Status: implemented. Cycle 24 is the active ledger, Cycle 23 is marked completed/superseded, and the carry-forward register uses an `Age @ r10c24` basis with Cycle 24 rows.

### WP3 - Fix stale admin backfill clamp comment

Findings: `AGG-C24-07`

Files:

- `apps/web/src/lib/admin-backfill-runner.ts`

Plan:

1. Change the local concurrency arithmetic comment from "silently clamped DOWN" to wording that matches runtime behavior: clamped down with a warning.
2. Keep the code behavior unchanged.

Acceptance:

- The comment matches `admin-backfill-runner.ts` warning behavior and `CLAUDE.md`.

Status: implemented. The comment now says over-cap backfill concurrency is clamped down with a warning.

### WP4 - Full gates, signed push, and per-cycle deploy

Findings: cycle quality/deploy requirement.

Plan:

1. Run every configured gate against the whole repo:
   - `npm run lint --workspace=apps/web`
   - `npm run lint:api-auth --workspace=apps/web`
   - `npm run lint:action-origin --workspace=apps/web`
   - `npm run lint:public-route-rate-limit --workspace=apps/web`
   - `npm run typecheck --workspace=apps/web`
   - `npm run build --workspace=apps/web`
   - `npm test --workspace=apps/web`
   - `npm run test:e2e --workspace=apps/web`
2. Fix all gate errors before final commit/push.
3. Commit and push signed Conventional Commit + gitmoji changes after `git pull --rebase`.
4. Run `npm run deploy` once after all plan work is pushed and gates are green.

Status: gates passed; signed commit/push and per-cycle deploy pending.

## Finding Disposition Map

Scheduled here: `AGG-C24-04`, `AGG-C24-05`, `AGG-C24-06`, `AGG-C24-07`.

Deferred in `cycle-24-2026-07-08-deferred.md`: `AGG-C24-01`, `AGG-C24-02`, `AGG-C24-03`, `AGG-C24-08`, `AGG-C24-09`, `AGG-C24-10`, `AGG-C24-11`, `AGG-C24-12`, `AGG-C24-13`, `AGG-C24-14`, `AGG-C24-15`, `AGG-C24-16`, `AGG-C24-17`, `AGG-C24-18`, `AGG-C24-19`, `AGG-C24-20`, `AGG-C24-21`, `AGG-C24-22`, `AGG-C24-23`, `AGG-C24-24`, `AGG-C24-25`, `AGG-C24-26`, `AGG-C24-27`, `AGG-C24-28`, `AGG-C24-29`, `AGG-C24-30`, `AGG-C24-31`, `AGG-C24-32`, `AGG-C24-33`, `AGG-C24-34`, `AGG-C24-35`.

## Progress

- [x] WP1 auth rollback test.
- [x] WP2 provenance and carry-forward ledgers.
- [x] WP3 backfill clamp comment.
- [x] WP4 full gates. Signed commit/push and per-cycle deploy pending.

## Gate Evidence

- Targeted: `npm test --workspace=apps/web -- --run src/__tests__/auth-rate-limit-rollback.test.ts` - PASS (1 file, 4 tests).
- `npm run lint --workspace=apps/web` - PASS.
- `npm run lint:api-auth --workspace=apps/web` - PASS.
- `npm run lint:action-origin --workspace=apps/web` - PASS.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - PASS.
- `npm run typecheck --workspace=apps/web` - PASS.
- `npm run build --workspace=apps/web` - PASS (Next.js 16.2.10 production build).
- `npm test --workspace=apps/web` - PASS (360 files passed, 2 skipped; 3366 tests passed, 4 skipped).
- `npm run test:e2e --workspace=apps/web` - PASS (45 passed, 2 skipped).
