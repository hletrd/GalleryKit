# Cycle 11/100 Implementation Plan — 2026-07-18

Start HEAD: `7e40e95c`
Source: `.context/reviews/_aggregate.md` and all Cycle 11 provenance reviews
Status: **Source implementation published; original deploy result unknown**

## Finding disposition

Every unique Cycle 11 finding is scheduled below. No Cycle 11 finding is
deferred. `COR-C11-01` is a correctness/data-contract defect and
`TEST-C11-02` is a fired High/High schema-safety validation obligation, so
neither is eligible for deferral. `PERF-C11-03` has a bounded fix and a direct
browser regression. Existing carry-forward rows whose recorded schema-cycle
exit criteria fired are also scheduled; no finding is silently re-listed.

## Work packages

- [x] **C11-01 truthful delivered maximum (COR-C11-01, Medium/High):** make
  `processImageFormats` return the smaller of the effective processing width
  and the largest normalized configured derivative. Preserve the non-empty
  ladder invariant and existing no-upscale/WI-15 behavior.
- [x] **C11-01 producer proof:** add a real Sharp encode test with a source
  wider than its largest configured size. Decode the largest/base outputs and
  prove `derivativeMaxWidth` equals the largest delivered file, not the source
  ceiling. Keep existing queue/admin/sidecar persistence fixtures green.
- [x] **C11-02 disposable MySQL schema-convergence gate (TEST-C11-02,
  High/High):** export the reconcile entry point for the gate, add a script
  that refuses non-local or non-test/CI/e2e database targets, mutates only an
  explicitly disposable initialized database, removes the latest schema
  artifacts, runs reconcile, compares the recovered live schema with the
  current pre-mutation contract, and proves a second reconcile is idempotent.
- [x] **C11-02 CI wiring:** run the convergence gate after `npm run init` in
  the MySQL-backed quality job, pin the latest probed migration tag so every
  future schema authoring cycle must advance the proof, and execute the gate
  locally against a disposable MySQL container for this cycle.
- [x] **C11-03 search prefetch (PERF-C11-03, Medium/High):** set
  `prefetch={false}` on `SearchResultItem` photo links while preserving the
  current listbox semantics, 44 px-plus row target, keyboard behavior, and
  focus restoration.
- [x] **C11-03 browser proof:** extend public Playwright search coverage with
  a request listener that proves rendering populated results sends zero
  localized photo-detail RSC requests before activation, then proves the
  selected result navigates normally.
- [x] **C11-04 on-this-day index ride-along (reopened existing C2-16,
  Medium/Medium):** use generated capture-month/day columns and a processed
  composite index so the widget no longer applies non-sargable `MONTH()` and
  `DAY()` functions to every processed capture date. Update the query to use
  the indexed fields and cover leap-day/null/normal matching.
- [x] **C11-05 listing keyset index ride-along (reopened existing AGG-C20-12,
  Medium/Medium):** extend the processed and topic capture-date indexes with
  the final `id` tie-breaker used by the canonical
  `capture_date, created_at, id` ordering. Replace the old definitions instead
  of retaining overlapping indexes, and make reconcile repair mismatched
  named index definitions.
- [x] **C11-04/C11-05 migration contract:** author one monotonic 0032
  migration containing the generated columns/index and the two index-shape
  replacements; mirror schema, reconcile, privacy/internal-field treatment,
  journal, tests, and `CLAUDE.md` index/runbook documentation. The migration
  must be DDL-only and pass the disposable convergence gate.
- [x] **Carry-forward hygiene:** retire stale `C2-21` and
  `C8b-04/PERF8-BF-01` rows with citations to their already-shipped 0029/0030
  indexes. Preserve `C94-10/C88-03` at Medium/High and add the required
  approximately-16-cycle re-justification: unrelated image-delivery/index
  DDL does not satisfy its dedicated multi-model embedding-storage exit
  criterion, and opportunistically changing embedding PK/write/query/GC
  semantics would be a separate product/data migration.
- [x] **Cycle 10 release ledger:** archive Cycle 10 and record its prior-cycle
  signed publication plus orchestrator-reported `per-cycle-success`, without
  inventing a deployed SHA. Advance `.context/plans/README.md` to Cycle 11.
- [x] Run every configured blocking gate against the whole repository:
  ESLint, API-auth lint, action-origin/mutation-barrier lint, public-route-rate-
  limit lint, app/script typecheck, production build, full Vitest, production
  dependency audit, and full Playwright (browser-flow coverage changed).
- [x] Record focused/full gate evidence and `GATE_FIXES`. Fix every error at
  root cause without suppression; record any unavoidable warning under the
  strict deferred rules.
- [x] Commit coherent work in fine-grained GPG-signed gitmoji Conventional
  Commits, `git pull --rebase`, and push `master`. The original Cycle 11 deploy
  result is not inferable from Git and remains explicitly unknown; Cycle 12's
  required deploy will supersede the published source state.

## Deferred finding register

No Cycle 11 finding is deferred. Existing still-open carry-forward items remain
governed by `.context/plans/deferred-carry-forward.md` with original severity,
confidence, citations, reasons, exit criteria, and repository policy. The
Cycle 11-specific deferral reconciliation is recorded in
`cycle-11-2026-07-18-deferred.md`.

## Acceptance criteria

- A source wider than the configured ladder persists and publishes the decoded
  width of the largest derivative/base file.
- Disposable MySQL proves the latest schema can be reconstructed by reconcile,
  matches the initialized current contract, and remains unchanged on a second
  reconcile; the gate cannot target a production-like database.
- A populated search result list generates no photo-detail RSC requests until
  a result is activated.
- On-this-day filtering is sargable through generated month/day fields and
  retains the same six-photo/date ordering semantics, including February 29.
- Listing indexes end in the `id` tie-breaker and reconcile repairs an old
  same-named shape rather than treating name presence as structural parity.
- Migration 0032 is monotonic, DDL-only, schema/reconcile symmetric, and fully
  covered by both source tripwires and live MySQL convergence.
- Stale carry-forward rows are retired, the aged embedding item is honestly
  re-justified, and Cycle 10 is archived with truthful terminal evidence.
- Every required gate passes without a new suppression; commits are signed,
  pushed, and the per-cycle deploy succeeds.

## Progress

- [x] Fourteen-perspective review coverage complete across two bundled child
  agents plus lead aggregation.
- [x] Aggregate review complete with three unique findings.
- [x] Every new finding and fired carry-forward trigger scheduled; no new
  deferral.
- [x] Cycle 10 terminal evidence reconciled for archival planning.
- [x] Source/schema/test implementation complete.
- [x] Focused validation complete.
- [x] Full required gates green.
- [x] Signed commits pushed.
- [ ] Original per-cycle deploy result unknown; do not infer from Git.

## Validation evidence

- Prompt 1 engineering gates passed ESLint, all three custom security/contract
  lints, full typecheck, and full Vitest (363 files; 3,447 tests; expected CLIP
  skips only).
- Product review independently passed lint/type/build/audit/Vitest and used the
  required browser-skill matrix against desktop/mobile, EN/KO, themes,
  keyboard/focus, offline, accessibility structure, and network behavior.
- The lead passed full app/script typecheck and four focused suites (35 tests).
- Cycle 10 implementation/review commits and terminal ledger commit have good
  GPG signatures; local and remote `master` equal `7e40e95c`.
- The prior cycle report supplied by the orchestrator records
  `DEPLOY: per-cycle-success`; no exact deployed commit is inferred.
- Focused app typecheck and 150 tests are green: 31 producer/timeline/privacy/
  convergence tests plus 119 migration/reconcile/journal tests.
- Disposable MySQL 8.4 database `gallery_ci_cycle11` passed migration 0032
  bootstrap, deliberate latest-artifact degradation, exact structured-schema
  recovery, and second-run reconcile idempotence. The temporary container was
  stopped and removed after the proof.
- Full configured gates: ESLint PASS; API-auth lint PASS; action-origin/
  mutation-barrier lint PASS; public-route-rate-limit lint PASS; app/scripts
  typecheck PASS; production Next build PASS; Vitest PASS (364 files passed,
  2 skipped; 3,452 tests passed, 4 expected CLIP skips); production dependency
  audit PASS (0 vulnerabilities); Playwright PASS (64 passed, 2 intentional
  CI capability-marker skips), including the new zero-photo-RSC-prefetch flow.
- `GATE_FIXES`: the first focused privacy run caught that inserting new omit
  fields ahead of a test's explicit extraction sentinel made the sentinel
  fail. The generated keys remain omitted, but were moved after the stable
  first key in each destructuring block; the focused suite then passed. No
  lint/build/full-suite suppression or production-code workaround was added.
- The disposable Playwright MySQL 8.4 container was stopped and removed after
  the 66-test browser run.
- Cycle 12 verified that local `master` and `origin/master` both contained the
  signed Cycle 11 publication through `ff6532f4`. This proves source
  publication only. No independent Cycle 11 deploy transcript was present, so
  deployment remains unknown rather than guessed.
