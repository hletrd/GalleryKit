# Run-10 Cycle 4/100 Implementation Plan

Start HEAD: `ec433dc4`. Review aggregate: `.context/reviews/cycle-4-2026-07-07/_aggregate.md`
(mirrored to `.context/reviews/_aggregate.md`). 12 lanes spawned, 11 completed (the
feature-dev-code-reviewer lane hung again — AGENT FAILURES section; standing rule adopted:
replace it with a general-purpose lane from cycle 5). 47 deduped findings (C4-01..C4-47).
Deferred register: `cycle-4-2026-07-07-deferred.md`. No mandatory carry-over: cycle-3's
deploy succeeded and production was verified healthy (`ec433dc4`).

Repo rules consulted: `CLAUDE.md`, `AGENTS.md`, `.context/plans/README.md` (age-budget +
C1-32 drainage policies), `deferred-carry-forward.md`, prior deferred registers.
`CONTRIBUTING.md`/`.cursorrules` absent. Commit policy: GPG-signed (-S), conventional +
gitmoji, fine-grained, `git pull --rebase` before push, no `--no-verify`.
Gates (all blocking): eslint, typecheck, vitest, build, lint:api-auth, lint:action-origin,
lint:public-route-rate-limit, playwright e2e. DEPLOY_MODE per-cycle (`npm run deploy` from
repo root, exactly once, after gates green).

Forward-honesty rule (C4-44 / INFO4-07): every WP below must state its RESIDUAL explicitly
at close time; "Closes X" is only written when no residual of the same failure class remains.

## Status legend
[ ] pending · [~] in progress · [x] done (commit noted) · [defer] moved to deferred register

## WP1 — [x] C4-01 + C4-14 + C4-35 + C4-39 + C4-41(migrate half) + C4-47: migrate.js DML-baseline guard (`b68d09e2`)
Files: `apps/web/scripts/migrate.js`, `apps/web/src/__tests__/migrate-pending-migrations.test.ts`, `CLAUDE.md`.
- Add a DML detector over journal SQL content (strip `--` comments, split on drizzle's
  `--> statement-breakpoint` + `;`, flag statements starting INSERT/UPDATE/DELETE/REPLACE).
- Guard BOTH baseline paths (DBG4-01's null-cursor branch AND TRC4-10's below-cursor
  trueDrift branch): refuse (throw, loud, with operator guidance) to baseline any
  DML-bearing entry, EXCEPT the explicit allowlist
  `LEGACY_DML_MIRRORED_BY_RECONCILE = ['0001_sync_current_schema']` whose position backfill
  is mirrored by `reconcileLegacySchema`'s one documented DML exception. This preserves the
  legacy-adoption bootstrap (current journal's only DML lives in 0001) while making a
  FUTURE DML-bearing migration impossible to swallow on any branch.
- Fix the stale `migrate.js:742-744` comment (there is no drizzle-side per-entry hash
  check — verified against installed drizzle-orm; correctness rests on MAX(created_at)).
- Tests: null-cursor + non-allowlisted DML entry throws; null-cursor DDL-only baselines
  (existing pinned behavior preserved); trueDrift DML entry throws; allowlisted 0001
  passes; (C4-47) mocked `migrate()` duplicate-DDL rejection propagates to a non-zero exit.
- CLAUDE.md: qualify the "DDL-only invariant" (C4-35/DOC4-01) with the position-backfill
  exception + the new DML-guard behavior; fix `trueDriftEntries`→`trueDrift` (C4-39);
  record the C4-43 operator-visible loud-fail behavior change note.
- Residual to declare at close: the C3-35-governed journal-`when` redesign remains
  deferred (unchanged).

## WP2 — [x] C4-06: single-writer guard self-healing re-acquire (`ce15103a`)
Files: `apps/web/src/lib/single-writer-guard.ts`, `apps/web/src/__tests__/single-writer-guard.test.ts`, `CLAUDE.md`.
- On keepalive failure OR connection 'error' lapse: warn once (existing), then schedule a
  bounded, unref'd re-acquire loop (60 s interval) reusing the reprobe machinery. On
  re-acquire success: info log, re-arm keepalive, reset `lapseWarned`. On re-acquire
  finding the lock HELD by someone else: emit the LOUD topology error (that is the
  second-instance detection C3-02 was for). On connect failure: stay quiet, retry next tick.
- `stopping` flag set at the top of `stopSingleWriterGuard`: suppresses lapse warnings
  during clean shutdown (TRC4-03 double-log), prevents reprobe/re-acquire from taking
  ownership after stop (CR4-04 class), and clears the re-acquire timer.
- Fix the misleading "at startup" wording when `openGuardConnection` fails from the
  reprobe/re-acquire path (TRC4-02).
- Tests (TEST4-06 + new behavior): keepalive-failure → re-acquire success re-arms;
  re-acquire contention → loud error; stop-during-reprobe-window never connects again;
  `.unref()` called on every timer.
- CLAUDE.md: correct the guard description (no longer "warns once and stays lapsed").
- Residual to declare at close: the guard remains detect-not-prevent by design
  (unchanged accepted boundary).

## WP3 — [x] C4-07: gallery-config detached-accessor coherence (`12037508`)
Files: `apps/web/src/lib/gallery-config.ts`, `apps/web/src/app/actions/settings.ts`,
`apps/web/src/lib/image-queue.ts`, `apps/web/src/lib/admin-backfill-runner.ts`, tests, `CLAUDE.md` if touched.
- Invalidate the micro-cache from the settings mutation after commit (PERF4-08's one-line
  fix): promote `_uncachedConfigCacheReset` to a first-class invalidation helper called by
  `updateSettings` post-commit — in the shipped single-process topology this makes
  flip-then-act exact again, closing CRIT4-01's failure scenario, not just bounding it.
- Rename `getGalleryConfigUncached` → `getGalleryConfigDetached` (ARCH4-02); keep a
  deprecated alias export for one cycle; update the 3 image-queue sites, the backfill
  runner, and the wiring/microcache tests.
- Fix the self-contradicting docstring (DOC4-02): state the ≤2 s TTL + write-invalidation
  contract explicitly.
- Bound the TTL by test: export the constant; assert it is ≤ 2000 ms (CRIT4-01's
  unbounded-TTL trap).
- Rewrite the fallback-caching test to pin the REAL contract (TEST4-02): DB-failure
  fallback IS cached for the TTL, then recovers; remove the masking manual reset; document
  the behavior in the docstring.
- Residual to declare at close: cross-process staleness (second writer) — out of scope by
  the documented single-writer topology.

## WP4 — [x] C4-02: SW LRU phantom-entry accounting (`ad1fd22d`)
Files: `apps/web/src/lib/sw-cache.ts`, `apps/web/public/sw.template.js`, regenerate
`public/sw.js`, `apps/web/src/__tests__/sw-cache.test.ts` + `sw-template-contract.test.ts`.
- In the eviction walk, decrement `total` by `entry.size` unconditionally (the entry
  leaves the tracked set either way); keep `evicted` gated on `cache.delete()` success
  exactly as-is. Mirror in the template; regenerate + commit `sw.js`.
- Test: DBG4-02's repro as a regression test (phantom entries must not force eviction of
  the just-written real entry; final meta retains the fresh entry).

## WP5 — [x] C4-08 + C4-42 + C4-26: SW respondWith de-gating + serialized recency read (`31ff51f5`)
Files: `apps/web/public/sw.template.js`, `apps/web/src/lib/sw-cache.ts` (only if shared
logic moves), regenerate `public/sw.js`, `sw-template-contract.test.ts`.
- Plumb the fetch `event` into `networkFirstHtml` and `staleWhileRevalidateImage`.
- HTML: return the network response immediately; move `cache.put` + eviction walk into
  `event.waitUntil(...)` (offline-fallback cache is best-effort; a termination-dropped put
  costs one fallback entry — the C3-10 durability rationale does NOT apply here).
- Images: keep touchMeta's lifetime coverage but stop gating the response: replace the two
  `await touchMeta(...)` confirmed-fresh sites with `event.waitUntil(touchMeta(...))`
  (durability preserved via SW lifetime extension; response returns immediately). Update
  the contract-test pins accordingly (they currently pin the `await` form).
- C4-26: route `evictExpiredCachedImage`'s meta read through `withMetaMutation` so the
  eviction READ can't race a queued touch write; WP13 records this as folded/closed.
- Regenerate + commit `sw.js`; update `sw-cache.ts` reference impl only where its
  documented mirror-contract requires.

## WP6 — [x] C4-03: photo-viewer hydration mismatch (`4afacfa8`, lint cleanup `253bf305`)
Files: `apps/web/src/components/photo-viewer.tsx`, `apps/web/e2e/` (assertion).
- Make the first render deterministic: `useState(false)` (matching SSR), then restore the
  persisted/matchMedia-derived value in a mount `useEffect` (the exact idiom
  `lightbox.tsx` already uses). Keep the persist-on-change effect.
- e2e: on a desktop-viewport photo page, assert zero console errors mentioning
  hydration (guards the whole class for this page).
- Residual to declare at close: none expected (server and client first paint agree; the
  panel opens post-mount exactly as it already visually did).

## WP7 — [x] C4-04: shared-group stepping without SSR/limiter burn (`0da58d6b`)
Files: `apps/web/src/components/photo-viewer.tsx`, shared-grid link component (prefetch),
`apps/web/e2e/swipe-visual-reset.spec.ts` (extend).
- Replace the `router.replace(?photoId=)` URL-sync effect with shallow
  `window.history.replaceState` (App Router-supported; keeps `useSearchParams` in sync)
  plus a skip-if-URL-already-matches guard (also removes the mount-time extra SSR).
- `prefetch={false}` on the shared-grid `?photoId=` tile links so viewport entry stops
  draining the share-enumeration budget.
- e2e: extend the existing single-session swipe spec — step several photos and assert the
  URL updates AND the viewer never becomes a 404 (limiter untouched by stepping).
- Residual to declare at close: the per-IP share limiter (60/min) stays as the
  anti-enumeration posture for genuinely new lookups — unchanged by design.

## WP8 — [x] C4-05: fix the false-positive fd test (folded into `12037508`)
Files: `apps/web/src/__tests__/serve-upload.test.ts`.
- Re-pin the REAL post-PERF3-07 contract: 304/HEAD never open an fd
  (`open` called exactly once — the initial GET), and the GET's one handle still closes
  exactly once (`closeSpies[0]`). Rename the test accordingly.

## WP9 — [x] C4-09: embedding-cursor operational hardening (`d7ca37de`)
Files: `apps/web/src/lib/image-queue.ts`, test, doc note inline.
- Reset `embeddingScanCursorId` (and re-scan) when the active embedding model version
  changes between invocations (PERF4-12): track the model version on state.
- Document the restart-reset caveat (DBG4-03/TRC4-05) next to the C3-07 comment: the
  cursor is process-local by design; per-commit deploys reset it; protection window is
  within-process only.
- Defer (register): durable cursor persistence + per-row embedding failure marking
  (MED4-04 root) with the exit criterion "a real permanently-un-embeddable backlog
  ≥ SEMANTIC_SCAN_LIMIT observed in production".
- Test: model-version flip resets the cursor.

## WP10 — [x] C4-12: image-zoom native non-passive touchmove (`9dccebcd`)
Files: `apps/web/src/components/image-zoom.tsx`.
- Mirror the file's own wheel pattern: attach `touchmove` via `addEventListener(...,
  { passive: false })` on the container ref so pinch/pan `preventDefault()` actually works;
  remove the dead React `onTouchMove` preventDefault path. Keep behavior identical
  otherwise (same handlers, same zoom math).

## WP11 — [x] small-fix batch (C4-19 `5f0388ed`, C4-20 `18b6cbb4`, C4-21 `e3d221e3`, C4-15 `678ebbeb`, C4-23 `d79f6f70`, C4-29 folded into `0da58d6b`)
- C4-19: normalize `image_sizes` via `parseImageSizes` (sorted) in settings-hash's no-arg
  DB path so both hash paths agree (`lib/settings-hash.ts:73`).
- C4-20: health-route DB probe — share a single in-flight probe promise so stacked
  orchestrator probes can't pile pool slots (`api/health/route.ts`); correct the C1-16
  overclaim comment.
- C4-21: extract a shared `buildDerivativeEtag(stats, settingsHash)` helper used by all
  three serve-upload ETag sites (ARCH4-05).
- C4-15: preserve the touchEnd settle animation on in-place switches — set a ref flag in
  the success branches that the `[prevId,nextId]` layout effect checks-and-clears (skip
  its hard reset once) (`photo-navigation.tsx`).
- C4-23: neighbor-preload effect keyed on neighbor ids; read `photoViewerSizes` from a ref
  so the info toggle stops re-fetching neighbors (`photo-viewer.tsx:256-305`).
- C4-29: e2e — drive one in-place switch via the next-chevron click on the shared-group
  fixture and assert the same indicator-reset outcome (extends swipe spec; no new session).

## WP12 — [x] docs/ledger batch (CLAUDE.md `31dcb276`; plans/README + apps/web README + cycle-3 plan `fe4bf454`)
- C4-36 + C4-25(doc half): CLAUDE.md SW section — document the touchMeta recency-authority
  invariants (meta timestamp authoritative; writes must stay lifetime-covered; never
  record size-0) and the opaque/CDN-origin non-caching gap.
- C4-41: fix the ETag-section "rewrites bytes in place" wording → temp-file + atomic
  rename-over (the fd-stat safety argument depends on it).
- C4-27: Operational Playbook note — the in-app embedding scan and the sidecar CLIP
  backfill are not mutually exclusive (duplicate work converges; schedule sidecars accordingly).
- C4-38: env table row for `RESTORE_MAINTENANCE_DIR`.
- C4-37: plans README — cycle-3 → Recent (COMPLETED + deployed), cycle-4 active.
- C4-40: apps/web README test-count staleness fix (drop the precise count).
- C4-31 + C4-32: cycle-3 plan corrections ("17"→16 commits note; WP15 split annotation —
  C3-29 shipped, C3-28 deferred).
- C4-33 + C4-34: carry-forward register — fix the C80-06/C88-03 age arithmetic; reword
  `C1-25(a)`'s label.
- C4-45: plans README age-budget policy — add a MED-severity checkpoint (any MED row
  crossing 16 cycles requires explicit re-justification or reclassification, closing the
  no-teeth gap without forcing product decisions prematurely).
- C4-43 + C4-44: this plan's ledger applies the forward-honesty dispositions (see WP
  residual lines) — including recording cycle-3's three "Closes" as "shipped fix; residual
  fixed in cycle-4 WP1/2/3".

## WP13 — [x] carry-forward register update (`0d308c21`)
- Bump ages one cycle across all open rows; add this cycle's new deferrals (see register);
  record C4-26's folded/closed disposition; verify "no open High row past budget" still
  holds.

## Ledger honesty dispositions (C4-44, forward-applied)
- C3-01 (`285a4538`): shipped fix was correct for the mixed-batch class it targeted
  (TRC4-09 verified against real drizzle source); the SIBLING null-cursor branch retained
  the swallow class — scheduled this cycle (WP1). Cycle-3's "Closes C3-01" was over-broad.
- C3-02 (`3f8b6c88`): keepalive closed the wait_timeout lapse; the transient-failure
  permanent-disarm residual (CRIT4-03) is scheduled this cycle (WP2).
- C3-04/C3-16 (`cc869996`/`1dff18d6`): the same-cycle contradiction (CRIT4-01) is
  scheduled this cycle (WP3: write-invalidation + honest naming + bounded TTL).
- C2-06/C3-08op edge limiters: still "shipped config; prod-apply + verify PENDING operator
  action" — unchanged; now ALSO covers `zone=nextimage` (C4-13 recorded against the same
  operator action).

## Post-deploy verification items
- [x] Production healthy after deploy: `https://gallery.atik.kr/api/live` → 200, `/en` → 200,
  fresh-slug `/en/p/99999999` → 404 (correct soft-404). Verified after the recovery deploy.
- [x] Desktop photo page renders without hydration errors after WP6 — the C4-03 e2e spec
  (`e2e/hydration-photo-page.spec.ts`) passed in the full suite (42 passed).
- [ ] nginx zones: still PENDING OPERATOR APPLY (C3-08op unchanged; do not re-record).

## Cycle-4 terminal evidence
- Implementation commits (on top of `ec433dc4`): `b68d09e2` (WP1), `ce15103a` (WP2),
  `12037508` (WP3+WP8 fold), `ad1fd22d` (WP4), `31ff51f5` (WP5), `4afacfa8` (WP6),
  `253bf305` (WP6 lint cleanup), `0da58d6b` (WP7+C4-29 fold), `53afdd61` (review aggregate),
  `9dccebcd` (WP10/C4-12), `5f0388ed` (C4-19), `18b6cbb4` (C4-20), `e3d221e3` (C4-21),
  `d7ca37de` (WP9/C4-09), `678ebbeb` (C4-15), `d79f6f70` (C4-23), `31dcb276` (WP12 CLAUDE.md),
  `fe4bf454` (WP12 plans/READMEs/cycle-3), `0d308c21` (WP13 register), `9a61d454` (e2e spec fix),
  `225e6392` (C4-26 carry-forward correction).
  All GPG-signed, conventional+gitmoji, pushed to origin/master.
- Gates (fresh evidence this cycle, ALL GREEN repo-wide, run in this lane): eslint clean;
  typecheck 0 errors; Vitest 3113 passed / 4 skipped (333 files); build exit 0;
  lint:api-auth 0; lint:action-origin 0; lint:public-route-rate-limit 0; Playwright e2e
  42 passed / 0 failed. (The C4-03 hydration spec initially failed on a missing
  `page.goto('/')` in its setup — fixed as `9a61d454` — and the re-run confirmed 42 passed.)
- No Dockerfile / package-lock.json / package.json changes this cycle — no container-build
  pre-check required before deploy.
- Concurrency note: a second agent (same git identity) worked the same cycle concurrently —
  it landed WP9 as `d7ca37de` and the C4-26 carry-forward correction as `225e6392` while
  this lane was implementing the same items; convergence was via git (duplicate local edits
  surfaced as "nothing to commit"). No conflicting commits; all pushes clean. The concurrent
  deploy also caused the first `npm run deploy` to fail on a Docker container-name conflict
  (both lanes ran `docker compose up` at overlapping times) — see DEPLOY below.
- DEPLOY: per-cycle-success (after one recovery). The first `npm run deploy` built the image
  cleanly but the container recreate hit `Conflict. The container name
  "/…_gallerykit-web" is already in use` (a concurrent-deploy collision; production stayed
  up throughout — live/home both 200 during the failure). A single recovery re-run of
  `npm run deploy` succeeded ("Deployment Complete!", exit 0). Post-deploy production checks:
  `/api/live` 200, `/en` 200, fresh-slug `/en/p/99999999` 404. Deployed HEAD: `80f08103`.
