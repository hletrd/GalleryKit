# Cycle 61/100 Implementation Plan

Start HEAD: `7e85644e`.
Review aggregate: `.context/reviews/cycle-61-2026-07-01/_aggregate.md`.

## Goal

Fix the actionable Cycle 61 findings, keep review/plan ledgers current, run all required gates, commit, push, and deploy with `npm run deploy` from the repo root.

## Scheduled Fixes

1. `C61-01`: add restore-maintenance short-circuit guards to `/api/og` and `/api/og/photo/[id]` before rate-limit charging and before DB/config/image work, returning `503` with no-store headers. Add focused regression tests.
2. `C61-02`: move the Lightroom upload restore re-check and upload-processing contract lock before the topic DB `SELECT`, so a restore that begins during multipart parsing stops the request cleanly before DB work. Update source-contract coverage.
3. `C61-03`: extend `migration-journal.test.ts` with the reverse SQL-file-to-journal assertion so orphan `drizzle/*.sql` files fail tests.
4. `C61-04`: close the Cycle 60 ledger by recording signed commit/origin/deployed-baseline evidence for `7e85644e`, mark commit/push/deploy progress complete, advance the plan index to Cycle 61, and point the latest aggregate at Cycle 61.
5. `C61-05`: tighten the local ignored `apps/web/.env.local` mode to `0600`. This is a local operational correction and will not create a committed diff.

## Deferred This Cycle

Broad test-coverage gaps are recorded in `.context/plans/cycle-61-2026-07-01-deferred.md` with preserved severity and exit criteria:

- `C61-06`: shared-group view-count flush behavioral tests.
- `C61-07`: Lightroom route handler-level integration tests beyond the source-contract coverage updated for `C61-02`.

Carry-forward items remain unchanged and are repeated in the deferred file.

## Regression Coverage

- Add/update focused Vitest coverage for OG restore-maintenance and migration journal integrity.
- Update Lightroom source-contract coverage for the pre-topic-select maintenance re-check and lock ordering.
- Run all required gates in order after implementation.

## Validation

Run required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then sign, `git pull --rebase`, push, and deploy with `npm run deploy`.

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run required gates.
- [ ] Commit, pull --rebase, push.
- [ ] Deploy with `npm run deploy`.

## Gate Evidence

- Focused regression: `npm test --workspace=apps/web -- og-route-rate-limit-behavior lr-upload-hdr-gate migration-journal` - pass: 4 files passed, 55 tests passed.
- `npm run lint --workspace=apps/web` - pass.
- `npm run lint:api-auth --workspace=apps/web` - pass.
- `npm run lint:action-origin --workspace=apps/web` - pass.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
- `npm run typecheck --workspace=apps/web` - pass.
- `npm run build --workspace=apps/web` - pass. Local sitemap generation logged the expected unavailable local MySQL fallback and exited successfully.
- `npm test --workspace=apps/web` - pass: 287 files passed, 2 skipped; 2740 tests passed, 4 skipped.
- Local operational fix: `apps/web/.env.local` mode tightened to `0600`.

## Gate Fix Iterations

- 0 so far.
