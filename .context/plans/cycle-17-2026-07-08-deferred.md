# Run-10 Cycle 17/100 Deferred Findings

Status: SUPERSEDED BY CYCLE 18 DEFERRED REGISTER WHERE STILL OPEN
Aggregate: `.context/reviews/_aggregate.md`
Date: 2026-07-08 KST

Deferred items preserve original severity/confidence. Repo policies still apply when reopened: GPG-signed Conventional Commits with gitmoji, `git pull --rebase` before push, no force-push, required quality gates, and deploy policy.

Repo-rule basis for operator/manual deferrals:

- `CLAUDE.md` says: "The shipped Docker Compose deployment is a single web-instance / single-writer topology" and "do not horizontally scale the web service unless those coordination states are moved to a shared store."
- `CLAUDE.md` says: "Dumps are plaintext SQL at rest; host/storage encryption is the operator boundary."
- `CLAUDE.md` says: "Admin DB backup/restore is SQL-only" and "does not snapshot or roll back host files."
- `CLAUDE.md` says: "Deploys do NOT touch host nginx" and live nginx/proxy changes are operator-applied.
- `CLAUDE.md` says: "Run one GalleryKit per MySQL server — or prefix advisory-lock names with a per-instance identifier if multi-tenant co-location is required."
- `CLAUDE.md` says CLIP production activation requires live model/env/DB checks and that the manual pre-flight is "the ONLY verification that the real encoder loads offline and ranks semantically."

## Deferred Register

| Finding | Original severity/confidence | Citation | Reason for deferral | Exit criterion |
| --- | --- | --- | --- | --- |
| `AGG-C17-03` tracked `.omc` runtime artifact | Low / High | `.omc/plans/plan-cycle12-fixes.md:1` | Removing tracked/runtime files is a cleanup/deletion action; the user-level destructive-action policy requires explicit confirmation before deleting artifacts. | Reopen after explicit user confirmation to remove tracked `.omc` files and add a source-hygiene check. |
| `AGG-C17-16` toast-only admin validation | Medium / High | `topic-manager.tsx`, `tag-manager.tsx`, `admin-user-manager.tsx`, `seo-client.tsx` | Cross-form validation architecture touches multiple admin workflows; no confirmed data/security defect this cycle. | Reopen on next admin form refactor or accessibility review targeting admin validation. |
| `AGG-C17-17` oversized DB restore toast-only rejection | Medium / High | `apps/web/src/app/[locale]/admin/(protected)/db/page.tsx:76-209` | Admin UX/a11y improvement; restore size enforcement still works and scheduled DB action fixes cover server reliability. | Reopen when DB restore UI is touched or an accessibility audit prioritizes persistent file-input errors. |
| `AGG-C17-18` zoom keyboard pan missing | Medium / High | `apps/web/src/components/image-zoom.tsx:118-368` | Viewer accessibility feature work; no data/security defect and larger than current correctness batch. | Reopen when image zoom interactions are touched or keyboard/photo-viewer a11y work is scheduled. |
| `AGG-C17-19` upload progress not fully announced | Low-Medium / Medium | `apps/web/src/components/upload-dropzone.tsx:469-485` | Needs localized live-region copy and screen-reader validation; upload correctness remains intact. | Reopen on upload UI/a11y work or screen-reader bug report. |
| `AGG-C17-20` home filters before photos | Medium / High | `apps/web/src/components/home-client.tsx:287-330`, `apps/web/src/components/tag-filter.tsx:63-122` | Information-architecture redesign; current behavior is functional and no security/correctness defect was reported. | Reopen when designing mobile home IA, filter sheet/rail, or if UX review escalates to High. |
| `AGG-C17-21` table-first admin image management | Medium / High | `dashboard-client.tsx:135-143`, `image-manager.tsx:427-590` | Product/UI redesign spanning dashboard, image manager, tag input, and responsive layout. | Reopen when building admin workbench/cards/inspector or fixing authenticated responsive admin workflows. |
| `AGG-C17-22` flat mobile admin nav | Medium / High | `admin-nav.tsx:15-49`, `admin-header.tsx:13-27` | Admin IA redesign; not a narrow correctness fix. | Reopen on admin nav redesign or mobile admin review. |
| `AGG-C17-23` native `title` for truncated values | Low-Medium / High | `info-bottom-sheet.tsx:413-420`, `photo-viewer.tsx:803-810`, `upload-dropzone.tsx:535`, `image-manager.tsx:498` | Accessibility polish across public and admin surfaces; lower risk than scheduled correctness work. | Reopen when touching metadata display, filename rendering, or adding accessible tooltips/copy affordances. |
| `AGG-C17-24` shared token pending state | Low-Medium / Medium | `users/tokens-client.tsx:28-303` | Localized UX-state issue; token creation/revocation are still guarded, and target-specific revoke copy is scheduled separately. | Reopen when token page async state, expiry UI, or token e2e coverage is touched. |
| `AGG-C17-25` zoom pan/swipe conflict risk | Medium / Low-Medium | `photo-viewer.tsx:697`, `photo-navigation.tsx:134-221`, `image-zoom.tsx:230-307` | Requires mobile touch-device validation; no confirmed runtime failure. | Reopen after device/browser validation confirms conflict or when zoom/navigation gestures are refactored. |
| `AGG-C17-26` multipart body materialization | High / High | `upload-limits.ts:1-5`, `next.config.ts:111-119`, upload/restore routes/actions | Architectural rewrite to streaming multipart handlers. Deferred under `CLAUDE.md` rule documenting "multipart uploads are buffered on the heap by the framework" and current memory budgeting. | Reopen when implementing streaming upload/restore routes or after production RSS measurement shows breach. |
| `AGG-C17-27` independent background DB budgets | High / High | `db/index.ts:31-42`, `image-queue.ts:121-141`, `admin-backfill-runner.ts:97-143` | Known architecture/perf risk explicitly documented in `CLAUDE.md` as "Mutual over-subscription"; fixing requires shared budget design. | Reopen when changing queue/backfill concurrency or implementing a shared background semaphore. |
| `AGG-C17-28` batch derivative delete scans | Medium / High | `images.ts:759-884`, `process-image.ts:575-663` | Performance optimization; deletion correctness remains intact. | Reopen when optimizing image deletion or NAS latency becomes measurable. |
| `AGG-C17-29` map 10k markers | Medium / High | `data.ts:1766-1816`, map page/client | Product/performance redesign; bounded cap prevents unbounded data growth. | Reopen when map data approaches thousands of markers or map UX/perf work starts. |
| `AGG-C17-30` semantic/similar brute-force scans | Medium / High | semantic/similar routes, `clip-embeddings.ts` | Performance scalability issue; feature is operator-enabled and bounded by documented `SEMANTIC_SCAN_LIMIT`. | Reopen when gallery size/search traffic exceeds current limits or adding vector index/cache. |
| `AGG-C17-31` missing stale-pipeline index | Medium / High | `db/schema.ts:123-131`, backfill candidate queries | Requires schema migration, reconcile update, and EXPLAIN validation; not a current correctness bug. | Reopen when planning migration window or when backfill status latency is measured. |
| `AGG-C17-32` unbounded temp cleanup fan-out | Low / High | `image-queue.ts:41-97`, `process-topic-image.ts:146-168` | Startup performance hardening; no current startup failure reported. | Reopen when temp cleanup or startup readiness is touched, or if `EMFILE`/slow-start logs appear. |
| `AGG-C17-33` authenticated photo duplicate fan-out | Low / High | `p/[id]/page.tsx:148-159`, `data.ts:1057-1198` | Low-risk DB optimization; public/admin page correctness remains intact. | Reopen when refactoring photo data loading or measuring admin browse latency. |
| `AGG-C17-34` listing tag aggregation before limit | Medium / Medium | `data.ts:786-940`, public page | Query optimization needing two-phase data path and regression coverage. | Reopen on listing query performance work or tag-heavy gallery latency evidence. |
| `AGG-C17-35` non-sargable On This Day query | Medium / Medium | public page, `data-timeline.ts:102-130` | Performance/index work requiring migration or cache/materialization design. | Reopen when home query latency is measured or adding generated month/day index. |
| `AGG-C17-36` smart collection predicate cost | Medium / Medium | `smart-collections.ts:142-267`, `data.ts:1488-1544` | Product/perf design; smart collection authoring is not admin-exposed per `CLAUDE.md`. | Reopen before exposing smart-collection authoring UI or if public collection latency appears. |
| `AGG-C17-37` leading-wildcard public search | Medium / Medium | `sql-like.ts:5-10`, `data.ts:1574-1737`, `public.ts:247-317` | Search architecture work; rate limits bound frequency but not per-query cost. | Reopen when adding full-text/search index or public search latency evidence. |
| `AGG-C17-38` timeline/year 500 cards | Low / Medium | `.context/reviews/perf-reviewer.md:332-355` | Low-priority archive performance; bounded response size. | Reopen on archive pagination/lazy-load work. |
| `AGG-C17-39` shutdown/drain host measurement | Low / Low | `.context/reviews/perf-reviewer.md:383-405` | Host-measurement task, not a repo-local code defect. | Reopen when collecting deploy drain metrics or if shutdown loses work. |
| `AGG-C17-40` single-writer warn-only | High / High | `single-writer-guard.ts:6-235`, `instrumentation.ts:22-31` | Deferred under `CLAUDE.md` quote: shipped deployment is "single web-instance / single-writer" and must not horizontally scale until shared coordination exists. | Reopen if deployment adds a second web process, blue/green overlap becomes expected, or product decides to fail health on contention. |
| `AGG-C17-41` server-global advisory lock names | Medium / High | `advisory-locks.ts:10-71` | Deferred under `CLAUDE.md` quote: "Run one GalleryKit per MySQL server" unless lock names are prefixed. | Reopen if co-locating multiple GalleryKit DBs on one MySQL server or adding instance-key lock namespace. |
| `AGG-C17-42` manual restore writer registry | Medium / Medium | `db-actions.ts:497-536`, `admin-mutation-barrier.ts`, `background-db-writes.ts` | Likely/future correctness risk; current known writers are registered and the repo documents the checklist. | Reopen when adding detached DB writers/timers or implementing a centralized background DB scheduler/lint. |
| `AGG-C17-43` SQL-only backup/file consistency | Medium / High | `db-actions.ts:192-829`, `docker-compose.yml:24-32` | Deferred under `CLAUDE.md` quote: "Admin DB backup/restore is SQL-only" and "does not snapshot or roll back host files." | Reopen if implementing full backup bundles, post-restore file reconciliation, or changing backup UI claims. |
| `AGG-C17-44` `IMAGE_BASE_URL` build/runtime split | Medium / High | `next.config.ts:32-125`, `content-security-policy.ts:139-143`, `docker-compose.yml:7-23` | Already documented in `CLAUDE.md` as "BUILD-TIME-FROZEN"; guard/startup assertion is architecture hardening, not current failure. | Reopen when env/deploy config ownership changes or adding build/runtime env invariant checks. |
| `AGG-C17-45` semantic embedding host-wide coordination | Low / Medium | `image-queue.ts:527-622`, `backfill-clip-embeddings.ts:120-130`, `clip-model.ts:53-173` | Operational CPU/RSS risk; `CLAUDE.md` documents in-app scan vs sidecar overlap and advises low-traffic scheduling. | Reopen when changing CLIP bootstrap/backfill coordination or measuring host contention. |
| `AGG-C17-46` sidecar color concurrency bypass | Low / Medium | `backfill-color-pipeline.ts:383-387`, `admin-backfill-runner.ts:130-143` | Operator-controlled maintenance capacity; `CLAUDE.md` documents sidecar uses a separate pool and max 8. | Reopen when enforcing sidecar capacity, changing backfill concurrency defaults, or measuring saturation. |
| `AGG-C17-47` plaintext SQL backup storage | Medium / Medium | `CLAUDE.md:226-228`, backup/download code | Deferred under `CLAUDE.md` quote: "Dumps are plaintext SQL at rest; host/storage encryption is the operator boundary." | Reopen if backups move to shared/untrusted storage or encrypted backup support is requested. |
| `AGG-C17-48` proxy topology/client-IP validation | Medium / High | `rate-limit.ts:175-206`, `nginx/default.conf:20-71`, `check-proxy-topology.mjs` | Deferred under operator-boundary rules: `CLAUDE.md` says deploy does not apply host nginx and proxy/CDN operators must configure equivalent edge limits. | Reopen on proxy/CDN/LB changes, nginx template changes, or failed topology/burst-limit evidence. |
| `AGG-C17-49` coverage ratchet missing | Medium / High | `package.json`, `vitest.config.ts`, `.github/workflows/quality.yml` | Broad CI/tooling policy change with baseline and exemption work. | Reopen when adopting coverage tooling or changed-file ratchet policy. |
| `AGG-C17-50` DB restore child cleanup source-pinned | Medium / High | `db-restore.test.ts:47-74`, restore import path | Test architecture gap; scheduled DB action fixes do not require extracting restore runner. | Reopen when restore runner is refactored or in a dedicated behavior-test hardening cycle. |
| `AGG-C17-51` broad LR upload failure-branch matrix | Medium / High | `lr-upload-route-behavior.test.ts:182-370`, LR route | One confirmed setup branch is scheduled as `AGG-C17-06`; the full matrix is larger than this cycle. | Reopen when extending LR route tests or when any untested LR branch is modified. |
| `AGG-C17-52` admin token UI e2e gap | Medium / High | tokens page/client and `apps/web/e2e/admin.spec.ts` | Browser-flow expansion needs authenticated e2e data/session setup; revoke-target copy is scheduled separately. | Reopen when adding admin token browser tests or changing token plaintext/revoke flows. |
| `AGG-C17-53` migration reconcile structural parity | Medium / Medium | `migrate-reconcile-coverage.test.ts`, `migrate.js` | Disposable-MySQL structural diff is an integration-test infrastructure project. | Reopen for migration changes, schema incident, or CI MySQL parity work. |
| `AGG-C17-54` nav screenshot artifacts lack visual assertions | Low / High | `nav-visual-check.spec.ts:58-85`, Playwright config | Visual baseline adoption requires snapshot policy and baseline churn management. | Reopen when adding visual regression gate or renaming/spec cleanup. |
| `AGG-C17-55` single-engine Playwright coverage | Medium / High | `playwright.config.ts:72-77`, quality workflow | Browser matrix expansion increases CI/runtime complexity; no browser-specific failure reported. | Reopen when mobile/WebKit/Firefox bug appears or browser-matrix work is scheduled. |
| `AGG-C17-56` real CLIP proof manual | Medium / High | CLIP tests/workflow | Deferred under `CLAUDE.md` quote that the env-gated CLIP preflight is the "ONLY verification" before production activation and requires host weights. | Reopen before semantic-production changes merge or when adding path-filtered CLIP preflight. |
| `AGG-C17-57` hydration e2e brittle wait/assertion | Low / Medium | `hydration-photo-page.spec.ts:36-49` | Test stability refinement; no current failing gate. | Reopen if hydration test flakes or photo restored-state logic changes. |
| `AGG-C17-58` source-contract-heavy high-risk flows | High for regression detection / High | pending revocation, upload claim, tag aggregation source tests | Proof-strength gap, not a current runtime bug; repo already uses source contracts as guardrails while behavior harnesses are added incrementally. | Reopen immediately when those flows change or in a dedicated behavior/concurrency hardening cycle. |
| `AGG-C17-59` Docker image CI gap | Medium / High | `.github/workflows/quality.yml`, `apps/web/Dockerfile` | CI policy/build-time cost change; per-cycle deploy is still the operational Docker proof. | Reopen when native deps change, Dockerfile changes, or adding Docker build gate/pin checker. |
| `AGG-C17-60` subjective "Photographer-grade" heading | Low / Medium | `README.md:42-44` | Product wording polish; current docs qualify browser/codec limits and no legal/security risk was found. | Reopen when editing README product positioning or HDR public-delivery copy. |
| `AGG-C17-61` Live Demo parity unvalidated | Low / Medium | `README.md:21-24` | Requires live deploy/config parity audit outside repo source; this cycle will run deploy but not prove feature/config parity claims. | Reopen when making demo parity claims or adding deployed-commit/version disclosure. |
| `AGG-C17-62` live operator claims depend on host state | Low / High | CLIP/proxy runbooks | Deferred under repo operator-boundary rules for CLIP weights/env/DB and host nginx/proxy state. | Reopen before claiming live semantic/proxy activation or after running host-state checks. |
