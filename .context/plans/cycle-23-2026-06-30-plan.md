# Cycle 23/100 Implementation Plan

Date: 2026-06-30 KST  
Review source: `.context/reviews/_aggregate.md`  
Status: DONE - implemented, verified, pending commit/push/deploy bookkeeping

## Repo Rules Read Before Planning

Read order satisfied for deferral policy: `CLAUDE.md`, `AGENTS.md`, `.context/**` index/current plans and reviews, and `docs/superpowers/**`. No `.cursorrules` or `CONTRIBUTING.md` exists in this checkout. No existing active plan was archived because this cycle did not establish fresh evidence that those older active entries are fully implemented; historical completed entries already live under `.context/plans/archive/` or the index's completed section.

## Scheduled Fixes

These findings are scheduled for Prompt 3. Security, correctness, and data-loss risks are intentionally not deferred.

### P23-01 - Harden Lightroom token insert ID handling [done]

- Finding: `AGG23-20` (`.context/reviews/_aggregate.md:239`)
- Severity/confidence: Low / High
- Files: `apps/web/src/lib/admin-tokens.ts`, `apps/web/src/__tests__/admin-tokens.test.ts`
- Plan: replace manual `insertId` coercion with `safeInsertId`; add BigInt insert-id coverage.
- Acceptance: targeted admin-token test passes; full typecheck passes.

### P23-02 - Tighten destructive E2E seed authorization [done]

- Finding: `AGG23-23` (`.context/reviews/_aggregate.md:269`)
- Severity/confidence: Medium-High / High
- Files: `apps/web/scripts/seed-e2e.ts`, `apps/web/src/__tests__/seed-e2e-safety.test.ts`, docs/comments if needed
- Plan: remove `CI=true` as a standalone destructive-seed authorization; require explicit opt-in or disposable DB naming.
- Acceptance: seed-safety test proves `CI=true` alone does not permit cleanup and disposable DB / explicit opt-in still do.

### P23-03 - Make `retryFailedImage` affected-row aware [done]

- Finding: `AGG23-24` (`.context/reviews/_aggregate.md:279`)
- Severity/confidence: Medium / Medium
- Files: `apps/web/src/app/actions/images.ts`, retry-failed image tests
- Plan: inspect the failed-state-clear update result; if no row changed, preserve in-memory failed state and avoid enqueue. Add behavior tests for stale row / enqueue-reject restoration if the existing test harness can support it in-cycle.
- Acceptance: targeted retry tests pass and action still restores failure state on enqueue rejection.

### P23-04 - Make browser upload quota settlement idempotent [done]

- Finding: `AGG23-05` (`.context/reviews/_aggregate.md:89`)
- Severity/confidence: Medium / High
- Files: `apps/web/src/app/actions/images.ts`, upload action tests/source-contract tests
- Plan: mirror the LR route's one-shot settlement helper in browser upload so every post-claim exit path settles at most once. Add or adjust regression coverage around known post-claim failure paths.
- Acceptance: targeted upload quota tests pass and source no longer relies on per-branch settlement comments alone.

### P23-05 - Batch audit retention deletion [done]

- Finding: `AGG23-06` (`.context/reviews/_aggregate.md:99`)
- Severity/confidence: Low / High
- Files: `apps/web/src/lib/audit.ts`, `apps/web/src/__tests__/audit-retention.test.ts`
- Plan: delete expired audit rows in bounded batches with an iteration cap, matching the analytics retention pattern.
- Acceptance: tests prove invalid retention fallback remains safe and batched deletion repeats until cap/empty result.

### P23-06 - Serve upload fallback from the validated file descriptor [done]

- Finding: `AGG23-07` (`.context/reviews/_aggregate.md:109`)
- Severity/confidence: Low / Medium
- Files: `apps/web/src/lib/serve-upload.ts`, `apps/web/src/__tests__/serve-upload.test.ts`
- Plan: open the resolved file once, stat that descriptor, build headers from descriptor stats, and stream from the same handle.
- Acceptance: upload-serving tests pass and code no longer reopens the pathname after validation.

### P23-07 - Harden deploy helper env handling [done]

- Finding: `AGG23-17` (`.context/reviews/_aggregate.md:209`)
- Severity/confidence: Low / Medium
- Files: `scripts/deploy-remote.sh`, deploy helper tests if present
- Plan: reject group/world-writable deploy env files before sourcing; keep command behavior otherwise unchanged for the selected per-cycle deploy path.
- Acceptance: shell syntax remains valid and deploy still reads the selected env file.

### P23-08 - Reduce mutable Docker input ambiguity [done]

- Finding: `AGG23-16` (`.context/reviews/_aggregate.md:199`)
- Severity/confidence: Low / High
- Files: `apps/web/Dockerfile`, docs/tests if practical
- Plan: add an explicit digest/SBOM/provenance hook or documented build assertion that records the resolved Node base image digest during deploy/build. Avoid package-version pin churn that would fight Debian security updates unless current package versions can be verified safely.
- Acceptance: build/deploy docs tell operators how the resolved image input is audited; no runtime behavior change.

### P23-09 - Clarify nginx cleartext-edge invariant [done]

- Finding: `AGG23-18` (`.context/reviews/_aggregate.md:219`)
- Severity/confidence: Low / Medium
- Files: `apps/web/nginx/default.conf`, deploy docs
- Plan: strengthen comments/config docs so the port-80 template is unmistakably internal behind TLS termination and HSTS is not mistaken for TLS enforcement.
- Acceptance: docs/config state the invariant unambiguously without changing production routing.

### P23-10 - Fix CLIP and deploy documentation drift [done]

- Findings: `AGG23-27`, `AGG23-28`, `AGG23-29`, `AGG23-33`, `AGG23-34`, `AGG23-40`, `AGG23-41`, `AGG23-42`, `AGG23-50`
- Severity/confidence: Medium/High through Low/Medium
- Files: `apps/web/scripts/backfill-clip-embeddings.ts`, `CLAUDE.md`, `README.md`, `docs/superpowers/plans/2026-06-15-clip-semantic-search.md`, `apps/web/src/__tests__/feed-sized-derivative.test.ts`, admin settings copy if needed
- Plan: align the CLIP sidecar header with canonical mounts; add semantic backfill lock to the lock inventory; remove hardcoded deploy-host wording; fix the broken plan link; update stale size comment; add visible caveats for color/HDR, semantic similar-photo production gating, and GA placeholder opt-in.
- Acceptance: docs no longer contain the cited stale or misleading text; targeted doc/comment tests remain green.

### P23-11 - Normalize Lightroom/PAT wording and reserved token scopes [done]

- Finding: `AGG23-30` (`.context/reviews/_aggregate.md:339`)
- Severity/confidence: Low / Medium-High
- Files: PAT/LR comments/docs, `apps/web/src/lib/admin-tokens.ts`
- Plan: rename comment wording from bundled-plugin implications to "Lightroom-compatible publish API"; document `lr:read`/`lr:delete` as reserved until routes ship.
- Acceptance: grep no longer finds misleading "bundled plugin" style comments in active implementation docs.

### P23-12 - Correct route-scanner extension comments [done]

- Finding: `AGG23-32` (`.context/reviews/_aggregate.md:359`)
- Severity/confidence: Low / Medium
- Files: `apps/web/scripts/check-api-auth.ts`, `apps/web/scripts/check-public-route-rate-limit.ts`, related tests comments
- Plan: reword comments to say the scanners defensively inspect extra extensions if present; repo-standard route files remain `route.ts`/`route.tsx`.
- Acceptance: scanner behavior remains unchanged and comments no longer overclaim Next support.

### P23-13 - Correct auto-alt-text internal wording [done]

- Finding: `AGG23-43` (`.context/reviews/_aggregate.md:469`)
- Severity/confidence: Low / Medium
- Files: `apps/web/src/db/schema.ts`
- Plan: replace "AI-generated" schema comment with "auto-generated EXIF-derived" plus future-model hook wording.
- Acceptance: user-facing copy stays honest and internal comments stop overstating AI capability.

### P23-14 - Improve public route error recovery [done]

- Findings: `AGG23-35`, `AGG23-36` (`.context/reviews/_aggregate.md:389`, `:399`)
- Severity/confidence: Medium / High
- Files: `apps/web/src/app/[locale]/error.tsx`, messages/tests if needed
- Plan: set a clear error document title and render the normal public shell or equivalent recovery shell with nav/footer.
- Acceptance: error boundary keeps recovery affordances and no longer retains failed page title.

### P23-15 - Make hidden lightbox controls pointer-inert [done]

- Finding: `AGG23-37` (`.context/reviews/_aggregate.md:409`)
- Severity/confidence: Medium / Medium
- Files: `apps/web/src/components/lightbox.tsx`, e2e/source test if practical
- Plan: ensure auto-hidden controls cannot intercept pointer events until visible again.
- Acceptance: hidden overlay cannot activate close/fullscreen/slideshow/prev/next by invisible hit zones.

### P23-16 - Hide empty mobile nav expander [done]

- Finding: `AGG23-38` (`.context/reviews/_aggregate.md:419`)
- Severity/confidence: Low / High
- Files: `apps/web/src/components/nav-client.tsx`
- Plan: render the mobile expand button only when it exposes hidden content.
- Acceptance: zero-topic/fallback nav has no misleading expand affordance.

### P23-17 - Add localized login required-field feedback [done]

- Finding: `AGG23-39` (`.context/reviews/_aggregate.md:429`)
- Severity/confidence: Low / High
- Files: `apps/web/src/app/[locale]/admin/login-form.tsx`, `apps/web/messages/en.json`, `apps/web/messages/ko.json`, i18n tests
- Plan: add component-level required-field validation with `aria-invalid` and localized persistent messages.
- Acceptance: i18n parity and relevant login tests pass.

### P23-18 - Fix upload staging density and skipped-file feedback [done]

- Findings: `AGG23-45`, `AGG23-46` (`.context/reviews/_aggregate.md:489`, `:499`)
- Severity/confidence: Low-Medium / High
- Files: `apps/web/src/components/upload-dropzone.tsx`, messages/tests if needed
- Plan: start accepted-file cards at one column on phones and render structured skipped-file reasons for aggregate-limit filtering.
- Acceptance: mobile class path is `grid-cols-1 sm:grid-cols-2 md:grid-cols-3`; skipped accepted files surface names/reasons persistently.

### P23-19 - Respect reduced/professional motion in photo UI [done]

- Findings: `AGG23-47`, `AGG23-48` (`.context/reviews/_aggregate.md:509`, `:519`)
- Severity/confidence: Low-Medium/High and Low/Medium-High
- Files: `apps/web/src/components/photo-viewer.tsx`, `apps/web/src/components/photo-navigation.tsx`
- Plan: remove long layout animation from info toggling and gate swipe snap transitions through reduced-motion state/CSS.
- Acceptance: no 500ms `transition-all` on photo canvas layout; reduced motion bypasses swipe snap animation.

### P23-20 - Mark aggregate stale-env finding resolved [done]

- Finding: `AGG23-31` (`.context/reviews/_aggregate.md:349`)
- Severity/confidence: Low / High
- Files: `.context/reviews/_aggregate.md`
- Plan: already completed by replacing the stale aggregate with this cycle's aggregate.
- Acceptance: this item is checked done in the plan after Prompt 3 verification.

## Prompt 3 Execution Notes

- Run Ralph-style implementation with a context snapshot under `.omx/context/`.
- Run every configured gate after code/doc changes.
- Commit and push with signed Conventional Commit + gitmoji after gates pass.
- Deploy once with `npm run deploy` after push because `DEPLOY_MODE=per-cycle`.

## Progress

- [x] P23-01 through P23-20 implemented in code/docs.
- [x] Deferred findings preserved in `cycle-23-2026-06-30-deferred.md` with original severity/confidence and exit criteria.
- [x] Full gate list green: lint, api-auth lint, action-origin lint, public-route-rate-limit lint, typecheck, build, and full Vitest passed.
- [ ] Signed commit pushed.
- [ ] Per-cycle deploy recorded.
