# Cycle 32/100 Implementation Plan

Date: 2026-06-30 KST
Review source: `.context/reviews/_aggregate.md`
Status: implemented; gates green; push/deploy pending

## Repo Rules Read Before Planning

Read order satisfied for deferred-fix policy: `CLAUDE.md`, `AGENTS.md`, current `.context/reviews/*`, `.context/plans/README.md`, recent cycle plans/deferred files, lookup for `.cursorrules` and `CONTRIBUTING.md` (none present), and docs/policy files under `docs/`.

Repo rules require signed Conventional Commit + gitmoji commits, `git pull --rebase` before push, no `Co-Authored-By`, all configured gates, and per-cycle deploy with `npm run deploy` after green pushed changes.

Every cycle-32 aggregate finding is either scheduled below or explicitly deferred in `cycle-32-2026-06-30-deferred.md`. Security, correctness, and data-loss defects are scheduled unless they are documented operator/trust-boundary choices, manual production validation tasks, or review/test-infrastructure gaps rather than confirmed product defects.

## Scheduled Fixes

### P32-01 - Fix CLIP inference abort-after-handoff slot leak

- Finding: `AGG32-01`
- Original severity/confidence: High / High
- Files: `apps/web/src/lib/clip-model.ts`, `apps/web/src/__tests__/clip-model-contract.test.ts` or a focused behavior test.
- Plan:
  1. Inspect the current slot acquisition/release state machine and identify when a queued waiter has been handed an active slot.
  2. Make abort-after-handoff release that reserved slot before rethrowing.
  3. Add a regression test that queues a waiter, aborts it after handoff, then proves a third request can acquire a slot.
- Acceptance: targeted CLIP slot test fails before the fix and passes after; full gates pass.
- Progress: [x] Implemented in `clip-model.ts`; source contract added for abort-after-handoff ownership release.

### P32-02 - Fail bulk image update on invalid add/remove tag candidates

- Finding: `AGG32-02`
- Original severity/confidence: Medium / High
- Files: `apps/web/src/app/actions/images.ts`, `apps/web/src/__tests__/bulk-update-images.test.ts`.
- Plan:
  1. Validate and normalize every add/remove tag before opening the transaction.
  2. Return a localized error for any rejected candidate instead of silently continuing.
  3. Add invalid add/remove and mixed valid+invalid tests.
- Acceptance: invalid batch tags return failure and do not audit/report success; existing valid batch tag behavior stays green.
- Progress: [x] Implemented in `actions/images.ts`; invalid add/remove tag inputs now fail before the transaction.

### P32-03 - Gate collapsed lightbox color-pip transfer text by `isAdmin`

- Finding: `AGG32-03`
- Original severity/confidence: Medium / High
- Files: `apps/web/src/components/lightbox-color-pip.tsx`, `apps/web/src/__tests__/lightbox-color-pip-hdr.test.ts`.
- Plan:
  1. Compute/render collapsed transfer text and button aria label only when `isAdmin` is true.
  2. Preserve public-safe primaries/delivery text for non-admin users.
  3. Add a test with admin-shaped data and `isAdmin={false}` proving transfer text is absent from visible text and aria labels.
- Acceptance: public-rendered admin-shaped data cannot expose `transfer_function`; admin rendering still shows transfer details.
- Progress: [x] Implemented in `lightbox-color-pip.tsx`; transfer and pipeline text are gated on `isAdmin`.

### P32-04 - Include SEO/feed-shaping changes in Atom feed conditionals

- Finding: `AGG32-04`
- Original severity/confidence: Medium / High
- Files: `apps/web/src/app/feed.xml/route.ts`, `apps/web/src/app/[locale]/(public)/[topic]/feed.xml/route.ts`, `apps/web/src/lib/data.ts`, feed tests.
- Plan:
  1. Inspect the current feed conditional helper and SEO settings storage.
  2. Choose the smallest durable freshness source: settings revision/hash timestamp if already available, or conservative conditional behavior that cannot 304 stale SEO-shaped XML.
  3. Apply the same validator logic to root and topic feeds.
  4. Add tests for SEO/feed setting change plus `If-Modified-Since`.
- Acceptance: feed routes return fresh XML after SEO/feed-shaping settings change, even when image freshness is unchanged.
- Progress: [x] Implemented in both Atom feed routes; XML content ETags now drive 304 responses.

### P32-05 - Stop load-more retry spin on transient failures

- Finding: `AGG32-05`
- Original severity/confidence: Medium / High
- Files: `apps/web/src/components/load-more.tsx`, `apps/web/src/__tests__/load-more-source-contracts.test.ts` or a focused component/source test.
- Plan:
  1. Add a retry/cooldown guard for `maintenance`, `rateLimited`, and generic `error` statuses before calling the server action again.
  2. Keep normal successful pagination unchanged.
  3. Add a test/source contract proving one server-action call per cooldown window while the sentinel remains visible.
- Acceptance: transient failures do not loop server actions/toasts; manual retry after cooldown remains possible.
- Progress: [x] Implemented in `load-more.tsx`; transient failures set a retry cooldown.

### P32-06 - Keep essential lightbox controls accessible while visually hidden

- Finding: `AGG32-06`
- Original severity/confidence: Medium / High
- Files: `apps/web/src/components/lightbox.tsx`, lightbox accessibility tests.
- Plan:
  1. Separate visual auto-hide from assistive-tech removal for close and previous/next controls.
  2. Preserve keyboard/focus behavior without creating accidental tab traps.
  3. Add a source or component test that hidden controls are not `aria-hidden`/`tabIndex=-1` solely because of idle visual auto-hide.
- Acceptance: essential modal controls remain discoverable to AT/keyboard after idle; visual fade remains intact.
- Progress: [x] Implemented in `lightbox.tsx`; idle visual hiding no longer adds `aria-hidden`/`tabIndex=-1` to essential controls.

### P32-07 - Point Dependabot npm updates at the workspace root

- Finding: `AGG32-07`
- Original severity/confidence: Medium / High
- Files: `.github/dependabot.yml`.
- Plan:
  1. Change the npm package ecosystem directory from `/apps/web` to `/` so Dependabot sees the root `package-lock.json` and root overrides.
  2. Preserve the existing schedule and labels.
  3. Add a lightweight source assertion only if an existing test surface already covers repository config.
- Acceptance: Dependabot npm config covers the canonical workspace lockfile.
- Progress: [x] Implemented in `.github/dependabot.yml`.

### P32-08 - Clamp listing visible page size before lookahead

- Finding: `AGG32-08`
- Original severity/confidence: Low / High
- Files: `apps/web/src/lib/data.ts`, pagination tests.
- Plan:
  1. Clamp visible page size to `LISTING_QUERY_LIMIT`, not `LISTING_QUERY_LIMIT_PLUS_ONE`.
  2. Keep the single lookahead row for `hasMore`.
  3. Add a regression assertion that SQL limits cannot exceed 101 for a visible cap of 100.
- Acceptance: exported helper contract matches documented cap and public pagination behavior remains unchanged.
- Progress: [x] Implemented in `lib/data.ts`; source contracts cover `LISTING_QUERY_LIMIT`.

### P32-09 - Documentation trust and accuracy cleanup

- Findings: `AGG32-31`, `AGG32-33`, `AGG32-34`, `AGG32-35`
- Original severity/confidence: Medium / High; Medium / High; Low / High; Low / High
- Files: `CLAUDE.md`, `README.md`, possibly source comments in `apps/web/src/lib/photo-title.ts`.
- Plan:
  1. Correct the auto-alt-text contract to the implemented public fallback chain.
  2. Front-load the first-upload GPS stripping decision in README without weakening the private-originals claim.
  3. Clarify the `lr` namespace as legacy external-upload naming, not bundled Lightroom feature parity.
  4. Merge duplicate sharing bullets / move product boundaries into clearer README structure.
- Acceptance: docs match implementation and reduce operator trust ambiguity.
- Progress: [x] Implemented in `README.md`, `CLAUDE.md`, and `photo-title.ts` comments.

### P32-10 - Refresh plan index for current cycle state

- Finding: `AGG32-32`
- Original severity/confidence: Low / High
- Files: `.context/plans/README.md`.
- Plan:
  1. Add cycle 32 plan/deferred entries.
  2. Move clearly completed cycle 30/31 implementation entries out of the active block without moving files.
  3. Correct or remove broken relative plan links called out by the document review.
- Acceptance: plan index no longer omits cycle 30/31/32 state or points at nonexistent `../../plan/...` files.
- Progress: [x] Implemented in `.context/plans/README.md`.

## Deferred Findings

Deferred in `cycle-32-2026-06-30-deferred.md`: `AGG32-09` through `AGG32-30` and `AGG32-36`, except scheduled items listed above.

## Prompt 3 Execution Notes

- Use Ralph-style inspect/edit/verify loops until every scheduled item is complete.
- Run all configured gates: `npm run lint --workspace=apps/web`, `npm run lint:api-auth --workspace=apps/web`, `npm run lint:action-origin --workspace=apps/web`, `npm run lint:public-route-rate-limit --workspace=apps/web`, `npm run typecheck --workspace=apps/web`, `npm run build --workspace=apps/web`, `npm test --workspace=apps/web`.
- Fix blocking gate errors before final commit/push. Record unavoidable warnings in the deferred register.
- Commit/push signed Conventional Commit + gitmoji commits after green gates and `git pull --rebase`.
- Per-cycle deploy command after green pushed HEAD: `npm run deploy`.

## Progress

- [x] Prompt 1 aggregate review written.
- [x] Plan and deferred register written.
- [x] P32-01 implemented.
- [x] P32-02 implemented.
- [x] P32-03 implemented.
- [x] P32-04 implemented.
- [x] P32-05 implemented.
- [x] P32-06 implemented.
- [x] P32-07 implemented.
- [x] P32-08 implemented.
- [x] P32-09 implemented.
- [x] P32-10 implemented.
- [x] Full configured gates green.
- [ ] Signed commit(s) pushed.
- [ ] Per-cycle deploy complete.

## Gate Warnings

- `npm run build --workspace=apps/web` emitted the existing sitemap fallback message because local MySQL was unavailable at `127.0.0.1:3306` during static sitemap generation. Severity/confidence: Low / High. Reason deferred: the build is designed to fall back to a homepage-only sitemap in DB-unavailable local builds and exited successfully; changing that contract would be broader than the cycle-32 review fixes. Exit criterion: revisit when a local DB-backed build is required for CI or when sitemap fallback should be silenced/structured as non-error output.
