# Run-10 Cycle 23/100 Deferred Findings

Status: OPEN
Aggregate: `.context/reviews/_aggregate.md`
Date: 2026-07-08 KST

This register records every Cycle 23 aggregate finding not scheduled in `cycle-23-2026-07-08-plan.md`. Severity and confidence are preserved from the aggregate. Deferred work remains bound by repo policy: GPG-signed Conventional Commits with gitmoji, `git pull --rebase` before push, required gates, no force-push, no `--no-verify`, and the repo's Node/Next/React/TypeScript toolchain rules.

Repo rule basis for deferral: `.context/plans/README.md` defines carry-forward budgeting, `.context/plans/deferred-carry-forward.md` is the committed open-deferred register, and `CLAUDE.md` documents the single-web-instance topology, process-local state boundaries, host-nginx operator boundary, DB-only backup/restore boundary, large multipart RSS measurement boundary, and permanently deferred 2FA/WebAuthn posture. This register does not defer contained auth/session, restore, scanner, or directly testable correctness items; those are scheduled in the Cycle 23 plan.

## Deferred Items

| ID | Severity / Confidence | Citation | Reason for deferral | Exit criterion |
|----|-----------------------|----------|---------------------|----------------|
| AGG-C23-04 | High / High for source shape; Medium to Medium-high live RSS impact | `apps/web/next.config.ts:111-119`; `apps/web/src/app/actions/images.ts:87-106`; `apps/web/src/app/api/admin/lr/upload/route.ts:174-181`; `apps/web/src/app/[locale]/admin/db-actions.ts:421-427`, `725-745` | Streaming multipart upload/restore ingress crosses browser upload, PAT upload, restore import, request parsing, temp-file handoff, and memory/load validation. Repo-rule quote: `CLAUDE.md` Important Notes says "multipart uploads are buffered on the heap by the framework... (run-10 cycle-1 C1-33, pending an on-host RSS measurement)." Prior committed registers preserve this as broad architecture work until measurement or approved streaming-ingress project. | Production RSS/OOM incident during upload/restore, on-host RSS trace proving breach, or approved streaming-ingress project. |
| AGG-C23-05 | High / High | `apps/web/src/app/actions/images.ts:87-230`, `325-455`; `apps/web/src/app/api/admin/lr/upload/route.ts:84-190`, `254-520` | Shared browser/PAT ingest extraction is a broad architecture refactor across two hot upload paths. No current divergence was confirmed; contained restore/auth/scanner issues are scheduled first. | Before adding upload-time privacy/metadata/processing fields, or the next confirmed browser/PAT drift. |
| AGG-C23-06 | High / High | `apps/web/src/db/index.ts:31-41`; `apps/web/src/lib/image-queue.ts:121-153`; `apps/web/src/lib/admin-backfill-runner.ts:97-143`, `716-727`; `apps/web/src/lib/background-db-writes.ts:3-10`, `42-64`; `apps/web/src/lib/clip-model.ts:53-72`, `156-173` | Shared DB/CPU background-budget work changes queue/backfill/search concurrency semantics. `CLAUDE.md` documents the current single-process pool budget and known mutual over-subscription as a sizing boundary; no measured starvation evidence was collected this cycle. | Measured pool starvation during queue/backfill/search, or shared background-budget project approved. |
| AGG-C23-11 | Medium / Medium | `apps/web/nginx/default.conf:1-10`, `274-310`; `apps/web/deploy.sh:51-58`; `CLAUDE.md:511-523` | Operator/live-host validation. Repo-rule quote: `CLAUDE.md` says "Deploys do NOT touch host nginx" and not to mark nginx-config findings closed on commit alone. | Operator applies/verifies nginx zones with burst tests, or app-layer public page limiter project approved. |
| AGG-C23-12 | Medium / Medium | `apps/web/nginx/default.conf:20-32`, `59-72`; `apps/web/src/lib/rate-limit.ts:175-216`; `apps/web/src/lib/request-origin.ts:81-107` | Operator topology validation requires live request traces and proxy configuration authority outside repo edits. `CLAUDE.md` deployment docs bind correct behavior to `TRUST_PROXY` and live reverse-proxy topology. | Proxy topology changes, shared-IP false-positive/spoof incident, or operator runs/verifies topology checker. |
| AGG-C23-13 | Medium / High | `CLAUDE.md:244-247`; `apps/web/src/lib/single-writer-guard.ts:6-18`, `218-235` | `CLAUDE.md` explicitly documents the shipped single web-instance/single-writer topology and process-local state. Fail-closed singleton semantics require product/operator decision because rolling deploy overlap is tolerated. | Scale-out is attempted, singleton contention incident occurs, or shared-state/fail-closed topology project approved. |
| AGG-C23-14 | Low / High | `CLAUDE.md:223-228`; `apps/web/src/app/[locale]/admin/db-actions.ts:228-243`; `apps/web/src/app/api/admin/db/download/route.ts:21-89` | Repo-rule quote: `CLAUDE.md` documents plaintext SQL backups as a host/storage encryption boundary and restore as DB-only rather than filesystem rollback. | Backup encryption requirement, filesystem rollback product decision, or restore/file integrity incident. |
| AGG-C23-15 | Low / High | `apps/web/src/db/schema.ts:193-200`, `225-241`; `apps/web/src/app/actions/auth.ts:79-150`, `217-253`; `CLAUDE.md:248`; `CLAUDE.md` Permanently Deferred | Repo-rule quote: `CLAUDE.md` permanently defers "2FA/WebAuthn: Not planned. Multiple root admins with Argon2id + rate limiting is sufficient for a personal gallery." | Admin compromise risk posture changes, step-up auth project approved, or roles/capabilities are added. |
| AGG-C23-16 | Medium / High | `apps/web/src/app/actions/public.ts:247-317`; `apps/web/src/lib/sql-like.ts:5-10`; `apps/web/src/lib/data.ts:1574-1655`, `1693-1737`; `apps/web/src/lib/smart-collections.ts:221-267` | Search-index redesign is product/performance work. Existing request counts are rate-limited; no live latency evidence was gathered this cycle. | Search latency rises, corpus grows materially, or full-text/search-index cycle approved. |
| AGG-C23-17 | Medium / High | `apps/web/src/app/api/search/semantic/route.ts:1-17`, `263-311`; `apps/web/src/app/api/search/similar/[id]/route.ts:1-29`, `177-214`; `apps/web/src/lib/clip-embeddings.ts:36-48`; `apps/web/src/db/schema.ts:314-326` | Bounded residual risk behind semantic mode gates/scan limits. A vector index, worker-thread scorer, ANN service, or cache redesign is larger than this cycle. | Semantic traffic/gallery size exceeds scan limits, measured CPU/RSS pressure, or vector-index/cache project approved. |
| AGG-C23-18 | Medium / High | `apps/web/src/__tests__/migrate-reconcile-coverage.test.ts:28-37`; `apps/web/src/__tests__/db-restore.test.ts:12-136`; `apps/web/src/__tests__/semantic-scan-limit-source.test.ts:19-77`; `apps/web/src/__tests__/search-stale-response.test.ts:13-35`; `apps/web/src/__tests__/pending-file-deletions-source.test.ts:5-45` | Cycle 23 schedules the contained pending-deletion behavior gaps. Remaining DB-backed migration, child-process restore, semantic-route, and client behavior harness work requires broader test infrastructure. | Next high-risk source-contract-only finding, or behavior/integration test-hardening cycle. |
| AGG-C23-19 | Medium / High | `apps/web/playwright.config.ts:72-77`; `.github/workflows/quality.yml:75-80`; `apps/web/e2e/nav-visual-check.spec.ts:40-86`; `apps/web/e2e/hydration-photo-page.spec.ts:20-49` | Browser matrix and deterministic visual assertions affect CI runtime and fixture stability. This is test-strategy work, not a contained runtime defect. | Browser-specific regression, PWA/SW incident, visual regression budget approved, or test-matrix project approved. |
| AGG-C23-20 | Medium / High | `apps/web/src/lib/data.ts:1766-1816`; `apps/web/src/app/[locale]/(public)/map/page.tsx:50-66`, `98-110`; `apps/web/src/components/map/map-client.tsx:77-94`, `120-141` | Map clustering/viewport APIs and spatial indexing are product/performance scope. Current cap and rate limits bound worst-case work; no live map jank measurement was collected. | GPS-visible rows approach thousands, mobile map trace shows jank, or map clustering project approved. |
| AGG-C23-21 | Low-Medium / High | `apps/web/src/lib/data-timeline.ts:103-110`, `121-131`; `apps/web/src/db/schema.ts:123-132` | Generated month/day columns require a schema/performance migration. No measured home latency or corpus-growth evidence was collected this cycle. | Measured home latency, corpus growth makes the scan costly, or next schema/index cycle folds generated month/day columns. |
| AGG-C23-22 | Low / High | `apps/web/public/sw.template.js:31-39`, `147-164` | Bounded service-worker maintenance cost. This is a low-risk performance polish item and should ride a PWA/cache cycle. | Navigation stutter trace implicates HTML eviction, or next service-worker cache redesign cycle. |
| AGG-C23-25 | Medium / High | `apps/web/src/app/[locale]/admin/(protected)/dashboard/dashboard-client.tsx:135-144`; `apps/web/src/components/image-manager.tsx:427-450`, `472-488`, `500-552`, `571-607`; `.context/plans/cycle-22-2026-07-08-deferred.md` | Admin responsive workbench redesign is product/UX scope and should be paired with broader admin IA work. No wrong-row action incident was reported. | Mobile/tablet admin operation becomes priority, wrong-row action report, or admin redesign cycle. |
| AGG-C23-26 | Low-Medium / High | `apps/web/src/components/admin-nav.tsx:15-49`; `apps/web/src/components/admin-header.tsx:13-26`; `apps/web/src/components/masonry-card.tsx:149-154` | Admin IA redesign and public masonry metadata placement need product/visual iteration. Touch targets and core modal isolation were verified as healthy. | Admin nav redesign cycle, sensitive-operation IA concern, mobile overlay crop complaint, or masonry visual redesign cycle. |

## Scheduled, Not Deferred

Scheduled in `cycle-23-2026-07-08-plan.md`: `AGG-C23-01`, `AGG-C23-02`, `AGG-C23-03`, `AGG-C23-07`, `AGG-C23-08`, `AGG-C23-09`, `AGG-C23-10`, `AGG-C23-23`, `AGG-C23-24`.

## Age-Budget Check

Newly deferred High-severity Cycle 23 rows (`AGG-C23-04`, `AGG-C23-05`, `AGG-C23-06`) are broad architecture/performance/availability items with explicit exit criteria and prior committed carry-forward lineage. Direct contained auth/session correctness, restore, scanner, and evidence defects are scheduled. Security/operator rows quote the documented host-nginx, proxy, single-instance, DB-only backup, and permanently deferred MFA boundaries in `CLAUDE.md`.
