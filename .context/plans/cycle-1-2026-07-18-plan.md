# Cycle 1/100 Implementation Plan — 2026-07-18

Start HEAD: `64f6ac63`
Source: `.context/reviews/_aggregate.md` and all Cycle 1 provenance reviews
Status: Complete

## Finding disposition

Every new aggregate finding is scheduled below. Revalidated findings map to the existing authoritative records in `.context/plans/deferred-carry-forward.md`; those records retain file/line citations, original severity/confidence, concrete deferral reasons, repo-policy constraints, and exit criteria. No security, correctness, or data-loss finding is newly deferred.

## Work packages

- [x] C1-01 auth: update IP and account in-memory login budgets before any durable increment; isolate DB increment failures; add a first-increment-rejection behavioral regression.
- [x] C1-02 Similar Photos: restore `mountedRef` during effect setup and strengthen lifecycle source/behavior coverage.
- [x] C1-03 responsive nav: hide the desktop search label on narrow viewports even in production semantic mode; add 320 px non-zero brand/no-overlap coverage and correct the source contract.
- [x] C1-04 settings copy: explain that Timeline/Map switches affect nav, footer, and sitemap discovery while direct URLs remain public; preserve English/Korean parity.
- [x] C1-05 GeoIP: log a clear once-per-process diagnostic when module/data lookup is unavailable, keep `XX` fail-degrade semantics, and add focused tests/contracts.
- [x] C1-06 deploy ownership: reject deploy/runtime env files not owned by the executing user; extend shell/source contract tests.
- [x] C1-07 image priority: make the initial first-row eager set SSR-safe for the five-column maximum without marking every eager card high priority; add initial-policy coverage.
- [x] Run every configured blocking gate against the whole repository.
- [x] Commit with signed gitmoji Conventional Commits, pull --rebase, push master, and run the selected per-cycle `npm run deploy` once after all gates are green.

## Completion evidence

- Focused regression run: 7 files / 102 tests passed.
- Full Vitest: 361 files passed, 2 skipped; 3,408 tests passed, 4 skipped.
- Playwright: 46 passed, 2 skipped, using a disposable MySQL 8 E2E database removed after the run.
- ESLint, API-auth lint, action-origin lint, public-route-rate-limit lint, typecheck, production build, production dependency audit, and `git diff --check`: passed.
- Gate repair: updated one stale semantic-search copy assertion left by the pre-existing July 8 wording trim.
- Signed commits, rebased push, and per-cycle deploy are recorded by this cycle's terminal git/deploy history.
