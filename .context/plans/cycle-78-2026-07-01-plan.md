# Cycle 78/100 Implementation Plan

Start HEAD: `9286bef16f3401fb0d8c17f52de5c96804c04533`.
Review aggregate: `.context/reviews/cycle-78-2026-07-01/_aggregate.md`.

## Goal

Close the safe Cycle 78 review findings, keep the review/plan ledgers current, run all required gates, commit, push, and deploy with `npm run deploy` from the repo root.

## Scheduled Fixes

1. `C78-01`: close the stale Cycle 77 ledger by marking commit/push complete, recording missing committed deploy evidence, and moving Cycle 77 out of the active index.
2. `C78-02`: make the public-route rate-limit scanner's expensive-marker fallback AST-aware so marker words in strings/comments do not false-positive cheap handlers; add fixtures.
3. `C78-03`: strengthen the sidecar backfill timestamp test so both `flushBatch` UPDATE branches are pinned independently.
4. `C78-04`: apply the native `sharp` optional-dependency workaround to the Docker `prod-deps` runtime dependency stage and add a `require('sharp')` smoke check; lock it with deploy contract tests.

## Deferred This Cycle

No new Cycle 78 findings are deferred.

Carry-forward deferred items remain in `.context/plans/cycle-78-2026-07-01-deferred.md`.

## Regression Coverage

- Scanner fixtures: cheap GET with marker text in a string literal and in a comment must pass without rate-limit/exemption.
- Sidecar source contract: the full metadata and derivative-only `flushBatch` SQL templates must each contain `updated_at = CURRENT_TIMESTAMP`.
- Docker contract: the `prod-deps` stage must install the Linux `@img/sharp-*` packages for the target architecture and run a runtime `sharp` require smoke check before the builder stage.

## Validation

Run required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then sign, `git pull --rebase`, push, and deploy with `npm run deploy`.

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run required gates.
- [x] Commit, pull --rebase, push.
- [x] Deploy with `npm run deploy`.

## Gate Evidence

- Focused regression: `npm test --workspace=apps/web -- --run src/__tests__/check-public-route-rate-limit.test.ts src/__tests__/backfill-color-pipeline-deleted-mid-reencode.test.ts src/__tests__/deploy-script-contract.test.ts` - pass: 3 files passed, 122 tests passed.
- `npm run lint --workspace=apps/web` - pass.
- `npm run lint:api-auth --workspace=apps/web` - pass: both admin API routes OK.
- `npm run lint:action-origin --workspace=apps/web` - pass: all mutating server actions enforce same-origin provenance.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
- `npm run typecheck --workspace=apps/web` - pass.
- `npm run build --workspace=apps/web` - pass. Local sitemap generation logged the expected unavailable local MySQL fallback and exited successfully.
- `npm test --workspace=apps/web` - pass: 301 files passed, 2 skipped; 2817 tests passed, 4 skipped.
- Commit/push: `9cc143d0` (`fix(deploy): 🐛 prevent runtime sharp load failures`) is signed and was `origin/master` at Cycle 79 start.
- Deploy: Cycle 79 invocation states the current deployed `master` HEAD at start was `9cc143d0`; Cycle 79's per-cycle deploy will supersede this production state after its own push.

## Gate Fix Iterations

0 so far.
