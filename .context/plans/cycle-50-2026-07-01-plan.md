# Cycle 50/100 Implementation Plan

Start HEAD: `3a02f7ee`.
Current integration HEAD after review lane artifact commit: `646d98c9`.
Review aggregate: `.context/reviews/cycle-50-2026-07-01/_aggregate.md`.

## Goal

Fix the actionable Cycle 50 review finding, keep review/plan ledgers current, run all required gates, commit, push, and deploy with `npm run deploy` from the repo root.

## Scheduled Fixes

1. `C50-01`: strengthen the service-worker offline HTML fallback regression test by evaluating `isRevocableShareHtmlRoute` from both `apps/web/public/sw.template.js` and generated `apps/web/public/sw.js` against concrete localized and unlocalized route examples. Normal public photo pages (`/p/123`, `/ko/p/123`, `/en-US/p/123`) must remain eligible for offline HTML fallback while revocable/share/object pages (`/s`, `/g`, `/c`, `/map`) continue to bypass it.

## Deferred This Cycle

No new Cycle 50 findings are deferred. Carry-forward items are recorded in `.context/plans/cycle-50-2026-07-01-deferred.md`.

## Regression Coverage

- `apps/web/src/__tests__/sw-template-contract.test.ts` must prove the concrete classifier behavior for both `sw.template.js` and generated `sw.js`.

## Validation

Run required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then sign, `git pull --rebase`, push, and deploy with `npm run deploy`.

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run required gates.
- [ ] Commit, pull --rebase, push.
- [ ] Deploy with `npm run deploy`.

## Gate Evidence

- `npm run lint --workspace=apps/web` - pass
- `npm run lint:api-auth --workspace=apps/web` - pass
- `npm run lint:action-origin --workspace=apps/web` - pass
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass
- `npm run typecheck --workspace=apps/web` - pass
- `npm run build --workspace=apps/web` - pass (local sitemap generation fell back to homepage-only because MySQL was not running on `127.0.0.1:3306`; build exited 0)
- `npm test --workspace=apps/web` - pass, 2709 passed / 4 skipped
- Focused pre-gate check: `npm test --workspace=apps/web -- sw-template-contract.test.ts` - pass, 26 tests
