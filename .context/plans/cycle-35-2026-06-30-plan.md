# Cycle 35/100 Implementation Plan

Date: 2026-06-30 KST
Source review: `.context/reviews/_aggregate.md`
Reviewed HEAD: `96160854ebadca1606e9f99b2e6f5bc4689e366c`

## Scope Rules

- Follow `AGENTS.md` and `CLAUDE.md`.
- Commit and push a GPG-signed Conventional Commit with gitmoji and Lore trailers.
- Run the configured blocking gates before commit/deploy:
  - `npm run lint --workspace=apps/web`
  - `npm run lint:api-auth --workspace=apps/web`
  - `npm run lint:action-origin --workspace=apps/web`
  - `npm run lint:public-route-rate-limit --workspace=apps/web`
  - `npm run typecheck --workspace=apps/web`
  - `npm run build --workspace=apps/web`
  - `npm test --workspace=apps/web`
- Run `git diff --cached --check` before commit.
- After successful push, deploy once from the repo root with `npm run deploy`.
- Terminal push/deploy evidence is reported in the Cycle 35 final report to avoid committing stale future-state checkboxes.

## Scheduled Findings

### C35-P1 - Close upload route file handles on non-stream responses

Status: implemented
Findings: `AGG-C35-01`
Original severity/confidence: Medium / High
Files:

- `apps/web/src/lib/serve-upload.ts`
- `apps/web/src/__tests__/serve-upload.test.ts`

Plan:

- Add a single helper that closes the open `FileHandle` and clears ownership before non-stream success returns.
- Use it for `304`, wildcard `304`, `HEAD`, and unsupported-content non-stream branches after `open()`.
- Add regression coverage that spies on `FileHandle.close()` for matching ETag, wildcard ETag, and HEAD responses.

Acceptance:

- `serveUploadFile()` never returns a successful non-stream response while retaining ownership of an open descriptor.
- Focused serve-upload tests pass.

### C35-P2 - Fail closed on inverted generic action-origin guards

Status: implemented
Findings: `AGG-C35-02`
Original severity/confidence: High / High
Files:

- `apps/web/scripts/check-action-origin.ts`
- `apps/web/src/__tests__/check-action-origin.test.ts`

Plan:

- Tighten `conditionChecksGuardVariable()` so arbitrary binary expressions no longer satisfy the guard.
- Accept the canonical `if (originError)` shape and safe non-null comparisons only.
- Add negative fixtures for `originError === null`, `originError === false`, and `originError && false`.

Acceptance:

- `lint:action-origin` rejects inverted/neutralized generic `requireSameOriginAdmin()` guards and still passes current source.

### C35-P3 - Detect imported side-effect helpers before guards/limiters

Status: implemented
Findings: `AGG-C35-03`
Original severity/confidence: Medium / High
Files:

- `apps/web/scripts/check-action-origin.ts`
- `apps/web/scripts/check-public-route-rate-limit.ts`
- `apps/web/src/__tests__/check-action-origin.test.ts`
- `apps/web/src/__tests__/check-public-route-rate-limit.test.ts`

Plan:

- Collect imported function names whose source/imported names start with side-effect verbs such as write, delete, enqueue, log, revalidate, acquire, release, flush, drain, and restore.
- Treat those imported calls as pre-guard mutations in the action-origin scanner and as pre-limiter mutations in the public-route scanner.
- Add negative fixtures for an imported write helper before the action guard and before a public route limiter.

Acceptance:

- The scanners fail the imported side-effect fixtures without flagging current pure validation/translation pre-guard helpers.

### C35-P4 - Execute the shipped histogram worker in tests

Status: implemented
Findings: `AGG-C35-04`
Original severity/confidence: Medium / High
Files:

- `apps/web/src/__tests__/histogram.test.ts`
- `apps/web/public/histogram-worker.js` (read-only test target)

Plan:

- Load `public/histogram-worker.js` in a VM-backed worker scope with fake `self.postMessage`.
- Invoke `self.onmessage` with sRGB and Display P3 sample pixels.
- Assert request id propagation, histogram shape, and differing luminance bins.

Acceptance:

- The test now executes the shipped worker file rather than duplicating its algorithm.

### C35-P5 - Correct cycle-34 plan progress drift and record cycle-35 artifacts

Status: implemented
Findings: `AGG-C35-05`
Original severity/confidence: Low / Medium
Files:

- `.context/reviews/_aggregate.md`
- `.context/reviews/critic-verifier-designer-document.md`
- `.context/plans/README.md`
- `.context/plans/cycle-34-2026-06-30-plan.md`
- `.context/plans/cycle-35-2026-06-30-plan.md`
- `.context/plans/cycle-35-2026-06-30-deferred.md`

Plan:

- Write the Cycle 35 aggregate and local critic/verifier/designer/document artifact.
- Add the Cycle 35 plan/deferred files.
- Mark the Cycle 34 plan/index as done, using the Cycle 35 task's current deployed master HEAD context as terminal evidence.

Acceptance:

- The plan index no longer advertises Cycle 34 as active/in progress.
- No new Cycle 35 finding is silently dropped or deferred.

## Deferred Findings

See `.context/plans/cycle-35-2026-06-30-deferred.md`. No new Cycle 35 findings are deferred.

## Progress

- [x] Review lanes completed and findings aggregated.
- [x] Plan and deferred records created.
- [x] C35-P1 implemented and tested with focused coverage.
- [x] C35-P2 implemented and tested with focused coverage.
- [x] C35-P3 implemented and tested with focused coverage.
- [x] C35-P4 implemented and tested with focused coverage.
- [x] C35-P5 documentation updates completed.
- [x] Full required gates passed.

## Verification Evidence

- `npm test --workspace=apps/web -- --run src/__tests__/serve-upload.test.ts src/__tests__/check-action-origin.test.ts src/__tests__/check-public-route-rate-limit.test.ts src/__tests__/histogram.test.ts`: passed, 130 tests.
- `npm run lint --workspace=apps/web`: passed.
- `npm run lint:api-auth --workspace=apps/web`: passed.
- `npm run lint:action-origin --workspace=apps/web`: passed.
- `npm run lint:public-route-rate-limit --workspace=apps/web`: passed.
- `npm run typecheck --workspace=apps/web`: passed.
- `npm run build --workspace=apps/web`: passed; local MySQL was unavailable and sitemap used its documented homepage-only fallback.
- `npm test --workspace=apps/web`: passed, 2601 passed / 4 skipped.
