# Run-10 Cycle 9/100 Implementation Plan

Date: 2026-07-07
Start HEAD: `ff0c79d607208bae9487be8152fa648f4161674f`
Review aggregate: `.context/reviews/_aggregate.md`

## Scope

This plan schedules the cycle-9 findings that are concrete, local, and safe to fix in this cycle. Every finding from the aggregate is either scheduled below or recorded in `cycle-9-2026-07-07-deferred.md` with original severity/confidence, deferral reason, and reopening criterion.

No existing active plan needed archiving during Prompt 2: `.context/plans/README.md` already listed no active current-cycle plans, and previous run-10 cycle plans are completed or in the archive/history section.

## Work Packages

### WP1 - Repair failed-image migration compatibility

- Covers: AGG-C9-01
- Files likely touched: `apps/web/scripts/migrate.js`, migration tests under `apps/web/src/__tests__`
- Plan:
  1. Add a compatibility preflight before Drizzle applies pending migrations so a DB at the `0024` cursor has `images.processing_error` and `images.failed_at` before historical `0025_processing_settings_snapshot.sql` references `failed_at`.
  2. Keep the fix idempotent and scoped to this historical migration gap; do not mutate already-correct schemas.
  3. Add regression coverage proving the compatibility step runs before pending migration application and that `0025` cannot reference a missing `failed_at`.
- Progress: implemented. Added an idempotent historical pending-migration prerequisite hook in `migrate.js` for `0025_processing_settings_snapshot`, plus a regression in `migrate-pending-migrations.test.ts`.

### WP2 - Make semantic embedding ownership honest

- Covers: AGG-C9-02
- Files likely touched: `CLAUDE.md`, `apps/web/README.md`, semantic-search docs/tests
- Plan:
  1. Choose the current implemented invariant for this cycle: one active embedding row per image keyed by `image_id`; model-version changes are destructive replacements, not side-by-side history.
  2. Update docs/comments that claim one row per `(image_id, model_version)`.
  3. Add a source/doc contract test so the old per-model-row claim does not return without a schema migration.
- Progress: implemented. Live docs now state the current one-active-row-per-image invariant, and `semantic-embedding-storage-contract.test.ts` pins both docs and physical schema.

### WP3 - Add a source-column privacy guard against public alias leaks

- Covers: AGG-C9-03
- Files likely touched: `apps/web/src/__tests__/privacy-fields.test.ts` or a new focused test, public select modules
- Plan:
  1. Add a source-contract test that scans public select modules for direct sensitive `images` column references even when aliased.
  2. Preserve explicit exceptions for already-audited public map coordinates.
  3. Keep the existing key-name public select guard; this adds a source-column layer rather than replacing it.
- Progress: implemented. `privacy-fields.test.ts` now scans standalone public select modules for aliases to sensitive `images.*` columns.

### WP4 - Single-flight maintenance sweeps

- Covers: AGG-C9-09
- Files likely touched: `apps/web/src/lib/maintenance-scheduler.ts`, `apps/web/src/__tests__/maintenance-scheduler-source.test.ts` or new behavior test
- Plan:
  1. Add a module-level in-flight sweep promise/guard so a startup or hourly tick skips/reuses while a sweep body is running.
  2. Preserve `activeMaintenanceSweeps` drain semantics for restore.
  3. Add regression coverage proving two rapid `runMaintenanceSweep()` calls cannot start two sweep bodies.
- Progress: implemented. `maintenance-scheduler.ts` now single-flights sweeps with `maintenanceSweepInFlight`, and the source contract pins the guard.

### WP5 - Lock real cursor normalization behavior

- Covers: AGG-C9-14
- Files likely touched: `apps/web/src/__tests__/public-actions.test.ts`, `apps/web/src/__tests__/smart-collection-pagination.test.ts`, new/updated data cursor tests
- Plan:
  1. Add direct tests for `normalizeImageListCursor` covering accepted ISO/MySQL forms, null capture dates, invalid dates, slash dates, overlong strings, non-integer ids, and non-object values.
  2. Update action tests to import the actual normalizer where practical while continuing to mock DB fetches.
  3. Avoid widening accepted cursor syntax unless product code already accepts it.
- Progress: implemented. Added direct `normalizeImageListCursor` behavior coverage for accepted and rejected cursor shapes.

### WP6 - Fix review/doc/user-facing drift

- Covers: AGG-C9-25, AGG-C9-26
- Files likely touched: upload route comments, public-route rate-limit exemptions, smart-collection delete guidance/copy/docs, messages if applicable
- Plan:
  1. Remove the false "range handling" claim from upload derivative route comments/exemptions unless actual range support is implemented in this cycle.
  2. Update smart-collection deletion guidance to say current remediation/authoring is direct-DB/operator-only until an admin UI ships.
  3. Add source-contract coverage for the corrected claims if the project already has adjacent docs/copy tests.
- Progress: implemented. Removed the false range-handling claim from both upload route exemptions and updated smart-collection category-delete copy in English/Korean.

### WP7 - Make admin/CLIP proof paths explicit and runnable

- Covers: AGG-C9-04, AGG-C9-05
- Files likely touched: `apps/web/package.json`, `CLAUDE.md`, `apps/web/README.md`, tests/source contracts
- Plan:
  1. Add explicit scripts for authenticated admin e2e proof and CLIP real-model preflight so the proof paths are commandable instead of buried in prose.
  2. Keep remote admin and real CLIP model-weight tests opt-in under existing repo safety rules; do not require production credentials or seeded model weights in default unit gates.
  3. Add source-contract coverage that these scripts and docs point to the same gated suites.
  4. Record the remaining default-gate/CI-weight limitations in the deferred register with the repo rule quote from `CLAUDE.md`.
- Progress: implemented/deferred split. Added `test:e2e:admin` and `test:clip:preflight` scripts plus docs/source-contract coverage; default-credential and model-weight enforcement limits remain deferred in `DEF-C9-04-RESIDUAL` and `DEF-C9-05-RESIDUAL`.

### WP8 - Attempt production PostCSS audit remediation

- Covers: AGG-C9-06
- Files likely touched: `apps/web/package.json`, `package-lock.json`, dependency tests/docs
- Plan:
  1. Check the package registry for the current stable Next/PostCSS path before changing dependencies.
  2. If a safe stable Next update removes `next/node_modules/postcss@8.4.31`, update and run the full gates.
  3. If no safe stable remediation exists, record the upstream-blocked state in the deferred register with current registry/audit evidence; do not use `npm audit fix --force`.
- Progress: upstream-blocked/deferred after registry/audit check. Current stable Next 16.2.10 still declares nested `postcss@8.4.31`, and audit offers only an invalid downgrade. See `DEF-C9-06-UPSTREAM`.

## Deferred Register

See `cycle-9-2026-07-07-deferred.md`. Deferred findings: AGG-C9-04/05 residual enforcement limits, AGG-C9-06 if upstream-blocked after WP8, AGG-C9-07, AGG-C9-08, AGG-C9-10 through AGG-C9-13, AGG-C9-15 through AGG-C9-24, plus any WP-scoped residuals explicitly marked there after implementation evidence.

## Required Gates

Run all cycle gates after implementation:

- `npm run lint --workspace=apps/web`
- `npm run lint:api-auth --workspace=apps/web`
- `npm run lint:action-origin --workspace=apps/web`
- `npm run lint:public-route-rate-limit --workspace=apps/web`
- `npm run typecheck --workspace=apps/web`
- `BASE_URL=https://gallery.atik.kr npm run build --workspace=apps/web`
- `npm test --workspace=apps/web`
- `npm run test:e2e --workspace=apps/web` when browser-flow coverage is required

## Completion Ledger

- Prompt 1 aggregate: complete.
- Prompt 2 plan/deferred: complete.
- Prompt 3 implementation: complete for scheduled WPs; upstream/residual items are recorded in `cycle-9-2026-07-07-deferred.md`.
- Focused tests: `npm test --workspace=apps/web -- --run src/__tests__/migrate-pending-migrations.test.ts src/__tests__/semantic-embedding-storage-contract.test.ts src/__tests__/privacy-fields.test.ts src/__tests__/maintenance-scheduler-source.test.ts src/__tests__/image-list-cursor.test.ts src/__tests__/proof-scripts.test.ts` - pass, 6 files / 45 tests.
- Gates:
  - `npm run lint --workspace=apps/web` - pass.
  - `npm run lint:api-auth --workspace=apps/web` - pass.
  - `npm run lint:action-origin --workspace=apps/web` - pass.
  - `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
  - `npm run typecheck --workspace=apps/web` - pass.
  - `BASE_URL=https://gallery.atik.kr npm run build --workspace=apps/web` - pass; build logged the existing local-DB-unavailable sitemap fallback and completed.
  - `npm test --workspace=apps/web` - pass, 343 files / 3163 tests; 2 files / 4 tests skipped by existing policy.
  - `npm run test:e2e --workspace=apps/web` - not run because this cycle did not change browser-flow behavior; admin/CLIP proof commands were added for the deferred manual/evidence paths.
- Commit/push: pending.
- Deploy: pending per `DEPLOY_MODE=per-cycle`.
