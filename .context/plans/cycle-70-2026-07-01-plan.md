# Cycle 70/100 Implementation Plan

Start HEAD: `6e3e54e99ad1d4472431d0ee760fea54c4fdb3af`.
Review aggregate: `.context/reviews/cycle-70-2026-07-01/_aggregate.md`.

## Goal

Fix the safe, reviewable Cycle 70 findings, keep review/plan ledgers current, run all required gates, commit, push, and deploy with `npm run deploy` from the repo root.

## Scheduled Fixes

1. `C70-01`: fail `lint:api-auth` on non-type admin route `export * from ...` declarations and add a mixed wrapped-handler/star-export fixture.
2. `C70-02`: hoist the service-worker cached image size into the same lexical scope as both HEAD freshness branches, regenerate `public/sw.js`, and pin the source contract.
3. `C70-03`: remove stale Florence-2 references from English and Korean auto-alt-text settings copy.
4. `C70-04`: update `CLAUDE.md` cache documentation to include viewer/detail cached data access without a brittle count.

## Deferred This Cycle

No new Cycle 70 findings are deferred.

Carry-forward items remain in `.context/plans/cycle-70-2026-07-01-deferred.md`.

## Regression Coverage

- Add `check-api-auth` fixture coverage for mixed wrapped direct handler + `export *`.
- Extend `sw-template-contract` to require `cachedSize` declaration before the first HEAD status branch and to keep same-ETag HEAD 200 body-fetch-free.
- Run i18n parity and focused tests covering changed messages/docs where applicable.

## Validation

Run required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then sign, `git pull --rebase`, push, and deploy with `npm run deploy`.

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run required gates.
- [x] Commit, pull --rebase, push.
- [x] Deploy with `npm run deploy`.

## Gate Evidence

- Focused regression: `npm test --workspace=apps/web -- --run src/__tests__/check-api-auth.test.ts src/__tests__/sw-template-contract.test.ts src/__tests__/i18n-key-parity.test.ts` - pass: 3 files, 45 tests.
- Service-worker syntax: `node --check apps/web/public/sw.template.js && node --check apps/web/public/sw.js` - pass.
- `npm run lint --workspace=apps/web` - pass.
- `npm run lint:api-auth --workspace=apps/web` - pass: both admin API routes OK.
- `npm run lint:action-origin --workspace=apps/web` - pass: all mutating server actions enforce same-origin provenance.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
- `npm run typecheck --workspace=apps/web` - pass.
- `npm run build --workspace=apps/web` - pass. Local sitemap generation logged the expected unavailable local MySQL fallback and exited successfully.
- `npm test --workspace=apps/web` - pass: 294 files passed, 2 skipped; 2769 tests passed, 4 skipped.

## Gate Fix Iterations

0.

## Completion Notes

Cycle 70 was committed and pushed as `bf86f7c176ecb1ed542d851bfa0e76e2b9d73cd5` (`fix(cycle-70): 🐛 harden scanner and SW cache path`) and is the deployed `master` HEAD for the Cycle 71 start context.
