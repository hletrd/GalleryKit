# Cycle 69/100 Implementation Plan

Start HEAD: `87e2b98db76e90985299e37ad90cf2faad12c5c4` at cycle start.
Review aggregate: `.context/reviews/cycle-69-2026-07-01/_aggregate.md`.

## Goal

Fix the safe, reviewable Cycle 69 findings, keep review/plan ledgers current, run all required gates, commit, push, and deploy with `npm run deploy` from the repo root.

## Scheduled Fixes

1. `C69-01`: enforce the documented 128 px minimum for `image_sizes` in the shared validator/parser and tests.
2. `C69-02`: distinguish a zero-candidate in-app backfill from a clean completed re-encode in runner state and the Settings last-run panel.
3. `C69-03`: extract and test a pure helper for saved settings-only re-encode pending-state transitions.
4. `C69-04`: treat same-ETag `HEAD 200` in the service worker like a verified cache hit with timestamp refresh, then regenerate `public/sw.js`.
5. `C69-05`: resolve the current runtime-gated semantic search mode immediately before post-upload embedding writes so stale upload snapshots cannot skip/overwrite the active model version.

## Deferred This Cycle

No new Cycle 69 findings are deferred.

Carry-forward items remain in `.context/plans/cycle-69-2026-07-01-deferred.md`.

## Regression Coverage

- Extend `gallery-config-shared` tests for minimum image-size rejection/acceptance.
- Add pure helper tests for saved backfill pending state.
- Add runner/status/UI source or behavior tests for no-candidate backfill summary.
- Add service-worker source contract for same-ETag HEAD 200 avoiding `startRevalidate()`.
- Update image-queue embedding wiring tests to require current mode resolution before `storeImageEmbeddingForMode()`.

## Validation

Run required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then sign, `git pull --rebase`, push, and deploy with `npm run deploy`.

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run required gates.
- [ ] Commit, pull --rebase, push.
- [ ] Deploy with `npm run deploy`.

## Gate Evidence

- Focused regression: `npm test --workspace=apps/web -- --run src/__tests__/gallery-config-shared.test.ts src/__tests__/settings-backfill-warning.test.ts src/__tests__/settings-backfill-warning-source.test.ts src/__tests__/admin-backfill-runner-batching.test.ts src/__tests__/admin-backfill-status-shape.test.ts src/__tests__/sw-template-contract.test.ts src/__tests__/image-queue-embed-wiring.test.ts src/__tests__/i18n-key-parity.test.ts` - pass after one focused test-contract cleanup: 8 files passed, 63 tests passed.
- `npm run lint --workspace=apps/web` - pass.
- `npm run lint:api-auth --workspace=apps/web` - pass.
- `npm run lint:action-origin --workspace=apps/web` - pass.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
- `npm run typecheck --workspace=apps/web` - pass.
- `npm run build --workspace=apps/web` - pass. Local sitemap generation logged the expected unavailable local MySQL fallback and exited successfully.
- `npm test --workspace=apps/web` - pass: 294 files passed, 2 skipped; 2768 tests passed, 4 skipped.

## Gate Fix Iterations

1. Focused pre-gate test-contract cleanup for the semantic embedding source assertion after changing the queue behavior.
