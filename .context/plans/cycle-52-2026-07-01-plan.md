# Cycle 52/100 Implementation Plan

Start HEAD: `d7326789`.
Terminal implementation HEAD: `17db8e38`.
Review aggregate: `.context/reviews/cycle-52-2026-07-01/_aggregate.md`.

## Goal

Fix the actionable Cycle 52 findings, keep review/plan ledgers current, run all required gates, commit, push, and deploy with `npm run deploy` from the repo root.

## Scheduled Fixes

1. `C52-01`: update the Cycle 51 plan/index ledger so it no longer presents the already-pushed/deployed Cycle 51 ledger fix as active or deploy-unknown. Record commit `d7326789`, upstream push state, and the Cycle 52 invocation evidence that `d7326789` was the current deployed `master` HEAD at start.
2. `C52-02`: make admin Settings show operator-enabled production semantic search as an explicit read-only active state, while keeping production enablement outside the UI. Add focused source-contract coverage and English/Korean copy.

## Deferred This Cycle

No new Cycle 52 findings are deferred. Carry-forward items are recorded in `.context/plans/cycle-52-2026-07-01-deferred.md`.

## Regression Coverage

- `apps/web/src/__tests__/cycle-52-source-contracts.test.ts` is a source-contract smoke check that pins the Settings page threading the server-resolved semantic mode into the client and the client source for active/healed production states. Cycle 53 adds action-boundary and healed-clear-path coverage for the behavior gaps this smoke test did not exercise.
- Existing i18n/key-parity coverage must include the new English/Korean messages.

## Validation

Run required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then sign, `git pull --rebase`, push, and deploy with `npm run deploy`.

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run required gates.
- [x] Commit, pull --rebase, push.
- [x] Deploy with `npm run deploy`.

## Gate Evidence

- Focused pre-gate checks: `npm test --workspace=apps/web -- cycle-52-source-contracts.test.ts` - pass, 1 test; `npm test --workspace=apps/web -- i18n-key-parity.test.ts` - pass, 2 tests.
- `npm run lint --workspace=apps/web` - pass
- `npm run lint:api-auth --workspace=apps/web` - pass
- `npm run lint:action-origin --workspace=apps/web` - pass
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass
- `npm run typecheck --workspace=apps/web` - pass
- `npm run build --workspace=apps/web` - pass (local sitemap generation fell back to homepage-only because MySQL was not running on `127.0.0.1:3306`; build exited 0)
- `npm test --workspace=apps/web` - pass, 2710 passed / 4 skipped

## Release Evidence

- Signed commit: `17db8e38` (`fix(settings): prevent hidden production search state`).
- Push evidence: Cycle 53 started with `HEAD` and `origin/master` both at `17db8e38`.
- Deploy evidence: Cycle 53 invocation states the current deployed `master` HEAD at start was `17db8e38`.
