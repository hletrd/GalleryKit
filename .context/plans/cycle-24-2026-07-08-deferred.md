# Run-10 Cycle 24/100 Deferred Findings

Status: OPEN
Aggregate: `.context/reviews/_aggregate.md`
Date: 2026-07-08 KST

This register records every Cycle 24 aggregate finding not scheduled in `cycle-24-2026-07-08-plan.md`. Severity and confidence are preserved from the aggregate. Deferred work remains bound by repo policy: GPG-signed Conventional Commits with gitmoji, `git pull --rebase` before push, required gates, no force-push, no `--no-verify`, and the repo's Node/Next/React/TypeScript toolchain rules.

Repo rule basis for deferral: `.context/plans/README.md` defines carry-forward budgeting, `.context/plans/deferred-carry-forward.md` is the committed open-deferred register, and `CLAUDE.md` documents the single-web-instance topology, process-local state boundaries, host-nginx operator boundary, DB-only backup/restore boundary, large multipart RSS measurement boundary, and permanently deferred 2FA/WebAuthn posture. This register does not defer the contained test false-confidence defect (`AGG-C24-04`), current-cycle provenance defects (`AGG-C24-05`, `AGG-C24-06`), or stale source-comment defect (`AGG-C24-07`); those are scheduled in the Cycle 24 plan.

## Deferred Items

| ID | Severity / Confidence | Citation | Reason for deferral | Exit criterion |
|----|-----------------------|----------|---------------------|----------------|
| AGG-C24-01 | High / High | `apps/web/next.config.ts:111-119`; `apps/web/src/app/actions/images.ts:87-106`, `154-159`, `197-221`; `apps/web/src/app/[locale]/admin/db-actions.ts:421-427`, `745-767`; `apps/web/src/app/api/admin/lr/upload/route.ts:101-187` | Streaming multipart upload/restore ingress crosses browser upload, restore import, request parsing, temp-file handoff, and memory/load validation. Repo-rule quote: `CLAUDE.md` Important Notes says "multipart uploads are buffered on the heap by the framework... (run-10 cycle-1 C1-33, pending an on-host RSS measurement)." Prior registers preserve this as broad architecture work until measurement or an approved streaming-ingress project. | Production RSS/OOM incident during upload/restore, on-host RSS trace proving breach, or approved streaming-ingress project. |
| AGG-C24-02 | High / High source design, Medium production threshold | `apps/web/src/db/index.ts:31-41`; `apps/web/src/lib/image-queue.ts:121-153`, `447-456`; `apps/web/src/lib/admin-backfill-runner.ts:97-143`, `716-727`; `apps/web/src/lib/background-db-writes.ts:3-10`, `42-64`; `apps/web/src/lib/clip-model.ts:53-72`, `156-173` | Shared DB/CPU background-budget work changes queue/backfill/search/analytics semantics. Repo-rule basis: `CLAUDE.md` documents the shipped single-process pool budget and known mutual over-subscription as a sizing boundary, with no measured starvation evidence collected this cycle. | Measured pool starvation during queue/backfill/search, or shared background-budget project approved. |
| AGG-C24-03 | High / High | `apps/web/src/app/actions/images.ts:87-610`; `apps/web/src/app/api/admin/lr/upload/route.ts:84-633` | Shared browser/PAT ingest extraction is a broad architecture refactor across two hot upload paths. No current parity divergence was confirmed; contained test/provenance/comment issues are scheduled first. | Before adding upload-time privacy/metadata/processing fields, or the next confirmed browser/PAT drift. |
| AGG-C24-08 | Low / High | `apps/web/src/lib/data.ts:13-249`, `251-506`, `514-1820` | Data-layer modularization is maintainability work across many call sites. Repo policy favors deletion/existing patterns but does not require broad refactors absent a contained defect; no public privacy leak or query bug was confirmed. | Next `data.ts`-touching feature or confirmed projection/cross-surface drift, or approved data-access modularization cycle. |
| AGG-C24-09 | Medium / High | `apps/web/src/lib/storage/index.ts:4-12`; `apps/web/src/lib/storage/types.ts:4-9`, `51-76`; `apps/web/src/lib/storage/local.ts:38-61`, `159-167`; `apps/web/src/lib/upload-paths.ts:12-23`, `28-41`, `59-88`; `apps/web/src/lib/serve-upload.ts:198-229`; `apps/web/src/app/actions/images.ts:336-373` | Storage abstraction product decision is broad. Repo-rule quote: `CLAUDE.md` says the `@/lib/storage` module exists but "the product currently supports local filesystem storage only" and not to expose S3/MinIO switching until the pipeline is wired end-to-end. | Storage-backend support is approved, storage abstraction causes maintainer confusion, or next storage/upload architecture cycle. |
| AGG-C24-10 | High / High | `apps/web/src/lib/single-writer-guard.ts:7-16`, `218-235`; `apps/web/src/instrumentation.ts:22-31`; `apps/web/src/lib/image-queue.ts:313-372`; `apps/web/src/lib/background-db-writes.ts:3-10` | This correctness/topology issue is deferred only because repo rules explicitly define the current guard as non-fatal. Repo-rule quote: `CLAUDE.md` states "The guard is warn-only and never blocks startup or crashes on DB errors" and documents the shipped single-web-instance topology. Changing to fail-closed affects rolling deploy/operator behavior and needs product/operator approval. | Scale-out is attempted, singleton contention incident occurs, or a fail-closed/shared-state topology project is approved. |
| AGG-C24-11 | Medium / Medium | `apps/web/src/db/schema.ts:328-342`; `apps/web/src/app/actions/collections.ts:16-158`; `apps/web/src/app/[locale]/(public)/c/[slug]/page.tsx:84-111`; `apps/web/src/components/admin-nav.tsx:15-26`; `apps/web/messages/en.json:507-508` | Smart-collection authoring is a product/UI lifecycle project. `CLAUDE.md` documents that smart collection mutations exist but no admin UI invokes them yet and direct DB authoring is the current boundary. | Collections authoring UI is prioritized, a collection blocks normal admin operations, or smart-collection public behavior incident occurs. |
| AGG-C24-12 | Medium / High source shape, Medium defect likelihood | `apps/web/src/components/photo-viewer.tsx:47-71`; `apps/web/src/app/[locale]/admin/(protected)/settings/settings-client.tsx:55-253`; `apps/web/src/components/image-manager.tsx:72-120`, `427-620`; `apps/web/src/components/search.tsx:131-260` | Large component decomposition is broad refactor work. No contained stale-state/focus bug was confirmed this cycle; splitting should ride feature-touching or UI-hardening work with behavior tests. | Confirmed stale async/focus/mutation bug in these components, or approved component state-machine extraction cycle. |
| AGG-C24-13 | Medium / High | `apps/web/src/app/actions/public.ts:247-329`; `apps/web/src/lib/data.ts:1574-1583`, `1637-1655`, `1693-1701`, `1716-1738`; `apps/web/src/db/schema.ts:123-132` | Search-index redesign is product/performance work. Existing request counts are rate-limited; no live latency evidence was gathered this cycle. | Search latency rises, corpus grows materially, or full-text/search-index cycle approved. |
| AGG-C24-14 | Low-Medium / High | `apps/web/src/lib/data-timeline.ts:103-110`, `121-131`; `apps/web/src/db/schema.ts:123-132` | Generated month/day columns require a schema/performance migration. No measured home latency or corpus-growth evidence was collected this cycle. | Measured home latency, corpus growth makes the scan costly, or next schema/index cycle folds generated month/day columns. |
| AGG-C24-15 | Medium / High | `apps/web/src/lib/data.ts:1766-1816`; `apps/web/src/app/[locale]/(public)/map/page.tsx:42-66`, `89-110`; `apps/web/src/components/map/map-client.tsx:77-94`, `120-141` | Map clustering/viewport APIs and spatial indexing are product/performance scope. Current cap and rate limits bound worst-case work; no large-corpus browser trace was collected. | GPS-visible rows approach thousands, mobile map trace shows jank, or map clustering/viewport project approved. |
| AGG-C24-16 | Medium / High | `apps/web/src/lib/clip-embeddings.ts:36-48`; `apps/web/src/app/api/search/semantic/route.ts:263-311`; `apps/web/src/app/api/search/similar/[id]/route.ts:177-214`; `apps/web/src/db/schema.ts:314-326` | Bounded residual risk behind semantic mode gates/scan limits. A vector index, worker-thread scorer, ANN service, or cache redesign is larger than this cycle. | Semantic traffic/gallery size exceeds scan limits, measured CPU/RSS pressure, or vector-index/cache project approved. |
| AGG-C24-17 | Low / High | `apps/web/public/sw.template.js:31-39`, `147-164`, `446-480`, `555-563`; `apps/web/src/__tests__/sw-template-contract.test.ts:59-112`, `400-461` | Bounded service-worker maintenance cost; correctness/privacy risks are guarded. This should ride a PWA/cache cycle or measured trace. | Navigation stutter trace implicates HTML eviction, or next service-worker cache redesign cycle. |
| AGG-C24-18 | Low-Medium / High | `apps/web/src/app/[locale]/admin/db-actions.ts:71-76`, `109`, `116-144` | Bounded admin-only memory risk. Streaming export is a route/API redesign and no production heap issue was collected. | CSV export heap/latency incident, very large export fields observed, or approved streaming-export project. |
| AGG-C24-19 | Medium / High | `apps/web/src/__tests__/migrate-reconcile-coverage.test.ts:13-19`, `95-101`; `apps/web/src/__tests__/db-restore.test.ts:47-74`; `apps/web/src/__tests__/semantic-scan-limit-source.test.ts:1-17`; `apps/web/src/__tests__/search-stale-response.test.ts:8-10`; `apps/web/src/app/api/search/semantic/route.ts:270-279`; `apps/web/src/app/api/search/similar/[id]/route.ts:181-190` | Cycle 24 schedules the confirmed auth source-test false confidence. Remaining DB-backed migration, child-process restore, semantic-route, and client behavior harness work requires broader test infrastructure. | Next high-risk source-contract-only finding, or behavior/integration test-hardening cycle. |
| AGG-C24-20 | Medium / High | `apps/web/playwright.config.ts:48-77`; `.github/workflows/quality.yml:75-80`; `apps/web/e2e/nav-visual-check.spec.ts:40-86`; `apps/web/e2e/hydration-photo-page.spec.ts:36-49` | Browser matrix and deterministic visual assertions affect CI runtime and fixture stability. This is test-strategy work, not a contained runtime defect. | Browser-specific regression, PWA/SW incident, visual regression budget approved, or test-matrix project approved. |
| AGG-C24-21 | Low-Medium / High | `apps/web/src/__tests__/clip-offline-load.test.ts:15-18`, `32-41`; `apps/web/src/__tests__/clip-semantic-integration.test.ts:8-10`, `30-31`; `.github/workflows/quality.yml:54-83`; `.github/workflows/clip-preflight.yml:3-45` | Real CLIP preflight requires model weights and CI/runtime budget. Existing workflow is scheduled/manual; no CLIP-touching source change occurred in this cycle. | CLIP/semantic production files change, semantic production incident, or required preflight policy approved. |
| AGG-C24-22 | Low-Medium / High | `package.json:17-29`; `apps/web/package.json:13-29`; `apps/web/vitest.config.ts:16-39`; `.github/workflows/quality.yml:54-83` | Coverage instrumentation/thresholds are test-strategy scope and can create noisy churn without an agreed ratchet. | Coverage reporting project approved, repeated missed branch regression, or changed-file coverage policy adopted. |
| AGG-C24-23 | Low-Medium / High | `apps/web/e2e/admin.spec.ts:6-13`; `apps/web/e2e/origin-guard.spec.ts:27-31`, `55-73`; `apps/web/package.json:21-23` | Release evidence wording/test-infra issue. Current cycle will record full `test:e2e` output and skips; expanding admin E2E release enforcement needs credential/runtime policy. | Admin-flow regression, release evidence ambiguity incident, or admin E2E expansion/enforcement cycle. |
| AGG-C24-24 | Medium / High | `apps/web/src/components/image-manager.tsx:427-450`, `472-488`, `500-552`, `571-607`; browser evidence in `.context/reviews/designer.md` | Admin responsive workbench redesign is product/UX scope and should be paired with broader admin IA work. No wrong-row action incident was reported. | Mobile/tablet admin operation becomes priority, wrong-row action report, or admin redesign cycle. |
| AGG-C24-25 | Low-Medium / High | `apps/web/src/components/admin-nav.tsx:15-49`; `apps/web/src/components/admin-header.tsx:13-26`; browser evidence in `.context/reviews/designer.md` | Admin IA redesign requires product decisions about workflow grouping and sensitive operation separation. | Admin nav redesign cycle, sensitive-operation IA concern, or user report of admin navigation confusion. |
| AGG-C24-26 | Low / High | `apps/web/src/components/masonry-card.tsx:149-154`; browser evidence in `.context/reviews/designer.md` | Low-risk public gallery presentation preference. Changing metadata placement needs visual/browser iteration. | Photographer/user report that mobile overlays obscure important crops, or masonry-card visual redesign cycle. |
| AGG-C24-27 | Medium / High | `apps/web/src/app/[locale]/admin/(protected)/seo/seo-client.tsx:42-72`, `98-184`; `apps/web/src/app/actions/seo.ts:85-96`, `111-139` | Field-level SEO errors are a contained UX improvement but part of a broader admin form-validation carry-forward set (`C96-09`/`C96-10`). This cycle schedules higher-risk test/provenance defects first. | Admin form-UX pass, accessibility/user report on SEO validation recovery, or next SEO form cycle. |
| AGG-C24-28 | Low-Medium / High | `apps/web/src/app/[locale]/admin/(protected)/categories/topic-manager.tsx:91-126`, `205-223`, `363-383` | Same admin form-validation class as `AGG-C24-27`; should be fixed with a consistent field-error pattern across admin dialogs. | Admin form-UX pass, topic validation recovery report, or next categories/topic-manager cycle. |
| AGG-C24-29 | Medium / Medium | `apps/web/src/components/image-zoom.tsx:206-214`; `.context/plans/deferred-carry-forward.md` keyboard-zoom carry-forward entries | Keyboard-pannable zoom requires interaction design decisions around arrow-key navigation vs pan mode. Existing carry-forward records preserve this accessibility risk. | AT/keyboard-user report, a11y cycle, or explicit keyboard zoom mode design approved. |
| AGG-C24-30 | Medium / High | `apps/web/src/site-config.json:2-10`; `README.md:60-77`, `121-122`, `171-172`, `198-200`; `apps/web/README.md:19-20`, `49-50`, `57`; `apps/web/scripts/ensure-site-config.mjs:6-42`; `apps/web/src/app/sitemap.ts:14-18`, `70-113`; `apps/web/src/app/[locale]/layout.tsx:15-26`; `apps/web/src/components/footer.tsx:33-37`; `apps/web/src/lib/data.ts:1866-1890` | This checkout is the configured production deployment for `gallery.atik.kr`, and the user explicitly chose that target for this loop. Changing committed site-config semantics would be a distribution/product decision. | Before distributing GalleryKit as a template/package, or if copied installs publish wrong canonical URLs. |
| AGG-C24-31 | Medium / Medium | `apps/web/nginx/default.conf:1-29`, `52-71`, `115-120`, `246-311`; `apps/web/deploy.sh:51-58`; `apps/web/src/lib/rate-limit.ts:175-216`; `scripts/check-proxy-topology.mjs:7-17`, `131-134`; `CLAUDE.md` host-nginx runbook | Operator/live-host validation. Repo-rule quote: `CLAUDE.md` says "Deploys do NOT touch host nginx" and not to mark nginx-config findings closed on commit alone. | Operator applies/verifies nginx zones and real-IP topology, proxy topology changes, or app-layer public limiter project approved. |
| AGG-C24-32 | Medium / Medium | `.env.deploy.example`; `apps/web/.env.local.example`; deploy helpers; `CLAUDE.md` and `README.md` secret-handling docs | Live gitignored secret provenance cannot be proven from source review. This is an operator validation/rotation boundary, not an editable repo defect. | Secret provenance audit, rotation requirement, or evidence that live env was seeded from historical checked-in examples. |
| AGG-C24-33 | Low / High | `apps/web/src/app/[locale]/admin/db-actions.ts` backup regions; `apps/web/src/app/api/admin/db/download/route.ts`; `CLAUDE.md`; admin copy | Repo-rule quote: `CLAUDE.md` documents plaintext SQL backups as a host/storage encryption boundary and restore as DB-only rather than filesystem rollback. | Backup encryption requirement, filesystem rollback product decision, or restore/file integrity incident. |
| AGG-C24-34 | Medium / Medium | `apps/web/next.config.ts:32-38`, `121-125`; `apps/web/Dockerfile:91-99`, `117-120`; `apps/web/docker-compose.yml:18-32`; `scripts/deploy-remote.sh:22-29`, `55-93` | Operator/runtime boundary already documented; fixing requires deployment validation design. No restart-only incident was observed this cycle. | Restart-without-rebuild thumbnail/site-config incident, or deploy validation project for baked-vs-runtime config. |
| AGG-C24-35 | Medium / High | `apps/web/src/app/[locale]/admin/db-actions.ts:646-695`; `apps/web/src/lib/image-queue.ts:1285-1345`; `apps/web/scripts/restore-maintenance-recovery.mjs:76-85`; `CLAUDE.md` restore recovery runbook | Operational recovery risk is explicitly documented. Repo-rule quote: `CLAUDE.md` says if the recovery command is run from a separate shell/process after live web entered maintenance, "restart/redeploy `gallerykit-web` after the clear" so process-local maintenance resets. | Restore recovery incident, operator confusion report, or diagnostics project exposing durable and process-local maintenance states. |

## Scheduled, Not Deferred

Scheduled in `cycle-24-2026-07-08-plan.md`: `AGG-C24-04`, `AGG-C24-05`, `AGG-C24-06`, `AGG-C24-07`.

## Age-Budget Check

Newly deferred High-severity Cycle 24 rows (`AGG-C24-01`, `AGG-C24-02`, `AGG-C24-03`, `AGG-C24-10`) are broad architecture/performance/topology items with explicit exit criteria and prior committed carry-forward lineage. The only contained high-confidence test defect is scheduled. `AGG-C24-10` has correctness implications but is deferred only under the repo's explicit warn-only single-writer guard rule quoted above; changing it requires operator/product approval.
