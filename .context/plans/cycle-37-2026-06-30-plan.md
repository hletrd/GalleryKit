# Cycle 37/100 Implementation Plan

Date: 2026-06-30 KST
Source review: `.context/reviews/cycle-37-2026-06-30/_aggregate.md`
Reviewed HEAD: `d6c3a8f69911c84a63985a59827d4597def922d4`

## Scope Rules

- Follow `AGENTS.md` and `CLAUDE.md`.
- Commit and push a GPG-signed Conventional Commit with gitmoji and Lore trailers.
- Run the configured blocking gates before commit/deploy:
  - `npm run lint --workspace=apps/web`
  - `npm run lint:api-auth --workspace=apps/web`
  - `npm run lint:action-origin --workspace=apps/web`
  - `npm run lint:public-route-rate-limit --workspace=apps/web`
  - `npm run typecheck --workspace=apps/web`
  - `npm run build --workspace=apps/web`
  - `npm test --workspace=apps/web`
- Run `git diff --cached --check` before commit.
- After successful push, deploy once from the repo root with `npm run deploy`.

## Scheduled Findings

### C37-P1 - Classify imported credential/persistence helpers as pre-guard mutations

Status: implemented
Findings: `AGG-C37-01`
Original severity/confidence: High / High
Files:

- `apps/web/scripts/check-action-origin.ts`
- `apps/web/scripts/check-public-route-rate-limit.ts`
- `apps/web/src/__tests__/check-action-origin.test.ts`
- `apps/web/src/__tests__/check-public-route-rate-limit.test.ts`

Plan:

- Extend the imported side-effect helper verb reach to include `create`, `revoke`, `issue`, `mint`, `rotate`, and `upsert`.
- Keep the action-origin and public-route scanners aligned so the same helper classes do not drift.
- Add regression fixtures for imported credential/create helpers before the required guard/limiter.

Acceptance:

- `lint:action-origin` rejects pre-guard imported `createToken()` / `revokeToken()` calls.
- `lint:public-route-rate-limit` rejects pre-limiter imported create helpers.
- Current source remains green.

### C37-P2 - Resolve or fail exported identifier aliases

Status: implemented
Findings: `AGG-C37-02`
Original severity/confidence: High / High
Files:

- `apps/web/scripts/check-action-origin.ts`
- `apps/web/src/__tests__/check-action-origin.test.ts`

Plan:

- When an exported variable initializer is an identifier, resolve it to a collected local function body and evaluate that body under the exported name.
- Fail closed if the identifier target cannot be resolved.
- Add guarded, unguarded, and unresolved alias fixtures.

Acceptance:

- Identifier-alias exports cannot silently disappear from scanner output.

### C37-P3 - Fail closed on multi-callback exported wrappers

Status: implemented
Findings: `AGG-C37-03`
Original severity/confidence: High / High
Files:

- `apps/web/scripts/check-action-origin.ts`
- `apps/web/src/__tests__/check-action-origin.test.ts`

Plan:

- Narrow exported call-wrapper unwrapping to exactly one inline function body.
- Treat zero, multiple, or hidden function bodies as unsupported wrapper shapes.
- Add a two-callback wrapper fixture where the first callback has a guard and the second mutates.

Acceptance:

- Multi-callback wrappers cannot hide unguarded mutating callbacks.
- Existing single-callback `cache(async function ...)` read-only exemption remains supported.

## Deferred Findings

See `.context/plans/cycle-37-2026-06-30-deferred.md`.

## Progress

- [x] Review lanes completed and findings aggregated.
- [x] Plan and deferred records created.
- [x] C37-P1 implemented and tested.
- [x] C37-P2 implemented and tested.
- [x] C37-P3 implemented and tested.
- [x] Full required gates passed.

## Verification Evidence

- `npm test --workspace=apps/web -- --run src/__tests__/check-action-origin.test.ts src/__tests__/check-public-route-rate-limit.test.ts`: passed, 122 tests.
- `npm run lint --workspace=apps/web`: passed.
- `npm run lint:api-auth --workspace=apps/web`: passed.
- `npm run lint:action-origin --workspace=apps/web`: passed.
- `npm run lint:public-route-rate-limit --workspace=apps/web`: passed.
- `npm run typecheck --workspace=apps/web`: passed.
- `npm run build --workspace=apps/web`: passed; local MySQL was unavailable and sitemap used its documented homepage-only fallback.
- `npm test --workspace=apps/web`: passed, 2634 passed / 4 skipped.
