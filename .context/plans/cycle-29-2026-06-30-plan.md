# Cycle 29/100 Implementation Plan

Date: 2026-06-30 KST  
Review source: `.context/reviews/_aggregate.md`  
Status: implementation complete; gates green; commit/deploy pending

## Repo Rules Read Before Planning

Read order satisfied for deferred-fix policy: `CLAUDE.md`, `AGENTS.md`, `.context/` review/plan files, lookup for `.cursorrules` and `CONTRIBUTING.md` (none present), and `docs/superpowers/**` historical policy/spec files. Repo rules require signed Conventional Commit + gitmoji commits, `git pull --rebase` before push, all configured gates, and per-cycle deploy with `npm run deploy` after green pushed changes.

Every cycle-29 aggregate finding is either scheduled below or explicitly deferred in `cycle-29-2026-06-30-deferred.md`. Security, privacy, correctness, data-consistency, and data-loss findings are scheduled here unless they are operational/manual-validation findings with no safe repo-code action this cycle.

## Scheduled Fixes

### P29-01 - Commit plan history and harden rate-limit bucket retention

- Findings: `AGG-C29-01`, `AGG-C29-19`
- Original severity/confidence: Medium / High; Medium / High
- Files: `.gitignore`, `apps/web/src/db/schema.ts`, `apps/web/drizzle/0028_rate_limit_bucket_start_idx.sql`, `apps/web/drizzle/meta/_journal.json`, `apps/web/scripts/migrate.js`, `apps/web/src/lib/rate-limit.ts`, rate-limit/migration tests
- Plan: unignore `.context/plans/**`, add a `bucket_start` index in Drizzle schema/migration/reconcile, and bound expired bucket deletion batches.
- Acceptance: migration journal remains monotonic; reconcile covers the new index; tests/source contracts prove purge is indexed and bounded.

### P29-02 - Replace copy-only privacy/ranking coverage with executable contracts

- Findings: `AGG-C29-10`, `AGG-C29-11`
- Original severity/confidence: Medium / High; Medium / High
- Files: `apps/web/src/__tests__/map-privacy.test.ts`, `apps/web/src/__tests__/semantic-search-route.test.ts`
- Plan: lock the real `getMapImages()` query/guard source instead of reimplementing filtering in the test, and add a stub semantic ranking behavior test where dot-product and cosine ordering diverge.
- Acceptance: tests fail if the production map query drops the `topics.map_visible=true`/GPS predicates or if stub ranking becomes magnitude-biased.

### P29-03 - Extend the public API rate-limit gate to expensive GET routes

- Findings: `AGG-C29-13`
- Original severity/confidence: Medium / High
- Files: `apps/web/scripts/check-public-route-rate-limit.ts`, `apps/web/src/__tests__/check-public-route-rate-limit.test.ts`
- Plan: audit exported public `GET` handlers with expensive markers such as `ImageResponse`, DB imports/helpers, filesystem streams, Sharp, or embedding work; require an approved rate-limit helper or reasoned exemption.
- Acceptance: the gate passes existing public APIs and tests prove expensive unmetered GET routes fail while cheap GET routes pass.

### P29-04 - Add restore-maintenance metadata guard

- Findings: `AGG-C29-17`
- Original severity/confidence: Low / Medium
- Files: DB-backed public `generateMetadata()` routes under `apps/web/src/app/[locale]/(public)/`, shared helper/tests
- Plan: add a shared noindex restore-maintenance metadata helper and call it before DB reads in public metadata functions.
- Acceptance: source tests prove representative DB-backed metadata functions short-circuit before data helpers during restore maintenance.

### P29-05 - Align CLIP sidecar with runtime semantic-search mode

- Findings: `AGG-C29-27`
- Original severity/confidence: Low / High
- Files: `apps/web/src/lib/gallery-config.ts`, `apps/web/scripts/backfill-clip-embeddings.ts`, focused tests/source contracts
- Plan: share the runtime semantic mode resolver with the sidecar so raw DB `production` does not run as production unless `SEMANTIC_SEARCH_ALLOW_PRODUCTION=true` permits it.
- Acceptance: tests/source checks prove sidecar mode resolution matches `getGalleryConfig()` semantics.

### P29-06 - Fix retry/hydration/admin-selector UI defects

- Findings: `AGG-C29-30`, `AGG-C29-33`, `AGG-C29-36`
- Original severity/confidence: Low / High; Medium / High; Low / High
- Files: `apps/web/src/components/similar-photos.tsx`, `apps/web/src/components/nav-client.tsx`, `apps/web/e2e/admin.spec.ts`, `apps/web/e2e/helpers.ts`, focused tests/source contracts
- Plan: clear `SimilarPhotos` fetch cache on retryable failures, render a stable theme-control label/icon until mounted, and replace stale admin E2E `#admin-content` selectors with `#main-content`.
- Acceptance: tests/source contracts prove transient errors can retry, theme SSR/client first render is stable, and admin E2E selectors target the current layout landmark.

### P29-07 - Add explicit GPS publish confirmation and privacy copy

- Findings: `AGG-C29-34`, `AGG-C29-37`, `AGG-C29-38`
- Original severity/confidence: Medium / High; Medium / High; Medium / High
- Files: `apps/web/src/app/[locale]/admin/(protected)/categories/topic-manager.tsx`, `apps/web/messages/en.json`, `apps/web/messages/ko.json`, docs/copy tests
- Plan: require confirmation before switching a category from hidden to map-visible, make GPS-first-run consequences harder to miss, and update privacy copy to disclose short-lived full-IP rate-limit buckets.
- Acceptance: i18n parity remains green and source/UI tests prove false-to-true map publication cannot occur from a single switch click.

### P29-08 - Correct operator documentation drift

- Findings: `AGG-C29-21`, `AGG-C29-22`, `AGG-C29-23`, `AGG-C29-24`, `AGG-C29-25`, `AGG-C29-26`, `AGG-C29-40`
- Original severity/confidence: Medium / High; Medium / High; Medium / High; Low / High; Low / High; Low / Medium; Low-Medium / Medium
- Files: `apps/web/README.md`, `CLAUDE.md`, possibly top-level `README.md`
- Plan: update first-upload GPS guidance, current photographer audit baseline, auto-alt-text behavior/runbook, dynamic route freshness list, touch-target audit scope, migration-language errata, and DB-vs-file backup wording.
- Acceptance: docs reflect current code and do not tell future operators or agents to rely on stale review baselines or incomplete backups.

## Deferred Findings

Deferred in `cycle-29-2026-06-30-deferred.md`: `AGG-C29-02`, `AGG-C29-03`, `AGG-C29-04`, `AGG-C29-05`, `AGG-C29-06`, `AGG-C29-07`, `AGG-C29-08`, `AGG-C29-09`, `AGG-C29-12`, `AGG-C29-14`, `AGG-C29-15`, `AGG-C29-16`, `AGG-C29-18`, `AGG-C29-20`, `AGG-C29-28`, `AGG-C29-29`, `AGG-C29-31`, `AGG-C29-32`, `AGG-C29-35`, `AGG-C29-39`, `AGG-C29-41`, `AGG-C29-42`.

## Prompt 3 Execution Notes

- Use Ralph-style inspect/edit/verify loops until every scheduled item is complete.
- Run the full configured gate list: `npm run lint --workspace=apps/web`, `npm run lint:api-auth --workspace=apps/web`, `npm run lint:action-origin --workspace=apps/web`, `npm run lint:public-route-rate-limit --workspace=apps/web`, `npm run typecheck --workspace=apps/web`, `npm run build --workspace=apps/web`, `npm test --workspace=apps/web`.
- Commit/push signed Conventional Commit + gitmoji commits after green gates and `git pull --rebase`.
- Per-cycle deploy command after green pushed HEAD: `npm run deploy`.

## Progress

- [x] Prompt 1 aggregate review written.
- [x] Plan and deferred register written.
- [x] P29-01 implemented.
- [x] P29-02 implemented.
- [x] P29-03 implemented.
- [x] P29-04 implemented.
- [x] P29-05 implemented.
- [x] P29-06 implemented.
- [x] P29-07 implemented.
- [x] P29-08 implemented.
- [x] Deferred findings preserved with original severity/confidence and exit criteria.
- [x] Focused validation green: `npm test --workspace=apps/web -- check-public-route-rate-limit rate-limit-db semantic-search-route map-privacy cycle-29-source-contracts gallery-config` (115 tests) and `npm run lint:public-route-rate-limit --workspace=apps/web`.
- [x] Full gate list green: lint, api-auth lint, action-origin lint, public route rate-limit lint, typecheck, build, and full Vitest.
- [x] Gate warning recorded: local sitemap DB fallback in `cycle-29-2026-06-30-deferred.md`.
- [ ] Signed commit(s) pushed.
- [ ] Per-cycle deploy complete.
