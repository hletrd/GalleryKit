# Run-10 Cycle 16/100 Deferred Findings

Status: OPEN  
Aggregate: `.context/reviews/_aggregate.md`  
Date: 2026-07-08 KST

Deferred items preserve original severity/confidence. Repo policies still apply when reopened: GPG-signed Conventional Commits with gitmoji, `git pull --rebase` before push, no force-push, required quality gates, and deploy policy.

Repo-rule basis for operator/manual deferrals:

- `CLAUDE.md` documents the shipped deployment as a "single web-instance / single-writer topology" and says not to horizontally scale until process-local coordination is moved to shared storage.
- `CLAUDE.md` documents plaintext SQL dumps as "host/storage encryption is the operator boundary."
- `CLAUDE.md` / `apps/web/nginx/default.conf` document that live nginx/proxy application is operator-managed and that per-cycle deploys rebuild the app container, not host nginx.
- `CLAUDE.md` documents CLIP production activation as operator-enabled and requiring live host/model/DB verification before claiming activation.

## Deferred Register

| Finding | Original severity/confidence | Citation | Reason for deferral | Exit criterion |
| --- | --- | --- | --- | --- |
| `AGG-C16-10` mobile tag-filter wall | Medium / High | `apps/web/src/components/home-client.tsx:287-330`, `apps/web/src/components/tag-filter.tsx:62-122` | UI IA redesign larger than this correctness-focused cycle; no current functional break. | Reopen when mobile home IA is touched, when adding tag-filter sheet/rail work, or if mobile UX review marks it High. |
| `AGG-C16-11` toast-only admin validation | Medium / High | `topic-manager.tsx`, `tag-manager.tsx`, `admin-user-manager.tsx`, `seo-client.tsx` | Cross-form validation architecture touches multiple admin workflows; no confirmed data/security defect. | Reopen on next admin form refactor or accessibility review targeting admin validation. |
| `AGG-C16-12` admin image table-first workbench | Medium / Medium-High | `apps/web/src/components/image-manager.tsx:427-603` | Product/UI redesign, not a narrow bug fix. | Reopen when adding admin photo workbench, inspector, or mobile admin redesign. |
| `AGG-C16-13` truncated values rely on `title` | Low-Medium / High | `info-bottom-sheet.tsx`, `photo-viewer.tsx`, `upload-dropzone.tsx`, `image-manager.tsx` | Accessibility polish across several surfaces; lower risk than scheduled correctness work. | Reopen when touching metadata display or table filename rendering. |
| `AGG-C16-14` upload progress live announcement | Low-Medium / Medium | `apps/web/src/components/upload-dropzone.tsx:469-488` | Needs screen-reader validation and localized copy; no current upload correctness issue. | Reopen on upload UI/a11y work or screen-reader bug report. |
| `AGG-C16-15` tag autocomplete clipping | Medium / Medium | `image-manager.tsx:427-534`, `tag-input.tsx:184-233` | Likely issue requiring authenticated responsive visual validation; overlaps deferred workbench redesign. | Reopen if authenticated admin visual validation confirms clipping or when refactoring tag input/table layout. |
| `AGG-C16-16` token pending state split | Low-Medium / Medium | `tokens-client.tsx:28-321` | Localized UX-state issue, not correctness/security; token creation/revocation still guarded. | Reopen when token page async state or expiry UI is touched. |
| `AGG-C16-18` flat admin nav wrap | Low-Medium / High | `admin-nav.tsx:15-49`, `admin-header.tsx:13-27` | Information-architecture redesign larger than this cycle. | Reopen on admin nav redesign or mobile admin review. |
| `AGG-C16-20` logout revocation behavior tests | High / High | `apps/web/src/app/actions/auth.ts:286-312`, source tests | Proof-strength gap only; current source behavior was not reported broken in this cycle. Deferred under repo's existing source-contract-heavy test strategy until the flow is touched. | Reopen immediately if logout/restore/session code changes, or in a dedicated test-hardening cycle. |
| `AGG-C16-21` upload quota concurrent behavior test | High / High | `apps/web/src/app/actions/images.ts:232-319` | Proof-strength gap; source currently preclaims synchronously. Concurrent harness is broader than this cycle. | Reopen when upload action changes or when adding concurrency test harness. |
| `AGG-C16-22` tag aggregation behavior test | High / High | `apps/web/src/lib/data.ts:1682-1729` | Proof-strength gap; no current aggregation bug confirmed. | Reopen when search/tag query changes or in behavior-test hardening cycle. |
| `AGG-C16-23` GPS cleanup behavior tests | Medium / High | `images.ts:409-422`, `lr/upload/route.ts:407-424` | Implementation currently matches fail-closed source; missing behavior proof only. | Reopen when GPS stripping/upload rollback code changes or in privacy-test hardening cycle. |
| `AGG-C16-24` coverage ratchet | Medium / High | `apps/web/vitest.config.ts`, `.github/workflows/quality.yml` | Tooling/process change with broad CI implications; not tied to a current failing behavior. | Reopen when adopting coverage tooling or CI ratchet policy. |
| `AGG-C16-25` nav screenshot assertions | Medium / High | `apps/web/e2e/nav-visual-check.spec.ts:40-85` | Visual-baseline adoption requires broader screenshot policy and baseline churn management. | Reopen when adding visual regression gate. |
| `AGG-C16-26` semantic scan cap route assertions | Medium / Medium | semantic/similar route tests | Test-proof gap; runtime caps exist. | Reopen when semantic/similar query code changes or in semantic test-hardening work. |
| `AGG-C16-27` migration structural parity | Medium / High | `migrate-reconcile-coverage.test.ts:13-19`, `apps/web/scripts/migrate.js:858-947` | Manual/integration DB structural-diff proof; repo already documents migration discipline but lacks CI infra. | Reopen for migration changes, CI MySQL structural diff work, or schema incident. |
| `AGG-C16-28` password-change browser test | Medium / High | `password-form.tsx:36-120`, `apps/web/e2e/admin.spec.ts` | E2E coverage expansion; no current password-change bug confirmed. | Reopen when password page changes or admin e2e coverage expands. |
| `AGG-C16-29` touch-target unsized plain links | Low / Medium | `touch-target-audit.test.ts:457-465` | Policy/test harness refinement; current sampled surfaces pass. | Reopen when adding rendered DOM touch-target audit. |
| `AGG-C16-30` shared background DB budget | High / High | `image-queue.ts`, `admin-backfill-runner.ts`, `db/index.ts` | Performance/architecture work; CLAUDE already documents mutual over-subscription as a known budget note. | Reopen when implementing shared background semaphore or changing queue/backfill concurrency. |
| `AGG-C16-31` multipart framework buffering | High / High | `upload-limits.ts`, `images.ts`, `db-actions.ts` | Architectural route rewrite; CLAUDE "Important Notes" already documents multipart heap buffering. | Reopen when building streaming upload/restore route handlers or after RSS measurement breach. |
| `AGG-C16-32` brute-force semantic scan cost | Medium / High | semantic/similar routes, `clip-embeddings.ts` | Performance scalability; bounded by documented limits and operator-enabled feature gate. | Reopen when gallery size/search traffic exceeds current limits or adding vector index/cache. |
| `AGG-C16-33` missing backfill candidate index | Medium / High | `db/schema.ts`, backfill candidate queries | Requires migration + EXPLAIN validation; not blocking correctness. | Reopen when backfill scan latency is measured or schema migration window is planned. |
| `AGG-C16-34` map 10k marker scale | Medium-High / High | `data.ts:1766-1817`, map page/client | Product/perf redesign; no current correctness issue. | Reopen when map data approaches thousands of markers or map UX work starts. |
| `AGG-C16-35` batch delete directory scans | Medium / High | `images.ts:860-884`, `process-image.ts:575-663` | Performance optimization; deletion correctness remains intact. | Reopen when optimizing image deletion or NAS latency becomes measurable. |
| `AGG-C16-36` On This Day non-sargable query | Medium / Medium | `on-this-day-widget.tsx`, `data-timeline.ts` | Performance/index work requiring schema migration. | Reopen when home query latency is measured or adding generated month/day index. |
| `AGG-C16-37` smart collection predicate cost | Medium / Medium | `smart-collections.ts`, `data.ts` | Product/perf design; smart collection authoring is not yet admin-exposed. | Reopen before exposing smart-collection authoring UI or if public collection latency appears. |
| `AGG-C16-38` timeline/year 500 cards | Low / Medium | timeline/year pages | Low-priority archive performance; bounded response. | Reopen on archive pagination/lazy-load work. |
| `AGG-C16-39` admin CSV export memory | Low / High | `apps/web/src/app/[locale]/admin/db-actions.ts:45-118` | Admin-only bounded memory concern. | Reopen on export scaling issue or streaming export feature. |
| `AGG-C16-40` single-writer warning-only | High if multi-instance / High | `single-writer-guard.ts`, process-local state | Deferred under repo rule: "shipped Docker Compose deployment is a single web-instance / single-writer topology" and "do not horizontally scale" until shared coordination exists. | Reopen if deploying more than one web process, changing deploy topology, or deciding to fail production readiness on contention. |
| `AGG-C16-41` live proxy/nginx topology | High if misconfigured / High | `nginx/default.conf`, `deploy.sh`, `rate-limit.ts` | Deferred under repo operator-boundary rule: host nginx/proxy application is manual/operator-managed; deploy does not mutate host nginx. | Reopen on proxy/CDN/LB change, nginx template change, or failed `check-proxy-topology` evidence. |
| `AGG-C16-42` plaintext SQL backups | Medium / Medium | backup docs/actions | Deferred under repo rule: "Dumps are plaintext SQL at rest; host/storage encryption is the operator boundary." | Reopen if backups move to shared/untrusted host storage or encrypted backup feature is requested. |
| `AGG-C16-43` CLIP live activation proof | Medium / High | CLIP tests/workflow/runbook | Code/copy clarification is scheduled in WP3; live activation remains operator-state validation per CLAUDE CLIP runbook. | Reopen before enabling production semantic search or changing model/deploy path. |
| `AGG-C16-44` embedding model row replacement | Medium / Medium | `db/schema.ts:277-304`, `image-queue.ts` | Known active-row design documented in CLAUDE; schema migration/product decision required. | Reopen if supporting model history/upgrades without destructive replacement. |
| `AGG-C16-45` Docker image CI gap | Medium / High | `.github/workflows/quality.yml`, `Dockerfile` | CI policy/build-time cost change; deploy currently exercises Docker build. | Reopen when native deps change or adding Docker build gate. |
| `AGG-C16-46` Playwright browser matrix/networkidle | Medium/Low / High/Medium | `playwright.config.ts`, `hydration-photo-page.spec.ts` | Test matrix expansion; not tied to current failure. | Reopen when browser-specific bug appears or e2e matrix work is scheduled. |
| `AGG-C16-47` OMC runtime artifacts | Low / High | `.omc/plans/...`, `apps/web/src/__tests__/.omc/...` | Fix requires deleting tracked/runtime files; destructive-action policy requires explicit confirmation before file deletion. | Reopen after explicit user confirmation to remove tracked `.omc` artifacts and clean nested runtime state. |
| `AGG-C16-49` RTL structural-only support | Low / Medium | locale layout/constants/path tests | No RTL locale currently shipped; future feature. | Reopen when adding RTL locale or logical-layout audit. |

## Gate Warning Deferrals

| Finding | Original severity/confidence | Citation | Reason for deferral | Exit criterion |
| --- | --- | --- | --- | --- |
| `GATE-C16-W01` local build emits sitemap DB fallback warning | Low / High | `npm run build --workspace=apps/web`, `apps/web/src/app/sitemap.ts` | The local gate environment has no MySQL listener at the `.env.local` default `127.0.0.1:3306`; the sitemap route intentionally falls back to the homepage-only sitemap and the build exits 0. Changing the operator/local DB environment is outside this code-fix cycle. | Reopen if CI or production deploy emits the same fallback with a configured database, or if the sitemap fallback becomes a blocking build condition. |
| `GATE-C16-W02` Playwright web-server logs non-fatal `ResponseAborted` and AVIF fixture warnings | Low / Medium | `npm run test:e2e --workspace=apps/web`, `apps/web/e2e/*.spec.ts` | The e2e gate passed with 45 passing tests and 2 expected skips; the messages are server/test-fixture noise rather than failed assertions. Fixing them cleanly requires dedicated test-server instrumentation or fixture regeneration beyond the scheduled fixes. | Reopen if the warnings become failed assertions, appear in production logs, or a dedicated e2e-noise cleanup is scheduled. |
