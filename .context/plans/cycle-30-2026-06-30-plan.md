# Cycle 30/100 Implementation Plan

Date: 2026-06-30 KST  
Review source: `.context/reviews/_aggregate.md`  
Status: implementation complete; gates green; commit/deploy pending

## Repo Rules Read Before Planning

Read order satisfied for deferred-fix policy: `CLAUDE.md`, `AGENTS.md`, `.context/**` review/plan history, lookup for `.cursorrules` and `CONTRIBUTING.md` (none present), and docs/policy references found in the repo. Repo rules require signed Conventional Commit + gitmoji commits, `git pull --rebase` before push, all configured gates, and per-cycle deploy with `npm run deploy` after green pushed changes.

Every cycle-30 aggregate finding is either scheduled below or explicitly deferred in `cycle-30-2026-06-30-deferred.md`. Security, correctness, and data-loss findings are scheduled unless they are production/manual-validation findings with no safe local-code action this cycle.

## Scheduled Fixes

### P30-01 - Resume image queue after partial restore-prep failure

- Finding: `AGG-C30-01`
- Original severity/confidence: High / High
- Files: `apps/web/src/app/[locale]/admin/db-actions.ts`, `apps/web/src/__tests__/restore-upload-lock.test.ts`
- Plan: set `imageQueueQuiesced = true` immediately after successful `quiesceImageProcessingQueueForRestore()` and before subsequent drain work; lock that ordering in the restore source contract.
- Acceptance: targeted restore lock test and full test suite pass.
- Progress: [x] Implemented.

### P30-02 - Execute the real map GPS query in tests

- Finding: `AGG-C30-02`
- Original severity/confidence: High / High
- Files: `apps/web/src/__tests__/map-get-images-behavior.test.ts`
- Plan: add a mocked Drizzle-chain behavior test that imports and invokes `getMapImages()`, asserts the map-visible/GPS query predicates, and asserts the runtime GPS leak guard rejects a bad row.
- Acceptance: targeted map test fails if `getMapImages()` drops the `topics.map_visible=true` predicate, GPS non-null predicates, marker limit, or runtime guard.
- Progress: [x] Implemented.

### P30-03 - Enforce public expensive-GET limiter dominance

- Finding: `AGG-C30-03`
- Original severity/confidence: Medium / Medium
- Files: `apps/web/scripts/check-public-route-rate-limit.ts`, `apps/web/src/__tests__/check-public-route-rate-limit.test.ts`
- Plan: make the checker reject expensive public GET handlers when expensive markers appear before a pre-increment limiter gate; support captured limiter-result patterns.
- Acceptance: one test fails when DB work precedes the limiter and another passes when a captured limiter result returns before DB work.
- Progress: [x] Implemented.

### P30-04 - Correct public-route gate docs

- Finding: `AGG-C30-04`
- Original severity/confidence: Medium / High
- Files: `AGENTS.md`
- Plan: update the gate description to state that mutating public routes and expensive public GET routes require a limiter before protected work.
- Acceptance: docs no longer say GET handlers are not scanned.
- Progress: [x] Implemented.

### P30-05 - Make generic search failure copy accurate

- Findings: `AGG-C30-05`, `AGG-C30-19`, partial mitigation for `AGG-C30-21`
- Original severity/confidence: High / High symptom, Medium root cause; Medium / Medium; Medium / High
- Files: `apps/web/messages/en.json`, `apps/web/messages/ko.json`, `apps/web/src/components/search.tsx`, `apps/web/src/__tests__/search-short-query-guard.test.ts`
- Plan: change the generic search error copy to temporary unavailability, preserving short-query and rate-limit messages; clean stale comments that reference the old copy.
- Acceptance: i18n files compile and search short-query source contract remains green.
- Progress: [x] Implemented.

### P30-06 - Verify `/api/health` docs alignment

- Finding: `AGG-C30-06`
- Original severity/confidence: Medium / High
- Files: `README.md`, `apps/web/README.md`, `CLAUDE.md`, `apps/web/src/__tests__/health-route.test.ts`
- Plan: inspect docs/tests for liveness-only default and optional DB readiness. If already aligned, mark no code change needed.
- Acceptance: docs explicitly state `/api/health` is liveness-only by default and DB readiness only when `HEALTH_CHECK_DB=true`.
- Progress: [x] Verified; no code change needed.

## Deferred Findings

Deferred in `cycle-30-2026-06-30-deferred.md`: `AGG-C30-07` through `AGG-C30-18`, `AGG-C30-20` through `AGG-C30-27`, plus the live root-cause portion of `AGG-C30-05`.

## Prompt 3 Execution Notes

- Use Ralph-style inspect/edit/verify loops until every scheduled item is complete.
- Run full configured gates: `npm run lint --workspace=apps/web`, `npm run lint:api-auth --workspace=apps/web`, `npm run lint:action-origin --workspace=apps/web`, `npm run lint:public-route-rate-limit --workspace=apps/web`, `npm run typecheck --workspace=apps/web`, `npm run build --workspace=apps/web`, `npm test --workspace=apps/web`.
- Fix blocking gate errors before commit/push. Record warnings in the deferred register if not fixed.
- Commit/push signed Conventional Commit + gitmoji commits after green gates and `git pull --rebase`.
- Per-cycle deploy command after green pushed HEAD: `npm run deploy`.

## Progress

- [x] Prompt 1 aggregate review written.
- [x] Plan and deferred register written.
- [x] P30-01 implemented.
- [x] P30-02 implemented.
- [x] P30-03 implemented.
- [x] P30-04 implemented.
- [x] P30-05 implemented.
- [x] P30-06 verified.
- [x] Deferred findings preserved with original severity/confidence and exit criteria.
- [x] Focused validation green: `npm test --workspace=apps/web -- map-get-images-behavior check-public-route-rate-limit restore-upload-lock`.
- [x] Full gate list green: lint, api-auth lint, action-origin lint, public route rate-limit lint, typecheck, build, and full Vitest.
- [x] Gate warning recorded: local sitemap DB fallback in `cycle-30-2026-06-30-deferred.md`.
- [ ] Signed commit(s) pushed.
- [ ] Per-cycle deploy complete.
