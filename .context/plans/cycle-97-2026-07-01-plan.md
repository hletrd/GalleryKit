# Cycle 97/100 Implementation Plan

Starting deployed HEAD: `061c1c81af234469641f75a53e5bbc61fa63114a`.

Review aggregate: `.context/reviews/cycle-97-2026-07-01/_aggregate.md`.

## Scheduled Safe Narrow Fixes

- [x] `C97-01` Cycle 96 terminal ledger stale for current deployed `master` - update cycle 96 plan evidence, latest aggregate pointer, and plan index.
- [x] `C97-02` Upload accept regression test does not enforce backend/dropzone parity - compare backend `ALLOWED_EXTENSIONS` and dropzone accept set exactly.
- [x] `C97-03` Derivative cleanup deletes fresh sidecar temp files and misses stale backups - delete only stale `.tmp`/`.bak` files and preserve fresh active temps.
- [x] `C97-04` Grid P3 badges never become visible - bridge display-gamut and force-show root attributes from `HomeClient` on grid pages.
- [x] `C97-05` Restore SQL scanner raw tail can miss split routine/view DDL - retain compact sanitized scan-tail forms across chunks and test long-gap splits.
- [x] `C97-06` Per-topic feed miss path lacks public rate limit - add a feed-specific pre-DB limiter.
- [x] `C97-07` Atom feeds bypass restore maintenance - return no-store 503 before feed-shaping work.

## Implementation Notes

- No schema migration, new dependency, broad refactor, production config change, or destructive operation.
- Feed success responses stay cacheable; only restore-maintenance and rate-limit responses are no-store.
- The grid color bridge is scoped to `HomeClient` so it does not fight `PhotoViewer` cleanup outside grid pages.

## Required Gates

- [x] `npm run lint --workspace=apps/web` - passed.
- [x] `npm run lint:api-auth --workspace=apps/web` - passed.
- [x] `npm run lint:action-origin --workspace=apps/web` - passed.
- [x] `npm run lint:public-route-rate-limit --workspace=apps/web` - passed.
- [x] `npm run typecheck --workspace=apps/web` - passed.
- [x] `BASE_URL=https://gallery.atik.kr npm run build --workspace=apps/web` - passed; local DB unavailable, sitemap used the documented homepage-only fallback during static generation.
- [x] `npm test --workspace=apps/web` - passed, 303 files / 2851 tests; 2 files / 4 tests skipped.
- [x] Focused tests: `npm test --workspace=apps/web -- feed-conditional.test.ts client-source-contracts.test.ts sql-restore-scan.test.ts image-queue-cleanup.test.ts cycle-22-source-contracts.test.ts` - passed, 5 files / 58 tests.
- [x] `git diff --cached --check` - passed.

## Progress

- Prompt 1 review fan-out completed; aggregate written.
- Prompt 2 plan/deferred files written.
- Prompt 3 safe narrow implementation completed for all seven scheduled findings.

## Completion Evidence

- Full required gates through unit tests passed.
- `git diff --cached --check` passed.
- Signed commit `6f40f66d9a6949ea866966230e5fe0ba61024637` (`fix(cycle-97): 🐛 close review findings`) is on `origin/master`; `git show --show-signature HEAD` in Cycle 98 reported a good GPG signature.
- Cycle 98 invocation identified `6f40f66d9a6949ea866966230e5fe0ba61024637` as the current deployed `master` HEAD. Cycle 98 deploy supersedes production smoke after its pushed fix.
