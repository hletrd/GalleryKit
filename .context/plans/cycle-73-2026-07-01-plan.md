# Cycle 73/100 Implementation Plan

Start HEAD: `96459b7a264c4e0110d15bb76840d260447335e8`.
Review aggregate: `.context/reviews/cycle-73-2026-07-01/_aggregate.md`.

## Goal

Fix the safe, reviewable Cycle 73 findings, keep review/plan ledgers current, run all required gates, commit, push, and deploy with `npm run deploy` from the repo root.

## Scheduled Fixes

1. `C73-01`: distinguish existing unprocessed photo rows from truly missing rows in the per-photo OG route and keep pending-row fallback redirects non-cacheable.
2. `C73-02`: close Cycle 72 terminal plan/index state using `96459b7a` start evidence.
3. `C73-03`: replace stale feed conditional helper-only coverage with root/topic route-level 200, matching-ETag 304, changed-SEO ETag, topic 404, and invalid-locale tests.
4. `C73-04`: add route-level tests proving per-photo OG temporary fallback redirects use `no-store` for derivative misses and pending rows.

## Deferred This Cycle

New deferred findings are recorded in `.context/plans/cycle-73-2026-07-01-deferred.md`:

- `C73-05`: behavior-level sidecar derivative write-boundary rollback test.
- `C73-06`: Settings UI backfill-warning persistence behavior test.

Carry-forward deferred items remain repeated in the deferred file.

## Regression Coverage

- `feed-conditional.test.ts`: route-level root/topic feed conditional behavior and locale/topic miss branches.
- `og-route-rate-limit-behavior.test.ts`: route-level per-photo OG derivative-miss and pending-row fallback cache headers.
- Existing source contract tests remain as belt-and-braces coverage for feed and OG route wiring.

## Validation

Run required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then sign, `git pull --rebase`, push, and deploy with `npm run deploy`.

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run required gates.
- [x] Commit, pull --rebase, push.
- [x] Deploy with `npm run deploy`.

## Gate Evidence

- Focused regression: `npm test --workspace=apps/web -- --run src/__tests__/feed-conditional.test.ts src/__tests__/feed-sized-derivative.test.ts src/__tests__/og-route-rate-limit-behavior.test.ts src/__tests__/og-photo-fallback.test.ts` - pass: 4 files passed, 50 tests passed. First focused run found one test setup issue in the new OG route test; fixed before required gates.
- `npm run lint --workspace=apps/web` - pass.
- `npm run lint:api-auth --workspace=apps/web` - pass: both admin API routes OK.
- `npm run lint:action-origin --workspace=apps/web` - pass: all mutating server actions enforce same-origin provenance.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
- `npm run typecheck --workspace=apps/web` - pass after normalizing nullable `images.processed` to an explicit helper boolean.
- `npm run build --workspace=apps/web` - pass. Local sitemap generation logged the expected unavailable local MySQL fallback and exited successfully.
- `npm test --workspace=apps/web` - pass: 297 files passed, 2 skipped; 2788 tests passed, 4 skipped.
- Commit/push evidence: signed commit `92924220ff51258a8d29d78444200d7f7dcbd430` is `master` and `origin/master` at Cycle 74 start.
- Deploy evidence: Cycle 74 invocation states the current deployed `master` HEAD at start was `92924220`.

## Gate Fix Iterations

1. Focused OG route regression initially asserted the derivative-fetch path without a valid mocked canonical `BASE_URL`; mocked `BASE_URL` explicitly and reran focused tests green.
2. `typecheck` initially failed because the minimal image-processing-state helper returned nullable `images.processed`; normalized it to `row.processed === true` and reran `typecheck` green.
