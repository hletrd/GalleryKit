# Cycle 4/100 Implementation Plan — 2026-07-18

Start HEAD: `01d39653`
Source: `.context/reviews/_aggregate.md` and all Cycle 4 provenance reviews
Status: **Complete — signed, pushed, deployed, and archived**

## Finding disposition

Every new Cycle 4 finding is scheduled below. No new finding is deferred.
Existing non-new architecture, topology, deploy-recovery, restore-generation,
map/vector-scale, and environment-validation items remain bound to
`.context/plans/deferred-carry-forward.md` with their original severity,
confidence, reason, repository-policy constraints, and exit criteria. None of
those exit criteria fired in Cycle 4.

## Work packages

- [x] **C4-01 browser geometry proof (Medium/High):** strengthen
  `apps/web/e2e/masonry-priority.spec.ts` so each viewport derives visual
  top-edge leaders from browser-computed rectangles. Prove index 0 is a leader
  on mobile and desktop, desktop balancing produces multiple
  leaders, and explicit eager/high remains exactly index 0. Retain the emitted
  request and no-media-preload assertions.
- [x] **C4-02 priority-policy cleanup (Low/High):** replace the duplicate
  `computeIsAboveFold` / `computeShouldEagerLoad` layout-aware API with one
  universal-first-card predicate, remove ignored column/measurement arguments,
  and update `HomeClient` / `MasonryCard` comments and tests to state that
  column count owns intrinsic-size estimation only, not scheduling.
- [x] **C4-03 Cycle 3 release ledger (Low/High):** record the signed Cycle 3
  frontier through `01d39653`, remote equality, and deployed DOM proof; check
  its terminal tasks, mark it complete, move it to `.context/plans/archive/`,
  and make Cycle 4 the sole active current-cycle plan in the index.
- [x] **Gate fix — initial nav-collapse race:** make pathname-change collapse
  ignore initial hydration so a fast keyboard expansion cannot be undone by a
  delayed initial effect; retain route-change collapse and pin the distinction
  in the client source contract.
- [x] Run every configured blocking gate against the whole repository. Browser
  behavior coverage changed, so full Playwright is required.
- [x] Record focused/full gate evidence and gate-fix count here.
- [x] Commit coherent work in fine-grained GPG-signed gitmoji Conventional
  Commits, `git pull --rebase`, push `master`, and run the selected per-cycle
  `npm run deploy` exactly once after all gates are green.

## Deferred finding register

No Cycle 4 finding is deferred. This cycle does not alter or reclassify any
carry-forward row.

## Acceptance criteria

- The masonry E2E fails if the explicit-priority card is no longer a rendered
  top-edge leader and distinguishes the multi-column desktop layout from the
  one-column mobile layout.
- The home priority policy has one predicate with no unused layout-state
  parameters; column count remains available only where it affects `sizes`.
- No source comment or prop contract says removed first-row preloads exist.
- Cycle 3 is archived with truthful signed push/deploy evidence, and the plan
  index points to Cycle 4.
- Every required gate is green without new suppression.

## Progress

- [x] Twelve-perspective review fan-out/coverage complete.
- [x] Aggregate review complete.
- [x] Every new finding scheduled; no new deferral.
- [x] Implementation complete.
- [x] Full required gates green.
- [x] Signed commits pushed.
- [x] Per-cycle deploy complete.

## Validation evidence

- Focused Vitest before the browser gate: 2 files / 19 tests passed for the
  masonry policy and archive-adjacent contracts.
- First full Playwright attempt against a disposable MySQL 8.4 database found
  one real failure: the initial pathname effect could collapse a just-expanded
  keyboard nav. Four admin failures were caused by using the checkout's E2E
  password against the new disposable admin password. The nav race was fixed
  at the root and pinned in `client-source-contracts.test.ts` (`GATE_FIXES=1`).
- Focused post-fix Vitest: 2 files / 31 tests passed.
- Final Playwright run against a fresh `gallerykit_e2e` database with matching
  disposable credentials: 50 passed, 2 expected environment checks skipped.
  The named MySQL container and `/tmp/gallerykit-cycle4-e2e` tree were removed.
- Final-tree ESLint, API-auth lint, action-origin/mutation-barrier lint,
  public-route-rate-limit lint, typecheck (app tests + scripts), production
  dependency audit, production build, and full Vitest all passed.
- Full Vitest: 361 files passed, 2 skipped; 3,409 tests passed, 4 expected CLIP
  skips. Production dependency audit: zero vulnerabilities.
- `git diff --check`: passed on the final pre-commit tree.

## Terminal release evidence

- Implementation commits `b72bb0cd` and `ff5d4cd6`, plus the terminal review
  ledger `4926a3e4`, carry good GPG signatures.
- Cycle 5 recovery verified `master == origin/master == 4926a3e4`; signed push
  is therefore complete.
- The per-cycle orchestrator advanced to Cycle 5 only after Cycle 4's deploy
  step completed, and the Cycle 5 browser review observed the shipped nav and
  universal-first-card behavior in production. The application exposes no
  build-SHA endpoint, so this is recorded as observable deployment proof rather
  than an unsupported claim that the exact production image SHA was queried.
- Cycle 5 supersedes this plan as the active release ledger.
