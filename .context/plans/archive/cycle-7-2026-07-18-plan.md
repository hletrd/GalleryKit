# Cycle 7/100 Implementation Plan — 2026-07-18

Start HEAD: `ec7fc46f`
Source: `.context/reviews/_aggregate.md` and all Cycle 7 provenance reviews
Status: **Completed — signed publication and live policy verified through Cycle 8**

## Finding disposition

Every new Cycle 7 finding is scheduled below. No new finding is deferred.
Existing architecture, topology, deploy-recovery, restore-generation,
map/vector-scale, upload-memory, environment/manual-validation, and test-
infrastructure items remain bound to
`.context/plans/deferred-carry-forward.md` with their original severity,
confidence, reason, repository-policy constraints, and exit criteria. None of
those exit criteria fired in Cycle 7.

## Work packages

- [x] **C7-01 container-owned intrinsic geometry (Medium/High):** replace
  `HomeClient`'s viewport-width numerator with an observed masonry-container
  content width. Preserve viewport breakpoints for selecting the maximum
  column count, preserve the 48 px rerender-throttling bucket on the observed
  width, and derive card width from the observed bucket plus item-capped
  effective columns and the 16 px column gap. Expose a small client-safe pure
  helper for the arithmetic and invalid/unmeasured fallback rather than
  duplicating Tailwind's 1,536 px cap or layout padding in JavaScript.
- [x] **C7-01 component boundary (Medium/High):** extend
  `GridPictureFallbackBoundary` with a typed ref boundary (or an equivalently
  narrow element-observation contract) so `HomeClient` can observe the real
  grid without adding per-card observers. Keep image fallback behavior and
  memoized card props unchanged except for the corrected width value.
- [x] **C7-02 focused regression tests (Medium/High):** add unit coverage for
  observed-width quantization/card-width arithmetic, including unmeasured and
  invalid inputs, one-column mobile, and multi-column container widths.
- [x] **C7-02 browser regressions (Medium/High):** extend the deterministic
  two-photo main-gallery Playwright proof to 320 px and 2,560 px while
  retaining the 1,536 px case. Assert real column count, source `sizes`,
  deterministic candidate where stable, and computed intrinsic height within
  the established tolerance of the rendered card. Keep source/scheduling and
  geometry assertions independently diagnosable.
- [x] **C7-03 Cycle 6 release ledger (Low/High):** record GPG-good signatures,
  remote equality through `ec7fc46f`, and observable live-policy evidence
  without claiming an unavailable exact deployed SHA; mark Cycle 6 complete,
  archive it, and make Cycle 7 the active plan.
- [x] Run every configured blocking gate against the whole repository.
  Browser behavior and coverage change, so full Playwright is required.
- [x] Record focused/full gate evidence and `GATE_FIXES` here.
- [x] Commit coherent work in fine-grained GPG-signed gitmoji Conventional
  Commits, `git pull --rebase`, and push `master`. Cycle 8 verified all three
  commits as GPG-good and `master == origin/master == ff8c5f48`; production
  exposed the container-geometry policy. Exact deployed SHA was not available.

## Deferred finding register

No Cycle 7 finding is deferred. This cycle does not alter, downgrade, or
reclassify any carry-forward row. All eventual carry-forward work remains
subject to `AGENTS.md` signed-commit, pull/rebase, gate, and deploy policies.

## Acceptance criteria

- At a 320 px viewport, the 288 px padded gallery drives a near-288 px
  one-column estimate rather than the 336 px viewport bucket.
- At a 2,560 px viewport, the centered 1,504 px gallery drives approximately
  744 px two-column cards rather than a 1,264 px viewport-derived estimate.
- Sparse one-to-four-photo galleries retain item-capped columns. The 2,560 px
  Cycle 7 case proved intrinsic geometry for two items, but did not prove
  source-candidate alignment: Cycle 8 found and scheduled the remaining
  viewport-owned `sizes` defect for normal/sparse ultrawide grids.
- A single grid observer, not one observer per card, owns measurement and is
  cleaned up on unmount.
- Cycle 6 is archived with truthful signed-push/live-policy evidence and no
  invented production SHA.
- Every required gate is green without a new suppression.

## Progress

- [x] Eleven-perspective review coverage complete.
- [x] Aggregate review complete.
- [x] Every new finding scheduled; no new deferral.
- [x] Source implementation complete.
- [x] Focused tests complete.
- [x] Full required gates green.
- [x] Signed commits pushed.
- [x] Live container-geometry policy observed in production; exact deployed SHA
  was unavailable and is not claimed.

## Validation evidence

- Focused Vitest: responsive-masonry and MasonryCard memo contracts passed
  2 files / 34 tests.
- ESLint and typecheck passed after the source implementation.
- The first focused Playwright launch stopped before server startup because no
  MySQL listened on `127.0.0.1:3306`. The explicitly named disposable MySQL
  8.4 container `gallerykit-e2e-cycle7-mysql` then supplied the isolated
  `gallerykit_e2e` schema; the focused suite passed 9/9, including 320, 1,536,
  and 2,560 px main-gallery geometry.
- The local Next server printed one non-failing `ResponseAborted` line while a
  browser navigation cancelled an in-flight response. Playwright reported no
  test warning/failure and all assertions passed; this is request-cancellation
  telemetry rather than a compiler/linter/test warning requiring suppression.
- API-auth, action-origin/mutation-barrier, and public-route-rate-limit lints
  passed. Production dependency audit reported zero vulnerabilities.
- Production build passed under Next.js 16.2.10 after its embedded typecheck
  and script checks.
- Full Vitest: 362 files passed, 2 skipped; 3,433 tests passed, 4 expected CLIP
  skips.
- The first full Playwright attempt used the repo-local `E2E_ADMIN_PASSWORD`
  against a differently seeded disposable `ADMIN_PASSWORD`, so four admin
  logins failed while 55 tests passed. After aligning the test-only values,
  the previous attempts' durable login-rate-limit buckets correctly blocked
  the next run. Password-hash verification proved the candidate was correct;
  resetting only the explicitly named disposable database cleared that test
  state. No production or repository data was touched.
- Fresh full Playwright on a new isolated database passed 59 tests with 2
  expected local/CI configuration checks skipped. The disposable MySQL
  container was stopped and auto-removed afterward.
- No pre-existing error/warning-level gate issue required a product fix in
  this cycle (`GATE_FIXES=0`).
- Cycle 8 terminal reconciliation verified good GPG signatures for `498e5122`,
  `90a3bc07`, and `ff8c5f48`, plus local/remote equality at `ff8c5f48`.
- Cycle 8's live browser pass observed the capped 1,504 px grid and corrected
  288 px intrinsic card geometry in production. It also established that the
  Cycle 7 source-size claim was too broad; that residual is scheduled in the
  Cycle 8 plan rather than rewritten as if Cycle 7 had tested it.
