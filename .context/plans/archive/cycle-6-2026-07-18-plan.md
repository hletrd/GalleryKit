# Cycle 6/100 Implementation Plan — 2026-07-18

Start HEAD: `6e4c25c8`
Source: `.context/reviews/_aggregate.md` and all Cycle 6 provenance reviews
Status: **Complete — signed, pushed, and deployed**

## Finding disposition

Every new Cycle 6 finding was scheduled below. No new finding was deferred.
Existing architecture, topology, deploy-recovery, restore-generation,
map/vector-scale, and environment/manual-validation items remain bound to
`.context/plans/deferred-carry-forward.md` with their original severity,
confidence, reason, repository-policy constraints, and exit criteria. None of
those exit criteria fired in Cycle 6.

## Work packages

- [x] **C6-01 effective-column ownership (Medium/High):** add a client-safe
  effective-column helper in `apps/web/src/lib/responsive-masonry.ts`, reuse it
  for `HomeClient`'s item-count-capped Tailwind classes and for the
  `estimatedCardWidth` divisor, and update comments so source-size, layout,
  and intrinsic geometry describe the same invariant. Preserve the current
  viewport-width estimator and 48 px resize bucket; container observation is
  not required for this confirmed item-count defect.
- [x] **C6-01 focused regression tests (Medium/High):** extend
  `responsive-masonry.test.ts` to prove effective columns for empty/invalid,
  sparse, and full counts at every supported maximum, and update the
  MasonryCard memo contract only where the shared policy changes its inputs.
- [x] **C6-02 home browser integration (Medium/High):** extend
  `e2e/responsive-masonry.spec.ts` to visit the seeded two-photo main route at
  representative responsive boundaries. Assert actual computed columns,
  emitted source `sizes`, selected candidate where deterministic, and that
  the intrinsic-height hint stays close to the rendered card rather than a
  raw five-column estimate. Exercise runtime DOM behavior rather than adding
  another source-text replica.
- [x] **C6-03 Cycle 5 release ledger (Low/High):** record good-signature and
  remote-equality evidence for Cycle 5, preserve the distinction between
  observable live policy and unavailable exact deploy SHA, archive Cycle 5,
  and make Cycle 6 the current plan.
- [x] Run every configured blocking gate against the whole repository.
  Browser-flow coverage changes, so full Playwright is required.
- [x] Record focused/full gate evidence and `GATE_FIXES` here.
- [x] Commit coherent work in fine-grained GPG-signed gitmoji Conventional
  Commits, `git pull --rebase`, push `master`, and run the selected per-cycle
  `npm run deploy` exactly once after every gate is green.

## Deferred finding register

No Cycle 6 finding is deferred. This cycle does not alter, downgrade, or
reclassify any carry-forward row.

## Acceptance criteria

- A one-to-four-photo main gallery uses the same item-capped effective column
  count for Tailwind classes, source-size hints, and intrinsic-width math.
- At 1,536 px, the deterministic two-photo E2E gallery renders two columns,
  advertises `50vw`, and no longer emits a five-column-derived intrinsic hint.
- Five-plus-photo galleries retain the existing one/two/three/four/five
  breakpoint policy, scheduling attributes, and source candidate behavior.
- Cycle 5 is archived with truthful signed-push and live-policy evidence,
  without claiming an exact production SHA that the deploy surface cannot
  expose.
- Every required gate is green without a new suppression.

## Progress

- [x] Eleven-perspective review coverage complete.
- [x] Aggregate review complete.
- [x] Every new finding scheduled; no new deferral.
- [x] Implementation complete.
- [x] Full required gates green.
- [x] Signed commits pushed.
- [x] Per-cycle deploy complete.

## Validation evidence

- Focused Vitest: responsive-masonry and MasonryCard memo contracts passed
  2 files / 22 tests.
- Focused Playwright: the new seeded two-photo main-gallery proof plus the six
  archive/shared responsive cases passed 7/7.
- ESLint, API-auth lint, action-origin/mutation-barrier lint, and public-route-
  rate-limit lint passed with no new warnings or suppressions.
- Full typecheck passed for the app/tests and JavaScript/TypeScript scripts.
- Production build passed under Next.js 16.2.10.
- Full Vitest: 362 files passed, 2 skipped; 3,421 tests passed, 4 expected CLIP
  skips.
- Production dependency audit: zero vulnerabilities.
- Full Playwright: 57 passed, 2 expected environment checks skipped. The new
  main route proved two columns, `50vw`, deterministic 1536w selection, and an
  intrinsic-height hint within 15% of the rendered sparse card.
- The first focused Playwright attempt failed before server startup because no
  MySQL process listened on `127.0.0.1:3306`. An explicitly named disposable
  MySQL 8.4 container supplied the repository's E2E database; focused and full
  browser suites then passed. This was environment recovery, not a gate issue.
- No pre-existing error/warning-level gate issue required a product fix in
  this cycle (`GATE_FIXES=0`).

## Terminal release evidence

- `git verify-commit` reports good signatures for `fcbce386`, `03a96a3d`, and
  `ec7fc46f`; `master` and `origin/master` both reached `ec7fc46f`.
- Cycle 6's orchestrator recorded `DEPLOY: per-cycle-success`. Cycle 7 live
  production validation confirmed the item-count-capped source, column, and
  intrinsic policy was deployed. The public surface does not expose an exact
  Git SHA, so this record does not claim one.
