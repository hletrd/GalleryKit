# Cycle 2/100 Implementation Plan — 2026-07-18

Start HEAD: `ba4bc60a`
Source: `.context/reviews/_aggregate.md` and all Cycle 2 provenance reviews
Status: **Complete — signed, pushed, deployed, and superseded by Cycle 3**

## Finding disposition

Every new aggregate finding was scheduled below. Revalidated findings remain
in the authoritative `.context/plans/deferred-carry-forward.md` register with
their original severity/confidence, concrete reasons, repository-policy
constraints, and exit criteria. No security, correctness, or data-loss finding
was newly deferred.

The deploy-owner hypothesis raised by code-reviewer/critic/verifier/test/docs
was explicitly resolved, not silently dropped: the repository owner already
owns the executed checkout script, so accepting that owner's private env file
does not cross an additional privilege boundary. The security reviewer
independently confirmed this. Documentation now states that an immutable,
root-owned installed helper would require a different policy.

## Completed work packages

- [x] **C2-01 sitemap runtime ownership:** prevent a DB-less production build
  from sealing fallback rows into a fresh one-hour ISR artifact; keep the route
  resilient to runtime DB outages; update diagnostics and regression coverage.
- [x] **C2-02 responsive image scheduling:** restore a mobile-safe SSR eager
  budget and provide media-qualified preload hints for desktop. Cycle 3 later
  found that CSS-column geometry invalidated the selected desktop identities;
  that follow-up is tracked in the Cycle 3 plan rather than hidden here.
- [x] **C2-03 combobox state:** make `aria-expanded` track actual listbox
  presence, retain dialog-trigger semantics, and cover empty/loading/no-result
  and result states.
- [x] **C2-04 semantic recall docs:** distinguish repeated backfill runs that
  complete a missing-embedding backlog from request-time recall.
- [x] **C2-05 deploy trust wording:** document the inherent checkout-owner trust
  boundary without weakening private-file checks.
- [x] Run every configured blocking gate, including Playwright.
- [x] Commit each coherent work item with signed gitmoji Conventional Commits,
  pull/rebase, push `master`, and run the selected per-cycle deploy.

## Deferred-finding mapping

- ARCH-2-01 / TRC-C2-R1 / DBG-C2-R2: existing shared background DB-budget row.
- CR-2-03 / ARCH-2-03 / CRIT-2-03 / TEST-2-03 / DBG-C2-R1: existing failed
  deployment promotion/rollback/cleanup rows.
- SEC-C2-R1: existing warn-only single-writer/topology row.
- PERF-C2-R1: existing large GPS-map rendering row.
- PERF-C2-R2: existing semantic-vector scan/ranking row.
- TRC-C2-R2: existing SQL-restore/file-store generation boundary row.

All mappings retain the authoritative register's original severity and
confidence. Their exit criteria did not fire in Cycle 2.

## Completion evidence

- Focused regression run: 3 files / 25 tests passed.
- Full Vitest: 361 files passed, 2 skipped; 3,410 tests passed, 4 skipped.
- Playwright: 46 passed, 2 skipped against the disposable MySQL E2E database.
- ESLint, API-auth lint, action-origin lint, public-route-rate-limit lint,
  typecheck, production build, production dependency audit, and
  `git diff --check`: passed.
- Built artifact: `/sitemap.xml` absent from `.next/prerender-manifest.json`.
- Gate repair: one incorrect helper import found by typecheck was corrected
  before the full typecheck/build rerun (`GATE_FIXES=1`).
- Signed release frontier: `e307fdd4`, `2875b816`, `0059d2bb`, and `afa11cf4`
  carry good GPG signatures; `master == origin/master == afa11cf4` at Cycle 3
  review start.
- Live Cycle 3 browser verification observed the shipped search and responsive
  preload behavior in production. Cycle 3 also corrected this plan's earlier
  overstatement: the preload validation was one-off/manual evidence, not a
  committed request-timeline Playwright regression.
