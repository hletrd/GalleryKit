# Cycle 8/100 Implementation Plan — 2026-07-18

Start HEAD: `ff8c5f48`
Source: `.context/reviews/_aggregate.md` and all Cycle 8 provenance reviews
Status: **Completed — signed publication and live policy verified through Cycle 9**

## Finding disposition

Every new Cycle 8 finding is scheduled below. No new finding is deferred.
Existing architecture, topology, deploy-recovery, restore-generation,
map/vector-scale, upload-memory, environment/manual-validation, and test-
infrastructure items remain bound to
`.context/plans/deferred-carry-forward.md` with their original severity,
confidence, reason, repository-policy constraints, and exit criteria. None of
those exit criteria fired in Cycle 8.

## Work packages

- [x] **C8-01 container-capped responsive source policy (Medium/High):**
  replace viewport-only `vw` hints in `responsive-masonry.ts` with a
  server-emittable policy derived from Tailwind's default container caps,
  accumulated public-layout padding, the 16 px column gap, and each grid's
  effective columns. Cover all Tailwind container-width transitions, including
  the `lg` and `2xl` transitions where column count may stay constant but the
  capped content box changes. Keep the live `ResizeObserver` as the intrinsic-
  geometry authority; do not wait for hydration to correct an image request
  that may already have started.
- [x] **C8-01 main/archive/shared parity (Medium/High):** reuse the source-slot
  helper for `getMainMasonrySizes`, `ARCHIVE_MASONRY_SIZES`, and the nested
  shared-group container variant. Main/archive account for one `px-4` public
  container (32 px total horizontal padding); shared groups account for the
  page's nested `container px-4` inside the public container (64 px total).
  Preserve current breakpoint column policies and item-count capping.
- [x] **C8-02 deterministic unit coverage (Medium/High):** update responsive-
  masonry unit expectations to the capped slot strings and add boundary cases
  for one-to-five effective columns, the `lg` container transition, top-cap
  main/archive slots, and nested shared slots. Assert the source policy instead
  of preserving the removed viewport fractions.
- [x] **C8-02 deterministic E2E fixture (Medium/High):** extend the disposable
  E2E seed with a third gallery image and a tag shared only by the original two
  photos. Keep existing two-item geometry tests on the filtered route while
  exposing an unfiltered three-item main route that crosses the ultrawide
  640w/1536w boundary. Keep cleanup filename containment aligned with the new
  owned fixture name.
- [x] **C8-02 browser candidate regressions (Medium/High):** retain existing
  320/1,536/2,560 two-item geometry proofs, then add exact post-cap cases for
  (a) three-item main at 2,560 px/DPR 1, (b) archive at 2,560 px/DPR 2, and
  (c) shared group above 2,560 px at DPR 1 or the independently proven DPR
  boundary. Assert real card/grid width, rendered columns, emitted `sizes`, and
  `_640` `currentSrc`; these cases must fail against the current `vw` policy.
- [x] **C8-03 component contract (Low/High):** update `MasonryCard` comments to
  name the observed container-width bucket and the true memo invariant:
  unchanged bucket observations/unrelated parent state bail out, while a new
  numeric `estimatedCardWidth` intentionally re-renders existing cards.
- [x] **C8-04 Cycle 7 release ledger (Low/High):** qualify Cycle 7's 2,560 px
  evidence as intrinsic-geometry coverage rather than source-candidate proof;
  record GPG-good signatures, `master == origin/master == ff8c5f48`, and live
  container-geometry evidence without inventing an exact deployed SHA; archive
  Cycle 7 and advance the plan index.
- [x] Run every configured blocking gate against the whole repository. Browser
  fixture and behavior changed, so full Playwright is required.
- [x] Record focused/full gate evidence and `GATE_FIXES` here.
- [x] Commit coherent work in fine-grained GPG-signed gitmoji Conventional
  Commits, `git pull --rebase`, push `master`, and run the selected per-cycle
  `npm run deploy` exactly once after every gate is green. Cycle 9 verified all
  three Cycle 8 commits as GPG-good, `master == origin/master == f50e96b3`, the
  prior cycle report recorded `per-cycle-success`, and production exposes the
  corrected `_640` ultrawide selection policy. Exact deployed SHA is not
  claimed from behavioral evidence.

## Deferred finding register

No Cycle 8 finding is deferred. This cycle does not alter, downgrade, or
reclassify any carry-forward row. All eventual carry-forward work remains
subject to `AGENTS.md` signed-commit, pull/rebase, gate, and deploy policies.

## Acceptance criteria

- Main/archive source hints stop growing after each Tailwind container cap and
  describe the actual padded/gapped slot for one-to-five columns.
- Shared-group hints model both public and nested page padding without changing
  its md/lg/xl column transitions.
- At 2,560 px/DPR 1, an unfiltered three-photo main gallery renders three
  approximately 491 px cards and selects the 640w derivative.
- At 2,560 px/DPR 2, a five-column archive card renders approximately 288 px
  wide and selects the 640w derivative rather than 1536w.
- A sufficiently wide shared-group case selects 640w for its approximately
  356 px nested-container card at the chosen DPR boundary.
- Existing two-photo intrinsic geometry, item-capped columns, fallback behavior,
  and priority scheduling remain unchanged.
- Cycle 7 is archived with truthful signed-push/live-policy evidence and no
  invented production SHA.
- Every required gate is green without a new suppression.

## Progress

- [x] Eleven-perspective review coverage complete.
- [x] Aggregate review complete.
- [x] Every new finding scheduled; no new deferral.
- [x] Source implementation complete.
- [x] Focused tests complete.
- [x] Full required gates green.
- [x] Signed commits pushed.
- [x] Per-cycle deploy completed and the corrected source policy was observed
  live; exact deployed SHA is unavailable and is not claimed.

## Validation evidence

- Review baseline: ESLint, API-auth lint, action-origin/mutation-barrier lint,
  public-route-rate-limit lint, production dependency audit, 34/34 focused
  responsive tests, and `git diff --check` passed at `ff8c5f48`.
- Independent Chromium selection proofs reproduced main/archive/shared
  post-container-cap candidate mismatches.
- Live production designer evidence reproduced 288 px main and Timeline cards
  selecting `_1536.avif` at 2,560 px/DPR 2 while the same Timeline geometry
  selected `_640.avif` at 1,536 px/DPR 2.
- Focused Vitest passed 2 files / 35 tests after the implementation.
- Focused Playwright passed 12/12 cases against a disposable MySQL 8.4
  database, including the three new candidate-crossing main/archive/shared
  boundaries. The generated production build also passed during server setup.
- ESLint, API-auth lint, action-origin/mutation-barrier lint, public-route-rate-
  limit lint, app/script typecheck, and the production Next.js build passed.
- Production dependency audit reported zero vulnerabilities.
- Full Vitest passed 362 files with 2 expected skips: 3,434 tests passed and 4
  expected CLIP tests skipped.
- Full Playwright passed 62 tests with 2 expected local/CI configuration checks
  skipped. The explicitly named disposable MySQL container was stopped and
  auto-removed afterward; no production or repository data was touched.
- The local server logged non-failing `ResponseAborted` lines when browser
  navigations cancelled in-flight responses. Playwright reported no warning or
  failure; these are request-cancellation telemetry, not a suppressed gate.
- No pre-existing error/warning-level gate issue required a product fix in
  this cycle (`GATE_FIXES=0`).
- Cycle 9 terminal reconciliation verified good GPG signatures for
  `b3e299f1`, `d2a90c3c`, and `f50e96b3`, plus local/remote equality at
  `f50e96b3`.
- Cycle 8's terminal report recorded `DEPLOY: per-cycle-success`; Cycle 9's
  live browser pass independently observed 288 px ultrawide cards selecting
  `_640.avif`, the shipped behavior. This proves the policy is active without
  inventing an exact production commit identifier.
