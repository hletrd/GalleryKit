# Cycle 5/100 Implementation Plan - 2026-06-29

Status: DONE
Review source: `.context/reviews/_aggregate.md`

Completed: 2026-06-29

## Completion Evidence

- Fix commits: `5df06fce`, `fba6f622`, `252b1008`, `89678b78`, `55cc289b`, `714f1b38`, `5114d758`, `dba54859`, `a5d87a1f`.
- Full gates passed:
  - `npm run lint --workspace=apps/web`
  - `npm run lint:api-auth --workspace=apps/web`
  - `npm run lint:action-origin --workspace=apps/web`
  - `npm run lint:public-route-rate-limit --workspace=apps/web`
  - `npm run typecheck --workspace=apps/web`
  - `npm run build --workspace=apps/web`
  - `npm test --workspace=apps/web`
- Deploy status is recorded in the cycle report after the required per-cycle deploy.

## Scope

This plan schedules the cycle 5 findings that are correctness, data-loss, production-packaging, privacy/trust, or small test/documentation fixes. Deferred performance/large-architecture/test-strategy items are recorded separately in `cycle-5-2026-06-29-deferred.md` with severity preserved and exit criteria.

## Workstream A - Deploy and Runtime Storage Boundary

### A1. Package immutable public assets in the Docker runner

- Finding: AGG-C5-01
- Severity/confidence: High / High
- Files: `apps/web/Dockerfile`, `apps/web/src/__tests__/deploy-script-contract.test.ts`
- Plan:
  - Copy `/app/apps/web/public` from the builder into `./apps/web/public` in the runner image.
  - Keep runtime `public/uploads` and `public/resources` bind mounts.
  - Update the deploy contract test to assert the public copy exists and that narrow mounts remain narrow.
- Validation: targeted deploy contract test, full gates.
- Status: DONE (`5df06fce`; targeted deploy contract test; full gates)

### A2. Exclude runtime topic resources from Docker build contexts

- Finding: AGG-C5-02
- Severity/confidence: Medium / High
- Files: `.dockerignore`, `apps/web/.dockerignore`, `apps/web/src/__tests__/deploy-script-contract.test.ts`
- Plan:
  - Add `apps/web/public/resources` and `apps/web/public/resources/**` to root `.dockerignore`.
  - Add `public/resources` and `public/resources/**` to app `.dockerignore`.
  - Test both ignore files for uploads and resources exclusions.
- Validation: targeted deploy contract test, full gates.
- Status: DONE (`5df06fce`; targeted deploy contract test; full gates)

## Workstream B - Data-Loss and Restore Correctness

### B1. Make legacy original migration conflict-safe

- Finding: AGG-C5-03
- Severity/confidence: High / High
- Files: `apps/web/scripts/migrate.js`, `apps/web/src/__tests__/upload-paths.test.ts`
- Plan:
  - Add file identity comparison using size plus SHA-256.
  - When the private target exists, delete the legacy source only if bytes match.
  - On divergent conflict, fail startup with an actionable message and leave both files intact.
  - Keep `EXDEV` copy fallback safe by verifying the copied target before unlinking source.
  - Add tests for identical conflict, divergent conflict, and cross-device copy behavior.
- Validation: targeted upload-path/migrate tests, full gates.
- Status: DONE (`fba6f622`; targeted legacy-original migration tests; full gates)

### B2. Quiesce color-pipeline backfill during DB restore

- Finding: AGG-C5-07
- Severity/confidence: High / High
- Files: `apps/web/src/app/[locale]/admin/db-actions.ts`, restore/backfill tests
- Plan:
  - Reuse the existing color-pipeline backfill advisory lock constant/path where possible.
  - Acquire the backfill lock during restore maintenance before SQL import and release it after restore/migration work.
  - Add a source/contract test proving restore includes the backfill lock.
- Validation: targeted restore tests, full gates.
- Status: DONE (`252b1008`; restore lock/DB restore tests; full gates)

### B3. Run migration/reconcile after DB restore before resuming app work

- Finding: AGG-C5-08
- Severity/confidence: High / High
- Files: `apps/web/scripts/migrate.js`, `apps/web/src/app/[locale]/admin/db-actions.ts`, script/typecheck tests
- Plan:
  - Expose a safe script entry point or command invocation so restore can run the same committed migration/reconcile postconditions inside maintenance.
  - Run it after successful SQL import and before ending maintenance/revalidating.
  - Keep error handling fail-closed so restore does not resume on a stale schema.
- Validation: targeted restore source tests, typecheck scripts, full gates.
- Status: DONE (`252b1008`; restore lock/DB restore tests; full gates)

### B4. Make DB-only restore scope explicit

- Finding: AGG-C5-09
- Severity/confidence: Medium / High
- Files: admin DB page/messages/docs
- Plan:
  - Update visible copy and docs to say DB backup/restore covers database rows only and does not snapshot derivative/original/resource files.
  - Include operator guidance that file reconciliation or full host backups are required for complete rollback.
- Validation: i18n parity, targeted DB/admin tests, full gates.
- Status: DONE (`5114d758`; i18n parity and typecheck; full gates)

## Workstream C - Public Cache and Rate-Limit Correctness

### C1. Prevent offline cache of revocable share pages

- Finding: AGG-C5-04
- Severity/confidence: Medium / High
- Files: `apps/web/public/sw.template.js`, generated `apps/web/public/sw.js`, SW contract tests, `CLAUDE.md`
- Plan:
  - Exclude locale and non-locale `/s/<key>` and `/g/<key>` HTML routes from offline HTML caching.
  - Regenerate `sw.js`.
  - Add tests proving share pages are not routed through `networkFirstHtml`.
  - Update SW offline-cache documentation.
- Validation: SW contract tests, full gates.
- Status: DONE (`89678b78`, `5114d758`, `a5d87a1f`; SW contract tests; full gates)

### C2. Charge or avoid disabled semantic search body work

- Finding: AGG-C5-05
- Severity/confidence: Medium / High
- Files: `apps/web/src/app/api/search/semantic/route.ts`, `apps/web/src/__tests__/semantic-search-route.test.ts`
- Plan:
  - Move semantic-mode resolution before request body materialization when possible.
  - Return disabled-mode 503 before `request.text()`.
  - Keep cheap same-origin/maintenance/content-type guards first.
  - Add a test that disabled mode does not read the request body.
- Validation: semantic route tests, public route rate-limit lint, full gates.
- Status: DONE (`55cc289b`, `dba54859`; semantic route tests; lint clean; full gates)

### C3. Settle Lightroom upload quota on topic lookup failures

- Finding: AGG-C5-06
- Severity/confidence: Medium / High
- Files: `apps/web/src/app/api/admin/lr/upload/route.ts`, LR upload tests
- Plan:
  - Wrap topic lookup after upload preclaim in `try/catch`.
  - Call the existing settlement helper on thrown lookup errors before returning/throwing.
  - Add a regression test with a mocked topic lookup failure after preclaim.
- Validation: LR upload test, full gates.
- Status: DONE (`714f1b38`; LR upload source-contract tests; full gates)

## Workstream D - Semantic Search Test and Docs Alignment

### D1. Add active model-version filter regression coverage

- Finding: AGG-C5-15
- Severity/confidence: Medium / High
- Files: `apps/web/src/__tests__/semantic-search-route.test.ts`
- Plan:
  - Add tests that stub mode ignores production-version embedding rows and production mode ignores stub-version rows.
  - Keep production mode operator-gated in tests.
- Validation: semantic route tests, full gates.
- Status: DONE (`55cc289b`; semantic route source-contract coverage; full gates)

### D2. Fix stale semantic-search comments

- Findings: AGG-C5-18, AGG-C5-19
- Severity/confidence: Low / High
- Files: `apps/web/src/__tests__/semantic-search-route.test.ts`, `apps/web/src/app/actions/embeddings.ts`
- Plan:
  - Update comments to describe current stub and gated-production serving behavior.
  - Update embeddings action header to describe disabled/stub/production behavior and active model-version writes.
- Validation: lint/typecheck, full gates.
- Status: DONE (`55cc289b`; semantic route tests and typecheck; full gates)

## Workstream E - UI/i18n/Product Copy Corrections

### E1. Localize analytics preview links and new-window labels

- Finding: AGG-C5-20
- Severity/confidence: Low / High
- Files: `analytics-client.tsx`, admin analytics page wiring, `messages/en.json`, `messages/ko.json`, tests if present
- Plan:
  - Pass locale into `AnalyticsClient`.
  - Use localized `/p/<id>` and `/g/<key>` paths.
  - Add localized `opensInNewWindow` copy for aria-labels.
  - Format view counts with locale while touching the component.
- Validation: i18n parity, relevant UI tests, full gates.
- Status: DONE (`5114d758`; i18n parity and typecheck; full gates)

### E2. Correct GPS privacy copy

- Finding: AGG-C5-21
- Severity/confidence: Medium / High
- Files: `apps/web/messages/en.json`, `apps/web/messages/ko.json`
- Plan:
  - Explain that new uploads omit GPS from gallery metadata and, when possible, remove GPS metadata from retained originals.
  - State existing images are not changed.
- Validation: i18n parity, full gates.
- Status: DONE (`5114d758`; i18n parity and full gates)

### E3. Correct Auto Alt-Text copy while it is an EXIF-derived stub

- Finding: AGG-C5-22
- Severity/confidence: Medium / High
- Files: `apps/web/messages/en.json`, `apps/web/messages/ko.json`, possibly labels using "AI-suggested"
- Plan:
  - Reword settings copy to current behavior: EXIF-derived placeholders, no local Florence-2 runtime yet.
  - Avoid “AI” wording for this setting until real inference ships.
- Validation: i18n parity, full gates.
- Status: DONE (`5114d758`; i18n parity and full gates)

## Completion Criteria

- All TODO items above are marked DONE with evidence. DONE.
- Deferred file records every non-implemented aggregate finding with original severity/confidence and exit criteria. DONE in `cycle-5-2026-06-29-deferred.md`.
- Full configured gates pass. DONE:
  - `npm run lint --workspace=apps/web`
  - `npm run lint:api-auth --workspace=apps/web`
  - `npm run lint:action-origin --workspace=apps/web`
  - `npm run lint:public-route-rate-limit --workspace=apps/web`
  - `npm run typecheck --workspace=apps/web`
  - `npm run build --workspace=apps/web`
  - `npm test --workspace=apps/web`
- Changes are committed with GPG-signed conventional gitmoji commits, pushed, then `npm run deploy` succeeds from repo root.
