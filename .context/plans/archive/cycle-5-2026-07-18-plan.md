# Cycle 5/100 Implementation Plan — 2026-07-18

Start HEAD: `4926a3e4`
Source: `.context/reviews/_aggregate.md` and all Cycle 5 provenance reviews
Status: **Completed, signed, pushed, deployed, and archived**

## Finding disposition

Every new Cycle 5 finding is scheduled below. No new finding is deferred.
Existing architecture, topology, deploy-recovery, restore-generation,
map/vector-scale, and environment-validation items remain bound to
`.context/plans/deferred-carry-forward.md` with their original severity,
confidence, reason, repository-policy constraints, and exit criteria. None of
those exit criteria fired in Cycle 5.

## Work packages

- [x] **C5-01 responsive breakpoint alignment (Medium/High):** introduce a
  client-safe responsive masonry-size policy with descending inclusive
  `min-width` ranges matching Tailwind's `sm=640`, `md=768`, `lg=1024`,
  `xl=1280`, and `2xl=1536` behavior. Reuse the main/archive policy in
  `MasonryCard`, timeline, and year AVIF/WebP/JPEG/fallback paths, and use a
  distinct 1/2/3/4-column policy for shared groups.
- [x] **C5-02 item-count-aware main-gallery sizes (Medium/High):** derive the
  main-gallery source-size string from the same one-through-five column caps
  as `HomeClient`, pass the stable derived value to each `MasonryCard`, and
  test one-, few-, and many-photo outputs so a small desktop gallery cannot
  advertise a slot narrower than its actual column.
- [x] **C5-03 independent scheduling assertions (Medium/High):** strengthen
  `apps/web/e2e/masonry-priority.spec.ts` to assert eager and high-priority
  indices separately and prove every non-first card remains lazy with
  auto/absent fetch priority. Preserve top-edge geometry, no-media-preload,
  and emitted-request assertions.
- [x] **C5-01 browser boundary proof (Medium/High):** extend browser coverage
  at high DPR across the exact main breakpoints and the shared grid's
  1024/1280 transitions. Assert the rendered column count, advertised source
  size policy, and selected candidate class where deterministic fixture
  derivatives permit it.
- [x] **C5-04 Cycle 4 release ledger (Low/High):** record the good-signature
  commits and `master == origin/master == 4926a3e4`, mark implementation and
  signed push complete, record observable production verification separately
  from unavailable exact deploy-SHA proof, archive Cycle 4, and make Cycle 5
  the sole active current-cycle plan.
- [x] Run every configured blocking gate against the whole repository.
  Browser-flow coverage changes, so full Playwright is required.
- [x] Record focused/full gate evidence and the gate-fix count here.
- [x] Commit coherent work in fine-grained GPG-signed gitmoji Conventional
  Commits, `git pull --rebase`, push `master`, and run the selected per-cycle
  `npm run deploy` exactly once after every gate is green.

## Deferred finding register

No Cycle 5 finding is deferred. This cycle does not alter or reclassify any
carry-forward row.

## Acceptance criteria

- At every responsive boundary, the browser's column layout and source-size
  hint enter the same column-count policy on the same CSS pixel.
- Shared groups advertise 100/50/33/25vw for their 1/2/3/4-column layout,
  including the 1024 and 1280 px transitions.
- A one-photo main gallery advertises 100vw at every width; two-to-four-photo
  galleries never advertise more columns than they render; five-plus photos
  use the full 1/2/3/4/5 policy.
- A non-first eager-only or high-only regression fails the masonry E2E.
- Cycle 4 is archived with truthful signed-push and live-verification evidence,
  without claiming an exact production SHA that the system cannot expose.
- Every required gate is green without new suppression.

## Progress

- [x] Eleven-perspective review coverage complete.
- [x] Aggregate review complete.
- [x] Every new finding scheduled; no new deferral.
- [x] Implementation complete.
- [x] Full required gates green.
- [x] Signed commits pushed.
- [x] Per-cycle deploy complete.

## Validation evidence

- Focused Vitest: responsive-masonry and MasonryCard memo contracts passed
  2 files / 16 tests.
- Focused Playwright against a disposable MySQL 8.4 `gallerykit_e2e` database:
  priority and responsive-boundary specs passed 8/8 tests. The first attempt
  correctly exposed an over-specific new-test expectation at 1024 px: 33vw at
  DPR 2 exceeds the seeded 640w candidate, so the coarse ladder selects 1536w.
  The expectation was corrected while retaining the independently proven
  three-column layout and aligned source-size policy.
- ESLint, API-auth lint, action-origin/mutation-barrier lint, and public-route-
  rate-limit lint passed with no warnings or suppressions.
- Full typecheck passed for the app/tests and JavaScript/TypeScript scripts.
- Production build passed under Next.js 16.2.10.
- Full Vitest: 362 files passed, 2 skipped; 3,415 tests passed, 4 expected CLIP
  skips.
- Production dependency audit: zero vulnerabilities.
- Full Playwright: 56 passed, 2 expected environment checks skipped. This
  includes exact DPR-2 archive boundaries at 640/768/1280/1536 px, shared-group
  boundaries at 1024/1280 px, and independent eager/high negative-state proof.
- The named disposable MySQL container was removed after browser validation.
- No pre-existing error/warning-level gate issue required a product fix in this
  cycle (`GATE_FIXES=0`).
- `git diff --check`: passed on the final pre-commit tree.

## Terminal release reconciliation (Cycle 6)

- `baec70b5`, `45a9417f`, and `6e4c25c8` carry good GPG signatures.
- `master == origin/master == 6e4c25c8` at the Cycle 6 review boundary.
- Cycle 5's terminal report recorded `DEPLOY: per-cycle-success`, and the
  production browser served the new responsive-size policy. The deployment
  surface does not expose an exact Git SHA, so this records observable live
  behavior without claiming cryptographic deploy identity.
- Cycle 6 archives this plan after reconciling the signed remote and live
  policy evidence.
