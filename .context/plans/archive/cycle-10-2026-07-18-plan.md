# Cycle 10/100 Implementation Plan — 2026-07-18

Start HEAD: `1e3646e3`
Source: `.context/reviews/_aggregate.md` and all Cycle 10 provenance reviews
Status: **Complete — implementation, gates, signed publication, and per-cycle deploy succeeded**

## Finding disposition

Every unique Cycle 10 finding is scheduled below. No finding is deferred.
COR-C10-01 is a confirmed correctness defect and is therefore non-deferrable
under the loop policy. DOC-C10-02 and MAINT-C10-03 are bounded current findings
with immediately actionable fixes. Existing deferred architecture, topology,
operator, UI, and test-infrastructure items remain governed by
`.context/plans/deferred-carry-forward.md` with their original severity,
confidence, reasons, repository-policy constraints, and exit criteria.

## Work packages

- [x] **C10-01 persist truthful derivative width (COR-C10-01,
  Medium/High):** add nullable public-safe `images.derivative_max_width`, a
  monotonic journal migration, Drizzle schema mirror, and
  `reconcileLegacySchema` mirror. Return the effective `processingBaseWidth`
  from `processImageFormats` and persist it on successful fresh upload,
  queue processing, in-app backfill, and sidecar backfill, including the
  detection-failure derivative-only branches whose bytes were still replaced.
- [x] **C10-01 legacy-safe projection:** for legacy non-WI-15 rows whose new
  column is null, project original `width` as the truthful maximum because
  `was_downscaled=false`; keep the maximum null for legacy
  `was_downscaled=true` rows whose historical processing cap cannot be
  reconstructed safely. Do this as a read projection rather than DML so the
  migration remains baseline-safe under the repository's DML guard.
- [x] **C10-01 standards-correct source builder:** extend the shared image URL
  helper to accept delivered maximum width, map each configured alias URL to
  `min(alias, deliveredMaxWidth)`, and deduplicate repeated actual widths while
  retaining the first alias that owns the capped render. Never emit a `w`
  descriptor above the bytes' delivered width.
- [x] **C10-01 all-consumer wiring:** use the truthful candidates in main
  masonry, timeline, year, shared group, and photo viewer. For unresolved
  legacy WI-15 rows, omit sized `<source>` ladders and use the base JPEG
  fallback rather than publishing false width metadata. Preserve CDN URL
  resolution, format priority, loading/fetch priority, responsive `sizes`, and
  error fallback behavior.
- [x] **C10-01 pipeline/version and operational honesty:** bump
  `IMAGE_PIPELINE_VERSION` so existing WI-15 rows become explicit re-encode
  candidates, update version documentation/comments, regenerate the shipped
  service worker through the normal build script, and document that old
  downscaled rows remain on the safe base-JPEG path until re-encoded.
- [x] **C10-01 behavior coverage:** add unit tests for below-first,
  between-sizes, above-largest, null, and dedupe cases; add producer/persistence
  contract tests for upload, queue, and both backfill paths; update privacy and
  migration parity fixtures. Replace the suffix-only ultrawide Playwright
  assertion with decoded-resource-width and truthful-descriptor assertions.
- [x] **C10-02 Cycle 9 release ledger (DOC-C10-02, Low/High):** record good
  signatures for `7f6fb05e`, `819f5432`, and `1e3646e3`, exact local/remote
  equality, and observable live ladder behavior without inventing a deployed
  SHA; archive Cycle 9 and advance the active plan index. Completed during
  Prompt 2's required archival step.
- [x] **C10-03 remove expired alias (MAINT-C10-03, Low/High):** delete the
  unused `getGalleryConfigUncached` export and its test import/assertion, and
  update the accessor documentation to describe the completed rename while
  preserving TTL, invalidation generation, and promise ownership behavior.
- [x] Run every configured blocking gate against the whole repository:
  ESLint, API-auth lint, action-origin/mutation-barrier lint, public-route-rate-
  limit lint, app/script typecheck, production build, full Vitest, production
  dependency audit, and full Playwright because browser-flow coverage changes.
- [x] Record focused/full gate evidence and `GATE_FIXES` here. Fix every
  error-level issue at root cause; record any unavoidable warning under the
  strict deferred rules rather than suppressing it.
- [x] Commit coherent work in fine-grained GPG-signed gitmoji Conventional
  Commits, `git pull --rebase`, push `master`, and run `npm run deploy` exactly
  once after all gates are green.

## Deferred finding register

No Cycle 10 finding is deferred. This plan neither downgrades nor reclassifies
any open carry-forward row. Eventual carry-forward work remains subject to
`AGENTS.md` signed-commit, pull/rebase, full-gate, and deploy policies.

## Acceptance criteria

- Every emitted responsive `w` descriptor equals the intrinsic width of its
  corresponding encoded resource and repeated capped aliases collapse to one
  candidate.
- A 1200 px source under the default ladder emits `_640 640w` plus one capped
  alias at `1200w`; it never claims 1536–7680 delivered pixels.
- WI-15-downscaled uploads and re-encodes persist the actual processing width,
  while unresolved legacy downscaled rows publish no guessed descriptors.
- All five responsive consumers use the same truthful candidate contract and
  retain existing CDN, format, scheduling, layout, and fallback behavior.
- The ultrawide DPR-2 browser proof inspects decoded pixels and reports a
  source-limited result honestly rather than treating a filename suffix as
  physical resolution.
- Migration journal, schema, reconcile, privacy projection, and all processing
  persistence paths remain symmetric.
- The expired detached-config alias is absent with generation/ownership tests
  still green.
- Cycle 9 is archived with truthful signed-push and observable-deploy evidence.
- Every required gate passes without a new suppression.

## Progress

- [x] Fourteen-perspective review coverage complete across two bundled child
  agents plus lead aggregation (runtime thread limit documented in aggregate).
- [x] Aggregate review complete.
- [x] Every new finding scheduled; no new deferral.
- [x] Cycle 9 plan reconciled and archived.
- [x] Source implementation complete.
- [x] Focused tests complete.
- [x] Full required gates green.
- [x] Signed commits pushed.
- [x] Per-cycle deploy complete.

## Validation evidence

- Prompt 1 focused validation passed 4 files / 52 tests plus full app/script
  typecheck; the product reviewer independently passed 3 files / 28 tests.
- Direct metadata inspection established that the 1200 px square fixture's
  `_1536`, `_2048`, `_4096`, `_5120`, and `_7680` derivatives are all 1200 px
  wide across AVIF, WebP, and JPEG.
- Live browser review covered desktop/mobile, EN/KO, themes, keyboard/focus,
  offline fallback, and 320 px reflow; no unrelated UI defect was confirmed.
- Focused implementation validation passed full app/script typecheck and 13
  files / 159 tests covering truthful candidates, privacy, migration/reconcile,
  queue, both backfills, detection-failure persistence, and alias removal.
- Full gates passed: ESLint, API-auth lint, action-origin/mutation-barrier lint,
  public-route-rate-limit lint, app/script typecheck, production build, and
  production dependency audit with zero vulnerabilities.
- Full Vitest passed 363 files with 2 expected skips: 3,447 tests passed and 4
  expected CLIP tests skipped.
- Full Playwright passed 56 tests with 9 expected opt-in admin/configuration
  skips against disposable database `gallerykit_e2e_cycle10`. The updated
  ultrawide case decoded the selected resource at 1200 px and verified the
  final source descriptor is `1200w`, with no duplicate oversized aliases.
  The named MySQL container was stopped and auto-removed afterward.
- The first Playwright launch failed before tests because no local MySQL was
  listening. A first disposable run auto-enabled optional admin tests and hit
  existing admin-login fixture drift; the required public/browser-flow suite
  was rerun with the explicit supported admin opt-out and passed. No production
  or repository data was used.
- Two implementation-induced error-level focused-test diagnostics were fixed
  at root cause by updating the public-safe projection allowlist and canonical
  sidecar persistence-column fixture (`GATE_FIXES=2`). No suppression was added.
- Signed implementation/review commits `1ff3b7ff`, `7f5ae70a`, and `e1868a41`
  verify with good GPG signatures and were pushed after a clean
  `git pull --rebase`. The terminal ledger sync commit records that publication
  state.
- The Cycle 10 orchestrator report recorded `DEPLOY: per-cycle-success`. The
  exact deployed SHA was not independently exposed, so this ledger records the
  command result without inventing one. Cycle 11 archived the completed plan.
