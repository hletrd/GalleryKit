# Cycle 3/100 Implementation Plan — 2026-07-18

Start HEAD: `afa11cf4`
Source: `.context/reviews/_aggregate.md` and all eleven Cycle 3 provenance reviews
Status: **Complete — signed, pushed, and deployed**

## Finding disposition

All five new aggregate findings are scheduled below. No new finding is
deferred. Existing non-new architecture, topology, deploy-recovery,
restore-generation, map/vector scale, and broad validation items remain bound
to `.context/plans/deferred-carry-forward.md` with their original
severity/confidence, reasons, repository policy, and exit criteria; none of
their exit criteria fired in this review.

## Work packages

- [x] **C3-01 closed mobile tag disclosure (High/High):** make the mobile tag
  group explicitly `display:none` while `<details>` is closed and restore flex
  layout only in the open state. Add mobile Playwright coverage proving the
  closed group has no rendered box/hit target, opening exposes the controls and
  moves the gallery below them, and closing restores the compact flow.
- [x] **C3-02/C3-04 masonry priority ownership and browser proof
  (Medium/High):** remove the invalid DOM-first-N = visual-first-row inference
  and media preloads from the home gallery; keep explicit eager/high priority
  only on the universally visible first card. Apply the same safe policy to
  shared-group, timeline, and year masonry surfaces. Replace source-string-only
  confidence with browser geometry/attribute coverage at mobile and desktop
  widths so explicit priority can never target a below-fold CSS-column card.
  Correct the prior Cycle 2 evidence record to distinguish manual validation
  from committed coverage.
- [x] **C3-03 mobile navigation focus flow (Medium/High):** when keyboard
  activation expands the topic disclosure, deliberately focus the first
  revealed link; keep pointer activation stable. Support Escape from the
  expanded nav, collapse it, and restore focus to the toggle. Add mobile
  Playwright coverage for keyboard open, traversal of revealed links, Escape,
  and focus restoration; update the incomplete source contract.
- [x] **C3-05 prior release ledger (Low/High):** record Cycle 2's signed pushed
  frontier and live production evidence, correct the unsupported automated
  request-timeline claim, mark it complete, archive its plan, and make Cycle 3
  the current plan.
- [x] Run all configured blocking gates against the whole repository. Browser
  behavior changes make `npm run test:e2e --workspace=apps/web` mandatory.
- [x] Update this plan with focused/full gate evidence.
- [x] Commit each work item in fine-grained GPG-signed gitmoji Conventional
  Commits, pull --rebase, push
  `master`, and run the selected per-cycle `npm run deploy` exactly once after
  every gate is green.

## Deferred finding register

No Cycle 3 finding is deferred. Carry-forward items are not reclassified or
silently dropped; their authoritative detail remains in
`.context/plans/deferred-carry-forward.md`.

## Progress

- [x] Eleven-perspective review fan-out complete.
- [x] Aggregate review complete.
- [x] Every new finding scheduled.
- [x] Fully implemented Cycle 2 plan archived.
- [x] Source and browser-test implementation complete.
- [x] All configured gates green.
- [x] Signed commits pushed.
- [x] Per-cycle deploy complete.

## Validation evidence

- Focused Vitest: 3 files / 42 tests passed. The first focused run exposed one
  stale assertion that still expected column-count changes to grant priority to
  later DOM cards; the corrected regression now proves the opposite
  (`GATE_FIXES=1`).
- ESLint, API-auth lint, action-origin lint, and public-route-rate-limit lint:
  passed.
- Typecheck (app tests included + scripts): passed.
- Production build: passed under Next.js 16.2.10.
- Full Vitest: 361 files passed, 2 skipped; 3,411 tests passed, 4 expected CLIP
  skips.
- Production dependency audit: zero vulnerabilities.
- Full Playwright: the first launch stopped before tests because no local MySQL
  listened on 127.0.0.1:3306. A named disposable MySQL 8.4 container on
  127.0.0.1:33327 provided the repository's isolated `gallerykit_e2e` database;
  the rerun passed 50 tests with 2 expected environment checks skipped. The
  temporary container was stopped and auto-removed after the run.
- `git diff --check`: passed.

## Terminal release evidence

- `master` and `origin/master` both reached GPG-signed `01d39653`; Cycle 3's
  implementation lineage is `2d9060de`, `235e8cb8`, `d2ef7817`, `54418100`,
  and the terminal documentation cleanup `01d39653`.
- Cycle 4 production verification at `https://gallery.atik.kr/en` confirmed the
  deployed disclosure, navigation, and masonry output: the closed mobile tag
  panel has no rendered box, keyboard expansion focuses the first revealed
  topic and Escape restores the toggle, and exactly one masonry card carries
  explicit eager/high priority.
- Cycle 4 identified and schedules a separate missing geometry assertion in
  the committed masonry Playwright test; that evidence gap does not change the
  confirmed Cycle 3 production release state.
