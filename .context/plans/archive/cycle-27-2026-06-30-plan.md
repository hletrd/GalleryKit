# Cycle 27/100 Implementation Plan

Date: 2026-06-30 KST
Review source: `.context/reviews/_aggregate.md`
Status: IMPLEMENTED - validating gates

## Repo Rules Read Before Planning

Read order satisfied for deferral policy: `CLAUDE.md`, `AGENTS.md`, `.context/` review/plan files, lookup for `.cursorrules` and `CONTRIBUTING.md` (none present), and docs/policy files. Repo rules require signed Conventional Commit + gitmoji commits, pull-rebase before push, all configured gates, and per-cycle deploy with `npm run deploy` after green pushed changes.

Security, correctness, and data-loss findings are scheduled below. Manual operational risks and low-risk validation/process items are recorded in `cycle-27-2026-06-30-deferred.md` with original severity/confidence and exit criteria.

## Scheduled Fixes

### P27-01 - Harden SQL restore scanner grammar and restore-language allowlist

- Findings: `AGG-C27-01`, `AGG-C27-02`
- Original severity/confidence: High / High; Medium / Medium-High
- Files: `apps/web/src/lib/sql-restore-scan.ts`, `apps/web/src/__tests__/sql-restore-scan.test.ts`
- Plan: scan write targets across both comment-normalized forms, cover legal MySQL `INSERT`/`REPLACE` modifiers and no-`INTO` shape, reject schema-qualified identifiers anywhere in executable statements, reject `CREATE/DROP TEMPORARY TABLE`, and reject `INSERT ... SELECT` / `CREATE TABLE ... AS SELECT` cross-schema sources.
- Acceptance: regression tests fail on current HEAD and pass after the fix for comment-separated write targets, priority/no-`INTO` inserts, temp-table DDL, schema-qualified read sources, and ordinary app dump shapes.

### P27-02 - Make restore-maintenance recovery production-real in docs/runtime

- Findings: `AGG-C27-03`
- Original severity/confidence: High / High
- Files: `apps/web/scripts/restore-maintenance-recovery.*`, `apps/web/package.json`, `apps/web/Dockerfile`, `CLAUDE.md`, focused source-contract tests
- Plan: add a production-runnable recovery CLI script copied into the Docker runner and make the package command use `node`, not dev-only `tsx`. Update runbook language to state that an external CLI clears durable marker state and requires restarting the running web process unless an in-process recovery path is later added.
- Acceptance: source/tests prove the runner includes the recovery script and docs no longer claim external clear mutates a separate live process.

### P27-03 - Normalize private original permissions during legacy migration

- Findings: `AGG-C27-04`
- Original severity/confidence: Medium / High
- Files: `apps/web/scripts/migrate.js`, `apps/web/src/__tests__/migrate-legacy-originals.test.ts`
- Plan: set private original root permissions where supported and chmod every migrated target to `0600` after rename/copy.
- Acceptance: test creates a permissive legacy file and asserts the migrated private file is not group/world-readable.

### P27-04 - Correct backfill runbook semantics

- Findings: `AGG-C27-05`
- Original severity/confidence: Medium / High
- Files: `CLAUDE.md`, `apps/web/README.md`, optional script comments/source-contract tests
- Plan: distinguish shared safety/lock contracts from candidate selection; document in-app stale-version-only behavior, sidecar `--force-reencode` for settings-only byte changes, and fail-fast/10-second lock wait semantics.
- Acceptance: docs cannot be read as saying both entry points are candidate-equivalent or queue indefinitely.

### P27-05 - Add sharing action behavior tests

- Findings: `AGG-C27-06`
- Original severity/confidence: Medium / High
- Files: `apps/web/src/__tests__/sharing-actions.test.ts` or existing sharing tests
- Plan: add behavior-level tests for create/revoke/delete sharing action postconditions: rate-limit rollback, transaction child-row delete, audit calls, affectedRows races, and localized revalidation.
- Acceptance: tests fail if audit/revalidation/child-delete/rollback behavior is removed.

### P27-06 - Add image action audit/cache tests

- Findings: `AGG-C27-07`
- Original severity/confidence: Medium / High
- Files: `apps/web/src/__tests__/images-actions.test.ts` or dedicated metadata tests
- Plan: assert upload success audit metadata and no all-failed audit; add `updateImageMetadata` tests for sanitized success, audit, topic/share/admin/home revalidation, and no audit/revalidation on failure branches.
- Acceptance: tests fail if `image_upload`/`image_update` audit or key cache paths regress.

### P27-07 - Fix desktop nav overflow/clipping

- Findings: `AGG-C27-08`
- Original severity/confidence: Medium / High
- Files: `apps/web/src/components/nav-client.tsx`, relevant touch/focus/nav tests
- Plan: make desktop nav overflow behavior explicit. Prefer allowing the desktop nav to grow with `md:h-auto md:min-h-16 md:overflow-visible` while preserving mobile collapsed behavior.
- Acceptance: long/many topic links are not clipped on desktop and existing nav/touch/focus tests pass.

### P27-08 - Associate create-user password help text

- Findings: `AGG-C27-09`
- Original severity/confidence: Low / High
- Files: `apps/web/src/components/admin-user-manager.tsx`
- Plan: add stable help id and wire `aria-describedby` on the create-password field; include confirm-password description if useful.
- Acceptance: accessibility tree exposes the minimum-length instruction as the field description.

### P27-09 - Add targeted tests for `updateTag`

- Findings: `AGG-C27-12`
- Original severity/confidence: Low / Medium-High
- Files: `apps/web/src/__tests__/tags-actions.test.ts`
- Plan: add direct `updateTag` tests for success audit/revalidation, validation errors, missing tag, DB failure, and affected-image timestamp branch.
- Acceptance: tests fail if `tag_update` audit or `/admin/dashboard` revalidation is removed.

### P27-10 - Align SEO OG image field with accepted path semantics

- Findings: `AGG-C27-13`
- Original severity/confidence: Low / Medium
- Files: `apps/web/src/app/[locale]/admin/(protected)/seo/seo-client.tsx`
- Plan: change the OG image input from `type="url"` to `type="text"` plus `inputMode="url"` so relative same-origin paths do not look browser-invalid.
- Acceptance: server validation remains unchanged and the UI control matches copy.

## Deferred Findings

Deferred in `cycle-27-2026-06-30-deferred.md`: `AGG-C27-10`, `AGG-C27-11`, `AGG-C27-R01`, `AGG-C27-R02`, `AGG-C27-R03`, `AGG-C27-R04`, `AGG-C27-R05`, `AGG-C27-R06`, `AGG-C27-R07`, `AGG-C27-R08`, `AGG-C27-R09`.

## Prompt 3 Execution Notes

- Use Ralph-style grounding before edits: write a context snapshot under `.omx/context/`.
- Run every configured gate: `npm run lint --workspace=apps/web`, `npm run lint:api-auth --workspace=apps/web`, `npm run lint:action-origin --workspace=apps/web`, `npm run lint:public-route-rate-limit --workspace=apps/web`, `npm run typecheck --workspace=apps/web`, `npm run build --workspace=apps/web`, `npm test --workspace=apps/web`.
- Commit/push signed Conventional Commit + gitmoji commits after green gates and `git pull --rebase`.
- Per-cycle deploy command after green pushed HEAD: `npm run deploy`.

## Progress

- [x] Aggregate review written.
- [x] Plan and deferred register written.
- [x] P27-01 implemented.
- [x] P27-02 implemented.
- [x] P27-03 implemented.
- [x] P27-04 implemented.
- [x] P27-05 implemented.
- [x] P27-06 implemented.
- [x] P27-07 implemented.
- [x] P27-08 implemented.
- [x] P27-09 implemented.
- [x] P27-10 implemented.
- [x] Deferred findings preserved with original severity/confidence and exit criteria.
- [x] Focused validation green: `npm test --workspace=apps/web -- --run src/__tests__/sql-restore-scan.test.ts src/__tests__/migrate-legacy-originals.test.ts src/__tests__/cycle-26-source-contracts.test.ts`.
- [x] Focused validation green: `npm test --workspace=apps/web -- --run src/__tests__/sharing-actions.test.ts src/__tests__/images-actions.test.ts src/__tests__/tags-actions.test.ts`.
- [x] Focused validation green: `npm test --workspace=apps/web -- --run src/__tests__/touch-target-audit.test.ts src/__tests__/focus-visible-links-scan.test.ts src/__tests__/a11y-us-p15.test.ts src/__tests__/privacy-page-landmark.test.ts src/__tests__/lightbox-controls-contract.test.ts`.
- [x] `git diff --check` green after review-artifact whitespace cleanup.
- [x] Full gate list green: `npm run lint --workspace=apps/web`, `npm run lint:api-auth --workspace=apps/web`, `npm run lint:action-origin --workspace=apps/web`, `npm run lint:public-route-rate-limit --workspace=apps/web`, `npm run typecheck --workspace=apps/web`, `npm run build --workspace=apps/web`, `npm test --workspace=apps/web`.
- [x] Signed commit(s) pushed: `4a51d345`, `0e935638`, `1e8bba02`, `9cd0da3d`, `15d0845f`.
- [x] Per-cycle deploy recorded: first `npm run deploy` run failed during container recreation with a transient Docker name conflict; the single non-destructive recovery rerun of the exact command succeeded, recreated and started `gallerykit-web`, completed health wait, pruned old Docker artifacts, and ended with `Deployment Complete!`.
