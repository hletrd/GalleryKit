# Cycle 12/100 Implementation Plan — 2026-07-18

Start HEAD: `ff6532f4`
Source: `.context/reviews/_aggregate.md` and all Cycle 12 provenance reviews
Status: **IMPLEMENTED AND PUBLISHED — original deploy result unknown**

## Repository rules read before planning

- `CLAUDE.md` (all 779 lines), then `AGENTS.md`.
- `.context/plans/README.md`, the Cycle 11 plan/deferred pair, the consolidated
  carry-forward register, and all eleven Cycle 12 provenance reviews.
- Root and app README files plus `.github/workflows/quality.yml`.
- No `.cursorrules` or `CONTRIBUTING.md` exists in this checkout.

The implementation remains bound to signed gitmoji Conventional Commits,
fine-grained work items, pull/rebase before push, no verification bypasses,
every configured blocking gate, and one root `npm run deploy` after green gates.

## Finding disposition

Every Cycle 12 finding is scheduled below. None is deferred. The High schema-
upgrade validation defect is not deferrable. The Medium schema correctness,
query-shape, and executable-test gaps have bounded fixes in the same schema
surface. The Low release-ledger mismatch is direct housekeeping.

## Work packages

- [x] **WP1 — definition-aware reconcile (SCHEMA-C12-02, Medium/High):**
  extend column introspection to include `EXTRA` and
  `GENERATION_EXPRESSION`; normalize MySQL expression formatting; repair
  same-named `capture_month`, `capture_day`, and the new `capture_year` when
  type, nullability, generated/stored state, or expression drifts.
- [x] **WP1 — index-definition parity:** replace the 0032 list-only index
  helper with a material descriptor check covering ordered columns,
  uniqueness, BTREE type, visibility, direction, prefix, and expression.
  Recreate the capture-date/month-day/topic/year indexes on any mismatch.
- [x] **WP2 — sargable timeline years (PERF-C12-03, Medium/High):** add stored
  generated `capture_year` plus `(processed, capture_year)` in a monotonic,
  DDL-only 0033 migration. Mirror it in Drizzle, reconcile, internal/privacy
  omissions, symmetric privacy fixtures, migration journal, source tests, and
  `CLAUDE.md` index/schema-authoring documentation.
- [x] **WP2 — request sequencing:** make `getTimelineYears()` select the
  indexed generated key with no `YEAR(capture_date)` expression. When a valid
  explicit `?year=YYYY` exists, start its bounded image query in parallel with
  year-list/config/SEO work; preserve default-year and empty-gallery behavior.
- [x] **WP3 — real pending-upgrade convergence (SCHEMA-C12-01, High/High):**
  key the disposable upgrade fixture by the latest journal tag and fail if no
  fixture exists. Downgrade the initialized disposable schema to the exact
  0031 cursor, remove the 0032/0033 hashes, run the real production Drizzle
  migration entry point through both SQL files, assert their hashes, and
  compare the full structured schema with the current bootstrap snapshot.
- [x] **WP3 — reconcile drift matrix (SCHEMA-C12-02):** after the real upgrade
  proof, damage same-named generated-column definitions and same-column index
  visibility/shape, run reconcile, compare the full structured snapshot, and
  prove a second reconcile is idempotent. Cleanup must leave the disposable DB
  current even when an assertion fails.
- [x] **WP4 — executable capture-date semantics (TEST-C12-04, Medium/High):**
  in the guarded disposable MySQL lane, insert isolated null, cross-year,
  nonmatching, ordering-tie, and February 29 rows. Assert MySQL-generated
  year/month/day values and the exact On This Day equivalent ids/order/limit;
  separately assert the usable visible capture indexes. Roll fixture data back
  or delete it deterministically.
- [x] **WP4 — source/unit contracts:** require every future latest migration to
  have an explicit keyed upgrade fixture, require the real migration runner and
  hash assertions, pin the richer generated-column/index repair paths, and
  replace timeline `YEAR()` expectations with `capture_year` expectations.
- [x] **WP5 — truthful release ledger (DOC-C12-05, Low/High):** record Cycle
  11's good signed commits and proven `origin/master` publication, keep its
  original deploy result explicitly unknown, and schedule archival only after
  this Cycle 12 deploy supersedes the deployed source state. Advance the plan
  index without inventing a deployed SHA.
- [x] **WP6 — validation:** run all configured blocking gates
  against the whole repo, including full Playwright because timeline/browser
  sequencing changes. Run the disposable MySQL convergence command locally.
  Fix every error at root cause without suppression; record `GATE_FIXES`.
  Publication completed through four fine-grained GPG-signed gitmoji
  Conventional Commits on `origin/master`. The original deploy result is not
  inferable from Git and remains unknown; Cycle 13 owns the next superseding
  per-cycle deploy.

## Deferred finding register

No Cycle 12 finding is deferred. Existing open items remain governed by
`.context/plans/deferred-carry-forward.md`; this cycle does not change an exit
criterion unrelated to capture-date schema/query work.

## Acceptance criteria

- The disposable live gate executes the actual 0032 and 0033 SQL from a 0031
  cursor, records both hashes, and produces the same structured schema as fresh
  bootstrap.
- A new latest journal tag without an explicit upgrade fixture fails the gate.
- Reconcile repairs wrong same-named generated definitions and invisible or
  otherwise malformed capture indexes, then remains idempotent.
- MySQL itself proves generated year/month/day values and On This Day matching,
  leap-day, null exclusion, limit, and deterministic ordering.
- Timeline year discovery contains no `YEAR(capture_date)` expression and uses
  the new generated/indexed key; explicit-year photos begin without waiting for
  year discovery.
- Migration 0033 is monotonic, DDL-only, and mirrored in Drizzle, reconcile,
  privacy guards, tests, journal, and documentation.
- Cycle 11 publication facts and Cycle 12 terminal state are recorded without
  inferring deployment evidence.
- All configured gates pass, commits verify as GPG-signed, remote master equals
  local HEAD, and the required per-cycle deploy succeeds.

## Progress

- [x] Eleven-perspective review coverage complete.
- [x] Five unique findings aggregated and deduplicated.
- [x] Every finding scheduled; no deferral.
- [x] Implementation complete.
- [x] Focused and full gates green.
- [x] Signed commits pushed through `8bd8999f`.
- [ ] Original per-cycle deploy result unknown; do not infer from Git.

## Focused validation evidence

- 123 focused schema, timeline, reconcile, and privacy tests passed.
- Full app/script typecheck passed; all JavaScript operational scripts passed
  the script checker.
- ESLint, API-auth lint, action-origin/mutation-barrier lint, and public-route-
  rate-limit lint passed.
- Disposable MySQL 8.4 database `gallery_cycle12_test` passed fresh bootstrap,
  a real 0031-cursor upgrade through committed migrations 0032 and 0033 with
  both hashes recorded, exact structured-schema comparison, generated leap-day
  and null semantics, deterministic six-row On This Day behavior, forced-index
  usability, malformed same-name generated/index recovery, and second-run
  idempotence. The temporary container was stopped and removed afterward.
- `git diff --check` passed.

## Full gate evidence

- `npm run lint --workspace=apps/web`: passed.
- `npm run lint:api-auth --workspace=apps/web`: passed.
- `npm run lint:action-origin --workspace=apps/web`: passed; every mutating
  server action preserves same-origin and restore-barrier policy.
- `npm run lint:public-route-rate-limit --workspace=apps/web`: passed.
- `npm run typecheck --workspace=apps/web`: passed for app, tests, and scripts.
- `npm run build --workspace=apps/web`: Next.js 16.2.10 production build passed.
- `npm test --workspace=apps/web`: 364 files passed, 2 skipped; 3,459 tests
  passed, 4 expected CLIP skips.
- `npm run audit:prod`: passed with 0 production vulnerabilities.
- `npm run test:e2e --workspace=apps/web`: 64 passed, 2 intentional local
  CI-capability marker skips. The disposable seeded MySQL container was stopped
  and removed after the run.
- `GATE_FIXES`: 5. The post-commit `git diff --check` audit found five
  end-of-file blank-line warnings in new review/plan Markdown files; all five
  were removed in a signed follow-up without suppression. No error-level gate
  issue or other actionable warning remained.
