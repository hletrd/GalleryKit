# Cycle 17/100 Implementation Plan

Date: 2026-06-30 KST  
Status: IMPLEMENTED - gates passed, awaiting commit/deploy  
Source reviews: `.context/reviews/*.md`, aggregate `.context/reviews/_aggregate.md`

Repo rules reviewed before planning: `CLAUDE.md`, `AGENTS.md`, `.context/plans/README.md`, current `.context` review/plan history, and `docs/superpowers/**`. No `.cursorrules` or `CONTRIBUTING.md` were present. Scheduled work remains bound by repo policy: small reversible diffs, no new dependencies, GPG-signed Conventional Commits with gitmoji, `git pull --rebase` before push, full quality gates, then `npm run deploy` for this per-cycle run.

## Scheduled Fixes

| ID | Finding(s) | Severity / confidence | Implementation plan | Progress |
| --- | --- | --- | --- | --- |
| C17-P01 | AGG-C17-01 tag rename/delete freshness; AGG-C17-02 non-atomic tag link freshness | Medium / High; Medium / High | Make tag membership/name mutations and affected `images.updated_at` writes transactional. Bump affected images on tag rename/delete. Add regression tests for tag rename/delete and failed timestamp update rollback where practical. | DONE - tag rename/delete/link mutations now update affected image freshness inside transactions. |
| C17-P02 | AGG-C17-03 public route rate-limit scanner false negatives; AGG-C17-04 action-origin try/catch false negative; AGG-C17-54 TSX/JSX parser risk | Medium / High; Medium / High; Low / Medium | Add failing fixtures for local mutator/inverted helper and catch/finally exceptional writes. Harden scanner control-flow/local-helper analysis. Select parser `ScriptKind` by extension. | DONE - scanner fixtures and ScriptKind handling added. |
| C17-P03 | AGG-C17-05 GPS stripping can silently retain GPS in originals | Medium / High | Change GPS stripping to report success/failure and reject uploads when `strip_gps_on_upload` is enabled but original scrubbing cannot be guaranteed. Add source/unit coverage for strip failure handling. | DONE - strip failures now reject browser/LR uploads when GPS stripping is enabled. |
| C17-P04 | AGG-C17-06 public gallery DB failures; AGG-C17-07 route segment reservation | High UX / High; Medium / High | Stop rendering image-list query failures as a successful empty gallery; use explicit unavailable/error behavior. Add missing public reserved route segments (`c`, `privacy`, `timeline`, `year`) to topic slug/alias validation and test it. | DONE - home list failures propagate and reserved public route segments are tested. |
| C17-P05 | AGG-C17-08 proxy client-IP collapse; AGG-C17-09 stale offline photo pages | Medium / High; Medium / High | Preserve forwarded client chains through nginx while keeping app trust bounded by `TRUSTED_PROXY_HOPS`; document the topology. Add `/p/:id` to service-worker revocable HTML bypass and lock generated/template parity in tests. | DONE - nginx preserves trusted XFF chain; SW excludes public photo pages from offline HTML cache. |
| C17-P06 | AGG-C17-10 in-app settings backfill over-promise; AGG-C17-31/32 cache comment drift; AGG-C17-35/46 HDR output wording; AGG-C17-36 pipeline v7 comment; AGG-C17-44/45 semantic UI/setup copy; AGG-C17-47 upload token bearer-risk copy; AGG-C17-11 stale `withAdminAuth` comment; AGG-C17-30 semantic refund comment; AGG-C17-33/34 docs drift | Mixed High/Medium/Low, mostly High confidence | Align comments and localized copy with current behavior: settings-only re-encode requires force sidecar unless a future in-app force mode is added; route-handler settings-hash scope; one-hour upload cache; HDR source/SDR delivery wording; semantic bounded-scan/setup-specific states; bearer-token warning; actual token context API; semantic rate-limit refund policy; analytics/deploy docs matching current schema/scripts. | DONE - localized copy, comments, semantic setup codes, and docs now match current behavior. |
| C17-P07 | AGG-C17-41 timeline/year mobile titles; AGG-C17-43 RTL helper hardcoded LTR | Medium / High; Low / High | Mirror home mobile title overlays on timeline/year cards. Replace misleading RTL future-proofing with explicit supported-LTR behavior or a tested RTL locale set. | DONE - mobile archive titles render by default and locale direction uses an explicit supported RTL set with tests. |

## Deferred Findings

All remaining aggregate findings are recorded in `.context/plans/cycle-17-2026-06-30-deferred.md` with original severity/confidence, reason, and exit criterion.

## Required Gates

- `npm run lint --workspace=apps/web`
- `npm run lint:api-auth --workspace=apps/web`
- `npm run lint:action-origin --workspace=apps/web`
- `npm run lint:public-route-rate-limit --workspace=apps/web`
- `npm run typecheck --workspace=apps/web`
- `npm run build --workspace=apps/web`
- `npm test --workspace=apps/web`

Gate status: PASSED. Focused regression slice also passed: 14 files / 287 tests.

Gate warning recorded: `npm run build --workspace=apps/web` logged `[sitemap] falling back to homepage-only sitemap` because this local cycle machine has no MySQL listener at `127.0.0.1:3306`. Severity/confidence: Low / High. Reason for deferral: environment-local warning from an intentional sitemap fallback; production deploy host supplies the DB for runtime/deploy behavior. Exit criterion: re-open if the deploy host emits the same warning with a healthy DB, or if the sitemap fallback stops being an accepted build-time degradation.

## Deployment

Deploy mode: per-cycle. After all scheduled fixes are implemented, committed, pushed, and gates are green, run exactly:

```sh
npm run deploy
```
