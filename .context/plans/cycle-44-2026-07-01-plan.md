# Cycle 44/100 Implementation Plan

Start HEAD: `f417d86b`.
Review aggregate: `.context/reviews/cycle-44-2026-07-01/_aggregate.md`.

## Goal

Close the Cycle 44 scanner false-negatives and documentation/context drift without schema changes, dependency changes, or production-runtime behavior changes.

## Scheduled Fixes

1. `TV-C44-01`: make `lint:action-origin` fail protected reads in reasoned exempt concise arrow actions unless a safe proof can be modeled, and require read-only exemption auth proof to come from approved imports rather than a same-file/top-level spoofed `isAdmin` / `getCurrentUser` / `getSession` binding.
2. `TV-C44-02`: make `lint:public-route-rate-limit` fail closed for concise expensive `GET` / `HEAD` handler bodies instead of accepting any approved limiter call somewhere in the expression.
3. `DOC-C44-01`: update CLIP activation docs in `CLAUDE.md` and `apps/web/README.md` to require applying `SEMANTIC_SEARCH_ALLOW_PRODUCTION=true` to the live container via deploy/recreate before setting `admin_settings.semantic_search_mode='production'`.
4. `DOC-C44-02`: update Cycle 43 plan/index context to reflect that the Cycle 44 invocation confirmed `f417d86b` as the current deployed `master` HEAD at cycle start.

## Deferred This Cycle

No new Cycle 44 findings are deferred. Prior deferred items are carried forward in `.context/plans/cycle-44-2026-07-01-deferred.md`.

## Regression Coverage

- Add `check-action-origin.test.ts` fixtures for exempt concise arrow protected reads and same-file/top-level fake auth proof.
- Add `check-public-route-rate-limit.test.ts` fixtures for concise expensive `GET` and `HEAD` bodies where protected work runs before a later limiter.

## Validation

Run the required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then sign, `git pull --rebase`, push, and deploy with `npm run deploy` from the repo root.

## Progress

- [x] Review fan-out complete.
- [x] Aggregate/deferred/plan artifacts written.
- [x] Implement scheduled fixes.
- [x] Add focused regression tests.
- [x] Run gates.
- [x] Commit, push, deploy. Source commit `4ecfdde0` pushed to `origin/master`; per-cycle deploy completed with `Deployment Complete!`, app running at `http://localhost:3000`, remote image `sha256:3dd6089525dad0647f79e45563642b3874a1ca0f23da5117acaecfb8a8038915`, and host filesystem `/dev/sda1` at 14% used after prune.
