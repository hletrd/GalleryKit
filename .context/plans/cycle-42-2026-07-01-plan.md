# Cycle 42/100 Implementation Plan

Start HEAD: `6efd00a8`.
Review aggregate: `.context/reviews/cycle-42-2026-07-01/_aggregate.md`.

## Goal

Close the Cycle 42 scanner, shared-view, accessibility, and deploy-context gaps without schema changes, dependency changes, or production-runtime coordination changes.

## Scheduled Fixes

1. `PA-42-01`: extend `lint:action-origin` protected-read detection to collect namespace `@/db` imports, normalize relative DB module imports, and flag `namespace.db.query.<table>.findFirst/findMany()` before auth.
2. `TV-C42-01`: require public analytics `@action-origin-exempt` limiter gates to be positive over-limit checks; reject under-limit exits and inverted status checks.
3. `TV-C42-02`: reject action-local declarations or parameters that shadow trusted public analytics limiter helper names.
4. `TV-C42-03`: make `check-js-scripts.mjs` fail closed when it discovers zero JavaScript script files.
5. `UX-C42-01`: make the selected shared-group photo back affordance return to `/g/{key}` with destination-specific copy.
6. `A11Y-C42-02`: make `LightboxColorPip` pointer and click interactivity follow the `interactive` prop while controls are hidden.
7. `DOC-C42-01`: add `NEXT_UPLOAD_BODY_MAX_BYTES` to root README deploy environment guidance and pre-build variable wording.
8. `DOC-C42-02`: update the plan index and Cycle 41 plan state so pushed source is no longer marked active/incomplete, while preserving the fact that committed Cycle 41 deploy evidence was not found.

## Deferred This Cycle

- `PA-42-02`: production CLIP web-process catch-up advisory locking and caps. See `.context/plans/cycle-42-2026-07-01-deferred.md`.

## Regression Coverage

- Extend `check-action-origin.test.ts` for namespace/relative DB protected reads, positive-only public analytics gates, and limiter-name shadowing.
- Add a source-contract test for `check-js-scripts.mjs` zero-discovery failure.
- Extend shared-group render coverage for the selected-photo back link destination and label.
- Extend lightbox source-contract coverage for hidden color-pip pointer/click suppression.
- Extend deploy-script/docs contract coverage for root README `NEXT_UPLOAD_BODY_MAX_BYTES` guidance.

## Validation

Run the required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then sign, pull --rebase, push, and deploy with `npm run deploy` from the repo root.

## Progress

- [x] Write aggregate/deferred artifacts.
- [x] Implement scheduled fixes.
- [x] Add focused regression tests.
- [x] Run gates.
- [ ] Commit, push, deploy.
