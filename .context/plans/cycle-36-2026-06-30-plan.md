# Cycle 36/100 Implementation Plan

Date: 2026-06-30 KST
Source review: `.context/reviews/cycle-36-2026-06-30/_aggregate.md`
Reviewed HEAD: `bdfb38a1c39bd828c07851d3d096602441b4122c`

## Scope Rules

- Follow `AGENTS.md` and `CLAUDE.md`.
- Commit and push a GPG-signed Conventional Commit with gitmoji and Lore trailers.
- Run the configured blocking gates before commit/deploy:
  - `npm run lint --workspace=apps/web`
  - `npm run lint:api-auth --workspace=apps/web`
  - `npm run lint:action-origin --workspace=apps/web`
  - `npm run lint:public-route-rate-limit --workspace=apps/web`
  - `npm run typecheck --workspace=apps/web`
  - `npm run build --workspace=apps/web`
  - `npm test --workspace=apps/web`
- Run `git diff --cached --check` before commit.
- After successful push, deploy once from the repo root with `npm run deploy`.

## Scheduled Findings

### C36-P1 - Prove guard and limiter branches exit before side effects

Status: implemented
Findings: `AGG-C36-01`
Original severity/confidence: High / High
Files:

- `apps/web/scripts/check-action-origin.ts`
- `apps/web/src/__tests__/check-action-origin.test.ts`

Plan:

- Add an order-aware helper that walks rejecting guard/limiter branch statements and accepts only a return/throw/redirect before any mutation or pre-origin auth read.
- Use it for standard action guards, auth-file same-origin guards, and public analytics rate-limit exemption gates.
- Add negative fixtures for a mutating `originError` branch, mutating auth-origin branch, and mutating public rate-limit rejection branch.

Acceptance:

- `lint:action-origin` rejects branches that mutate before returning an origin/rate-limit error.
- Existing valid guard shapes still pass.

### C36-P2 - Compute local mutating helpers to a fixed point

Status: implemented
Findings: `AGG-C36-02`
Original severity/confidence: High / High
Files:

- `apps/web/scripts/check-action-origin.ts`
- `apps/web/src/__tests__/check-action-origin.test.ts`

Plan:

- Collect local function/const function bodies first.
- Repeatedly scan bodies until no additional local mutating helper names are discovered.
- Add a regression fixture where a wrapper declared before the real mutator runs before `requireSameOriginAdmin()`.

Acceptance:

- Transitive local mutating helpers are treated as mutations regardless of declaration order.

### C36-P3 - Fail closed on wrapped/default action exports

Status: implemented
Findings: `AGG-C36-03`
Original severity/confidence: High / High
Files:

- `apps/web/scripts/check-action-origin.ts`
- `apps/web/src/app/actions/auth.ts`
- `apps/web/src/__tests__/check-action-origin.test.ts`

Plan:

- Unwrap call-expression initializers when their first argument is an async arrow/function expression, then evaluate that body like other exported actions.
- Fail closed on unsupported exported call-wrapper shapes and default exports in scanned action files.
- Add an explicit read-only exemption to the existing cached `getCurrentUser` export.
- Add negative fixtures for mutating wrappers and default exports.

Acceptance:

- Wrapped async action bodies are checked instead of silently omitted.
- Default action exports are rejected as unsupported scanner shapes.

### C36-P4 - Classify helper-backed public upload GET routes explicitly

Status: implemented
Findings: `AGG-C36-04`
Original severity/confidence: Medium / High
Files:

- `apps/web/scripts/check-public-route-rate-limit.ts`
- `apps/web/src/app/uploads/[...path]/route.ts`
- `apps/web/src/app/[locale]/(public)/uploads/[...path]/route.ts`
- `apps/web/src/__tests__/check-public-route-rate-limit.test.ts`

Plan:

- Treat `serveUploadFile(...)` as expensive public GET work.
- Fail closed on bodyless named `GET` re-exports from another module.
- Add explicit route-level `@public-no-rate-limit-required` comments to the two upload fallback routes explaining public derivative serving, cache validators, and why rate limiting is not desired.
- Add regression fixtures.

Acceptance:

- Helper-backed public upload GETs require either a limiter or an explicit exemption.
- Unknown named `GET` re-exports no longer pass as cheap.

### C36-P5 - Repair admin-token FK convergence and orphan-token verification

Status: implemented
Findings: `AGG-C36-05`
Original severity/confidence: High / High
Files:

- `apps/web/scripts/migrate.js`
- `apps/web/src/lib/admin-tokens.ts`
- `apps/web/src/__tests__/migrate-reconcile-coverage.test.ts`
- `apps/web/src/__tests__/admin-tokens.test.ts`

Plan:

- Add explicit `ensureForeignKey(...)` calls for current FK constraints that previously existed only in create-table DDL.
- Make `verifyToken()` join `admin_users` so orphan token rows fail closed before and after FK repair.
- Add source/contract tests for explicit FK repair and token owner existence enforcement.

Acceptance:

- Reconcile source coverage fails if current FK names only appear in create-table DDL.
- PAT verification requires a surviving owner admin row.

## Deferred Findings

See `.context/plans/cycle-36-2026-06-30-deferred.md`.

## Progress

- [x] Review lanes completed and findings aggregated.
- [x] Plan and deferred records created.
- [x] C36-P1 implemented and tested.
- [x] C36-P2 implemented and tested.
- [x] C36-P3 implemented and tested.
- [x] C36-P4 implemented and tested.
- [x] C36-P5 implemented and tested.
- [x] Full required gates passed.

## Verification Evidence

- `npm test --workspace=apps/web -- --run src/__tests__/check-action-origin.test.ts src/__tests__/check-public-route-rate-limit.test.ts src/__tests__/migrate-reconcile-coverage.test.ts src/__tests__/admin-tokens.test.ts`: passed, 229 tests.
- `npm run lint --workspace=apps/web`: passed.
- `npm run lint:api-auth --workspace=apps/web`: passed.
- `npm run lint:action-origin --workspace=apps/web`: passed.
- `npm run lint:public-route-rate-limit --workspace=apps/web`: passed.
- `npm run typecheck --workspace=apps/web`: passed.
- `npm run build --workspace=apps/web`: passed; local MySQL was unavailable and sitemap used its documented homepage-only fallback.
- `npm test --workspace=apps/web`: passed, 2628 passed / 4 skipped.
