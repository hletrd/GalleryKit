# Cycle 34/100 Implementation Plan

Date: 2026-06-30 KST
Source review: `.context/reviews/_aggregate.md`
Reviewed HEAD: `e1f124a265998ea51297d6716df6c03a2056a96c`

## Scope Rules

- Follow `AGENTS.md` and `CLAUDE.md`.
- Commit and push a GPG-signed Conventional Commit with gitmoji and Lore trailers.
- Run the configured blocking gates before commit/deploy:
  - `npm run lint --workspace=apps/web`
  - `npm run lint:api-auth --workspace=apps/web`
  - `npm run lint:action-origin --workspace=apps/web`
  - `npm run lint:public-route-rate-limit --workspace=apps/web`
  - `npm run typecheck --workspace=apps/web`
  - `npm run build --workspace=apps/web`
  - `npm test --workspace=apps/web`
- Run `git diff --cached --check` before commit.
- After successful push, deploy once from the repo root with `npm run deploy`.

## Scheduled Findings

### C34-P1 - Release LR multipart parse slot only after quota-safe acquisition

Status: implemented
Findings: `AGG-C34-01`
Original severity/confidence: High / High
Files:

- `apps/web/src/app/api/admin/lr/upload/route.ts`
- `apps/web/src/__tests__/lr-upload-hdr-gate.test.ts`

Plan:

- Move `tryAcquireLrMultipartParseSlot()` after the LR upload tracker count/byte quota rejection branches.
- Keep acquisition before `request.formData()` so body materialization remains single-flight.
- Add a regression assertion that both quota branches are before acquisition and that no `return NextResponse` exists between acquisition and the release-dominated `request.formData()` block.

Acceptance:

- A quota-rejected LR request cannot acquire and leak the singleton parse slot.
- Focused LR upload source-contract tests pass.

### C34-P2 - Fail closed on inverted auth same-origin scanner guards

Status: implemented
Findings: `AGG-C34-02`
Original severity/confidence: High / High
Files:

- `apps/web/scripts/check-action-origin.ts`
- `apps/web/src/__tests__/check-action-origin.test.ts`

Plan:

- Require the auth-specific scanner path to recognize only `if (!hasTrustedSameOrigin(...)) { exit }` as an approved early-exit guard.
- Add a negative fixture where `if (hasTrustedSameOrigin(...)) return` fails the lint scanner.

Acceptance:

- `lint:action-origin` fails closed for inverted trusted-origin early exits and still passes current `auth.ts`.

## Deferred Findings

See `.context/plans/cycle-34-2026-06-30-deferred.md`. No new Cycle 34 findings are deferred; Cycle 33 deferred items remain governed by `cycle-33-2026-06-30-deferred.md` and were not re-opened this cycle.

## Progress

- [x] Review lanes completed and findings aggregated.
- [x] Plan and deferred records created.
- [x] C34-P1 implemented and tested.
- [x] C34-P2 implemented and tested.
- [x] Full required gates passed.
- [ ] Signed commit pushed.
- [ ] Per-cycle deploy completed.

## Verification Evidence

- `npm test --workspace=apps/web -- --run src/__tests__/lr-upload-hdr-gate.test.ts src/__tests__/check-action-origin.test.ts`: passed, 86 tests.
- `npm run lint --workspace=apps/web`: passed.
- `npm run lint:api-auth --workspace=apps/web`: passed.
- `npm run lint:action-origin --workspace=apps/web`: passed.
- `npm run lint:public-route-rate-limit --workspace=apps/web`: passed.
- `npm run typecheck --workspace=apps/web`: passed.
- `npm run build --workspace=apps/web`: passed; local MySQL was unavailable and sitemap used its documented homepage-only fallback.
- `npm test --workspace=apps/web`: passed, 2594 passed / 4 skipped.
