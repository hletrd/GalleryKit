# Cycle 68/100 Implementation Plan

Start HEAD: `e221b01a6f57ae019ba1ba3fb2852926f91ebea1` at cycle start.
Review aggregate: `.context/reviews/cycle-68-2026-07-01/_aggregate.md`.

## Goal

Fix the safe, reviewable Cycle 68 findings, keep review/plan ledgers current, run all required gates, commit, push, and deploy with `npm run deploy` from the repo root.

## Scheduled Fixes

1. `C68-01`: when the Settings page has a saved settings-only re-encode obligation and the in-app backfill returns zero pipeline-version candidates, show a distinct message that existing derivatives still require the sidecar backfill with `--force-reencode`.
2. `C68-02`: strengthen the CLIP embedding sidecar source contract so the operator-visible `SEMANTIC_SCAN_LIMIT` rerun notice body cannot disappear while tests stay green.
3. `C68-03`: canonicalize scalar gallery setting values across client diffing, backfill-warning comparison, and server persistence so whitespace-only edits do not create fake byte-impacting changes.

## Deferred This Cycle

No new Cycle 68 findings are deferred.

Carry-forward items remain in `.context/plans/cycle-68-2026-07-01-deferred.md`.

## Regression Coverage

- Add/extend tests for Settings submit/backfill-warning normalization: scalar whitespace is equivalent to canonical values, blank values still mean defaults, and image-size canonicalization remains unchanged.
- Add source-contract coverage for the settings-only zero-candidate message branch.
- Add source-contract coverage for the CLIP scan-limit notice body.
- Existing i18n parity tests cover the new localized message key.

## Validation

Run required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then sign, `git pull --rebase`, push, and deploy with `npm run deploy`.

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run required gates.
- [x] Commit, pull --rebase, push.
- [x] Deploy with `npm run deploy`.

## Gate Evidence

- Focused regression: `npm test --workspace=apps/web -- --run src/__tests__/settings-submit-payload.test.ts src/__tests__/settings-backfill-warning.test.ts src/__tests__/settings-backfill-warning-source.test.ts src/__tests__/settings-semantic-mode-action.test.ts src/__tests__/cycle-6-source-contracts.test.ts src/__tests__/i18n-key-parity.test.ts` - pass: 6 files passed, 28 tests passed.
- `npm run lint --workspace=apps/web` - pass.
- `npm run lint:api-auth --workspace=apps/web` - pass.
- `npm run lint:action-origin --workspace=apps/web` - pass.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
- `npm run typecheck --workspace=apps/web` - pass.
- `npm run build --workspace=apps/web` - pass. Local sitemap generation logged the expected unavailable local MySQL fallback and exited successfully.
- `npm test --workspace=apps/web` - pass: 294 files passed, 2 skipped; 2762 tests passed, 4 skipped.
- `git diff --cached --check` - pass after one whitespace cleanup iteration on new artifacts.

## Gate Fix Iterations

1. Whitespace cleanup before `git diff --cached --check`.

## Terminal Evidence

- Cycle 69 started from deployed `master` HEAD `87e2b98db76e90985299e37ad90cf2faad12c5c4` (`fix(cycle-68): 🐛 clarify settings backfill state`), confirming the Cycle 68 commit/push/deploy path had completed before this cycle.
