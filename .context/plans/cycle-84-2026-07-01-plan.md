# Cycle 84/100 Implementation Plan

Start HEAD: `023ae28d41ee757caaa408710bd864d88087a40c`.
Review aggregate: `.context/reviews/cycle-84-2026-07-01/_aggregate.md`.

## Goal

Close the safe Cycle 84 findings, keep review/plan ledgers current, strengthen the failed-image retry source contract, run all required gates, commit/push all work, and deploy with `npm run deploy` from the repo root.

## Scheduled Fixes

1. `C84-01`: close Cycle 83's release ledger by marking commit/pull-rebase/push complete with signed `023ae28d` / `origin/master` evidence, recording that no committed Cycle 83 deploy transcript existed at Cycle 84 review time and that Cycle 84's verified per-cycle deploy will supersede production state, then moving Cycle 83 out of the active plan index.
2. `C84-02`: strengthen `failed-image-retry.test.ts` so the failed-image row map body must assign `const label = getFailedImageLabel(img);`, render `{label}`, and use that same `label` in the retry `aria-label`.
3. `C84-03`: keep Cycle 84 plan artifacts trackable by extending the existing `.gitignore` whitelist for the new plan and deferred files.

## Deferred This Cycle

No new Cycle 84 findings are deferred.

Carry-forward deferred items remain in `.context/plans/cycle-84-2026-07-01-deferred.md`.

## Regression Coverage

- Failed-image retry labels: source-contract coverage must prove the helper-derived `label` value is assigned inside the `failedImages.map((img) => { ... })` body, rendered as visible row text, and passed into the localized retry button accessible name.
- Ledger: Cycle 83 must no longer be listed as active, and Cycle 83's terminal state must record signed commit/push evidence for `023ae28d` plus an explicit deploy-evidence gap/supersession note.

## Validation

Run required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then signed commit, `git pull --rebase`, push, and deploy with `npm run deploy`.

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run required gates.
- [ ] Commit, pull --rebase, push.
- [ ] Deploy with `npm run deploy`.

## Gate Evidence

- Focused regression: `npm test --workspace=apps/web -- --run src/__tests__/failed-image-retry.test.ts` - pass: 1 file passed, 18 tests passed.
- `npm run lint --workspace=apps/web` - pass.
- `npm run lint:api-auth --workspace=apps/web` - pass: both admin API routes OK.
- `npm run lint:action-origin --workspace=apps/web` - pass: all mutating server actions enforce same-origin provenance.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
- `npm run typecheck --workspace=apps/web` - pass.
- `npm run build --workspace=apps/web` - pass. Local sitemap generation logged the expected unavailable local MySQL fallback and exited successfully.
- `npm test --workspace=apps/web` - pass: 301 files passed, 2 skipped; 2835 tests passed, 4 skipped.
- `git diff --cached --check` - initial run failed on review-artifact trailing spaces and two blank EOFs; fixed and rerun passed.

## Gate Fix Iterations

1. Removed review/plan artifact trailing whitespace and blank EOFs after `git diff --cached --check` reported them.
