# Cycle 39/100 Implementation Plan

Start HEAD: `addf64ac`.
Review aggregate: `.context/reviews/cycle-39-2026-06-30/_aggregate.md`.

## Goal

Close five safe, reviewable guardrail/accessibility/cache findings without changing photographer-facing product policy or schema.

## Scheduled Fixes

1. `UI-C39-01`: remove `aria-hidden` from the visible search empty/error status and pin the accessibility contract.
2. `PERF-C39-01`: serialize service-worker image metadata mutations in `sw.template.js` and the `lib/sw-cache.ts` reference implementation, with a concurrent write regression test.
3. `PERF-C39-02`: prefer `Content-Length` for service-worker image response sizing and fall back to `blob()` only when the header is missing or invalid; keep `sw.js` generated from the template.
4. `TEST-C39-01`: include the top-level `app/actions.ts` barrel in the action-origin lint surface and allow only action-module re-exports plus type-only exports.
5. `TEST-C39-02`: treat `HEAD` as an expensive public read method in the public-route rate-limit scanner, including tests for missing limiter/exemption and the existing GET/HEAD upload exemption shape.

## Validation

Run the required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then sign, pull --rebase, push, and deploy with `npm run deploy` from the repo root.
