# Run-10 Cycle 22/100 Implementation Plan

Status: IMPLEMENTED - GATES PENDING
Aggregate: `.context/reviews/_aggregate.md`
Date: 2026-07-08 KST
Review start HEAD: `8b795862079b0e5318242a09390b4cdff1dc2058`

## Scope

This plan schedules the contained Cycle 22 findings that are correctness, privacy/data-retention, restore-recovery, and provenance critical. Every aggregate finding not scheduled here is recorded in `cycle-22-2026-07-08-deferred.md` with original severity/confidence, citation, deferral reason, and exit criterion.

Repo rules read before scheduling/deferring: `CLAUDE.md`, `AGENTS.md`, `.context/plans/README.md`, `.context/plans/deferred-carry-forward.md`, the Cycle 21 plan/deferred pair, root/app README files, and current review artifacts. No `.cursorrules` or `CONTRIBUTING.md` exists in this checkout.

## Scheduled Work Packages

### WP1 - Close the positive mutation-slot guard scanner bypass

Findings: `AGG-C22-01`, targeted part of `AGG-C22-09`

Files:

- `apps/web/scripts/check-action-origin.ts`
- `apps/web/src/__tests__/check-action-origin.test.ts`

Plan:

1. Add a negative fixture where `using mutationSlot = acquireAdminMutationSlot(); if (mutationSlot.acquired) {}` is followed by a mutation outside the branch.
2. Update `bodyAcquiresAdminMutationSlot()` so the positive acquired-guard shape is accepted only when protected mutations after the slot declaration are lexically contained by the positive branch.
3. Keep the existing legitimate `logout()` / positive-guard pattern accepted.
4. Run the targeted scanner tests and `npm run lint:action-origin --workspace=apps/web`.

Acceptance:

- A post-branch mutation fails the lint gate.
- The real positive-guard action shape remains accepted.
- The negative early-return shape remains accepted when the acquired check precedes mutation.

Status: implemented. `check-action-origin.ts` now rejects positive acquired checks when later sibling statements can still mutate; the scanner test fixture covers the bypass.

### WP2 - Add an autonomous drain for pending image file deletions

Findings: `AGG-C22-02`, `AGG-C22-03`, targeted part of `AGG-C22-09`

Files:

- `apps/web/src/lib/pending-file-deletions.ts`
- `apps/web/src/lib/maintenance-scheduler.ts`
- `apps/web/src/app/[locale]/admin/db-actions.ts` if restore completion needs a direct flush hook
- focused tests under `apps/web/src/__tests__/`

Plan:

1. Add a bounded `drainPendingFileDeletions()` helper that pages oldest rows, calls `cleanupPendingFileDeletion()`, removes rows on full success, preserves failures with attempt/error metadata, and treats already-missing files as success through the existing strict cleanup behavior or a safe wrapper if needed.
2. Wire the drain into the startup/hourly maintenance sweep behind the existing restore-maintenance guard.
3. Ensure restored `pending_file_deletions` rows are retried after restore maintenance clears, either through the regular scheduler path or an explicit post-restore flush.
4. Add behavior tests for transient failure then later success, persistent failure, missing-file idempotency, and restore-active suppression.

Acceptance:

- A retained `pending_file_deletions` row is retried by a later sweep and removed after files are gone.
- The drain does not run during restore maintenance.
- Repeated failures stay durable with updated attempt/error evidence.

Status: implemented. `drainPendingFileDeletions()` retries oldest rows in bounded batches, maintenance runs it behind the restore guard, and restore completion drains after clearing the durable marker.

### WP3 - Repair Cycle 21 terminal ledger and pending-deletion runbook docs

Findings: `AGG-C22-04`, documentation half of `AGG-C22-02`/`AGG-C22-03`

Files:

- `.context/plans/cycle-21-2026-07-08-plan.md`
- `.context/plans/README.md`
- `CLAUDE.md`
- possibly `README.md` or `apps/web/README.md` if operator-facing deletion recovery text belongs there

Plan:

1. Add Cycle 21 terminal evidence that the commit-body deploy wording is stale and superseded by orchestrator-provided deploy/live-smoke success, including exact commit hash `8b795862079b0e5318242a09390b4cdff1dc2058`.
2. Move Cycle 21 from active to recently completed in the plan index and add Cycle 22 as the active pair.
3. Document `pending_file_deletions` as operator-visible state: what a row means, how automatic retry works after WP2, and how to inspect unresolved rows.
4. Clarify that Cycle 21 shipped durable recording plus same-request cleanup retry, while Cycle 22 closes the later-drain half.

Acceptance:

- Future agents can determine that Cycle 21 is completed/deployed or superseded without relying on stale commit body text.
- Operator docs name `pending_file_deletions` and its recovery path.

Status: implemented. Cycle 21 is marked completed in the plan index/ledger, Cycle 22 is indexed as active, and `CLAUDE.md` documents `pending_file_deletions` recovery/inspection.

### WP4 - Tighten parser-backed timeline grouping test coverage

Findings: `AGG-C22-10`

Files:

- `apps/web/src/__tests__/data-timeline.test.ts`
- `apps/web/src/lib/mysql-datetime.ts` or an existing timeline grouping helper if export shape needs a small pure helper

Plan:

1. Add a timezone-sensitive test fixture that proves persisted MySQL datetime grouping does not use JavaScript `Date.parse`.
2. Prefer exercising an existing pure parser-backed helper; if none exists, extract the smallest reusable grouping helper without changing runtime behavior.

Acceptance:

- Tests fail if timeline/year grouping regresses to `new Date(capture_date)` semantics.

Status: implemented. `data-timeline-behavior.test.ts` exercises `getYearInReviewImages()` through the mocked query chain and verifies parser-backed month grouping skips invalid MySQL DATETIME values.

### WP5 - Refresh plan/deferred carry-forward state for Cycle 22

Findings: planning requirement for every review row; deferred-fix rules.

Files:

- `.context/plans/cycle-22-2026-07-08-deferred.md`
- `.context/plans/deferred-carry-forward.md`
- `.context/plans/README.md`

Plan:

1. Add the Cycle 22 deferred register for all non-scheduled aggregate findings.
2. Update carry-forward age text from run-10 cycle 21 to run-10 cycle 22.
3. Add new Cycle 22 deferrals to the carry-forward register and preserve prior deferred severity/confidence.
4. Run the High 8-cycle and Medium 16-cycle checkpoint; no direct unpatched correctness/security/data-loss item may be deferred unless an explicit repo rule allows it.

Acceptance:

- Every `AGG-C22-*` row is either scheduled here or recorded in the deferred register.
- Deferred rows preserve severity/confidence and exit criteria.

Status: implemented. Cycle 22 deferred and carry-forward registers were created/updated with every non-scheduled aggregate row.

### WP6 - Full gates, commit, push, and per-cycle deploy

Findings: cycle quality/deploy requirement.

Plan:

1. Run every configured gate against the whole repo:
   - `npm run lint --workspace=apps/web`
   - `npm run lint:api-auth --workspace=apps/web`
   - `npm run lint:action-origin --workspace=apps/web`
   - `npm run lint:public-route-rate-limit --workspace=apps/web`
   - `npm run typecheck --workspace=apps/web`
   - `npm run build --workspace=apps/web`
   - `npm test --workspace=apps/web`
   - `npm run test:e2e --workspace=apps/web`
2. Fix all gate errors before final commit/push.
3. Commit and push signed Conventional Commit + gitmoji changes after `git pull --rebase`.
4. Run `npm run deploy` once after all plan work is pushed and gates are green.

Status: gates passed; commit/push/deploy pending.

## Progress

- [x] WP1 mutation-slot positive-guard scanner bypass.
- [x] WP2 pending deletion drain and restore recovery.
- [x] WP3 Cycle 21 terminal ledger and pending-deletion docs.
- [x] WP4 parser-backed timeline grouping tests.
- [x] WP5 Cycle 22 deferred/carry-forward/index refresh.
- [ ] WP6 full gates, signed commit/push, and per-cycle deploy.

## Gate Evidence

- Targeted: `npm test --workspace=apps/web -- check-action-origin pending-file-deletions pending-session-revocations data-timeline-behavior data-timeline` - PASS (7 files, 150 tests).
- `npm run lint --workspace=apps/web` - PASS.
- `npm run lint:api-auth --workspace=apps/web` - PASS.
- `npm run lint:action-origin --workspace=apps/web` - PASS.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - PASS.
- `npm run typecheck --workspace=apps/web` - PASS.
- `npm run build --workspace=apps/web` - PASS.
- `npm test --workspace=apps/web` - PASS (358 files passed, 2 skipped; 3353 tests passed, 4 skipped).
- `npm run test:e2e --workspace=apps/web` - PASS (45 passed, 2 skipped). Playwright emitted existing `ResponseAborted` web-server noise during navigations, but no test failed.
