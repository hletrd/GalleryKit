# Cycle 64/100 Implementation Plan

Start HEAD: `efdbaf9a4971e8c59051fe422c8b44d6e9dd455f` at cycle start.
Review aggregate: `.context/reviews/cycle-64-2026-07-01/_aggregate.md`.

## Goal

Fix the actionable Cycle 64 findings, keep review/plan ledgers current, run all required gates, commit, push, and deploy with `npm run deploy` from the repo root.

## Scheduled Fixes

1. `C64-01`: reset search `activeIndex` and stale refs whenever the shared search reset path runs, including semantic-mode toggles.
2. `C64-02`: give admin-only GPS Google Maps links in the photo viewer and mobile info sheet a 44 x 44 px hit target.
3. `C64-03`: give Radix `SelectItem` option rows a 44 px minimum height.
4. `C64-04`: keep a post-save backfill/force-reencode obligation visible after byte-impacting color/quality settings are saved for galleries with existing images.

## Deferred This Cycle

No new Cycle 64 findings are deferred.

Carry-forward items remain in `.context/plans/cycle-64-2026-07-01-deferred.md`.

## Regression Coverage

- Extend the semantic-toggle source contract to require `clearSearchState()` to reset active selection and result refs.
- Add GPS map-link source-contract coverage for both admin-only Google Maps anchors.
- Add SelectItem source-contract coverage for `min-h-11`.
- Add Settings source-contract coverage that saved byte-impacting settings set a persistent post-save backfill warning and the banner uses dirty-or-saved-pending state.

## Validation

Run required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then sign, `git pull --rebase`, push, and deploy with `npm run deploy`.

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run required gates.
- [ ] Commit, pull --rebase, push.
- [ ] Deploy with `npm run deploy`.

## Gate Evidence

- Focused regression: `npm test --workspace=apps/web -- search-semantic-toggle-source gps-map-link-touch-targets select-item-touch-target settings-backfill-warning-source search-status-source analytics-link-touch-targets` - pass: 6 files passed, 11 tests passed.
- Focused rerun after nearby Settings comment cleanup: `npm test --workspace=apps/web -- settings-backfill-warning-source` - pass: 1 file passed, 1 test passed.
- `npm run lint --workspace=apps/web` - pass.
- `npm run lint:api-auth --workspace=apps/web` - pass.
- `npm run lint:action-origin --workspace=apps/web` - pass.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
- `npm run typecheck --workspace=apps/web` - pass.
- `npm run build --workspace=apps/web` - pass. Local sitemap generation logged the expected unavailable local MySQL fallback and exited successfully.
- `npm test --workspace=apps/web` - pass: 292 files passed, 2 skipped; 2751 tests passed, 4 skipped.

## Gate Fix Iterations

0.
