# Cycle 74/100 Implementation Plan

Start HEAD: `92924220ff51258a8d29d78444200d7f7dcbd430`.
Review aggregate: `.context/reviews/cycle-74-2026-07-01/_aggregate.md`.

## Goal

Fix the safe, reviewable Cycle 74 findings, keep review/plan ledgers current, run all required gates, commit, push, and deploy with `npm run deploy` from the repo root.

## Scheduled Fixes

1. `C74-01`: keep feed conditional behavior ETag-only, update stale If-Modified-Since helper/route comments, and add route tests proving IMS-only requests return 200.
2. `C74-02`: lock `getImageProcessingState()` as an unprocessed-safe helper and add route coverage for permanent photo misses using the long success cache policy.
3. `C74-03`: close Cycle 73 terminal plan/index state using signed commit `92924220` and the Cycle 74 start evidence that deployed master was `92924220`.
4. `C74-04`: associate the password-change minimum-length help text with both new-password fields via `aria-describedby` and source-lock the pattern.

## Deferred This Cycle

No new Cycle 74 findings are deferred. Carry-forward deferred items remain recorded in `.context/plans/cycle-74-2026-07-01-deferred.md`.

## Regression Coverage

- `feed-conditional.test.ts`: root/topic feed ETag-only behavior, matching ETag 304s, IMS-only 200s, SEO ETag invalidation, topic 404, and invalid-locale branches.
- `og-photo-fallback.test.ts`: source contract for `getImageProcessingState()` querying by id without `processed = true`.
- `og-route-rate-limit-behavior.test.ts`: permanent photo misses retain success cache policy while pending/temporary misses stay no-store.
- `password-form-a11y.test.ts`: PasswordForm associates visible minimum-length help with both password fields and concatenates the confirm-error id when present.

## Validation

Run required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then sign, `git pull --rebase`, push, and deploy with `npm run deploy`.

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run required gates.
- [ ] Commit, pull --rebase, push.
- [ ] Deploy with `npm run deploy`.

## Gate Evidence

- Focused regression: `npm test --workspace=apps/web -- --run src/__tests__/feed-conditional.test.ts src/__tests__/feed-sized-derivative.test.ts src/__tests__/og-photo-fallback.test.ts src/__tests__/og-route-rate-limit-behavior.test.ts src/__tests__/password-form-a11y.test.ts` - pass: 5 files passed, 55 tests passed.
- `npm run lint --workspace=apps/web` - pass.
- `npm run lint:api-auth --workspace=apps/web` - pass: both admin API routes OK.
- `npm run lint:action-origin --workspace=apps/web` - pass: all mutating server actions enforce same-origin provenance.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
- `npm run typecheck --workspace=apps/web` - pass.
- `npm run build --workspace=apps/web` - pass. Local sitemap generation logged the expected unavailable local MySQL fallback and exited successfully.
- `npm test --workspace=apps/web` - pass: 298 files passed, 2 skipped; 2793 tests passed, 4 skipped.

## Gate Fix Iterations

- 0.
