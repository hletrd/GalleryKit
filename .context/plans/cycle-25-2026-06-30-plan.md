# Cycle 25/100 Implementation Plan

Date: 2026-06-30 KST
Review source: `.context/reviews/_aggregate.md`
Status: DONE - implemented; required gates passed; commit/push/deploy pending

## Repo Rules Read Before Planning

Read order satisfied for deferral policy: `CLAUDE.md`, `AGENTS.md`, `.context/` plan/review index and current cycle reviews, `.cursorrules`/`CONTRIBUTING.md` lookup, and docs/style-policy lookup. No `.cursorrules`, `CONTRIBUTING.md`, or `docs/**/*.md` policy files exist in this checkout. `.context/plans/README.md` says plan history is committed, so this cycle adds new plan/deferred files instead of overwriting older cycle history.

Cycle 24 is marked implemented/verified in its plan and the orchestrator states cycles 1-24 completed with commits and per-cycle deploy success. This cycle will move the cycle-24 plan pair from active index into the completed/archive section while preserving the files.

## Scheduled Fixes

Every aggregate finding is either scheduled below or explicitly deferred in `cycle-25-2026-06-30-deferred.md`. Security/correctness items are scheduled unless a repo rule explicitly permits deferral or the finding is an accepted operational/design risk rather than a confirmed source defect.

### P25-01 - Make failed-restore maintenance durable and recoverable [done]

- Findings: `AGG25-28`, `AGG25-24`
- Original severity/confidence: High / High; High / Medium-High
- Files: `apps/web/src/lib/restore-maintenance.ts`, `apps/web/src/app/[locale]/admin/db-actions.ts`, restore tests/source contracts as needed
- Plan: add a host-side durable restore-maintenance marker outside the DB import path; write it before restore starts, preserve it on failed import/post-migration failure, clear it only after success or explicit existing cleanup path, and make in-process checks load the durable marker. Keep login blocked during failed restore until marker is cleared by a successful restore/recovery path. Add behavior/source tests around failed restore marker persistence and success clearing.
- Acceptance: failed restore state survives process-local reset in tests; success clears durable marker; existing restore maintenance behavior remains compatible.
- Result: `restore-maintenance-durable.ts` now persists a host-side marker under `data/restore-maintenance.json` (test-overridable), reloads it after process state reset, and clears it on `endDurableRestoreMaintenance()`. `restore-maintenance.ts` remains the process-local state surface. `restore-maintenance.test.ts` proves durable recovery and clearing.

### P25-02 - Fence public analytics writes against abuse and restore races [done]

- Findings: `AGG25-19`, `AGG25-29`, `AGG25-37`
- Original severity/confidence: Medium / High; Medium / High; Low / High
- Files: `apps/web/src/app/actions/public.ts`, `apps/web/src/lib/rate-limit.ts`, `apps/web/src/__tests__/public-actions.test.ts`
- Plan: add a DB-backed public view-record bucket before durable analytics writes, keep the process-local fast cap as defense in depth, re-check restore maintenance immediately before inserts, and record tests proving rate-limit denial/rollback and late-maintenance blocking. If full shutdown drain is too large for this cycle, explicitly document row analytics as approximate and preserve it in deferred carry-forward.
- Acceptance: public analytics recorders consult a durable limiter before SELECT/INSERT; late maintenance prevents inserts; tests cover the new gates.
- Result: `recordPhotoView`, `recordTopicView`, and `recordSharedGroupView` now use the DB-backed `view_record` bucket before target lookups and re-check restore maintenance immediately before insert. `public-actions.test.ts` covers durable limiter denial and late-maintenance blocking. Full shutdown drain/drop-counter work remains deferred as approximate analytics design work in `cycle-25-2026-06-30-deferred.md`.

### P25-03 - Clamp image queue concurrency to two-connection worker budget [done]

- Finding: `AGG25-02`
- Original severity/confidence: Medium / High
- Files: `apps/web/src/lib/image-queue.ts`, `apps/web/src/__tests__/image-queue-concurrency-cap.test.ts`, docs if needed
- Plan: budget one long-held advisory-lock connection plus one transient DB connection per worker and keep reserved headroom. Default remains safe; explicit high values clamp lower.
- Acceptance: tests assert the two-connection invariant and shipped pool defaults cannot exhaust all DB connections with queue workers alone.
- Result: `resolveImageQueueConcurrency` now caps to `floor((pool - reserved) / 2)`, so the default 10-connection pool clamps high requests to 2 foreground workers. `image-queue-concurrency-cap.test.ts` locks the two-connection invariant. `storeImageEmbeddingForMode` also skips before CLIP generation when restore maintenance is active.

### P25-04 - Add deploy health wait and narrow startup ownership repair [done]

- Findings: `AGG25-30`, `AGG25-13`
- Original severity/confidence: Medium / High; Low-Medium / High
- Files: `apps/web/deploy.sh`, `apps/web/scripts/entrypoint.sh`, `apps/web/src/__tests__/deploy-script-contract.test.ts`, entrypoint/deploy source-contract tests as available
- Plan: wait for Docker health or `/api/live` after `docker compose up -d --build` and before prune/success output; dump recent logs on timeout. Replace recursive startup `chown -R` over large bind mounts with targeted directory creation/writability checks and a clear remediation failure.
- Acceptance: contract tests prove health check occurs before prune/success and broad recursive chown over bind-mounted stores is not reintroduced.
- Result: `apps/web/deploy.sh` waits for Docker health or `/api/live` before pruning/success and dumps recent logs on timeout. `entrypoint.sh` now verifies targeted writable directories and avoids recursive chown over large bind mounts. `deploy-script-contract.test.ts` locks both contracts.

### P25-05 - Fix docs and operator contract drift [done]

- Findings: `AGG25-20`, `AGG25-21`, `AGG25-38`, `AGG25-39`, `AGG25-40`, `AGG25-41`, `AGG25-45`, `AGG25-46`
- Original severity/confidence: Low-Medium / High; Medium / High; Medium / High; Medium / High; High / High; Low / High; Medium / High; Low / High
- Files: `README.md`, `apps/web/README.md`, `AGENTS.md`, `CLAUDE.md`, `apps/web/.env.local.example`, `.omc/wiki/clip-semantic-search-us-p51.md`, `.omc/wiki/deploy-disk-hygiene-runbook.md`, `apps/web/nginx/default.conf`
- Plan: align fresh-install DB examples, PAT upload request/response docs, semantic-search rate-limit/live-production wording, CLIP pre-enable `--force` command, disk-hygiene mount list, and nginx external-upload API comments.
- Acceptance: docs describe current route/config behavior exactly and no longer make unverified live-production or bundled-plugin claims.
- Result: README/app README/CLAUDE/AGENTS/env/wiki/nginx docs now align DB onboarding values, PAT upload fields/response, semantic-search live/limiter wording, CLIP `--force`, disk-hygiene mounts, and external-upload API wording.

### P25-06 - Fix focused UI accessibility defects [done]

- Findings: `AGG25-43`, `AGG25-44`, `AGG25-48`
- Original severity/confidence: Low / High; Low / Medium; Medium / High
- Files: `apps/web/src/app/[locale]/(public)/p/[id]/loading.tsx`, `apps/web/src/components/search.tsx`, `apps/web/src/components/photo-viewer.tsx`, `apps/web/messages/en.json`, `apps/web/messages/ko.json`, targeted tests if existing
- Plan: add an accessible name/text to auto-lightbox loading, add local search result keyboard instructions tied to the input, and expand photo-viewer shortcut guards so shortcuts do not fire from focused controls/menu items.
- Acceptance: targeted tests or existing a11y/i18n gates pass and keyboard behavior is safer without changing pointer workflows.
- Result: auto-lightbox loading now has localized status text, search results expose arrow/Enter instructions through `aria-describedby` and visible desktop hint, and photo-viewer shortcuts ignore focused interactive controls.

### P25-07 - Add smart-collection pagination behavior tests [done]

- Finding: `AGG25-25`
- Original severity/confidence: Medium / High
- Files: `apps/web/src/__tests__/public-actions.test.ts`
- Plan: add behavior tests for invalid/missing/private smart collection, cursor forwarding, page-size fetch count, and `hasMore` boundaries.
- Acceptance: tests fail on private leakage, cursor loss, or incorrect `hasMore` behavior.
- Result: existing smart-collection pagination coverage was extended with terminal `hasMore=false` behavior and query-parse error contract coverage.

## Prompt 3 Execution Notes

- Use Ralph-style persistence before edits: write a context snapshot under `.omx/context/`.
- Run every configured gate after implementation: `lint`, `lint:api-auth`, `lint:action-origin`, `lint:public-route-rate-limit`, `typecheck`, `build`, `test`.
- Commit and push signed Conventional Commit + gitmoji commits after green gates and `git pull --rebase`.
- Per-cycle deploy command: `npm run deploy`.

## Progress

- [x] Aggregate review written.
- [x] Plan and deferred register written.
- [x] Cycle-24 plan pair archived from active index.
- [x] P25-01 implemented and tested.
- [x] P25-02 implemented and tested.
- [x] P25-03 implemented and tested.
- [x] P25-04 implemented and tested.
- [x] P25-05 implemented.
- [x] P25-06 implemented and tested.
- [x] P25-07 implemented and tested.
- [x] Deferred findings preserved with original severity/confidence and exit criteria.
- [x] Full gate list green.
- [ ] Signed commit(s) pushed.
- [ ] Per-cycle deploy recorded.

## Validation

- Targeted scheduled tests: `npm test --workspace=apps/web -- restore-maintenance.test.ts image-queue-concurrency-cap.test.ts deploy-script-contract.test.ts public-actions.test.ts smart-collection-pagination.test.ts` - pass, 5 files / 56 tests.
- `npm run lint --workspace=apps/web` - pass.
- `npm run lint:api-auth --workspace=apps/web` - pass.
- `npm run lint:action-origin --workspace=apps/web` - pass.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
- `npm run typecheck --workspace=apps/web` - pass.
- `npm run build --workspace=apps/web` - pass. The build still emits the tracked Turbopack NFT warning recorded as `GW25-01` in `cycle-25-2026-06-30-deferred.md`; local sitemap generation also falls back because no MySQL server is listening on `127.0.0.1:3306`.
- `npm test --workspace=apps/web` - pass, 267 files passed / 2 skipped and 2505 tests passed / 4 skipped.
