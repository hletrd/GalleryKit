# Run-10 Cycle 19/100 Implementation Plan

Status: IMPLEMENTED + LOCAL GATES GREEN
Aggregate: `.context/reviews/_aggregate.md`
Date: 2026-07-08 KST
Start HEAD: `6efd737b3ad5791c662fded4801701992684e54d`

## Scope

This plan schedules the Cycle 19 findings that are correctness-critical, bounded, and safe to fix in this cycle. Every aggregate finding is either scheduled here or recorded in `cycle-19-2026-07-08-deferred.md`.

Repo rules read before scheduling/deferring: `CLAUDE.md`, `AGENTS.md`, `.context/plans/README.md`, `.context/plans/deferred-carry-forward.md`, current Cycle 18 plan/deferred files, root/app README files, and `docs/superpowers/*`. No `.cursorrules` or `CONTRIBUTING.md` exists in this checkout.

## Scheduled Work Packages

### WP1 - Fix Lightroom upload parse-slot cleanup

Findings: `AGG-C19-01`

Files:

- `apps/web/src/app/api/admin/lr/upload/route.ts`
- `apps/web/src/__tests__/lr-upload-route-behavior.test.ts`

Plan:

1. Move `markAdminAuthTokenUsed(request)` into the same `try/finally` region that releases `releaseMultipartParseSlot()`, or otherwise ensure every post-acquire throw releases the slot exactly once.
2. Add a focused route behavior test that makes `markAdminAuthTokenUsed` reject after slot acquisition, then proves the next LR upload can pass the parse-slot gate.

Acceptance:

- A token-use write failure cannot wedge later LR uploads with "Another Lightroom upload is being parsed".
- Regression test fails on the old route shape and passes on the fix.

### WP2 - Fix CLIP backfill attempt-budget pagination

Findings: `AGG-C19-02`

Files:

- `apps/web/scripts/backfill-clip-embeddings.ts`
- `apps/web/src/app/actions/embeddings.ts`
- focused tests under `apps/web/src/__tests__/`
- `CLAUDE.md` / `apps/web/README.md` only if wording needs adjustment after the code fix

Plan:

1. Decouple page-fetch termination from fixed batch size: track the actual fetch limit and do not treat a budget-limited short page as end-of-backlog while attempt budget remains.
2. Apply the same pagination semantics to the currently exported in-app embedding action so it cannot reintroduce the skip-heavy quiet-stop behavior if wired later.
3. Add behavior coverage for a remaining budget smaller than batch size, skip-only page, and later valid rows.

Acceptance:

- A skipped/missing-original partial page no longer causes a false "complete" result.
- The documented continuation message remains tied to attempt-budget exhaustion.

### WP3 - Strengthen narrow test/source contracts and comments

Findings: `AGG-C19-03`, `AGG-C19-06`, `AGG-C19-18`, `AGG-C19-25`

Files:

- `apps/web/src/lib/process-image.ts`
- `apps/web/src/app/[locale]/globals.css` or the relevant map component/style file
- `apps/web/src/app/[locale]/admin/(protected)/password/password-form.tsx`
- `apps/web/src/__tests__/password-form-a11y.test.ts`
- `apps/web/src/__tests__/deploy-script-contract.test.ts`

Plan:

1. Reword the upload helper comment so it says disk streaming avoids an additional full-size copy after framework `File` materialization, not that the end-to-end upload path is heap-safe.
2. Add scoped Leaflet CSS/custom hit-area styling so visible map controls satisfy the 44 px target policy without removing the accessible fallback list.
3. Focus/select the confirm-password field when the local mismatch validation fires, and extend the a11y test.
4. Extend deploy contract coverage so all manual native dependency pins in the Dockerfile are checked against `package-lock.json`, not only the current partial package subset.

Acceptance:

- The upload memory boundary is accurately documented in source comments.
- Leaflet controls meet the repo's 44 px touch-target policy in source/test coverage.
- Password mismatch moves focus to the invalid field.
- Docker native pin drift is caught for every manually installed native package.

### WP4 - Reconcile cycle plan ledgers

Findings: `AGG-C19-19`, `AGG-C19-20`, `AGG-C19-21`

Files:

- `.context/plans/README.md`
- `.context/plans/deferred-carry-forward.md`
- `.context/plans/cycle-18-2026-07-08-plan.md`
- `.context/plans/cycle-19-plan.md`
- `.context/plans/cycle-19-deferred.md`

Plan:

1. Mark Cycle 18 commit/push complete at `6efd737b` and separate the deploy evidence gap from commit/push status.
2. Make Cycle 19's dated plan/deferred pair the active current-cycle pair.
3. Mark older unindexed `cycle-19-plan.md` / `cycle-19-deferred.md` files as historical so they cannot collide with this cycle.
4. Refresh the carry-forward age label for the current cycle and add Cycle 19's deferred register to the lineage.

Acceptance:

- Future agents see one current Cycle 19 plan/deferred pair.
- Cycle 18 is not described as commit/push pending.
- The carry-forward register no longer presents the current table as `Age @ r10c4`.

### WP5 - Gate verification, commit, push, deploy

Findings: cycle quality/deploy requirement.

Plan:

1. Run all configured gates against the whole repo:
   - `npm run lint --workspace=apps/web`
   - `npm run lint:api-auth --workspace=apps/web`
   - `npm run lint:action-origin --workspace=apps/web`
   - `npm run lint:public-route-rate-limit --workspace=apps/web`
   - `npm run typecheck --workspace=apps/web`
   - `npm run build --workspace=apps/web`
   - `npm test --workspace=apps/web`
   - `npm run test:e2e --workspace=apps/web` because this cycle touches browser/UI behavior.
2. Fix blocking gate errors before commit/push.
3. Commit and push in signed, fine-grained Conventional Commits with gitmoji after `git pull --rebase`.
4. Run `npm run deploy` once after pushed gates are green because `DEPLOY_MODE=per-cycle`.

## Progress

- [x] WP1 fix Lightroom upload parse-slot cleanup
- [x] WP2 fix CLIP backfill attempt-budget pagination
- [x] WP3 strengthen narrow test/source contracts and comments
- [x] WP4 reconcile cycle plan ledgers
- [x] WP5 gate verification; signed commit/push/deploy are recorded by the cycle runner after this plan snapshot

## Gate Evidence

- Focused regression tests: `npm test --workspace=apps/web -- lr-upload-route-behavior.test.ts embeddings-action-behavior.test.ts backfill-clip-embeddings-reembed.test.ts password-form-a11y.test.ts map-thumb-wiring.test.ts deploy-script-contract.test.ts` -> 6 files passed, 55 tests passed.
- `npm run lint --workspace=apps/web` -> passed.
- `npm run lint:api-auth --workspace=apps/web` -> passed.
- `npm run lint:action-origin --workspace=apps/web` -> passed.
- `npm run lint:public-route-rate-limit --workspace=apps/web` -> passed.
- `npm run typecheck --workspace=apps/web` -> passed.
- `npm run build --workspace=apps/web` -> passed.
- `npm test --workspace=apps/web` -> first run exposed a stale source contract in `apps/web/src/__tests__/cycle-6-source-contracts.test.ts`; after updating the contract, rerun passed with 354 files passed / 2 skipped and 3297 tests passed / 4 skipped.
- `npm run test:e2e --workspace=apps/web` -> first startup attempt failed in the Playwright web-server build wrapper without a reproduced typecheck diagnostic; standalone `typecheck` and `build` reruns passed, and the retry passed with 45 browser tests passed / 2 skipped.

## Gate Fixes

- 1 error-level gate issue fixed: refreshed the stale Cycle 6 source-contract assertion so it now requires fixed SQL page size plus per-embedding `SEMANTIC_SCAN_LIMIT` guards instead of the removed budget-limited SQL page.
