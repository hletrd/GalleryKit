# Run-10 Cycle 13/100 Implementation Plan

Date: 2026-07-07
Review aggregate: `.context/reviews/_aggregate.md`
Status: Planned; implementation not started in Prompt 2.

## Repo Rules Read Before Planning

- `CLAUDE.md`
- `AGENTS.md`
- `.context/plans/README.md`
- Active `.context/plans/*.md`, `.context/plan/*.md`, current cycle review files, and docs inventory under `docs/`
- `.cursorrules` and `CONTRIBUTING.md`: not present

Deferral constraints applied:

- Repo explicitly documents a single web-instance topology and warns not to horizontally scale until process-local coordination is moved to shared state: `CLAUDE.md:244-247`.
- This cycle's destructive-safety constraint forbids CI/CD workflow, deployment pipeline, DNS/network/firewall/auth config changes unless explicitly confirmed. Therefore findings whose direct fix requires `.github/workflows/**`, deploy scripts, Dockerfile/deploy image topology, or nginx config are deferred in the matching deferred register.
- Per repo rules, all implementation commits must be GPG-signed, conventional-commit + gitmoji, pushed after `git pull --rebase`, and followed by the configured quality gates.

## Scheduled Work

### WP1 - E2E runtime origin isolation

Findings: `C13-AGG-01`.

Files:

- `apps/web/scripts/run-e2e-server.mjs`
- `apps/web/src/__tests__/request-origin.test.ts` or a new focused script/source test

Plan:

- Preserve build-time `BASE_URL` behavior for metadata generation.
- Ensure the runtime child started by `run-e2e-server.mjs` receives the local Playwright origin (`http://127.0.0.1:<port>` or configured host/port) instead of inheriting a non-local CI/public `BASE_URL`.
- Add a regression test proving the runtime env override is present and the same-origin predicate accepts local E2E origin when CI-style `BASE_URL` is set.

Acceptance:

- Targeted origin/run-e2e tests pass.
- Full quality gates pass.

### WP2 - Proxy topology checker accuracy

Findings: `C13-AGG-02`, `C13-AGG-03`, supports `C13-AGG-27`.

Files:

- `scripts/check-proxy-topology.mjs`
- `apps/web/src/__tests__/cycle12-ops-contracts.test.ts` or a new focused script-contract test

Plan:

- Make probe status classification allowlist-only.
- Update the semantic-search probe so it reaches the route's IP/rate-limit path, or narrow the script output so it no longer claims XFF validation it does not perform.
- Add source/behavior checks for the intended probe content type/status handling.

Acceptance:

- `node --check scripts/check-proxy-topology.mjs` passes.
- Focused ops contract test passes.
- Full quality gates pass.

### WP3 - DB child-process watchdog timeout correctness

Findings: `C13-AGG-18`, `C13-AGG-20`.

Files:

- `apps/web/src/app/[locale]/admin/db-actions.ts`
- New or existing Vitest coverage under `apps/web/src/__tests__/`

Plan:

- Separate "real child exited/closed" state from "watchdog cleanup requested" state.
- Ensure timeout path sends SIGTERM and still sends SIGKILL after the grace period unless the child actually exits/closes.
- Add a behavioral fake-child timer test for the timeout path, including the case where `onTimeout` invokes the returned clear function.

Acceptance:

- New watchdog behavior test fails against the old behavior and passes after the fix.
- Full quality gates pass.

### WP4 - DB pool initialization timeout disposal

Findings: `C13-AGG-19`, `C13-AGG-20`.

Files:

- `apps/web/src/db/index.ts`
- `apps/web/src/__tests__/db-pool-connection-handler.test.ts`

Plan:

- Destroy, rather than release, a connection whose per-connection initialization times out.
- Add a behavioral mocked-connection test proving timeout calls `destroy()` and not `release()`.

Acceptance:

- DB pool handler tests pass.
- Full quality gates pass.

### WP5 - Service-worker registration wiring test

Findings: `C13-AGG-32`.

Files:

- `apps/web/src/__tests__/service-worker*.test.ts` or a new source-contract test
- `apps/web/src/app/[locale]/layout.tsx` / provider component only if the test exposes a real wiring gap

Plan:

- Add a focused test that the registration component is still mounted from the app tree.
- Do not rewrite service worker behavior unless the test proves a current wiring defect.

Acceptance:

- Focused SW registration test passes.
- Full quality gates pass.

### WP6 - Analytics CSP endpoint

Findings: `C13-AGG-40`.

Files:

- `apps/web/src/lib/content-security-policy.ts`
- `apps/web/src/__tests__/content-security-policy.test.ts`

Plan:

- Add `https://www.google.com` to analytics-only `connect-src` when GA is configured.
- Keep ad/doubleclick hosts excluded.
- Add a unit test for the observed `www.google.com/g/collect` endpoint.

Acceptance:

- CSP tests pass.
- Full quality gates pass.

### WP7 - Public feature discoverability

Findings: `C13-AGG-41`, `C13-AGG-42`, `C13-AGG-44`.

Files:

- `apps/web/src/components/footer.tsx`
- `apps/web/src/components/search.tsx`
- `apps/web/src/components/nav-client.tsx`
- `apps/web/src/app/sitemap.ts` if appropriate for non-private timeline discovery
- Relevant i18n messages/tests

Plan:

- Add persistent footer affordances for Timeline and Map without over-promoting private/noindex content.
- Show visible desktop search copy while preserving the existing icon affordance on constrained widths.
- Align mobile header DOM/focus order with visual order.
- Add targeted source/UI tests where existing conventions allow.

Acceptance:

- Existing touch-target and i18n tests pass.
- Full quality gates pass.

### WP8 - Mobile similar photos surface

Findings: `C13-AGG-43`.

Files:

- `apps/web/src/components/photo-viewer.tsx`
- `apps/web/src/components/info-bottom-sheet.tsx`
- `apps/web/src/components/similar-photos.tsx`
- Relevant tests/messages

Plan:

- Pass enough semantic-search/image-size data into the mobile info sheet to render `<SimilarPhotos>` when production semantic search is enabled.
- Keep the feature hidden when semantic search is disabled/stubbed according to current product honesty rules.
- Add focused coverage that the mobile info sheet includes the similar-photo surface when enabled.

Acceptance:

- Focused component/source tests pass.
- Full quality gates pass.

### WP9 - Destructive confirmation specificity

Findings: `C13-AGG-48`.

Files:

- `apps/web/src/app/[locale]/admin/(protected)/tags/tag-manager.tsx`
- `apps/web/src/app/[locale]/admin/(protected)/categories/topic-manager.tsx`
- `apps/web/messages/en.json`
- `apps/web/messages/ko.json`
- Relevant source/i18n tests

Plan:

- Track the selected tag/category object or display name for delete dialogs.
- Interpolate the target name in title/description in both locales.
- Preserve existing confirm button copy and destructive semantics.

Acceptance:

- i18n key parity passes.
- Relevant admin/source tests pass.
- Full quality gates pass.

### WP10 - Current cycle plan/review index hygiene

Findings: `C13-AGG-24`, `C13-AGG-25`, `C13-AGG-28`.

Files:

- `.context/plans/README.md`
- `.context/plans/cycle-13-2026-07-07-plan.md`
- `.context/plans/cycle-13-2026-07-07-deferred.md`

Plan:

- Update the active current-cycle pointers to cycle 13.
- Ensure this plan cites the cycle 13 aggregate.
- Merge the duplicate runtime-site-config carry-forward reference if present in the active index without altering historical archive files.

Acceptance:

- Plan index points to cycle 13 active plan/deferred files.
- Full quality gates pass.

## Verification Plan

Run the required gates after implementation:

- `npm run lint --workspace=apps/web`
- `npm run lint:api-auth --workspace=apps/web`
- `npm run lint:action-origin --workspace=apps/web`
- `npm run lint:public-route-rate-limit --workspace=apps/web`
- `npm run typecheck --workspace=apps/web`
- `BASE_URL=https://gallery.atik.kr npm run build --workspace=apps/web`
- `npm test --workspace=apps/web`
- `npm run test:e2e --workspace=apps/web` only if the implemented browser-flow changes require browser coverage and can run without mutating the existing cycle 7/10 MySQL containers

## Progress

- [x] WP1 - E2E runtime origin isolation
- [x] WP2 - Proxy topology checker accuracy
- [x] WP3 - DB child-process watchdog timeout correctness
- [x] WP4 - DB pool initialization timeout disposal
- [x] WP5 - Service-worker registration wiring test
- [x] WP6 - Analytics CSP endpoint
- [x] WP7 - Public feature discoverability
- [x] WP8 - Mobile similar photos surface
- [x] WP9 - Destructive confirmation specificity
- [x] WP10 - Current cycle plan/review index hygiene
- [x] Required gates passed
- [ ] Signed commits pushed
- [ ] Per-cycle deploy completed
