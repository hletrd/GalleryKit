# Run-10 Cycle 20/100 Deferred Findings

Status: OPEN
Aggregate: `.context/reviews/_aggregate.md`
Date: 2026-07-08 KST

This register records every Cycle 20 aggregate finding not scheduled in `cycle-20-2026-07-08-plan.md`. Severity and confidence are preserved from the aggregate. Deferred work remains bound by repo policy: GPG-signed Conventional Commits with gitmoji, `git pull --rebase` before push, required gates, no force-push, no `--no-verify`, and the repo's Node/Next/React/TypeScript toolchain rules.

Repo rule basis for deferral: `.context/plans/README.md` defines the carry-forward age budget for deferred High/Medium findings; `.context/plans/deferred-carry-forward.md` is the committed register for open deferred rows. For topology/edge/security-ops items, `CLAUDE.md` documents the single web-instance/process-local topology and that public page throttling is applied at manually managed nginx edge config, not by per-iteration deploys. For product/distribution items, current repo docs identify `gallery.atik.kr` as this checkout's production target.

## Deferred Items

| ID | Severity / Confidence | Citation | Reason for deferral | Exit criterion |
|----|-----------------------|----------|---------------------|----------------|
| AGG-C20-04 | High / High | `apps/web/src/app/actions/images.ts:129-653`; `apps/web/src/app/api/admin/lr/upload/route.ts:84-634` | Shared ingest-service extraction is a broad architecture refactor on the hottest upload path. Current cycle closed concrete restore/SW/lint correctness gaps first. | Before adding upload-time privacy/metadata/processing fields, or the next confirmed browser/PAT drift. |
| AGG-C20-05 | High / High | `apps/web/src/app/actions/images.ts:129-263`; `apps/web/src/app/api/admin/lr/upload/route.ts:101-181`; `apps/web/src/app/[locale]/admin/db-actions.ts:400-714`; `apps/web/next.config.ts:111-119`; `apps/web/src/lib/upload-limits.ts:1-33` | Streaming multipart Route Handler migration crosses browser upload, PAT upload, and DB restore. It requires RSS measurement and a shared temp-file handoff design. | Production RSS/OOM incident during upload/restore, or approved streaming-ingress project. |
| AGG-C20-06 | High / High | `apps/web/src/db/index.ts:31-45`; `apps/web/src/lib/image-queue.ts:121-153`; `apps/web/src/lib/admin-backfill-runner.ts:106-143`; `apps/web/src/lib/process-image.ts:1205-1418` | Shared DB/CPU background-budget work changes queue/backfill concurrency contracts. Existing topology is single-instance and no live starvation evidence was collected this cycle. | Measured pool starvation during queue/backfill/search, or next queue/backfill architecture cycle. |
| AGG-C20-07 | Medium / High | `apps/web/src/app/actions/public.ts:247-329`; `apps/web/src/lib/data.ts:1574-1749`; `apps/web/src/lib/sql-like.ts:9-10` | Search-index redesign is a product/performance project. Current rate limits bound request count and no current live latency evidence was gathered. | Search latency rises, corpus grows materially, or full-text/search-index cycle is approved. |
| AGG-C20-08 | Medium / Medium | `apps/web/src/lib/smart-collections.ts:142-352`; `apps/web/src/app/[locale]/(public)/c/[slug]/page.tsx:17`; `apps/web/src/app/[locale]/(public)/c/[slug]/page.tsx:96-112`; `apps/web/src/lib/data.ts:1488-1551` | Public smart collections are DB-authored with no admin authoring UI yet; cost-class/materialization should ride the Collections product cycle. | Collections admin UI ships, broad public collection latency is measured, or materialization project is approved. |
| AGG-C20-09 | Medium / High | `apps/web/src/db/schema.ts:292-304`; `apps/web/src/lib/clip-embeddings.ts:22-48`; `apps/web/src/app/api/search/semantic/route.ts:263-311`; `apps/web/src/app/api/search/similar/[id]/route.ts:177-214` | Vector/ANN isolation is a larger performance/security-availability investment; current routes are bounded by mode gates, scan limits, and rate limits. | Semantic traffic/gallery size exceeds scan limits, measured CPU/RSS pressure, or vector-index/cache project approved. |
| AGG-C20-10 | Medium / High | `apps/web/src/app/[locale]/(public)/map/page.tsx:13-110`; `apps/web/src/lib/data.ts:1766-1816`; `apps/web/src/components/map/map-client.tsx:77-139`; `apps/web/src/db/schema.ts:123-131` | Map clustering/viewport APIs and spatial indexing are broader than the current restore/SW/lint recovery. | GPS-visible rows approach thousands, mobile map trace shows jank, or map clustering project is approved. |
| AGG-C20-11 | Low-Medium / High | `apps/web/src/components/map/map-client.tsx:120-137`; `apps/web/src/app/[locale]/(public)/map/page.tsx:80-110` | A11y improvement is contained but lower risk because the page already provides a skip link and accessible fallback list. | Next map-touching cycle, AT/keyboard user report, or a11y polish batch. |
| AGG-C20-12 | Medium / Medium | `apps/web/src/db/schema.ts:123-131`; `apps/web/src/lib/data.ts:761-783`; `apps/web/src/lib/data.ts:806-828`; `apps/web/src/lib/data.ts:918-939`; `apps/web/src/lib/data.ts:1498-1544` | Index change needs migration, production-sized query plans, and overlap/drop decision. No migration was otherwise needed this cycle. | Next schema/index migration cycle or measured listing/smart-collection filesort/temp-table cost. |
| AGG-C20-13 | Medium / High | `apps/web/src/lib/upload-tracker-state.ts:7-78`; `apps/web/src/lib/rate-limit.ts:78-109`; `apps/web/src/lib/rate-limit.ts:404-427`; `apps/web/src/lib/data.ts:13-63`; `apps/web/src/lib/single-writer-guard.ts:6-235`; `apps/web/src/instrumentation.ts:22-31` | Topology-bound security/correctness risk. `CLAUDE.md` documents single-instance support and process-local limits; fail-closed guard semantics require an operator/product decision. | Scale-out is attempted, singleton contention incident occurs, or shared-state/fail-closed topology project is approved. |
| AGG-C20-14 | Medium / High-Medium | `CLAUDE.md:247`; `CLAUDE.md:510-522`; `apps/web/nginx/default.conf:1-29`; `apps/web/nginx/default.conf:274-306`; `apps/web/deploy.sh:51-107`; `scripts/deploy-remote.sh:31-93` | Operator/live-host validation item. Repo policy explicitly says deploys do not apply host nginx. | Operator applies/verifies nginx zones, proxy topology changes, or app-layer public page limiter project is approved. |
| AGG-C20-15 | Medium / Medium | `apps/web/src/lib/data.ts:49-63`; `apps/web/src/lib/data.ts:1392-1407`; `apps/web/src/lib/data.ts:1830-1834`; `apps/web/src/app/[locale]/(public)/g/[key]/page.tsx:111-142`; `apps/web/src/app/actions/public.ts:518-559` | Shared-group counters are approximate analytics, not billing/audit data. Refactor should be paired with shared-group page/cache changes. | Counter drift bug report, shared-group cache refactor, or analytics consistency cycle. |
| AGG-C20-16 | Medium / Medium-High | `apps/web/src/db/schema.ts:10-40`; `apps/web/src/db/schema.ts:251-260`; `apps/web/src/app/actions/topics.ts:287-372`; `apps/web/src/__tests__/topic-slug-fk-registry.test.ts:1-79` | Natural-key migration to surrogate IDs is schema/product work with high blast radius; current rename fan-out is tested. | New slug-bearing store lands, rename bug observed, or topic schema migration cycle. |
| AGG-C20-17 | Medium / High | `apps/web/src/lib/image-queue.ts:112-113`; `apps/web/src/lib/image-queue.ts:320-324`; `apps/web/src/lib/image-queue.ts:767`; `apps/web/src/lib/image-queue.ts:1029-1041`; `apps/web/src/lib/image-queue.ts:1155-1156`; `apps/web/src/__tests__/image-queue-permanent-failure.test.ts:56-63` | Correctness/perf risk is process-local and needs an image-queue test harness for claim-exhaustion. Not a current data-loss issue. | Claim-exhaustion incident, next image-queue cycle, or queue bootstrap predicate growth observed. |
| AGG-C20-18 | Medium / Medium-High | `apps/web/src/app/actions/images.ts:259-269`; `apps/web/src/app/actions/images.ts:563-578`; `apps/web/src/app/api/admin/lr/upload/route.ts:160-188`; `apps/web/src/lib/upload-tracker.ts:19-33`; `apps/web/src/lib/upload-tracker-state.ts:70-78` | Refactor of currently covered upload quota paths. No active leak was found this cycle. | Upload quota leak observed, throwing cleanup introduced, or upload-flow refactor cycle. |
| AGG-C20-19 | Low / High | `CLAUDE.md:248`; `CLAUDE.md:649-650`; `apps/web/src/app/actions/admin-users.ts:79-92`; `apps/web/src/app/actions/admin-users.ts:194-218`; `apps/web/src/app/actions/auth.ts:216-253` | Product/security model decision; repo history explicitly defers 2FA/WebAuthn. | Admin compromise risk posture changes, step-up auth project approved, or roles/capabilities are added. |
| AGG-C20-20 | Low / High | `apps/web/src/lib/content-security-policy.ts:182-190`; `apps/web/src/lib/safe-json-ld.ts:14-19` | Defense-in-depth hardening; no current XSS sink found and removing inline styles needs framework/browser validation. | Framework-supported style nonce/hash path lands, style injection sink appears, or CSP hardening cycle approved. |
| AGG-C20-21 | Low / High | `.context/reviews/logs-cycle4/security-reviewer.log:158-159`; `.context/reviews/logs-cycle4/security-reviewer.log:19495-19496`; `README.md:152-153`; `CLAUDE.md:81-82`; `apps/web/.env.local.example:27-33` | Repository hygiene issue in historical redacted logs, not live secret exposure. Raw-log retention policy should be decided separately. | A secret scanner flags tracked logs, future log artifact includes unredacted secret-shaped output, or review-log retention policy is updated. |
| AGG-C20-24 | Medium / High | `apps/web/eslint.config.mjs:18-29`; `apps/web/package.json:13-15`; `.github/workflows/quality.yml:54-55` | Making warnings blocking can create broad cleanup churn; cycle focused on concrete high-confidence correctness fixes. | Next lint/config cleanup cycle, or a stale warning masks a real regression. |
| AGG-C20-25 | Medium / High | `apps/web/src/__tests__/touch-target-audit.test.ts:97-117`; `apps/web/src/__tests__/touch-target-audit.test.ts:188-243`; `apps/web/src/__tests__/touch-target-audit.test.ts:769-803`; `apps/web/src/components/ui/button.tsx:23-30` | Requires pruning historical budgets and adding primitive-level assertions without destabilizing touch-target audit noise. | Button primitive changes, touch-target false-green observed, or a11y audit cycle. |
| AGG-C20-26 | Medium / High | `apps/web/src/__tests__/i18n-key-parity.test.ts:13-20`; `apps/web/src/__tests__/i18n-key-parity.test.ts:135-168`; `apps/web/messages/en.json:156-177`; `apps/web/messages/ko.json:156-177` | Test-infrastructure improvement; no current placeholder mismatch was identified. | Next i18n/message edit cycle, placeholder bug, or i18n test-hardening cycle. |
| AGG-C20-27 | Medium / High | `apps/web/src/__tests__/cycle-20-source-contracts.test.ts:8-82`; `apps/web/src/__tests__/semantic-scan-limit-source.test.ts:42-77`; `apps/web/src/__tests__/load-more-source-contracts.test.ts:7-30`; `apps/web/src/components/search.tsx:163-281`; `apps/web/src/components/load-more.tsx:43-111` | Broad test-harness work across client behavior and source contracts. This cycle added behavior tests for the high-confidence fixed paths. | Next high-risk source-contract-only finding, or dedicated behavior-test hardening cycle. |
| AGG-C20-28 | High / High | `apps/web/src/__tests__/migrate-reconcile-coverage.test.ts:13-19`; `apps/web/src/__tests__/migrate-reconcile-coverage.test.ts:95-103`; `apps/web/src/__tests__/migrate-reconcile-coverage.test.ts:175-180`; `apps/web/scripts/migrate.js:348-493` | DB-backed schema-convergence gate needs disposable MySQL orchestration and may be CI/nightly-only. No migration was added this cycle. | Next migration/schema authoring cycle or reconcile/schema drift incident. |
| AGG-C20-29 | High / Medium | `apps/web/src/__tests__/db-restore.test.ts:47-136`; `apps/web/src/app/[locale]/admin/db-actions.ts:137-260` | Stub-binary child-process harness is substantial test infrastructure. The cycle fixed the concrete restore drain ordering issue first. | Next backup/restore cycle, child-process regression, or reusable spawn/fixture harness lands. |
| AGG-C20-30 | Medium / High | `apps/web/playwright.config.ts:72-77`; `.github/workflows/quality.yml:69-83`; `apps/web/e2e/admin.spec.ts:20-165`; `apps/web/e2e/nav-visual-check.spec.ts:40-86`; `apps/web/src/components/register-service-worker.tsx:13-23`; `apps/web/src/__tests__/sw-template-contract.test.ts:1-16`; `apps/web/scripts/build-sw.ts:27-43` | Cross-browser/admin/PWA/CLIP/visual matrix expansion increases runtime, fixtures, and CI complexity. | Browser-specific regression, admin workflow regression, SW/PWA incident, CLIP path change, or test-matrix project approved. |
| AGG-C20-31 | Medium / High | `apps/web/src/components/tag-filter.tsx:145-156` | Small UI fix but requires browser/DOM verification across responsive tag-filter behavior. Deferred to avoid mixing new frontend source changes after the source gate sweep. | Next frontend/UI cycle, mobile tag-filter bug report, or tag-filter component touch. |
| AGG-C20-32 | Low / Medium | `apps/web/src/components/tag-filter.tsx:11-19`; `apps/web/src/components/tag-filter.tsx:70-160` | Low DOM/hydration cost at current tag counts; redesign can ride the tag-filter closed-state fix. | Measured hydration/DOM cost or next tag-filter redesign. |
| AGG-C20-33 | Medium / High | `apps/web/src/components/image-manager.tsx:427-452`; `apps/web/src/components/image-manager.tsx:474-621` | Admin responsive redesign is product/UX scope and should be paired with broader admin IA work. | Mobile/tablet admin operation becomes priority, wrong-row action report, or admin redesign cycle. |
| AGG-C20-34 | Low-Medium / Medium | `apps/web/src/components/photo-viewer.tsx:111-135`; `apps/web/src/components/photo-viewer.tsx:757-811`; `apps/web/src/components/photo-viewer.tsx:975-1020` | Product/UX decision about default photo information density and hydration behavior. | Visitor/client misses download/color controls, or photo-page IA redesign cycle. |
| AGG-C20-35 | Low-Medium / Medium | `apps/web/src/site-config.json:1-12`; `apps/web/src/site-config.example.json:1-12`; `apps/web/scripts/ensure-site-config.mjs:12-40`; `README.md:60-72` | This checkout is the production deployment for `gallery.atik.kr`; changing tracked config semantics needs a distribution/product decision. | Before distributing as a template/package, or if copied installs publish wrong canonical URLs. |
| AGG-C20-36 | Medium / High-Low | `README.md:50`; `CLAUDE.md:169`; `CLAUDE.md:375-381`; `CLAUDE.md:505-523`; `apps/web/src/app/api/search/semantic/route.ts:186-311`; `apps/web/src/app/api/search/similar/[id]/route.ts:115-214`; `apps/web/src/lib/admin-backfill-runner.ts:106-143` | Live-host measurement and operator verification cannot be proven from repository files. Source runbooks remain accurate. | CLIP activation claimed, limits raised, capacity project starts, or host nginx verification is performed. |
| AGG-C20-37 | Low / High | `README.md:1-208`; `apps/web/src/site-config.json:1-12` | Product-documentation polish, not correctness or runtime safety. | README/product positioning pass. |

## Scheduled, Not Deferred

Scheduled in `cycle-20-2026-07-08-plan.md`: `AGG-C20-01`, `AGG-C20-02`, `AGG-C20-03`, `AGG-C20-22`, and `AGG-C20-23`.

## Age-Budget Check

New High rows deferred this cycle are broad architecture/performance/test-design items (`AGG-C20-04`, `AGG-C20-05`, `AGG-C20-06`, `AGG-C20-28`, `AGG-C20-29`), not direct unpatched data-loss/security bugs with a contained local fix. The direct high-confidence correctness/security gate issues (`AGG-C20-01`, `AGG-C20-02`) were scheduled and fixed; the revocable-photo privacy/cache issue (`AGG-C20-03`) was fixed. Topology and edge-security rows cite the repo's documented single-instance and nginx-edge operator boundaries in `CLAUDE.md`.
