# Cycle 93/100 Implementation Plan

Start HEAD: `2571d8a8c27e2d2a7bc95ed5e6a72e26487093dc`.
Review aggregate: `.context/reviews/cycle-93-2026-07-01/_aggregate.md`.

## Goal

Close safe, narrow Cycle 93 findings that can be handled with UI/a11y state, focused source-contract tests, E2E cleanup hardening, and release-ledger updates. Preserve larger schema, restore, route-harness, sitemap, coverage, keyboard interaction, and mobile redesign work in the deferred ledger.

## Scheduled Fixes

1. `C93-01`: close the stale Cycle 92 terminal ledger by recording that Cycle 93 started from user-provided deployed `master` HEAD `2571d8a8c27e2d2a7bc95ed5e6a72e26487093dc`, and update `.context/plans/README.md` / latest aggregate state.
2. `C93-02`: update `apps/web/src/components/load-more.tsx` so rate-limit, maintenance, invalid/error, and thrown failure paths update the existing polite live region. Add source-contract coverage in `apps/web/src/__tests__/load-more-source-contracts.test.ts`.
3. `C93-03`: update `apps/web/src/app/[locale]/admin/(protected)/tokens/tokens-client.tsx` so empty Lightroom token labels render inline field-associated validation with `aria-invalid`, `aria-describedby`, and an alert. Add source-contract coverage in `apps/web/src/__tests__/client-source-contracts.test.ts`.
4. `C93-04`: harden `apps/web/e2e/admin.spec.ts` so the GPS toggle is restored in `finally` after a successful first click.

## Deferred This Cycle

All non-scheduled confirmed findings are recorded in `.context/plans/cycle-93-2026-07-01-deferred.md`. No review finding is intentionally dropped.

## Validation

Run focused tests/checks first:

- `npm test --workspace=apps/web -- --run src/__tests__/load-more-source-contracts.test.ts src/__tests__/client-source-contracts.test.ts`

Then run required gates in exact order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `BASE_URL=https://gallery.atik.kr npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit. If changes are committed, create a GPG-signed Conventional Commit with gitmoji and Lore trailers, `git pull --rebase`, push, deploy with `npm run deploy`, and smoke:

- `curl -fsSIL https://gallery.atik.kr`
- `curl -fsS https://gallery.atik.kr/api/health`

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run focused tests.
- [x] Run required gates.
- [x] Commit, pull --rebase, push.
- [x] Deploy with `npm run deploy`.
- [x] Smoke production endpoints.

## Gate Evidence

- Focused test: `npm test --workspace=apps/web -- --run src/__tests__/load-more-source-contracts.test.ts src/__tests__/client-source-contracts.test.ts` - pass: 2 files passed, 16 tests passed.
- `npm run lint --workspace=apps/web` - pass.
- `npm run lint:api-auth --workspace=apps/web` - pass: both admin API routes OK.
- `npm run lint:action-origin --workspace=apps/web` - pass: all mutating server actions enforce same-origin provenance.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass: all scanned public route handlers covered or explicitly exempt.
- `npm run typecheck --workspace=apps/web` - pass: route types generated, app/scripts typechecks clean, 8 JavaScript script files checked.
- `BASE_URL=https://gallery.atik.kr npm run build --workspace=apps/web` - pass. Local sitemap generation logged the expected unavailable local MySQL fallback and exited successfully.
- `npm test --workspace=apps/web` - pass: 302 files passed, 2 skipped; 2843 tests passed, 4 skipped.

## Gate Fix Iterations

- `C93-02`: added live-region status updates for load-more rate-limit, maintenance, invalid/error, and thrown failure branches.
- `C93-03`: added inline field-associated validation for empty Lightroom token labels.
- `C93-04`: moved the GPS toggle E2E restore path into `finally` after the first click succeeds.

## Release Evidence

- Cycle 94 started from user-provided current deployed `master` HEAD `33eca7b5e4102bd5097777dbb926ee2cb94c6d71`.
- Local git state at Cycle 94 start: `HEAD == origin/master == 33eca7b5e4102bd5097777dbb926ee2cb94c6d71`.
- Signed commit: `33eca7b5` (`fix(a11y): ♿ make cycle 93 failures perceivable`).
