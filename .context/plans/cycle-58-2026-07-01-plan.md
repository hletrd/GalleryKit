# Cycle 58/100 Implementation Plan

Start HEAD: `51bca789`.
Review aggregate: `.context/reviews/cycle-58-2026-07-01/_aggregate.md`.

## Goal

Fix the actionable Cycle 58 findings, keep review/plan ledgers current, run all required gates, commit, push, and deploy with `npm run deploy` from the repo root.

## Scheduled Fixes

1. `C58-01`: close the Cycle 57 ledger by recording signed commit/origin/deployed-baseline evidence for `51bca789`, mark commit/push/deploy progress complete, and advance the plan index to Cycle 58.
2. `C58-02`: add behavior-level coverage for `PhotoPage` proving anonymous renders use only `getImageCached` while authenticated admin renders fetch `getImageForViewerCached(imageId, true)` only after a public row exists.
3. `C58-03`: parameterize the `strip_gps_on_upload` existing-image lock test over both `false -> true` and `true -> false`.
4. `C58-04`: make the histogram key-type tooltip trigger an explicit 44 px touch target and update touch-target audit documentation.

## Deferred This Cycle

No new Cycle 58 findings are deferred. Carry-forward items are recorded in `.context/plans/cycle-58-2026-07-01-deferred.md`.

## Regression Coverage

- Add `photo-page-fetch-behavior.test.ts` for the public/admin fetch split.
- Extend `settings-semantic-mode-action.test.ts` to cover both strip-GPS change directions.
- Keep `touch-target-audit.test.ts` at zero known histogram violations after the key-type tooltip touch target fix.

## Validation

Run required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then sign, `git pull --rebase`, push, and deploy with `npm run deploy`.

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run required gates.
- [ ] Commit, pull --rebase, push.
- [ ] Deploy with `npm run deploy`.

## Gate Evidence

- Focused regression gate: `npm test --workspace=apps/web -- photo-page-fetch-behavior.test.ts settings-semantic-mode-action.test.ts touch-target-audit.test.ts` - pass, 3 files / 26 tests.
- `npm run lint --workspace=apps/web` - pass.
- `npm run lint:api-auth --workspace=apps/web` - pass.
- `npm run lint:action-origin --workspace=apps/web` - pass.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
- `npm run typecheck --workspace=apps/web` - pass.
- `npm run build --workspace=apps/web` - pass. Local sitemap generation logged the expected unavailable local MySQL fallback and exited successfully.
- `npm test --workspace=apps/web` - pass: 287 files passed, 2 skipped; 2736 tests passed, 4 skipped.
- `git diff --cached --check` - pass.

## Gate Fix Iterations

- 0.
