# Cycle 21/100 Deferred Findings

Per review-plan-fix deferred rules: every cycle-21 aggregate finding is either scheduled in `cycle-21-2026-06-30-plan.md` or recorded here. Severity/confidence are preserved and not downgraded.

Repo rules consulted first: `CLAUDE.md`, `AGENTS.md`, `.context/plans/README.md`, current `.context/**` markdown inventory, and `docs/superpowers/**`. No `.cursorrules` or `CONTRIBUTING.md` exists. Deferred work remains bound by repo policy when reopened: GPG-signed commits, Conventional Commits + gitmoji, no force-push, no `--no-verify`, Node 24+/TS 6+.

Security, correctness, and data-loss findings are scheduled unless they are validation-only or scale/topology risks whose current production boundary is explicitly documented by the repo.

| ID | Finding | File:line | Original severity / confidence | Reason for deferral | Exit criterion |
|----|---------|-----------|--------------------------------|---------------------|----------------|
| AGG-C21-09 | Upload ingest remains duplicated across browser, Lightroom, and retry paths | `apps/web/src/app/actions/images.ts:114-531`, `apps/web/src/app/api/admin/lr/upload/route.ts:225-516`, `apps/web/src/app/actions/images.ts:1236-1282` | Medium / High | Broad architectural extraction on the upload hot path. Current parity is guarded by many behavior/source tests, and this cycle schedules the contained retry snapshot coverage gap (`AGG-C21-22`) instead of rewriting all ingest ownership at once. | Before adding any upload-time privacy/processing/metadata setting or when browser/LR/retry parity drifts again. |
| AGG-C21-10 | Image queue workers can pin most of the shared MySQL pool during Sharp work | `apps/web/src/lib/image-queue.ts:446-657`, `apps/web/src/db/index.ts:23-38` | Medium / High | Operational/performance risk mainly if `QUEUE_CONCURRENCY` is raised. Current default is one worker, and the repo explicitly documents single-instance operational limits in `CLAUDE.md` "Runtime topology": "The shipped Docker Compose deployment is a single web-instance / single-writer topology." | If `QUEUE_CONCURRENCY` is raised in production, pool acquisition warnings appear, or queue throughput tuning is next touched. |
| AGG-C21-11 | Single-process topology is documented but not enforced | `apps/web/src/lib/restore-maintenance.ts:1-56`, `apps/web/src/lib/upload-tracker-state.ts:7-20`, `apps/web/src/lib/data.ts:13-249` | Medium / High | Current production contract explicitly forbids horizontal scaling: `CLAUDE.md` says "The shipped Docker Compose deployment is a single web-instance / single-writer topology" and "do not horizontally scale the web service unless those coordination states are moved to a shared store." Implementing a distributed lease is a broader topology project beyond this cycle. | Before adapting compose/orchestration to more than one web process, or if a second web instance is detected in production. |
| AGG-C21-12 | Topic slug is a mutable natural key with manual fan-out | `apps/web/src/app/actions/topics.ts`, `apps/web/src/db/schema.ts`, `apps/web/src/lib/smart-collections.ts` | Medium / High | Schema redesign from mutable natural keys to surrogate ids is broad and migration-heavy. Current rename fan-out is documented in `CLAUDE.md` "Topic slug rename" and guarded by existing tests. No fresh child-table drift was found this cycle. | Before adding a new topic-slug-referencing store, or if topic rename fan-out misses a table/predicate again. |
| AGG-C21-13 | Public map ships and hydrates up to 10,000 markers at once | `apps/web/src/lib/data.ts:1649-1685`, `apps/web/src/app/[locale]/(public)/map/page.tsx:31-89`, `apps/web/src/components/map/map-client.tsx:118-140` | Low-Medium / High | Scale/UI performance issue needing production marker-count and INP/heap evidence. No current crash or correctness failure was observed. | If map marker counts approach thousands for common galleries, mobile map INP/heap traces degrade, or map code is next refactored. |
| AGG-C21-14 | Infinite masonry keeps every loaded card in client state and DOM | `apps/web/src/components/home-client.tsx:124-410`, `apps/web/src/components/load-more.tsx:116-132` | Medium / High | Virtualized masonry is a substantial UI architecture change with layout/accessibility risk. Current behavior is correct and this cycle already schedules higher-severity UI correctness/accessibility fixes. | If field INP/memory traces show deep-scroll pressure, or if masonry/list rendering is next redesigned. |
| AGG-C21-15 | Initial public gallery pages do grouped count work on every dynamic request | `apps/web/src/lib/data.ts:878-907`, public home/topic pages | Medium / High | Query-shape performance optimization; no current latency evidence was provided. Removing exact counts changes public UI contract and needs separate product validation. | If production traces show slow public first-page loads or exact total counts are next redesigned. |
| AGG-C21-16 | CSV export materializes the full export in server and browser memory | `apps/web/src/app/[locale]/admin/db-actions.ts:79-159` | Medium / High | Admin-triggered scale issue, not a confirmed data-loss/security bug. Streaming export route is a broader admin/API change and current export is capped. | If large CSV exports hit memory/timeout limits, or admin export code is next touched. |
| AGG-C21-17 | Admin analytics fans out multiple aggregate scans against the shared pool | `apps/web/src/app/[locale]/admin/(protected)/analytics/page.tsx:24-36`, `apps/web/src/lib/analytics-data.ts` | Low-Medium / Medium | Admin-only performance risk without observed pool starvation. | If admin analytics latency or DB pool warnings appear, especially on `window=all`. |
| AGG-C21-18 | Timeline/archive date filters are non-sargable on dynamic public pages | `apps/web/src/lib/data-timeline.ts`, timeline/year pages | Low / High | Low-priority query optimization; no current latency evidence. | If timeline/year pages become slow or date index strategy is next changed. |
| AGG-C21-20 | CLIP inference queue safety is source-contract tested, not behavior-tested | `apps/web/src/lib/clip-model.ts:65-160`, `apps/web/src/__tests__/clip-model-contract.test.ts:32-50` | Medium / High | Coverage quality gap requiring a test seam/fake model injection. Current production route behavior is guarded by bounded queue code and no failing behavior was reproduced. | When CLIP queue code is next refactored, abort bugs appear, or a test seam is introduced. |
| AGG-C21-21 | Real CLIP production behavior is opt-in and skipped by the default gate | `clip-semantic-integration.test.ts`, `clip-offline-load.test.ts`, `semantic-route-production.test.ts` | Low-Medium / High | Heavy model/weights tests are intentionally conditional because weights are not baked into the repo/image. Current docs already require operator seeding for production. | Before release/production semantic upgrades, add scheduled/pre-deploy evidence for seeded offline weights. |
| AGG-C21-24 | Admin image management remains a desktop table with horizontal panning on narrow screens | `apps/web/src/components/image-manager.tsx:424-590` | Medium / High | Responsive admin media-list redesign is a substantial UI surface. This cycle schedules higher-severity public/admin accessibility fixes first. | If admin mobile/small-tablet workflows become a priority or `ImageManager` is next redesigned. |
| AGG-C21-25 | Admin data tables repeat horizontal-scroll patterns instead of a responsive primitive | multiple admin table files | Low-Medium / Medium | Design-system refactor across many admin pages. No correctness failure; current horizontal scroll remains functional. | When creating a responsive admin data-surface component or touching multiple admin tables. |
| AGG-C21-38 | Audit retention purge remains a single unbounded DELETE | `apps/web/src/lib/audit.ts:97-122` | Low / High | Low-write operational risk; analytics retention is higher volume and already chunked. No audit GC lock issue observed. | If audit row volume grows, retention is lowered, or audit cleanup lock time appears. |
| AGG-C21-39 | Upload fallback serving validates by path, then reopens by path | `apps/web/src/lib/serve-upload.ts` | Low / Medium | Low-confidence TOCTOU risk on public derivative fallback. Backup download descriptor cleanup is scheduled first because it has a confirmed leak path. | When `serve-upload.ts` is next touched or if symlink/path-race concerns are reproduced. |

## Scheduled, Not Deferred

Scheduled in `cycle-21-2026-06-30-plan.md`: `AGG-C21-01`, `AGG-C21-02`, `AGG-C21-03`, `AGG-C21-04`, `AGG-C21-05`, `AGG-C21-06`, `AGG-C21-07`, `AGG-C21-08`, `AGG-C21-19`, `AGG-C21-22`, `AGG-C21-23`, `AGG-C21-26`, `AGG-C21-27`, `AGG-C21-28`, `AGG-C21-29`, `AGG-C21-30`, `AGG-C21-31`, `AGG-C21-32`, `AGG-C21-33`, `AGG-C21-34`, `AGG-C21-35`, `AGG-C21-36`, `AGG-C21-37`.

## Notes

- No confirmed security finding is deferred.
- Confirmed correctness/concurrency findings with cross-process or descriptor failure modes (`AGG-C21-05`, `AGG-C21-06`, `AGG-C21-07`) are scheduled.

## Gate Warning Notes

| ID | Finding | File:line | Original severity / confidence | Reason for deferral | Exit criterion |
|----|---------|-----------|--------------------------------|---------------------|----------------|
| C21-GATE-WARN-01 | `npm run build --workspace=apps/web` logged the designed sitemap fallback because local MySQL was unavailable (`ECONNREFUSED 127.0.0.1:3306`) while still exiting 0. | `apps/web/src/app/sitemap.ts` build-time data fetch | Low / High | Environment-bound warning in this workspace: no local DB service is running, and the route intentionally falls back to a homepage-only sitemap during build. Starting or modifying local MySQL is outside the requested safe code-change scope and not required for the gate to pass. | Re-open if CI/prod build has database connectivity and still falls back, or if the fallback warning becomes an error-level gate failure. |
