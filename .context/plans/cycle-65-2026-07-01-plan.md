# Cycle 65/100 Implementation Plan

Start HEAD: `ad1bc983b61d4251b7b29f8d3315b16c1c0f22f` at cycle start.
Review aggregate: `.context/reviews/cycle-65-2026-07-01/_aggregate.md`.

## Goal

Fix the safe, reviewable Cycle 65 findings, keep the review/plan ledgers current, run all required gates, commit, push, and deploy with `npm run deploy` from the repo root.

## Scheduled Fixes

1. `C65-01`: clear the session-pending Settings re-encode warning when a successful save leaves all backfill-relevant values equal to the pre-change baseline; extend source-contract coverage.
2. `C65-03`: add a 44 px `min-h-11` floor to both Radix Select scroll buttons; extend source-contract coverage.
3. `C65-04`: clarify that abbreviated `apps/web/README.md` model/backfill commands are local/dev helpers and that production sidecars must use the full `CLAUDE.md` mount/env runbooks.
4. `C65-05`: abort Similar Photos fetches when the disclosure closes, keep retry eligibility, and guard late responses; add source-contract coverage.

## Deferred This Cycle

See `.context/plans/cycle-65-2026-07-01-deferred.md`.

## Regression Coverage

- Extend `settings-backfill-warning-source.test.ts` to require the pending flag clear path.
- Extend `select-item-touch-target.test.ts` to cover `SelectScrollUpButton` and `SelectScrollDownButton`.
- Add a source-contract test for Similar Photos abort-on-close and late-response guards.
- Documentation change is verified by review and the existing build/lint gates.

## Validation

Run required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then sign, `git pull --rebase`, push, and deploy with `npm run deploy`.

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run required gates.
- [x] Commit, pull --rebase, push.
- [x] Deploy with `npm run deploy`.

## Gate Evidence

- Focused regression: `npm test --workspace=apps/web -- settings-backfill-warning-source select-item-touch-target similar-photos-abort-source cycle-29-source-contracts` - pass: 4 files passed, 19 tests passed.
- `npm run lint --workspace=apps/web` - pass.
- `npm run lint:api-auth --workspace=apps/web` - pass.
- `npm run lint:action-origin --workspace=apps/web` - pass.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
- `npm run typecheck --workspace=apps/web` - pass.
- `npm run build --workspace=apps/web` - pass. Local sitemap generation logged the expected unavailable local MySQL fallback and exited successfully.
- `npm test --workspace=apps/web` - pass: 293 files passed, 2 skipped; 2754 tests passed, 4 skipped.
- `git diff --cached --check` - pass.

## Terminal Evidence

- Signed commit: `d3e18c6f6f8db7f064a612a045a2033c1660ca95` (`fix(cycle-65): 🐛 keep gallery maintenance cues honest`), GPG signature verified locally on 2026-07-01.
- Push evidence: local `HEAD`, `origin/master`, and `origin/HEAD` all pointed at `d3e18c6f` at Cycle 66 start.
- Deploy evidence: Cycle 66 invocation states `d3e18c6f` was the current deployed `master` HEAD for `gallery.atik.kr` at the start of the cycle.

## Gate Fix Iterations

0.
