# Cycle 71/100 Implementation Plan

Start HEAD: `bf86f7c176ecb1ed542d851bfa0e76e2b9d73cd5`.
Review aggregate: `.context/reviews/cycle-71-2026-07-01/_aggregate.md`.

## Goal

Fix the safe, reviewable Cycle 71 findings, keep review/plan ledgers current, run all required gates, commit, push, and deploy with `npm run deploy` from the repo root.

## Scheduled Fixes

1. `C71-01`: add a durable restore-maintenance guard for operator sidecar backfills.
   - Implement a shared script-safe assertion in `restore-maintenance-durable`.
   - Call it in `backfill-clip-embeddings.ts` before advisory-lock acquisition, after lock acquisition, before each scan, and before embedding upsert.
   - Call it in `backfill-color-pipeline.ts` before advisory-lock acquisition, after lock acquisition, before candidate selection, and before DB batch writes.
   - Add source/unit contracts proving sidecars use the guard.
2. `C71-02`: replace hardcoded `ssh ubuntu@atik.kr` in the disk-full runbook with deploy-env-derived host/user/key instructions.
3. `C71-03`: add a commented `DB_SSL_CA=/path/to/ca.pem` line beside `DB_SSL` in `apps/web/.env.local.example`.
4. `C71-04`: update stale semantic snapshot wording and add focused behavior coverage that a `production` job snapshot does not invoke `embedImageReal` when current runtime gating heals production to disabled.
5. `C71-05`: mark Cycle 70 commit/push/deploy progress complete and update `.context/plans/README.md` so Cycle 71 is active and Cycle 70 is recent/completed.

## Deferred This Cycle

No new Cycle 71 findings are deferred.

Carry-forward items remain in `.context/plans/cycle-71-2026-07-01-deferred.md`.

## Regression Coverage

- Extend restore-maintenance durable tests with the script assertion helper.
- Add source-contract coverage that both sidecar backfills import and call the durable guard.
- Extend image-queue behavior coverage for runtime semantic gating overriding a stale `production` job snapshot.
- Documentation/env changes are covered by source contract and full gates.

## Validation

Run required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then sign, `git pull --rebase`, push, and deploy with `npm run deploy`.

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run required gates.
- [ ] Commit, pull --rebase, push.
- [ ] Deploy with `npm run deploy`.

## Gate Evidence

- Focused regression: `npm test --workspace=apps/web -- --run src/__tests__/restore-maintenance.test.ts src/__tests__/cycle-71-source-contracts.test.ts src/__tests__/image-queue-settings-wiring.test.ts src/__tests__/image-queue-embed-wiring.test.ts` - pass: 4 files, 24 tests. First focused run found two test-contract issues in new tests; fixed before full gates.
- `npm run lint --workspace=apps/web` - pass.
- `npm run lint:api-auth --workspace=apps/web` - pass: both admin API routes OK.
- `npm run lint:action-origin --workspace=apps/web` - pass: all mutating server actions enforce same-origin provenance.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
- `npm run typecheck --workspace=apps/web` - pass.
- `npm run build --workspace=apps/web` - pass. Local sitemap generation logged the expected unavailable local MySQL fallback and exited successfully.
- `npm test --workspace=apps/web` - pass: 295 files passed, 2 skipped; 2775 tests passed, 4 skipped.

## Gate Fix Iterations

1.
