# Cycle 12/100 Implementation Plan

Date: 2026-06-29
Source reviews: `.context/reviews/_aggregate.md` plus per-agent reports.
Status: IMPLEMENTED

Repo rules read before planning: `CLAUDE.md`, `AGENTS.md`, `.context/plans/README.md`, current `.context/reviews/*.md`, and `docs/superpowers/**`. No `.cursorrules` or `CONTRIBUTING.md` files exist in this repo.

## Scheduled Fixes

Security, privacy, correctness, and user-facing confirmed defects are scheduled here rather than deferred. Prompt 3 must fix every item in this table and update the Status column.

| ID | Aggregate finding | Files / surface | Planned fix | Status |
| --- | --- | --- | --- | --- |
| C12-01 | AGG-C12-01 journal-covered databases can skip schema repair | `apps/web/scripts/migrate.js`, migrations/tests | Add a schema-convergence assertion for legacy/reconciled schemas, covering defaults that currently rely on reconcile-only behavior. Repair the specific default/index drift the test exposes. | DONE |
| C12-02 | AGG-C12-02 LR upload parses oversized multipart before per-file cap | `apps/web/src/app/api/admin/lr/upload/route.ts`, upload-limit tests | Import/use `MAX_UPLOAD_FILE_BYTES`; reject declared LR request sizes above the file cap plus multipart overhead before `formData()`, then reject `fileEntry.size` immediately after parse. | DONE |
| C12-03 | AGG-C12-03 smart-collection offline cache can outlive revoke/delete | `apps/web/public/sw.template.js`, `apps/web/public/sw.js`, SW tests | Treat public smart-collection pages as revocable HTML that bypasses the offline HTML cache. | DONE |
| C12-04 | AGG-C12-04 map offline cache can preserve GPS markers | `apps/web/public/sw.template.js`, `apps/web/public/sw.js`, SW tests | Treat the public map route as privacy-sensitive HTML that bypasses the offline HTML cache. | DONE |
| C12-05 | AGG-C12-05 deleted image derivatives remain in SW cache | `apps/web/public/sw.template.js`, `apps/web/public/sw.js`, SW tests | Evict cached image derivatives and metadata when HEAD/GET revalidation returns 404/410. | DONE |
| C12-06 | AGG-C12-14 DB backup/restore CLI TLS does not verify identity | `apps/web/src/lib/mysql-cli-ssl.ts`, DB action tests/docs | Add verifying TLS mode support for MySQL CLI calls with CA env/config where available; fail closed for remote DB hosts when verification is requested but impossible. | DONE |
| C12-07 | AGG-C12-15 / AGG-C12-36 nginx edge/header guidance is unsafe or drifting | `apps/web/nginx/default.conf`, `CLAUDE.md`, nginx tests | Align nginx config/docs around trusted edge assumptions, forwarded host handling, and public-edge TLS caveats. | DONE |
| C12-08 | AGG-C12-16 plaintext backups at rest need explicit boundary | DB backup UI/runbook docs/messages | Document plaintext-at-rest backup behavior and operator storage/encryption responsibility where admins encounter backup/restore guidance. | DONE |
| C12-09 | AGG-C12-17 Atom attribution wording is stale | `apps/web/src/lib/data.ts`, privacy tests, docs | Replace stale per-entry public author wording with the current feed-level author privacy invariant. | DONE |
| C12-10 | AGG-C12-19 auth server actions need behavior locks | `apps/web/src/app/actions/auth.ts`, auth tests | Add behavior-level tests for hostile-origin login/logout/password paths and secure-cookie/protocol handling where source tests are currently the only guard. | DONE |
| C12-11 | AGG-C12-26 shared-group view recording can be forged by id | `apps/web/src/app/[locale]/(public)/g/[key]/page.tsx`, `apps/web/src/app/actions/public.ts`, tests | Pass the share key with the view action and verify it against the group id before writing durable analytics. | DONE |
| C12-12 | AGG-C12-27 sidecar backfill warning is stale | `apps/web/scripts/backfill-color-pipeline.ts`, docs/tests if present | Update the warning/comment to describe the current shared advisory-lock behavior. | DONE |
| C12-13 | AGG-C12-29 privacy guards need column-origin coverage | `apps/web/src/lib/data.ts`, `apps/web/src/lib/search-enrichment-fields.ts`, privacy/source tests | Add a source-level privacy regression that prevents direct sensitive-column references from public selectors beyond the approved public-safe set. | DONE |
| C12-14 | AGG-C12-32 semantic search can materialize oversized body without length | `apps/web/src/app/api/search/semantic/route.ts`, semantic route tests | Reject missing `Content-Length` for semantic POST requests after chunked transfer is already disallowed, or add a bounded reader with coverage. Prefer rejection for this small JSON endpoint. | DONE |
| C12-15 | AGG-C12-33 / 34 / 35 docs/comment drift | `apps/web/src/lib/caption-generator.ts`, `CLAUDE.md`, `privacy-fields.test.ts` | Correct Florence/onnxruntime, effective base URL, and stale line-reference comments. | DONE |
| C12-16 | AGG-C12-37 privacy page nested `main` landmark | public layout/privacy page | Remove the nested landmark by changing the privacy page wrapper or layout ownership. | DONE |
| C12-17 | AGG-C12-39 / 40 product copy drift | `README.md`, `CLAUDE.md`, nginx/docs | Clarify Lightroom upload API versus bundled plugin, semantic-search scale caveats, Firefox display limitation wording, and self-hosting nginx assumptions. | DONE |

## Deferred Ledger

Deferred findings from this cycle are recorded in `.context/plans/cycle-12-2026-06-29-deferred.md`. Prompt 3 must preserve that ledger and may update an item only if it is fixed.

## Verification Plan

Run targeted tests for touched surfaces first, then every configured gate:

- `npm run lint --workspace=apps/web`
- `npm run lint:api-auth --workspace=apps/web`
- `npm run lint:action-origin --workspace=apps/web`
- `npm run lint:public-route-rate-limit --workspace=apps/web`
- `npm run typecheck --workspace=apps/web`
- `npm run build --workspace=apps/web`
- `npm test --workspace=apps/web`

After all scheduled fixes are committed, pushed, and gates are green, run `npm run deploy` once from repo root because this cycle is per-cycle deploy mode.

## Commit / Deploy Plan

Use fine-grained signed commits with Conventional Commit + gitmoji. Pull with rebase before each push. Do not deploy until all scheduled fixes and configured gates are complete.
