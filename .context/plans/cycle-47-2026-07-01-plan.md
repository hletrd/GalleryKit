# Cycle 47/100 Implementation Plan

Start HEAD: `ab38f260`.
Review aggregate: `.context/reviews/cycle-47-2026-07-01/_aggregate.md`.

## Goal

Close all six Cycle 47 findings with narrow, reversible changes, then run the required gates, commit, push, and deploy with `npm run deploy`.

## Scheduled Fixes

1. `C47-SW-01`: refresh the cached derivative response's `sw-cached-at` header on successful `HEAD 304` validation, regenerate `public/sw.js`, and pin the behavior in `sw-template-contract.test.ts`.
2. `C47-BF-01`: add a source-contract test proving the sidecar production loop passes the DB-backed `rowExists` callback into `reprocessRow()`.
3. `C47-DOC-01`: record that Cycle 47 started from current deployed `master` HEAD `ab38f260`, mark Cycle 46 terminal progress complete, and update the plan index.
4. `C47-UI-01`: render admin image-table gamut and HDR indicators independently so BT.709/unknown HDR sources still show the HDR audit badge.
5. `C47-A11Y-01`: include P3 status in the masonry card link accessible name when the visible P3 badge renders.
6. `C47-IMG-01`: make failed-image retry clear failure state with the same failed-state predicate used for selection, and avoid restoring failed state over rows that are no longer unprocessed retry candidates.

## Deferred This Cycle

No new Cycle 47 findings are deferred. Prior carry-forward deferrals are recorded in `.context/plans/cycle-47-2026-07-01-deferred.md`.

## Regression Coverage

- Service-worker source contracts for 304 freshness-marker refresh and generated `sw.js` parity.
- Cycle 47 source contracts for admin HDR independence and sidecar `rowExists` wiring.
- Existing failed-image retry source contract extended to require conditional retry updates.
- Existing Cycle 22 accessibility source contract extended to require P3 status in the card link accessible name.

## Validation

Run the required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then sign, `git pull --rebase`, push, and deploy with `npm run deploy` from the repo root.

## Progress

- [x] Review findings aggregated.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Add focused regression/source-contract coverage.
- [x] Run required gates.
- [ ] Commit, push, deploy.

## Gate Evidence

- `npm run lint --workspace=apps/web` - pass
- `npm run lint:api-auth --workspace=apps/web` - pass
- `npm run lint:action-origin --workspace=apps/web` - pass
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass
- `npm run typecheck --workspace=apps/web` - pass
- `npm run build --workspace=apps/web` - pass (local sitemap generation fell back to homepage-only because MySQL was not running on `127.0.0.1:3306`; build exited 0)
- `npm test --workspace=apps/web` - pass, 2706 passed / 4 skipped
