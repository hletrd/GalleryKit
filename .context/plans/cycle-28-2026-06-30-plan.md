# Cycle 28/100 Implementation Plan

Date: 2026-06-30 KST  
Review source: `.context/reviews/_aggregate.md`  
Status: implementation complete; gates green; commit/deploy pending

## Repo Rules Read Before Planning

Read order satisfied for deferral policy: `CLAUDE.md`, `AGENTS.md`, `.context/` review/plan files, lookup for `.cursorrules` and `CONTRIBUTING.md` (none present), and `docs/` policy/history files. Repo rules require signed Conventional Commit + gitmoji commits, pull-rebase before push, all configured gates, and per-cycle deploy with `npm run deploy` after green pushed changes.

Every aggregate finding is either scheduled below or explicitly deferred in `cycle-28-2026-06-30-deferred.md`. Security, privacy, correctness, data-consistency, and data-loss/privacy-leak risks are scheduled here unless they are purely operational/manual-validation findings with no repo-code action this cycle.

## Scheduled Fixes

### P28-01 - Restore-aware background DB side-effect drain

- Findings: `AGG-C28-01`, `AGG-C28-02`, `AGG-C28-03`
- Original severity/confidence: Medium / High; Medium / Medium; Low / Medium
- Files: `apps/web/src/app/actions/public.ts`, `apps/web/src/lib/audit.ts`, `apps/web/src/app/[locale]/admin/db-actions.ts`, restore/public-action tests
- Plan: add a small restore-aware background DB write tracker for public analytics and audit writes. New writes no-op or avoid scheduling while restore maintenance is active; restore preparation drains in-flight analytics/audit promises before import. Add a public-page maintenance helper for DB-backed public SSR routes so restore maintenance returns a no-store maintenance state rather than querying during import.
- Acceptance: tests prove restore waits on delayed analytics/audit writes, restore success audit is not silently detached, and representative public pages short-circuit while maintenance is active.

### P28-02 - Fix upload audit accounting for RAW rejects

- Findings: `AGG-C28-04`
- Original severity/confidence: Low / High
- Files: `apps/web/src/app/actions/images.ts`, `apps/web/src/__tests__/images-actions.test.ts`
- Plan: include `rawRejectedCount` in upload audit failure metadata and, if practical, record raw rejection count separately for forensic clarity.
- Acceptance: mixed success-plus-RAW upload test proves action return values and audit metadata agree.

### P28-03 - Constrain standalone trace output and marker path scope

- Findings: `AGG-C28-05`
- Original severity/confidence: Medium / High local, Medium production
- Files: `apps/web/src/lib/restore-maintenance-durable.ts`, `apps/web/src/instrumentation.ts`, build/source-contract tests, possibly `apps/web/next.config.ts`
- Plan: make restore-maintenance marker access less likely to trigger whole-project NFT tracing, then add a post-build/source contract that rejects `.next/standalone/apps/web/{data,public/uploads,public/resources,src/__tests__,e2e}`.
- Acceptance: `npm run build --workspace=apps/web` has no unexpected whole-project trace warning, or the contract at least fails loudly if sensitive/runtime-irrelevant paths are traced.

### P28-04 - Harden private original storage boundaries

- Findings: `AGG-C28-15`, `AGG-C28-16`
- Original severity/confidence: Low now / High, Medium if integrated; Low / High
- Files: `apps/web/src/lib/storage/local.ts`, `apps/web/src/lib/storage/types.ts`, `apps/web/src/lib/upload-paths.ts`, `apps/web/src/lib/process-image.ts`, `apps/web/scripts/entrypoint.sh`, storage/upload-path tests
- Plan: make quarantined local storage route `original/*` to the private original root or reject original keys until full integration, stop creating `UPLOAD_ROOT/original`, and enforce `0700` on private original directories across normal runtime and deploy paths.
- Acceptance: tests/source contracts prove original storage is private, `getUrl('original/...')` cannot expose originals, and directory creation applies private mode/chmod.

### P28-05 - Align CLIP sidecar safety, retry, and operator docs

- Findings: `AGG-C28-22`, `AGG-C28-25`, `AGG-C28-26`
- Original severity/confidence: Medium / High; Medium / High; Low / High
- Files: `apps/web/scripts/backfill-clip-embeddings.ts`, `CLAUDE.md`, `apps/web/README.md` if needed, focused tests/source-contracts
- Plan: make production sidecar backfill require the same effective production env gate as runtime, update the script header sidecar example to mount `/app/data`, and retry/log failed IDs so transient per-row failures are not silently skipped until a new process.
- Acceptance: tests/source checks prove stored `production` without `SEMANTIC_SEARCH_ALLOW_PRODUCTION=true` does not run production sidecar, docs include the originals mount, and failed IDs are retried or explicitly reported.

### P28-06 - Replace semantic ranking source-only proof with behavior coverage

- Findings: `AGG-C28-17`
- Original severity/confidence: Medium / High
- Files: `apps/web/src/__tests__/semantic-search-route.test.ts`, possibly semantic route mocks
- Plan: add a stub-mode behavior test with non-normalized candidates where dot product and cosine produce opposite ordering.
- Acceptance: test fails if stub mode ranks by magnitude/dot product instead of cosine.

### P28-07 - Fix admin UI validation and dense table responsiveness

- Findings: `AGG-C28-28`, `AGG-C28-29`
- Original severity/confidence: Medium / High; Medium / High
- Files: `apps/web/src/components/image-manager.tsx`, `apps/web/src/app/[locale]/admin/(protected)/settings/settings-client.tsx`, locale messages/tests
- Plan: add a deliberate min-width/overflow contract to the image manager table and add field-level slideshow interval validation with `aria-invalid`, linked error text, and localized copy.
- Acceptance: source/UI tests cover the table width contract and invalid slideshow interval error association.

### P28-08 - Fix privacy/onboarding and public-facing product copy

- Findings: `AGG-C28-31`, `AGG-C28-32`, `AGG-C28-33`, `AGG-C28-34`, `AGG-C28-35`
- Original severity/confidence: High / High; High / High; Medium / High; Medium / High; Medium / Medium
- Files: `README.md`, `apps/web/README.md`, `apps/web/messages/en.json`, `apps/web/messages/ko.json`, `apps/web/src/app/[locale]/admin/(protected)/categories/topic-manager.tsx`, `apps/web/src/app/[locale]/admin/(protected)/settings/settings-client.tsx`, `apps/web/src/site-config.json`
- Plan: make first-run docs send operators to privacy/GPS settings before real uploads, rename/help the map visibility toggle so GPS publication is explicit, make stock SEO/default branding less misleading for the live config, temper color-fidelity claims, and clarify Stub semantic search as test mode rather than public production search.
- Acceptance: English/Korean key parity remains green, copy surfaces state consequences at the decision point, and README/site-config no longer overpromise privacy/color/semantic behavior.

### P28-09 - Correct low-risk documentation/comment mismatches

- Findings: `AGG-C28-23`, `AGG-C28-24`
- Original severity/confidence: Low / High; Low / Medium
- Files: `apps/web/src/lib/process-image.ts`, `apps/web/src/__tests__/process-image-max-input-pixels-env.test.ts`, `CLAUDE.md`
- Plan: correct `256e6` comments/test title and rephrase deploy-target prose to the configured deploy host.
- Acceptance: comments/test names match assertions and deploy docs no longer hardcode the host contrary to policy.

## Deferred Findings

Deferred in `cycle-28-2026-06-30-deferred.md`: `AGG-C28-06`, `AGG-C28-07`, `AGG-C28-08`, `AGG-C28-09`, `AGG-C28-10`, `AGG-C28-11`, `AGG-C28-12`, `AGG-C28-13`, `AGG-C28-14`, `AGG-C28-18`, `AGG-C28-19`, `AGG-C28-20`, `AGG-C28-21`, `AGG-C28-27`, `AGG-C28-30`, `AGG-C28-R01`, `AGG-C28-R02`, `AGG-C28-R03`.

## Prompt 3 Execution Notes

- Use Ralph-style grounding before edits: write a context snapshot under `.omx/context/`.
- Run every configured gate: `npm run lint --workspace=apps/web`, `npm run lint:api-auth --workspace=apps/web`, `npm run lint:action-origin --workspace=apps/web`, `npm run lint:public-route-rate-limit --workspace=apps/web`, `npm run typecheck --workspace=apps/web`, `npm run build --workspace=apps/web`, `npm test --workspace=apps/web`.
- Commit/push signed Conventional Commit + gitmoji commits after green gates and `git pull --rebase`.
- Per-cycle deploy command after green pushed HEAD: `npm run deploy`.

## Progress

- [x] Prompt 1 aggregate review written.
- [x] Cycle 26 and 27 completed plan/deferred files archived.
- [x] Plan and deferred register written.
- [x] P28-01 implemented.
- [x] P28-02 implemented.
- [x] P28-03 implemented.
- [x] P28-04 implemented.
- [x] P28-05 implemented.
- [x] P28-06 implemented.
- [x] P28-07 implemented.
- [x] P28-08 implemented.
- [x] P28-09 implemented.
- [x] Deferred findings preserved with original severity/confidence and exit criteria.
- [x] Focused validation green: `npm test --workspace=apps/web -- background-db-writes storage-local upload-paths images-actions semantic-search-route restore-maintenance cycle-28-source-contracts process-image-max-input-pixels-env` (85 tests).
- [x] Full gate list green: lint, api-auth lint, action-origin lint, public route rate-limit lint, typecheck, build, and full Vitest.
- [x] Gate warning recorded: local sitemap DB fallback in `cycle-28-2026-06-30-deferred.md`.
- [ ] Signed commit(s) pushed.
- [ ] Per-cycle deploy complete.
