# Run-10 Cycle 21/100 Implementation Plan

Status: IMPLEMENTED; full gates green; commit/push/deploy pending
Aggregate: `.context/reviews/_aggregate.md`
Date: 2026-07-08 KST
Review HEAD: `45b32d1db373e03d82a29511f53832051c770880`

## Scope

This plan schedules contained Cycle 21 findings that can be fixed and verified in this cycle. Every aggregate finding not scheduled here is recorded in `cycle-21-2026-07-08-deferred.md` with original severity/confidence, citations, deferral reason, and exit criterion.

Repo rules read before scheduling/deferring: `CLAUDE.md`, `AGENTS.md`, `.context/plans/README.md`, `.context/plans/deferred-carry-forward.md`, the current Cycle 20 plan/deferred pair, and `docs/superpowers/**`. No `.cursorrules` or `CONTRIBUTING.md` exists in this checkout.

## Scheduled Work Packages

### WP1 - Enforce mutation-barrier acquired checks before mutations

Findings: `AGG-C21-01`

Files:

- `apps/web/scripts/check-action-origin.ts`
- `apps/web/src/__tests__/check-action-origin.test.ts`

Plan:

1. Add a negative fixture where a mutation runs before the acquired-state check.
2. Change `bodyAcquiresAdminMutationSlot()` so the early-return pattern only passes when the next executable statement after the `using` slot declaration is the `if (!slot.acquired) return/throw` gate.
3. Keep the existing positive `if (slot.acquired) { ... }` shape accepted.
4. Run `npm run lint:action-origin --workspace=apps/web` and the targeted scanner tests.

Acceptance:

- A mutation before `if (!mutationSlot.acquired)` fails the lint gate.
- Existing legitimate action shapes still pass.

Status: implemented; focused scanner tests and `lint:action-origin` passed.

### WP2 - Make image file cleanup durable after row deletion

Findings: `AGG-C21-02`

Files:

- `apps/web/src/db/schema.ts`
- `apps/web/drizzle/0030_pending_file_deletions.sql`
- `apps/web/drizzle/meta/_journal.json`
- `apps/web/scripts/migrate.js`
- `apps/web/src/lib/pending-file-deletions.ts`
- `apps/web/src/app/actions/images.ts`
- focused tests under `apps/web/src/__tests__/`

Plan:

1. Add a `pending_file_deletions` ledger keyed by deleted image filename set, with attempts, last error, and timestamps.
2. Insert a ledger row before deleting image rows, then delete the DB row and attempt cleanup through a shared helper.
3. Remove the ledger row only after every original/derivative cleanup succeeds; otherwise preserve the row with failure details so a later retry can finish cleanup.
4. Wire single and batch image deletes through the helper.
5. Mirror the schema in `reconcileLegacySchema`.
6. Add tests that prove a cleanup failure is recorded durably instead of being only logged after row deletion.

Acceptance:

- File cleanup failures after DB deletion leave durable retry state.
- Successful cleanup removes the ledger row.
- Migration journal `when` is strictly greater than the current max.

Status: implemented; pending-file-deletion source contracts cover migration/reconcile and delete ordering.

### WP3 - Centralize image permanent-failure marking and cap enforcement

Findings: `AGG-C21-06`

Files:

- `apps/web/src/lib/image-queue.ts`
- `apps/web/src/__tests__/image-queue-permanent-failure*.test.ts`

Plan:

1. Extract `markPermanentlyFailed(state, id)` that adds the ID, enforces `MAX_PERMANENTLY_FAILED_IDS`, and cleans `claimRetryCounts`, `retryCounts`, and `lastErrors` for evicted IDs.
2. Use the helper in both normal failure and claim-exhaustion branches.
3. Add a test/source contract that prevents direct `permanentlyFailedIds.add(...)` drift or exercises the claim-exhaustion helper path.

Acceptance:

- Every permanent-failure add uses the shared cap/cleanup helper.
- Claim-exhaustion cannot grow the set beyond the cap.

Status: implemented; production permanent-failure add sites route through the shared helper.

### WP4 - Add an index for pipeline backfill candidate scans

Findings: `AGG-C21-15`

Files:

- `apps/web/src/db/schema.ts`
- `apps/web/drizzle/0030_pending_file_deletions.sql` or the next migration file if WP2 uses `0030`
- `apps/web/drizzle/meta/_journal.json`
- `apps/web/scripts/migrate.js`
- `apps/web/src/__tests__/migrate-reconcile-coverage.test.ts`

Plan:

1. Add an `images(processed, pipeline_version, id)` index for backfill candidate discovery.
2. Include it in the same migration batch as WP2 if one migration can safely contain both DDL changes.
3. Mirror it in `reconcileLegacySchema`.
4. Add/update schema source coverage for the index.

Acceptance:

- Drizzle schema, migration SQL, journal, and reconcile mirror all agree on the index.

Status: implemented; schema, migration, reconcile, and source contracts include the index.

### WP5 - Stop parsing persisted MySQL datetimes with JavaScript `Date`

Findings: `AGG-C21-25`

Files:

- `apps/web/src/lib/mysql-datetime.ts` or a new adjacent parser helper
- `apps/web/src/lib/data-timeline.ts`
- `apps/web/src/app/[locale]/(public)/timeline/page.tsx`
- `apps/web/src/components/on-this-day-widget.tsx`
- `apps/web/src/__tests__/data-timeline.test.ts` or focused parser tests

Plan:

1. Add a shared parser that extracts year/month/day from persisted `YYYY-MM-DD HH:mm:ss` strings without `Date.parse`.
2. Replace archive/timeline/on-this-day grouping/labeling uses of `new Date(capture_date)`.
3. Add invalid-string and valid MySQL datetime tests.

Acceptance:

- Timeline/year/on-this-day grouping uses deterministic string parsing.
- Tests cover invalid and valid storage formats.

Status: implemented; deterministic parser coverage added and timeline/on-this-day call sites updated.

### WP6 - Put root operator scripts under syntax/quality checks

Findings: `AGG-C21-30`

Files:

- `apps/web/scripts/check-js-scripts.mjs`
- `scripts/check-proxy-topology.mjs`
- possibly a focused test under `apps/web/src/__tests__/`

Plan:

1. Extend the JS script syntax gate to include root `scripts/*.mjs`, or add an equivalent root script check invoked by the existing quality path.
2. Add a small fixture/test proving `scripts/check-proxy-topology.mjs` remains parseable and has basic argument handling coverage where feasible.

Acceptance:

- `npm run typecheck --workspace=apps/web` or its script sub-gate syntax-checks the root operator checker.

Status: implemented; root `scripts/*.mjs` files are covered by `typecheck:scripts`.

### WP7 - Fix bounded docs, i18n, and accessibility defects

Findings: `AGG-C21-31`, `AGG-C21-32`, `AGG-C21-33`, `AGG-C21-34`, `AGG-C21-35`, `AGG-C21-36`

Files:

- `README.md`
- `AGENTS.md`
- `CLAUDE.md`
- `apps/web/messages/en.json`
- `apps/web/messages/ko.json`
- `apps/web/src/components/map/map-client.tsx`
- `apps/web/src/components/image-manager.tsx`
- focused tests where existing coverage should pin the behavior

Plan:

1. Correct the `DB_SSL_CA` README snippet to include runtime, Drizzle Kit, and backup/restore CLI TLS.
2. Correct public route rate-limit docs to say public App Router route handlers under `apps/web/src/app/**/route.*`.
3. Clarify that `OG_PHOTO_MAX_BYTES` caps fetched source candidates unless a final-output cap is added.
4. Add missing `common.cancel` and `common.tryAgain` locale keys or switch call sites to existing keys.
5. Give map markers useful accessible names.
6. Remove duplicate checkbox accessible text in the admin image manager.

Acceptance:

- Admin settings/tokens no longer emit missing-message keys.
- Map marker and checkbox accessibility snapshots are no longer misleading/duplicated.
- Docs match current scanner/runtime behavior.

Status: implemented; docs, locale keys, map marker labels, and checkbox labels updated.

### WP8 - Repair current release/deferred ledgers

Findings: `AGG-C21-17`, `AGG-C21-18`

Files:

- `.context/plans/cycle-20-2026-07-08-plan.md`
- `.context/plans/README.md`
- `.context/plans/deferred-carry-forward.md`
- `.context/plans/cycle-21-2026-07-08-plan.md`
- `.context/plans/cycle-21-2026-07-08-deferred.md`

Plan:

1. Mark the Cycle 20 terminal docs/deploy status honestly: either record supersession by Cycle 21 deploy or state no committed evidence exists for a `45b32d1d` deploy.
2. Add Cycle 21 plan/deferred pair to the index.
3. Recompute/advance carry-forward ages for Cycle 21 and add new deferred rows.
4. Re-run the 8-cycle High and 16-cycle Medium checkpoint policy and document the result.

Acceptance:

- Future cycles can identify Cycle 21 as the active pair.
- The carry-forward register no longer claims the latest age check is Cycle 20.

Status: implemented; Cycle 20 status is superseded honestly and Cycle 21 carry-forward rows were added.

### WP9 - Full gates, commit, push, and per-cycle deploy

Findings: cycle quality/deploy requirement.

Plan:

1. Run every configured gate against the whole repo:
   - `npm run lint --workspace=apps/web`
   - `npm run lint:api-auth --workspace=apps/web`
   - `npm run lint:action-origin --workspace=apps/web`
   - `npm run lint:public-route-rate-limit --workspace=apps/web`
   - `npm run typecheck --workspace=apps/web`
   - `npm run build --workspace=apps/web`
   - `npm test --workspace=apps/web`
   - `npm run test:e2e --workspace=apps/web`
2. Fix all gate errors before commit.
3. Commit and push signed Conventional Commit + gitmoji changes after `git pull --rebase`.
4. Run `npm run deploy` once after all plan work is pushed and gates are green.

Status: in progress; full local gate sweep is green, signed commit/push and per-cycle deploy still pending.

## Progress

- [x] WP1 mutation-barrier acquired checks must be adjacent to the disposable slot before mutations.
- [x] WP2 pending file-deletion ledger is inserted before image-row deletion and retained on cleanup failure.
- [x] WP3 permanent image-processing failure marking is centralized behind `markPermanentlyFailed`.
- [x] WP4 backfill candidate scans have an `images(processed, pipeline_version, id)` index in schema, migration, and reconcile.
- [x] WP5 persisted MySQL datetimes are parsed by string components for timeline/month/year grouping.
- [x] WP6 root operator scripts are syntax-checked by the existing script gate.
- [x] WP7 bounded docs, i18n, and accessibility defects are fixed.
- [x] WP8 current-cycle plan/deferred/carry-forward ledger repair is recorded.
- [x] WP9 full local gates are green.
- [ ] WP9 signed commit/push and per-cycle deploy.

## Gate Evidence

Focused checks before the full sweep:

- `npm run lint:action-origin --workspace=apps/web` -> passed.
- `npm run typecheck --workspace=apps/web` -> passed.
- `npm test --workspace=apps/web -- --run src/__tests__/check-action-origin.test.ts src/__tests__/image-queue-permanent-failure.test.ts src/__tests__/failed-image-retry.test.ts src/__tests__/pending-file-deletions-source.test.ts src/__tests__/mysql-datetime.test.ts src/__tests__/check-js-scripts-contract.test.ts` -> 6 files passed; 154 tests passed.

Full gate sweep before commit:

- `npm run lint --workspace=apps/web` -> passed.
- `npm run lint:api-auth --workspace=apps/web` -> passed.
- `npm run lint:action-origin --workspace=apps/web` -> passed.
- `npm run lint:public-route-rate-limit --workspace=apps/web` -> passed.
- `npm run typecheck --workspace=apps/web` -> passed.
- `npm run build --workspace=apps/web` -> passed.
- `npm test --workspace=apps/web` -> 356 files passed, 2 skipped; 3348 tests passed, 4 skipped.
- `npm run test:e2e --workspace=apps/web` -> 45 passed, 2 skipped.

Deploy note:

- `npm run deploy` must run after the pushed commit per `DEPLOY_MODE=per-cycle`; the cycle report records the result.

## Deferred Register

All aggregate findings not listed in WP1-WP8 are recorded in `cycle-21-2026-07-08-deferred.md`.
