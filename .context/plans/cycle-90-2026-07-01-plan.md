# Cycle 90/100 Implementation Plan

Start HEAD: `baefb4277e67bf387c350b56b61b56d40451c933`.
Review aggregate: `.context/reviews/cycle-90-2026-07-01/_aggregate.md`.

## Goal

Close the safe Cycle 90 release-ledger finding, keep review/plan indexes current, run all required gates, commit/push, deploy with `npm run deploy`, and smoke `https://gallery.atik.kr`.

## Scheduled Fixes

1. `C90-01`: close Cycle 89's release ledger by marking commit/pull-rebase/push and deploy complete, recording signed `baefb4277e67bf387c350b56b61b56d40451c933` / `origin/master` evidence, recording that Cycle 90 started from deployed `baefb42`, and moving Cycle 89 out of the active plan index.
2. `C90-02`: keep Cycle 90 review/plan artifacts trackable by extending the existing `.gitignore` whitelist for the new plan and deferred files.

## Deferred This Cycle

No newly deferred Cycle 90 findings.

Carry-forward deferred items remain in `.context/plans/cycle-90-2026-07-01-deferred.md`.

## Regression Coverage

- Ledger: Cycle 89 must no longer show unchecked release tasks after Prompt 3.
- Index: `.context/plans/README.md` must list Cycle 90 as active and Cycle 89 as recent with terminal state.
- Review aggregation: `.context/reviews/_aggregate.md` must point to the Cycle 90 aggregate.

## Validation

Run required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `BASE_URL=https://gallery.atik.kr npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then signed commit, `git pull --rebase`, push, deploy with `npm run deploy`, and smoke:

- `curl -fsSIL https://gallery.atik.kr`
- `curl -fsS https://gallery.atik.kr/api/health`

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run required gates.
- [x] Commit, pull --rebase, push.
- [x] Deploy with `npm run deploy`.

## Focused Evidence

- Pre-edit production smoke: `curl -fsSIL https://gallery.atik.kr` returned HTTP 307 to `/en`; `curl -fsS https://gallery.atik.kr/api/health` returned `{"status":"ok"}`.
- Primary cycle commit: signed `dcc8055ee04e8f56805ad81be429749c157748d3` was pushed to `origin/master`.
- Primary cycle deploy: `npm run deploy` pulled `dcc8055`, rebuilt/restarted `gallerykit-web`, and completed cleanup.
- Post-deploy smoke: `curl -sS -o /tmp/gallery-root-smoke.out -w '%{http_code} %{redirect_url}\n' https://gallery.atik.kr` returned `307 https://gallery.atik.kr/en`; `curl -sS -L -o /tmp/gallery-root-follow-smoke.out -w '%{http_code} %{url_effective}\n' https://gallery.atik.kr` returned `200 https://gallery.atik.kr/en`; `curl -sS https://gallery.atik.kr/api/health` returned `{"status":"ok"}`.
- Terminal docs-only evidence sync: Cycle 91 started from `HEAD == origin/master == origin/HEAD == c648634b666f59c29cfe40ea5bbd547bc98d1885`, confirming signed `c648634` as the current deployed master baseline after the Cycle 90 terminal sync.
- Cycle 91 pre-edit production smoke for terminal `c648634`: `curl -sS -o /tmp/cycle91-pre-root.out -w '%{http_code} %{redirect_url}\n' https://gallery.atik.kr` returned `307 https://gallery.atik.kr/en`; `curl -sS -L -o /tmp/cycle91-pre-root-follow.out -w '%{http_code} %{url_effective}\n' https://gallery.atik.kr` returned `200 https://gallery.atik.kr/en`; `curl -fsS https://gallery.atik.kr/api/health` returned `{"status":"ok"}`.

## Gate Evidence

- `npm run lint --workspace=apps/web` - pass.
- `npm run lint:api-auth --workspace=apps/web` - pass: both admin API routes OK.
- `npm run lint:action-origin --workspace=apps/web` - pass: all mutating server actions enforce same-origin provenance.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
- `npm run typecheck --workspace=apps/web` - pass.
- `BASE_URL=https://gallery.atik.kr npm run build --workspace=apps/web` - pass. Local sitemap generation logged the expected unavailable local MySQL fallback and exited successfully.
- `npm test --workspace=apps/web` - pass: 302 files passed, 2 skipped; 2840 tests passed, 4 skipped.

## Gate Fix Iterations

- `C90-01`: fixed one release-ledger/documentation drift by recording signed pushed/deployed Cycle 89 evidence and moving the active plan pointer forward.
- `C90-02`: terminal docs-only sync evidence for signed `c648634` was verified at Cycle 91 start and recorded without reopening Cycle 90 implementation.
