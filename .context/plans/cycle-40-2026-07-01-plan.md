# Cycle 40/100 Implementation Plan

Start HEAD: `490b93c5`.
Review aggregate: `.context/reviews/cycle-40-2026-07-01/_aggregate.md`.

## Goal

Close three safe, reviewable honesty/guardrail findings without schema changes, product-policy changes, or deploy-script rewrites.

## Scheduled Fixes

1. `UI-C40-01`: derive the JPEG download option label/description from `isWideGamutSource && !forceSrgbDerivatives` in both `photo-viewer.tsx` and `info-bottom-sheet.tsx`. Wide-gamut default delivery should say Display P3 JPEG; forced-sRGB delivery should say sRGB JPEG.
2. `TV-40-01`: extend `check-action-origin.ts` protected-read detection to flag Drizzle relational reads (`db.query.*.findFirst`, `findMany`, and similar methods) before auth in reasoned read-only exemptions.
3. `TV-40-02`: extend `check-public-route-rate-limit.ts` expensive-read detection so DB-backed imported data helpers from known server data modules are treated as expensive when called from public GET/HEAD handlers.
4. Adjacent regression coverage: while touching the public-route scanner tests, pin the existing fail-closed behavior for named external `HEAD` re-exports to keep parity with the current `GET` fixture.

## Validation

Run the required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then sign, pull --rebase, push, and deploy with `npm run deploy` from the repo root.

## Progress

- [x] Write aggregate/deferred artifacts.
- [x] Implement scheduled fixes.
- [x] Add focused regression tests.
- [ ] Run gates, commit, push, deploy.
