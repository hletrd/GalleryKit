# Cycle 57/100 Implementation Plan

Start HEAD: `677a8410`.
Review aggregate: `.context/reviews/cycle-57-2026-07-01/_aggregate.md`.

## Goal

Fix the actionable Cycle 57 findings, keep review/plan ledgers current, run all required gates, commit, push, and deploy with `npm run deploy` from the repo root.

## Scheduled Fixes

1. `C57-01`: restore public photo page cache reuse by starting `getImageCached(imageId)` immediately, reusing it for non-admin visitors, and reserving `getImageForViewerCached(imageId, true)` for admin viewers.
2. `C57-02`: add behavior-level coverage proving `getImageForViewer` public selects omit representative privacy-sensitive keys while admin selects include representative audit fields.
3. `C57-03`: add behavior coverage for a changed `strip_gps_on_upload` payload when images already exist, including lock release and no persistence side effects.
4. `C57-04`: close Cycle 56's release ledger with terminal commit/push/deploy evidence for `30dad6a8` and `677a8410`, and advance plan/review pointers to Cycle 57.

## Deferred This Cycle

No new Cycle 57 findings are deferred. Carry-forward items are recorded in `.context/plans/cycle-57-2026-07-01-deferred.md`.

## Regression Coverage

- Extend `cycle-56-source-contracts.test.ts` to require the non-admin photo page branch to reuse `getImageCached` and the admin branch to call `getImageForViewerCached(..., true)`.
- Add `data-viewer-select-fields.test.ts` for behavior-level public/admin select field coverage around `getImageForViewer`.
- Extend `settings-semantic-mode-action.test.ts` for the changed `strip_gps_on_upload` existing-image lock branch.

## Validation

Run required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then sign, `git pull --rebase`, push, and deploy with `npm run deploy`.

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run required gates.
- [x] Commit, pull --rebase, push.
- [x] Deploy with `npm run deploy`.

## Gate Evidence

- Focused regression gate: `npm test --workspace=apps/web -- settings-semantic-mode-action.test.ts data-viewer-select-fields.test.ts cycle-56-source-contracts.test.ts` - pass, 3 files / 10 tests.
- `npm run lint --workspace=apps/web` - pass.
- `npm run lint:api-auth --workspace=apps/web` - pass.
- `npm run lint:action-origin --workspace=apps/web` - pass.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
- `npm run typecheck --workspace=apps/web` - pass.
- `npm run build --workspace=apps/web` - pass. Local sitemap generation logged the expected unavailable local MySQL fallback and exited successfully.
- `npm test --workspace=apps/web` - pass: 286 files passed, 2 skipped; 2733 tests passed, 4 skipped.
- `git diff --cached --check` - pass.
- Commit evidence: signed commit `51bca78933a702e237853a509ddce10f13f9ed6b` (`fix(cycle-57): 🐛 restore photo cache and coverage`) with Good signature from `Jiyong Youn <01@0101010101.com>`.
- Push evidence: `origin/master` and remote `refs/heads/master` both resolved to `51bca78933a702e237853a509ddce10f13f9ed6b` at the start of Cycle 58.
- Deploy evidence: Cycle 58 invocation specified `51bca789` as the current deployed `master` HEAD at start; no separate Cycle 57 deploy log artifact was committed with Cycle 57.

## Gate Fix Iterations

- 1: `git diff --cached --check` flagged trailing spaces in the perf review artifact; removed the whitespace-only violations and reran the staged check successfully.
