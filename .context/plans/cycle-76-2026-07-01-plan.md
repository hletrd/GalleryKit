# Cycle 76/100 Implementation Plan

Start HEAD: `a295ae4432f071c374cb68278a706f5a516ae593`.
Review aggregate: `.context/reviews/cycle-76-2026-07-01/_aggregate.md`.

## Goal

Fix the safe, reviewable Cycle 76 correctness and freshness findings, keep review/plan ledgers current, run all required gates, commit, push, and deploy with `npm run deploy` from the repo root.

## Scheduled Fixes

1. `C76-01`: stop treating same-value MySQL updates as deleted rows in in-app and sidecar backfill paths; confirm row absence before derivative cleanup.
2. `C76-02`: make derivative rewrites advance image freshness and include derivative byte-impact settings plus pipeline version in per-photo OG ETags.
3. `C76-03`: close the Cycle 75 plan/index/aggregate ledger using the Cycle 76 deployed-start evidence for `a295ae44`.

## Deferred This Cycle

New deferred items are recorded in `.context/plans/cycle-76-2026-07-01-deferred.md`.

Carry-forward deferred items remain referenced in the Cycle 76 deferred file.

## Regression Coverage

- Backfill deleted-mid-reencode tests: affected-row zero plus row-still-exists must not trigger cleanup.
- Sidecar helper tests: cleanup partition must require both zero affected rows and confirmed row absence.
- Per-photo OG route tests: ETag formula must include settings hash and pipeline version.

## Validation

Run required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then sign, `git pull --rebase`, push, and deploy with `npm run deploy`.

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run required gates.
- [ ] Commit, pull --rebase, push.
- [ ] Deploy with `npm run deploy`.

## Gate Evidence

- Focused regression: `npm test --workspace=apps/web -- --run src/__tests__/admin-backfill-runner-deleted-mid-reencode.test.ts src/__tests__/admin-backfill-runner-deleted-mid-reencode-detection-failure.test.ts src/__tests__/backfill-color-pipeline-deleted-mid-reencode.test.ts src/__tests__/og-route-rate-limit-behavior.test.ts` - pass: 4 files passed, 32 tests passed.
- `npm run lint --workspace=apps/web` - pass.
- `npm run lint:api-auth --workspace=apps/web` - pass: both admin API routes OK.
- `npm run lint:action-origin --workspace=apps/web` - pass: all mutating server actions enforce same-origin provenance.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
- `npm run typecheck --workspace=apps/web` - pass.
- `npm run build --workspace=apps/web` - pass. Local sitemap generation logged the expected unavailable local MySQL fallback and exited successfully.
- `npm test --workspace=apps/web` - pass: 301 files passed, 2 skipped; 2808 tests passed, 4 skipped.

## Gate Fix Iterations

0 so far.
