# Cycle 22/100 Implementation Plan

Source: `.context/reviews/_aggregate.md` and all cycle-22 per-agent reviews.  
Planning date: 2026-06-30 KST.  
Repo rules consulted before scheduling/defer decisions: `CLAUDE.md`, `AGENTS.md`, `.context/plans/README.md`, current `.context/**` review/plan inventory, and `docs/superpowers/**`. No `.cursorrules` or `CONTRIBUTING.md` exists.

Every implementation commit must be GPG-signed (`git commit -S`), Conventional Commits + gitmoji, no `Co-Authored-By`, `git pull --rebase` before push, and all configured gates must pass before deploy.

## Implement This Cycle

### T1 - Normalize advisory-lock result handling

Findings: `AGG22-01`, `V22-04`  
Severity/confidence: High / High; Low / High test gap  
Files: `apps/web/src/lib/advisory-locks.ts`, `apps/web/src/lib/image-queue.ts`, `apps/web/src/lib/admin-backfill-runner.ts`, `apps/web/scripts/backfill-color-pipeline.ts`, related advisory-lock tests.

- [x] Add a shared helper that treats MySQL advisory-lock acquisition results as acquired for `1`, `1n`, and driver string forms such as `'1'`.
- [x] Wire the helper through every vulnerable lock acquisition call site flagged by review, without weakening existing explicit release/finally behavior.
- [x] Add regression tests for `bigint` and string lock acquisition paths, including at least one in-app backfill/image-queue path and the sidecar color backfill source contract.

### T2 - Align smart-collection tag predicate validation with compilation

Finding: `AGG22-05`  
Severity/confidence: Medium / High  
Files: `apps/web/src/lib/smart-collections.ts`, smart-collection tests.

- [x] Reject non-string `tag` predicate values for `eq` and `contains`, matching the compiler's tag-name semantics.
- [x] Add regression tests proving numeric tag predicate values are rejected at write/parse time instead of silently compiling as tag names.

### T3 - Add confirmation for settings re-encode

Findings: `AGG22-02`, `AGG22-19`  
Severity/confidence: High / High; Medium / High  
Files: `apps/web/src/app/[locale]/admin/(protected)/settings/settings-client.tsx`, `apps/web/messages/en.json`, `apps/web/messages/ko.json`, focused UI/source tests if needed.

- [x] Require an explicit confirmation dialog before starting the in-app derivative re-encode/backfill.
- [x] Update copy to describe operational cost and cache/byte-rewrite scope without implying ordinary settings saves require re-encode.
- [x] Preserve existing disabled/running state and maintenance guards.

### T4 - Fix token plaintext dialog dismissal affordance

Finding: `AGG22-13`  
Severity/confidence: Medium / High  
Files: `apps/web/src/app/[locale]/admin/(protected)/tokens/tokens-client.tsx`, messages/tests if needed.

- [x] Remove or disable misleading close controls while the one-time token must be acknowledged, or route every close attempt to the same explicit acknowledgment.
- [x] Keep the current safety requirement that the token cannot be accidentally dismissed before acknowledgement.

### T5 - Restore public error shell escape

Finding: `AGG22-12`  
Severity/confidence: Medium / High  
Files: `apps/web/src/app/[locale]/error.tsx`, public layout/nav components or source-contract tests.

- [x] Ensure localized route errors present a stable escape path and do not strand visitors on a failing page without public navigation.
- [x] Add or update a focused regression proving the error boundary contains a usable public home/gallery link.

### T6 - Expose public color badges to assistive tech

Finding: `AGG22-04`  
Severity/confidence: High/Medium / High  
Files: `apps/web/src/components/grid-picture.tsx`, message strings/tests.

- [x] Add accessible text/labels for meaningful public wide-gamut/color badges while keeping purely decorative icons hidden.
- [x] Add a regression that catches a visual P3 badge without an accessible label.

### T7 - Documentation and positioning alignment

Findings: `AGG22-03`, `AGG22-06`, `AGG22-18`, `AGG22-20`, `DOC22-01`, `DOC22-02`, `DOC22-03`, `DOC22-04`, `TEST22-04`  
Severity/confidence: High / High through Low / High  
Files: `README.md`, `apps/web/README.md`, `CLAUDE.md`, `apps/web/.env.local.example`, `docs/superpowers/plans/2026-06-15-clip-semantic-search.md`, deployment/source-contract tests if existing.

- [x] Clarify README analytics posture: built-in self-hosted analytics are first-party; Google Analytics is optional and disabled unless configured.
- [x] Add concise "for / not for" positioning so users do not expect editing/culling/proofing SaaS workflows.
- [x] Mark lower-level Docker compose commands as local/manual only and keep production deploy guidance on `npm run deploy`.
- [x] Add missing documented operator env vars to `.env.local.example`.
- [x] Remove or clarify stale Lightroom-plugin and historical CLIP snippets that contradict current implementation.
- [x] Add/extend a lightweight docs/source-contract test for deploy-command drift where practical.

### T8 - Upload quota settlement guardrail

Finding: `AGG22-11`, `TEST22-05`  
Severity/confidence: Medium / High  
Files: `apps/web/src/app/actions/images.ts`, upload quota/source-contract tests.

- [x] Add a focused regression guard around the current claim/settle/release topology so future early returns or awaits between quota claim and final settle are caught.
- [x] Prefer a small structural helper if the existing shape supports it without a broad upload rewrite; otherwise preserve behavior and lock the invariant with tests in this cycle.

### T9 - Quality gates, commits, push, deploy

- [x] Run every configured gate from the orchestrator: `npm run lint --workspace=apps/web`, `npm run lint:api-auth --workspace=apps/web`, `npm run lint:action-origin --workspace=apps/web`, `npm run lint:public-route-rate-limit --workspace=apps/web`, `npm run typecheck --workspace=apps/web`, `npm run build --workspace=apps/web`, `npm test --workspace=apps/web`.
- [x] Fix blocking errors. Record any non-blocking warnings that cannot be fixed in `cycle-22-2026-06-30-deferred.md`.
- [ ] Commit and push fine-grained signed commits.
- [ ] Run `npm run deploy` once after green gates and pushed commits.

## Explicitly Deferred

All remaining aggregate findings are recorded in `cycle-22-2026-06-30-deferred.md` with original severity/confidence preserved, concrete reason, and reopen criterion. No confirmed high-severity finding is deferred.

## Progress

Status: Implementation complete and required local gates are green. Evidence: `npm run lint --workspace=apps/web`, `npm run lint:api-auth --workspace=apps/web`, `npm run lint:action-origin --workspace=apps/web`, `npm run lint:public-route-rate-limit --workspace=apps/web`, `npm run typecheck --workspace=apps/web`, `npm run build --workspace=apps/web`, and `npm test --workspace=apps/web` (265 passed, 2 skipped; 2485 tests passed, 4 skipped). Commit, push, and deploy remain.
