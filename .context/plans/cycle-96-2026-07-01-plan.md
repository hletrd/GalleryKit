# Cycle 96/100 Implementation Plan

Starting HEAD: `2f22620c361304ba0408053f546f45e3c74ddfdb`.

Review aggregate: `.context/reviews/cycle-96-2026-07-01/_aggregate.md`.

## Scheduled Safe Narrow Fixes

- [x] `C96-01` Release/deploy ledgers stale for current `master` - update cycle 96 plan/review ledgers and plan index with the current baseline and final cycle evidence.
- [x] `C96-02` LR token list collapses DB/table failures into an empty-token state - make `listLrTokens()` return the existing persistent error state when `listTokensForUser()` throws, and add/update source-contract tests.
- [x] `C96-03` LR token label browser `maxLength` conflicts with server code-point validation - remove or widen the UTF-16 HTML cap and correct the stale server comment/test contract.
- [x] `C96-05` Privacy page omits OpenStreetMap tile disclosure - add English/Korean privacy copy and render a map-tile section.
- [x] `C96-06` Browser upload accept list omits backend/doc-supported formats - align the dropzone accept extensions with backend `ALLOWED_EXTENSIONS` for `.heic`, `.heif`, and `.bmp`, with a source-contract test.

## Implementation Notes

- Keep changes source/test/docs only; no schema migration, new dependencies, production config changes, or broad UI redesign in this cycle.
- Preserve current token create/revoke security behavior; only list-load failure presentation changes.
- For privacy copy, disclose that the public map loads OpenStreetMap tiles and that tile providers may receive request/device/referrer information.
- For upload accept, align the browser dropzone with already-supported backend extensions without changing upload limits or processing behavior.

## Required Gates

- [x] `npm run lint --workspace=apps/web` - passed.
- [x] `npm run lint:api-auth --workspace=apps/web` - passed.
- [x] `npm run lint:action-origin --workspace=apps/web` - passed.
- [x] `npm run lint:public-route-rate-limit --workspace=apps/web` - passed.
- [x] `npm run typecheck --workspace=apps/web` - passed.
- [x] `BASE_URL=https://gallery.atik.kr npm run build --workspace=apps/web` - passed; local DB was unavailable, so sitemap used its existing homepage-only fallback during static generation.
- [x] `npm test --workspace=apps/web` - passed, 302 files / 2846 tests; 2 files / 4 tests skipped.
- [x] Focused tests for touched source contracts - passed, 4 files / 36 tests.
- [x] `git diff --cached --check` - passed.

## Progress

- Prompt 1 review fan-out completed; aggregate written.
- Prompt 2 plan/deferred files written.
- Prompt 3 implementation complete for scheduled fixes.
- Focused regression slice passed: `npm test --workspace=apps/web -- lr-tokens-action.test.ts client-source-contracts.test.ts cycle-11-source-contracts.test.ts privacy-page-landmark.test.ts` (4 files, 36 tests).

## Completion Evidence

- Focused regression: `npm test --workspace=apps/web -- lr-tokens-action.test.ts client-source-contracts.test.ts cycle-11-source-contracts.test.ts privacy-page-landmark.test.ts` passed (4 files, 36 tests).
- Full gates through unit tests passed.
- `git diff --cached --check` passed after mechanical trailing-whitespace cleanup in generated review artifacts.
- Terminal evidence recorded by Cycle 97: signed commit `061c1c81af234469641f75a53e5bbc61fa63114a` was `origin/master` and the user-provided deployed `master` HEAD at Cycle 97 start.
