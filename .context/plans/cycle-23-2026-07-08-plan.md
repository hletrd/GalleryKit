# Run-10 Cycle 23/100 Implementation Plan

Status: IMPLEMENTED - GATES PASSED; PUSH/DEPLOY PENDING
Aggregate: `.context/reviews/_aggregate.md`
Date: 2026-07-08 KST
Review start HEAD: `57c1ae33c0b9a0dd483cfdf58750b81d42a7d775`
Planning HEAD: `66a2ec6f0797d4c7a3a12bab6d610a2dbae21013`

## Scope

This plan schedules every contained correctness, auth/session, restore, evidence, and small UX/test hygiene finding from Cycle 23. Every aggregate finding not scheduled here is recorded in `cycle-23-2026-07-08-deferred.md` with original severity/confidence, citation, reason, and exit criterion.

Repo rules read before scheduling/deferring: `CLAUDE.md`, `AGENTS.md`, `.context/plans/README.md`, `.context/plans/deferred-carry-forward.md`, Cycle 22 plan/deferred, current Cycle 23 review artifacts, and the docs/style/policy search result. No `.cursorrules` or `CONTRIBUTING.md` exists in this checkout; no additional tracked docs policy file was found outside `CLAUDE.md`/`AGENTS.md`/`.context/**`.

## Scheduled Work Packages

### WP1 - Fail-closed restore logout revocations before reopening maintenance

Findings: `AGG-C23-02`

Files:

- `apps/web/src/app/[locale]/admin/db-actions.ts`
- `apps/web/src/lib/pending-session-revocations.ts`
- `apps/web/src/__tests__/pending-session-revocations.test.ts`
- docs/ledger updates if behavior wording changes

Plan:

1. Move successful-restore `flushPendingSessionRevocations()` before `endDurableRestoreMaintenance()` so restored sessions are revoked before normal auth resumes.
2. Make the restore path distinguish an empty queue from a failed non-empty flush; fail closed by keeping/returning restore-maintenance recovery state if queued revocations cannot be proven flushed.
3. Update tests that currently pin flush-after-marker order to assert the safer post-import/pre-marker order and failure handling.
4. Ensure pending file deletion drain remains after marker clear unless the source proves it is safe earlier.

Acceptance:

- A queued logout revocation is flushed before maintenance clears after a successful import.
- A failed non-empty revocation flush does not silently reopen admin auth.
- Targeted pending-session revocation tests pass.

Status: implemented. Successful restores now run a strict pre-marker session-revocation flush; a failed non-empty flush keeps restore maintenance active and returns a failure result after locks are released.

### WP2 - Gate protected admin SSR reads during restore maintenance

Findings: `AGG-C23-03`

Files:

- `apps/web/src/app/[locale]/admin/(protected)/layout.tsx`
- a small protected admin maintenance component if needed
- focused tests under `apps/web/src/__tests__/`
- messages if user-visible copy needs i18n

Plan:

1. Check restore maintenance in the protected admin layout after auth and before child pages render.
2. Render a non-querying maintenance shell while restore is active.
3. Preserve the DB restore UI path if it needs an explicit exception; otherwise block all protected children consistently.
4. Add a regression test proving protected admin children/data-access imports are not rendered/called while the restore marker is active.

Acceptance:

- Protected admin SSR pages do not query application tables during restore maintenance.
- Existing admin auth behavior remains unchanged outside restore windows.
- Targeted layout/maintenance tests pass.

Status: implemented. Protected admin layout renders the existing restore-maintenance shell while active and does not return protected children.

### WP3 - Close nested mutation-slot scanner dominance bypass

Findings: `AGG-C23-01`

Files:

- `apps/web/scripts/check-action-origin.ts`
- `apps/web/src/__tests__/check-action-origin.test.ts`

Plan:

1. Add negative scanner fixtures for a valid slot inside `if`, `for`, and `try`/nested blocks followed by an outer mutation.
2. Update the mutation-barrier scanner to accept only slot/gate shapes that protect the later mutating statements in the same lexical/control-flow region.
3. Preserve valid early-return and current production action shapes.
4. Run scanner tests and `npm run lint:action-origin --workspace=apps/web`.

Acceptance:

- Nested slot followed by outer mutation fails the lint gate.
- Real action files continue to pass.

Status: implemented. The scanner now accepts slots only in the exported action body block, with negative fixtures for nested `if`, loop, and `try` slots before outer mutations.

### WP4 - Complete pending-deletion behavior coverage and quiet false cleanup debug

Findings: `AGG-C23-07`, `AGG-C23-08`

Files:

- `apps/web/src/lib/process-image.ts`
- `apps/web/src/lib/pending-file-deletions.ts`
- `apps/web/src/lib/maintenance-scheduler.ts`
- `apps/web/src/__tests__/pending-file-deletions.test.ts`
- `apps/web/src/__tests__/process-image-variant-scan.test.ts`

Plan:

1. Add behavior tests for transient pending-deletion failure followed by success.
2. Add temp-dir behavior coverage for already-missing original/derivative files through the strict helpers or the drain path.
3. Add restore-active scheduler coverage proving the pending-deletion drain is skipped while maintenance is active.
4. Fix `deleteImageVariantsStrict(..., [])` full-scan close handling so successful cleanup does not emit `ERR_DIR_CLOSED` debug noise.
5. Add a no-debug regression around the full-scan success path.

Acceptance:

- Missing files are treated as clean through executable tests.
- Restore-active maintenance does not drain pending file deletions.
- Successful full-scan cleanup produces no false debug error.

Status: implemented. Pending-deletion tests now cover transient cleanup retry and restore-active scheduler suppression; variant-scan tests cover strict missing variants and no false `ERR_DIR_CLOSED` debug output.

### WP5 - Fix open-empty search combobox state

Findings: `AGG-C23-24`

Files:

- `apps/web/src/components/search.tsx`
- focused accessibility/source test under `apps/web/src/__tests__/` or Playwright if needed

Plan:

1. Separate dialog-open state from result-list popup state, or bind `aria-expanded` to the visible popup relationship actually exposed by the combobox.
2. Add `aria-controls` when the input owns the result list.
3. Add an accessibility regression for the open-empty search state.

Acceptance:

- Opening search no longer leaves the focused combobox reporting a contradictory collapsed state.
- Existing result navigation behavior remains intact.

Status: implemented. Search combobox expansion now follows dialog-open state and controls the dialog before results exist, with source coverage.

### WP6 - Repair Cycle 22/23 provenance and review-artifact hygiene

Findings: `AGG-C23-09`, `AGG-C23-10`, `AGG-C23-23`

Files:

- `.context/plans/cycle-22-2026-07-08-plan.md`
- `.context/plans/README.md`
- `.context/plans/cycle-23-2026-07-08-plan.md`
- `.context/plans/cycle-23-2026-07-08-deferred.md`
- `.context/plans/deferred-carry-forward.md`
- `.context/reviews/*.md`

Plan:

1. Append Cycle 22 terminal evidence: exact commit hash, pushed state, and whether deploy was completed or superseded by Cycle 23.
2. Move Cycle 22 out of active current-cycle plans and list Cycle 23 as active.
3. Add historical-name disambiguation for the pre-run-10 `cycle-23-2026-06-30-*` pair.
4. Trim trailing whitespace in changed review artifacts.
5. Update carry-forward age-budget text and rows for Cycle 23.

Acceptance:

- Current plan index points at Cycle 23, while Cycle 22 terminal state is no longer ambiguous.
- Historical Cycle 23 files cannot be mistaken for the current run-10 Cycle 23.
- `git diff --check` passes.

Status: implemented. Cycle 22 terminal source/push evidence, Cycle 23 plan index, historical Cycle 23 disambiguation, carry-forward rows, and review aggregate are updated.

### WP7 - Full gates, commit, push, and per-cycle deploy

Findings: cycle quality/deploy requirement.

Plan:

1. Run every configured gate against the whole repo:
   - `npm run lint --workspace=apps/web`
   - `npm run lint:api-auth --workspace=apps/web`
   - `npm run lint:action-origin --workspace=apps/web`
   - `npm run lint:public-route-rate-limit --workspace=apps/web`
   - `npm run typecheck --workspace=apps/web`
   - `npm run build --workspace=apps/web`
   - `npm test --workspace=apps/web`
   - `npm run test:e2e --workspace=apps/web`
2. Fix all gate errors before final commit/push.
3. Commit and push signed Conventional Commit + gitmoji changes after `git pull --rebase`.
4. Run `npm run deploy` once after all plan work is pushed and gates are green.

Status: gates passed; signed commit/push and per-cycle deploy pending.

## Finding Disposition Map

Scheduled here: `AGG-C23-01`, `AGG-C23-02`, `AGG-C23-03`, `AGG-C23-07`, `AGG-C23-08`, `AGG-C23-09`, `AGG-C23-10`, `AGG-C23-23`, `AGG-C23-24`.

Deferred in `cycle-23-2026-07-08-deferred.md`: `AGG-C23-04`, `AGG-C23-05`, `AGG-C23-06`, `AGG-C23-11`, `AGG-C23-12`, `AGG-C23-13`, `AGG-C23-14`, `AGG-C23-15`, `AGG-C23-16`, `AGG-C23-17`, `AGG-C23-18`, `AGG-C23-19`, `AGG-C23-20`, `AGG-C23-21`, `AGG-C23-22`, `AGG-C23-25`, `AGG-C23-26`.

## Progress

- [x] WP1 restore logout revocation fail-closed ordering.
- [x] WP2 protected admin restore-maintenance SSR gate.
- [x] WP3 nested mutation-slot scanner dominance.
- [x] WP4 pending-deletion behavior coverage and cleanup debug noise.
- [x] WP5 search combobox open-empty accessibility state.
- [x] WP6 Cycle 22/23 provenance, carry-forward, and whitespace hygiene.
- [x] WP7 full gates; signed commit/push and per-cycle deploy pending.

## Gate Evidence

- Targeted: `npm test --workspace=apps/web -- --run src/__tests__/pending-session-revocations.test.ts src/__tests__/protected-admin-restore-maintenance-layout.test.tsx src/__tests__/check-action-origin.test.ts src/__tests__/pending-file-deletions.test.ts src/__tests__/maintenance-scheduler-pending-deletions.test.ts src/__tests__/process-image-variant-scan.test.ts src/__tests__/search-status-source.test.ts` - PASS (7 files, 142 tests).
- Targeted: `npm test --workspace=apps/web -- --run src/__tests__/auth-mutation-barrier-source.test.ts` - PASS (1 file, 4 tests).
- Full gates: `npm run lint --workspace=apps/web` - PASS.
- Full gates: `npm run lint:api-auth --workspace=apps/web` - PASS.
- Full gates: `npm run lint:action-origin --workspace=apps/web` - PASS.
- Full gates: `npm run lint:public-route-rate-limit --workspace=apps/web` - PASS.
- Full gates: `npm run typecheck --workspace=apps/web` - PASS.
- Full gates: `npm run build --workspace=apps/web` - PASS.
- Full gates: `npm test --workspace=apps/web` - PASS (360 files passed, 2 skipped; 3365 tests passed, 4 skipped).
- Full gates: `npm run test:e2e --workspace=apps/web` - PASS (45 passed, 2 skipped).
