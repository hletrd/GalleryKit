# Cycle 21/100 Implementation Plan

Source: `.context/reviews/_aggregate.md` and all cycle-21 per-agent reviews.  
Planning date: 2026-06-30 KST.  
Repo rules consulted before scheduling/defer decisions: `CLAUDE.md`, `AGENTS.md`, `.context/plans/README.md`, current `.context/**` markdown inventory, and `docs/superpowers/**`. No `.cursorrules` or `CONTRIBUTING.md` exists.

Every implementation commit must be GPG-signed (`git commit -S`), Conventional Commits + gitmoji, no `Co-Authored-By`, `git pull --rebase` before push, and all configured gates must pass before deploy.

## Implement This Cycle

### T1 - Photo accessible names and similar-photo labels

Findings: `AGG-C21-01`, `AGG-C21-23`  
Severity/confidence: High / High and Medium / Medium-High  
Files: `apps/web/src/lib/photo-title.ts`, `apps/web/src/components/photo-viewer.tsx`, `apps/web/src/components/lightbox.tsx`, `apps/web/src/components/similar-photos.tsx`, focused tests.

- [x] Make tag-only direct photo pages expose meaningful accessible names to the primary photo, zoom button, related-photo thumbnails, and lightbox.
- [x] Make similar-photo fallback labels unique/contextual instead of repeated `Photo`.
- [x] Add regression coverage for tag-only photo accessible names and repeated similar-photo labels.

### T2 - Lightbox and Settings accessibility/UX fixes

Findings: `AGG-C21-02`, `AGG-C21-03`, `AGG-C21-27`  
Severity/confidence: High / High, High / High, Medium / High  
Files: `apps/web/src/components/lightbox.tsx`, `apps/web/src/components/lightbox-color-pip.tsx`, `apps/web/src/app/[locale]/admin/(protected)/settings/settings-client.tsx`, messages/tests.

- [x] Ensure hidden lightbox controls do not leave an invisible keyboard stop in the color pip.
- [x] Reword or disable the backfill CTA when no eligible re-encode action exists.
- [x] Add persistent field-associated settings validation errors with `aria-invalid` / `aria-describedby` where currently toast-only.
- [x] Add focused tests or source-contract coverage for these UI states.

### T3 - Backup download descriptor cleanup

Finding: `AGG-C21-06`  
Severity/confidence: Low-Medium / High  
Files: `apps/web/src/app/api/admin/db/download/route.ts`, `apps/web/src/__tests__/backup-download-route.test.ts`.

- [x] Close an opened backup file handle if a pre-stream step throws before stream ownership is transferred.
- [x] Add a regression where `open()` succeeds and a later step fails, asserting `close()` is called.

### T4 - Photo navigation prefetch and analytics accounting

Finding: `AGG-C21-07`  
Severity/confidence: Medium / High  
Files: `apps/web/src/components/photo-navigation.tsx`, `apps/web/src/__tests__/cycle-20-source-contracts.test.ts` or adjacent source-contract test.

- [x] Remove hover prefetch of adjacent photo detail pages while view analytics remain render-bound.
- [x] Add/extend a regression that scans photo navigation for photo-detail prefetches.

### T5 - Deployment/product documentation alignment

Findings: `AGG-C21-04`, `AGG-C21-08`, `AGG-C21-28`, `AGG-C21-29`, `AGG-C21-30`, `AGG-C21-32`, `AGG-C21-33`, `AGG-C21-34`, `AGG-C21-35`, `AGG-C21-36`  
Severity/confidence: High / High; Medium / High; Low docs items as recorded  
Files: `README.md`, `apps/web/README.md`, `CLAUDE.md`, `docs/superpowers/plans/2026-06-15-clip-semantic-search.md`, `.context/plan/plan-cycle21.md`, deploy contract tests if needed, upload API docs.

- [x] Rewrite the README lead around GalleryKit's photographer/operator value proposition while keeping technical proof points.
- [x] Add copy-pasteable MySQL bootstrap guidance for local setup.
- [x] Update manual Docker commands to use `--env-file apps/web/.env.local` and add a source-contract test.
- [x] Make semantic-search activation public-doc authoritative or link to a committed operator runbook.
- [x] Add an Upload API contract section covering endpoint, header, scopes, multipart fields, limits, and response.
- [x] Update the directory tree for `public/resources` and private originals.
- [x] Remove or clearly label host-specific CLIP/backfill examples and Lightroom-plugin wording.
- [x] Mark the stale `.context/plan/plan-cycle21.md` as superseded so it no longer contradicts current stub-mode semantics.

### T6 - CLIP embedding backfill restore coordination

Finding: `AGG-C21-05`  
Severity/confidence: Medium / High  
Files: `apps/web/src/lib/advisory-locks.ts`, `apps/web/src/app/[locale]/admin/db-actions.ts`, `apps/web/scripts/backfill-clip-embeddings.ts`, `apps/web/src/app/actions/embeddings.ts`, tests.

- [x] Add a semantic embedding backfill advisory lock.
- [x] Have restore acquire/release it with the other restore-conflicting locks.
- [x] Have both CLIP backfill entry points acquire/release it for the run.
- [x] Add source-contract or behavior tests for restore and both backfill entry points.

### T7 - Small contract/test hardening

Findings: `AGG-C21-19`, `AGG-C21-22`, `AGG-C21-26`, `AGG-C21-31`, `AGG-C21-37`  
Severity/confidence: Low to Low-Medium / High  
Files: `apps/web/src/lib/rate-limit.ts`, `apps/web/src/__tests__/failed-image-retry.test.ts`, `apps/web/src/app/[locale]/admin/(protected)/users/page.tsx`, `apps/web/src/components/admin-user-manager.tsx`, messages/settings copy, `apps/web/src/proxy.ts`.

- [x] Fix the semantic rollback helper comment to match charged disabled/stub mode behavior.
- [x] Add a failed-image retry test asserting every processing snapshot field is forwarded.
- [x] Remove nested Admin Users card chrome.
- [x] Rename visible Auto Alt-Text copy to EXIF Alt-Text Hints until visual captioning exists.
- [x] Simplify or clarify the dead localized admin route branch in `proxy.ts`.

### T8 - Quality gates, commits, and deploy

- [x] Run every configured gate: lint, lint:api-auth, lint:action-origin, lint:public-route-rate-limit, typecheck, build, full Vitest.
- [x] Fix blocking errors and best-effort warnings.
- [x] Update plan progress and deferred records.
- [ ] Commit and push fine-grained signed commits.
- [ ] Run `npm run deploy` once after green gates and pushed commits.

## Explicitly Deferred

All remaining aggregate findings are recorded in `cycle-21-2026-06-30-deferred.md` with severity/confidence preserved, concrete reason, and reopen criterion.

## Progress

Status: Implementation complete with full configured gates green; pending signed commit/push and per-cycle deploy.

Focused validation completed:
- `npm test --workspace=apps/web -- photo-title backup-download-route advisory-locks restore-upload-lock backfill-clip-embeddings-reembed cycle-20-source-contracts cycle-21-source-contracts lightbox-controls-contract failed-image-retry resolved-stream-source` — 11 files / 97 tests passed.
- `npm run typecheck --workspace=apps/web` — passed.
- `npm run lint --workspace=apps/web` — passed.
- `npm run lint:api-auth --workspace=apps/web` — passed.
- `npm run lint:action-origin --workspace=apps/web` — passed.
- `npm run lint:public-route-rate-limit --workspace=apps/web` — passed.
- `npm run build --workspace=apps/web` — passed; emitted the expected local-no-DB sitemap fallback warning recorded in `cycle-21-2026-06-30-deferred.md`.
- `npm test --workspace=apps/web` — passed, 264 files passed / 2 skipped, 2477 tests passed / 4 skipped.
- `git diff --check` — passed.
