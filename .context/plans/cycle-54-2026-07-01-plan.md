# Cycle 54/100 Implementation Plan

Start HEAD: `1a65247c`.
Review aggregate: `.context/reviews/cycle-54-2026-07-01/_aggregate.md`.

## Goal

Fix the actionable Cycle 54 findings, keep review/plan ledgers current, run all required gates, commit, push, and deploy with `npm run deploy` from the repo root.

## Scheduled Fixes

1. `C54-01`: close Cycle 53's release ledger with terminal commit/push/deploy evidence for `1a65247c`, and advance the plan/review pointers to Cycle 54.
2. `C54-02`: extract the Settings changed-payload builder, use it from `SettingsClient`, and add focused regression coverage proving inactive stored `production` clears via `disabled` or `stub` payloads.

## Deferred This Cycle

No new Cycle 54 findings are deferred. Carry-forward items are recorded in `.context/plans/cycle-54-2026-07-01-deferred.md`.

## Regression Coverage

- Add `settings-submit-payload.test.ts` for `production -> disabled`, `production -> stub`, and canonicalized `image_sizes` diff behavior.
- Keep existing semantic-search Settings helper and server-action tests unchanged.

## Validation

Run required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then sign, `git pull --rebase`, push, and deploy with `npm run deploy`.

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run required gates.
- [ ] Commit, pull --rebase, push.
- [ ] Deploy with `npm run deploy`.

## Gate Evidence

- Focused pre-gate check:
  - `npm test --workspace=apps/web -- settings-submit-payload.test.ts semantic-search-settings-ui.test.ts settings-semantic-mode-action.test.ts` - pass, 3 files / 10 tests.
- Full required gate pass:
  - `npm run lint --workspace=apps/web` - pass
  - `npm run lint:api-auth --workspace=apps/web` - pass
  - `npm run lint:action-origin --workspace=apps/web` - pass
  - `npm run lint:public-route-rate-limit --workspace=apps/web` - pass
  - `npm run typecheck --workspace=apps/web` - pass
  - `npm run build --workspace=apps/web` - pass (local sitemap generation fell back to homepage-only because MySQL was not running on `127.0.0.1:3306`; build exited 0)
  - `npm test --workspace=apps/web` - pass, 2720 passed / 4 skipped
