# Cycle 3/100 Implementation Plan

Date: 2026-06-29
Review aggregate: `.context/reviews/_aggregate.md`
Status: complete; deployment pending

Repo rules read before planning/deferral: `CLAUDE.md`, `AGENTS.md`, `.context/plans/README.md`, current `.context/reviews/**`, and current `.context/plans/**`. No `.cursorrules` or `CONTRIBUTING.md` exists in this checkout. No active docs/policy files were found under `docs/`.

## Scheduled Findings

Security/correctness/data-loss findings are scheduled, not deferred. Restore-maintenance mutation gaps are treated as correctness issues because the repo already has a restore barrier pattern for mutating admin actions.

### C3-01 - Add restore-maintenance guards to bulk image updates

- Finding: C3-AGG-01.
- Original severity/confidence: High / High.
- Citation: `apps/web/src/app/actions/images.ts:928-933`, mutation region `apps/web/src/app/actions/images.ts:1008-1102`.
- Plan: add the same `getRestoreMaintenanceMessage(t('restoreInProgress'))` early return used by sibling image mutations before origin/admin checks and before any DB work.
- Tests: add or extend a source-contract test proving `bulkUpdateImages` has a restore-maintenance guard before DB mutation.
- Progress: DONE in `apps/web/src/app/actions/images.ts`; covered by `bulk-update-images.test.ts`.

### C3-02 - Add restore-maintenance guards to LR token create/revoke

- Finding: C3-AGG-02.
- Original severity/confidence: Medium / High.
- Citation: `apps/web/src/app/actions/lr-tokens.ts:27-99`, `apps/web/src/app/actions/lr-tokens.ts:102-118`, `apps/web/src/lib/admin-tokens.ts:216-231`.
- Plan: import `getRestoreMaintenanceMessage`, guard `createLrToken` and `revokeLrToken` before credential mutation, and leave `listLrTokens` read-only.
- Tests: add focused source-contract or unit tests that restore maintenance blocks create/revoke.
- Progress: DONE in `apps/web/src/app/actions/lr-tokens.ts`; covered by `lr-tokens-action.test.ts`.

### C3-03 - Skip public analytics writes during restore maintenance

- Finding: C3-AGG-03.
- Original severity/confidence: Low / Medium.
- Citation: `apps/web/src/app/actions/public.ts:357-408`, `apps/web/src/lib/data.ts:48-51`.
- Plan: return early from `recordPhotoView`, `recordTopicView`, and `recordSharedGroupView` when `isRestoreMaintenanceActive()` is true.
- Tests: add behavior coverage for invalid/over-limit/maintenance public analytics recorders.
- Progress: DONE in `apps/web/src/app/actions/public.ts`; covered by `public-actions.test.ts`.

### C3-04 - Align upload picker with runtime-supported formats

- Finding: C3-AGG-04.
- Original severity/confidence: Medium / High.
- Citation: `apps/web/src/components/upload-dropzone.tsx:175-177`, `apps/web/src/lib/process-image.ts:385-461`, `apps/web/src/lib/process-image.ts:879-887`.
- Plan: remove unsupported advertised extensions from the picker contract (`.arw`, `.heic`, `.heif`, `.bmp`) unless runtime capability detection is implemented. Keep accepted-format copy consistent with JPEG/PNG/WebP/AVIF/TIFF/GIF.
- Tests: update source-contract tests or add a small test that picker accept lists do not advertise known unsupported extensions.
- Progress: DONE in `apps/web/src/components/upload-dropzone.tsx`; covered by `client-source-contracts.test.ts`.

### C3-05 - Correct README nginx upload-serving guidance

- Finding: C3-AGG-05.
- Original severity/confidence: Low / High.
- Citation: `README.md:186`, `apps/web/README.md:49`, `apps/web/nginx/default.conf:167-175`, `apps/web/src/__tests__/nginx-config.test.ts:32-35`.
- Plan: update both README sections to say checked-in nginx proxies uploads to Next, with host-static guidance only as an optional custom-deploy caveat.
- Tests: doc/source review; existing nginx test remains the code contract.
- Progress: DONE in `README.md` and `apps/web/README.md`; existing nginx proxy contract remains covered by `nginx-config.test.ts`.

### C3-06 - Document the Lightroom upload nginx body-size exception

- Finding: C3-AGG-06.
- Original severity/confidence: Medium / High.
- Citation: `README.md:148`, `apps/web/README.md:46`, `apps/web/nginx/default.conf:122-160`, `CLAUDE.md:151`, `CLAUDE.md:558`.
- Plan: add `/api/admin/lr/upload` 216 MiB longest-prefix guidance to both README body-size lists.
- Tests: extend `nginx-config.test.ts` if straightforward to assert the LR location remains 216M and precedes/wins over generic `/api/admin/`.
- Progress: DONE in `README.md` and `apps/web/README.md`; LR longest-prefix/body-size contract covered by `nginx-config.test.ts`.

### C3-07 - Correct feed attribution docs/comments

- Finding: C3-AGG-07.
- Original severity/confidence: Low / High.
- Citation: `CLAUDE.md:170`, `apps/web/src/db/schema.ts:87-90`, `apps/web/src/__tests__/privacy-fields.test.ts:28-30`, `.context/plans/photographer-r22/README.md:36`, `apps/web/src/lib/data.ts:827-839`.
- Plan: update docs/comments to state `uploaded_by` is admin-only/future input and public Atom currently uses feed-level author until a safe public display-name field exists.
- Tests: comment/doc source review; keep privacy tests green.
- Progress: DONE in `CLAUDE.md`, `apps/web/src/db/schema.ts`, and existing feed data comments.

### C3-08 - Fix archive/map/year document title duplication

- Finding: C3-AGG-08.
- Original severity/confidence: Medium / High.
- Citation: `apps/web/src/app/[locale]/layout.tsx:24-27`, `apps/web/src/app/[locale]/(public)/timeline/page.tsx:30-32`, `apps/web/src/app/[locale]/(public)/map/page.tsx:18-20`, `apps/web/src/app/[locale]/(public)/year/[year]/page.tsx:37-44`.
- Plan: return bare route titles from timeline/map/year metadata and let the layout title template append the site name. Preserve explicit OpenGraph/Twitter titles where needed.
- Tests: add/extend metadata/source tests for no baked-in `| ${seo.title}` on templated page titles.
- Progress: DONE in timeline/map/year metadata; covered by `client-source-contracts.test.ts`.

### C3-09 - Make theme toggle accessible name stateful

- Finding: C3-AGG-09.
- Original severity/confidence: Low / High.
- Citation: `apps/web/src/components/nav-client.tsx:155-160`, `apps/web/src/lib/theme.ts:39-45`, `apps/web/messages/en.json:610`, `apps/web/messages/ko.json:610`.
- Plan: add localized current/next theme labels and compute an `aria-label` that exposes current state and next action.
- Tests: update nav e2e/source tests that currently query the static label, and assert the accessible name changes after activation if feasible.
- Progress: DONE in `nav-client.tsx` and locale messages; covered by `client-source-contracts.test.ts`.

### C3-10 - Add a map client-chunk loading fallback

- Finding: C3-AGG-10.
- Original severity/confidence: Low / High.
- Citation: `apps/web/src/components/map/map-loader.tsx:8-10`, `apps/web/src/components/map/map-client.tsx:108-112`.
- Plan: add a same-height localized loading fallback with `role="status"` for the dynamic Leaflet chunk.
- Tests: add a source-contract check for `dynamic(..., { loading })` or a component/source test if existing patterns support it.
- Progress: DONE in `map-loader.tsx` and map messages; covered by `map-thumb-wiring.test.ts`.

### C3-11 - Add behavior tests for public analytics recorders

- Finding: C3-AGG-11.
- Original severity/confidence: Medium / High.
- Citation: `apps/web/src/app/actions/public.ts:316-408`, `apps/web/src/__tests__/public-actions.test.ts:72-250`.
- Plan: test `recordPhotoView`, `recordTopicView`, and `recordSharedGroupView` for valid under-limit writes, invalid inputs, maintenance skip, and over-limit skip.
- Tests: targeted Vitest, then full `npm test --workspace=apps/web`.
- Progress: DONE in `public-actions.test.ts` with valid, invalid, maintenance, and over-budget recorder cases.

### C3-12 - Harden public-route rate-limit lint against unreachable calls

- Finding: C3-AGG-12.
- Original severity/confidence: Medium / High.
- Citation: `apps/web/scripts/check-public-route-rate-limit.ts:107-126`, `apps/web/src/__tests__/check-public-route-rate-limit.test.ts:103-179`.
- Plan: add failing fixtures for unreachable limiter calls and uncalled nested helper calls, then update the scanner to require an executed top-level pre-increment/check call before mutation or fail closed.
- Tests: `npm run lint:public-route-rate-limit --workspace=apps/web` and targeted scanner tests.
- Progress: DONE in `check-public-route-rate-limit.ts`; covered by targeted scanner tests and `lint:public-route-rate-limit`.

### C3-13 - Make admin metadata coverage filesystem-driven

- Finding: C3-AGG-13.
- Original severity/confidence: Low / High.
- Citation: `apps/web/src/__tests__/client-source-contracts.test.ts:35-53`, admin route tree under `apps/web/src/app/[locale]/admin`.
- Plan: replace or supplement the static list with filesystem enumeration that proves every admin page is covered by route or ancestor metadata, with explicit exemptions.
- Tests: targeted `client-source-contracts.test.ts`.
- Progress: DONE in `client-source-contracts.test.ts` with filesystem-driven admin page metadata coverage.

### C3-14 - Add Docker/Compose loopback binding regression coverage

- Finding: C3-AGG-20.
- Original severity/confidence: Medium / High.
- Citation: `apps/web/Dockerfile:80-85`, `apps/web/docker-compose.yml:14-21`, `apps/web/src/__tests__/nginx-config.test.ts:7-36`.
- Plan: add a static test that asserts the production Dockerfile default and compose service bind `HOSTNAME` to `127.0.0.1`.
- Tests: targeted config test plus full Vitest.
- Progress: DONE in `nginx-config.test.ts` for Dockerfile and Compose loopback binding.

### C3-15 - Isolate client-safe semantic constants from server CLIP helpers

- Finding: C3-AGG-22.
- Original severity/confidence: Low / High.
- Citation: `apps/web/src/components/search.tsx:19`, `apps/web/src/lib/clip-embeddings.ts:18-40`.
- Plan: move client-needed constants to a pure shared module with no `process`/`Buffer`/server imports, update client/server imports, and add an import-boundary/source-contract test if feasible.
- Tests: typecheck, build, targeted source-contract tests.
- Progress: DONE via `clip-embedding-constants.ts`; covered by `client-source-contracts.test.ts` and `semantic-scan-limit-source.test.ts`.

### C3-16 - Fix immediate service-worker deploy artifact consistency

- Finding: C3-AGG-15.
- Original severity/confidence: Medium / High.
- Citation: `apps/web/package.json:10-11`, `apps/web/scripts/build-sw.ts:28-56`, `apps/web/Dockerfile:71-75`, `apps/web/docker-compose.yml:23-26`, `apps/web/public/sw.js:21-29`.
- Plan: prefer the narrow deploy-safe fix: mount only mutable uploads from `./public/uploads` rather than all of `./public`, so generated immutable assets such as `sw.js` come from the built image. Preserve persistent upload guarantees.
- Tests: add/update compose/source tests for public mount shape, run build so `sw.js` stamp is regenerated and committed if changed.
- Progress: DONE in `apps/web/docker-compose.yml`; covered by `nginx-config.test.ts`. `sw.js` build stamp verification pending full build.

### C3-17 - Correct misleading On-This-Day query comment

- Finding: C3-AGG-16 partial.
- Original severity/confidence: Medium / High.
- Citation: `apps/web/src/lib/data-timeline.ts:88-94`.
- Plan: update the misleading comment to state `MONTH()`/`DAY()` predicates are not sargable and are accepted only at current personal-gallery scale. Defer the generated-column/range-index work separately.
- Tests: source review; full gates.
- Progress: DONE in `apps/web/src/lib/data-timeline.ts`.

## Deferred Findings

See `.context/plans/cycle-3-2026-06-29-deferred.md`. Every aggregate finding not fully scheduled above is explicitly recorded there with original severity/confidence and exit criteria. For split finding C3-AGG-16, only the comment-honesty part is scheduled above; the schema/query-performance work is deferred there.

## Required Gates

Run the full configured cycle gates before final commit/push/deploy:

- `npm run lint --workspace=apps/web`
- `npm run lint:api-auth --workspace=apps/web`
- `npm run lint:action-origin --workspace=apps/web`
- `npm run lint:public-route-rate-limit --workspace=apps/web`
- `npm run typecheck --workspace=apps/web`
- `npm run build --workspace=apps/web`
- `npm test --workspace=apps/web`

## Gate Evidence

Targeted evidence before full gates:

- `npm test --workspace=apps/web -- bulk-update-images.test.ts lr-tokens-action.test.ts public-actions.test.ts check-public-route-rate-limit.test.ts`
- `npm run lint:public-route-rate-limit --workspace=apps/web`
- `npm run lint:action-origin --workspace=apps/web`
- `npm test --workspace=apps/web -- client-source-contracts.test.ts map-thumb-wiring.test.ts nginx-config.test.ts semantic-scan-limit-source.test.ts`
- `npm test --workspace=apps/web -- public-actions.test.ts`
- `npm run lint --workspace=apps/web -- src/components/map/map-loader.tsx src/components/nav-client.tsx src/components/search.tsx`

Full configured gates:

- PASS `npm run lint --workspace=apps/web` (initial warning fixed in `039e9627`, rerun clean)
- PASS `npm run lint:api-auth --workspace=apps/web`
- PASS `npm run lint:action-origin --workspace=apps/web`
- PASS `npm run lint:public-route-rate-limit --workspace=apps/web`
- PASS `npm run typecheck --workspace=apps/web`
- PASS `npm run build --workspace=apps/web`
- PASS `npm test --workspace=apps/web`

Additional final-head checks after the last test-only commit:

- PASS `npm run lint --workspace=apps/web -- src/__tests__/focus-visible-rings-cycle17.test.ts`
- PASS `npm run typecheck --workspace=apps/web`
