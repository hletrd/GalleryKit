# Cycle 80/100 Implementation Plan

Start HEAD: `8c4999c9294e0196608b4a0bce8078edc3be2366`.
Review aggregate: `.context/reviews/cycle-80-2026-07-01/_aggregate.md`.

## Goal

Close the safe Cycle 80 findings, keep review/plan ledgers current, run all required gates, commit/push all work, and deploy with `npm run deploy` from the repo root.

## Scheduled Fixes

1. `C80-01`: harden `apps/web/scripts/check-public-route-rate-limit.ts` so dynamic imports of expensive public-route modules fail closed before limiter checks; add fixtures for dynamic `sharp` and dynamic filesystem imports.
2. `C80-02`: add a generic background DB write drain and include it in `apps/web/src/instrumentation.ts` graceful shutdown; add a source contract test.
3. `C80-03`: close Cycle 79's ledger by recording `8c4999c9` commit/push/deploy evidence, move Cycle 79 out of active state, and advance the latest aggregate pointer to Cycle 80.
4. `C80-04`: guard `apps/web/scripts/backfill-alt-text.ts` with `assertNoDurableRestoreMaintenanceForScript` before reads and write batches; extend the sidecar guard source contract.
5. `C80-05`: provide localized map marker display titles and use them for popup thumbnail alt text, popup action labels, and accessible list fallback; add a source contract test.

## Deferred This Cycle

`C80-06` is deferred in `.context/plans/cycle-80-2026-07-01-deferred.md`.

Carry-forward deferred items remain in the deferred file.

## Regression Coverage

- Public-route scanner: dynamic `import('sharp')` and dynamic `import('node:fs/promises')` must be classified as expensive GET/HEAD work unless a limiter gate runs first.
- Shutdown: `instrumentation.ts` must dynamically import and await `drainBackgroundDbWrites()` alongside queue and shared-group drains.
- Alt-text sidecar: `backfill-alt-text.ts` must call `assertNoDurableRestoreMaintenanceForScript` before initial reads and before writes; sidecar guard contract must include this script.
- Map accessibility: map markers must carry localized `displayTitle`, and raw `String(marker.id)` / `marker.title ?? marker.id` fallbacks must not remain in popup alt/aria/list labels.
- Ledger: Cycle 79 must no longer be listed as active, and `.context/reviews/_aggregate.md` must point at Cycle 80.

## Validation

Run required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then signed commit, `git pull --rebase`, push, and deploy with `npm run deploy`.

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run required gates.
- [ ] Commit, pull --rebase, push.
- [ ] Deploy with `npm run deploy`.

## Gate Evidence

- Focused regression: `npm test --workspace=apps/web -- --run src/__tests__/check-public-route-rate-limit.test.ts src/__tests__/instrumentation-sigterm.test.ts src/__tests__/background-db-writes.test.ts src/__tests__/cycle-71-source-contracts.test.ts src/__tests__/map-thumb-wiring.test.ts` - pass: 5 files passed, 107 tests passed. First focused run found one brittle source-contract assertion in the new alt-text sidecar test; fixed before required gates.
- Focused scanner gate: `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
- `npm run lint --workspace=apps/web` - pass.
- `npm run lint:api-auth --workspace=apps/web` - pass: both admin API routes OK.
- `npm run lint:action-origin --workspace=apps/web` - pass: all mutating server actions enforce same-origin provenance.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
- `npm run typecheck --workspace=apps/web` - pass.
- `npm run build --workspace=apps/web` - pass. Local sitemap generation logged the expected unavailable local MySQL fallback and exited successfully.
- `npm test --workspace=apps/web` - pass: 301 files passed, 2 skipped; 2830 tests passed, 4 skipped.
- `git diff --cached --check` - pass.

## Gate Fix Iterations

1.
