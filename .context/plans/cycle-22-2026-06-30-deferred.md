# Cycle 22/100 Deferred Findings

Per review-plan-fix deferred rules: every cycle-22 aggregate finding is either scheduled in `cycle-22-2026-06-30-plan.md` or recorded here. Original severity/confidence are preserved and not downgraded.

Repo rules consulted first: `CLAUDE.md`, `AGENTS.md`, `.context/plans/README.md`, current `.context/**` markdown inventory, and `docs/superpowers/**`. No `.cursorrules` or `CONTRIBUTING.md` exists. Deferred work remains bound by repo policy when reopened: GPG-signed commits, Conventional Commits + gitmoji, no force-push, no `--no-verify`, Node 24+/TypeScript 6+, and per-cycle deploy policy when implemented.

Security, confirmed correctness, and data-loss findings are scheduled unless noted as architecture/performance/test-surface risks with no reproduced current failure and an explicit repo boundary.

| ID | Finding | File:line citation | Original severity / confidence | Reason for deferral | Exit criterion |
|----|---------|--------------------|--------------------------------|---------------------|----------------|
| AGG22-07 | Upload ingest has multiple implementation owners | `apps/web/src/app/actions/images.ts`, `apps/web/src/app/api/admin/lr/upload/route.ts`, `.context/reviews/_aggregate.md:139` | Medium / High | Broad architectural extraction on the upload hot path. Current browser/LR paths are heavily guarded by existing behavior/source tests; this cycle schedules a quota settlement guardrail and the confirmed advisory-lock issue first. | Before adding any upload-time privacy, quota, metadata, or processing setting, or when browser/LR parity drifts again. |
| AGG22-08 | Queue workers can pin most shared MySQL pool connections during image encoding | `apps/web/src/lib/image-queue.ts`, `apps/web/src/lib/admin-backfill-runner.ts`, `.context/reviews/_aggregate.md:153` | Medium / High | Operational/performance architecture risk, not a reproduced gate failure. Current production contract is single web instance with queue concurrency default 1; `CLAUDE.md` "Runtime topology" explicitly says the shipped Docker Compose deployment is a "single web-instance / single-writer topology" and warns not to horizontally scale until coordination state is moved. | If `QUEUE_CONCURRENCY` or admin backfill concurrency is raised, pool warnings appear, or queue/backfill connection ownership is next refactored. |
| AGG22-09 | Single-process topology is documented but not enforced | `apps/web/src/lib/restore-maintenance.ts`, `apps/web/src/lib/upload-tracker-state.ts`, `.context/reviews/_aggregate.md:167` | Medium / High | Topology enforcement project. Repo rule currently fences this: `CLAUDE.md` says "do not horizontally scale the web service unless those coordination states are moved to a shared store." | Before any multi-instance deployment/orchestrator support, or if a second web instance is detected in production. |
| AGG22-10 | Mutable `topics.slug` natural key requires manual fan-out | `apps/web/src/app/actions/topics.ts`, `apps/web/src/db/schema.ts`, `apps/web/src/lib/smart-collections.ts`, `.context/reviews/_aggregate.md:181` | Medium / High | Schema redesign from mutable natural keys to surrogate ids is migration-heavy and already fenced by existing topic rename tests from prior cycles. No fresh missed child table was reproduced in this cycle's tracer review. | Before adding a new topic-slug-referencing table/store, or if topic rename fan-out misses any dependent state. |
| AGG22-14 | Lightroom upload route lacks route-behavior regression coverage | `apps/web/src/app/api/admin/lr/upload/route.ts`, `.context/reviews/_aggregate.md:237` | Medium / High | Valuable coverage, but this cycle prioritizes confirmed runtime correctness and UI high items. Existing auth scanner, HDR gate, source contract, and route-specific tests still cover core LR upload invariants. | When LR upload route behavior is next changed, or before publishing an official external client/plugin. |
| AGG22-15 | CLIP inference pool tests rely on string matching instead of concurrency behavior | `apps/web/src/lib/clip-model.ts`, `apps/web/src/__tests__/clip-model-contract.test.ts`, `.context/reviews/_aggregate.md:251` | Medium / Medium-High | Requires a deterministic fake-inference seam. Current production path has bounded queue/timeouts and this cycle has higher-confidence correctness work. | When CLIP queue code is next refactored, abort/timeout bugs appear, or a fake model seam is introduced. |
| AGG22-16 | Real CLIP/offline model smoke coverage is skipped by default | CLIP route/model tests, `.context/reviews/_aggregate.md:265` | Medium / Medium-High | Model weights are intentionally not baked into the repo or image; default gates cannot require host-seeded weights. Operator runbook already requires seeding/backfill before production mode. | Before semantic-search model/version upgrades or production CLIP deployment changes, run/add an opt-in seeded model smoke gate. |
| AGG22-17 | Admin image management remains desktop-table-only on narrow screens | `apps/web/src/components/image-manager.tsx`, `.context/reviews/_aggregate.md:279` | Medium / High | Responsive admin data-surface redesign is broad. Current table is functional with horizontal scroll, and this cycle schedules higher-severity public accessibility and confirmation issues first. | If admin mobile/small-tablet management becomes a product priority or `ImageManager` is next redesigned. |
| AGG22-21 | Dynamic public gallery first pages still pay grouped exact-count work | `apps/web/src/lib/data.ts`, public gallery pages, `.context/reviews/_aggregate.md:331` | Medium / High | Performance optimization needing production latency evidence and product decision on exact counts. No current gate failure or user-visible outage reproduced. | If public first-page latency traces show count work as a bottleneck or exact count UI is next redesigned. |
| AGG22-22 | Infinite masonry keeps all loaded cards mounted | `apps/web/src/components/home-client.tsx`, `apps/web/src/components/load-more.tsx`, `.context/reviews/_aggregate.md:338` | Medium / High | Virtualized masonry is a substantial UI architecture change with accessibility/layout risk. | If deep-scroll INP/heap traces degrade or masonry rendering is next redesigned. |
| AGG22-23 | CSV export materializes the full export in memory | `apps/web/src/app/[locale]/admin/db-actions.ts`, `.context/reviews/_aggregate.md:345` | Medium / High | Admin-triggered scale issue; streaming export is a route/API redesign and no memory failure was reproduced. | If large CSV exports time out/OOM or admin export code is next touched. |
| AGG22-24 | Admin analytics fans out aggregate scans on shared DB pool | `apps/web/src/app/[locale]/admin/(protected)/analytics/page.tsx`, `apps/web/src/lib/analytics-data.ts`, `.context/reviews/_aggregate.md:352` | Low-Medium / Medium | Admin-only performance risk without observed pool starvation. | If analytics latency/pool warnings appear, especially for all-time windows. |
| AGG22-25 | Timeline/date archive filters are non-sargable | `apps/web/src/lib/data-timeline.ts`, timeline/year pages, `.context/reviews/_aggregate.md:359` | Low / High | Low-priority query optimization with no current latency evidence. | If timeline/year pages become slow or date index strategy changes. |
| AGG22-26 | Shared topic lists compute per-topic latest timestamps on common renders | topic list data paths, `.context/reviews/_aggregate.md:366` | Low / Medium | Low-priority render/query optimization. | If topic list latency appears in traces or topic summary data is next refactored. |
| AGG22-27 | Audit retention deletes all expired rows in one statement | `apps/web/src/lib/audit.ts`, `.context/reviews/_aggregate.md:373` | Low / High | Low-volume operational risk. View retention is chunked because it is higher volume; audit GC lock pressure was not observed. | If audit row volume grows, retention window shrinks, or audit cleanup lock time appears. |
| AGG22-28 | Upload fallback serving validates one path and opens a later path by name | `apps/web/src/lib/serve-upload.ts`, `.context/reviews/_aggregate.md:380` | Low / Medium | Low-confidence TOCTOU risk on public derivative fallback; no exploit/failure reproduced and derivative directories are whitelisted. | When `serve-upload.ts` is next touched or symlink/path race concerns are reproduced. |
| AGG22-29 | Docker image and apt inputs are mutable | `apps/web/Dockerfile`, `.context/reviews/_aggregate.md:387` | Low / High | Supply-chain hardening preference. Pinning digests increases maintenance burden and is not required by current repo policy. | Before a hardened/reproducible-build release process or if upstream image drift causes build/runtime issues. |
| AGG22-30 | Deploy helper executes shell from trusted but unchecked env file | `scripts/deploy-remote.sh`, `.env.deploy.example`, `.context/reviews/_aggregate.md:394` | Low / Medium | `.env.deploy` is gitignored and already treated as a trusted operator secret file. Not a multi-operator untrusted input surface today. | If deployment is delegated to less-trusted operators or deploy command overrides become user-editable UI/config. |
| AGG22-31 | Nginx template unsafe if exposed directly as public edge | `apps/web/nginx/default.conf`, `.context/reviews/_aggregate.md:401` | Low / Medium | Current docs define the nginx template as sitting behind a TLS-terminating edge. | If nginx becomes the public edge or TLS/HSTS is expected from this checked-in template. |
| DOC22-04 | Historical CLIP plan contains obsolete snippets | `docs/superpowers/plans/2026-06-15-clip-semantic-search.md`, `.context/reviews/_aggregate.md:413` | Low / High | Historical plan artifact; this cycle schedules clarification only if simple. It is not runtime/operator authoritative compared with README/CLAUDE.md. | If the plan is linked as current operator guidance or edited for CLIP work. |
| UI22-06 | Upload staging becomes cramped on phones | `apps/web/src/components/upload-dropzone.tsx`, `.context/reviews/_aggregate.md:415` | Low-Medium / Medium | Narrow-screen polish; current upload flow remains functional and touch-target tests pass. | If mobile admin upload becomes a priority or upload staging UI is next redesigned. |
| UI22-07 | Admin navigation is a flat ten-link wrap with no task grouping | `apps/web/src/components/admin-nav.tsx`, `.context/reviews/_aggregate.md:416` | Low-Medium / Medium | Information architecture polish. No workflow-blocking issue or a11y failure reproduced. | When adding another admin section or redesigning admin navigation. |

## Scheduled, Not Deferred

Scheduled in `cycle-22-2026-06-30-plan.md`: `AGG22-01`, `AGG22-02`, `AGG22-03`, `AGG22-04`, `AGG22-05`, `AGG22-06`, `AGG22-11`, `AGG22-12`, `AGG22-13`, `AGG22-18`, `AGG22-19`, `AGG22-20`, `DOC22-01`, `DOC22-02`, `DOC22-03`, `TEST22-04`, `TEST22-05`, `V22-04`.

## Gate Warning Notes

| ID | Warning | File:line citation | Original severity / confidence | Reason for deferral | Exit criterion |
|----|---------|--------------------|--------------------------------|---------------------|----------------|
| GATE22-W01 | `npm run build --workspace=apps/web` logs `[sitemap] falling back to homepage-only sitemap` when local MySQL is unavailable at `127.0.0.1:3306`; build still exits 0. | `apps/web/src/app/sitemap.ts`, build output from cycle 22 | Low / High | Environmental local-build warning. The sitemap implementation already catches database unavailability and intentionally emits a homepage-only fallback so builds can complete without a local DB. Fixing it would require either starting local MySQL for every build gate or changing the accepted offline fallback behavior, which is outside this cycle's review findings. | Reopen if production deploy/build emits the fallback with a configured DB, if sitemap generation becomes a blocking gate, or if repo policy changes to require database-backed sitemap data during local builds. |
