# Cycle 24/100 Implementation Plan

Date: 2026-06-30 KST  
Review source: `.context/reviews/_aggregate.md`  
Status: DONE - implemented and verified; pending commit/push/deploy bookkeeping

## Repo Rules Read Before Planning

Read order satisfied for deferral policy: `CLAUDE.md`, `AGENTS.md`, `.context/**` index/current plans and reviews, and `docs/superpowers/**`. No `.cursorrules` or `CONTRIBUTING.md` exists in this checkout. Cycle 23's implementation plan still has pending commit/deploy bookkeeping checkboxes, so it was not archived in this cycle.

## Scheduled Fixes

Security, correctness, and gate-break findings are not deferred. Larger performance/architecture/product redesigns are recorded in `cycle-24-2026-06-30-deferred.md`.

### P24-01 - Fix CI E2E DB name and lock workflow safety [done]

- Finding: `AGG24-01`
- Severity/confidence: High / High
- Files: `.github/workflows/quality.yml`, `apps/web/src/__tests__/seed-e2e-safety.test.ts`
- Plan: change the CI MySQL/database env to a disposable E2E/CI DB name accepted by `seed-e2e.ts`; add a source-contract test proving workflow E2E DB configuration remains disposable or explicitly opted in.
- Acceptance: seed safety tests pass and workflow cannot drift back to non-disposable `gallery` without test failure.
- Result: `.github/workflows/quality.yml` now uses `gallery_ci`; `seed-e2e-safety.test.ts` locks the disposable DB contract.

### P24-02 - Refuse group/world-readable deploy env files [done]

- Findings: `AGG24-02`, `AGG24-03`
- Severity/confidence: High / High and Medium / High
- Files: `scripts/deploy-remote.sh`, `apps/web/src/__tests__/deploy-script-contract.test.ts`
- Plan: reject any group/world permission bit before sourcing the deploy env file. Add tests for `0600` allowed and `0644`/`0640`/unsafe modes rejected.
- Acceptance: deploy contract test covers read-bit refusal; shell syntax remains valid.
- Result: `scripts/deploy-remote.sh` rejects any group/world permission bit before sourcing `.env.deploy`; deploy contract tests cover ordering and chmod guidance.

### P24-03 - Clamp foreground queue concurrency to shared-pool budget [done]

- Finding: `AGG24-04`
- Severity/confidence: Medium / High
- Files: `apps/web/src/lib/image-queue.ts`, queue/backfill concurrency tests, `CLAUDE.md`
- Plan: reuse or mirror the admin-backfill reserve formula to compute an effective queue concurrency that cannot consume live DB headroom. Keep default behavior unchanged at 1. Document the clamp for operators.
- Acceptance: tests prove requested `QUEUE_CONCURRENCY=8` clamps below the shared-pool starvation point at the shipped pool limit.
- Result: `resolveImageQueueConcurrency` reserves live DB headroom; tests prove the shipped pool mirror clamps requested concurrency 8 to 5.

### P24-04 - Filter semantic text scan to processed images before scoring [done]

- Finding: `AGG24-05`
- Severity/confidence: Medium / Medium
- Files: `apps/web/src/app/api/search/semantic/route.ts`, semantic route tests
- Plan: mirror similar-photo search by joining `images` and requiring `images.processed = true` in the candidate scan.
- Acceptance: test proves an unprocessed/stale high-score embedding cannot hide a processed lower-score result.
- Result: semantic scan joins `images` and filters `processed = true`; route tests lock the join/filter contract.

### P24-05 - Reset photo swipe state on `touchcancel` [done]

- Finding: `AGG24-06`
- Severity/confidence: Low / High
- Files: `apps/web/src/components/photo-navigation.tsx`, targeted test/source-contract if practical
- Plan: add a shared snap-back/reset handler and register/remove `touchcancel` alongside `touchend`.
- Acceptance: `touchcancel` path resets `swipeOffset` and `isSwiping`.
- Result: `photo-navigation.tsx` shares the swipe reset path between vertical cancel and `touchcancel`.

### P24-06 - Abort similar-photo fetches on unmount [done]

- Finding: `AGG24-07`
- Severity/confidence: Low / High
- Files: `apps/web/src/components/similar-photos.tsx`, targeted component/source test if practical
- Plan: add an `AbortController`, mounted guard, and cleanup so post-await state commits are skipped after unmount/abort. Allow retry after an aborted first fetch.
- Acceptance: unmount cleanup aborts pending request and does not mark the panel permanently fetched.
- Result: `similar-photos.tsx` uses an `AbortController`, mounted guard, cleanup abort, and retry reset for aborted fetches.

### P24-07 - Document `IMAGE_CLEANUP_CONCURRENCY` [done]

- Finding: `AGG24-08`
- Severity/confidence: Low / High
- Files: `apps/web/.env.local.example`, `CLAUDE.md`
- Plan: add default/max and clarify it controls post-DB delete cleanup, not upload queue or encoder concurrency.
- Acceptance: env example and operator variable table mention the knob consistently.
- Result: `.env.local.example` and `CLAUDE.md` document the cleanup concurrency knob.

### P24-08 - Correct EXIF-derived auto-alt-text comments [done]

- Finding: `AGG24-09`
- Severity/confidence: Low / High
- Files: `apps/web/src/lib/caption-generator.ts`, `apps/web/src/lib/gallery-config-shared.ts`, `apps/web/src/lib/photo-title.ts`
- Plan: reword current comments to EXIF-derived auto-alt hints with a future model hook, not current Florence/AI generation.
- Acceptance: active source comments no longer imply current Florence/AI captioning.
- Result: active source comments now describe EXIF-derived hints and future captioning-model hooks; old migration content was left unchanged to preserve hashes.

### P24-09 - Polish Korean privacy terminology [done]

- Finding: `AGG24-10`
- Severity/confidence: Low / High
- Files: `apps/web/messages/ko.json`
- Plan: replace user-facing privacy "토픽" wording with "카테고리" where it describes public/category visibility.
- Acceptance: i18n key parity remains green.
- Result: Korean privacy copy now uses `카테고리`; the older source-contract test was updated to the corrected term.

### P24-10 - Localize JSON-LD/feed photo fallback labels [done]

- Finding: `AGG24-11`
- Severity/confidence: Low / High
- Files: localized public metadata/feed routes and tests if available
- Plan: use localized `common.photo` in localized route JSON-LD/feed fallbacks. Leave the unlocalized root feed as default-locale output unless a safe localized context exists.
- Acceptance: Korean localized routes no longer emit English `Photo ${id}` fallbacks.
- Result: localized home/topic/smart-collection JSON-LD and localized topic feed now use `common.photo` for fallback names; the unlocalized root feed remains default-locale output.

### P24-11 - Correct admin SEO share-preview copy [done]

- Finding: `AGG24-12`
- Severity/confidence: Medium / High
- Files: `apps/web/messages/en.json`, `apps/web/messages/ko.json`
- Plan: state that public photo pages use generated photo cards while share links use generic noindex metadata to avoid unthrottled share-key lookups.
- Acceptance: admin helper copy matches route behavior.
- Result: English and Korean SEO settings helper copy now distinguishes public generated photo cards from generic private share metadata.

### P24-12 - Narrow color-fidelity positioning [done]

- Finding: `AGG24-13`
- Severity/confidence: Medium / High
- Files: `README.md`, `CLAUDE.md` if needed
- Plan: change top-level positioning from absolute/every-browser accuracy to browser-managed/best-effort color-faithful delivery with P3/Firefox/HDR caveats.
- Acceptance: docs remain honest without weakening the product's photographer-intent positioning.
- Result: README/CLAUDE wording now says browser/display/codec-managed color-faithful delivery instead of absolute every-viewer accuracy.

### P24-13 - Add accessible new-tab cues [done]

- Finding: `AGG24-14`
- Severity/confidence: Low / High
- Files: `apps/web/src/components/footer.tsx`, `apps/web/src/components/photo-viewer.tsx`, `apps/web/src/components/info-bottom-sheet.tsx`, messages if needed
- Plan: add `opens in new window` accessible labels or hidden text to target-blank links and normalize `rel="noopener noreferrer"`.
- Acceptance: links that open a new tab expose the cue in accessible names.
- Result: footer GitHub and admin GPS map links expose localized new-window cues and use `noopener noreferrer`.

### P24-14 - Split mobile viewer description from desktop shortcuts [done]

- Finding: `AGG24-15`
- Severity/confidence: Medium / High
- Files: `apps/web/src/components/photo-viewer.tsx`, messages/tests if needed
- Plan: keep desktop shortcut hint visible for keyboard-capable layouts but avoid attaching desktop-only shortcuts to the root mobile accessibility description.
- Acceptance: mobile/screen-reader description is concise and not keyboard-only.
- Result: the viewer root now references a concise localized description, while desktop shortcuts remain visible only on `md` and above.

## Prompt 3 Execution Notes

- Use Ralph-style persistence: implement until plan items are complete, gates pass, changes are committed/pushed, and deploy is attempted once.
- Run every configured gate from the cycle prompt against the repo.
- Fine-grained signed Conventional Commit + gitmoji commits; pull with rebase before push.
- Per-cycle deploy command: `npm run deploy`.

## Progress

- [x] Aggregate review written.
- [x] Plan and deferred findings written.
- [x] P24-01 through P24-14 implemented.
- [x] Deferred findings preserved with original severity/confidence and exit criteria.
- [x] Full gate list green: `lint`, `lint:api-auth`, `lint:action-origin`, `lint:public-route-rate-limit`, `typecheck`, `build`, `test`.
- [ ] Signed commit(s) pushed.
- [ ] Per-cycle deploy recorded.

## Validation

- `npm run lint --workspace=apps/web` - pass, no warnings after fixing the similar-photo unused catch warning.
- `npm run lint:api-auth --workspace=apps/web` - pass.
- `npm run lint:action-origin --workspace=apps/web` - pass.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
- `npm run typecheck --workspace=apps/web` - pass.
- `npm run build --workspace=apps/web` - pass; local build emitted the existing sitemap fallback because no local MySQL listener is available on `127.0.0.1:3306`.
- `npm test --workspace=apps/web` - pass: 267 files, 2498 tests passed; 2 files and 4 tests skipped.
