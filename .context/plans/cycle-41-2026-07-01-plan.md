# Cycle 41/100 Implementation Plan

Start HEAD: `ae71bd5a`.
Review aggregate: `.context/reviews/cycle-41-2026-07-01/_aggregate.md`.

## Goal

Close the Cycle 41 scanner, documentation, and photographer-facing product gaps without schema changes, deploy-helper changes, or new dependencies.

## Scheduled Fixes

1. `TV-C41-01`: require public-route limiter gates to be positive over-limit checks. Reject direct negation and equality-to-false checks for both mutating handlers and expensive GET/HEAD handlers.
2. `TV-C41-02`: preserve local aliases for named `db` imports from `@/db` and treat property-access calls on those names as expensive public read work.
3. `TV-C41-03`: normalize relative imports against the route path so known DB-backed server modules remain expensive even without the `@/` alias.
4. `TV-C41-04`: make public mutating handlers require the limiter before either mutation or expensive public work.
5. `TV-C41-05`: collect local aliases for pre-origin auth/session read helpers and use them in the action-origin pre-guard scanner.
6. `TV-C41-06`: collect local aliases for named `db` imports and use them in protected Drizzle relational-read detection.
7. `DOC-C41-01`: add the read-only `tsconfig.json` mount to the CLIP seed/backfill sidecar examples in `CLAUDE.md`.
8. `DOC-C41-02`: align root `README.md` GPS stripping copy with the enforced lock-after-first-photo behavior.
9. `UX-C41-01`: suppress the whole-library `SimilarPhotos` panel in shared photo/group viewer contexts.
10. `UX-C41-02`: keep similarity scores internal for sorting but omit numeric `score` fields from public semantic/similar API JSON and client-facing types.

## Regression Coverage

- Extend `check-public-route-rate-limit.test.ts` for inverted gates, aliased DB imports, relative expensive imports, and expensive work before mutating-handler limiters.
- Extend `check-action-origin.test.ts` for aliased auth/session reads and aliased Drizzle relational reads.
- Update semantic/similar route tests to assert order without exposing public score fields.
- Add source-contract coverage for the CLIP `tsconfig.json` mount, GPS stripping README copy, and shared-view `SimilarPhotos` guard.

## Validation

Run the required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then sign, pull --rebase, push, and deploy with `npm run deploy` from the repo root.

## Progress

- [x] Write aggregate/deferred artifacts.
- [x] Implement scheduled fixes.
- [x] Add focused regression tests.
- [x] Run gates.
- [ ] Commit, push, deploy.
