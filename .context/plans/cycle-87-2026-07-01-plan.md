# Cycle 87/100 Implementation Plan

Start HEAD: `ee83c13835e5d09f2adff272536c644c2e5fc260`.
Review aggregate: `.context/reviews/cycle-87-2026-07-01/_aggregate.md`.

## Goal

Close the safe Cycle 87 ledger finding, keep review/plan indexes current, run all required gates, commit/push, deploy with `npm run deploy`, and smoke `https://gallery.atik.kr`.

## Scheduled Fixes

1. `C87-01`: close Cycle 86's release ledger by marking commit/pull-rebase/push and deploy complete, recording signed `ee83c13835e5d09f2adff272536c644c2e5fc260` / `origin/master` evidence, recording that Cycle 87 was started from the deployed `ee83c13` baseline, and moving Cycle 86 out of the active plan index.
2. `C87-02`: keep Cycle 87 review/plan artifacts trackable by extending the existing `.gitignore` whitelist for the new plan and deferred files.

## Deferred This Cycle

No new Cycle 87 findings are deferred.

Carry-forward deferred items remain in `.context/plans/cycle-87-2026-07-01-deferred.md`.

## Regression Coverage

- Ledger: Cycle 86 must no longer show unchecked release tasks after Prompt 3.
- Index: `.context/plans/README.md` must list Cycle 87 as active and Cycle 86 as recent with its terminal state.
- Review aggregation: `.context/reviews/_aggregate.md` must point to the Cycle 87 aggregate.

## Validation

Run required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `BASE_URL=https://gallery.atik.kr npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then signed commit, `git pull --rebase`, push, deploy with `npm run deploy`, and smoke:

- `curl -fsSIL https://gallery.atik.kr`
- `curl -fsS https://gallery.atik.kr/api/health`

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run required gates.
- [x] Commit, pull --rebase, push.
- [x] Deploy with `npm run deploy`.

## Gate Evidence

- `npm run lint --workspace=apps/web` - pass.
- `npm run lint:api-auth --workspace=apps/web` - pass: both admin API routes OK.
- `npm run lint:action-origin --workspace=apps/web` - pass: all mutating server actions enforce same-origin provenance.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
- `npm run typecheck --workspace=apps/web` - pass.
- `BASE_URL=https://gallery.atik.kr npm run build --workspace=apps/web` - pass. Local sitemap generation logged the expected unavailable local MySQL fallback and exited successfully.
- `npm test --workspace=apps/web` - pass: 301 files passed, 2 skipped; 2838 tests passed, 4 skipped.

## Gate Fix Iterations

None so far.

## Terminal Release Evidence

- Signed commit: `afc2bf5245932fd421d84e8d29ca2e0be01280fb` (`docs(review): preserve cycle 87 audit trail`) with a good GPG signature.
- Remote state: Cycle 88 started with clean `master...origin/master` at `afc2bf5245932fd421d84e8d29ca2e0be01280fb`.
- Deploy baseline: Cycle 88 invocation identified `afc2bf5245932fd421d84e8d29ca2e0be01280fb` as the current deployed master.
- Smoke before Cycle 88 edits: `curl -fsSIL https://gallery.atik.kr` returned HTTP 307 to `/en`; `curl -fsS https://gallery.atik.kr/api/health` returned `{"status":"ok"}`.
- Cycle 88 started from deployed `afc2bf5`.
