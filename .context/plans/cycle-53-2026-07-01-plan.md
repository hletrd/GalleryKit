# Cycle 53/100 Implementation Plan

Start HEAD: `17db8e38`.
Review aggregate: `.context/reviews/cycle-53-2026-07-01/_aggregate.md`.

## Goal

Fix the actionable Cycle 53 findings, keep review/plan ledgers current, run all required gates, commit, push, and deploy with `npm run deploy` from the repo root.

## Scheduled Fixes

1. `C53-01`: reject `semantic_search_mode='production'` inside `updateGallerySettings()` so Settings cannot become a production semantic-search enablement path. Preserve the shared validator because `production` remains a valid operator-owned stored DB value.
2. `C53-02`: make the healed stored-production Settings state explicit and clearable. A stored raw `production` row without the production env opt-in must not display as ordinary Disabled; selecting Disabled or Stub must submit that supported value.
3. `C53-03`: close the Cycle 52 ledger with terminal commit/push evidence, advance the plan/review pointers to Cycle 53, and add focused coverage that honestly reflects source-contract and action-boundary behavior.

## Deferred This Cycle

No new Cycle 53 findings are deferred. Carry-forward items are recorded in `.context/plans/cycle-53-2026-07-01-deferred.md`.

## Regression Coverage

- Add action-level coverage proving `updateGallerySettings({ semantic_search_mode: 'production' })` returns a Settings-specific error before any DB transaction, while `disabled` and `stub` remain accepted.
- Extend source-contract coverage to pin the distinct stored-production-inactive select state, the disabled production-active display item, and the Settings-action production-write guard.
- Keep i18n key parity for new English/Korean copy.

## Validation

Run required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then sign, `git pull --rebase`, push, and deploy with `npm run deploy`.

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run required gates.
- [ ] Commit, pull --rebase, push.
- [ ] Deploy with `npm run deploy`.

## Gate Evidence

- Focused pre-gate checks:
  - `npm test --workspace=apps/web -- cycle-52-source-contracts.test.ts settings-semantic-mode-action.test.ts i18n-key-parity.test.ts` - pass, 3 files / 6 tests.
  - `npm test --workspace=apps/web -- cycle-52-source-contracts.test.ts settings-semantic-mode-action.test.ts semantic-search-settings-ui.test.ts i18n-key-parity.test.ts` - pass, 4 files / 10 tests after extracting the pure UI-state helper.
- First full gate pass after the initial implementation:
  - `npm run lint --workspace=apps/web` - pass
  - `npm run lint:api-auth --workspace=apps/web` - pass
  - `npm run lint:action-origin --workspace=apps/web` - pass
  - `npm run lint:public-route-rate-limit --workspace=apps/web` - pass
  - `npm run typecheck --workspace=apps/web` - pass
  - `npm run build --workspace=apps/web` - pass (local sitemap generation fell back to homepage-only because MySQL was not running on `127.0.0.1:3306`; build exited 0)
  - `npm test --workspace=apps/web` - pass, 2713 passed / 4 skipped
- Second full gate pass after architect-verification fixes:
  - `npm run lint --workspace=apps/web` - pass
  - `npm run lint:api-auth --workspace=apps/web` - pass
  - `npm run lint:action-origin --workspace=apps/web` - pass
  - `npm run lint:public-route-rate-limit --workspace=apps/web` - pass
  - `npm run typecheck --workspace=apps/web` - pass
  - `npm run build --workspace=apps/web` - pass (same local sitemap DB fallback; build exited 0)
  - `npm test --workspace=apps/web` - pass, 2717 passed / 4 skipped

## Verification Notes

- Architect verification initially blocked because Cycle 53 plan artifacts were ignored and because the clear-path claim was covered only by source substrings. Fixes: `.gitignore` now explicitly unignores the Cycle 53 plan/deferred files; `semantic-search-settings-ui.ts` provides a pure tested helper for the stored-production select state and writable Disabled/Stub payload mapping.
