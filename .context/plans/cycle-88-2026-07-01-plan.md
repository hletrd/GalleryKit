# Cycle 88/100 Implementation Plan

Start HEAD: `afc2bf5245932fd421d84e8d29ca2e0be01280fb`.
Review aggregate: `.context/reviews/cycle-88-2026-07-01/_aggregate.md`.

## Goal

Close the safe Cycle 88 findings, keep review/plan indexes current, run all required gates, commit/push, deploy with `npm run deploy`, and smoke `https://gallery.atik.kr`.

## Scheduled Fixes

1. `C88-01`: close Cycle 87's release ledger by marking commit/pull-rebase/push and deploy complete, recording signed `afc2bf5245932fd421d84e8d29ca2e0be01280fb` / `origin/master` evidence, recording that Cycle 88 was started from the deployed `afc2bf5` baseline, and moving Cycle 87 out of the active plan index.
2. `C88-02`: tighten `apps/web/src/__tests__/failed-image-retry.test.ts` so retry-specific assertions inspect the extracted `retryFailedImage` function body, not the whole `actions/images.ts` file.
3. `C88-04`: keep Cycle 88 review/plan artifacts trackable by extending the existing `.gitignore` whitelist for the new plan and deferred files.

## Deferred This Cycle

`C88-03` is deferred in `.context/plans/cycle-88-2026-07-01-deferred.md` because the safe fix is a dedicated schema/data migration plan, not a narrow Cycle 88 patch.

Carry-forward deferred items remain in `.context/plans/cycle-88-2026-07-01-deferred.md`.

## Regression Coverage

- Ledger: Cycle 87 must no longer show unchecked release tasks after Prompt 3.
- Index: `.context/plans/README.md` must list Cycle 88 as active and Cycle 87 as recent with terminal state.
- Review aggregation: `.context/reviews/_aggregate.md` must point to the Cycle 88 aggregate.
- Retry payload test: `failed-image-retry.test.ts` must fail if `retryFailedImage()` drops processing-settings fields even when upload keeps them.

## Validation

Run required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `BASE_URL=https://gallery.atik.kr npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then signed commit, `git pull --rebase`, push, deploy with `npm run deploy`, and smoke:

- `curl -fsSIL https://gallery.atik.kr`
- `curl -fsS https://gallery.atik.kr/api/health`

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run required gates.
- [x] Commit, pull --rebase, push.
- [x] Deploy with `npm run deploy`.

## Focused Evidence

- `npm test --workspace=apps/web -- --run src/__tests__/failed-image-retry.test.ts` - pass: 1 file, 19 tests.

## Gate Evidence

- `npm run lint --workspace=apps/web` - pass.
- `npm run lint:api-auth --workspace=apps/web` - pass: both admin API routes OK.
- `npm run lint:action-origin --workspace=apps/web` - pass: all mutating server actions enforce same-origin provenance.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
- `npm run typecheck --workspace=apps/web` - pass.
- `BASE_URL=https://gallery.atik.kr npm run build --workspace=apps/web` - pass. Local sitemap generation logged the expected unavailable local MySQL fallback and exited successfully.
- `npm test --workspace=apps/web` - pass: 301 files passed, 2 skipped; 2838 tests passed, 4 skipped.

## Gate Fix Iterations

- `C88-02`: fixed one source-contract test gap by scoping retry action assertions to the extracted `retryFailedImage` body.

## Terminal Release Evidence

- Signed commit: `10cd16622c9c7d1d2b26dd45e9e6afe34b21b3e5` (`test(review): ✅ tighten cycle 88 retry contracts`) with a good GPG signature.
- Remote state: Cycle 89 started with clean `master...origin/master` at `10cd16622c9c7d1d2b26dd45e9e6afe34b21b3e5`.
- Deploy baseline: Cycle 89 invocation identified `10cd16622c9c7d1d2b26dd45e9e6afe34b21b3e5` as the current deployed master.
- Smoke before Cycle 89 edits: `curl -fsSIL https://gallery.atik.kr` returned HTTP 307 to `/en`; `curl -fsS https://gallery.atik.kr/api/health` returned `{"status":"ok"}`.
- Cycle 89 started from deployed `10cd166`.
