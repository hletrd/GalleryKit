# Cycle 10/100 Implementation Plan

Date: 2026-06-29
Source reviews: `.context/reviews/_aggregate.md` plus per-agent reports.
Status: TODO

## Scheduled Fixes

These findings are scheduled for Prompt 3. High-severity, correctness, privacy, and data-loss findings are not deferred.

| ID | Review finding | Files / surface | Planned fix | Status |
| --- | --- | --- | --- | --- |
| C10-01 | Public analytics rate limiting runs after DB lookups | `apps/web/src/app/actions/public.ts`, `public-actions.test.ts` | Charge a cheap pre-lookup per-IP budget before DB validation; update tests so exhausted valid-but-missing targets do not query DB. | TODO |
| C10-02 | Failed re-encode can delete previously good derivatives | `apps/web/src/lib/process-image.ts`, process-image tests | Track pre-existing final paths and only unlink invocation-created final files on failure; add regression with existing derivatives and late format failure. | TODO |
| C10-03 | Image delete cleanup masks unlink failures | `upload-paths.ts`, `process-image.ts`, `images.ts`, delete tests | Add strict cleanup variants that report non-`ENOENT` unlink/scan failures to delete actions; lock with mocked unlink failure tests. | TODO |
| C10-04 | Semantic bootstrap retries only one missing-embedding batch | `image-queue.ts`, embedding bootstrap tests | Drain or continue capped missing-embedding batches until no full batch remains; add a test for >50 missing rows. | TODO |
| C10-05 | Batch upload resolves the same tag set once per file | `images.ts`, upload action tests | Resolve unique tag records once per upload batch and reuse them for every image. | TODO |
| C10-09 | Stale semantic search requests are not aborted | `search.tsx`, semantic route/tests | Abort stale semantic fetches with `AbortController`; check `request.signal.aborted` before expensive route phases where possible. | TODO |
| C10-16 | Blocking-gate docs omit Playwright E2E | `AGENTS.md`, `apps/web/README.md` | Add `npm run test:e2e --workspace=apps/web` to short gate docs/script table. | TODO |
| C10-17 | Service-worker docs describe git-SHA stamping | `CLAUDE.md`, optional SW test | Update docs to template-hash plus pipeline-version stamping; add/adjust contract test if small. | TODO |
| C10-18 | Rate-limit convention docs describe stale semantic rollback | `rate-limit.ts`, semantic tests | Update comments to document charged expensive-work failures and no rollback after body/embedding/scan work begins. | TODO |
| C10-25 | Lightbox opens with focus on body | `lightbox.tsx`, focused UI test | Ensure initial focus lands on a lightbox control/dialog and restores on close. | TODO |
| C10-28 | Lightroom plugin marketed without artifact/setup path | token page/messages/API docs | Relabel UI/docs as upload API tokens or explicitly state plugin distribution is not included yet. | TODO |
| C10-29 | Google Analytics lacks public disclosure surface | `layout.tsx`, `footer.tsx`, messages/site config docs | Add a minimal privacy/analytics disclosure page/link when GA is configured, or explicit operator-facing disclosure docs. | TODO |
| C10-30 | GPS retention default is locked after first upload | upload/settings copy | Add a first-upload/upload-page warning when GPS retention is active; avoid changing existing install behavior. | TODO |
| C10-31 | Shared-group photo links lack action-oriented labels | shared-group page/components/tests | Align accessible labels with main gallery photo links. | TODO |
| C10-32 | Admin row actions use generic accessible names | admin row action components/tests | Include row-specific target names in accessible labels for repeated edit/delete actions. | TODO |
| C10-33 | Shared-group masonry lacks dimension guard | shared-group masonry/source tests | Reuse main gallery image dimension fallback/guard in shared-group cards. | TODO |

## Deferred Ledger

Deferred findings from this cycle are recorded in `.context/plans/cycle-10-2026-06-29-deferred.md`. Prompt 3 must not silently drop them; it should preserve the ledger and may update an item only if it is fixed.

## Verification Plan

Run every configured gate after implementation:

- `npm run lint --workspace=apps/web`
- `npm run lint:api-auth --workspace=apps/web`
- `npm run lint:action-origin --workspace=apps/web`
- `npm run lint:public-route-rate-limit --workspace=apps/web`
- `npm run typecheck --workspace=apps/web`
- `npm run build --workspace=apps/web`
- `npm test --workspace=apps/web`

Targeted tests should be added or updated for the fixed behavior before full gates.

## Commit / Deploy Plan

Use fine-grained signed commits with Conventional Commit + gitmoji. Pull with rebase before each push. After all scheduled fixes are committed, pushed, and gates are green, run `npm run deploy` once from repo root.
