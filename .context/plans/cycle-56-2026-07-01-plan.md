# Cycle 56/100 Implementation Plan

Start HEAD: `e82311b9`.
Review aggregate: `.context/reviews/cycle-56-2026-07-01/_aggregate.md`.

## Goal

Fix the actionable Cycle 56 findings, keep review/plan ledgers current, run all required gates, commit, push, and deploy with `npm run deploy` from the repo root.

## Scheduled Fixes

1. `C56-01`: make deploy env permission checks portable by preferring GNU `stat -c '%a'` before BSD `stat -f '%Lp'` in both deploy scripts.
2. `C56-02`: move upload-processing contract change detection server-side so unchanged `image_sizes` / `strip_gps_on_upload` payloads do not take active-upload or advisory-lock paths.
3. `C56-03`: close Cycle 55's release ledger with terminal commit/push/deploy evidence for `e82311b9`, and advance the plan/review pointers to Cycle 56.
4. `C56-04`: replace the brittle `image_sizes` source-slice test with behavior coverage for changed and unchanged server-action payloads.
5. `C56-05`: add execution-level deploy-script tests proving unsafe env permissions exit before source/Docker side effects.
6. `C56-06`: add an admin-aware photo viewer data fetch that selects admin audit fields only after authentication; keep public metadata and OG paths on public data.
7. `C56-07`: correct the app README alt-text wording to title/description fields.

## Deferred This Cycle

No new Cycle 56 findings are deferred. Carry-forward items are recorded in `.context/plans/cycle-56-2026-07-01-deferred.md`.

## Regression Coverage

- Extend deploy script contract tests for GNU-first `stat` ordering and execution-level unsafe-permission exits.
- Extend Settings action tests for semantically unchanged contract keys under active upload claims and changed `image_sizes` with existing images.
- Add source contract coverage that the photo page uses admin-aware viewer fetches while metadata/OG remain public-shaped.

## Validation

Run required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then sign, `git pull --rebase`, push, and deploy with `npm run deploy`.

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run required gates.
- [x] Commit, pull --rebase, push.
- [x] Deploy with `npm run deploy`.

## Gate Evidence

- `npm run lint --workspace=apps/web` - pass.
- `npm run lint:api-auth --workspace=apps/web` - pass.
- `npm run lint:action-origin --workspace=apps/web` - pass.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
- `npm run typecheck --workspace=apps/web` - pass.
- `npm run build --workspace=apps/web` - pass. Local sitemap generation logged the expected unavailable local MySQL fallback and exited successfully.
- `npm test --workspace=apps/web` - pass: 285 files passed, 2 skipped; 2728 tests passed, 4 skipped.
- Focused regression gate: `npm test --workspace=apps/web -- settings-semantic-mode-action.test.ts settings-image-sizes-lock.test.ts deploy-script-contract.test.ts cycle-56-source-contracts.test.ts photo-og-metadata.test.ts` - pass: 5 files, 29 tests.

## Gate Fix Iterations

- 1: tracked-secret fixture rejected a deploy-permission test sentinel; replaced the literal `SESSION_SECRET` fixture with a placeholder built from split strings, then reran focused tracked-secret coverage successfully.

## Release Evidence

- Implementation commit: `30dad6a8` (`fix(cycle-56): 🐛 prevent deploy and audit regressions`), signed.
- Follow-up deploy recovery commit: `677a8410` (`fix(deploy): 🐛 fall back on empty stat mode probes`), signed.
- Push: `HEAD`, `origin/master`, and `origin/HEAD` all pointed at `677a8410` at the start of Cycle 57.
- Deploy: Cycle 57 was invoked with `677a8410` identified as the current deployed `master` HEAD at start.
