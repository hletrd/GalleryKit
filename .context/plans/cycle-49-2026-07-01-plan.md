# Cycle 49/100 Implementation Plan

Start HEAD: `dc4f4acf`.
Review aggregate: `.context/reviews/cycle-49-2026-07-01/_aggregate.md`.

## Goal

Fix the actionable Cycle 49 review findings, keep review/plan ledgers current, run all required gates, commit, push, and deploy with `npm run deploy` from the repo root.

## Scheduled Fixes

1. `C49-01`: restore offline HTML fallback eligibility for normal public photo pages by removing `/p/<id>` from the revocable/share route classifier, updating the service-worker contract test, and regenerating `sw.js`.
2. `C49-02`: serialize `deleteTopic` with topic create/update/alias mutations by wrapping the delete transaction in `withTopicRouteMutationLock`; add a regression test.
3. `C49-03`: close stale Cycle 48 plan/index/aggregate state using the Cycle 49 start condition that `dc4f4acf` was the current deployed `master` HEAD.
4. `C49-04`: update action-origin gate docs to reflect that `auth.ts` is scanned with the approved `hasTrustedSameOrigin` guard shape.
5. `C49-05`: document `chmod 600 .env.deploy` in the remote deploy setup path and example comments.
6. `C49-06`: clarify force-show color chip copy in English and Korean so public P3 chip behavior is distinct from admin-only HDR audit badges.

## Deferred This Cycle

No new Cycle 49 findings are deferred. Carry-forward items are recorded in `.context/plans/cycle-49-2026-07-01-deferred.md`.

## Regression Coverage

- `apps/web/src/__tests__/sw-template-contract.test.ts` must prove `/p/<id>` is not classified as revocable/share HTML while `/s`, `/g`, `/c`, and `/map` still bypass the offline HTML cache.
- `apps/web/src/__tests__/topics-actions.test.ts` must prove `deleteTopic` acquires and releases `LOCK_TOPIC_ROUTE_SEGMENTS` around the delete transaction.

## Validation

Run required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then sign, `git pull --rebase`, push, and deploy with `npm run deploy`.

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run required gates.
- [ ] Commit, pull --rebase, push.
- [ ] Deploy with `npm run deploy`.

## Gate Evidence

- `npm run lint --workspace=apps/web` - pass
- `npm run lint:api-auth --workspace=apps/web` - pass
- `npm run lint:action-origin --workspace=apps/web` - pass
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass
- `npm run typecheck --workspace=apps/web` - pass
- `npm run build --workspace=apps/web` - pass (local sitemap generation fell back to homepage-only because MySQL was not running on `127.0.0.1:3306`; build exited 0)
- `npm test --workspace=apps/web` - pass, 2708 passed / 4 skipped
- Focused pre-gate checks: `sw-template-contract.test.ts` pass, 25 tests; `topics-actions.test.ts` pass, 21 tests.
