# Cycle 98/100 Implementation Plan

Starting deployed HEAD: `6f40f66d9a6949ea866966230e5fe0ba61024637`.

Review aggregate: `.context/reviews/cycle-98-2026-07-01/_aggregate.md`.

## Scheduled Safe Narrow Fixes

- [x] `C98-01` Public select privacy guard does not pin the exact public allowlist - add an explicit `PUBLIC_SAFE_KEYS` fixture and equality assertion so public field additions require intentional review.
- [x] `C98-02` i18n duplicate-key test cannot detect duplicate JSON keys after parsed imports collapse duplicates - scan raw locale JSON source for duplicate object keys before parsed parity checks.
- [x] `C98-03` Cycle 97 terminal ledger still says commit/push/deploy/smoke are pending - update Cycle 97 completion evidence, latest aggregate pointer, and plan index for deployed signed HEAD `6f40f66d9a6949ea866966230e5fe0ba61024637`.

## Implementation Notes

- No schema migration, dependency, runtime behavior change, broad refactor, production config change, or destructive operation.
- Runtime source change is limited to correcting the `publicSelectFields` privacy comment to match the rest-spread contract; behavior is unchanged.
- The restore foreground mutation fence was re-confirmed but remains the existing broad carry-forward item in `.context/plans/cycle-96-2026-07-01-deferred.md`.

## Required Gates

- [x] `npm run lint --workspace=apps/web` - passed.
- [x] `npm run lint:api-auth --workspace=apps/web` - passed; both scanned admin API routes OK.
- [x] `npm run lint:action-origin --workspace=apps/web` - passed; all mutating server actions guarded or explicitly exempt.
- [x] `npm run lint:public-route-rate-limit --workspace=apps/web` - passed; all scanned public routes OK.
- [x] `npm run typecheck --workspace=apps/web` - passed; Next route types generated, app and script typechecks clean.
- [x] `BASE_URL=https://gallery.atik.kr npm run build --workspace=apps/web` - passed; local DB unavailable, sitemap used the documented homepage-only fallback during static generation.
- [x] `npm test --workspace=apps/web` - passed, 303 files / 2852 tests; 2 files / 4 tests skipped.
- [x] Focused tests: `npm test --workspace=apps/web -- privacy-fields.test.ts i18n-key-parity.test.ts` - passed, 2 files / 12 tests.
- [x] `git diff --cached --check` - passed.

## Progress

- Prompt 1 review fan-out completed; aggregate written.
- Prompt 2 plan/deferred files written.
- Prompt 3 safe narrow implementation completed for all three scheduled findings.

## Completion Evidence

- Focused regression tests and full required gates through unit tests passed.
- `git diff --cached --check` passed.
- Pending signed commit, pull/rebase, push, deploy, and production smoke.
