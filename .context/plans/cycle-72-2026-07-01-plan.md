# Cycle 72/100 Implementation Plan

Start HEAD: `363dc1c9715c9e04ee07997c0e321c7d7815f6b1`.
Review aggregate: `.context/reviews/cycle-72-2026-07-01/_aggregate.md`.

## Goal

Fix the safe, reviewable Cycle 72 findings, keep review/plan ledgers current, run all required gates, commit, push, and deploy with `npm run deploy` from the repo root.

## Scheduled Fixes

1. `C72-01`: make durable restore-maintenance marker reads fail closed for all non-`ENOENT` read/stat errors and add regression coverage.
2. `C72-02`: add a sidecar derivative-write guard to `processImageFormats()` and wire the color sidecar through it so maintenance appearing mid-encode aborts and rolls back already-swapped derivatives before DB state diverges.
3. `C72-03`: use a short/non-cacheable cache policy for temporary per-photo OG fallback redirects when sized derivatives are unavailable, and add route/source coverage.
4. `C72-05`: add subprocess behavior tests for the shipped `restore-maintenance-recovery.mjs` command: inactive/active status, refused clear without confirmation, and confirmed clear.
5. `C72-07`: make settings invalid-field focus respect `prefers-reduced-motion` and update the source contract.
6. `C72-08`: suppress swipe haptics when `shouldReduceMotion` is true and add source/behavior coverage.
7. `C72-09`: close Cycle 71 ledger state and advance `.context/plans/README.md` to Cycle 72.

## Deferred This Cycle

New deferred findings are recorded in `.context/plans/cycle-72-2026-07-01-deferred.md`:

- `C72-04`: feed conditional route-level behavioral coverage.
- `C72-06`: Firefox/WebKit display-capability smoke coverage.

Carry-forward deferred items remain unchanged and are repeated in the deferred file.

## Regression Coverage

- `restore-maintenance.test.ts`: non-`ENOENT` marker read failures fail closed.
- New or extended process-image/sidecar contracts: `processImageFormats()` accepts and calls a derivative write guard before final-path swaps; the color sidecar passes the durable restore assertion.
- `restore-maintenance-recovery` tests: subprocess coverage for the shipped `.mjs`.
- OG photo fallback tests: derivative-miss fallback redirects do not use the long success cache policy.
- Cycle 21/source contracts: settings validation reduced-motion branch and swipe haptic guard.

## Validation

Run required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then sign, `git pull --rebase`, push, and deploy with `npm run deploy`.

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run required gates.
- [x] Commit, pull --rebase, push.
- [x] Deploy with `npm run deploy`.

## Gate Evidence

- Focused regression: `npm test --workspace=apps/web -- --run src/__tests__/restore-maintenance.test.ts src/__tests__/restore-maintenance-recovery-mjs.test.ts src/__tests__/cycle-72-source-contracts.test.ts src/__tests__/cycle-21-source-contracts.test.ts src/__tests__/og-photo-fallback.test.ts` - pass: 5 files passed, 42 tests passed. First focused run found two test/code-contract issues; fixed before required gates.
- `npm run lint --workspace=apps/web` - pass.
- `npm run lint:api-auth --workspace=apps/web` - pass: both admin API routes OK.
- `npm run lint:action-origin --workspace=apps/web` - pass: all mutating server actions enforce same-origin provenance.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
- `npm run typecheck --workspace=apps/web` - pass.
- `npm run build --workspace=apps/web` - pass. Local sitemap generation logged the expected unavailable local MySQL fallback and exited successfully.
- `npm test --workspace=apps/web` - pass: 297 files passed, 2 skipped; 2788 tests passed, 4 skipped.
- Terminal evidence: Cycle 73 started from current deployed `master` HEAD `96459b7a264c4e0110d15bb76840d260447335e8`; local `HEAD`, `origin/master`, and `origin/HEAD` matched that commit at Cycle 73 start.

## Gate Fix Iterations

1.
