# Cycle 67/100 Implementation Plan

Start HEAD: `3e8ab924b5ed714f8a0f1dbfe1f9739d6fe25886` at cycle start.
Review aggregate: `.context/reviews/cycle-67-2026-07-01/_aggregate.md`.

## Goal

Fix the safe, reviewable Cycle 67 findings, keep review/plan ledgers current, run all required gates, commit, push, and deploy with `npm run deploy` from the repo root.

## Scheduled Fixes

1. `C67-01`: derive the Settings backfill-warning key set from a client-safe derivative-byte-impacting setting list, excluding upload-admission settings such as `allow_hdr_ingest`.
2. `C67-02`: log the CLIP sidecar scan-limit rerun notice when a non-`BATCH_SIZE`-aligned batch exhausts `SEMANTIC_SCAN_LIMIT`.
3. `C67-03`: ignore repeated lightbox shortcut keydown events before toggling slideshow or navigating.
4. `C67-04`: extract/test a pure Settings backfill-warning comparison helper for blank baseline/default equivalence.
5. `C67-05`: make the Similar Photos abort source contract whitespace-tolerant.
6. `C67-06`: close Cycle 66 terminal ledger and update active plan/review pointers to Cycle 67.

## Deferred This Cycle

No new Cycle 67 findings are deferred.

Carry-forward items remain in `.context/plans/cycle-67-2026-07-01-deferred.md`.

## Regression Coverage

- Add focused tests for the Settings backfill-warning helper: byte-impacting key membership, `allow_hdr_ingest` exclusion, `image_sizes` warning exclusion, and blank/default equivalence.
- Extend lightbox source contracts to require an early `e.repeat` guard.
- Extend CLIP sidecar source contracts to require scan-limit notice logging before the short-page break.
- Replace the exact Similar Photos fetch string check with whitespace-tolerant signal wiring.
- Documentation/ledger changes are verified by review plus the full gate sequence.

## Validation

Run required gates in order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then sign, `git pull --rebase`, push, and deploy with `npm run deploy`.

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run required gates.
- [ ] Commit, pull --rebase, push.
- [ ] Deploy with `npm run deploy`.

## Gate Evidence

- Focused regression: `npm test --workspace=apps/web -- settings-backfill-warning settings-backfill-warning-source similar-photos-abort-source lightbox-controls-contract cycle-6-source-contracts settings-hash` - pass: 6 files passed, 37 tests passed.
- `npm run lint --workspace=apps/web` - pass.
- `npm run lint:api-auth --workspace=apps/web` - pass.
- `npm run lint:action-origin --workspace=apps/web` - pass.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass.
- `npm run typecheck --workspace=apps/web` - pass.
- `npm run build --workspace=apps/web` - pass. Local sitemap generation logged the expected unavailable local MySQL fallback and exited successfully.
- `npm test --workspace=apps/web` - pass: 294 files passed, 2 skipped; 2758 tests passed, 4 skipped.

## Gate Fix Iterations

0.
