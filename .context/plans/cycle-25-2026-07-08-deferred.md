# Run-10 Cycle 25/100 Deferred Findings

Status: OPEN
Aggregate: `.context/reviews/_aggregate.md`
Date: 2026-07-08 KST

This register records every Cycle 25 aggregate finding not scheduled in `cycle-25-2026-07-08-plan.md`. Severity and confidence are preserved from the aggregate. Deferred work remains bound by repo policy: GPG-signed Conventional Commits with gitmoji, `git pull --rebase` before push, required gates, no force-push, no `--no-verify`, and the repo's Node/Next/React/TypeScript toolchain rules.

Repo rule basis for deferral: `.context/plans/README.md` defines carry-forward budgeting, `.context/plans/deferred-carry-forward.md` is the committed open-deferred register, and `CLAUDE.md` documents the single-web-instance topology, process-local state boundaries, host-nginx operator boundary, DB-only backup/restore boundary, large multipart RSS measurement boundary, current Atik deployment config boundary, build-time config behavior, and permanently deferred 2FA/WebAuthn posture. This register does not defer the contained High correctness finding `AGG-C25-01`, the contained restore cleanup leak `AGG-C25-02`, the admin form recovery issue `AGG-C25-21`, or the narrow mobile semantic-search affordance issue `AGG-C25-23`; those are scheduled in the Cycle 25 plan.

## Deferred Items

| ID | Severity / Confidence | Citation | Reason for deferral | Exit criterion |
|----|-----------------------|----------|---------------------|----------------|
| AGG-C25-03 | High / High | `apps/web/next.config.ts:111-119`; `apps/web/src/app/actions/images.ts:87-106`, `154-221`; `apps/web/src/app/[locale]/admin/db-actions.ts:421-427`, `745-767`; `apps/web/src/app/api/admin/lr/upload/route.ts:101-187` | Streaming multipart ingress crosses browser upload, restore import, framework request parsing, temp-file handoff, and memory/load validation. Repo-rule quote: `CLAUDE.md` Important Notes says "multipart uploads are buffered on the heap by the framework... (run-10 cycle-1 C1-33, pending an on-host RSS measurement)." Prior registers preserve this as broad architecture work until measurement or an approved streaming-ingress project. | Production RSS/OOM incident during upload/restore, on-host RSS trace proving breach, or approved streaming-ingress project. |
| AGG-C25-04 | High / High source shape, Medium-High impact | `apps/web/src/db/index.ts:31-42`; `apps/web/src/lib/image-queue.ts:120-153`, `447-456`; `apps/web/src/lib/admin-backfill-runner.ts:97-143`, `324-379`, `716-727`; `apps/web/src/lib/background-db-writes.ts:3-75`; `apps/web/src/lib/clip-model.ts:53-72`, `156-173`; `CLAUDE.md:269-284` | Shared DB/CPU background-budget work changes queue/backfill/search/analytics semantics. Repo-rule basis: `CLAUDE.md` documents the shipped single-process pool budget and known mutual over-subscription as a sizing boundary. | Measured pool starvation during queue/backfill/search, or shared background-budget project approved. |
| AGG-C25-05 | High / High | `apps/web/src/app/actions/images.ts:87-221`, `325-516`; `apps/web/src/app/api/admin/lr/upload/route.ts:84-188`, `254-381`, `383-613` | Shared browser/PAT ingest extraction is a broad architecture refactor across two hot upload paths. No current parity divergence was confirmed; the contained color re-encode correctness issue is scheduled first. | Before adding upload-time privacy/metadata/processing fields, or the next confirmed browser/PAT drift. |
| AGG-C25-06 | Medium-High / High | `CLAUDE.md:244-249`; `apps/web/src/lib/single-writer-guard.ts:7-16`, `218-235`; `apps/web/src/instrumentation.ts:22-31` | This correctness/topology issue is deferred only because repo rules explicitly define the current guard as non-fatal. Repo-rule quote: `CLAUDE.md` states "The guard is warn-only and never blocks startup or crashes on DB errors" and documents the shipped single-web-instance topology. Changing to fail-closed affects rolling deploy/operator behavior and needs product/operator approval. | Scale-out is attempted, singleton contention incident occurs, or a fail-closed/shared-state topology project is approved. |
| AGG-C25-07 | Medium / High | `apps/web/src/lib/storage/index.ts:4-12`; `apps/web/src/lib/upload-paths.ts:12-47`, `59-88`; `apps/web/src/lib/serve-upload.ts:198-229`; `apps/web/src/__tests__/storage-quarantine.test.ts:1-27`, `111-132` | Storage abstraction product decision is broad. Repo-rule quote: `CLAUDE.md` says the `@/lib/storage` module exists but "the product currently supports local filesystem storage only" and not to expose backend switching until the pipeline is wired end-to-end. | Storage-backend support is approved, storage abstraction causes maintainer confusion, or next storage/upload architecture cycle. |
| AGG-C25-08 | Medium / High | `apps/web/src/lib/clip-embeddings.ts:36-48`; `apps/web/src/app/api/search/semantic/route.ts:263-311`; `apps/web/src/app/api/search/similar/[id]/route.ts:177-214`; `apps/web/src/db/schema.ts:314-326` | Bounded residual risk behind semantic mode gates/scan limits. Vector indexing, worker-backed scorer, ANN service, or cache redesign is larger than this cycle. | Semantic traffic/gallery size exceeds scan limits, measured CPU/RSS pressure, or vector-index/cache project approved. |
| AGG-C25-09 | Medium / High | `apps/web/src/lib/data.ts:1766-1816`; `apps/web/src/app/[locale]/(public)/map/page.tsx:42-66`, `89-110`, `98-110`; `apps/web/src/components/map/map-client.tsx:77-95`, `120-141` | Map clustering/viewport APIs and spatial indexing are product/performance scope. Current cap and rate limits bound worst-case work; no large-corpus browser trace was collected. | GPS-visible rows approach thousands, mobile map trace shows jank, or map clustering/viewport project approved. |
| AGG-C25-10 | Medium / High | `apps/web/src/app/actions/public.ts:247-329`; `apps/web/src/lib/data.ts:1574-1584`, `1637-1655`, `1693-1701`, `1716-1738`; `apps/web/src/db/schema.ts:123-132` | Search-index redesign is product/performance work. Existing request counts are rate-limited; no live latency evidence was gathered this cycle. | Search latency rises, corpus grows materially, or full-text/search-index cycle approved. |
| AGG-C25-11 | Low-Medium / High | `apps/web/src/lib/data-timeline.ts:103-110`, `121-131`; `apps/web/src/db/schema.ts:123-132` | Generated month/day columns require a schema/performance migration. No measured home latency or corpus-growth evidence was collected this cycle. | Measured home latency, corpus growth makes the scan costly, or next schema/index cycle folds generated month/day columns. |
| AGG-C25-12 | Low-Medium / High | `apps/web/src/app/[locale]/admin/db-actions.ts:71-76`, `94-109`, `111-145` | Bounded admin-only memory risk. Streaming export is a route/API redesign and no production heap issue was collected. | CSV export heap/latency incident, very large export fields observed, or approved streaming-export project. |
| AGG-C25-13 | Low / High | `apps/web/public/sw.template.js:31-39`, `147-164`, `446-480`, `555-563` | Bounded service-worker maintenance cost; correctness/privacy risks are guarded. This should ride a PWA/cache cycle or measured trace. | Navigation stutter trace implicates HTML eviction, or next service-worker cache redesign cycle. |
| AGG-C25-14 | Low-Medium / Medium | `apps/web/src/app/actions.ts:1-34` | Server-action barrel retirement is maintainability/layering work. The existing security lint gates recognize the barrel and no guard bypass was found. | New imports from `@/app/actions` spread, dependency-shape confusion causes review misses, or action-boundary cleanup cycle approved. |
| AGG-C25-15 | Medium / High | `apps/web/src/__tests__/migrate-reconcile-coverage.test.ts:13-19`, `86-101`, `157-171`; `apps/web/scripts/migrate.js:877-897` | Disposable-MySQL parity harness is broader test infrastructure. Existing source tripwires remain useful; no current schema drift was confirmed. | Next migration/schema cycle, fresh-vs-migrated schema drift incident, or DB-backed test infra approved. |
| AGG-C25-16 | Medium / High | `apps/web/src/__tests__/bottom-sheet-dropdown-portal.test.ts:14-26`; `apps/web/src/components/info-bottom-sheet.tsx:565-603`; `apps/web/e2e/test-fixes.spec.ts:56-65`; `apps/web/e2e/focus-restore.spec.ts:34-59` | Browser-flow coverage is a test-infra/UI fixture task. No current mobile dropdown break was reproduced; source contract remains in place. | Radix/sheet/dropdown changes, mobile info-sheet regression, or mobile browser-flow coverage cycle. |
| AGG-C25-17 | Medium / High | `apps/web/playwright.config.ts:72-77`; `.github/workflows/quality.yml:75-80`; touch/mobile policy in `AGENTS.md` and `CLAUDE.md` | Browser matrix expansion affects CI runtime and fixture stability. This is test-strategy scope, not a contained runtime defect. | Browser-specific regression, PWA/SW incident, visual regression budget approved, or test-matrix project approved. |
| AGG-C25-18 | Low / High | `apps/web/e2e/nav-visual-check.spec.ts:40-87` | Visual baseline adoption can introduce noisy artifacts and needs a stable visual-regression policy. Geometry checks still provide value. | Visual-regression budget approved, nav visual regression escapes geometry checks, or spec rename/visual-baseline cleanup cycle. |
| AGG-C25-19 | Low-Medium / High | `apps/web/src/__tests__/clip-offline-load.test.ts:15-18`, `32-41`; `apps/web/src/__tests__/clip-semantic-integration.test.ts:8-10`, `30-31`; `.github/workflows/clip-preflight.yml:3-6`; `.github/workflows/quality.yml:54-83` | Real CLIP preflight requires model weights and CI/runtime budget. Existing workflow is scheduled/manual; no CLIP-touching source change is scheduled this cycle. | CLIP/semantic production files change, semantic production incident, or required preflight policy approved. |
| AGG-C25-20 | Low-Medium / High | root `package.json:17-29`; `apps/web/package.json:13-29`; `apps/web/vitest.config.ts:16-39`; `.github/workflows/quality.yml:54-83` | Coverage instrumentation/thresholds are test-strategy scope and can create noisy churn without an agreed ratchet. | Coverage reporting project approved, repeated missed branch regression, or changed-file coverage policy adopted. |
| AGG-C25-22 | Medium / High | `apps/web/src/site-config.json:2-10`; `apps/web/src/site-config.example.json:2-11`; `apps/web/scripts/ensure-site-config.mjs:11-42`; `README.md:60-77`, `118-122`, `171-172`, `198-200`; `apps/web/README.md:15-20`, `48-50` | This checkout is the configured production deployment for `gallery.atik.kr`, and the user explicitly chose that target for this loop. Changing committed site-config semantics would be a distribution/product decision and may affect the current deploy target. | Before distributing GalleryKit as a template/package, or if copied installs publish wrong canonical URLs. |
| AGG-C25-24 | Medium / Medium | `apps/web/src/app/[locale]/admin/db-actions.ts:745-900`; `apps/web/src/lib/sql-restore-scan.ts:88-156`, `262-341` | No restore-scanner bypass was found and targeted security tests passed. This is an admin-trust/manual-validation risk; stronger isolation/signed backups is a larger restore architecture project. | Restore scanner bypass found, restore-threat model changes, or signed/isolated restore project approved. |
| AGG-C25-25 | Low-Medium / High | `apps/web/.env.local.example:21-33`, `57-70`; `.env.deploy.example:1-16`; `scripts/deploy-remote.sh:55-80`; `apps/web/deploy.sh:15-43`; `apps/web/src/lib/session.ts:16-35` | Live gitignored secret provenance cannot be proven from source review. This is an operator validation/rotation boundary, not an editable repo defect. | Secret provenance audit, rotation requirement, or evidence that live env was seeded from historical checked-in examples. |
| AGG-C25-26 | Low-Medium / High | `apps/web/src/app/[locale]/admin/db-actions.ts:189-195`, `229-244`; `apps/web/src/app/api/admin/db/download/route.ts:21-31`, `45-67` | Repo-rule quote: `CLAUDE.md` documents plaintext SQL backups as a host/storage encryption boundary and restore as DB-only rather than filesystem rollback. Backup encryption requires key-management and restore-flow decisions. | Backup encryption requirement, filesystem rollback product decision, or restore/file integrity incident. |
| AGG-C25-27 | High if proxy topology is wrong; Low-Medium to Medium source confidence / Medium | `apps/web/nginx/default.conf:1-29`, `20-29`, `52-72`, `246-311`; `apps/web/docker-compose.yml:15-23`; `apps/web/deploy.sh:51-77`; `scripts/deploy-remote.sh:31-53`, `87-93`; `apps/web/src/lib/rate-limit.ts:175-217`; `CLAUDE.md:447-449` | Operator/live-host validation. Repo-rule quote: `CLAUDE.md` says "Deploys do NOT touch host nginx" and not to mark nginx-config findings closed on commit alone. | Operator applies/verifies nginx zones and real-IP topology, proxy topology changes, or app-layer public limiter project approved. |
| AGG-C25-28 | Medium / Medium | `apps/web/next.config.ts:32-40`, `121-125`; `apps/web/Dockerfile:91-120`; `apps/web/docker-compose.yml:4-32` | Operator/runtime boundary already documented; fixing requires deployment validation design. No restart-only incident was observed this cycle. | Restart-without-rebuild thumbnail/site-config incident, or deploy validation project for baked-vs-runtime config. |
| AGG-C25-29 | Medium / High | `apps/web/src/app/[locale]/admin/db-actions.ts:545-695`; `apps/web/src/lib/image-queue.ts:1285-1338`; `apps/web/scripts/restore-maintenance-recovery.mjs:55-85`; `apps/web/src/instrumentation.ts:1-10` | Operational recovery risk is explicitly documented. Repo-rule quote: `CLAUDE.md` says if the recovery command is run from a separate shell/process after live web entered maintenance, "restart/redeploy `gallerykit-web` after the clear" so process-local maintenance resets. | Restore recovery incident, operator confusion report, or diagnostics project exposing durable and process-local maintenance states. |
| AGG-C25-30 | Low now, Medium if promoted to product/audit state / High | `apps/web/src/app/actions/public.ts:435-470`; `apps/web/src/lib/background-db-writes.ts:3-75`; `CLAUDE.md:249` | Repo-rule quote: `CLAUDE.md` documents shared-group view counts as best-effort approximate analytics and lost on crash/SIGKILL. No product copy change this cycle promotes them to billing/audit state. | Analytics become billing/audit/ranking-critical, undercount incident occurs, or durable analytics queue project approved. |

## Scheduled, Not Deferred

Scheduled in `cycle-25-2026-07-08-plan.md`: `AGG-C25-01`, `AGG-C25-02`, `AGG-C25-21`, `AGG-C25-23`.

## Age-Budget Check

Newly deferred High-severity Cycle 25 rows (`AGG-C25-03`, `AGG-C25-04`, `AGG-C25-05`, conditional `AGG-C25-27`) are broad architecture/performance/operator items with explicit exit criteria and prior carry-forward lineage. The contained High correctness issue (`AGG-C25-01`) is scheduled. `AGG-C25-06` has correctness implications but is deferred only under the repo's explicit warn-only single-writer guard rule quoted above; changing it requires operator/product approval.
