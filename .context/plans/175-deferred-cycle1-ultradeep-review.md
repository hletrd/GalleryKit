# Deferred Review Coverage — Cycle 1 Ultradeep

**Created:** 2026-04-22
**Status:** TODO
**Purpose:** Preserve cycle-1 ultradeep findings that are broader architectural, operational, or lower-priority follow-up work outside this cycle’s bounded implementation lane.

## Deferred / follow-up items

| Finding ID | Citation / source | Original severity / confidence | Reason for deferral | Exit criterion |
| --- | --- | --- | --- | --- |
| A1 | `.context/reviews/architect.md` | MEDIUM / High | Public-photo auth/cache decoupling is real architectural debt, but extracting admin capability checks out of the public route touches page/render contracts, `PhotoViewer`, and auth composition beyond this cycle’s tighter correctness lane. | Re-open when a dedicated auth-boundary / caching cleanup cycle is scheduled. |
| A4 | `.context/reviews/architect.md` | MEDIUM / High | Extracting auth primitives out of `app/actions/auth.ts` is a worthwhile boundary repair, but it is a cross-cutting refactor affecting pages, layouts, API wrappers, and mutation modules. | Re-open when an auth-layer refactor cycle is approved. |
| A5 | `.context/reviews/architect.md` | MEDIUM / High | Splitting `lib/data.ts` by bounded concern is structural cleanup, not a narrow bug fix. The current cycle targets concrete defects first. | Re-open when a repo-wide architecture / simplification pass is scheduled. |
| R1 | `.context/reviews/architect.md`, `.context/reviews/security-reviewer.md` | HIGH / Medium | Restore maintenance mode is broader operational/orchestration design than this cycle’s bounded code fixes. Existing repo context already tracks restore-boundary follow-up. | Re-open when restore workflow changes or when multi-actor maintenance semantics are prioritized. |
| R2 | `.context/reviews/architect.md`, `.context/reviews/verifier.md` | MEDIUM / High | Shared-group view counts are intentionally approximate/in-process today. Making them durable is a bigger design change than fits this cycle. | Re-open when analytics durability or multi-instance correctness becomes a requirement. |
| R3 | `.context/reviews/architect.md` | MEDIUM / High | Upload quota persistence remains a shared-store architecture decision, not a narrow defect fix. | Re-open when multi-instance/restart-resilient upload quotas are required. |
| R4 | `.context/reviews/architect.md`, `.context/reviews/critic.md` | MEDIUM / Medium | The dormant storage abstraction is broader future-facing architecture debt; this cycle keeps the current self-hosted local-filesystem model explicit instead of finishing or deleting the subsystem. | Re-open when remote storage support becomes an active product requirement. |
| C-OG | `.context/reviews/critic.md`, `.context/reviews/architect.md` | MEDIUM / High | Topic OG branding/label drift is user-facing and valid, but it is secondary to the more critical auth/delete/private-original fixes in this cycle. | Re-open when metadata/SEO polish is next in the queue. |
| C-VERIFY | `.context/reviews/critic.md`, `.context/reviews/test-engineer.md` | MEDIUM / High | Unifying lint + unit + seeded smoke E2E into one default verification lane is valuable but broader CI/test ergonomics work than the current bug-fix scope. | Re-open when test-pipeline consolidation is prioritized. |
| C-SITEMAP | `.context/reviews/critic.md` | MEDIUM / Medium | Sitemap sharding beyond 24k images is a scalability improvement, not a current correctness regression in the observed app behavior. | Re-open when gallery size approaches the cap or SEO scaling work is scheduled. |
| C-LIVENESS | `.context/reviews/critic.md` | MEDIUM / Medium | Splitting liveness/readiness probes is an operational improvement, but it changes deployment semantics beyond this cycle’s product-fix lane. | Re-open when deployment/orchestration hardening is scheduled. |
| D2 | `.context/reviews/debugger.md` | MEDIUM / High | Durable retry/reporting for post-commit file-cleanup failures is valid, but this cycle first fixes stale-success delete accounting; durable orphan-cleanup orchestration is a broader follow-up. | Re-open when filesystem cleanup is moved to a durable worker/job model. |
| D3 | `.context/reviews/debugger.md` | MEDIUM / Medium-High | Successful-login cleanup of the account bucket is best-effort today. Tightening it is worthwhile, but secondary after fixing the more fundamental overlength-key bug. | Re-open if stale `login_account` lockouts are observed after the bucket-key fix. |
| D4 | `.context/reviews/debugger.md` | LOW / High | Moving pre-increment rate limits below cheap validation in admin/share flows is a nice correctness polish, but lower priority than the higher-severity cycle items. | Re-open when rate-limit UX hardening is revisited. |
| UX-01 | `.context/reviews/designer.md` | HIGH / High | The `TagInput` Tab-trap is a real accessibility bug, but the cycle already includes multiple higher-blast-radius auth/data/operational fixes. | Re-open in the next focused accessibility/interaction pass. |
| UX-02 | `.context/reviews/designer.md` | MEDIUM / Medium-High | Bottom-sheet modal semantics need a careful state-model redesign, not a trivial tweak. | Re-open when mobile photo-viewer UX is revisited. |
| UX-03 | `.context/reviews/designer.md` | MEDIUM / Medium | Infinite-scroll fallback button is a useful resilience enhancement but not a correctness blocker in the current app. | Re-open when gallery pagination UX is next touched. |
| UX-04 | `.context/reviews/designer.md` | LOW / Medium | Route-specific loading/error polish is lower-priority UX refinement. | Re-open when public/admin visual polish is revisited. |
| SEC-OPS | `.context/reviews/security-reviewer.md` | HIGH / High | Live secrets in ignored workspace files (`apps/web/.env.local`, `.env.deploy`) are an operational issue outside tracked source. This cycle can add helper/docs/example scaffolding, but it cannot safely rotate the operator’s real secrets from committed code. Severity preserved. | Re-open immediately if workspace secrets are shared/exported, or when operator rotation/external secret-store migration is performed. |
| SEC-CSP | `.context/reviews/security-reviewer.md` | MEDIUM / Medium | Removing `'unsafe-inline'` from the production CSP requires reworking the analytics bootstrap path and validating all script-loading assumptions. | Re-open when CSP hardening/analytics integration work is scheduled. |
| SEC-SHARE | `.context/reviews/security-reviewer.md` | MEDIUM / Medium | Public share-link brute-force hardening (legacy short-key retirement + lookup throttling) is valid but broader than this cycle’s bounded fixes. | Re-open when share-link policy hardening is prioritized. |
| SEC-HEALTH | `.context/reviews/security-reviewer.md` | LOW / High | Health endpoint disclosure is low-risk recon and secondary to this cycle’s higher-impact fixes. | Re-open if external monitoring architecture changes or health-endpoint exposure becomes sensitive. |
| TEST-GAPS | `.context/reviews/test-engineer.md` | HIGH / High | The broad test-surface gaps are real, but this cycle cannot add exhaustive coverage across auth, uploads, restore, route handlers, and mutations without turning into a test-infrastructure project. | Re-open in the next test-focused cycle; add regression coverage incrementally when touching each surface. |

## Notes
- This file preserves original severity/confidence; deferral does **not** downgrade risk.
- Workspace-only operational issues remain valid findings even when not directly committable; they are deferred here only because safe remediation depends on operator-owned secrets/infrastructure.
