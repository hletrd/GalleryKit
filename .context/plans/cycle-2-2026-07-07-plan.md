# Run-10 Cycle 2/100 Implementation Plan

Start HEAD: `642c5091` (== origin/master at cycle start; clean tree).
Review aggregate: `.context/reviews/cycle-2-2026-07-07/_aggregate.md` (C2-01..C2-55).
Deferred register: `.context/plans/cycle-2-2026-07-07-deferred.md`.

Gates (all blocking, repo-wide): eslint, typecheck, vitest, build, lint:api-auth,
lint:action-origin, lint:public-route-rate-limit, playwright e2e. DEPLOY_MODE per-cycle
(`npm run deploy` from repo root, exactly once, after gates green).

## Status legend
[x] done · [~] in progress · [ ] not started · [defer] moved to deferred register

---

## WP0 — [x] MANDATORY carry-over: deploy-host container build failure (drizzle-kit TS2307)
Closes: cycle-1 deploy failure + ARCH-07 (same class, runner stage).
Root cause: `package-lock.json` (post `abd1b5e1` repin) nests `drizzle-kit@0.31.10` + its
esbuild tree under `apps/web/node_modules`; Dockerfile builder stage copied only
`/app/node_modules`; tsconfig includes `**/*.ts` so `drizzle.config.ts` is typechecked by
`next build` → in-image TS2307. Local gates were green because the host checkout has
`apps/web/node_modules`.
Fix (committed `223b3836`, pushed): deps/prod-deps `mkdir -p apps/web/node_modules`;
builder + runner `COPY --from=... /app/apps/web/node_modules ./apps/web/node_modules`;
`.dockerignore` gains `**/node_modules`, `**/.next`, `**/._*` (AppleDouble/BuildKit-sender
parity with apps/web/.dockerignore).
Verification (done): clean git-worktree docker builds on arm64 (same as deploy host) —
negative control (original Dockerfile) reproduces `drizzle.config.ts(1,30): error TS2307`
exit 1; fixed Dockerfile completes builder stage (typecheck + next build) exit 0 with
`/app/apps/web/node_modules/drizzle-kit` present. The end-of-cycle deploy is the final proof.

## WP1 — [x] C2-05 (DBG-01, MED/High): ISOBMFF child-box parent-bound validation
Files: `src/lib/color-detection.ts` (`parseCicpFromHeif` inline check),
`src/lib/gain-map-detection.ts` (`readBoxHeader` + `parseIinf`/`parseIref`/`walk` callers).
Approach: port the `gps-exif-strip.ts:395-415` pattern — a child's `pos + size` must be
validated against the enclosing container's true `end`, not `buffer.length` (thread a
containerEnd parameter through `readBoxHeader` and the inline `parseCicpFromHeif` check).
Add crafted-buffer unit tests: (a) `iinf` child `infe` declaring size past `iinf` end →
rejected; (b) container child overflowing into a sibling before a `colr` box → CICP not
read from foreign bytes; (c) well-formed fixtures still parse. Non-deferrable (feeds the
HDR upload-accept gate).
Done: containerEnd threaded through readBoxHeader/parseIinf/parseIref/walk; parseCicpFromHeif bounds against walk end; isobmff-parent-bounds.test.ts (9 crafted-buffer tests). Commit 9ce5cf96.

## WP2 — [x] C2-01 (UX-01, HIGH/High): focus restore on Lightbox + Info sheet close
Files: `src/components/photo-viewer.tsx:518`, `src/components/lightbox.tsx:434-450`,
`src/components/info-bottom-sheet.tsx`.
Approach: adopt the proven `search.tsx` triggerRef pattern — photo-viewer passes an
explicit trigger ref into Lightbox; on close/unmount, focus that ref (the current
`document.activeElement` snapshot restores into a toolbar hidden via `display:none` while
the lightbox is open → `.focus()` no-op → body). For InfoBottomSheet, restore focus
explicitly (its conditional `return null` unmounts FocusTrap in the same commit as
deactivation, racing focus-trap-react's restore). Verify with e2e assertions
(Escape/Close returns focus to the opener) and keep the Search dialog behavior green.
Done: lightbox restoreFocusRef prop from photo-viewer (search.tsx pattern) + explicit info-sheet opener restore; e2e focus-restore.spec.ts. Commits 2c82a69c, fc21007a.

## WP3 — [x] C2-04 (UX-03, MED-HIGH/High): soft 404 on public not-found routes
Live evidence: `/en/p/99999999`, `/en/<bogus-topic>`, `/en/<bogus-path>` → HTTP 200.
Approach: reproduce locally against a production build (e2e server infra); curl status
codes for the three URL classes; isolate Next-standalone vs nginx vs
streaming-after-shell (`notFound()` thrown after the first flush cannot change the HTTP
status — suspect a layout-level Suspense boundary or late notFound). Fix whichever layer
is proven (candidates: throw `notFound()` from `generateMetadata` pre-stream; remove a
layout-level Suspense wrapper). Pin with e2e status-code assertions. Live verification
post-deploy.
Done: root cause = global [locale]/loading.tsx streamed a 200 shell before any notFound() (vercel/next.js#75543; even generateMetadata notFound cannot set status once streaming). Fix: entity-existence LAYOUTS ([topic], p/[id], c/[slug], year/[year]) resolve pre-stream; global loading.tsx removed (p/[id] lightbox skeleton preserved); metadata notFound as defense in depth. Locally verified: six 404 classes + 200 controls; e2e not-found-status.spec.ts. Commits 911cb0f5, faa6f0e5.

## WP4 — [x] C2-02 (TRC-01, HIGH/High): surface re-encode requirement on byte-impacting settings change
Files: `src/app/actions/settings.ts`, `src/lib/settings-hash.ts` (authoritative key list),
settings UI client + `messages/en.json`/`ko.json`.
Approach: when any of the 7 unfenced `COLOR_IMPACTING_KEYS` changes AND processed images
exist, still accept the change (documented operator model) but return
`requiresBackfill: true` and render a persistent notice in the settings UI pointing at
the existing "Re-encode existing photos" button. No hard block (unlike the
image_sizes/strip_gps contract keys, which keep their existing fence). Unit tests for the
diff detection + action response; i18n en+ko.
Done: unfenced key set derived from DERIVATIVE_BYTE_IMPACTING_SETTING_KEYS minus contract keys; requiresBackfill on real change + processed images; settings UI notice + en/ko keys; behavior tests. Commits 4e2ca838, 9d6675ee, f899edec.

## WP5 — [x] C2-03 (ARCH-01, HIGH/High): boot-time single-writer detector (warn-only)
New `src/lib/single-writer-guard.ts` + wiring in `instrumentation.ts`.
Approach: at startup, acquire advisory lock `gallerykit_web_singleton` (timeout 0) on a
DEDICATED non-pool connection and hold it for process lifetime (released by connection
close on shutdown/crash). If unavailable → `console.error` loudly (another live instance
shares this MySQL server) but DO NOT refuse to start (deploys recreate containers; the
guard must never take production down). Connection errors must never crash the app;
failure to establish the guard degrades to a logged warning. Release on graceful
shutdown. CLAUDE.md runtime-topology note. Unit tests with a mocked connection.
Done: single-writer-guard.ts (warn-only, dedicated connection, never throws), instrumentation wiring + shutdown release, advisory-locks constant, 7 unit tests; CLAUDE.md note. Live-validated during local e2e server overlap (guard fired correctly). Commit e39ad990.

## WP6 — [x] C2-06 (CRIT-01, MED/High) doc+edge part: public SSR page rate limiting
(a) Shipped nginx config: add a generous `limit_req` for the catch-all page surface,
scoped so `/_next/static`, `/uploads`, and asset locations are NOT throttled (verify the
location structure first; pick a rate real users and prefetching never hit, e.g. 10 r/s
burst 40 nodelay per IP). Config-only change; the deploy host's live nginx is
operator-managed — document the reload step.
(b) CLAUDE.md Security section: state the boundary explicitly (SSR pages are throttled at
the nginx edge only; app-layer limiters cover routes/actions; operators behind other
proxies must supply their own edge limit).
Gate-extension half (CRIT-03 / C2-07) → deferred register.
Done: zone=public 10r/s burst=40 nodelay on catch-all only; /_next/static + /_next/image locations added un-throttled; 429 status; CLAUDE.md boundary note. Commit af3b2f7d.

## WP7 — [x] C2-08 (TRC-02, MED/High): image-queue pool-limit constant drift
`src/lib/image-queue.ts:113-134`: import `POOL_CONNECTION_LIMIT` from `@/db` and pass it
to `resolveImageQueueConcurrency` (keep the local constant only as the exported function's
default param for tests, matching admin-backfill-runner). Grep for other hardcoded pool
literals.
Done: POOL_CONNECTION_LIMIT imported from @/db and passed explicitly; no other drifted literals. Commit 02bea8d6.

## WP8 — [x] C2-09 (TRC-06, MED/High): audit-log failure visibility
`src/lib/audit.ts`: make `logAuditEvent` catch internally and `console.error`
(`[audit] failed to record <action>`) so a transient DB error during a security-relevant
action is operator-visible; callers' existing `.catch(console.debug)` become harmless
no-ops (the function no longer rejects). Update the doc comment + unit test.
Done: logAuditEvent never rejects, console.error with action name; audit-log-failure.test.ts. Commit b24572b0.

## WP9 — [x] C2-45 (TRC-05, LOW-MED/High): distinct restore-blocker errors
`db-actions.ts:403-528`: the four non-blocking lock acquisitions currently all return
`restoreInProgress`. Give the backfill-lock branches and the upload-contract branch
distinct i18n messages (en+ko); keep `restoreInProgress` for the literal concurrent
restore. Test the branch mapping.
Done: restoreBlockedByUpload / restoreBlockedByBackfill (en+ko); branch-mapping test. Commits 4e2ca838, fa35fc78.

## WP10 — [x] C2-11 (PERF-01, MED/High): SW 304 body-rewrite elimination
`public/sw.template.js` (+ `src/lib/sw-cache.ts` reference + contract test + regenerate
`public/sw.js` via build-sw): on 304/same-ETag confirmation, do NOT rebuild/re-`put` the
cached body; refresh recency via the LRU meta store (`touchMeta`) and make expiry read the
meta timestamp (header fallback only for pre-change entries).
Done: 304/same-ETag return cached body directly after touchMeta; meta-first expiry with legacy header fallback; sw-cache.ts mirror + contract tests; sw.js regenerated. Commit bf5a4da9.

## WP11 — [x] C2-13 (PERF-03, MED/High): lean topics accessor for hot pages
`src/lib/data.ts:516-528`: split `getTopics()` — nav/home/topic/collection pages get a
lean query without the per-topic `MAX(updated_at)` correlated subquery; the sitemap keeps
a dedicated accessor with the subquery (its ISR window is the documented justification).
Verify no non-sitemap consumer reads `updatedAt`. Unit test pins the lean shape.
Done: lean getTopics/getTopicsCached; sitemap-only getTopicsWithLatestUpdate; only sitemap consumed updatedAt; SQL-shape test. Commit 9bd2daf3.

## WP12 — [x] C2-14 (PERF-04, MED/High) decode part: zero-copy embedding decode
`src/lib/clip-embeddings.ts` (`decodeEmbeddingColumn`): replace the 512-iteration
`readFloatLE` loop with an aligned zero-copy `Float32Array(buf.buffer, buf.byteOffset, N)`
view when `byteOffset % 4 === 0` (copy fallback otherwise; keep a readFloatLE path for
big-endian platforms via a module-level endianness check). Unit tests: aligned,
misaligned, value-equivalence vs the old loop. TTL matrix cache → deferred (C2-14b).
Done: LE aligned-copy Float32Array (copy over zero-copy: mysql2 buffer pooling), BE readFloatLE fallback; equivalence/misalignment/NaN tests. Commit ea712cfc.

## WP13 — [x] C2-17 (PERF-07, MED/High): join-UPDATE for tag mutations
`src/app/actions/tags.ts` `updateTag`/`deleteTag`: replace SELECT-all-ids + `IN (...)`
UPDATE with a single
`UPDATE images JOIN image_tags ON image_tags.image_id = images.id SET updated_at = ...
WHERE image_tags.tag_id = ?` inside the existing transaction. Adapt existing tests.
Done: single join-UPDATE in updateTag/deleteTag (deleteTag stamps before join rows deleted); tests assert join shape. Commit eb3a7ad9.

## WP14 — [x] C2-18 (PERF-08+PERF-19, MED/High): ref-based drag/swipe transforms
`src/components/info-bottom-sheet.tsx:89-94` and `src/components/photo-navigation.tsx:115`:
write `style.transform` directly to refs during touchmove (the `image-zoom.tsx` idiom);
commit React state once on touchend. No behavioral change; mobile e2e stays green.
Done: ref-based transforms in info sheet + photo navigation, state settles on touchend. Commits fc21007a, ffc4a06e.

## WP15 — [x] C2-19 (PERF-09, MED/High): memoized MasonryCard
`src/components/home-client.tsx:310-438`: extract the inline card into a `React.memo`
`MasonryCard` (props: image, estimatedCardWidth, isAboveFold, topicLabel, locale, plus
pre-bound strings). Careful prop identity (stable callbacks, no fresh objects per render
where avoidable). Home e2e + unit tests stay green.
Done: React.memo MasonryCard extracted with stable prop contract; memo test; source contracts re-pointed. Commit e5504bc8.

## WP16 — [x] C2-42 (ARCH-09, LOW/High): complete SeoSettings fallback
Shared defaulted-fallback builder (all fields from `siteConfig`, correct `nav_title`
default) used by `nav.tsx:11` (and any other `.catch` fallback consumer). Unit test.
Done: buildSeoSettingsFallback complete-object fallback; nav.tsx fixed; test. Commit 9bd2daf3.

## WP17 — [x] C2-37 (SEC-05, LOW/Med): middleware CSP fail-open-to-safe-fallback
`src/proxy.ts:44` + `src/lib/content-security-policy.ts`: wrap the CSP build in try/catch;
on a malformed runtime `IMAGE_BASE_URL`, log loudly (once) and fall back to a CSP without
the CDN source instead of 500ing every request. Unit test the fallback.
Done: buildCspSafely never throws; once-per-process error log; fallback CSP without image origin; test. Commit a4a2d250.

## WP18 — [x] C2-29 (CRIT-02, LOW/High): sitemap URL budget
`src/app/sitemap.ts:44-49,90-112`: fold feed + per-topic-feed rows into the reservation
and clamp the final array to `MAX_SITEMAP_URLS`. Unit test the boundary arithmetic.
Done: reservation includes feed rows; final clamp at MAX_SITEMAP_URLS; boundary test. Commit 9bd2daf3.

## WP19 — [x] C2-10 (TRC-04, MED/Low-Med): config resolver for non-request contexts
`src/lib/gallery-config.ts`: export an explicit uncached accessor (the raw
`_getGalleryConfig`) and use it at the three detached-queue call sites in
`image-queue.ts` (:429, :673, :791) so semantic-mode flips are always re-read at job time
regardless of React `cache()` semantics outside a request store. Comment the contract;
test that the queue paths use the uncached resolver.
Done: getGalleryConfigUncached + three queue call sites switched; wiring test. Commit 02bea8d6.

## WP20 — [x] C2-32 (CQ-01, LOW/High): processing-retry backoff
`src/lib/image-queue.ts:822-829`: schedule the re-enqueue with the same escalating
`setTimeout(...).unref()` pattern the claim-retry path uses. Extend queue tests
deterministically (fake timers or injected scheduler).
Done: escalating unref-d setTimeout schedule mirroring claim retry; backoff test. Commit 02bea8d6.

## WP21 — [x] C2-34 (CQ-03, LOW→MED/Med): cap embedding bootstrap walk
`src/lib/image-queue.ts:426-482`: bound each `bootstrapMissingActiveEmbeddings`
invocation (reuse the semantic scan limit) and log a continuation line when more rows
remain (matches the sidecar's bounded-per-run model). Test the cap.
Done: per-invocation cap at semantic scan limit + continuation log; cap test. Commit 02bea8d6.

## WP22 — [x] C2-33 (CQ-02, LOW/Med): duplicate hourly GC timer on state re-init
`src/lib/image-queue.ts:337-350`: clear the orphaned `gcInterval` when the defensive
re-init path replaces a malformed state object. Test.
Done: defensive re-init clears orphaned gcInterval; test. Commit 02bea8d6.

## WP23 — [x] C2-41 (ARCH-08, LOW/High): CSP for prod /api responses
`next.config.ts headers()`: non-dev minimal CSP (`default-src 'none'; frame-ancestors
'none'`) for `/api/:path*` (image responses are unaffected as embedded resources). Pin
with a header-shape test.
Done: non-dev /api/:path* CSP default-src none/frame-ancestors none/sandbox; header test. Commit 3b8d05c8.

## WP24 — [x] Tests: C2-22, C2-23, C2-48, C2-49 (TEST-01/02/03/04)
(a) `checkRateLimit` direct tests (mock db.select chain; exceeded/not-exceeded boundary,
`count ?? 0`, clause args). (b) `editable-target.test.ts` jsdom matrix (input/textarea/
contentEditable/button-closest/role-switch/plain-div). (c) two cases for
`humanizeColorPrimariesOrLabel` incl. the `viewer.colorUnknown` fallback. (d) renderHook
test for `useRestoreFocusAfterPending` (refocus only when activeElement is body/null).
Done: checkRateLimit direct tests, editable-target jsdom-free matrix, color-label fallback, restore-focus hook tests. Commit 2fe8f556.

## WP25 — [x] Docs batch (C2-43, C2-44, C2-51, C2-24-doc, C2-36, C2-47)
CLAUDE.md: GPS-strip HEIC/HEIF fail-closed wording (VER-01); name
`requireCleanInput`/`sanitizeAdminString` as the bidi enforcement points (VER-02); add the
two env-gated CLIP test commands to the production-activation runbook (TEST-06); document
site-config.json as build-time-inlined (mount is inert until rebuild) + compose comment
(ARCH-03 doc part); one-line comments at the two `Promise.all` count/rows call sites in
`data.ts` documenting the benign non-atomicity (DBG-02); pool-budget doc note that
topic/restore locks transiently pin 1-2 extra connections (TRC-07).
Done: all six doc edits (CLAUDE.md x5 + compose comment + data.ts comments via WP11 commit). Commits cc400622, 9bd2daf3.

## WP26 — [x] Ledger upkeep
Update `.context/plans/README.md` index (cycle-2 entries); keep this plan's checkboxes
current during implementation; fold the pending feature-dev code-reviewer message into
`cycle-2-2026-07-07/fd-code-reviewer.md` + an aggregate addendum when it arrives (add any
new findings as plan/deferred items before closing the cycle).

## Post-deploy verification items
- [x] UX-02 (C2-52): VERIFIED live post-deploy 2026-07-07 — timeline month headings render
  "November 2025 · 276 photos" style separators.
- [x] WP3: VERIFIED live post-deploy — photo/topic/path/collection/year not-found classes all
  return 404; home/timeline/real-photo/ko return 200.
- [x] WP0: deploy succeeded (per-cycle-success; container image rebuilt on the deploy host,
  /api/live 200).

## Verified-covered this cycle (no action; recorded so nothing is silently dropped)
- C2-25 (ARCH-04): `__tests__/migration-journal-monotonicity.test.ts` already locks
  strictly-monotonic `when` by idx with a historical allowlist + the post-condition
  predicate + the loud-fail throw. Architect lane missed the existing test.
- C2-26 (ARCH-05/CQ-05): `__tests__/migrate-reconcile-coverage.test.ts` already asserts
  every schema table/column appears in migrate.js executable code (comment-stripped).
- C2-52 (UX-02): already fixed at HEAD (`c923e15d`); resolved by this cycle's deploy
  (verification item above).
- C2-39/C2-40 (SEC-01/03/04/06): security lane's own disposition is
  accepted-by-design/no-action (denylist is documented defense-in-depth on an admin-gated
  surface; audit IP retention is intentional forensic logging bounded by
  AUDIT_LOG_RETENTION_DAYS; XFH topology + per-process buckets are documented boundaries).
