# Cycle 11/100 Implementation Plan

Date: 2026-06-29
Source reviews: `.context/reviews/_aggregate.md` plus per-agent reports.
Status: IMPLEMENTED - awaiting full gates

Repo rules read before planning: `CLAUDE.md`, `AGENTS.md`, `.context/plans/README.md`, current `.context/reviews/*.md`, and `docs/superpowers/**`. No `.cursorrules` or `CONTRIBUTING.md` files exist in this repo.

## Scheduled Fixes

These findings are scheduled for Prompt 3. Security, correctness, privacy, and user-facing confirmed defects are not deferred.

| ID | Aggregate finding | Files / surface | Planned fix | Status |
| --- | --- | --- | --- | --- |
| C11-01 | Restore can hang if post-restore migration setup throws | `apps/web/src/app/[locale]/admin/db-actions.ts`, restore tests | Catch `runPostRestoreMigrations` setup/runtime throws inside the mysql close handler and resolve a failed restore result so the outer cleanup/finally always runs; add regression coverage. | DONE |
| C11-02 | Missing-embedding bootstrap schedules unbounded side effects | `apps/web/src/lib/image-queue.ts`, queue tests | Process missing-embedding retry pages with bounded awaited chunks; add regression that the next page is not selected until the prior page settles. | DONE |
| C11-03 | Sidecar backfills accept unbounded/non-integer concurrency | `apps/web/scripts/backfill-color-pipeline.ts`, `apps/web/scripts/backfill-cicp-recheck.ts`, env/source tests | Use finite bounded integer parsing with an explicit sidecar cap; lock `Infinity`, `1e309`, fractional, zero, negative, and huge values. | DONE |
| C11-04 | Pre-body semantic aborts consume search rate budget | `apps/web/src/app/api/search/semantic/route.ts`, semantic route tests | Check `request.signal.aborted` before semantic rate-limit pre-increment or roll back the pre-body abort branch; add already-aborted route test. | DONE |
| C11-05 | Same-origin checks run after auth/session work | server actions, `check-action-origin.ts`, scanner tests | Move mutating admin action prologues to `requireSameOriginAdmin()` before auth/current-user/DB side effects where safe; strengthen scanner tests for auth-before-origin ordering. | DONE |
| C11-06 | PAT auth reaches DB before app-level throttling | `apps/web/src/lib/api-auth.ts`, `apps/web/src/lib/rate-limit.ts`, API auth tests | Add a lightweight bounded in-memory pre-auth limiter for token attempts before `verifyToken`, keyed by client IP; keep generous budget for Lightroom bursts. | DONE |
| C11-07 | LIKE escaping depends on implicit MySQL backslash semantics | `apps/web/src/lib/data.ts`, `smart-collections.ts`, tests | Centralize LIKE pattern escaping in a helper that emits explicit `ESCAPE '\\'`, or add a startup/session SQL-mode guard. Prefer explicit SQL helper with tests. | DONE |
| C11-08 | Base URL docs/guard and OG origin can split | `ensure-site-config.mjs`, OG photo route, docs/tests | Align per-photo OG trusted origin and fallback URL with the effective canonical base URL (`BASE_URL || siteConfig.url`) or hard-fail mismatched `siteConfig.url`; update docs/tests to match chosen contract. | DONE |
| C11-09 | Service-worker docs overstate public-page no-store contract | `CLAUDE.md`, `sw.template.js`, generated `sw.js`, SW test | Narrow the comment/test wording to dynamic public gallery pages or add `revalidate = 0` to privacy. Prefer comment correction because privacy is static. | DONE |
| C11-10 | Atom feed comments imply per-entry admin authors | feed routes | Replace stale comments with current privacy invariant: public feed entries use feed-level author until a safe public display-name field exists. | DONE |
| C11-11 | Privacy page falsely says public pages exclude GPS | EN/KO messages, privacy page | Scope copy: standard gallery/photo pages exclude GPS, while the public map can show coordinates for admin-enabled map-visible topics. | DONE |
| C11-12 | Footer hides Privacy unless Google Analytics is configured | `apps/web/src/components/footer.tsx` | Always render the Privacy link; keep analytics-specific copy conditional inside the page. | DONE |
| C11-13 | Bulk editor claims AI-suggested alt text while generator is EXIF stub | EN/KO messages, bulk edit tests if needed | Change copy to EXIF-derived suggested alt text until real model inference ships. | DONE |
| C11-14 | README "batch editing" can imply photo editing | `README.md` | Rename to batch metadata editing and add a short "not an editor/culler/scorer" note. | DONE |
| C11-15 | Backup download chmod test can flake under root | `backup-download-route.test.ts` | Replace chmod permission oracle with deterministic mocked filesystem failure. | DONE |
| C11-16 | Timeline/year grids lazy-load first visible photos | timeline/year routes and source tests | Add above-the-fold eager/high-priority loading for initial archive cards, matching main/shared grids. | DONE |
| C11-17 | Timeline/year card geometry lacks dimension guard | timeline/year routes and source tests | Add valid-dimension fallback (`1 / 1`) and source coverage. | DONE |
| C11-18 | Map loading fallback is visually blank | `map-loader.tsx`, map tests | Render visible loading text/spinner while keeping `role="status"` and existing i18n. | DONE |
| C11-19 | Primary photo/lightbox images expose generic alt text | `photo-title.ts`, `photo-viewer.tsx`, `lightbox.tsx`, tests | Feed tag-derived display title into primary image/lightbox alt fallback when no authored alt text/title exists. | DONE |
| C11-20 | Photo page advertises Space slideshow where Space scrolls | messages/photo viewer or keyboard handling/tests | Either implement Space on the page or scope the shortcut copy to lightbox. Prefer scoping copy unless product wants page-level slideshow. | DONE |
| C11-21 | Mobile info-sheet swipes trigger photo navigation | `photo-navigation.tsx`, `photo-viewer.tsx`, mobile test | Disable `PhotoNavigation` while bottom sheet/dialog is open and/or ignore swipes from dialog/interactive targets. | DONE |

## Deferred Ledger

Deferred findings from this cycle are recorded in `.context/plans/cycle-11-2026-06-29-deferred.md`. Prompt 3 must preserve this ledger and may update an item only if it is fixed.

## Verification Plan

Run targeted tests for the touched surfaces first, then all configured gates:

- `npm run lint --workspace=apps/web`
- `npm run lint:api-auth --workspace=apps/web`
- `npm run lint:action-origin --workspace=apps/web`
- `npm run lint:public-route-rate-limit --workspace=apps/web`
- `npm run typecheck --workspace=apps/web`
- `npm run build --workspace=apps/web`
- `npm test --workspace=apps/web`

After all scheduled fixes are committed, pushed, and gates are green, run `npm run deploy` once from repo root because this cycle is per-cycle deploy mode.

## Implementation Progress

- Targeted regression/source-contract tests passed:
  `npm test --workspace=apps/web -- restore-upload-lock image-queue-embed-wiring semantic-search-route map-thumb-wiring backup-download-route cycle-11-source-contracts check-action-origin semantic-search-rate-limit photo-title alt-text-fallback`
  (11 files, 140 tests).
- `npm run typecheck --workspace=apps/web` passed after implementation.
- C11-19 was verified against existing viewer/lightbox code and `photo-title`/alt-text tests: both primary viewer and lightbox images call `getConcisePhotoAltText`, which already falls back through tag-derived titles and `alt_text_suggested`.
- Full gate results:
  - `npm run lint --workspace=apps/web` passed after fixing two React immutability lint errors in the archive eager-load implementation.
  - `npm run lint:api-auth --workspace=apps/web` passed.
  - `npm run lint:action-origin --workspace=apps/web` passed with the stricter auth-before-origin scanner.
  - `npm run lint:public-route-rate-limit --workspace=apps/web` passed.
  - `npm run typecheck --workspace=apps/web` passed.
  - `npm run build --workspace=apps/web` passed; it regenerated `public/sw.js` and used the sitemap DB-unavailable fallback because local MySQL was not listening.
  - `npm test --workspace=apps/web` passed (255 files passed, 2 skipped; 2364 tests passed, 4 skipped).

## Commit / Deploy Plan

Use fine-grained signed commits with Conventional Commit + gitmoji. Pull with rebase before each push. Do not deploy until all scheduled fixes and configured gates are complete.
