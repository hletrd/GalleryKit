# Cycle 14/100 Implementation Plan

Date: 2026-06-30
Source reviews: `.context/reviews/_aggregate.md` plus all cycle-14 per-agent reports.
Status: TODO

Repo rules read before planning: `CLAUDE.md`, `AGENTS.md`, `.context/plans/README.md`, `.context/reviews/*.md`, `docs/superpowers/**`, and current tracked project docs. No `.cursorrules` or `CONTRIBUTING.md` files exist in this repo.

Security, correctness, data-loss, and gate false-pass findings are scheduled here. Prompt 3 must fix every item below, update each Status, run all configured gates, commit/push, then deploy once with `npm run deploy`.

## Scheduled Fixes

| ID | Aggregate finding | Files / surface | Planned fix | Status |
| --- | --- | --- | --- | --- |
| C14-01 | AGG-C14-01 original-upload helpers trust DB filenames | `apps/web/src/lib/upload-paths.ts`, `apps/web/src/__tests__/upload-paths.test.ts` | Centralize safe original filename validation, absolute-path rejection, symlink rejection, and realpath containment in original path resolution/delete helpers. Add tests for traversal, absolute path, symlink escape, missing file, primary hit, and legacy hit. | DONE |
| C14-02 | AGG-C14-02 nginx proxy-header/docs mismatch | `apps/web/nginx/default.conf`, `README.md`, `CLAUDE.md`, `apps/web/src/__tests__/nginx-config.test.ts` | Align docs and tests with the supported direct nginx hop unless the config is changed. Keep this a docs/test contract fix, not production proxy surgery, because live deploy uses the documented single host. | DONE |
| C14-03 | AGG-C14-03 review scratch artifacts can be committed | `.gitignore` | Add targeted ignore rules for review scratch/temp artifacts while preserving intentional `.context/reviews/*.md` reports. | DONE |
| C14-04 | AGG-C14-09 backfill no-op leaves stale admin status | `apps/web/src/lib/admin-backfill-runner.ts`, relevant backfill tests | Treat zero-candidate trigger as a completed no-op run that clears per-run counters/failures. Add regression coverage seeded with stale failure state. | DONE |
| C14-05 | AGG-C14-10 touch-target audit stale slack | `apps/web/src/__tests__/touch-target-audit.test.ts` | Make known-violation budgets exact or fingerprinted; add a self-check fixture proving stale allowance cannot absorb a new violation. | DONE |
| C14-06 | AGG-C14-11 public route rate-limit exemption is file-level | `apps/web/scripts/check-public-route-rate-limit.ts`, `apps/web/src/__tests__/check-public-route-rate-limit.test.ts` | Make exemptions handler-local or fail mixed-handler file-level exemptions. Add a mixed exempt-POST/unmetered-DELETE fixture that must fail. | DONE |
| C14-07 | AGG-C14-12 similar-photo target visibility | `apps/web/src/app/api/search/similar/[id]/route.ts`, semantic/similar tests | Require target and candidate embeddings to join processed images before scoring. Add a regression that an unprocessed image with an embedding returns 404 or is excluded. | DONE |
| C14-08 | AGG-C14-13 backup creation lacks header validation | `apps/web/src/app/[locale]/admin/db-actions.ts`, backup tests | Reuse `hasPlausibleSqlDumpHeader()` after dump completion; delete invalid non-empty dumps and surface failure. Add a source/behavior regression. | DONE |
| C14-09 | AGG-C14-14 `db:push` unsafe docs | `CLAUDE.md`, `apps/web/README.md` | Mark `db:push` local throwaway/prototyping only or remove it from normal database commands; point schema work to journaled migrations. | DONE |
| C14-10 | AGG-C14-15 and AGG-C14-35 CLIP docs drift | `apps/web/scripts/backfill-clip-embeddings.ts`, `CLAUDE.md`, `apps/web/.env.local.example`, `docs/superpowers/specs/2026-06-14-clip-semantic-search-design.md` | Document `CLIP_INFERENCE_CONCURRENCY` default/cap/caveats and make all CLIP model-cache paths consistently `data/models/clip`. | DONE |
| C14-11 | AGG-C14-16 search input touch target | `apps/web/src/components/search.tsx`, touch-target audit if needed | Remove/replace the `h-8` override so the search input respects the 44 px target floor. | DONE |
| C14-12 | AGG-C14-17 mobile nav controlled regions | `apps/web/src/components/nav-client.tsx` | Point `aria-controls` at both controlled mobile regions or a shared wrapper. | DONE |
| C14-13 | AGG-C14-18 admin dialog descriptions | `topic-manager.tsx`, `tag-manager.tsx`, `apps/web/messages/en.json`, `apps/web/messages/ko.json` if needed | Add localized `DialogDescription` text for create/edit category and edit tag dialogs, or explicitly opt out if description is redundant. Prefer adding descriptions. | DONE |
| C14-14 | AGG-C14-19 admin table overflow | category, tag, and user admin table components | Wrap affected tables in local horizontal overflow with stable min widths, matching existing dashboard/analytics patterns. | DONE |
| C14-15 | AGG-C14-20 settings/SEO mobile Korean layout | settings and SEO clients | Add responsive wrapping/min-width controls and replace fixed select widths with mobile-safe widths. | DONE |
| C14-16 | AGG-C14-21 public home empty state | `apps/web/src/components/home-client.tsx`, messages if needed | Make the fresh-gallery empty state more informative; preserve filtered clear action. | DONE |
| C14-17 | AGG-C14-22 through AGG-C14-24 and AGG-C14-38/39 product copy/feed defaults | `README.md`, `apps/web/src/site-config.example.json`, feed fallback code if needed, EN/KO messages | Align terminology to Categories, narrow the metadata-search claim, qualify PWA offline behavior, correct Firefox color wording, and prevent blank feed author/rights on fresh config. | DONE |
| C14-18 | AGG-C14-25 invalid share keys consume lookup budget | public share/group pages and tests | Add cheap Base56 validation before charging lookup rate limit while preserving rate limit before valid-looking DB lookup. | DONE |
| C14-19 | AGG-C14-30 semantic rate-limit helper comment | `apps/web/src/lib/rate-limit.ts` | Update comment to match the current charged short-query/body-admission contract. | DONE |
| C14-20 | AGG-C14-31 sitemap/robots coverage | `apps/web/src/app/sitemap.ts`, `apps/web/src/app/robots.ts`, new/updated tests | Add direct unit tests for sitemap URL budget/feed entries/localization and robots crawl policy. | DONE |
| C14-21 | AGG-C14-34 embedding bootstrap restore quiescence | `apps/web/src/lib/image-queue.ts`, restore/queue tests | Track the bootstrap retry root promise so restore quiescence awaits it, and re-check maintenance near writes where feasible. Add regression coverage. | DONE |

## Deferred Ledger

All remaining aggregate findings and manual-validation risks are recorded in `.context/plans/cycle-14-2026-06-30-deferred.md`. Security/correctness/data-loss items are not deferred unless the item is an operational validation risk explicitly governed by the repo's documented single-instance/operator-boundary rules.

## Verification Plan

Run targeted tests for each touched surface first, then all configured gates:

- `npm run lint --workspace=apps/web`
- `npm run lint:api-auth --workspace=apps/web`
- `npm run lint:action-origin --workspace=apps/web`
- `npm run lint:public-route-rate-limit --workspace=apps/web`
- `npm run typecheck --workspace=apps/web`
- `npm run build --workspace=apps/web`
- `npm test --workspace=apps/web`

If browser-flow coverage is feasible after fixes, run `npm run test:e2e --workspace=apps/web`; otherwise preserve the local MySQL blocker in the report. After all scheduled fixes are committed, pushed, and gates are green, run `npm run deploy` once from repo root.

## Commit / Deploy Plan

Use fine-grained GPG-signed Conventional Commit + gitmoji commits. Always `git pull --rebase` before pushing. Do not deploy until all scheduled fixes are complete and configured gates are green.
