# Cycle 15/100 Implementation Plan

Date: 2026-06-30 KST
Status: IMPLEMENTED
Source reviews: `.context/reviews/*.md`, aggregate `.context/reviews/_aggregate.md`

## Scheduled Fixes

| ID | Finding(s) | Severity / confidence | Implementation plan | Progress |
| --- | --- | --- | --- | --- |
| C15-P01 | AGG-C15-01 restore retry dead-end | High / High | Split restore maintenance writer-blocking from restore retry admission. Let `restoreDatabase()` proceed under active maintenance only after admin/origin validation and DB restore advisory-lock acquisition, while other mutating paths remain blocked. Add regression coverage for retry after failed maintenance. | DONE |
| C15-P02 | AGG-C15-02 duplicate semantic toggle request | Medium / High | Remove the direct toggle-handler `performSearch` call and let the `[query, useSemanticSearch]` effect own mode-change searches. Add source or behavior coverage preventing direct handler searches. | DONE |
| C15-P03 | AGG-C15-03 upload metadata latest-wins mismatch | Medium / High | Keep topic and global tag metadata controls enabled during an active sequential upload while leaving drop/start/remove controls locked. Extend upload-dropzone source contract tests. | DONE |
| C15-P04 | AGG-C15-04 ignored rate-limit results; AGG-C15-05 helper mutation before origin guard | Medium / High | Harden custom gates: public mutating routes/actions must return early from over-limit results before mutation, and action-origin scanning must fail closed for pre-guard local helper calls that can hide mutation. Add failing fixtures. | DONE |
| C15-P05 | AGG-C15-06 semantic result-shape test gap | Medium / High | Add semantic route assertions for enriched public fields such as `lens_model` and `capture_date`, mirroring similar-route coverage. | DONE |
| C15-P06 | AGG-C15-07 scanner zero-discovery fail-open | Low / Medium | Fail closed when expected route roots exist but route discovery finds zero files for admin API auth or public route rate-limit scanners. Add scanner tests or source-contract assertions. | DONE |
| C15-P07 | AGG-C15-08, C15-09, C15-10, C15-11 docs/index drift | Low / Medium-High | Align semantic rollback comments, display capability comments/tests, sidecar backfill concurrency docs, and `.context/plans/README.md` active/completed status. | DONE |
| C15-P08 | AGG-C15-12 analytics ignores `BASE_URL` | Medium / High | Make referrer self-classification use the effective public URL contract (`BASE_URL || siteConfig.url`) and add a test. | DONE |
| C15-P09 | AGG-C15-14, C15-15 README product-copy drift | Low / High; Low / Medium-High | Update README wording from albums to categories/sharing and clarify that no Lightroom Classic plugin is bundled. | DONE |
| C15-P10 | AGG-C15-16 locale-prefixed upload derivatives skip SW cache | Medium / Medium | Update shared SW derivative predicate and generated/template service worker copy to accept optional locale prefixes while preserving original-upload exclusion. Add tests. | DONE |
| C15-P11 | AGG-C15-17 public map accessible structure | Medium / High | Add a labeled map region, skip-to-list link, concise instructions, and a dedicated fallback-list label. Add/update i18n messages and tests if present. | DONE |
| C15-P12 | AGG-C15-18 root `dir` future-proofing | Low / High | Introduce a locale-direction helper and use it in root layout, returning `ltr` for current `en`/`ko`. Add source/test coverage if a locale helper test already exists. | DONE |
| C15-P13 | AGG-C15-19 settings switch responsive pattern | Low / Medium-High | Apply the existing responsive switch-row layout pattern to the listed settings rows. Preserve accessible descriptions and 44 px targets. | DONE |

## Deferred Findings

All other aggregate findings are recorded in `.context/plans/cycle-15-2026-06-30-deferred.md` with original severity/confidence, reason, and exit criterion.

## Required Gates

- `npm run lint --workspace=apps/web`
- `npm run lint:api-auth --workspace=apps/web`
- `npm run lint:action-origin --workspace=apps/web`
- `npm run lint:public-route-rate-limit --workspace=apps/web`
- `npm run typecheck --workspace=apps/web`
- `npm run build --workspace=apps/web`
- `npm test --workspace=apps/web`

## Deployment

Deploy mode: per-cycle. After all scheduled fixes are implemented, committed, pushed, and gates are green, run exactly:

```sh
npm run deploy
```
