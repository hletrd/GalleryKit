# Cycle 20 — Deferred Findings

Per review-plan-fix deferred rules: every cycle-20 aggregate finding is either scheduled in `cycle-20-plan.md` or recorded here. Severity/confidence are preserved and not downgraded. Repo rules consulted first: `CLAUDE.md`, `AGENTS.md`, `.context/plans/README.md`, current `.context/reviews/*`, and current `.context/plans/*`.

Security/correctness/data-loss findings from the aggregate are scheduled unless they are explicitly a validation-only risk or a local-operator/deployment risk whose current repo rules fence the production topology. Deferred work remains bound by repo policy when reopened: GPG-signed commits, Conventional Commits + gitmoji, no force-push, no `--no-verify`, Node 24+/TS 6+.

| ID | Finding | File:line | Original severity / confidence | Reason for deferral | Exit criterion |
|----|---------|-----------|--------------------------------|---------------------|----------------|
| AGG-C20-06 | Upload ingest has multiple implementation owners | `apps/web/src/app/actions/images.ts:114-190`, `apps/web/src/app/api/admin/lr/upload/route.ts:225-516` | Medium / High | Broad architectural extraction on the hottest upload path. Current parity is covered by source-contract and behavior tests, and no fresh data-loss instance was found. Scheduling this with the high/security fixes would increase cycle blast radius. | Before adding any upload-time processing/privacy/metadata field, or when browser/LR upload parity drifts again. |
| AGG-C20-07 | Image processing jobs can pin most of the shared MySQL pool during Sharp work | `apps/web/src/lib/image-queue.ts:513-637` | Medium / High | Operational/performance risk mainly when `QUEUE_CONCURRENCY` is raised above the default 1. Current single-instance topology is documented in `CLAUDE.md` "Runtime topology"; no live pool starvation was observed this cycle. | If `QUEUE_CONCURRENCY` is raised in production, pool acquisition errors appear, or queue throughput tuning is next touched. |
| AGG-C20-13 | Behavior-critical route/UI/queue fixes rely too heavily on source-contract tests | `clip-model-contract.test.ts`, `cycle-19-source-contracts.test.ts`, `og-photo-fallback.test.ts` | Medium / High | Broad test-infra work across queue, route mocking, and browser behavior. This cycle adds behavior tests for scheduled fixes; the remaining source-contract replacement is coverage quality, not a confirmed live bug. | When touching CLIP queue, bulk-edit dialog, photo swipe/zoom, or OG route glue; or if a source-contract test gives false confidence in a regression. |
| AGG-C20-18 | Audit retention purge is a single unbounded DELETE | `apps/web/src/lib/audit.ts:97-122` | Low / High | Low-write operational risk; audit volume is much lower than analytics, which already has chunked retention. Not security/data-loss; no lock issue observed. | If audit volume grows, retention is lowered significantly, or audit GC lock time is observed. |
| AGG-C20-19 | Failed restore can leave maintenance active without evident recovery path | `apps/web/src/app/[locale]/admin/db-actions.ts:440-679` | Medium operational risk / Medium | Validation-only risk. Fail-closed maintenance is intentional for uncertain DB state, and this cycle schedules the watchdog correctness bug that can worsen stuck restore behavior. | If a failed restore is reproduced with no documented/admin recovery path after the watchdog fix, add recovery UX/runbook. |
| AGG-C20-20 | Listing/smart-collection pages combine tag aggregation with `COUNT(*) OVER()` | `apps/web/src/lib/data.ts:878-907`, `apps/web/src/lib/data.ts:1417-1461` | Medium / High | Scale-gated performance refactor; no current row-count/latency evidence. Existing public pages are functional and correctness is not in question. | Production row counts or traces show slow first-page loads, or listing query code is next refactored. |
| AGG-C20-21 | Public keyword search uses leading-wildcard scans after admission | `apps/web/src/lib/data.ts:1490-1563`, `apps/web/src/lib/data.ts:1601-1621` | Medium / High | Scale/performance issue. The user-visible live keyword failure is separately scheduled as T8; indexed search redesign is larger than this cycle. | If keyword search latency becomes measurable, corpus grows substantially, or search backend work is planned. |
| AGG-C20-22 | Semantic/similar search brute-force vector scoring runs on request thread | `api/search/semantic/route.ts:263-307`, `api/search/similar/[id]/route.ts:140-175` | Low-Medium / High | Bounded by `SEMANTIC_SCAN_LIMIT`, same-origin/rate limits, and current production corpus size. Similar abort handling is scheduled separately as T7. | Corpus approaches the scan limit, operators raise `SEMANTIC_SCAN_LIMIT`, or CPU/RSS traces show search pressure. |
| AGG-C20-23 | GPS stripping can materialize large originals and scrubbed copies in memory | `apps/web/src/lib/process-image.ts:1737-1792` | Low-Medium / High | Bounded by upload size limits and admin-only ingest; no memory incident observed. Privacy correctness of stripping was not challenged in this cycle. | If large GPS-stripped originals trigger RSS/GC pressure, or GPS stripping implementation is next changed. |
| AGG-C20-24 | Batch deletion repeats derivative-directory scans per image and format | `apps/web/src/lib/process-image.ts:575-627`, `apps/web/src/app/actions/images.ts:818-842` | Low-Medium / High | Scale-only admin performance issue; cleanup is idempotent and correctness-preserving. | If bulk deletes on large galleries become slow, or delete cleanup is next refactored. |
| AGG-C20-25 | High-cardinality map and masonry paths hydrate too much UI | `map/page.tsx`, `map-client.tsx`, `home-client.tsx`, `load-more.tsx` | Low-Medium / High/Medium | Scale/UI performance issue needing production cardinality and INP evidence. Dense auto-prefetch is scheduled separately as T9. | If map marker count approaches thousands for common users, or INP/heap traces show map/masonry pressure. |
| AGG-C20-26 | Timeline/archive predicates use non-sargable date functions | `apps/web/src/lib/data-timeline.ts` | Low-Medium / High | Scale-only query-shape issue without current latency evidence. | If timeline/year pages become slow or date-query indexes are redesigned. |
| AGG-C20-27 | Service-worker cached image hits wait on synchronous HEAD revalidation | `apps/web/public/sw.template.js`, `apps/web/public/sw.js` | Low-Medium / High | Deliberate freshness tradeoff documented in `CLAUDE.md`; no RUM/WebPageTest evidence that the 300 ms bound harms real paints. | If field data shows warm cached image paints blocked by HEAD latency, or cache freshness policy changes. |
| AGG-C20-28 | Admin dashboard/analytics fanout can consume much of the shared pool | admin dashboard/analytics data paths | Low-Medium / Medium | Admin-only performance risk without observed pool starvation. | If admin dashboard latency or pool acquisition warnings appear. |
| AGG-C20-29 | CSV export materializes up to 50,000 rows and duplicates payload in browser | CSV export path cited in `perf-reviewer.md` | Low-Medium / High | Admin-triggered scale issue; not a security/correctness defect. | If large CSV exports hit memory/timeout limits or export code is next touched. |
| AGG-C20-34 | Checked-in demo-domain config can become fresh-install canonical URL | `apps/web/src/site-config.json:2-10`, `ensure-site-config.mjs` | Medium / High | This repo is also the selected production deployment for `gallery.atik.kr`; replacing the checked-in config would break the current deploy target. Fresh-install packaging risk needs a separate product/distribution decision. | Before distributing GalleryKit as a template/package, require explicit non-demo site config or document fresh-install replacement more strongly. |
| AGG-C20-35 | Generic GalleryKit identity flows into public SEO/PWA/feed defaults | `site-config*.json`, `data.ts`, `manifest.ts`, feed route | Medium / High | Product-positioning/install polish. Current production config intentionally carries GalleryKit identity; no security/correctness defect. | Before public template distribution or if admin SEO defaults confuse operators. |
| AGG-C20-36 | README proves engineering depth before product experience | `README.md:1-208` | Low / High | Documentation/product-marketing polish, not correctness. | When README/product positioning is next edited. |
| AGG-C20-40 | Plan index status may mislead future cycles | `.context/plans/README.md:3-57` | Low / Medium | Process hygiene; not a product/runtime defect. | Before using the index as a backlog source, or during a dedicated plan-curation pass. |
| AGG-C20-41 | Nav visual checks create screenshots but do not compare them | `apps/web/e2e/nav-visual-check.spec.ts:6-79` | Low / High | Test naming/coverage quality. The existing spec still enforces target size and non-overlap; screenshot baseline work is optional and can be noisy. | If this spec is cited as visual-regression coverage, rename/comment it or add stable screenshot assertions. |

## Scheduled, Not Deferred

Scheduled in `cycle-20-plan.md`: `AGG-C20-01`, `AGG-C20-02`, `AGG-C20-03`, `AGG-C20-04`, `AGG-C20-05`, `AGG-C20-08`, `AGG-C20-09`, `AGG-C20-10`, `AGG-C20-11`, `AGG-C20-12`, `AGG-C20-14`, `AGG-C20-15`, `AGG-C20-16`, `AGG-C20-17`, `AGG-C20-30`, `AGG-C20-31`, `AGG-C20-32`, `AGG-C20-33`, `AGG-C20-37`, `AGG-C20-38`, `AGG-C20-39`.

## Notes

- No confirmed high-severity finding is deferred.
- No confirmed security finding is deferred except lower-risk/local-operator items that are scheduled for T11 where contained.
