# Run-10 Cycle 19/100 Deferred Findings

Status: OPEN
Aggregate: `.context/reviews/_aggregate.md`
Date: 2026-07-08 KST

This register records every Cycle 19 aggregate finding not scheduled in `cycle-19-2026-07-08-plan.md`. Severity and confidence are preserved from the aggregate. Deferred work remains bound by repo policy: GPG-signed Conventional Commits with gitmoji, `git pull --rebase` before push, required gates, no force-push, and the repo's Node/Next/React/TypeScript toolchain rules.

Repo rule basis for deferral: `.context/plans/README.md` defines the carry-forward age budget for deferred High/Medium findings; `.context/plans/deferred-carry-forward.md` is the committed register for open deferred rows. For topology/edge/security-ops items, `CLAUDE.md` documents the single web-instance/process-local topology and that public page throttling is applied at manually managed nginx edge config, not by per-iteration deploys.

## Deferred Items

| ID | Severity / Confidence | Citation | Reason for deferral | Exit criterion |
|----|-----------------------|----------|---------------------|----------------|
| AGG-C19-03 (streaming ingress remainder) | High / High | `apps/web/src/app/actions/images.ts:129-260`, `apps/web/src/app/api/admin/lr/upload/route.ts:101-188`, `apps/web/src/app/[locale]/admin/db-actions.ts:378-684` | Broad architecture change requiring streaming multipart route-handler redesign across browser upload, LR upload, and restore; this cycle schedules only the misleading helper-comment fix. Not security/data-loss by itself; current app has caps and the memory boundary is documented in `CLAUDE.md` Important Notes. | Production RSS breach/OOM during upload/restore, or approved streaming-ingress project. |
| AGG-C19-04 | High / High | `apps/web/src/db/index.ts:31-42`, `apps/web/src/lib/image-queue.ts:121-142`, `apps/web/src/lib/admin-backfill-runner.ts:106-727` | Shared background DB budget is a cross-cutting queue/backfill architecture change. Repo already documents this oversubscription in `CLAUDE.md` connection-pool notes as a known topology/performance risk. | Measured pool starvation during concurrent queue/backfill, or next queue/backfill architecture cycle. |
| AGG-C19-05 | High / High | `apps/web/src/app/api/search/semantic/route.ts:247-368`, `apps/web/src/app/api/search/similar/[id]/route.ts:137-285` | Vector indexing/search isolation is a larger performance/security-availability investment. `CLAUDE.md` explicitly documents semantic route scan limits, process-local fast paths, and operator-enabled production semantics. | Semantic traffic/gallery size exceeds scan limits, measured latency/CPU pressure, or vector index/cache project approved. |
| AGG-C19-06 (map scale remainder) | Medium / High | `apps/web/src/lib/data.ts:1766-1816`, `apps/web/src/components/map/map-client.tsx:77-140` | This cycle schedules the 44 px Leaflet control fix; clustering/viewport pagination is a larger map architecture change already present in carry-forward lineage. | GPS-visible rows approach thousands, mobile map trace shows multi-second mount, or map clustering project approved. |
| AGG-C19-07 | High / High | `apps/web/src/__tests__/migrate-reconcile-coverage.test.ts:13-19`, `apps/web/scripts/migrate.js:277-284` | DB-backed schema-convergence test requires disposable MySQL orchestration and may be CI/nightly-only; no current migration is being added this cycle. | Next migration/schema authoring cycle or a reconcile/schema drift incident. |
| AGG-C19-08 | Medium / High | `apps/web/src/__tests__/semantic-scan-limit-source.test.ts:1-76`, `apps/web/src/__tests__/load-more-source-contracts.test.ts:5-30` | Broad source-contract retirement spans many modules. This cycle adds behavior tests for the two correctness bugs it fixes. | Next source-contract-only finding on a high-risk path, or dedicated behavior-test hardening cycle. |
| AGG-C19-09 | Medium / High | `apps/web/playwright.config.ts:72-77`, `.github/workflows/quality.yml:75-80` | Adding cross-browser/mobile E2E expands runtime and CI surface; not tied to this narrow code fix except the touched UI smoke will still run in existing e2e. | Browser-specific regression, mobile/touch bug report, or approved Playwright matrix cycle. |
| AGG-C19-10 | Medium / High | `apps/web/e2e/admin.spec.ts:6-165`, `apps/web/e2e/helpers.ts:28-45` | Credentialed admin browser coverage needs env/fixture design and test-time budget. | Admin-flow regression or admin E2E expansion cycle. |
| AGG-C19-11 | Medium / High-Medium | `CLAUDE.md:245-247`, `apps/web/nginx/default.conf:274-306`, `apps/web/deploy.sh:51-55` | Security/availability item is operator/topology-bound. `CLAUDE.md` explicitly states public pages are throttled at nginx and deploys do not touch host nginx; process-local state is documented for the single-instance topology. | Operator applies/verifies nginx zones, proxy topology changes, scale-out attempted, or app-layer public page limiter project approved. |
| AGG-C19-12 | Low / High | `apps/web/src/lib/content-security-policy.ts:182-190` | Defense-in-depth hardening with no confirmed XSS sink; removing inline styles risks framework/component regressions and needs browser validation. | Framework-supported style nonce/hash path lands, HTML/style injection sink appears, or CSP hardening cycle approved. |
| AGG-C19-13 | Medium / Medium | `apps/web/src/lib/smart-collections.ts:142-352`, `apps/web/src/app/[locale]/(public)/c/[slug]/page.tsx:17` | Public smart collections are currently DB-authored/no admin UI; cost-class design should ride the Collections product cycle. | Collections admin UI ships, broad public collection causes measured latency, or smart-collection materialization project approved. |
| AGG-C19-14 | Medium / Medium | `apps/web/src/lib/data.ts:1574-1738`, `apps/web/src/app/actions/public.ts:266-317` | Search indexing/FTS is a product/performance project; existing rate limits bound request count. | Search latency measured high, search corpus grows materially, or full-text/search-index cycle approved. |
| AGG-C19-15 | Low / High | `apps/web/src/app/[locale]/admin/db-actions.ts:45-120` | CSV export is admin-only and capped; streaming route is a low-priority heap optimization. | Near-cap exports cause memory pressure, or admin export route refactor cycle. |
| AGG-C19-16 | Medium / Medium | `apps/web/src/lib/data.ts:1402-1407`, `apps/web/src/app/[locale]/(public)/g/[key]/page.tsx:111-142` | Shared-group view count is documented best-effort approximate analytics, not audit/billing-grade. | Counter drift bug report, view-count refactor, or shared-group page/cache cycle. |
| AGG-C19-17 | Medium / Medium | `apps/web/src/app/[locale]/admin/db-actions.ts:547-584`, `apps/web/src/lib/rate-limit.ts:480` | Restore/rate-limit quiescence change crosses public actions and persistent limiter storage; no current restore incident. Correctness risk is bounded by documented restore maintenance behavior. | Restore-time DB write error observed, or next restore/rate-limit storage cycle. |
| AGG-C19-22 | Medium / High | `apps/web/src/components/image-manager.tsx:427-604` | Responsive admin image workbench is a larger UX redesign. | Mobile/tablet admin operation becomes priority, wrong-row action report, or admin redesign cycle. |
| AGG-C19-23 | Low-Medium / High | `apps/web/src/components/admin-nav.tsx:15-49` | IA redesign is broader than this cycle and should be paired with admin responsive work. | Admin nav becomes confusing in protected browser validation or admin IA cycle. |
| AGG-C19-24 | Low-Medium / High | `apps/web/src/app/[locale]/admin/(protected)/seo/seo-client.tsx:42-184` | Field-addressable SEO errors require action result shape/error-code design and i18n mapping; existing server validation blocks bad data. | SEO admin usability report or next form-validation/a11y cycle. |
| AGG-C19-26 | Medium / High | `apps/web/src/components/photo-viewer.tsx:747-1010` | Photo-page IA/default-open behavior is product/UX design work with hydration implications. | Visitor/client misses download/color controls, or photo-page IA redesign cycle. |
| AGG-C19-27 | Medium / High | `apps/web/src/db/schema.ts:10-39`, `apps/web/src/app/actions/topics.ts:287-372` | Natural-key migration to surrogate IDs is a schema/product migration with high blast radius; current rename fan-out is tested/transactional. | New slug-bearing store added, rename bug observed, or topic schema migration cycle. |
| AGG-C19-28 | High / High | `apps/web/src/app/actions/images.ts:129-620`, `apps/web/src/app/api/admin/lr/upload/route.ts:84-620` | Shared ingest-service extraction is a broad architecture refactor. Current cycle fixes a concrete LR route bug inside the duplicated surface. | Third upload-path drift lands, upload-flow-touching architecture cycle, or shared ingest service approved. |
| AGG-C19-29 | Medium / Medium-High | `apps/web/src/app/actions/images.ts:259-578`, `apps/web/src/lib/upload-tracker.ts:19-33` | Browser quota settlement wrapper is a refactor of a currently covered path; no active leak was found this cycle. | Upload quota leak observed, throwing cleanup introduced, or upload-flow refactor cycle. |
| AGG-C19-30 | Low-Medium / High | `apps/web/src/lib/rate-limit.ts:135-205` | Security/resource-control improvement depends on proxy/CDN/operator identity model; login has account limits and public routes have other bounds. | IPv6 abuse observed, CDN identity adopted, or rate-limit algorithm cycle. |
| AGG-C19-31 | Medium / High | `apps/web/src/components/register-service-worker.tsx:13-23`, `apps/web/src/__tests__/sw-template-contract.test.ts:1-16` | Production SW browser/offline testing requires production server/browser harness beyond current unit/source contracts. | PWA/offline regression, SW-template-touching cycle, or Playwright PWA project. |
| AGG-C19-32 | Medium / Medium | `apps/web/src/__tests__/check-api-auth.test.ts:127-132`, `apps/web/scripts/check-public-route-rate-limit.ts:986-998` | CLI temp-fixture harness work is test-infrastructure scope; scanner AST fixtures remain strong. | Gate discovery incident, new scanner extension support, or lint-gate harness cycle. |
| AGG-C19-33 | Low / High | `apps/web/e2e/nav-visual-check.spec.ts:40-86` | Visual baselines can be flaky/noisy; current DOM geometry checks still catch target/overlap regressions. | Visual regression escapes geometry checks, or visual baseline policy is approved. |
| AGG-C19-34 | Mixed up to High / High | `.context/plans/deferred-carry-forward.md:36-170`, `plan/plan-377-cycle19-deferred.md:18-166` | This is a meta-finding that the broad carry-forward backlog remains live. It is represented by the individual rows above and existing carry-forward register. | Individual exit criteria fire or the carry-forward register schedules/reclassifies aged rows per policy. |

## Age-Budget Check

New High rows deferred this cycle are architecture/performance/test-design items, not direct unpatched correctness bugs; direct correctness items `AGG-C19-01` and `AGG-C19-02` are scheduled. Security/availability deferrals cite the repo's documented single-instance and nginx-edge operator boundaries in `CLAUDE.md`. The new deferred register is added to the carry-forward lineage in the implementation plan.
