# Cycle 13/100 Implementation Plan — 2026-07-18

Start HEAD: `8bd8999f`
Source: `.context/reviews/_aggregate.md` and all Cycle 13 provenance reviews
Status: **IMPLEMENTED — all gates green; publication/deploy pending**

## Repository rules read before planning

- `CLAUDE.md` (all 780 lines), then `AGENTS.md`.
- `.context/plans/README.md`, the Cycle 11/12 plan/deferred pairs, the latest
  aggregate and all eleven Cycle 13 provenance reviews, plus the consolidated
  carry-forward register.
- Root/app README and current quality/deploy instructions.
- No `.cursorrules` or `CONTRIBUTING.md` exists in this checkout.

Implementation remains bound to GPG-signed gitmoji Conventional Commits,
fine-grained work items, pull/rebase before push, no verification bypasses,
every configured blocking gate, and one root `npm run deploy` only after the
published source and all gates are green.

## Finding disposition

Every Cycle 13 finding is scheduled below; none is deferred. The archive-domain
finding is a correctness defect and is not deferrable. The two Medium
performance/UX findings have bounded fixes in the same archive surface. Both
Low documentation/ledger findings are direct maintenance work.

## Work packages

- [x] **WP1 — one archive-year authority (ARCHIVE-C13-01, Medium/High):** add a
  client-safe/pure archive-year module that accepts canonical four-digit MySQL
  `DATETIME` years 1000..9999, rejects malformed/out-of-domain values, validates
  optional months, and represents year/December 9999 without constructing an
  impossible year-10000 exclusive bound.
- [x] **WP1 — route/data adoption:** make timeline query parsing, year layout,
  year metadata/page, and `getTimelineImages` use the shared contract. Preserve
  half-open capture-date ranges for ordinary years and omit the redundant upper
  predicate only at the maximum representable boundary.
- [x] **WP1 — availability authority (UX-C13-03, Medium/High):** after the
  timeline loads its authoritative year list, retain a requested selection only
  when `years.includes(requestedYear)`; otherwise render the newest available
  year. Preserve the valid explicit-year request's parallel photo query and
  discard a speculative unavailable-year result before the fallback query.
- [x] **WP1 — boundary proof:** add table-driven tests for malformed, 0000,
  below-domain, 1000, 9998, and 9999 values; cover maximum-year month/year
  bounds, shared route parsing, and unavailable-year fallback. Extend the
  guarded disposable MySQL convergence lane with a maximum-DATETIME fixture
  and a real equivalent archive query that never binds year 10000.
- [x] **WP2 — covering timeline-year query (PERF-C13-02, Medium/High):** replace
  the base `capture_date IS NOT NULL` predicate with the equivalent indexed
  `capture_year IS NOT NULL` predicate. Pin exact distinct-year/null behavior
  and extend disposable MySQL `EXPLAIN FORMAT=JSON` evidence to require the
  processed/year key parts and index-only access without brittle cost estimates.
- [x] **WP3 — truthful index documentation (DOC-C13-04, Low/High):** replace
  `data-timeline.ts`'s singular module-wide index claim with explicit mappings
  for On This Day, year discovery, and bounded archive-photo retrieval.
- [x] **WP4 — release ledger (DOC-C13-05, Low/High):** record Cycle 12's four
  GPG-good commits and remote equality through `8bd8999f`, retain the original
  deploy result as unknown, archive the completed Cycle 11/12 plan pairs, and
  advance the active index to Cycle 13 without inventing deployed-SHA evidence.
- [x] **WP5 — validation:** run every configured blocking gate against the
  whole repository: ESLint, API-auth lint, action-origin/mutation-barrier lint,
  public-route-rate-limit lint, app/script typecheck, production build, full
  Vitest, production dependency audit, and full Playwright because public
  browser-flow behavior changes. Run the disposable MySQL convergence command.
  Fix every error/warning at root cause or record a policy-compliant deferred
  warning; track `GATE_FIXES`.
- [ ] **WP6 — publication/deploy:** commit coherent work in fine-grained
  GPG-signed gitmoji Conventional Commits, `git pull --rebase`, push `master`,
  verify local/remote equality, then run root `npm run deploy` exactly once.

## Deferred finding register

No Cycle 13 finding is deferred. Existing unrelated open items remain governed
by `.context/plans/deferred-carry-forward.md`; none of their exit criteria is
satisfied merely by tightening archive-year parsing and covering-index use.

## Acceptance criteria

- Both archive route forms share one canonical 1000..9999 parser; invalid and
  noncanonical inputs never reach an archive query.
- Maximum year 9999 uses a valid MySQL predicate with no year-10000 literal;
  ordinary year/month queries retain half-open range semantics.
- An unavailable but domain-valid query year cannot become the selected year
  or generate a misleading year-review link.
- Distinct timeline-year discovery filters the generated year and is covered by
  `(processed, capture_year)` in disposable MySQL evidence.
- Unit/source/browser/live-MySQL coverage proves both domain endpoints, nulls,
  duplicate years, maximum datetime, fallback selection, and ordinary behavior.
- Timeline documentation names the three intended indexes accurately.
- Cycle 11/12 source publication facts are archived truthfully; unknown deploy
  evidence remains unknown.
- Every required gate passes, commits verify as GPG-signed, remote master equals
  local HEAD, and the required per-cycle deploy succeeds.

## Progress

- [x] Eleven-perspective review coverage complete.
- [x] Five unique findings aggregated and deduplicated.
- [x] Every finding scheduled; no deferral.
- [x] Implementation complete.
- [x] Focused and full gates green.
- [ ] Signed commits pushed.
- [ ] Per-cycle deploy complete.

## Validation evidence

- Focused archive/schema tests: 44 passed; full app/script typecheck passed.
- Disposable MySQL 8.4 `gallery_cycle13_test` passed the real 0031-to-current
  upgrade, maximum-year archive query with no year-10000 bound, exact generated
  year/month/day behavior, distinct-year semantics, index-only processed/year
  EXPLAIN, definition recovery, and second-run idempotence. The container was
  removed afterward.
- ESLint, API-auth lint, action-origin/mutation-barrier lint, and public-route-
  rate-limit lint passed.
- Full typecheck passed for application, tests, scripts, and all 11 JavaScript
  operational scripts.
- Production Next.js 16.2.10 build passed.
- Full Vitest: 364 files passed, 2 skipped; 3,478 tests passed and 4 expected
  CLIP tests skipped.
- Production dependency audit passed with zero vulnerabilities.
- Full Playwright: 66 passed, 2 intentional capability-marker skips, including
  maximum-year HTTP behavior and unavailable-year fallback. The first attempt
  correctly failed because no disposable MySQL endpoint was running; an
  isolated MySQL 8.4 container was started from the existing E2E env contract,
  the exact gate passed, and the container was removed.
- `git diff --check` passed.
- `GATE_FIXES`: 3 error-level issues in new validation work were fixed at root
  cause: an incorrectly escaped source-contract literal, an out-of-range
  TIMESTAMP fixture value (capture DATETIME remains 9999), and nested-string
  handling for MySQL's `EXPLAIN FORMAT=JSON` payload. No actionable warning
  remains.
