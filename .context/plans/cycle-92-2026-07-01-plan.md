# Cycle 92/100 Implementation Plan

Start HEAD: `508d35572563705008693da2dbff3e5d85442cdd`.
Current local HEAD after review-lane artifact commit: `a2a9e400657e128a3b6752933483842533dd8925`.
Review aggregate: `.context/reviews/cycle-92-2026-07-01/_aggregate.md`.

## Goal

Close the safe, narrow Cycle 92 findings that can be handled with source-contracts, docs, and copy without broad refactors, schema migrations, new dependencies, or production-topology changes. Preserve every other review finding in the deferred ledger with severity/confidence and exit criteria.

## Scheduled Fixes

1. `C92-03`: add an explicit Node runtime pin to `apps/web/src/app/api/health/route.ts` and lock it with a focused test in `apps/web/src/__tests__/health-route.test.ts`.
2. `C92-04`: record current-cycle release/deploy evidence in this plan and update `.context/plans/README.md` so later cycles can identify the Cycle 92 terminal state. The implementation step must include the pre-existing review commit `a2a9e400657e128a3b6752933483842533dd8925` in the pushed commit list.
3. `C92-10`: correct the smart-collection docs in `CLAUDE.md` so the criteria examples match `apps/web/src/lib/smart-collections.ts`, which currently allowlists `iso`, `focal_length`, `f_number`, `exposure_time`, `camera_model`, `lens_model`, `capture_date`, `topic`, and `tag`, but not `color_pipeline_decision`.
4. `C92-13`: adjust SEO/share copy in `apps/web/messages/en.json` and `apps/web/messages/ko.json` so share links are described as unlisted/bearer-style links, not private authenticated links.
5. `C92-14`: adjust SEO locale copy in both locale message files to state that the configured OG locale is only a fallback for unsupported/unknown route locales; supported route locales still win.

## Deferred This Cycle

All non-scheduled confirmed findings, likely issues, and manual-validation risks are recorded in `.context/plans/cycle-92-2026-07-01-deferred.md`. No review finding is intentionally dropped.

## Regression Coverage

- `health-route.test.ts` must fail if the route loses `export const runtime = 'nodejs'`.
- Existing health behavior tests must continue to pass for maintenance, DB-success, DB-failure, and liveness-only modes.
- `i18n-key-parity.test.ts` must still pass after message copy changes; keys remain unchanged.
- Documentation/copy changes must not add unsupported smart-collection criteria or imply authenticated share-link privacy.

## Validation

Run focused tests/checks first:

- `npm test --workspace=apps/web -- --run src/__tests__/health-route.test.ts src/__tests__/i18n-key-parity.test.ts`

Then run required gates in exact order:

1. `npm run lint --workspace=apps/web`
2. `npm run lint:api-auth --workspace=apps/web`
3. `npm run lint:action-origin --workspace=apps/web`
4. `npm run lint:public-route-rate-limit --workspace=apps/web`
5. `npm run typecheck --workspace=apps/web`
6. `BASE_URL=https://gallery.atik.kr npm run build --workspace=apps/web`
7. `npm test --workspace=apps/web`

Also run `git diff --cached --check` before commit, then create a signed Conventional Commit with gitmoji and Lore trailers, `git pull --rebase`, push, deploy with `npm run deploy`, and smoke:

- `curl -fsSIL https://gallery.atik.kr`
- `curl -fsS https://gallery.atik.kr/api/health`

## Progress

- [x] Review fan-out completed.
- [x] Aggregate review written.
- [x] Implementation plan created.
- [x] Implement scheduled fixes.
- [x] Run focused tests.
- [x] Run required gates.
- [ ] Commit, pull --rebase, push.
- [ ] Deploy with `npm run deploy`.
- [ ] Smoke production endpoints.

## Focused Evidence

- Starting expected deployed HEAD: `508d35572563705008693da2dbff3e5d85442cdd`.
- Local review-lane artifact commit before Prompt 2: `a2a9e400657e128a3b6752933483842533dd8925` (`docs(review): 📝 surface cycle 92 test risks`), signed and not reverted.
- Focused test: `npm test --workspace=apps/web -- --run src/__tests__/health-route.test.ts src/__tests__/i18n-key-parity.test.ts` - pass, 2 files passed, 7 tests passed.

## Gate Evidence

- `npm run lint --workspace=apps/web` - pass.
- `npm run lint:api-auth --workspace=apps/web` - pass: both admin API routes OK.
- `npm run lint:action-origin --workspace=apps/web` - pass: all mutating server actions enforce same-origin provenance.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - pass: all public route handlers covered or explicitly exempt.
- `npm run typecheck --workspace=apps/web` - pass: route types generated, app/scripts typechecks clean, 8 JavaScript script files checked.
- `BASE_URL=https://gallery.atik.kr npm run build --workspace=apps/web` - pass. Local sitemap generation logged the expected unavailable local MySQL fallback and exited successfully.
- `npm test --workspace=apps/web` - pass: 302 files passed, 2 skipped; 2841 tests passed, 4 skipped.

## Gate Fix Iterations

- `C92-03`: added `export const runtime = 'nodejs'` to `/api/health` and a focused assertion in `health-route.test.ts`.
- `C92-10`: corrected `CLAUDE.md` smart-collection criteria examples to remove unsupported `color_pipeline_decision` wording.
- `C92-13` / `C92-14`: corrected en/ko SEO copy for unlisted share-link and OG-locale fallback semantics.
