# Run-10 Cycle 18/100 Implementation Plan

Status: IMPLEMENTED + LOCAL GATES GREEN; COMMIT/PUSH/DEPLOY FINALIZATION IN PROGRESS
Aggregate: `.context/reviews/_aggregate.md`
Date: 2026-07-08 KST
Start HEAD: `a186340570351af0cab5347de21a5bb1b50c327a`

## Scope

This plan schedules the Cycle 18 findings that are correctness/provenance-critical, bounded, and safe to fix in this cycle. Every aggregate finding is either scheduled here or recorded in `cycle-18-2026-07-08-deferred.md`.

Repo rules read before scheduling/deferring: `CLAUDE.md`, `AGENTS.md`, `.context/plans/README.md`, `.context/plans/cycle-17-2026-07-08-plan.md`, `.context/plans/cycle-17-2026-07-08-deferred.md`, `.context/plans/deferred-carry-forward.md`, root/app README files, and `docs/superpowers/*`. No `.context/README.md`, `.cursorrules`, or `CONTRIBUTING.md` exists in this checkout.

## Scheduled Work Packages

### WP1 - Reconcile cycle ledgers and carry-forward age tracking

Findings: `AGG-C18-01`, `AGG-C18-02`

Files:

- `.context/plans/README.md`
- `.context/plans/cycle-17-2026-07-08-plan.md`
- `.context/plans/cycle-17-2026-07-08-deferred.md`
- `.context/plans/deferred-carry-forward.md`

Plan:

1. Mark Cycle 17 terminal with final commit/push/deploy evidence or an explicit deploy gap/supersession.
2. Update the plan index so Cycle 18 is the active current-cycle pair.
3. Refresh the carry-forward register for the current cycle: update the checkpoint label, remove or annotate closed rows where known, and add Cycle 17/Cycle 18 active deferred rows sufficiently for the 8-cycle High and 16-cycle Medium budget checks to remain mechanically auditable.

Acceptance:

- Future agents no longer see Cycle 17 as the current active/pending plan.
- Carry-forward age text no longer claims the current check is `run-10 c4`.
- New deferred Cycle 18 items are represented in the carry-forward lineage.

### WP2 - Strengthen token-use semantics and behavior proof

Findings: `AGG-C18-06`, `AGG-C18-10`

Files:

- `apps/web/src/lib/api-auth.ts`
- `apps/web/src/app/api/admin/lr/upload/route.ts`
- `apps/web/src/__tests__/cycle-17-source-contracts.test.ts`
- focused behavior tests under `apps/web/src/__tests__/`

Plan:

1. Stop marking scoped PAT `last_used_at` before route-specific admission gates.
2. Pass verified token context through the admin-auth wrapper so the Lightroom upload handler can mark use only after restore-maintenance and cheap admission gates pass.
3. Add behavior-level tests for the changed semantics and at least one previously source-shape-only Cycle 17 high-risk branch where a practical harness exists.
4. Keep source-contract tests only for invariants that cannot be cheaply executed.

Acceptance:

- A valid Lightroom PAT request blocked by restore maintenance does not update `last_used_at`.
- An admitted Lightroom PAT request still updates `last_used_at`.
- At least one Cycle 17 source-shape-only guard is backed by behavior-level coverage.

### WP3 - Repair CLIP/backfill and pipeline documentation contracts

Findings: `AGG-C18-11`, `AGG-C18-12`, `AGG-C18-13`, `AGG-C18-14`

Files:

- `apps/web/src/lib/process-image.ts`
- `apps/web/src/lib/gallery-config-shared.ts`
- `apps/web/src/app/actions/embeddings.ts`
- `apps/web/scripts/backfill-clip-embeddings.ts`
- `CLAUDE.md`
- `apps/web/README.md`
- focused tests/source contracts under `apps/web/src/__tests__/`

Plan:

1. Remove or update stale duplicated pipeline-version history in `process-image.ts` so `gallery-config-shared.ts` remains authoritative for v7.
2. Delete/unexport the unused CLIP server action or port the sidecar keyset/attempt-budget behavior into it; choose the smallest change that prevents future wiring of the skipped-prefix starvation path.
3. Update CLIP runbooks and sidecar header to say `SEMANTIC_SCAN_LIMIT` caps embedding attempts, not every candidate row scanned.
4. Replace the stale future-tense ONNX/concurrency comment with the current fixed-concurrency contract, unless a bounded env knob is added with tests.

Acceptance:

- Encoder maintainers see one current pipeline-version source of truth.
- No exported app action retains the old skipped-prefix CLIP starvation behavior.
- CLIP docs/script comments match the current sidecar budget and concurrency behavior.

### WP4 - Improve first-viewport public hierarchy and README truthfulness

Findings: `AGG-C18-15`, `AGG-C18-19`, `AGG-C18-20`

Files:

- `apps/web/src/components/home-client.tsx`
- `apps/web/src/components/tag-filter.tsx`
- `README.md`
- related tests/source contracts where applicable

Plan:

1. On mobile, keep the photo grid closer to the first viewport by moving the full tag list behind a compact filter disclosure/sheet or equivalent accessible collapsed control.
2. Preserve 44 px touch targets and keyboard access when filters are expanded.
3. Reword the README "Live Demo" link as an example deployment or add a source-default caveat.
4. Soften "Photographer-grade color management" to avoid implying public HDR/reference delivery beyond the documented browser/codec limits.

Acceptance:

- Mobile visitors no longer see all tag chips before the first photo by default.
- README copy distinguishes example deployment state from fresh-install defaults.
- README color wording aligns with the existing HDR/browser/codec caveats.

### WP5 - Gate verification, commit, push, deploy

Findings: cycle quality/deploy requirement.

Plan:

1. Run all configured gates against the whole repo:
   - `npm run lint --workspace=apps/web`
   - `npm run lint:api-auth --workspace=apps/web`
   - `npm run lint:action-origin --workspace=apps/web`
   - `npm run lint:public-route-rate-limit --workspace=apps/web`
   - `npm run typecheck --workspace=apps/web`
   - `npm run build --workspace=apps/web`
   - `npm test --workspace=apps/web`
   - `npm run test:e2e --workspace=apps/web` when browser-flow coverage is required by touched surfaces.
2. Fix blocking gate errors before commit/push.
3. Commit and push in signed, fine-grained Conventional Commits with gitmoji after `git pull --rebase`.
4. Run `npm run deploy` once after pushed gates are green because `DEPLOY_MODE=per-cycle`.

## Progress

- [x] WP1 reconcile cycle ledgers and carry-forward age tracking
- [x] WP2 strengthen token-use semantics and behavior proof
- [x] WP3 repair CLIP/backfill and pipeline documentation contracts
- [x] WP4 improve first-viewport public hierarchy and README truthfulness
- [x] WP5 gate verification
- [ ] WP5 signed commit/push and per-cycle deploy finalization

## Gate Evidence

- Targeted regression slice: `npm test --workspace=apps/web -- src/__tests__/api-auth-response-headers.test.ts src/__tests__/lr-upload-route-behavior.test.ts src/__tests__/embeddings-action-behavior.test.ts src/__tests__/backfill-clip-embeddings-reembed.test.ts src/__tests__/cycle-17-source-contracts.test.ts` -> 5 files passed, 40 tests passed.
- `npm run lint --workspace=apps/web` -> passed.
- `npm run lint:api-auth --workspace=apps/web` -> passed.
- `npm run lint:action-origin --workspace=apps/web` -> passed.
- `npm run lint:public-route-rate-limit --workspace=apps/web` -> passed.
- `npm run typecheck --workspace=apps/web` -> initially failed on missing freshly generated `.next/types/cache-life.d.ts` and `.next/types/validator.ts`; passed after adding deterministic typegen wrapper.
- `npm run build --workspace=apps/web` -> passed after the typegen wrapper fix; local no-DB sitemap fallback log observed before the e2e DB was started.
- `npm test --workspace=apps/web` -> 354 files passed, 2 skipped; 3291 tests passed, 4 skipped.
- `npm run test:e2e --workspace=apps/web` -> 45 passed, 2 skipped, using the repo-local disposable `gallerykit_e2e` database on `127.0.0.1:3306`.

## Gate Fixes

- Fixed 1 error-level gate issue: `typecheck:app` now runs `next typegen`, waits for required generated type files, and then runs `tsc`, eliminating the intermittent TS6053 failures seen by both `npm run typecheck --workspace=apps/web` and `npm run build --workspace=apps/web`.
- No suppressions were added.
