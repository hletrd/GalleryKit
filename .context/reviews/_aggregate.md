# Aggregate Deep Review — Cycle 6/100 (review-plan-fix, internally run-9 cycle-3)

**Date:** 2026-06-13
**Repo:** /Users/hletrd/flash-shared/gallery (GalleryKit — Next.js 16 / React 19 / TS6 photo gallery)
**Cycle:** orchestrator "cycle 6/100". Working tree CLEAN at start; HEAD `4c3d5924` in sync with origin/master.
**Agents that returned (11/11):** code-reviewer, security-reviewer, perf-reviewer, critic, verifier, test-engineer, tracer, architect, debugger, document-specialist, designer. **One write-failure (test-engineer) recovered — see AGENT FAILURES.**

**Gate baseline measured live this cycle (orchestrator ran every non-test gate; verifier + several agents re-ran independently):**
- `npm run lint` → **exit 0** (clean)
- `npm run typecheck` (app + scripts) → **exit 0**
- `lint:api-auth` / `lint:action-origin` / `lint:public-route-rate-limit` → **all exit 0**
- `npx vitest run` → **exit 0, 218 files / 2080 tests passed, 0 failed** (cold, ~268 s; the documented libheif cold-flake did NOT reproduce). Count grew 2068→2080 (+12), matching the cycle-5 test additions (sidecar backfill cleanup, i18n parity, queue wiring pin, select-pattern fixtures).
- i18n leaf-key parity: now a **committed gate** (`i18n-key-parity.test.ts`, landed cycle-5 `a062e81b`) — AGG-C5-T1 closed.

> The prior cycle-5 aggregate (run-9 cycle-2) has been superseded by this file. Per-agent files were overwritten by this cycle's fan-out. `security-reviewer.md` and `test-engineer.md` were persisted by the orchestrator (security runs read-only; test-engineer's own Write did not land — its investigation was captured + independently re-verified).

---

## TOP-LEVEL: the cycle-5 batch (plan-339 / AGG-C5-01..T2) landed clean — every fix RE-VERIFIED CLOSED at HEAD `4c3d5924`, not trusted on the plan's word

All 5 scheduled cycle-5 fixes (plan-339, commits `07a838d6`..`4c3d5924`) are CONFIRMED-CLOSED with present+correct code AND non-vacuous tests, by independent line-by-line verification across verifier (6/6 criteria), critic (proved the sidecar test RED-on-revert then restored), code-reviewer, test-engineer, architect, designer, tracer, document-specialist (very high cross-agent agreement).

| Prior finding | Status at HEAD `4c3d5924` | Closing commit | Re-verified by |
|---|---|---|---|
| AGG-C5-01 sidecar `flushBatch` orphan-cleanup ZERO test coverage | **CLOSED, PROVEN NON-VACUOUS** — `cleanupDeletedMidReencodeVariants`+`collectDeletedMidReencodeFiles` extracted as module-level test seams; new 149-LOC test drives `affectedRows:0`, asserts `[]`-scan cleanup for all 3 formats + tally; critic neutered the guard → 2 assertions RED → restored | `fad9c279` | critic (RED-on-revert), code-reviewer, verifier, architect |
| AGG-C5-02 `<select>` touch-target `max-` false-positive | **CLOSED** — `(?<!max-)` lookbehind on all 4 `<select>` patterns + `max-h-10`/`max-w-9`/`cn()` negative fixtures; ran committed regex in Node (no-flag on `max-h-10`, still-flag on `h-8`) | `07a838d6` | verifier (6/6 in Node), critic, designer |
| AGG-C5-03 three public inline `<Link>` < 44px | **CLOSED** — `topic-empty-state.tsx:18`, `home-client.tsx:434`, `timeline/page.tsx:154` all carry `inline-flex items-center min-h-11 px-2`; flex-parent compat checked | `e7d19f4b` | designer, critic, verifier |
| AGG-C5-T1 no committed en/ko leaf-key parity test | **CLOSED** — `i18n-key-parity.test.ts` flattens both message objects, asserts SET equality KEYS-only (honors DOC-R5C3-07); would catch a dropped ko key | `a062e81b` | verifier, critic, test-engineer |
| AGG-C5-T2 queue `[]`-dir-scan cleanup no call-path pin | **CLOSED** — `image-queue-delete-race-cleanup-wiring.test.ts` source-shape-pins 3-arg `[]` present / 2-arg absent at `image-queue.ts:384-386` | `56bddff5` | verifier (Node regex), test-engineer |
| AGG-C4-09 image-manager touch-target budget stale 6 | **CLOSED** — recount to 1 (real scanner count); enumerating comment corrected | `2637e5f2` | verifier, test-engineer |

**Consequence for PROMPT 2:** plan-339/340 already reflect cycle-5 as DONE/deferred. No plan-table hygiene needed beyond marking the cycle complete. Schedule only the genuinely-NEW findings below.

---

## Cross-agent convergence map — GENUINELY OPEN / NEW this cycle (highest signal first; all verified against HEAD by the orchestrator before counting)

| Agg ID | Finding | Severity (max) | Conf | Agents | Status |
|---|---|---|---|---|---|
| **AGG-C6-01** | **`stripGpsFromWebpBuffer` RIFF field-order inversion — the lossless WebP GPS-scrub path is DEAD CODE.** `apps/web/src/lib/gps-exif-strip.ts:564-565` reads `chunkSize = buf.readUInt32LE(offset)` and `chunkTag = buf.toString('ascii', offset+4, offset+8)`, but the WebP RIFF chunk layout (verified against the authoritative Google WebP RIFF-container spec: FourCC = bytes 0-3, size = bytes 4-7) puts the **tag first, size second** — they are swapped. On the FIRST sub-chunk of EVERY real WebP, the 4-byte FourCC (`VP8X` = 0x58385056 ≈ 1.48 GB) is misread as `chunkSize`, so `dataEnd = dataStart + chunkSize > buf.length` is immediately true and the function returns `null` before reaching any EXIF/XMP comparison. The line-580 XMP retag (`buf.write('JUNK', offset+4, …)`) also targets the size field instead of the tag, but is unreachable due to the same bug. **Reachability CONFIRMED:** `process-image.ts:1536-1537` dispatches `.webp` originals to this function when `strip_gps_on_upload=true`; the `null` return falls through to the Tier-2 lossy Sharp re-encode (`:1564-1567`, q95 for lossy VP8 / lossless only if a `VP8L` substring is present). **Privacy impact: NONE** (the fallback re-encode DOES strip GPS — security-reviewer explicitly assessed this out of scope). **Quality/correctness impact: REAL** — every `.webp` original with GPS-strip enabled is needlessly lossy-recompressed (generation loss on the paid-download deliverable) and logs a warning on every WebP upload, directly violating the module's stated contract (header comment: "the pixel stream is NEVER decoded, so the original stays bit-identical except for the neutralized GPS regions"). This is the COR-R4C8-01 class of defect the module exists to prevent, alive in the WebP branch. **Fix:** swap the two reads — `chunkTag = buf.toString('ascii', offset, offset+4)`, `chunkSize = buf.readUInt32LE(offset+4)` — and change the write to `buf.write('JUNK', offset, 4, 'ascii')`. Land with the AGG-C6-02 proven-RED test. | **MED** (correctness + silent quality degradation of the paid-download original; NOT a privacy/security leak — fallback re-encode still strips GPS) | **High** (field-order verified vs authoritative WebP spec; reachability traced through the dispatcher; the only real source bug this cycle) | **1** (debugger DBG-C6-01) | NEW — the one real code bug |
| **AGG-C6-02** | **The WebP GPS-strip test is VACUOUS for the lossless contract — it is the test gap that let AGG-C6-01 ship green.** `apps/web/src/__tests__/strip-gps-from-original.test.ts:116-126` (`it('removes GPS from a WebP original via the RIFF scrub (pixels byte-identical)')`) calls the top-level dispatcher `stripGpsFromOriginal`, never `stripGpsFromWebpBuffer` directly; its fixture is lossy (`pipeline.webp({quality:95})`); and it asserts equality of **decoded raw pixels** (`sharp(file).raw().toBuffer()`), NOT file bytes. A q95→q95 WebP re-encode of an already-q95 decode yields the identical decode, so the assertion passes through the Tier-2 fallback whether or not the lossless path ran. The `describe('gps-exif-strip pure scrubbers')` block (`:175`) tests `stripGpsFromJpegBuffer` directly but has NO `stripGpsFromWebpBuffer` entry, so the bug has zero direct unit coverage. Same root cause as AGG-C6-01 → 2-agent agreement. **Fix:** add a direct `stripGpsFromWebpBuffer(input)` pure-scrubber test asserting `result.stripped===true`, non-null, and the VP8/VP8L pixel-chunk bytes byte-identical (lossless), plus a GPS-free `{stripped:false}`/input-reference case and a non-WebP-bytes `null` case (mirror the JPEG pure-scrubber tests). Prove RED against the current buggy source, GREEN after the AGG-C6-01 fix. | **MED** (test-depth gap on a now-confirmed real bug; the WebP lossless contract is invisible to the suite) | **High** (vacuity mechanism traced line-by-line; the test passes through the fallback) | **2** (test-engineer TE-C6-1 + debugger DBG-C6-01b — independent, same root) | NEW — highest cross-agent signal; bundle with AGG-C6-01 |
| **AGG-C6-03** | **Two MORE public back-nav `<Link>`s render ~20 px — the adjacent siblings AGG-C5-03 missed.** (a) `apps/web/src/app/[locale]/(public)/s/[key]/page.tsx:105` `"text-sm text-muted-foreground hover:text-primary transition-colors flex items-center gap-1"` (~20 px line-box, the SOLE escape from the shared single-photo view); (b) `apps/web/src/app/[locale]/(public)/year/[year]/page.tsx:109` `"text-sm … inline-flex items-center gap-1"` (~20 px, the return path from a public year-timeline page). VERIFIED: neither className carries an `h-*`/`min-h-*`/`py-*` token, so each renders at its line-box height — below the repo's documented 44 px floor (WCAG 2.5.5 AAA) and the 24 px floor (WCAG 2.5.8 AA). Both sit in the touch-target audit's DELIBERATE bare-link gap (the `<Link>`/`<a>` patterns at `:440-466` only fire on an EXPLICIT sub-44 sizing token; `KNOWN_VIOLATIONS` correctly lists neither). Exact same shape AGG-C5-03 fixed three of — this is the recurring "fix one sibling, miss the next" theme, now on two more bare back-nav links in public page files. **Fix:** add `min-h-11` to both classNames (keep the existing flex/underline affordance), matching the cycle-5 `inline-flex items-center min-h-11 px-2` pattern; optionally add both to a positive-assertion pin (the durable bare-link audit rule remains DEFERRED per plan-340). | **MED** (WCAG 2.5.5 AAA / repo self-imposed 44 px; also fails 2.5.8 AA; PUBLIC sole/primary back-nav actions, mobile-relevant) | **High** (className verbatim + line-box math + audit-gap confirmed) | **1** (designer DES-C6-1) | NEW — adjacent gap of AGG-C5-03 |
| **AGG-C6-04** | **The `<Link>`/`<a>` touch-target patterns carry the SAME `max-` false-positive that `40a65aef` (Button) and `07a838d6` (select) already closed.** `apps/web/src/__tests__/touch-target-audit.test.ts:440,444,458,462` — the four `<Link>`/`<a>` bare-`(?:h-8|h-9|h-10)` FORBIDDEN patterns lack the `(?<!max-)` lookbehind every other tag-class now carries. VERIFIED (orchestrator ran the exact committed regex in Node): `<Link className="max-h-10">` and `<a className="max-h-9">` both FLAG as "renders below the 44 px floor" — FALSE (`max-h` is a ceiling, never the tap target). A complete audit confirms these four are the ONLY remaining bare-h/w patterns missing the lookbehind. Latent (zero `max-h-{8,9,10}` on a Link/a in the repo today — the identical profile as AGG-C5-02, which the team chose to schedule) but it is a BLOCKING gate that would mis-fire with a lying message the moment a legitimate `<a className="max-h-10">` lands, training a dev to silence it with a bogus `min-h-11`. The textbook recurring theme: Button → select → **now a/Link**. **Fix:** add `(?<!max-)` to the bare-token portion of all four patterns (mirror the Button/select fix exactly) + add `<Link>`/`<a>` `max-h`/`max-w` negative fixtures to the does-not-flag self-check block. | **LOW** (blocking regression-gate that mis-fires on valid code + erodes trust; test-file only, latent) | **High** (empirical — ran the committed regex) | **2** (critic NF-1 + debugger noted the family pattern) | NEW — adjacent gap of AGG-C5-02 |

### Lower-confidence / doc / record-only tail (LOW, schedule cheaply or DEFER)

| Agg ID | Finding | Severity | Disposition |
|---|---|---|---|
| **AGG-C6-05** | **CLAUDE.md does not document the `<select>` `max-` lookbehind** added in cycle-5 (`07a838d6`). `CLAUDE.md:512` describes the native-`<select>` touch-target patterns as catching `h-8/h-9/h-10` but omits that the branch now carries `(?<!max-)` (same as the Button fix it documents). Docs are incomplete, not wrong about live behavior — a maintainer reading CLAUDE.md would wrongly believe `<select className="max-h-10">` flags. (doc-specialist, LOW.) If AGG-C6-04 lands, the doc update should cover BOTH the `<select>` and the new `<Link>`/`<a>` lookbehinds in one edit. | LOW (doc completeness) | SCHEDULE-cheap — fold into the AGG-C6-04 commit's doc touch, or DEFER if AGG-C6-04 deferred. |
| **AGG-C6-T1** | **The AVIF/ISOBMFF GPS-strip test shares the WebP test's dispatcher-level shape but is LESS vacuous** (`strip-gps-from-original.test.ts:104-114`). The AVIF Tier-2 fallback re-encodes at q90 (lossy), which WOULD perturb decoded pixels, so a silent ISOBMFF-lossless break is more likely to surface than WebP's. Still weaker than a direct `stripGpsFromIsobmffBuffer` pure-scrubber test. (test-engineer TE-C6-2, LOW.) Recommendation: add a parallel direct ISOBMFF pure-scrubber test when fixing AGG-C6-02. | LOW (test depth; AVIF path not known-broken) | SCHEDULE-cheap (bundle with AGG-C6-02) OR DEFER. |

### Architecture / record-only (re-confirmed UNCHANGED; all prior-deferred)

| Agg ID | Finding | Severity | Disposition |
|---|---|---|---|
| **AGG-C6-R1** | Color-pipeline writer duplication still CONVERGING (architect re-diffed at source): the two backfill paths (`admin-backfill-runner.ts` / `scripts/backfill-color-pipeline.ts`) remain BYTE-EQUIVALENT on the 10-column UPDATE set, the `[]`-dir-scan cleanup contract, and detection-failure semantics. The cycle-5 sidecar test added two narrow module-level test seams (`cleanupDeletedMidReencodeVariants`, `collectDeletedMidReencodeFiles`) consumed ONLY by the new test — lowest-coupling option for a script with no library API, and WI-09 will absorb them for free. No 4th color-column writer introduced. WI-09 `applyColorPipelineResult()` consolidation deferral REMAINS JUSTIFIED. | MED (maintainability) | DEFER (WI-09; unchanged). |
| **AGG-C6-R2** | `lib/api-auth.ts:1` imports `isAdmin` from `@/app/actions/auth` — the SOLE `lib`→`app` layering inversion (architect; authoritative re-scan still finds exactly one, no cycle). UNCHANGED. | LOW (arch) | DEFER (plan-336/338, unchanged). |
| **AGG-C6-R3** | `COLOR_IMPACTING_KEYS` hand-maintained not derived from `GalleryConfig` — **9 keys** (`settings-hash.ts:37-49`; CLAUDE.md:263 cite verified CORRECT at 9: 5 color + 3 quality + `image_sizes`). NOTE: verifier miscounted "10" — orchestrator reconciled against the literal array, it is 9; the doc is accurate, no finding. 14 `@/db`-importing libs server-only by docstring, only `caption-generator.ts` carries `import 'server-only'` (architect). UNCHANGED. | LOW (arch hardening) | DEFER (unchanged). |
| **AGG-C6-R4** | `@/lib/storage` (390 LOC) dead seam — only its own index + a test consume it, honestly self-documented unwired per CLAUDE.md (architect). UNCHANGED. | LOW | RECORD/DEFER. |
| **AGG-C6-R5** | Perf record-only (perf-reviewer, all byte-identical at HEAD): SW metadata lost-update (PERF-L1), bootstrap `notInArray` over ≤1000 IDs (PERF-L2), decode-per-format ~18/image (WI-14 tradeoff), Atom feed filesort (bounded), timeline non-sargable YEAR()/MONTH() (bounded by LIMIT), single-pool/10 + single-writer topology — all DOCUMENTED-INTENTIONAL, NOT defects. | LOW | RECORD/DEFER — no live defect. |
| **AGG-C6-R6** | Designer prior-deferred, RE-CONFIRMED OPEN (not re-escalated): **DES-C5-2** nav theme/locale/expand `<button>`s + title/topic `<Link>`s have no `focus-visible:ring` (`nav-client.tsx:85,93,122,155,168`) — UA-default outline still applies so NOT a hard 2.4.7 failure. **DES-C5-3** color-pip `text-white/50` gamut suffix = 5.15:1 (passes 4.5:1, thinnest margin) + faint histogram dotted-underline (`histogram.tsx:691` ~2:1 affordance cue). **DES-C5-4** `photo-viewer.tsx:816` topic Badge renders raw slug. | LOW | DEFER (plan-336/340, unchanged) — fix in a UI-polish pass. |
| **AGG-C6-R7** | Prior-deferred, RE-CONFIRMED OPEN: SW image-cache metadata lost-update (whole-doc overwrite, no CAS; best-effort cache by design — debugger BUG-1 prior, perf-reviewer PERF-L1). Real-encode AVIF/WebP tests + libheif cold-flake share `public/uploads` (no per-test isolation; AGG-C4-T2). One dead-code note (no impact): `readNullTerminatedAscii` in `gain-map-detection.ts:87` has an unreachable `if (p > limit) return ''` guard (the `while (p < limit)` guarantees `p <= limit` on exit) — record-only, harmless. | LOW | DEFER / RECORD. |

### Cosmetic record-only (no action)

- **TRCR-C6-01** (tracer): under `BACKFILL_CONCURRENCY ≥ 2`, the sidecar's in-flight `processed` counter can be briefly inflated by up to the batch size before `flushBatch` applies its `processed--` correction. **Final summary is correct.** Log-accuracy only, no orphan, no action.

---

## VERIFIED-CLEAN (explicitly stress-tested this cycle, NO action)

- **All 6 GATES green** (lint, typecheck app+scripts, 3 security lint gates, full vitest 2080/2080 on a COLD run — libheif cold-flake did not reproduce). i18n leaf-key parity is now a COMMITTED gate (AGG-C5-T1 closed).
- **All 5 cycle-5 fixes RE-VERIFIED BEHAVING (not trusted):** see the top table. Notably the `<select>` `(?<!max-)` lookbehind was run in Node by the verifier (6/6); the sidecar backfill test was proven RED-on-guard-removal by the critic then restored; the i18n parity test confirmed KEYS-only; the three `<Link>` fixes confirmed carrying `min-h-11`.
- **Security (security-reviewer + tracer, hand-verified at current line numbers):** NO live-exploitable vuln across the full OWASP Top 10. The 7-commit delta `1dde9b1e..4c3d5924` is test + a11y className + a testability refactor — zero security surface. The one real bug (AGG-C6-01 WebP RIFF) is explicitly NOT a privacy regression (the fallback re-encode still strips GPS). A01 middleware guard + central `withAdminAuth` CSRF + `requireSameOriginAdmin` on every mutating action + base-56 share keys (no IDOR) + user-scoped PAT revoke; A02 Argon2id (65536/3/4) + timing-safe HMAC sessions + env-only `SESSION_SECRET` in prod; A03 zero `sql.raw`, Drizzle params + LIKE-escape + argv-array mysqldump + smart-collections allowlist + path containment/symlink-reject; A04 download single-use CAS open-before-claim; A08 Stripe signature-first + idempotency `insertId>0`; A10 OG own-origin + 10s + 1 MB; all 8 `dangerouslySetInnerHTML` JSON-LD via `safeJsonLd`; OG text via `sanitizeForOg`; no open redirect / ReDoS / prototype pollution / insecure deserialization / hardcoded secrets. Smart-collections `JSON.parse` reads only explicit allowlisted keys + rebuilds fresh objects (no `__proto__` walk). `npm audit` unchanged (downgrade-only fixes — do NOT take).
- **Boundary/arithmetic (debugger):** every OTHER ISOBMFF/ICC/GPS walker re-traced and bounds-correct — the WebP field-order inversion (AGG-C6-01) is the SOLE walker defect; it fails CLOSED (returns null → safe fallback). All JPEG/TIFF/ISOBMFF GPS-strip paths, the NCLX ISOBMFF walker, ICC extractor, ICC chromaticity, gain-map detection re-confirmed bounds-checked + try/catch-wrapped (no OOB/÷0/NaN-escape/infinite-loop). `useDisplayCapability` value-memoized → no React #185 loop; histogram AbortController + StrictMode worker lifecycle correct; view-count flush timer cannot reject.
- **Concurrency (tracer + architect, 3 flows re-traced from source):** (1) backfill delete-race FULL lifecycle on BOTH paths — cleanup fires with `[]` dir-scan in every interleaving; no re-orphan-after-cleanup window (`processImageFormats` fully materializes files before the row enters the batch; deferred unlink past commit is REQUIRED for batch-tx integrity). (2) settings-hash → ETag → cache invalidation across a backfill — BOTH serving paths invalidate (static rides mtime+size which the atomic rename changes; serve-upload ETag carries mtimeMs independently of the 5 s settings-hash TTL); no undocumented stale-serve gap. (3) upload → queue claim → process → conditional UPDATE → orphan cleanup across a restart-boundary two-worker race — connection-bound advisory lock releases on process death, `WHERE processed=false` is the single serialization point, atomic rename overwrites partial writes, lock release in `finally`; no double-write/orphan.
- **Architecture (architect):** zero new coupling across all cycle-5 commits — no new lib→app import, no circular dep; the two new test seams reach no production importer; the color-pipeline writers are converging, not drifting; no 4th color-column writer.
- **Docs (document-specialist):** ~35 prior CLAUDE.md/AGENTS.md claims re-checked + all cycle-5-touched claims (cache() count = 10 actual, `COLOR_IMPACTING_KEYS` = 9 at `settings-hash.ts:37-49`, both backfill paths' 10-column set, `IMAGE_PIPELINE_VERSION=7`, concurrency-cap arithmetic, `sanitizeForOg` consumers) all MATCH. One LOW omission (AGG-C6-05, `<select>` lookbehind undocumented). The AGENTS.md:40 ".context/plans/ gitignored" imprecision is unchanged (historical-artifact nuance, not a code contradiction).

---

## AGENT FAILURES

**test-engineer — Write did not land (recovered).** The test-engineer subagent ran on the initial fan-out AND on one retry (per PROMPT-1's retry rule). It performed a full investigation both times — confirming the WebP GPS-strip test vacuity (AGG-C6-02) and assessing the AVIF test (AGG-C6-T1) — but its final `Write` to `.context/reviews/test-engineer.md` did not complete in either attempt, leaving the STALE cycle-5 content (HEAD `1dde9b1e`, reporting the now-CLOSED TE-1/TE-5) in place. The orchestrator captured the agent's findings from its returned messages, INDEPENDENTLY re-verified every claim against current source (read `strip-gps-from-original.test.ts:104-126`, `gps-exif-strip.ts:554-591`, `process-image.ts:1522-1596`, confirmed the WebP RIFF spec via the authoritative Google doc), and persisted the corrected cycle-6 review to `test-engineer.md` with an authorship note. No finding was lost; the test-engineer findings (TE-C6-1/TE-C6-2) are fully reflected above as AGG-C6-02 / AGG-C6-T1.

All other 10 agents returned successfully and wrote their own files (security-reviewer is read-only by design; the orchestrator persisted its verbatim delivered report to `security-reviewer.md`, as last cycle).

---

## Disposition summary for PROMPT 2

**SCHEDULE (this cycle) — 1 real bug + its test + 2 adjacent a11y/audit gaps + 1 doc touch:**
- **AGG-C6-01 (MED)** — fix the `stripGpsFromWebpBuffer` RIFF field-order inversion in `gps-exif-strip.ts:564-565` (+ the `:580` write). The one real source bug.
- **AGG-C6-02 (MED)** — add a direct proven-RED `stripGpsFromWebpBuffer` pure-scrubber test (the gap that let AGG-C6-01 ship green). Land WITH the AGG-C6-01 fix.
- **AGG-C6-03 (MED)** — add `min-h-11` to the two public back-nav `<Link>`s (`s/[key]/page.tsx:105`, `year/[year]/page.tsx:109`); optional positive-assertion pin.
- **AGG-C6-04 (LOW)** — add `(?<!max-)` to the four `<Link>`/`<a>` touch-target patterns + negative self-check fixtures (close the AGG-C5-02 adjacent gap).
- **AGG-C6-05 (LOW)** — document the `<select>` (and new `<Link>`/`<a>`) `max-` lookbehind in CLAUDE.md:512 (fold into the AGG-C6-04 commit).
- **AGG-C6-T1 (LOW)** — optional parallel direct `stripGpsFromIsobmffBuffer` pure-scrubber test (bundle with AGG-C6-02) OR DEFER.

**DEFER (record in plan, severity preserved, exit criterion stated):**
- Durable bare-link audit rule (flag bare interactive `<Link>`/`<a>` with no sizing token) — high false-positive risk; re-open in a UI-polish/audit-hardening pass.
- AGG-C6-R1 (MED maintainability — WI-09 color-pipeline consolidation), AGG-C6-R2/R3/R4 (arch), AGG-C6-R5 (perf record-only), AGG-C6-R6 (designer DES-C5-2/3/4), AGG-C6-R7 (SW lost-update + real-encode test isolation + gain-map dead-code note).

**No CRITICAL or HIGH-severity defect this cycle.** The one MED correctness bug (AGG-C6-01) is a quality-degradation defect with NO privacy/security impact (the fallback re-encode still strips GPS — so it is NOT a data-loss/privacy finding requiring non-deferral; nonetheless it IS scheduled because the fix is small and the contract is broken). The substantive scheduled set is 1 real bug + 1 test + 2 a11y/audit gaps + 1 doc — all small, surgical. **Convergence holds:** the review surfaced exactly one genuine code bug (a long-latent WebP edge that the existing test was too weak to catch), its companion test gap, and two more instances of the well-known bare-link / `max-` audit blind-spots — no new architectural, security, or perf defect.
