# Aggregate Deep Review — Cycle 4/100 (review-plan-fix, internally run-9 cycle-1)

**Date:** 2026-06-13
**Repo:** /Users/hletrd/flash-shared/gallery (GalleryKit — Next.js 16 / React 19 / TS6 photo gallery)
**Cycle:** orchestrator "cycle 4/100". Working tree CLEAN at start; HEAD `ce0029aa` in sync with origin/master.
**Agents that returned (11/11):** code-reviewer, security-reviewer, perf-reviewer, critic, verifier, test-engineer, tracer, architect, debugger, document-specialist, designer. **No agent failures.**

**Gate baseline measured live this cycle (verifier ran every gate; multiple agents re-ran independently):**
- `npm run lint` → **exit 0** (clean)
- `npm run typecheck` (app + scripts) → **exit 0**
- `lint:api-auth` / `lint:action-origin` / `lint:public-route-rate-limit` → **all exit 0**
- `npx vitest run` → **exit 0, 214 files / 2067 tests passed, 0 failed** (cold run; the documented `backfill-color-pipeline` + `process-image-color-roundtrip` libheif cold-flake did NOT reproduce this run). Count grew 2060→2067 (+7), matching the new pins from `6454c4a3`.
- i18n leaf-key parity: **837 = 837, 0 drift**.

> The prior run-8 cycle-3 aggregate has been superseded by this file. Per-agent files were overwritten by this cycle's fan-out.

---

## TOP-LEVEL FINDING: the run-8 cycle-3 batch (AGG-R8c3-01..17) landed clean — every code fix independently RE-VERIFIED CLOSED at HEAD `ce0029aa`, not trusted on the plan's word

All 10 code-fixed findings from plan-335 (commits `0017a34e`..`6be638d2`) are CONFIRMED-CLOSED with present+correct code AND non-vacuous tests, by independent line-by-line verification across verifier, tracer, debugger, code-reviewer, perf-reviewer, critic, designer, and document-specialist (high cross-agent agreement). The 7 deferred findings in plan-336 remain correctly RECORDED (severity preserved, none falsely closed).

| Prior finding | Status at HEAD `ce0029aa` | Closing commit | Re-verified by |
|---|---|---|---|
| AGG-R8c3-01 NCLX code-2 isHdr side-effect | **CLOSED** — comment corrected + 2 pinning tests; behavior (upload-reject on PQ/HLG-named ICC) intentional & locked | 22387f32 | tracer, debugger, security-reviewer, verifier |
| AGG-R8c3-02 og-sanitize 3rd copy (JSON-LD) | **CLOSED** — `p/[id]/page.tsx` imports shared `sanitizeForOg`; lying docstring gone | 0028ede4 | security-reviewer, critic, document-specialist |
| AGG-R8c3-03 backfill orphan-leak on delete race | **CLOSED** — both UPDATE branches guard `affectedRows===0` → `cleanupDeletedMidReencodeVariants` (uses `sizes=[]` dir scan) + `deletedMidReencode` tally excluded from with-failures banner | 0017a34e | code-reviewer, perf-reviewer, tracer, debugger, verifier |
| AGG-R8c3-04 `text-destructive` dark-mode text 1.99:1 | **CLOSED** — new `--destructive-text` token: 6.46:1 light / 7.20:1 dark; 20 sites migrated; zero bare `text-destructive` text remain | 77013cd0 | designer (recomputed), critic, verifier |
| AGG-R8c3-05 home double heavy GROUP_CONCAT query | **CLOSED** — `getLatestImageForOgCached` (id+title, LIMIT 1, no JOIN/GROUP_CONCAT), `cache()`-wrapped | e9040d17 | perf-reviewer, architect, debugger |
| AGG-R8c3-06 24px alias button + scale-token blind spot | **CLOSED (but see AGG-C4-01)** — button `min-h-11 min-w-11`; audit scale-token patterns added | d70c1d98 | designer, verifier, **critic flags regex false-positive** |
| AGG-R8c3-07/08 amber dark-mode contrast (histogram/settings) | **CLOSED** — histogram `dark:text-amber-300`, settings `amber-700 dark:text-amber-400` | ecd093ab | designer (recomputed) |
| AGG-R8c3-11/14/16b/etc. test+doc pins | **CLOSED** — width≤0, SW bounded-HEAD, home-OG-strip, comment-stripped migrate tripwire, retryFailedImage i18n | 6454c4a3 / 6be638d2 / 5f097262 | test-engineer, document-specialist, verifier |

**Consequence for PROMPT 2:** plan-335/336 already reflect run-8-c3 as DONE/deferred. No plan-table hygiene needed. Schedule only the genuinely-NEW findings below.

---

## Cross-agent convergence map — GENUINELY OPEN / NEW this cycle (highest signal first; all verified against HEAD by the orchestrator before counting)

| Agg ID | Finding | Severity (max) | Conf | Agents | Status |
|---|---|---|---|---|---|
| **AGG-C4-01** | **Touch-target audit scale-token regex has empirically-confirmed FALSE POSITIVES on `max-h-{1..10}` / `max-w-{1..10}`.** The patterns added by `d70c1d98` (`touch-target-audit.test.ts:342,346,350,354`) alternate `(?:min-h\|min-w\|size\|h\|w)-(?:[1-9]\|10)` — the bare `h`/`w` branches are preceded only by `\b`, so `\bh-10\b` matches the `h-10` substring **inside `max-h-10`**, and `\bw-9\b` inside `max-w-9`. ORCHESTRATOR VERIFIED in Node against the exact committed regex: `<Button className="max-h-10">` and `<Button className="max-w-9">` both FLAG as "renders ≤40 px below 44 px floor" — FALSE (max-height/max-width are ceilings, not floors, and never constrain the tap target). `min-h`/`min-w`/`size` branches are correct (those ARE floors) — only the bare `h`/`w` branches are buggy. Green TODAY only because no current `<Button>` uses `max-h-{1..10}`/`max-w-{1..10}`; the moment one is added legitimately, the blocking gate fires with a lying message, training devs to silence it with a bogus `min-h-11` — defeating the audit. Fix: anchor the bare `h`/`w` branches with a negative lookbehind `(?<!max-)` (or `(?<![\w-]max-…)`), and add self-check assertions proving `max-h-10`/`max-w-9` are NOT flagged while `h-8`/`min-h-6` ARE. | **MED** (a blocking regression-gate that mis-fires on valid code + erodes trust in the audit) | High (empirical) | **1** (critic CRT-1; test-engineer separately confirmed the larger-token `\b` boundaries are correct, narrowing the bug to exactly the `max-` prefix) | NEW — adjacent gap of AGG-R8c3-06 |
| **AGG-C4-02** | **Sidecar backfill script `flushBatch` lacks the `affectedRows` orphan-cleanup guard that the in-app runner just gained — and the sidecar is the PRODUCTION path.** `scripts/backfill-color-pipeline.ts:318-342` runs `tx.execute(sql\`UPDATE images SET … WHERE id = ${item.id}\`)` in a batched transaction loop with NO `affectedRows` check and NO derivative cleanup. The in-app `admin-backfill-runner.ts` got the AGG-R8c3-03 fix (both branches guard `affectedRows===0 → cleanup`), but its sibling sidecar — which CLAUDE.md documents as the canonical production backfill (the prod container lacks `tsx`, so the `--rm` sidecar IS how prod re-encodes) — was NOT updated. Same delete-during-reencode race ⇒ orphaned derivative files for a deleted image on the production path, never cleaned up. CLAUDE.md asserts the two paths are equivalent ("SAME DB column set", "leave `pipeline_version` behind … never strand stale color metadata") — that equivalence is now FALSE for the cleanup contract. Admin-only, low-probability (concurrent delete during a backfill of the same id), disk-leak only. Fix: capture the ResultSetHeader from each `tx.execute`, and on `affectedRows===0` call the same `deleteImageVariants(dir, fn, [])` cleanup the runner uses. (Root cause is the duplication in AGG-C4-R1 below.) | **MED** (data-hygiene/disk leak on the PRODUCTION re-encode path; admin-only, low-prob) | High (mechanism verified — sidecar has no `affectedRows` check) | **1** (architect ARCH-R9-01) | NEW |
| **AGG-C4-03** | **Sales `StatusBadge` amber/green fail WCAG 1.4.3 small-text contrast in LIGHT mode.** `sales-client.tsx:91,93`: `downloaded` → `text-green-600` (#16a34a) and `pending` → `text-amber-600` (#d97706) on the white sales table. ORCHESTRATOR COMPUTED: green-600 on white = **3.30:1**, amber-600 on white = **3.19:1** — both below the 4.5:1 small-text floor. (Designer's "no dark variant" framing was inaccurate — both DO carry `dark:text-green-400`/`dark:text-amber-400`, which pass; the defect is the LIGHT-mode small-text value.) Notably the AGG-R8c3-08 amber fix explicitly mis-cited `sales-client.tsx` as the "correct reference" while leaving its own light-mode values failing, and never examined the green sibling. Admin-only surface. Fix: light value → `text-green-700` / `text-amber-700` (both = 5.02:1 on white), keep the passing `dark:` values. | **MED** (a11y WCAG 1.4.3; admin-only) | High (computed) | **1** (designer DES-A1) | NEW |
| **AGG-C4-04** | **Upload-queue delete-race cleanup orphans NON-default-size variants.** `image-queue.ts:375-379` (the `affectedRows===0 → "deleted during processing"` cleanup) calls `deleteImageVariants(UPLOAD_DIR_WEBP, job.filenameWebp)` with only TWO args ⇒ `sizes` defaults to `DEFAULT_OUTPUT_SIZES` (`process-image.ts:486`), which only deletes the *default-size* filenames — it does NOT trigger the directory scan (the scan only runs when `sizes.length === 0`, `:505`). So when the admin has configured NON-default `image_sizes` (the feature is admin-configurable up to 8 sizes), a delete-during-processing race leaves the non-default-size derivative files orphaned. The backfill runner's just-landed cleanup correctly passes `[]` (dir scan); the upload worker is the one writer that still passes default sizes. Pre-existing (predates the AGG-R8c3-03 fix), admin-only, low-prob, disk-leak only. Fix: pass `[]` as the third arg in all three `deleteImageVariants` calls at `image-queue.ts:375-379` so the dir scan catches every variant regardless of size config. | **LOW** (data-hygiene/disk leak; admin-only, low-prob, only with non-default sizes) | High (mechanism verified — default-sizes path, no scan) | **1** (critic CRT-2) | NEW |
| **AGG-C4-05** | **Backfill runner's SECOND UPDATE branch (detection-failure path) `affectedRows===0` cleanup is UNTESTED.** The AGG-R8c3-03 fix added the orphan-cleanup guard to BOTH UPDATE branches in `admin-backfill-runner.ts` — the detection-succeeded branch (`:573`) AND the detection-failed-but-re-encoded branch (`:605`) — but only the FIRST is covered by the 6 backfill test files. A refactor that drops the guard on the second branch leaves orphaned derivatives on disk forever with a green suite. One `it` block (mock `db.execute` → `affectedRows:0` on the detection-failure path → assert `cleanupDeletedMidReencodeVariants` called for all 3 formats + `deleted-mid-reencode` outcome, no version bump) closes it. | **LOW** (test depth on a freshly-landed correctness guard) | High | **1** (test-engineer TE-1) | NEW |
| **AGG-C4-06** | **CLAUDE.md says React `cache()` wraps "9" data-access functions; the actual count is 10.** `CLAUDE.md:357` enumerates 9 `*Cached` functions, but `getLatestImageForOgCached` (`data.ts`, added THIS-batch by `e9040d17` for the AGG-R8c3-05 perf fix) is missing from the prose — the doc was not updated alongside the code it documents. Trivial: bump 9→10 and add the name. | **LOW** (doc completeness) | High | **1** (document-specialist DOC-DS-1) | NEW |
| **AGG-C4-07** | **DOC/comment honesty cluster (3 sub-items, all non-exploitable):** (a) DOC-DS-2 / CRT-3 / TRC-1: the home `og:image` comment (`(public)/page.tsx:104-108`) says `pickFirstAvailablePhotoBuffer` "falls back to the site OG card," but the code tries only sized `_NNN.jpg` derivatives (never the base JPEG) and on total miss 302-redirects to `og_image_url` or the site ROOT HTML — not a "site OG card." Common case is fine (encoder writes sized derivatives on upload). (b) TRC-1: in `p/[id]/page.tsx` the JSON-LD block applies `sanitizeForOg` only to EXIF fields (`:222-226`), NOT to `displayTitle`/`description`/`topic_label` (`:217-219,246`) — correct posture (`safeJsonLd` escapes all; those fields are write-time validator-gated; EXIF is not) but the asymmetry is undocumented; a one-line comment prevents a future "fix the wrong way." (c) DOC-DS-3: `CLAUDE.md:263` cites `COLOR_IMPACTING_KEYS` at `settings-hash.ts:34-46`; actual array is `:37-49` (the "9 keys" count is correct, only the line range drifted ~3 lines). | **LOW** (doc/comment precision) | High | **3** (document-specialist ×2, critic, tracer — high agreement on the og:image comment) | NEW |
| **AGG-C4-08** | **SW image-cache metadata is a lost-update (whole-doc overwrite, no CAS).** `public/sw.js` `getMeta→mutate→setMeta` (`recordAndEvict`/`touchMeta`, ~`:70-122,152-161`) has no single-flight lock, so N concurrent masonry tiles drop each other's metadata writes. Cache-housekeeping ONLY — the 50 MB LRU `total` can drift low (cache exceeds the soft cap until browser-quota eviction); NO served-byte / correctness / crash impact. This is the previously-recorded AGG-R8c3-10 / BUG-1 / PERF-L1, re-confirmed unchanged. Defensible to DEFER given the documented best-effort cache posture. Fix (if wanted): a single-flight meta-write lock (must also re-stamp `sw.js` via `build-sw.ts` and update `sw-template-contract.test.ts`). | **LOW** (perf/cache-hygiene; best-effort by design) | High | **2** (debugger BUG-1, perf-reviewer PERF-L1) | RE-CONFIRMED (latent, prior) |
| **AGG-C4-09** | **`KNOWN_VIOLATIONS['components/image-manager.tsx']=6` is stale; the real scanner count is 1.** `touch-target-audit.test.ts:182`. test-engineer measured it by zeroing the budget and reading the audit output (then reverting): real count is 1, so the aggregate budget masks up to ~5 NEW sub-44 touch targets in that one file before the gate fires. The stale-budget detector at `:710-714` is informational, not a hard failure. This is the previously-recorded AGG-R8c3-15, already DEFERRED to plan-336 — re-confirmed still open. Fix: recount and tighten to the true value (1). | **LOW** (test precision; regression-detection slack in one file) | High (measured) | **2** (test-engineer TE-2, prior verifier VER-2) | RE-CONFIRMED (prior deferred) |

### Lower-confidence / test-depth tail (LOW, record or schedule cheaply)

| Agg ID | Finding | Severity | Disposition |
|---|---|---|---|
| **AGG-C4-T1** | TE-4/5/6 (test-engineer): `getLatestImageForOg`'s source-text test can't catch a dropped `processed=true` filter or reversed sort (it asserts SQL shape, not semantics); the home-OG sanitize pin asserts the *import* not the *call site*; `retryFailedImage`'s localized invalid-id branch has no behavioral test. | LOW (test depth) | SCHEDULE-cheap or DEFER — narrow, all on freshly-landed code that currently behaves. |
| **AGG-C4-T2** | TE-3 (test-engineer): the 4 encode-heavy real-AVIF tests still write derivatives into the SHARED `public/uploads` tree (only their *source* fixtures use `mkdtemp`); the AGG-R8c3-09 cold-flake mechanism is intact (did not reproduce this run, 4 warm rounds 78/78). | LOW (test-infra; RED-in-CI-noise class) | DEFER — isolate to per-test temp upload dir when the flake next reproduces; not a logic defect. |

### Architecture / record-only (re-confirmed; mostly prior-deferred)

| Agg ID | Finding | Severity | Disposition |
|---|---|---|---|
| **AGG-C4-R1** | ARCH-R9-01 root cause (architect): the "re-run `processImageFormats` + `detectColorSignals` → resolve decision → write the 10-column color set → handle delete-mid-reencode" operation is **triplicated** across `image-queue.ts` (upload), `admin-backfill-runner.ts` (in-app), and `scripts/backfill-color-pipeline.ts` (sidecar), coupled only by "mirrors X" comments. This duplication ALREADY produced the divergent guarantee in AGG-C4-02 (sidecar missing the cleanup guard) and AGG-C4-04 (upload worker wrong sizes arg). Extract a shared `applyColorPipelineResult()` writer owning the column list + cleanup contract; anchor one cross-site test. | MED (maintainability; has already materialized as 2 correctness divergences) | SCHEDULE the symptom fixes (AGG-C4-02, -04) now; the consolidation itself shares a root cause with the prior-deferred AGG-R8c3-13 (triplicated ICC ladder) and should DEFER to a dedicated WI-09 refactor. |
| **AGG-C4-R2** | AGG-R8c3-12 (architect, prior-deferred): `lib/api-auth.ts:1` imports `isAdmin` from `@/app/actions/auth` — the SOLE `lib`→`app` layering inversion (authoritative scan found exactly one). Concur LOW/Med + DEFER (exit criteria unmet). | LOW (arch) | DEFER (plan-336, unchanged). |
| **AGG-C4-R3** | AGG-R8c3-A5 (architect, prior-deferred): `COLOR_IMPACTING_KEYS` hand-maintained not derived from `GalleryConfig`; 14 `@/db`-importing libs are server-only by docstring, only `caption-generator.ts:19` carries `import 'server-only'`. Boundary test still enforces. Concur LOW + DEFER. | LOW (arch hardening) | DEFER (plan-336, unchanged). |
| **AGG-C4-R4** | ARCH-R9-02 (architect): `@/lib/storage` (390 LOC) is a dead seam (only its own index + a test consume it), but honestly self-documented as unwired per CLAUDE.md. Record-only, not a defect. | LOW | RECORD — risk is interface-rot if storage backends are wired without validating against the real fs call sites. |
| **AGG-C4-R5** | Perf record-only (perf-reviewer): bootstrap `notInArray` over ≤1000 permanently-failed IDs (`image-queue.ts:601-603`) — happy path zero-cost; prefer `processing_error IS NULL`. Decode-once-per-format ~18 decodes/image (WI-14 fresh-instance-per-format), Atom feed filesort (bounded by FEED_LIMIT+cache), timeline non-sargable `YEAR()/MONTH()` (bounded by LIMIT), single-pool/10 + single-writer — all DOCUMENTED-INTENTIONAL tradeoffs, NOT defects. | LOW | RECORD/DEFER — micro, restart-safe alternatives; no live defect. |

---

## VERIFIED-CLEAN (explicitly stress-tested this cycle, NO action)

- **All 6 GATES green** (lint, typecheck app+scripts, 3 security lint gates, full vitest 2067/2067 on a COLD run — the documented libheif cold-flake did not reproduce). i18n leaf-key parity **837 = 837, 0 drift**.
- **All 10 run-8-c3 code fixes RE-VERIFIED BEHAVING (not trusted):** see the top table. Notably the AGG-R8c3-03 backfill orphan-cleanup is correct across ALL delete/re-encode interleavings (tracer enumerated all three; the load-bearing invariant is delete-row-precedes-file-unlink ⇒ "files gone but row present → orphan" is structurally impossible; `deleteImageVariants` is ENOENT-tolerant; UUID filenames preclude the `sizes=[]` scan touching another image). The `--destructive-text` token recomputed 6.46:1 light / 7.20:1 dark / 5.54–5.98:1 on tinted `bg-destructive/10` seams. The new scale-token regex correctly rejects larger compliant tokens (`h-14`/`size-16`/`min-h-20`) — its ONLY bug is the `max-` false positive (AGG-C4-01).
- **Security (security-reviewer + tracer, hand-verified at HEAD):** NO live-exploitable vuln across full OWASP Top 10. A01 middleware guard + `withAdminAuth` central CSRF + `requireSameOriginAdmin` on every mutating action + random base-56 share keys (no IDOR) + user-scoped PAT revoke; A02/A07 Argon2id (65536/3/4) + timing-safe HMAC sessions + env-only `SESSION_SECRET` in prod + session-fixation prevention + dummy-hash timing equalization + pre-increment-before-Argon2; A03 Drizzle parameterization + argv-array mysqldump (no shell) + smart-collections scalar+allowlist+`hasOwnProperty.call` + path containment+symlink-reject+realpath-TOCTOU-close; A08 Stripe signature mandatory + idempotency dup-key-loser disambiguation; A10 OG fetch own-origin-only + 10s timeout + 1 MB cap; all 8 `dangerouslySetInnerHTML` are JSON-LD via `safeJsonLd`; OG text via `sanitizeForOg`; no open redirect / ReDoS / prototype pollution / insecure deserialization / hardcoded secrets; single-use download CAS (open-before-claim). The two fresh security fixes (`0028ede4` og-sanitize unification, `22387f32` NCLX isHdr) opened no new holes (the latter fails CLOSED).
- **npm audit:** unchanged — build/dev-time-only advisories (esbuild via drizzle-kit/tsx, postcss via next), absent from the prod runtime container. INFO only; the suggested fixes are major DOWNGRADES — do NOT take them.
- **Boundary/arithmetic (debugger):** every ISOBMFF/ICC/GPS `readUInt*` bounds-checked, walkers `try/catch`-wrapped (no OOB/÷0/NaN-escape); `gps-exif-strip.ts:514` `baseOffset+extentOffset` precision-loss above MAX_SAFE_INTEGER fails CLOSED via the `start+length > buf.length` guard; histogram AbortController + StrictMode worker lifecycle correct; view-count flush timer cannot reject; copy-button detached `setTimeout` is a silent no-op on React 18+/19.
- **Concurrency (tracer + debugger + architect):** per-image claim, backfill claim (in-app), delete-while-processing (upload + in-app backfill paths), restore quiesce, topic/tag creation all release locks/connections in `finally` and clean up orphaned files on the losing-worker path. The ONE remaining gap is the SIDECAR backfill path (AGG-C4-02) which the in-app fix did not reach.

---

## AGENT FAILURES

None. All 11 agents returned successfully on the first attempt.

---

## Disposition summary for PROMPT 2

**SCHEDULE (this cycle):**
- AGG-C4-01 (MED) — fix the touch-target scale-token regex `max-` false positive + self-check assertions.
- AGG-C4-02 (MED) — add `affectedRows` orphan-cleanup guard to the sidecar `flushBatch` (production backfill path).
- AGG-C4-03 (MED) — fix sales StatusBadge light-mode amber/green contrast (→ `*-700`).
- AGG-C4-04 (LOW) — pass `[]` to the upload-worker delete-race cleanup so the dir scan catches non-default sizes.
- AGG-C4-05 (LOW) — add the test for the backfill runner's second (detection-failure) cleanup branch.
- AGG-C4-06 + AGG-C4-07 (LOW doc) — CLAUDE.md cache()-count 9→10 + line-cite fix + the 3 comment-honesty one-liners.

**DEFER (record in plan, severity preserved, exit criterion stated):**
- AGG-C4-08 (LOW) — SW metadata lost-update (best-effort cache posture per CLAUDE.md; re-open if a hard 50 MB cap is required).
- AGG-C4-09 (LOW) — stale `KNOWN_VIOLATIONS=6` (already in plan-336; re-affirm).
- AGG-C4-T1/T2 (LOW test-depth/infra) — narrow tests on currently-behaving code + the encode-test isolation (re-open when the cold-flake reproduces).
- AGG-C4-R1 consolidation + AGG-C4-R2/R3/R4/R5 (arch/record) — root-cause refactor deferred to a dedicated WI-09; symptom fixes (AGG-C4-02, -04) scheduled now.

No CRITICAL or HIGH-severity-HIGH-confidence defect this cycle. No security/correctness/data-loss finding requires deferral (the two scheduled MED data-hygiene items are admin-only low-prob disk leaks, not data loss). Honest convergence is near: the substantive open set is 3 MED + 3 LOW scheduled, all adjacent-gap or contrast/test/doc hygiene.
