# Aggregate Deep Review — Run-8 Cycle-3 (review-plan-fix)

**Date:** 2026-06-13
**Repo:** /Users/hletrd/flash-shared/gallery (GalleryKit — Next.js 16 / React 19 / TS6 photo gallery)
**Cycle:** orchestrator "cycle 3/100" of this run (internally run-8). Working tree CLEAN at start; HEAD `ada92ba5` in sync with origin/master.
**Agents that returned (11/11):** code-reviewer, security-reviewer, perf-reviewer (code-reviewer lane), critic, verifier, test-engineer, tracer, architect, debugger, document-specialist, designer. **No agent failures.**

**Gate baseline measured live this cycle (verifier ran every gate; multiple agents re-ran independently):**
- `npm run lint` → **exit 0** (clean)
- `npm run typecheck` (app + scripts) → **exit 0**
- `lint:api-auth` / `lint:action-origin` / `lint:public-route-rate-limit` → **all exit 0**
- `npx vitest run` → **cold run: 2 failed / 2058 passed** (`backfill-color-pipeline` + `process-image-color-roundtrip` AVIF/libheif encoder contention under full parallelism — 13 files share the real `public/uploads` tree); **warm rerun: 2060/2060 pass.** Both flaking files pass in isolation (6/6, 11/11) and together (17/17). Neither was touched this cycle. This is a pre-existing CI-noise flake (VER-1 / AGG-R8c3-09).

> The prior run-8 cycle-2 aggregate has been superseded by this file. Per-agent files were overwritten by this cycle's fan-out.

---

## TOP-LEVEL FINDING: the run-8 cycle-2 fix batch (AGG-R8-01..13) landed clean — but TWO fixes are "fixed on paper" with an adjacent gap

**Independently re-verified by every agent against HEAD `ada92ba5` (not on the plan's word):** all 13 run-8 cycle-2 findings (AGG-R8-01 through AGG-R8-13) are CLOSED with verified, test-backed commits. High-signal cross-agent agreement (code-reviewer, tracer, verifier, debugger, designer, document-specialist, security-reviewer all converged on "closed & behaving").

| Prior finding | Status at HEAD | Closing commit | Verified by |
|---|---|---|---|
| AGG-R8-01 boundary test cold-flake | **CLOSED** — explicit `60_000` timeout (`:177`) + memoized `readCache`/`importSpecCache`; passes cold 2/2 | 02af4f95 | verifier (ran cold), test-engineer, code-reviewer |
| AGG-R8-02 home og:image oversized | **CLOSED** — home `og:image` → `/api/og/photo/${id}` at 1200×630 (`page.tsx:112-116`); 20 regression tests | 73496d2f | tracer (full trace), verifier, designer |
| AGG-R8-03 checkbox 32px + audit blind spot | **CLOSED** — `min-h-11 min-w-11` (`image-manager.tsx:418,444`); `scanRawCheckboxes` wired (verifier proved 0→1 on synthetic) | fbf91baa | designer, verifier (live probe), test-engineer |
| AGG-R8-04 active tag-chip contrast | **CLOSED** — `text-primary-foreground/90` = 13.81:1 light / 12.8:1 dark | ee0f38bd | designer (computed) |
| AGG-R8-05 SW HEAD unbounded | **CLOSED** — `AbortSignal.timeout(HEAD_REVALIDATE_TIMEOUT_MS=300)` in BOTH `sw.template.js` + `sw.js` (`:38,230`) | 9b7bb240 | tracer, perf-reviewer, verifier, debugger |
| AGG-R8-06 NCLX code-2 erases ICC | **CLOSED (but see AGG-R8c3-01)** — per-field `!== undefined` guard preserves ICC transfer/matrix | 74235265 | code-reviewer, tracer, **critic flags side-effect** |
| AGG-R8-07/08/09 latent-bug batch | **CLOSED** — load-more/home-client unmount + 0-width CSS + backfill width guard, all intact & symmetric | e8fce327 | debugger (line-by-line), tracer |
| AGG-R8-10 test depth ×3 | **CLOSED** — mixed-run counter partition + migration index-coverage tripwire; verified non-vacuous | f3667858 | test-engineer, code-reviewer |
| AGG-R8-11/12 doc drift | **CLOSED** — AGENTS.md test count, deploy-key, advisory-lock docblock | 007768c3 | document-specialist |
| AGG-R8-13 OG-sanitize symmetry | **CLOSED (but see AGG-R8c3-02)** — both OG IMAGE routes share `lib/og-sanitize.ts`; the JSON-LD copy was NOT migrated | d5399742 / ada92ba5 | tracer, security-reviewer, **critic flags third copy** |

**Consequence for PROMPT 2:** plan-333/334 already reflect the run-8-c2 work as DONE. No plan-table hygiene needed. Schedule only the genuinely-open findings below — most are LOW hygiene/test/doc; the substantive set is 2 MED + a small cluster of a11y.

---

## Cross-agent convergence map — GENUINELY OPEN this cycle (highest signal first)

| Agg ID | Finding | Severity (max) | Conf | Agents | Status |
|---|---|---|---|---|---|
| **AGG-R8c3-01** | **NCLX code-2 fix can now flip `isHdr` true → a previously-ingestable file is REJECTED at upload; commit's "no delivered-byte impact" claim is FALSE and the branch is untested.** `color-detection.ts:344` derives `transferFunction` from the ICC NAME via `inferTransferFunction` (returns `'pq'`/`'hlg'` for any ICC whose name contains `pq`/`st2084`/`hlg`/`arib`/`hybrid log`). Before 74235265, an NCLX box with transfer=code-2 (Unspecified) forced `transferFunction='unknown'` via `?? 'unknown'` → `isHdr=false`. After, code-2 leaves the ICC-name value intact → if the name is PQ/HLG-tokened, `isHdr=true` → `images.ts:283` REJECTS the upload when `allow_hdr_ingest=false` (default). VERIFIED: code-2 absent from all NCLX maps; `images.ts:283` gates on `isHdr`. Trigger (HEIF/AVIF with NCLX transfer=code-2 + PQ/HLG-named ICC) is RARE. Arguably the rejection is *correct* (an HDR-named source is plausibly HDR), so this is primarily a (a) false commit-message claim + (b) missing test, not a delivery-correctness regression. | **MED** (admin audit + a rare upload-rejection behavior change; commit message inaccurate) | High (reachability+mechanism verified) / Med (severity) | **2** (CRT-1, code-reviewer noted branch) | NEW — side-effect of AGG-R8-06 |
| **AGG-R8c3-02** | **og-sanitize unification is half-applied: a THIRD `sanitizeForOg` copy in `p/[id]/page.tsx:42-44` (JSON-LD) was NOT migrated and is WEAKER, and its docstring LIES.** The shared `lib/og-sanitize.ts:28` strips Unicode formatting **AND** C0 control chars (`OG_C0_CONTROL_CHARS`). The `p/[id]/page.tsx` local copy calls only `stripUnicodeFormatting()` — NO C0 strip — yet its docstring says "Matches the `sanitizeForOg` in the OG image route" (now false). The `sanitize-for-og-global.test.ts` guard passes for the wrong reason (it only checks `stripUnicodeFormatting` presence, not C0 parity). NOT exploitable (`JSON.stringify` escapes C0 in string values + `safeJsonLd` escapes `</script>`), pure defense-in-depth + doc honesty. Fix: import the shared `sanitizeForOg` into `p/[id]/page.tsx`, delete the local copy. | **LOW** (security hygiene + lying docstring; non-exploitable) | High | **2** (CRT-2, tracer-adjacent) | NEW |
| **AGG-R8c3-03** | **Backfill runner leaks orphaned derivative files on a delete-during-reencode race.** `admin-backfill-runner.ts:526-539` UPDATEs `WHERE id = ?` **unconditionally and never checks `affectedRows`** after re-encoding writes new derivative files. The sibling upload writer (`image-queue.ts:367-382`) explicitly checks `affectedRows === 0` → cleans up the variant files when the row was deleted mid-processing. `deleteImage` (`images.ts:538-632`) does NOT acquire the per-image processing lock (the backfill DOES, `:455`), so a delete that races an active re-encode of the same id deletes the row + unlinks files, then the backfill's `processImageFormats` re-materializes the derivatives → orphaned files for a deleted image, never cleaned up. Admin-only, low-probability (concurrent delete during a backfill of the *same* image), no corruption — disk leak + observability. Fix: mirror the `affectedRows === 0 → cleanup variants` guard in both backfill UPDATE branches; add a `deleted-mid-reencode` tally. | **MED** (data-hygiene/disk leak; admin-only, low-probability) | High (mechanism verified) | 1 (ARCH-1) | NEW |
| **AGG-R8c3-04** | **`text-destructive` error text fails WCAG 1.4.3 in DARK mode (~1.99:1).** The dark `--destructive` token (`globals.css:59` HSL `0 62.8% 30.6%`; oklch `40% 0.16 27` at `:124`) is tuned as a button *background* (white-on-it ≈ 9.6:1, fine) but is REUSED as a text foreground via `text-destructive` on `bg-card` across ~28 sites including the shared `ui/alert.tsx:13` primitive, the login error, and every `role="alert"` validation message. Dark red text on near-black card → ~1.99:1, well below 4.5:1. Public-facing (login/validation errors). Fix: a dedicated `--destructive-text` token (lighter in dark) OR a `dark:text-red-400` mirror following the established amber `dark:text-amber-400` convention; apply to `ui/alert.tsx` + the loose `text-destructive` text sites (exclude where it's only an icon/background). | **MED** (a11y, WCAG 1.4.3; widest-impact, public surface) | High (computed) | 1 (DES-1) | NEW |
| **AGG-R8c3-05** | **PERF: home page runs TWO uncached `GROUP_CONCAT` listing queries per request.** `(public)/page.tsx:92` `generateMetadata` calls `getImagesLite(…,1,0)` (full `LEFT JOIN imageTags + tags` + `GROUP BY` + filesort just to read the latest image's id/title for the OG card) and `:162` the body calls `getImagesLitePage(…,30,0)`. Neither `getImagesLite` (`data.ts:728`) nor `getImagesLitePage` (`:818`) is `cache()`-wrapped (confirmed — plain `async function`, no `Cached` suffix), so BOTH hit MySQL on every home render. Contrast `/p/[id]`, which routes both metadata + body through `getImageCached`. The metadata path needs only `SELECT id, title … LIMIT 1` — the tag JOIN/GROUP_CONCAT/filesort is wasted. Fix: add a minimal `getLatestImageForOg()` (id + title only, no tag agg) for the metadata path. Home-specific; the topic page is unaffected. | **MED** (perf; every home render does a redundant heavy query) | High | 1 (PERF-1) | NEW |
| **AGG-R8c3-06** | **Admin alias-remove button is 24px + audit cannot see Tailwind scale tokens.** `categories/topic-manager.tsx:330-337` is a `<button type="button">` (delete-alias, has `focus:ring`/hover/click) styled `min-h-6 min-w-6` (24px; inner `X` is 12px), below the repo's documented 44px floor. The `touch-target-audit.test.ts` FORBIDDEN regex matches `h-8/h-9/h-10` literals + bracket `min-h-[NNpx]` but STRUCTURALLY CANNOT match Tailwind scale tokens like `min-h-6`/`size-6` — a 2nd enforcement blind spot (mirrors the checkbox blind spot AGG-R8-03 closed). Fix: bump the button to `min-h-11 min-w-11` (keep the 12px X visible; label/button provides the hit area) AND extend the audit FORBIDDEN set to flag sub-44 scale tokens (`min-h-{1..10}` / `size-{1..10}` / `h-{1..10}`) on interactive elements. | **MED** (a11y, violates policy AND defeats the enforcement test) | High (static) | 1 (DES-2) | NEW |
| **AGG-R8c3-07** | **Amber clip-warning fails contrast in DARK mode (public histogram).** `histogram.tsx:608` `<span className="text-amber-700 font-medium">(sRGB preview)</span>` has NO dark variant → `text-amber-700` on the dark histogram surface ≈ 3.96:1, fails 4.5:1. The sibling pattern in `color-details-section.tsx:506/524/540` correctly uses `text-amber-700 dark:text-amber-300`. Public-facing (photo viewer histogram). Fix: `text-amber-700 dark:text-amber-300` (or `dark:text-amber-400`). | **LOW** (a11y, WCAG 1.4.3; public but narrow — only on clipped wide-gamut histograms) | High (computed) | 1 (DES-3) | NEW |
| **AGG-R8c3-08** | **Amber admin warnings fail contrast in DARK mode.** `settings-client.tsx:674` `text-amber-600` (no dark variant) ≈ 3.19:1 light / worse dark; `sales-client.tsx:93` pending-status uses the correct `text-amber-600 dark:text-amber-400`, and other settings amber sites (`:306,:338`) use `dark:text-amber-400` — `:674` is the outlier. Admin-only. Fix: `text-amber-700 dark:text-amber-400`. | **LOW** (a11y; admin-only surface) | High (computed) | 1 (DES-4) | NEW |
| **AGG-R8c3-09** | **TEST-INFRA: `backfill-color-pipeline` + `process-image-color-roundtrip` flake under full-suite parallelism.** Both encode real AVIF via libheif and share the real `public/uploads` tree with ~13 sibling tests; under full parallelism the encoder contends → "corrupt header" / `outcome: error`. Pass in isolation (6/6, 11/11), together (17/17), and on warm rerun (2060/2060). RED on a cold/contended CI run is indistinguishable from a real encode regression — same failure mode as the AGG-R8-01 boundary flake. Fix: isolate these encode-heavy tests to a unique temp upload dir per test (or a serial pool/`describe.sequential`) so they don't contend on shared files. Neither file was touched this cycle. | **LOW** (test-infra; RED-in-CI-noise class, not a logic defect) | High (verifier saw 2-fail cold / 0-fail warm) | **2** (VER-1, test-engineer baseline) | NEW |
| **AGG-R8c3-10** | **SW image-cache metadata is a lost-update (whole-doc overwrite, no CAS).** `sw.template.js` `touchMeta`/`recordAndEvict` do `getMeta → mutate → setMeta` with no single-flight lock, so N concurrent masonry tiles drop each other's metadata writes. Effect is cache-housekeeping ONLY — the 50 MB LRU `total` can drift low (cache exceeds cap until browser-quota eviction) or recency timestamps lost. Pre-existing (not from this cycle), no served-byte impact. Defensible to DEFER given the documented best-effort cache posture. Fix (if wanted): a single-flight meta-write lock. | **LOW** (perf/cache-hygiene; best-effort by design) | High | 1 (BUG-2) | NEW (latent) |
| **AGG-R8c3-11** | **TEST depth (new code unguarded):** (a) TEST-1/COR-1: the home OG route's `sanitizeForOg` application (`api/og/route.tsx:82-88`) — the exact AGG-R8-13 symmetry fix — is NOT pinned by any test (the `it.each` global-strip test lists only the photo route); a refactor dropping the home-route strip silently re-opens the gap. (b) TEST-2: the AGG-R8-09 backfill `width<=0` skip (`admin-backfill-runner.ts:430-436`) has NO test (all 4 backfill test files use `width:100`). (c) TEST-3: the SW bounded-HEAD `AbortSignal.timeout` (`sw.template.js:213-248`) is NOT pinned by `sw-template-contract.test.ts` (the reference `sw-cache.ts` doesn't implement HEAD probing) — drop the `signal:` and warm-paint tiles hang a full RTT each, green suite. | **LOW** (test depth on freshly-landed fixes) | High | **3** (test-engineer ×3, code-reviewer COR-1) | NEW |
| **AGG-R8c3-12** | **ARCH: `lib/api-auth.ts:1` imports `isAdmin` from `@/app/actions/auth` — a `lib`→`app` layering inversion.** The auth identity primitive lives in the server-action layer, forcing `withAdminAuth` (required on every admin API route) to reach UP into `app`. No hard ESM cycle today, but it's a near-cycle that will be copied. Fix: extract identity reads to a `lib/auth-session.ts` leaf and have both `app/actions/auth` and `lib/api-auth` import down from it. | **LOW** (architecture/maintainability) | Med | 1 (ARCH-2) | NEW |
| **AGG-R8c3-13** | **ARCH: ICC-name→gamut token ladder is hand-triplicated** across `color-detection.inferColorPrimaries`, `process-image.resolveColorPipelineDecision`, `resolveAvifIccProfile`. A new gamut keyword (WI-09/rec2100) must be added in all three or the admin audit silently disagrees with delivery — uncaught by per-function tests. (NOTE: the NCLX-first-audit vs ICC-first-delivery *precedence inversion* is intentional/documented — NOT a defect; only the duplicated token ladder is.) Fix: extract a shared `iccNameToGamut(name)` helper in a client-safe lib leaf. | **LOW** (maintainability/DRY; latent audit-vs-delivery drift) | High | 1 (ARCH-3) | NEW |
| **AGG-R8c3-14** | **DOC: CLAUDE.md prose lags the AGG-R8-* code batch (4 incompleteness items, not falsehoods).** (a) DOC-1: SW section (`:294`) says "ETag HEAD probe" but omits it is now bounded by `AbortSignal.timeout(300ms)`. (b) DOC-2: Touch-target "Pattern coverage" (`:520-528`) omits the new raw `<input type="checkbox|radio">` scanner. (c) DOC-3: the bidi-strip security bullet (`:178`) documents only the validation-layer defense, not the runtime `og-sanitize.ts` layer used by both OG routes. (d) DOC-4: home `og:image` → `/api/og/photo/${id}` (AGG-R8-02) is undocumented. One small doc patch closes all four. | **LOW** (doc completeness) | High | 1 (document-specialist) | NEW |
| **AGG-R8c3-15** | **TEST-PRECISION: `KNOWN_VIOLATIONS['components/image-manager.tsx']=6` is stale (real scanner count is lower post-AGG-R8-03).** `touch-target-audit.test.ts:182`. The aggregate count could mask up to ~5 NEW violations in that one file before the test fails. The dedicated checkbox unit test still guards AGG-R8-03 independently. Pre-existing. Fix: recount and tighten the entry to the true current value. | **LOW** (test precision; a regression-detection gap in one file) | Med (verifier saw mismatch) | 1 (VER-2) | NEW |
| **AGG-R8c3-16** | **Minor code-quality/i18n:** (a) COR-3 (code-reviewer): `migrate-reconcile-coverage.test.ts:79,143` index/column tripwire uses bare `MIGRATE_SRC.includes(name)` → a name appearing only in a COMMENT satisfies it (also CRT-5); strengthen to a statement-token match. (b) COR-4 (code-reviewer): `images.ts:1085` `retryFailedImage` has a lone unlocalized `'Invalid image ID'` string (every sibling uses `t('invalidImageId')`). (c) CRT-3: the home og:image comment overstates the fallback (`pickFirstAvailablePhotoBuffer` has no base-JPEG last resort, so a base-only legacy "latest" photo 302-redirects to HTML, not "the site OG card"). | **LOW** (test precision + i18n + comment honesty) | High | **2** (code-reviewer ×2, critic ×2) | NEW |
| **AGG-R8c3-17** | **DESIGN polish (LOW, designer DES-5/6/7):** (a) nav theme/locale toggle buttons lack the app's `focus-visible:ring` (rely on UA-default outline only). (b) lightbox color-pip `text-white/50` suffix is the thinnest passing contrast margin (5.15:1) + a faint dotted-underline tooltip cue. (c) info-sidebar topic `<Badge>` prints the raw slug instead of the humanized label. | **LOW** (a11y/UX polish) | Med | 1 (designer) | NEW |

### Perf/architecture observations — record-only (re-confirmed unchanged, NOT defects)

| Agg ID | Finding | Severity | Disposition |
|---|---|---|---|
| **AGG-R8c3-A1** | PERF-2: SW fires a 300ms-bounded HEAD per cached tile (~30 concurrent on a warm masonry paint). This is the DELIBERATE R10-H3 freshness behavior, already bounded by AGG-R8-05 — NOT a regression. Optional micro-opt: a short per-URL probe TTL in META_CACHE. | LOW | RECORD — documented freshness intent, bounded. |
| **AGG-R8c3-A2** | PERF-3: `image-queue.ts:601-627` bootstrap uses `notInArray` over ≤1000 permanently-failed IDs; happy path (empty set) is zero-cost. Prefer `processing_error IS NULL` (also restart-safe). | LOW | DEFER/record — micro, restart-safe alternative. |
| **AGG-R8c3-A3** | A1/A2 decode-once-per-format (~18 decodes/image) + Atom feed `updated_at` filesort (bounded by FEED_LIMIT=50 + route cache) — re-confirmed from prior cycles. | LOW | DEFER — scope/CPU-only, SAFE; low-traffic + cached. |
| **AGG-R8c3-A4** | Inherent single-pool / single-writer / view_count-best-effort tradeoffs. | MED | RECORD — deliberate topology, documented in CLAUDE.md. Not defects. |
| **AGG-R8c3-A5** | ARCH-4/ARCH-5: `COLOR_IMPACTING_KEYS` (settings-hash.ts) hand-maintained not derived from `GalleryConfig` (drifted 3→9 once); server-only enforced by docstring not `import 'server-only'` guard on the heavy `@/db`-importing libs. | LOW | DEFER/record — hardening levers; no live defect. |

---

## VERIFIED-CLEAN (explicitly stress-tested this cycle, NO action)

- **All 6 GATES green on a warm run** (lint, typecheck app+scripts, 3 security lint gates, full vitest 2060/2060). i18n leaf-key parity **837 = 837, 0 drift**.
- **Prior-cycle fixes re-verified BEHAVING (not trusted):** AGG-R8-01 boundary cold-pass + memoized reads + 60s timeout; AGG-R8-02 home og→/api/og 1200×630 (full tracer walk, every non-success branch is a 302 never a 404/blank, 1MB cap structurally kills the oversize problem); AGG-R8-03 checkboxes 44px + `scanRawCheckboxes` (verifier proved 0→1 on synthetic violation); AGG-R8-05 SW 300ms abort serves stale + bg-revalidate, single body GET, no unhandled rejection (tracer); AGG-R8-13 both OG IMAGE routes share `sanitizeForOg` with GLOBAL-flag replace-all.
- **Security (security-reviewer + tracer, hand-verified at HEAD):** NO live-exploitable vuln across full OWASP Top 10 sweep. Argon2id (65536/3/4) + timing-safe sessions; env-only `SESSION_SECRET` in prod; all 3 lint-gate invariants; centralized `withAdminAuth` CSRF; Drizzle parameterization + parameterized raw SQL; all 6 JSON-LD sites via `safeJsonLd`; byte-level GPS strip (never `withMetadata()`); single-use download CAS (open-before-claim); Stripe signature verification; `_PrivacySensitiveKeys` compile-time guard; own-origin-only OG fetch (no SSRF); no catastrophic-backtracking regex. SEC-1 (`style-src 'unsafe-inline'` + `img-src data:/blob:`) and SEC-2 (`LOG_PLAINTEXT_DOWNLOAD_TOKENS` opt-in scaffold) are LOW/INFO defense-in-depth notes, no code change required.
- **npm audit:** 5 advisories (3 HIGH esbuild via drizzle-kit/tsx + 2 MODERATE postcss via next) — ALL build/dev-time only, absent from the prod runtime container. INFO only, unchanged. The `isSemVerMajor` fixes are downgrades — do NOT take them.
- **Boundary/arithmetic (debugger):** every ISOBMFF/ICC/GPS `readUInt*` is bounds-checked, walkers are `try/catch`-wrapped (no OOB/÷0/NaN-escape/throw-to-caller); `icc-chromaticity` guards `|sum|<1e-9` / `|det|<1e-12`; `resolveBackfillConcurrency` can never yield NaN/0; histogram AbortController + og-photo-fetch timeout paths clean.
- **Concurrency (debugger + tracer + architect):** per-image claim, backfill claim, delete-while-processing (upload queue path), restore quiesce, topic/tag creation all release locks/connections in `finally` and clean up orphaned files on the losing-worker path — EXCEPT the backfill-vs-delete race (AGG-R8c3-03, the one new gap).
- **Color/HDR (tracer + code-reviewer):** NCLX code-2 per-field guard correct (preserves ICC transfer/matrix; matrix code-0 `'identity'` correctly overrides via `!==undefined`); `color_primaries` correctly public; ETag invalidation honest on both serve-upload (9-key hash) + static (mtime+size) paths. (The isHdr side-effect is AGG-R8c3-01.)
- **Designer fundamentals:** lightbox/search/bottom-sheet focus traps + focus restoration; settings form 16 `aria-describedby` refs all resolve (0 dangling/dup); `muted-foreground` contrast holds (6.04/7.76/7.72:1); CLS reservation via aspectRatio+containIntrinsicSize; back-to-top co-toggles aria-hidden+tabIndex+pointer-events; comprehensive reduced-motion + forced-colors CSS; masonry `columns-N` safelisted.
- **Doc accuracy (document-specialist):** IMAGE_PIPELINE_VERSION=7, COLOR_IMPACTING_KEYS=9 (CLAUDE.md `:260` correct — NO stale "5"; the "5" in some prompt-injected CLAUDE.md snapshots is from the embedded copy, not the on-disk file), Argon2 params, rate limits, pool 10/queue 20, nginx caps, app caps, all 6 advisory-lock names, blur MAX 4096, useDisplayCapability Firefox behavior, encoder decision enum, migration runbook fn names, backfill column set, i18n plural convention, `tagNamesAgg`, `uploaded_by` FK — all MATCH.

---

## ALREADY-OWNED / pre-acknowledged (recorded, not newly actionable)

### AGG-R8c3-OWNED-1 — Stripe `async_payment_succeeded` never writes an entitlement (HIGH/High · confirmed, = AGG-R8-OWNED-1)
- **Where:** `api/stripe/webhook/route.ts:88,105` handles only `checkout.session.completed` + `payment_status==='paid'`; `api/download/[imageId]/route.ts` returns 404 forever for ACH/bank-transfer settled payments.
- **Agents:** security-reviewer, document-specialist — re-confirmed correct security posture (fails CLOSED; mints NO false entitlement for unpaid funds).
- **Disposition:** **ALREADY-OWNED by plan-316 CRT-R5C1-04**, explicitly documented in CLAUDE.md (`entitlements` note scopes support to card/immediate-payment until plan-316 ships). Per the deferred-fix rule, security/correctness findings may be deferred when the repo's own rules explicitly allow — CLAUDE.md does. No data loss (funds settle in Stripe; entitlement row recoverable manually). Interim mitigation: disable async payment methods in Stripe Checkout. **Re-confirmed deferred to plan-316; NOT re-owned this cycle.** Exit criterion unchanged: plan-316 picked up, OR a real settled-but-undownloadable ACH purchase reported → escalate.

---

## AGENT FAILURES

None. All 11 spawned review agents returned successfully on the first attempt. (security-reviewer + perf-reviewer wrote their `.md` files via Bash heredoc where the Write tool was disabled; all other agents wrote directly. Verifier reverted all transient probe fixtures — working tree carries only the `.context/reviews/*.md` edits, no source residue.)

---

## Summary for PROMPT 2 (planning)

**Genuinely actionable this cycle (17 open):**
- **MED (4):** AGG-R8c3-01 (NCLX code-2 isHdr upload-rejection side-effect — fix commit-claim + add test, and decide whether the rejection is intended), AGG-R8c3-03 (backfill orphaned-file leak on delete-race — the substantive correctness/hygiene fix), AGG-R8c3-04 (`text-destructive` dark-mode contrast 1.99:1 — widest public a11y), AGG-R8c3-05 (home page two uncached heavy queries — add a minimal OG query), AGG-R8c3-06 (24px admin alias button + audit scale-token blind spot).
- **LOW (12):** AGG-R8c3-02 (third og-sanitize copy + lying docstring), AGG-R8c3-07/08 (amber dark-mode contrast ×2), AGG-R8c3-09 (encode-test parallelism flake), AGG-R8c3-10 (SW meta lost-update), AGG-R8c3-11 (test depth ×3 on freshly-landed fixes), AGG-R8c3-12 (lib→app layering inversion), AGG-R8c3-13 (triplicated ICC token ladder), AGG-R8c3-14 (CLAUDE.md doc completeness ×4), AGG-R8c3-15 (stale KNOWN_VIOLATIONS count), AGG-R8c3-16 (test-precision + i18n + comment honesty ×3), AGG-R8c3-17 (design polish ×3).

**Deferrable (record-only, with justification):** AGG-R8c3-A1..A5 (bounded SW HEAD micro-opt, image-queue notInArray micro, decode-once + feed filesort, inherent single-writer tradeoffs, COLOR_IMPACTING_KEYS/server-only hardening), AGG-R8c3-OWNED-1 (Stripe ACH — already plan-316).

**Cross-agent agreement (highest signal):** AGG-R8c3-11 (3 agents: test-engineer ×3 + code-reviewer), AGG-R8c3-01 (2: critic + code-reviewer), AGG-R8c3-02 (2: critic + tracer-adjacent), AGG-R8c3-09 (2: verifier + test-engineer), AGG-R8c3-16 (2: code-reviewer + critic). These duplicates raise confidence.

**Severity discipline:** No CRITICAL or HIGH-confidence-HIGH-severity defect. The 5 MED items are the cycle's substantive work; the LOW items are cheap hygiene/test-lock/doc fixes. Two MED items (AGG-R8c3-01, -03) are the only ones touching behavior; both are admin-only / low-probability. This is a convergence-leaning cycle — the run-8-c2 batch is solid; the new findings are mostly the *adjacent gaps* those fixes left and a small a11y/perf cluster.
