# Aggregate Deep Review — Cycle 7/100 (review-plan-fix, internally run-9 cycle-4)

**Date:** 2026-06-13
**Repo:** /Users/hletrd/flash-shared/gallery (GalleryKit — Next.js 16 / React 19 / TS6 photo gallery)
**Cycle:** orchestrator "cycle 7/100". Working tree CLEAN at start; HEAD `d0920957` in sync with origin/master.
**Agents that returned (11/11):** code-reviewer, security-reviewer, perf-reviewer, critic, verifier, test-engineer, tracer, architect, debugger, document-specialist, designer. **Two read-only-reviewer write-recoveries — see AGENT FAILURES (no finding lost).**

**Gate baseline measured live this cycle (orchestrator ran every gate before fan-out):**
- `npm run lint` → **exit 0** (clean)
- `npm run typecheck` (app + scripts) → **exit 0**
- `lint:api-auth` / `lint:action-origin` / `lint:public-route-rate-limit` → **all exit 0**
- `npx vitest run` → **exit 0, 218 files / 2086 tests passed, 0 failed** (cold, ~264 s; the documented libheif cold-flake did NOT reproduce). Count grew 2080→2086 (+6), matching the cycle-6 test additions (direct WebP + ISOBMFF pure-scrubber tests, Link/a max- negative fixtures, back-nav positive pins).

> The prior cycle-6 aggregate (run-9 cycle-3) has been superseded by this file. Per-agent files were overwritten by this cycle's fan-out. `perf-reviewer.md` was persisted by the orchestrator (perf-reviewer ran read-only — see AGENT FAILURES); all others wrote their own files.

---

## TOP-LEVEL: the cycle-6 batch (plan-341 / AGG-C6-01..05 + T1) landed clean — every fix RE-VERIFIED CLOSED at HEAD `d0920957`, not trusted on the plan's word

All 6 scheduled cycle-6 fixes (plan-341, commits `b6c4f915`..`d0920957`) are CONFIRMED-CLOSED with present+correct code AND non-vacuous tests, by independent line-by-line verification across verifier (12/12 items PASS), critic (proved both new GPS tests RED-on-revert), tracer (9 runtime WebP probes + a hand-built animated-WebP byte test), test-engineer, debugger, document-specialist (very high cross-agent agreement).

| Prior finding | Status at HEAD `d0920957` | Closing commit | Re-verified by |
|---|---|---|---|
| AGG-C6-01 `stripGpsFromWebpBuffer` RIFF field-order inversion (lossless path was dead code) | **CLOSED, PROVEN CORRECT + COMPLETE** — `gps-exif-strip.ts:566-567` now reads `chunkTag` from `offset..offset+4`, `chunkSize` from `readUInt32LE(offset+4)` (FourCC-first per WebP RIFF spec); XMP retag write targets the tag field `:584 buf.write('JUNK', offset, 4)`. Debugger re-traced every WebP edge (VP8/VP8L/VP8X, EXIF-only/XMP-only/both, odd-size padding, overflow fail-closed, frame-skip). Tracer ran it on lossy/lossless/near-lossless/alpha/animated layouts — all pixel-chunk byte-identical, GPS removed. | `b6c4f915` | debugger, tracer (9 probes), verifier, critic |
| AGG-C6-02 WebP GPS-strip test vacuous (passed through the lossy fallback) | **CLOSED, PROVEN NON-VACUOUS** — `strip-gps-from-original.test.ts:211-239` direct `stripGpsFromWebpBuffer` call asserts `stripped===true` + VP8 *compressed-chunk* byte-identity (not decoded-pixel equality); critic confirmed it goes RED at `expect(result).not.toBeNull()` on the reverted (null-returning) source. | `b6c4f915` (test landed same change) | critic (RED-on-revert), test-engineer (CONFIRM-1), verifier |
| AGG-C6-03 two public back-nav `<Link>`s ~20 px | **CLOSED** — `s/[key]/page.tsx:105` + `year/[year]/page.tsx:~107-109` both carry `min-h-11` (plus a positive-assertion pin added). | `1a483f9b` | designer (DES-C6-1 verified fixed), verifier, critic |
| AGG-C6-04 `<Link>`/`<a>` touch-target patterns missing `(?<!max-)` lookbehind | **CLOSED** — all four `<Link>`/`<a>` bare-token patterns at `touch-target-audit.test.ts:440,444,458,462` carry `(?<!max-)`; verifier ran the committed regex in Node (7/7 correct: `max-h-10` no-flag, `h-8` flags) + negative fixtures added. | `26f68430` | verifier (Node), critic, document-specialist |
| AGG-C6-05 CLAUDE.md `<select>`/`<Link>`/`<a>` `max-` lookbehind doc | **CLOSED** — `CLAUDE.md:516` now documents the `max-` lookbehind on Button/button, select, AND Link/a (the AGG-C6-04 lineage). | `26f68430` / docs | document-specialist, verifier |
| AGG-C6-T1 direct `stripGpsFromIsobmffBuffer` pure-scrubber test | **CLOSED, PROVEN NON-VACUOUS** — `strip-gps-from-original.test.ts:262-276` direct call asserts `stripped===true` + `buffer.length===input.length` (proves in-place `buf.fill(0)` zeroing, not a re-encode) + independently re-parses to assert GPS-gone. Strictly stronger than the dispatcher pixel proxy. | `23f62c66` | test-engineer (CONFIRM-2), critic, verifier |

**Consequence for PROMPT 2:** plan-341/342 already reflect cycle-6 as DONE/deferred (plan-341 marked complete in `d0920957`). No plan-table hygiene needed beyond marking the cycle complete. Schedule only the genuinely-NEW findings below.

---

## Cross-agent convergence map — GENUINELY OPEN / NEW this cycle (highest signal first; all verified against HEAD by the orchestrator before counting)

| Agg ID | Finding | Severity (max) | Conf | Agents | Status |
|---|---|---|---|---|---|
| **AGG-C7-01** | **The admin header brand/logo `<Link>` renders ~24 px tall — the admin-side twin of the public nav brand link that was already fixed.** `apps/web/src/components/admin-header.tsx:16` — `<Link className="mr-6 flex items-center space-x-2 font-bold" href={…/admin/dashboard}>` wraps `<span>{t('nav.admin')}</span>`. VERIFIED (orchestrator read the file + ran two independent bare-link sweeps across `src/app` + `src/components`): the className carries NO `h-*`/`min-h-*`/`size-*`/`py-*` token, the ancestor `flex items-center` row CENTERS (does not stretch) the link inside the `min-h-14` (56 px) bar, so the link renders at its text line-box height (~24 px, `text-base`). Below the repo's documented 44 px floor (WCAG 2.5.5 AAA) and AT the 24 px WCAG 2.5.8 AA boundary (fragile). It is in the touch-target audit's DELIBERATE bare-link blind spot (the `<Link>` patterns fire only on an EXPLICIT sub-44 sizing token); the file's existing `KNOWN_VIOLATIONS=1` is the SEPARATE Logout `<Button size="sm">` at `:22`, NOT this link. **This is the recurring bare-link theme's 4th consecutive cycle** — the public counterpart `nav-client.tsx:85` got `min-h-[44px]` in `bc7e2584`; the admin twin was never fixed. The two independent orchestrator sweeps confirm this is the SOLE remaining bare sub-44 interactive `<Link>`/`<a>`/`<button>` anywhere in the scanned surface (the two `p/[id]/page.tsx` Links at `:305,310` are `className="hidden" aria-hidden tabIndex={-1}` decorative prefetch hints, correctly excluded). **Fix:** add `min-h-11` to the className (a 1-token change), keep the existing flex affordance; optionally add an anchor-scoped positive pin. | **MED** (WCAG 2.5.5 AAA / repo self-imposed 44 px; fragile at 2.5.8 AA; the SOLE escape-to-dashboard affordance in the admin header, mobile-relevant) | **High** (className verbatim + line-box math + ancestor-flex centering + two exhaustive sweeps confirming it's the only one) | **1** (designer DES-C7-1) | NEW — the recurring theme's last untouched instance (admin side) |
| **AGG-C7-02** | **The WebP XMP-chunk `JUNK`-retag GPS branch has ZERO direct test coverage — symmetric to the EXIF-path field-order bug just fixed in AGG-C6-01.** `apps/web/src/lib/gps-exif-strip.ts:579-588` — the `else if (chunkTag === 'XMP ')` branch detects a GPS-bearing XMP chunk, retags its FourCC to `JUNK` (`buf.write('JUNK', offset, 4)`) and zeroes the payload (`buf.fill(0, dataStart, dataEnd)`). VERIFIED (orchestrator grepped all 9 `stripGpsFromWebpBuffer` references in `strip-gps-from-original.test.ts`): every one exercises the **EXIF** chunk path (`:211` byte-identity + EXIF neutralized), the GPS-free `{stripped:false}` case (`:241`), or the null/garbage cases (`:250,:483`) — the **XMP-bearing WebP path is completely unpinned**. The JPEG XMP path IS well-tested (`:287` + the SEC-R4C9-01 ExtendedXMP block), but the WebP XMP retag is JPEG-coverage-orphaned. Test-engineer verified the branch is reachable + currently-correct via a throwaway vitest probe (retags `XMP `→`JUNK`, zeroes payload, `stripped:true`, GPS gone) — **it works today but is invisible to the suite.** A wrong-offset regression in the `JUNK` write (the EXACT class of bug just fixed in the adjacent EXIF branch — `buf.write('JUNK', offset, 4)` vs e.g. `offset+4`) would leave the GPS-bearing XMP chunk readable in the paid-download ORIGINAL while still reporting `stripped:true` → **a SILENT PRIVACY LEAK on the deliverable, with no test to catch it.** Same root family as AGG-C6-01 → high signal. **Fix:** add two direct `stripGpsFromWebpBuffer` tests (positive: VP8 pixel-chunk byte-identity + GPS-gone + FourCC retagged to `JUNK`; negative: a GPS-free XMP chunk left intact, `stripped:false`), proven RED if the `JUNK` write offset is perturbed. | **MED** (test-depth gap on the privacy-critical paid-download original; the WebP XMP retag is a known-broken-class branch with no regression guard) | **High** (all 9 refs traced to EXIF/null/false; branch reachability + current-correctness proven by probe; the symmetry to the just-fixed EXIF bug is exact) | **1** (test-engineer TE-C7-1) | NEW — highest-value gap; the privacy-sensitive untested twin of the fixed bug |
| **AGG-C7-03** | **The `<Link>`/`<a>`/`<select>` touch-target patterns lack the sub-44 SCALE-TOKEN catch-all that `<Button>`/`<button>` carry — `<Link className="h-7">` (28 px) or `<a className="size-8">` (32 px) ships UNSEEN.** `apps/web/src/__tests__/touch-target-audit.test.ts` — the `<Button>`/`<button>` FORBIDDEN set has a scale-token catch-all (`(?<!max-)(?:min-h|min-w|size|h|w)-(?:[1-9]|10)`, added AGG-R8c3-06 after the alias-remove button shipped 24 px) that catches `h-7`/`size-8`/`min-h-6`/etc. But the `<Link>`/`<a>` patterns (`:440-472`) and the `<select>` patterns (`:415-428`) only enumerate the LITERAL `h-8|h-9|h-10` + `min-h-[<44px]` arbitrary values — they have NO scale-token branch. VERIFIED EMPIRICALLY (orchestrator ran the committed Link/a literal patterns in Node against `<Link className="h-7">`, `size-8`, `min-h-6`, `<a className="h-7">`, `size-8`): **all 5 MISSED**, while the equivalent `<Button>` source against the scale-token catch-all FLAGGED all 5. So a sub-44 scale token on an anchor-based or native-select touch target is invisible to the blocking gate. Latent (zero `h-7`/`size-8` on a Link/a/select in the repo today — the identical risk profile as AGG-C6-04, which the team chose to schedule) but it is the textbook recurring "fix one sibling, miss the next" theme one rung past last cycle's lookbehind fix: the prior cycle added `(?<!max-)` to Link/a but never added the scale-token catch-all that Button got two cycles earlier. **Fix:** add the scale-token catch-all pattern (string-literal + `cn()` composite, with the same ≥44 `h-1[12]`/`min-h-1[12]`/`size-1[12]` override lookahead and `(?<!max-)` lookbehind) to `<Link>`, `<a>`, and `<select>` (mirror the Button/button pair exactly) + add `h-7`/`size-8` negative-and-positive fixtures to the self-check block. Fold the AGG-C7-04 doc touch into the same commit. | **LOW** (blocking regression-gate with a real sub-44 blind spot on anchor/select touch targets; test-file only, latent) | **High** (empirical — ran the committed regex in Node; the Button catch-all proven to catch the same sources) | **2** (critic NF-1 + debugger noted the family pattern) | NEW — adjacent gap of AGG-C6-04 (one rung further: scale tokens, not just the lookbehind) |

### Lower-confidence / doc / record-only tail (LOW, schedule cheaply or DEFER)

| Agg ID | Finding | Severity | Disposition |
|---|---|---|---|
| **AGG-C7-04** | **CLAUDE.md:514 lists the scale-token catch-all coverage as "on `<Button>`/`<button>`" only.** With AGG-C7-03 unfixed the doc is currently ACCURATE (the catch-all genuinely is Button-only). The critic flagged that CLAUDE.md:516 reads as if `<Link>`/`<a>` carry "the scale-token catch-all" branch — re-reading, :516 is precisely about the `max-` LOOKBEHIND (true for Link/a) and the parenthetical "(and the scale-token catch-all)" refers to the Button branch also carrying the lookbehind (also true), so it does NOT literally claim Link/a have a scale-token branch — the imprecision is borderline. **Disposition:** if AGG-C7-03 lands, update :514 to list the new tag classes (`<Button>`/`<button>`/`<Link>`/`<a>`/`<select>`) in one edit; if AGG-C7-03 is deferred, the doc stays accurate as-is. Fold into the AGG-C7-03 commit. (critic NF-2, document-specialist did not independently flag — assessed the doc as accurate.) | LOW (doc completeness, contingent on AGG-C7-03) | SCHEDULE-cheap — fold into the AGG-C7-03 commit's doc touch, or NO-OP if AGG-C7-03 deferred. |
| **AGG-C7-05** | **`input.includes(Buffer.from('VP8L'))` whole-buffer substring scan can misclassify a lossy WebP as lossless in the GPS re-encode (Tier-2) fallback.** `apps/web/src/lib/process-image.ts:1566` — when `stripGpsFromWebpBuffer` returns `null` (malformed/unhandled WebP), the fallback re-encode picks lossless vs lossy by scanning the WHOLE input buffer for the bytes `VP8L`. A lossy VP8 file whose metadata/payload coincidentally contains those 4 bytes would re-encode LOSSLESS, bloating the stored original. **Privacy-safe** (GPS still stripped by the re-encode either way) — worst case is an oversized file on a rare fallback path. Canonical fix: check the chunk FourCC at the file's first sub-chunk (offset 12: `VP8 `/`VP8L`/`VP8X`) instead of scanning the whole buffer. (code-reviewer CR7-LOW-1 + debugger noted same; 2-agent agreement; pre-existing, NOT introduced this cycle.) | LOW (quality — rare oversized-file path; NO privacy/correctness impact) | SCHEDULE-cheap (1-line offset check) OR DEFER (pre-existing, privacy-safe, rare path). |

### Architecture / perf / designer record-only (re-confirmed UNCHANGED; all prior-deferred)

| Agg ID | Finding | Severity | Disposition |
|---|---|---|---|
| **AGG-C7-R1** | WI-09 color-pipeline writer consolidation: architect re-diffed at source — the two backfill paths (`admin-backfill-runner.ts` / `scripts/backfill-color-pipeline.ts`) remain BYTE-EQUIVALENT on the 10-column UPDATE set + `[]`-dir-scan cleanup + detection-failure semantics. **Clarification:** the actual color-column write count is **5 INSERT/UPDATE touchpoints** (browser INSERT in `actions/images.ts:350`, the queue UPDATE, the LR PAT-upload INSERT at `api/admin/lr/upload/route.ts:376` — a deliberately-mirrored parallel writer from US-P53, last touched `f3d68197`, NOT new — plus the two backfill writers). `scripts/backfill-cicp-recheck.ts` matched the grep but is read-only (diagnostic, never writes). No 4th/6th writer introduced. WI-09 consolidation deferral REMAINS JUSTIFIED. | MED (maintainability) | DEFER (WI-09; unchanged). |
| **AGG-C7-R2** | `lib/api-auth.ts:1` imports `isAdmin` from `@/app/actions/auth` — the SOLE `lib`→`app` layering inversion (architect; authoritative re-scan still finds exactly one, no cycle). UNCHANGED. | LOW (arch) | DEFER (plan-338/340/342, unchanged). |
| **AGG-C7-R3** | `COLOR_IMPACTING_KEYS` hand-maintained not derived — **9 keys** (`settings-hash.ts:37-48`; CLAUDE.md:263 verified CORRECT at 9 by architect + document-specialist; the brief's "5" paraphrase was stale-snapshot). Client/server-only boundary guard passes 2/2; `caption-generator.ts` is the lone server-only lib + is server-import-only; the 2 client `@/lib/data` imports are `import type` only. UNCHANGED. | LOW (arch hardening) | DEFER (unchanged). |
| **AGG-C7-R4** | `@/lib/storage` (~390 LOC) dead seam — zero production importers, honestly self-documented unwired per CLAUDE.md (architect). UNCHANGED. | LOW | RECORD/DEFER. |
| **AGG-C7-R5** | Perf record-only (perf-reviewer, all byte-identical at HEAD, NO source change this cycle): SW metadata lost-update (RC-1), bootstrap `notInArray` ≤1000 IDs (RC-2), decode-per-format ~18/image WI-14 (RC-3), Atom feed filesort bounded (RC-4), timeline non-sargable YEAR()/MONTH() bounded (RC-5), single-pool/10 single-writer (RC-6), `getMapImages` unbounded = PERF-R4C15-B (RC-7), analytics 'all'-window temp-table = PERF-R5C2-01 (RC-8). NEW observation **PERF-C7-OBS-1** (LOW, no fix): semantic-search scores ≤5000 512-dim vectors synchronously on the event loop — bounded by a hard cap + default-disabled + 30/min/IP rate limit + stub-demo design; single-digit-to-low-tens-ms worst case. All DOCUMENTED-INTENTIONAL / bounded, NOT defects. | LOW | RECORD/DEFER — no live defect. |
| **AGG-C7-R6** | Designer prior-deferred trio, RE-CONFIRMED OPEN (not re-escalated): **DES-C5-2** nav theme/locale/expand `<button>`s + title/topic `<Link>`s have no `focus-visible:ring` (`nav-client.tsx:85,93,122,155,166`) — UA-default outline still applies so NOT a hard 2.4.7 failure. **DES-C5-3** color-pip `text-white/50` gamut suffix = 5.15:1 (passes 4.5:1, thinnest margin; `lightbox-color-pip.tsx:237`) + faint histogram dotted-underline (`histogram.tsx:691` ~2:1 affordance cue). **DES-C5-4** `photo-viewer.tsx:816` topic Badge renders raw slug `{image.topic}` (sibling Back button + search both humanize). | LOW | DEFER (plan-336/340/342, unchanged) — fix in a UI-polish pass. |
| **AGG-C7-R7** | Prior-deferred, RE-CONFIRMED OPEN/record-only: SW image-cache metadata lost-update (whole-doc overwrite, no CAS; best-effort by design). Real-encode AVIF/WebP tests + libheif cold-flake share `public/uploads` (no per-test `mkdtemp` output isolation; AGG-C4-T2 — NOT regressed, did NOT reproduce this cycle; `process-image-color-roundtrip.test.ts:31-44`). One dead-code note (no impact): `readNullTerminatedAscii` in `gain-map-detection.ts:87` has an unreachable `if (p > limit) return ''` guard (the `while (p < limit)` guarantees `p <= limit` on exit) — DBG-C6-NC-01, record-only, harmless. | LOW | DEFER / RECORD. |
| **AGG-C7-R8** | Dependency hygiene (security-reviewer, DEV/BUILD-ONLY, runtime-risk NONE): **SEC-C7-01** esbuild RCE CVE (GHSA-gv7w-rqvm-qjhr) reachable only via `tsx`+`drizzle-kit` devDependencies (Deno-specific + needs a hostile `NPM_CONFIG_REGISTRY`; prod tree clean). **SEC-C7-02** postcss XSS-in-stringify (GHSA-qx2v-qp2m-jg93) is build-time over the app's own first-party CSS, no untrusted-input path. Do NOT `npm audit fix --force` — the fixes are downgrade-only (would downgrade Next). | High (CVE) / runtime NONE | RECORD/DEFER — not runtime-exploitable; downgrade-only fixes rejected. |

### Cosmetic record-only (no action)

- **DOC-C7-01** (document-specialist, LOW): AGENTS.md:40 says `.context/plans/` "is gitignored" but tracked historical artifacts remain in it (README.md, done/*.md per `git ls-files`); live plans actually live in repo-root `/plan/`. Pre-known historical-artifact nuance; the forward `.gitignore` rule is accurate. UNCHANGED.

---

## VERIFIED-CLEAN (explicitly stress-tested this cycle, NO action)

- **All 6 GATES green** (lint, typecheck app+scripts, 3 security lint gates, full vitest 2086/2086 on a COLD run — libheif cold-flake did not reproduce).
- **All 6 cycle-6 fixes RE-VERIFIED BEHAVING (not trusted):** see the top table. Notably the WebP RIFF fix was run by the tracer on 9 real WebP layouts (lossy/lossless/near-lossless/alpha/EXIF+XMP/odd-length-XMP/XMP-only/animated VP8X+ANIM+ANMF) — all pixel-chunk byte-identical with GPS removed, and the walker correctly does NOT descend into ANMF frames (a decoy "EXIF" string inside a frame was left untouched); both new GPS pure-scrubber tests proven RED-on-revert by the critic; the Link/a `(?<!max-)` lookbehind run in Node by the verifier (7/7).
- **Security (security-reviewer + tracer, hand-verified at current line numbers):** NO live-exploitable vuln across the full OWASP Top 10. **A01** lint:api-auth + lint:action-origin PASS, no bare-function admin exports, all-root model (no IDOR boundary), base-56 share keys; **A02** Argon2id (65536/3/4) + timing-safe HMAC sessions + hashed token storage + env-only prod `SESSION_SECRET`; **A03** Drizzle params, smart-collections scalar-enforcement blocks mysql2 object-expansion, 6/6 JSON-LD sinks via `safeJsonLd`, no eval; **A04** single-use atomic CAS + GET-interstitial/POST-claim + open-before-claim + advisory locks; **A05** nosniff global + no-store admin + restrictive interstitial CSP + serve-upload whitelist excludes `original/`; **A06** DEV/BUILD-ONLY CVEs (AGG-C7-R8, prod tree clean); **A07** dual-bucket rate limit + token-age cap + regex-after-crypto (no timing oracle); **A08** Stripe sig + payment_status gate + idempotency + tier allowlist + restore scanner (40+ patterns) + all `JSON.parse` allowlisted (no proto pollution); **A09** stderr cred redaction + PII dropped from webhook logs; **A10** OG fetch own-origin + DB-UUID filename constrained to `/uploads/jpeg/` + rate-limited + charged-404. **Privacy boundary AIRTIGHT** — `publicSelectFields` derived by destructuring-omission, 3 compile-time guards, `getImage`/`getMapImages` confirmed using the public field set + a `map_visible` JOIN + runtime row assertion, GPS byte-scrubbed at-rest on both ingest paths.
- **Boundary/arithmetic (debugger):** the WebP RIFF fix is CORRECT + COMPLETE (re-traced every edge); every OTHER walker re-confirmed bounds-correct + fails-CLOSED — `gps-exif-strip.ts` all 4 scrubbers + shared TIFF core, `color-detection.ts` NCLX `parseCicpFromHeif` (oversized size=1 box fails closed), `icc-extractor.ts` (tagCount/numRecords capped 100), `icc-chromaticity.ts` (div-by-zero `<1e-9`, det `<1e-12`, NaN escapes guarded), `gain-map-detection.ts` (depth-5/1MB caps, entry caps 1024). View-count flush FSM, histogram worker + AbortController, masonry rAF, `use-display-capability` value-memoized snapshot (no React #185), `blur-data-url` allowlist + 4096 cap — all re-traced clean.
- **Concurrency (tracer + architect, 4 flows re-traced from source):** (1) GPS-strip dispatch + WebP scrub — three-way branch (`null`→re-encode, `{stripped:false}`→byte-identical early-return, `{stripped:true}`→write) correct across all layouts. (2) paid-download token CAS → serve original — open-before-claim ordering holds (handle + Content-Length both derive from the opened inode; handle closed on every failure path); strip is upload-time + `strip_gps_on_upload` locked once any image exists, only 3 files touch the original dir; no un-stripped original reaches a buyer. (3) backfill detection-failure semantics — BOTH paths persist only `was_downscaled`+`avif_10bit` on detection failure, no `pipeline_version` bump, so the row stays a candidate (contract tests 3/3). (4) session/auth → mutating sink — `requireSameOriginAdmin` is CSRF-only by design; every mutating action ALSO does a real `getCurrentUser`/`isAdmin` check; all 14 action files + db-actions.ts audited clean; `withAdminAuth` gates origin (403) + auth (401) before the handler.
- **Architecture (architect):** zero new coupling — no new lib→app import (still exactly 1), no circular dep, no new client→server-only leak, no 4th/6th color-column writer, no config-layer bypass, no PII-boundary bypass. The color-pipeline writers are converging, not drifting.
- **Docs (document-specialist):** the on-disk CLAUDE.md/AGENTS.md at HEAD `d0920957` MATCH the code — `IMAGE_PIPELINE_VERSION=7`, `COLOR_IMPACTING_KEYS=9`, `cache()` count = 10, the Link/a max- lookbehind para, the WebP-now-wired claim, both backfill paths' 10-column set, the 6 advisory-lock names, login rate limits (5/15-min + `acct:<sha256>`), upload caps (200 MiB/2 GiB/100), nginx caps (2M/64K/250M/216M), Argon2 params, QUEUE_CONCURRENCY=1, avif_effort=6, MAX_BLUR_DATA_URL_LENGTH=4096 — all verified. Two of the brief's "expected mismatches" were stale-snapshot artifacts already corrected on-disk. Only the LOW AGENTS.md:40 imprecision (DOC-C7-01) remains, unchanged.

---

## AGENT FAILURES

**perf-reviewer — read-only context, Write disabled (recovered).** The perf-reviewer was spawned as a `code-reviewer`-lane subagent which ran read-only; its `Write` to `.context/reviews/perf-reviewer.md` was blocked, leaving the STALE cycle-6 content (HEAD `4c3d5924`) in place. The agent returned its FULL cycle-7 review inline. The orchestrator persisted that verbatim report to `perf-reviewer.md` (HEAD `d0920957`, with an authorship note) and independently re-confirmed the key claims against source (`process-image.ts:1109-1115,1566`, `image-queue.ts:609-611`, `clip-embeddings.ts:14`, `data.ts` unbounded sweep). No finding lost — PERF-C7-OBS-1 + RC-1..8 are fully reflected above as AGG-C7-R5 / AGG-C7-05.

**critic — no write failure** (wrote its own file). Note: the critic's findings NF-1/NF-2 are reflected as AGG-C7-03 / AGG-C7-04; the orchestrator INDEPENDENTLY re-proved NF-1 by running the committed Link/a regex in Node (5/5 sub-44 scale tokens MISSED vs the Button catch-all FLAGGING all 5).

All other 10 agents returned successfully and wrote their own files (security-reviewer + tracer + debugger + verifier + architect + code-reviewer + test-engineer + document-specialist + designer + critic).

---

## Disposition summary for PROMPT 2

**SCHEDULE (this cycle) — 1 a11y fix + 1 privacy-critical test + 1 audit-gate hardening + 1 contingent doc + 1 cheap quality fix:**
- **AGG-C7-01 (MED)** — add `min-h-11` to the admin-header brand `<Link>` (`admin-header.tsx:16`); optional positive-assertion pin. The recurring theme's last untouched instance.
- **AGG-C7-02 (MED)** — add two direct `stripGpsFromWebpBuffer` XMP-branch tests (positive byte-identity + GPS-gone + `JUNK` retag; negative GPS-free intact), proven RED if the `JUNK` write offset is perturbed. Closes the privacy-critical untested twin of the AGG-C6-01 bug.
- **AGG-C7-03 (LOW)** — add the scale-token catch-all pattern (string + `cn()`) to `<Link>`/`<a>`/`<select>` in `touch-target-audit.test.ts` (mirror Button/button) + `h-7`/`size-8` self-check fixtures. Closes the AGG-C6-04 adjacent gap one rung further.
- **AGG-C7-04 (LOW)** — if AGG-C7-03 lands, update CLAUDE.md:514 to list the new tag classes in the scale-token coverage line (fold into the AGG-C7-03 commit). NO-OP if AGG-C7-03 deferred.
- **AGG-C7-05 (LOW)** — replace the `input.includes(Buffer.from('VP8L'))` whole-buffer scan at `process-image.ts:1566` with a first-sub-chunk FourCC check (offset 12). Cheap quality fix; privacy-safe either way. (May DEFER — pre-existing, rare path.)

**DEFER (record in plan, severity preserved, exit criterion stated):**
- Durable bare-link audit rule (flag bare interactive `<Link>`/`<a>` with no sizing token) — high false-positive risk; the per-link positive-pin remains the accepted mitigation. NOTE: this is now the 4th cycle finding bare-link instances (c5=3, c6=2, c7=1) — the recurrence is *decreasing*, and AGG-C7-01 appears to be the last one, so the cost/benefit of the full layout-aware heuristic is still not met.
- AGG-C7-R1 (MED maintainability — WI-09 color-pipeline consolidation), AGG-C7-R2/R3/R4 (arch), AGG-C7-R5 (perf record-only incl. PERF-C7-OBS-1), AGG-C7-R6 (designer DES-C5-2/3/4 trio), AGG-C7-R7 (SW lost-update + real-encode test isolation + gain-map dead-code note), AGG-C7-R8 (dev/build-only CVEs), DOC-C7-01 (AGENTS.md imprecision).

**No CRITICAL or HIGH-severity runtime defect this cycle.** Two MED findings: AGG-C7-01 (a11y — the recurring bare-link theme's last instance, on the admin side) and AGG-C7-02 (a test-depth gap on a PRIVACY-CRITICAL branch — the WebP XMP retag — that is the untested symmetric twin of the bug fixed last cycle). Both are scheduled. AGG-C7-02 is a test gap, not a live bug (the branch is proven correct today), so it is not a non-deferrable data-loss finding — but it guards the paid-download privacy contract and the fix is small, so it is done this cycle. **Convergence holds with real signal:** the review surfaced the recurring theme's last instance (high-confidence a11y), a privacy-critical untested branch symmetric to last cycle's confirmed bug, and one more rung of the well-known touch-target-audit blind spot — no new architectural, security-runtime, or perf defect. The substantive scheduled set is small and surgical.
