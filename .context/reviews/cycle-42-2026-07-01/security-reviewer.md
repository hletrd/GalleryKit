# Cycle 42 Security/Auth Review

Scope: HEAD `6efd00a8`. I read `AGENTS.md`, `CLAUDE.md`, `.context/reviews/_aggregate.md`, the Cycle 41 aggregate, the recent Cycle 40/41 security reviews, and current source under `apps/web`, deploy scripts, Docker/nginx config, and security-focused tests. Review lens: auth/authz, CSRF/same-origin, rate limits, SSRF, secret leakage, path traversal, admin/public boundary, and deploy safety.

Deferred filter: I did not re-raise Cycle 41 carry-forward/deferred items where there was no new security evidence changing severity.

## Result

No real new findings.

## Findings

None.

## Evidence

- Admin API auth is centrally enforced by `withAdminAuth`, including PAT scope checks, token auth rate limiting, no-store/nosniff defaults, and same-origin checks before cookie auth (`apps/web/src/lib/api-auth.ts:72`, `apps/web/src/lib/api-auth.ts:82`, `apps/web/src/lib/api-auth.ts:83`, `apps/web/src/lib/api-auth.ts:98`, `apps/web/src/lib/api-auth.ts:114`, `apps/web/src/lib/api-auth.ts:123`, `apps/web/src/lib/api-auth.ts:135`). Current admin API exports are wrapped at `apps/web/src/app/api/admin/db/download/route.ts:21` and `apps/web/src/app/api/admin/lr/upload/route.ts:84`.
- Same-origin validation fails closed unless `Origin` or `Referer` matches the trusted expected origin, and proxy headers are only trusted when explicitly enabled (`apps/web/src/lib/request-origin.ts:45`, `apps/web/src/lib/request-origin.ts:58`, `apps/web/src/lib/request-origin.ts:87`, `apps/web/src/lib/request-origin.ts:96`, `apps/web/src/lib/request-origin.ts:101`, `apps/web/src/lib/request-origin.ts:106`).
- Session handling keeps the production signing secret out of the DB fallback path, signs tokens with HMAC-SHA256, verifies signatures with `timingSafeEqual`, hashes stored session ids, and checks DB expiry (`apps/web/src/lib/session.ts:26`, `apps/web/src/lib/session.ts:82`, `apps/web/src/lib/session.ts:117`, `apps/web/src/lib/session.ts:136`, `apps/web/src/lib/session.ts:145`).
- Login and password-change flows keep same-origin checks before auth work, pre-increment rate limits before Argon2 verification, use dummy Argon2 verification for username-enumeration resistance, and rotate sessions transactionally (`apps/web/src/app/actions/auth.ts:98`, `apps/web/src/app/actions/auth.ts:129`, `apps/web/src/app/actions/auth.ts:180`, `apps/web/src/app/actions/auth.ts:218`, `apps/web/src/app/actions/auth.ts:294`, `apps/web/src/app/actions/auth.ts:352`, `apps/web/src/app/actions/auth.ts:399`).
- Rate-limit posture is layered: client IP extraction only trusts proxy headers under `TRUST_PROXY=true`, PAT auth attempts are pre-incremented, and public OG/share/semantic/search/load-more surfaces are metered before guarded work (`apps/web/src/lib/rate-limit.ts:166`, `apps/web/src/lib/rate-limit.ts:245`, `apps/web/src/lib/rate-limit.ts:279`, `apps/web/src/lib/rate-limit.ts:336`, `apps/web/src/lib/rate-limit.ts:366`, `apps/web/src/app/actions/public.ts:141`, `apps/web/src/app/actions/public.ts:255`, `apps/web/src/app/api/search/semantic/route.ts:107`, `apps/web/src/app/api/search/semantic/route.ts:173`, `apps/web/src/app/api/search/similar/[id]/route.ts:72`, `apps/web/src/app/api/search/similar/[id]/route.ts:98`, `apps/web/src/app/api/og/route.tsx:74`, `apps/web/src/app/api/og/photo/[id]/route.tsx:45`).
- SSRF/open-redirect review found the OG photo route pins internal fetches to canonical `BASE_URL` and fails closed on invalid canonical origin; SEO OG image URLs reject scheme-relative/backslash relative escapes and require same-origin absolute URLs; CSP image base URLs require http(s), https in production, and no credentials/query/hash (`apps/web/src/app/api/og/photo/[id]/route.tsx:97`, `apps/web/src/app/api/og/photo/[id]/route.tsx:109`, `apps/web/src/lib/seo-og-url.ts:9`, `apps/web/src/lib/seo-og-url.ts:33`, `apps/web/src/lib/seo-og-url.ts:37`, `apps/web/src/lib/content-security-policy.ts:1`, `apps/web/src/lib/content-security-policy.ts:17`, `apps/web/src/lib/content-security-policy.ts:21`).
- Path traversal and file-serving checks are in place for public derivatives, private originals, backup downloads, and LR uploads: allowed top-level upload dirs/extensions, safe path segments, symlink rejection, realpath containment, private original directory mode, basename-only original filenames, and descriptor-stable streaming (`apps/web/src/lib/serve-upload.ts:136`, `apps/web/src/lib/serve-upload.ts:141`, `apps/web/src/lib/serve-upload.ts:153`, `apps/web/src/lib/serve-upload.ts:181`, `apps/web/src/lib/serve-upload.ts:185`, `apps/web/src/lib/serve-upload.ts:277`, `apps/web/src/lib/upload-paths.ts:49`, `apps/web/src/lib/upload-paths.ts:120`, `apps/web/src/lib/upload-paths.ts:139`, `apps/web/src/lib/upload-paths.ts:165`, `apps/web/src/app/api/admin/db/download/route.ts:31`, `apps/web/src/app/api/admin/db/download/route.ts:51`, `apps/web/src/app/api/admin/db/download/route.ts:75`, `apps/web/src/app/api/admin/lr/upload/route.ts:101`, `apps/web/src/app/api/admin/lr/upload/route.ts:188`, `apps/web/src/app/api/admin/lr/upload/route.ts:215`, `apps/web/src/app/api/admin/lr/upload/route.ts:221`, `apps/web/src/app/api/admin/lr/upload/route.ts:270`).
- Admin/public data separation remains guarded by the canonical public field omission block, compile-time privacy guards, and tests for public select/search enrichment shapes (`apps/web/src/lib/data.ts:368`, `apps/web/src/lib/data.ts:451`, `apps/web/src/lib/data.ts:473`, `apps/web/src/lib/data.ts:475`, `apps/web/src/lib/search-enrichment-fields.ts:29`, `apps/web/src/lib/search-enrichment-fields.ts:43`, `apps/web/src/__tests__/privacy-fields.test.ts:60`, `apps/web/src/__tests__/privacy-fields.test.ts:86`, `apps/web/src/__tests__/search-route-privacy.test.ts:54`).
- Secret leakage review found gitignored env files and Docker build ignores for env/data/uploads, placeholder-only env examples, and a tracked-secret regression test (`.gitignore:18`, `apps/web/.gitignore:33`, `.dockerignore:14`, `apps/web/.dockerignore:5`, `apps/web/.env.local.example:18`, `apps/web/.env.local.example:30`, `apps/web/src/__tests__/tracked-secrets.test.ts:7`, `apps/web/src/__tests__/tracked-secrets.test.ts:33`, `apps/web/src/__tests__/tracked-secrets.test.ts:57`).
- Deploy-safety review found the documented single-instance/proxy model preserved: Docker binds the app to host networking with `HOSTNAME=127.0.0.1` and `TRUST_PROXY=true`, persists mutable data as bind mounts, runs as `node` after entrypoint permission checks, checks health before prune, and prunes only after the live container/image is up (`apps/web/docker-compose.yml:15`, `apps/web/docker-compose.yml:20`, `apps/web/docker-compose.yml:24`, `apps/web/scripts/entrypoint.sh:16`, `apps/web/scripts/entrypoint.sh:20`, `apps/web/scripts/entrypoint.sh:47`, `apps/web/deploy.sh:30`, `apps/web/deploy.sh:50`, `apps/web/deploy.sh:61`, `apps/web/deploy.sh:77`). The remote deploy helper refuses group/world-readable deploy env files (`scripts/deploy-remote.sh:65`, `scripts/deploy-remote.sh:69`), and nginx keeps edge body/rate limits plus `/uploads/original/` denial (`apps/web/nginx/default.conf:2`, `apps/web/nginx/default.conf:31`, `apps/web/nginx/default.conf:58`, `apps/web/nginx/default.conf:74`, `apps/web/nginx/default.conf:124`, `apps/web/nginx/default.conf:148`, `apps/web/nginx/default.conf:165`).

## Validation

- `npm run lint:api-auth --workspace=apps/web` - passed; both admin API route files reported OK.
- `npm run lint:action-origin --workspace=apps/web` - passed; scanner reported all mutating server actions enforce same-origin provenance.
- `npm run lint:public-route-rate-limit --workspace=apps/web` - passed; public expensive/mutating routes reported either rate-limited or explicitly exempt.
- `npm audit --omit=dev --workspace=apps/web` - passed; found 0 vulnerabilities.
- `npm test --workspace=apps/web -- check-api-auth.test.ts check-action-origin.test.ts check-public-route-rate-limit.test.ts privacy-fields.test.ts search-route-privacy.test.ts tracked-secrets.test.ts api-auth-response-headers.test.ts request-origin.test.ts backup-download-route.test.ts serve-upload.test.ts upload-paths.test.ts semantic-route.test.ts similar-route.test.ts og-photo-fallback.test.ts seo-og-url.test.ts` - passed; 13 files, 253 tests.
- `npm run typecheck --workspace=apps/web` - passed.
- `npm run lint --workspace=apps/web` - passed.
