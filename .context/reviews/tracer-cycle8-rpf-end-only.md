# Tracer Review — Cycle 8 RPF (end-only)

## Causal trace

Stripe webhook → entitlements INSERT (with `entitlementId` field
generated by MySQL AUTO_INCREMENT). The webhook insert-failure log
(line 322 of webhook route) carries `sessionId / imageId / tier`. The
refund failure log (line 220 of sales actions) carries `entitlementId`.
The download stream-error log (line 206 of download route) carries
`entitlementId / code`.

The download route's lstat/realpath catch on line 151 is the **only
remaining catch** on the paid-asset surface that drops the
`entitlementId` correlation key. This is the exact pattern flagged by
cycle 7 RPF C7-RPF-01..05 on the upstream Stripe surface, now repeating
on the downstream download surface.

## Finding

#### C8-RPF-TR-01 — Close the audit chain on the download lstat/realpath catch

- File: `apps/web/src/app/api/download/[imageId]/route.ts:151`
- Severity: **Low** | Confidence: **High**
- **What:** convert positional log to structured-object form with
  `entitlementId` correlation key, mirroring the same-file stream-error
  log on line 206 and the upstream Stripe surface convention from
  cycles 5/6/7.

## Cross-agent agreement

Converges with code-reviewer (C8-RPF-CR-01), architect (C8-RPF-ARCH-01),
debugger (C8-RPF-DBG-01), critic (C8-RPF-CRIT-01).
