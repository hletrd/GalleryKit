# Cycle 85/100 Performance / Concurrency Review

Reviewer: perf-reviewer
Date: 2026-07-01
HEAD reviewed: `1d29b9886109` (`test(review): close cycle 84 release contracts`)
Scope: performance, concurrency, CPU/RSS, image pipeline, DB query/index fit, cache/ETag behavior, UI responsiveness, deploy/runtime resource risks.

## Verdict

No new confirmed performance, concurrency, CPU/memory, image-pipeline, DB-cost, cache/ETag, UI-responsiveness, or deploy/runtime-resource finding in this pass.

Severity: n/a
Confidence: medium-high. Cycle 84's implementation delta from `023ae28d41ee` to `1d29b9886109` changed review/plan artifacts, `.gitignore`, and one Vitest source-contract file; the only source file touched was `apps/web/src/__tests__/failed-image-retry.test.ts`, where the runtime-free test now slices the dashboard source and asserts the failed-row label flow. I still inspected the current runtime hot paths listed below at HEAD.

## Confirmed Findings

| Severity | Confidence | File:line citations | Failure scenario | Suggested fix |
| --- | --- | --- | --- | --- |
| n/a | medium-high | n/a | No actionable new performance or concurrency failure was confirmed. | No fix recommended. |

Findings count: 0.

## Inventory / Evidence

- Cycle 84 delta: `1d29b9886109` touched `apps/web/src/__tests__/failed-image-retry.test.ts` only among source files; its changed assertions remain test-time source inspection, not production runtime work. The relevant test body now builds `failedImageRowBody` from the dashboard source and checks helper-derived labels inside that slice at `apps/web/src/__tests__/failed-image-retry.test.ts:152`, `apps/web/src/__tests__/failed-image-retry.test.ts:157`, `apps/web/src/__tests__/failed-image-retry.test.ts:158`, and `apps/web/src/__tests__/failed-image-retry.test.ts:159`.
- Listing/search DB paths remain bounded. Public listing caps rows with `LISTING_QUERY_LIMIT = 100` at `apps/web/src/lib/data.ts:668`, caps `getImagesLite` to at most limit+1 at `apps/web/src/lib/data.ts:806`, and uses cursor filtering before limit at `apps/web/src/lib/data.ts:799` and `apps/web/src/lib/data.ts:808`. Search rejects overlong queries at `apps/web/src/lib/data.ts:1545`, caps effective result count at `apps/web/src/lib/data.ts:1547`, short-circuits tag/alias work once the main query fills the limit at `apps/web/src/lib/data.ts:1617`, and bounds parallel tag/alias over-fetch at `apps/web/src/lib/data.ts:1650`. Map payloads are capped at `apps/web/src/lib/data.ts:1734`.
- Supporting indexes still match the reviewed access patterns: processed/date and processed/created listing indexes at `apps/web/src/db/schema.ts:118` and `apps/web/src/db/schema.ts:119`, topic listing index at `apps/web/src/db/schema.ts:120`, tag join indexes at `apps/web/src/db/schema.ts:135` and `apps/web/src/db/schema.ts:136`, analytics retention/reporting indexes at `apps/web/src/db/schema.ts:236`, `apps/web/src/db/schema.ts:237`, `apps/web/src/db/schema.ts:238`, `apps/web/src/db/schema.ts:239`, and `apps/web/src/db/schema.ts:240`, and semantic scan index at `apps/web/src/db/schema.ts:298`.
- Image processing keeps CPU/RSS bounded for the current design. Sharp concurrency is divided by the three-format fan-out at `apps/web/src/lib/process-image.ts:44`, capped from env at `apps/web/src/lib/process-image.ts:47`, applied globally at `apps/web/src/lib/process-image.ts:54`, and cache-disabled at `apps/web/src/lib/process-image.ts:57`. Originals stream to disk instead of heap-buffering 200 MB uploads at `apps/web/src/lib/process-image.ts:905` and `apps/web/src/lib/process-image.ts:910`. Large wide-gamut sources are pixel-capped at `apps/web/src/lib/process-image.ts:1096` and downscaled before fan-out at `apps/web/src/lib/process-image.ts:1117`. Per-format work uses fresh Sharp instances at `apps/web/src/lib/process-image.ts:1279` and `apps/web/src/lib/process-image.ts:1283`, then all three formats settle before validation at `apps/web/src/lib/process-image.ts:1436`.
- Queue/backfill concurrency remains bounded against DB pool headroom. Queue reserved-live-connection budgeting is encoded at `apps/web/src/lib/image-queue.ts:88`, the queue cap calculation at `apps/web/src/lib/image-queue.ts:99`, env clamping at `apps/web/src/lib/image-queue.ts:104`, retry maps prune above their cap at `apps/web/src/lib/image-queue.ts:211`, per-image advisory claims happen at `apps/web/src/lib/image-queue.ts:472`, duplicate/processed rows are skipped at `apps/web/src/lib/image-queue.ts:578`, and bootstrap scans at most one batch at `apps/web/src/lib/image-queue.ts:931`. In-app backfill reserves roughly half the pool at `apps/web/src/lib/admin-backfill-runner.ts:105`, caps concurrency at `apps/web/src/lib/admin-backfill-runner.ts:139`, keyset-fetches 100-row batches at `apps/web/src/lib/admin-backfill-runner.ts:405`, and applies the cap when creating its PQueue at `apps/web/src/lib/admin-backfill-runner.ts:710` and `apps/web/src/lib/admin-backfill-runner.ts:717`.
- Semantic/CLIP CPU paths are still gated. Real inference concurrency is capped at `apps/web/src/lib/clip-model.ts:53`, pending waiters are capped at `apps/web/src/lib/clip-model.ts:57`, queue wait time is capped at `apps/web/src/lib/clip-model.ts:61`, model loading is lazy/offline at `apps/web/src/lib/clip-model.ts:198`, `apps/web/src/lib/clip-model.ts:205`, and `apps/web/src/lib/clip-model.ts:210`, and image preprocessing is fixed at 512 px inside the inference slot at `apps/web/src/lib/clip-model.ts:276`, `apps/web/src/lib/clip-model.ts:285`, and `apps/web/src/lib/clip-model.ts:286`. The semantic route bounds body size at `apps/web/src/app/api/search/semantic/route.ts:96`, rejects oversize bodies at `apps/web/src/app/api/search/semantic/route.ts:162`, charges rate limit before expensive work at `apps/web/src/app/api/search/semantic/route.ts:178`, caps query length at `apps/web/src/app/api/search/semantic/route.ts:243`, and scans only `SEMANTIC_SCAN_LIMIT` rows at `apps/web/src/app/api/search/semantic/route.ts:279`. Similar-photo search uses the same bounded scan at `apps/web/src/app/api/search/similar/[id]/route.ts:177`.
- Cache/ETag behavior avoids unbounded request work on the serving fallback path. The settings-hash cache TTL is module-scoped at `apps/web/src/lib/serve-upload.ts:46`, inflight work is deduped at `apps/web/src/lib/serve-upload.ts:59`, stale hashes are returned immediately after a refresh starts at `apps/web/src/lib/serve-upload.ts:75`, ETags include pipeline, mtime, size, and settings hash at `apps/web/src/lib/serve-upload.ts:229` and `apps/web/src/lib/serve-upload.ts:230`, 304 responses return before body work at `apps/web/src/lib/serve-upload.ts:239`, HEAD returns before `createReadStream` at `apps/web/src/lib/serve-upload.ts:271`, and abort cleanup destroys streams at `apps/web/src/lib/serve-upload.ts:297`.
- Service-worker cache behavior remains bounded. Image cache budget is 50 MB at `apps/web/public/sw.template.js:31`, HTML fallback cache is 50 entries at `apps/web/public/sw.template.js:34`, synchronous cached-image HEAD revalidation is capped at 300 ms at `apps/web/public/sw.template.js:39`, image metadata mutation is serialized at `apps/web/public/sw.template.js:100`, image LRU eviction walks oldest entries without sorting at `apps/web/public/sw.template.js:124`, cached-image revalidation is lazy/single-flight per fetch event at `apps/web/public/sw.template.js:259`, HEAD revalidation sends `If-None-Match` at `apps/web/public/sw.template.js:310`, and stale entries return immediately with background revalidation at `apps/web/public/sw.template.js:348`.
- UI responsiveness controls are still in place. Masonry resize is rAF-debounced and cleaned up at `apps/web/src/components/home-client.tsx:53`, `apps/web/src/components/home-client.tsx:55`, and `apps/web/src/components/home-client.tsx:65`; masonry reserves layout with aspect ratio and contain-intrinsic-size at `apps/web/src/components/home-client.tsx:321`, `apps/web/src/components/home-client.tsx:322`, and `apps/web/src/components/home-client.tsx:324`; above-fold eager/high-priority images are limited to the visible column count at `apps/web/src/components/home-client.tsx:296`, `apps/web/src/components/home-client.tsx:362`, and `apps/web/src/components/home-client.tsx:364`. Search aborts stale semantic requests at `apps/web/src/components/search.tsx:156`, creates per-request abort controllers at `apps/web/src/components/search.tsx:193`, ignores stale responses at `apps/web/src/components/search.tsx:203` and `apps/web/src/components/search.tsx:233`, and debounces input at `apps/web/src/components/search.tsx:312`. Histogram work downsamples to 256 px at `apps/web/src/components/histogram.tsx:180`, transfers pixel buffers to the worker at `apps/web/src/components/histogram.tsx:222`, rAF-debounces resize redraws at `apps/web/src/components/histogram.tsx:455`, and terminates its worker on unmount at `apps/web/src/components/histogram.tsx:544`.
- Deploy/runtime constraints still match the operational contract. Runtime Sharp/native deps are installed and smoke-checked at `apps/web/Dockerfile:71`, `apps/web/Dockerfile:77`, and `apps/web/Dockerfile:80`; CLIP weights stay on the persisted data mount instead of the image at `apps/web/Dockerfile:111`; app-owned signal handling is explicit at `apps/web/Dockerfile:116` and `apps/web/Dockerfile:128`; deploy waits for health before pruning at `apps/web/deploy.sh:57` and `apps/web/deploy.sh:73`; Docker cleanup runs only after healthy `up -d` and keeps bind-mounted data out of Docker volume pruning at `apps/web/deploy.sh:84`, `apps/web/deploy.sh:94`, `apps/web/deploy.sh:100`, `apps/web/deploy.sh:101`, `apps/web/deploy.sh:102`, and `apps/web/deploy.sh:103`.

## Deferred Items Not Re-Raised

I did not re-raise carry-forward performance/operator-scale items because this pass found no new runtime code change, measurement, corpus-size trigger, or exit-criterion hit. That includes the known deferred items for sidecar color backfill memory/concurrency, semantic-search CPU/concurrency, view-retention/rate-limit cleanup indexes, container CPU/RSS guidance, `processImageFormats` per-size re-decode, upload original orphan cleanup, shared-view flush pool pressure, serve-upload DB-outage circuit breaker, static-path derivative invalidation, process-local rate limiting, and search leading-wildcard LIKE from `.context/plans/run10-cycle2/deferred.md`, plus the Cycle 84 carry-forward list in `.context/plans/cycle-84-2026-07-01-deferred.md`.

## Validation

- Read-only review of AGENTS.md, CLAUDE.md, latest aggregate, Cycle 84 aggregate/perf review/plan/deferred, current deferred register, Cycle 84 git delta, and relevant source files/tests.
- No source or plan edits were made.
- Artifact written: `.context/reviews/cycle-85-2026-07-01/perf-reviewer.md`.
- Passed: `git diff --check -- .context/reviews/cycle-85-2026-07-01/perf-reviewer.md`.
- Full lint/type/build/test gates were not run because this lane is review-only and no runtime code was changed.
