# Cycle 83 Security Review

Date: 2026-07-01
Branch: `master`
Reviewed HEAD: `cc46b1d69c11cb175c88df69f17cbe526d23aa0d`
Role: `security-reviewer`

## Result

No confirmed actionable security issue found.

Severity summary: Critical 0, High 0, Medium 0, Low 0.
Confidence: High for the inventoried surfaces below.
Source modifications: None.

## Cycle 82 Baseline Check

- Read `.context/reviews/cycle-82/_aggregate.md`: Cycle 82 security lane had no confirmed actionable security findings; aggregate findings were release-ledger, result-label, and retry-button accessibility issues.
- Read `.context/reviews/cycle-82/security-reviewer.md`: prior security inventory covered auth/session/PATs, action origin guards, route rate limits, upload/path containment, backup/restore, SSRF, privacy fields, headers, and secrets.
- Read `.context/plans/cycle-82-2026-07-01-plan.md` and `.context/plans/cycle-82-2026-07-01-deferred.md`: Cycle 82 scheduled fixes were ledger/result-label/retry-button work; no newly deferred Cycle 82 security items. Carry-forward items remain operator/architecture/a11y/test-shape issues, not newly escalated by this lane.
- Diff from the Cycle 82 source baseline to this HEAD touches review/plan artifacts, `.gitignore`, i18n messages, source-contract tests, failed-image retry UI, search/similar labels, and `lib/photo-title.ts`. It does not touch auth, session, token, upload, backup/restore, route-wrapper, rate-limit, or privacy-select implementation files.

## Inventory And Evidence

| Area | Security property checked | Evidence | Why no actionable issue remains |
| --- | --- | --- | --- |
| Admin API auth and PAT scopes | Admin API requests are authenticated, cookie requests are same-origin, PAT requests are scoped/rate-limited and non-cacheable. | `apps/web/src/lib/api-auth.ts:72` checks token auth only when a route declares `allowTokenScope`; `apps/web/src/lib/api-auth.ts:82` verifies the presented token; `apps/web/src/lib/api-auth.ts:83` enforces scope; `apps/web/src/lib/api-auth.ts:98` adds no-store headers; `apps/web/src/lib/api-auth.ts:114` checks same-origin before cookie auth; `apps/web/src/lib/api-auth.ts:123` requires `isAdmin()`. | Current admin API exports are covered by `withAdminAuth(...)`, and token bypass is intentionally scoped to PAT use rather than cookie CSRF. |
| Sessions and token storage | Session secrets are not DB-fallback in production; tokens use strong randomness, HMAC/signature checks, hash-at-rest, expiry, and constant-time comparisons. | `apps/web/src/lib/session.ts:30` refuses production fallback without `SESSION_SECRET`; `apps/web/src/lib/session.ts:82` creates random HMAC-signed session tokens; `apps/web/src/lib/session.ts:117` uses `timingSafeEqual`; `apps/web/src/lib/session.ts:136` looks up hashed session ids; `apps/web/src/lib/admin-tokens.ts:52` generates 32 random bytes; `apps/web/src/lib/admin-tokens.ts:60` stores SHA-256 token hashes; `apps/web/src/lib/admin-tokens.ts:68` constant-time-compares token hashes; `apps/web/src/lib/admin-tokens.ts:141` verifies presented tokens fail-closed. | No new code weakens token entropy, persistence, expiry, or verification. |
| Server actions and CSRF | Mutating server actions return early unless request origin/referer matches the expected origin. | `apps/web/src/lib/request-origin.ts:87` fails closed by default when source headers are absent; `apps/web/src/lib/request-origin.ts:96` checks `Origin`; `apps/web/src/lib/request-origin.ts:101` checks `Referer`; `apps/web/src/lib/action-guards.ts:37` centralizes `requireSameOriginAdmin()`. | `lint:action-origin` passed and reported all mutating actions guarded or explicitly exempt/read-only/rate-limited. |
| Public route rate limits | Anonymous expensive routes charge before DB/CPU-heavy work; proxy spoofing is not trusted unless configured. | `apps/web/src/lib/rate-limit.ts:166` trusts proxy IP headers only with `TRUST_PROXY=true`; `apps/web/src/app/api/search/semantic/route.ts:173` charges before semantic config/body/embedding work; `apps/web/src/app/api/search/similar/[id]/route.ts:98` charges before production mode and embedding scans; `apps/web/src/app/api/og/route.tsx:83` rate-limits OG rendering; `apps/web/src/app/api/og/photo/[id]/route.tsx:100` rate-limits photo OG generation. | `lint:public-route-rate-limit` passed for public API routes; no unbounded new public expensive endpoint was found. |
| Upload and path traversal | User filenames are sanitized; disk paths are generated/contained; public upload serving excludes originals and rejects traversal/symlinks. | `apps/web/src/app/actions/images.ts:134` guards upload action origin; `apps/web/src/app/actions/images.ts:180` validates user filenames; `apps/web/src/app/actions/images.ts:252` claims upload quota before awaited work; `apps/web/src/app/actions/images.ts:402` strips GPS from DB/original when configured; `apps/web/src/app/api/admin/lr/upload/route.ts:84` wraps PAT upload in `withAdminAuth`; `apps/web/src/app/api/admin/lr/upload/route.ts:101` rejects chunked transfer; `apps/web/src/app/api/admin/lr/upload/route.ts:202` mirrors filename validation; `apps/web/src/app/api/admin/lr/upload/route.ts:407` mirrors GPS stripping. `apps/web/src/lib/upload-paths.ts:49` creates private originals with `0700`; `apps/web/src/lib/upload-paths.ts:120` rejects unsafe original filenames; `apps/web/src/lib/upload-paths.ts:153` rejects symlinks; `apps/web/src/lib/serve-upload.ts:15` allows only derivative directories; `apps/web/src/lib/serve-upload.ts:154` validates safe segments; `apps/web/src/lib/serve-upload.ts:176` realpath-checks the root and `apps/web/src/lib/serve-upload.ts:182` rejects symlinks. | Browser and PAT upload paths still use generated storage names, size/count caps, path containment, and original GPS stripping. Public serving cannot reach `original/`. |
| Backup, restore, SQL/raw command surfaces | DB backup/download/restore stay admin-gated, same-origin guarded, path-contained, non-shell, and SQL-scanned before import. | `apps/web/src/app/[locale]/admin/db-actions.ts:170` requires origin for backup; `apps/web/src/app/[locale]/admin/db-actions.ts:221` spawns `mysqldump` without shell and passes credentials via env; `apps/web/src/app/[locale]/admin/db-actions.ts:230` writes backup files mode `0600`; `apps/web/src/app/[locale]/admin/db-actions.ts:255` redacts stderr; `apps/web/src/app/[locale]/admin/db-actions.ts:365` starts restore with origin/auth; `apps/web/src/app/[locale]/admin/db-actions.ts:390` acquires restore lock; `apps/web/src/app/[locale]/admin/db-actions.ts:404` fences upload-processing contract; `apps/web/src/app/[locale]/admin/db-actions.ts:597` validates SQL header; `apps/web/src/app/[locale]/admin/db-actions.ts:620` chunk-scans restore SQL; `apps/web/src/app/[locale]/admin/db-actions.ts:674` spawns `mysql --one-database` without shell. `apps/web/src/app/api/admin/db/download/route.ts:21` wraps download with `withAdminAuth`; `apps/web/src/app/api/admin/db/download/route.ts:24` validates backup filenames; `apps/web/src/app/api/admin/db/download/route.ts:51` realpath-confines downloads. `apps/web/src/lib/sql-restore-scan.ts:61` blocks dangerous SQL primitives and `apps/web/src/lib/sql-restore-scan.ts:210` blocks disallowed write targets. | No raw command surface accepts untrusted shell strings, and restore remains constrained to app-table dump shapes. |
| SSRF and open redirect | Server-side OG photo fetches are pinned to canonical origin and bounded; fallback redirects are same-origin only. | `apps/web/src/app/api/og/photo/[id]/route.tsx:176` documents request-origin avoidance; `apps/web/src/app/api/og/photo/[id]/route.tsx:188` derives fetch origin from canonical `BASE_URL`; `apps/web/src/app/api/og/photo/[id]/route.tsx:195` fails closed if canonical URL is invalid; `apps/web/src/lib/og-photo-fetch.ts:64` uses bounded per-attempt fetches; `apps/web/src/lib/og-photo-fetch.ts:81` and `apps/web/src/lib/og-photo-fetch.ts:85` cap response bytes; `apps/web/src/app/api/og/photo/[id]/route.tsx:347` validates fallback `ogImageUrl` origin. | Host/header injection does not control internal fetch origin or redirects. |
| Privacy field boundaries | Public data paths omit admin/private fields; map GPS is the only public GPS exception and requires explicit topic opt-in. | `apps/web/src/lib/data.ts:368` defines `publicSelectFields` as an omit-derived public field set; `apps/web/src/lib/data.ts:410` documents map-only latitude/longitude exposure; `apps/web/src/lib/data.ts:473` defines `PrivacySensitiveKeys`; `apps/web/src/lib/data.ts:475` compile-guards public selects; `apps/web/src/lib/data.ts:486` compile-guards map selects excluding all sensitive fields except GPS; `apps/web/src/lib/data.ts:1716` returns map images only after `topics.map_visible = true` and non-null GPS filters. `apps/web/src/lib/search-enrichment-fields.ts:29` uses a dedicated public search enrichment field set; `apps/web/src/lib/search-enrichment-fields.ts:43` compile-guards it against `PrivacySensitiveKeys`. | Cycle 82 result-label changes use existing public title/description fields through `getPhotoResultLabel()` and did not add any public columns. |
| XSS, CSP, headers, and secrets | Public rendering has script-breaking escapes and production hardening headers; tracked secrets are placeholders/redacted history. | `apps/web/src/lib/content-security-policy.ts:17` requires HTTPS `IMAGE_BASE_URL` in production; `apps/web/src/lib/content-security-policy.ts:21` rejects credentials/query/hash in image base URLs; `apps/web/src/lib/content-security-policy.ts:111` sets production CSP with self/nonce script policy, frame/base/form/object restrictions; `apps/web/next.config.ts:75` sets global `nosniff`, frame, referrer, permissions policy, and production HSTS; `apps/web/src/lib/safe-json-ld.ts:14` escapes JSON-LD script-breaking characters. | Secret-pattern scan matched documented placeholders and historical redacted review text only; no live tracked credential was found. |

## Validation Evidence

- `npm run lint:api-auth --workspace=apps/web` passed: both admin API routes OK.
- `npm run lint:action-origin --workspace=apps/web` passed: all mutating server actions enforce same-origin provenance or carry approved exemptions.
- `npm run lint:public-route-rate-limit --workspace=apps/web` passed: public expensive/mutating API routes use rate-limit helpers or approved exemptions.
- `npm --workspace=apps/web exec vitest run src/__tests__/check-api-auth.test.ts src/__tests__/check-action-origin.test.ts src/__tests__/check-public-route-rate-limit.test.ts src/__tests__/session.test.ts src/__tests__/session-verify.test.ts src/__tests__/admin-tokens.test.ts src/__tests__/api-auth-response-headers.test.ts src/__tests__/backup-download-route.test.ts src/__tests__/sql-restore-scan.test.ts src/__tests__/db-restore.test.ts src/__tests__/upload-paths.test.ts src/__tests__/serve-upload.test.ts src/__tests__/search-route-privacy.test.ts src/__tests__/semantic-search-rate-limit.test.ts src/__tests__/og-rate-limit.test.ts` passed: 15 files, 340 tests.
- `npm audit --workspace=apps/web --audit-level=high` passed: found 0 vulnerabilities.
- Secret-pattern `rg` scan matched placeholders in README/CLAUDE/env examples, redacted historical review notes, and test fixtures only.
- `git status --short` was clean before writing this review artifact.

Not run: full lint, typecheck, build, full Vitest, e2e, or deploy. This review lane was read-only except for this artifact, and the targeted security gates covered the reviewed trust boundaries.

## Findings

None.

## Deferred Items Not Re-Raised

- `C80-06` remains a site-config runtime/build-time contract decision; no new SSRF/secrets evidence changes its severity.
- `C77-ARCH-01` remains deferred restore-maintenance foreground-mutation barrier work; current restore/upload/backfill locks and maintenance gates did not regress, and no exit criterion was hit.
- `C76-04`, `C76-05`, `C75-08`, and older performance/browser-matrix deferred items remain outside this security lane unless their recorded exit criteria are hit.
