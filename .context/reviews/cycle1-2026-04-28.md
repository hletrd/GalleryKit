# Deep Code Review — Cycle 1 (2026-04-28)

**HEAD:** `c73dc56 docs(reviews): record cycle-3 fresh review and plan-316 no-op convergence`
**Scope:** Full codebase deep review across all specialist angles
**Prior cycles:** Cycles 1-5 (fresh passes) + cycles 1-3 (current loop). Last 3 cycles found zero new code findings.

## Specialist Angles Covered (Inline Pass)

Code quality, performance, security (OWASP), architecture, testing, debugging, documentation, UI/UX, tracing, verification, and critique.

## Deduplicated Findings

Findings deduplicated against all prior cycles. Only genuinely new findings are listed.

### HIGH Severity (0)

None.

### MEDIUM Severity (0)

No new medium-severity findings. The codebase is well-hardened after 5+ fresh review cycles.

### LOW Severity (1)

| ID | Finding | File | Angles | Confidence |
|---|---|---|---|---|
| C1-28-F01 | `deleteAdminUser` uses raw SQL queries instead of Drizzle ORM. The `admin_users` and `sessions` table operations use `conn.query()` with string interpolation-safe parameterized queries, while every other action in the repo uses Drizzle ORM. This is intentional (advisory lock requires a dedicated pool connection), but it means the delete-admin path bypasses the compile-time type safety and column-validation that Drizzle provides for all other tables. The code is correct but represents a minor inconsistency in the ORM usage pattern. | `apps/web/src/app/actions/admin-users.ts:218-240` | Code, Architect | Low |

### INFO (0)

No new info findings.

## Verified Controls (No New Issues Found)

All controls verified in cycles 1-5 remain intact. Re-verified this cycle:

1. Argon2id + timing-safe comparison for auth
2. Path traversal prevention (SAFE_SEGMENT + realpath containment + symlink rejection)
3. Privacy guard (compile-time `_SensitiveKeysInPublic`/`_LargePayloadKeysInPublic`)
4. Blur data URL contract (3-point validation)
5. Rate limit TOCTOU fix (pre-increment pattern across all surfaces)
6. Advisory locks for concurrent operations
7. Unicode bidi/formatting rejection (consolidated via `UNICODE_FORMAT_CHARS`)
8. CSV formula injection prevention
9. Touch-target audit fixture passes
10. `requireSameOriginAdmin()` on every mutating server action
11. `withAdminAuth(...)` on every admin API route
12. Upload tracker TOCTOU closed with pre-claim pattern
13. View count buffer swap pattern intact
14. CSP GA domain conditional
15. SQL restore scanner blocks dangerous patterns
16. View-count flush exponential backoff
17. `stripControlChars` before validation on all admin input surfaces
18. `containsUnicodeFormatting` rejection on all admin-controlled string fields
19. Upload processing contract lock prevents config changes during uploads
20. `image_sizes` locked when images exist
21. GPS coordinate stripping configurable per-upload

## Summary

Production code remains converged. Zero new high/medium findings. One low-severity architectural consistency note (raw SQL in deleteAdminUser) that does not represent a bug or security issue. The codebase has been thoroughly hardened across multiple review cycles.
